---
phase: 63-business-dashboardin-preview-n-kymien-uudistus
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/business/analyze-website/route.ts
  - app/business/onboarding/page.tsx
autonomous: false
gap_closure: true
requirements: [BIZPANEL-07]
must_haves:
  truths:
    - "Clicking 'Analysoi →' in the onboarding wizard's step 1 actually starts a new website analysis (a POST to /api/business/analyze-website fires), not just a client-side phase change"
    - "An analysis that cannot complete within a bounded time window is written back as status='failed' with a descriptive error — no venue can be stuck at status='analyzing' forever"
    - "The waiting screen shows a distinguishable failure/timeout state with an explicit retry option, instead of silently dropping the user back to the same non-functional button after 60 seconds"
  artifacts:
    - "app/api/business/analyze-website/route.ts — runAnalysisPipeline extraction, Promise.race max-duration guard, Promise.allSettled upload parallelization, GET-side staleness self-heal"
    - "app/business/onboarding/page.tsx — handleRunAnalysis POST wiring, WaitingForAI failed/retry UI, reconciled MAX_POLLS"
  key_links:
    - "WizardInner's onRunAnalysis prop (app/business/WizardInner.tsx line 401-409, unchanged) is supplied by OnboardingWizardPage's new handleRunAnalysis, which POSTs to /api/business/analyze-website exactly like handleNimiUrlNext's existing AI-trigger block"
    - "WaitingForAI's poll loop reads GET /api/business/analyze-website's status field, which the route's new staleness self-heal now flips analyzing→failed after 12s instead of leaving it stuck, so failure is surfaced within the client's poll window"
---

<objective>
Close the blocker UAT gap (63-UAT.md Test 1, severity blocker): editing a draft venue and re-triggering brand-color analysis from the wizard's "Analysoi →" button never finishes — the button never actually starts an analysis (a pre-existing bug from Phase 61/WizardInner), and even when analysis IS triggered elsewhere, the sequential scrape→screenshot→Claude→upload pipeline routinely exceeds Vercel's documented ~10s `waitUntil` budget, silently killing the process mid-flight and leaving `business_branding.status` stuck at `'analyzing'` forever with no error and no way to retry.

Purpose: BIZPANEL-07's "edit/continue" dashboard action must lead somewhere usable — a business owner clicking through to re-analyze a venue's branding needs the flow to either succeed or fail visibly within a reasonable time, never hang indefinitely. Both compounding bugs are pre-existing (not introduced by Phase 63), but they block real usage of the Phase 63 dashboard's edit path and were surfaced by this phase's UAT.
Output: A hardened `/api/business/analyze-website` pipeline (parallel uploads, internal timeout guard, self-healing GET) and a fixed onboarding wizard client (button actually triggers analysis, failure is visible with a retry option).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server pipeline hardening — parallel uploads, max-duration guard, GET staleness self-heal</name>
  <files>app/api/business/analyze-website/route.ts</files>
  <read_first>
    - app/api/business/analyze-website/route.ts (full file — `runAnalysis` lines 26-163, sequential logo-candidate loop lines 55-65, sequential gallery-upload loop lines 71-92, catch-block failed-status upsert lines 147-162, GET handler lines 248-286)
    - lib/branding/storage.ts (uploadLogoCandidate/uploadGalleryImage signatures — `index` parameter is only used to build the deterministic storage path, e.g. `logo-{index}.png` / `gallery/{index}.png`)
    - .planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-UAT.md (Gap 3 under `## Gaps` — full root_cause and both missing items; note the file's own comment at lines 20-25 confirming Vercel Hobby's ~10s waitUntil kill is a known, accepted pre-existing constraint)
  </read_first>
  <action>
    Extract the current body of `runAnalysis` (all six numbered pipeline steps: scrape, optional screenshot, Claude analysis, logo upload, gallery upload, final UPSERT) into a new internal async function `runAnalysisPipeline(url: string, businessAccountId: string, paikkaId: number): Promise<void>` with identical logic, comments, and the final UPSERT unchanged — the only structural difference is that this extracted function has no try/catch of its own; let errors propagate to the caller.

    Add two module-level constants near the top of the file (after the `MAX_GALLERY_UPLOADS` constant): `MAX_ANALYSIS_DURATION_MS = 8000` (an internal soft deadline chosen to fire just before Vercel Hobby's documented ~10s `waitUntil` kill, so the pipeline can write a controlled `'failed'` status instead of being silently terminated mid-write) and `STALE_ANALYZING_MS = 12000` (used by the GET handler below as a backstop staleness threshold for rows the internal guard never got to write).

    Rewrite `runAnalysis` as a thin wrapper: inside its existing `try` block, `await Promise.race([...])` racing `runAnalysisPipeline(url, businessAccountId, paikkaId)` against a `new Promise` that `reject`s with an `Error` (message: something like `'Analyysi aikakatkaistiin'`) after `MAX_ANALYSIS_DURATION_MS` via `setTimeout`. Keep the existing `catch` block's failed-status UPSERT exactly as-is (already writes `status: 'failed'`, `error_message` derived from the caught error, scoped by `business_account_id`/`paikka_id`) — it now also fires when the timeout branch wins the race, giving a controlled, descriptive failure instead of a silent kill with no write at all.

    Inside `runAnalysisPipeline`, replace the sequential `for (const logo of result.logos)` loop (currently calling `uploadLogoCandidate` one at a time) with a concurrent `Promise.allSettled` over `result.logos.filter(logo => logo.index >= 0 && logo.index < logoBuffers.length).map(...)`, where each mapped entry awaits `uploadLogoCandidate(businessAccountId, paikkaId, logoBuffers[logo.index], logo.index)` and resolves to `{ url, type: logo.type }`. After settling, iterate the results in order, pushing `fulfilled` values into `logoCandidates` and `console.error`-logging `rejected` reasons with the same `[analyze-website] logo candidate upload error:` prefix the current catch uses — final `logoCandidates` shape and input-array ordering stays identical to today's sequential behavior.

    Replace the sequential `for (const imageUrl of imageUrls.slice(0, MAX_GALLERY_UPLOADS))` loop with a concurrent `Promise.allSettled` over the same sliced array via `.map(async (imageUrl, idx) => {...})`, where `idx` (the array position within the slice) replaces the current mutable `galleryIndex` counter as the third argument to `uploadGalleryImage` — this removes the shared-counter race condition concurrent execution would otherwise introduce, and keeps each upload's storage path stable and collision-free. Keep the existing `isUrlSafe` check, `fetchWithSsrfGuard` fetch, and `sharp` resize-and-convert steps identical inside the callback, but `throw` (not silently `continue`) on an unsafe URL or a failed fetch so `Promise.allSettled` correctly classifies it as rejected. After settling, push `fulfilled` values into `galleryUrls` and log `rejected` reasons with the existing `[analyze-website] gallery image upload error:` prefix.

    In the `GET` handler, add `updated_at` to the existing `.select(...)` column list. After fetching `data` and before the existing SEC-46-02 `logo_url` origin check, add a staleness self-heal: when `data?.status === 'analyzing'` and `Date.now() - new Date(data.updated_at).getTime() > STALE_ANALYZING_MS`, issue a `supabaseAdmin.from('business_branding').update(...)` setting `status: 'failed'`, `error_message: 'Analyysi aikakatkaistiin'`, `updated_at: new Date().toISOString()`, scoped by `.eq('business_account_id', user.id).eq('paikka_id', paikkaId).eq('status', 'analyzing')` (the trailing status filter is a lightweight best-effort guard against clobbering a write that completed in the same instant — not a strict concurrency-critical operation, since only a status/error_message field is at stake), then mutate the in-memory `data.status`/`data.error_message` so this same GET response already reflects the healed state. Before the final `return NextResponse.json(...)`, delete `data.updated_at` from the response object — it was only fetched for this server-side check and is not part of the client-facing `BrandingResult` shape.
  </action>
  <acceptance_criteria>
    - `grep -c "Promise.allSettled" app/api/business/analyze-website/route.ts` returns 2 (logo candidates + gallery images)
    - `grep -c "Promise.race" app/api/business/analyze-website/route.ts` returns 1
    - `grep -c "STALE_ANALYZING_MS" app/api/business/analyze-website/route.ts` returns at least 2 (declaration + usage)
    - `grep -c "MAX_ANALYSIS_DURATION_MS" app/api/business/analyze-website/route.ts` returns at least 2 (declaration + usage)
    - `grep -c "runAnalysisPipeline" app/api/business/analyze-website/route.ts` returns at least 2 (declaration + call site)
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Logo-candidate and gallery-image uploads run concurrently; runAnalysis writes a controlled 'failed' status if the pipeline exceeds 8s instead of being silently killed; GET self-heals any 'analyzing' row older than 12s back to 'failed' so no venue can be stuck in 'analyzing' forever.</done>
</task>

<task type="auto">
  <name>Task 2: Client wiring fix — button actually triggers analysis + visible failure/retry state</name>
  <files>app/business/onboarding/page.tsx</files>
  <read_first>
    - app/business/onboarding/page.tsx (full file — `WaitingForAI` lines 127-196, `handleNimiUrlNext`'s AI-trigger fetch block lines 278-307 as the exact fetch shape to mirror, `OnboardingWizardPage`'s `websiteUrl`/`paikkaId`/`aiTriggered` state lines 199-213, `WizardInner` render call passing `onRunAnalysis` line 369, `WaitingForAI` render call site lines 340-346)
    - app/business/WizardInner.tsx (lines 401-409 — confirms `onRunAnalysis` is only invoked from the step-1 "Analysoi →" button, gated by `canRunAnalysis`; no change needed in this file)
    - .planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-UAT.md (Gap 3 under `## Gaps` — both missing items)
  </read_first>
  <action>
    Add a new async handler `handleRunAnalysis` in `OnboardingWizardPage` (near `handleNimiUrlNext`) that: returns early if `!websiteUrl || paikkaId === null` (defensive — mirrors `canRunAnalysis`'s own gate, not reachable in practice since the button that calls this is only rendered when that gate is true); immediately calls `setPagePhase('waiting')`; then fetches the current session via `createBusinessBrowserClient().auth.getSession()` and `POST`s to `/api/business/analyze-website` with header `Authorization: Bearer <token>` and JSON body `{ url: websiteUrl, paikka_id: paikkaId }` — the exact same fetch shape `handleNimiUrlNext`'s AI-trigger block already uses (lines 289-293), just invoked from the wizard's manual retry button instead of the step-0 auto-trigger path. Replace the `onRunAnalysis={() => setPagePhase('waiting')}` prop passed to `WizardInner` (line 369) with `onRunAnalysis={handleRunAnalysis}` — this fixes UAT gap 3's first missing item: today's handler only flips the UI phase and never starts an analysis, so `WaitingForAI` polls a resource that was never triggered.

    Give `WaitingForAI` a new `onRetry: () => void` prop, and two new pieces of local state: `failed` (boolean, default `false`) and `attempt` (number, default `0`). Change the polling `useEffect`'s dependency array from `[]` to `[attempt]`, and reset `failed` to `false` at the top of the effect (before `run()` is invoked) so a retry re-enters the spinner view. Inside `run()`, replace both existing `onSkip()` calls — the one inside the `data.status === 'failed'` branch and the one after the `while` loop exits from exhausting `MAX_POLLS` — with `setFailed(true)`; `onSkip` (the prop) must now be reserved only for the user's explicit "give up" click, never fired automatically. Add a `handleRetryClick` function that calls `onRetry()` then `setAttempt(a => a + 1)`. Reduce the local `MAX_POLLS` constant from `30` to `20` (a 40-second worst-case client-side ceiling — the route's new 12-second staleness self-heal, added in Task 1, now resolves most stuck runs well before this), keeping the existing `2000`ms poll interval unchanged, and add a one-line comment explaining why. Add a conditional render branch: when `failed` is `true`, render a distinct message ("Analyysi epäonnistui tai kesti liian kauan." — kept as hardcoded Finnish text, consistent with this component's existing hardcoded strings, which predate this project's next-intl rollout and are out of scope to convert here) with two buttons — "Yritä uudelleen" calling `handleRetryClick`, and "Ohita" calling the existing `onSkip` prop — instead of the current spinner view. Keep the existing spinner / "Analysoidaan sivustoasi..." / "Ohita" view exactly as today's `!failed` branch.

    Update the `<WaitingForAI ... />` render call site (lines 340-346) to pass `onRetry={handleRunAnalysis}` alongside the existing `paikkaId`/`onReady`/`onSkip` props.
  </action>
  <acceptance_criteria>
    - `grep -c "async function handleRunAnalysis" app/business/onboarding/page.tsx` returns 1
    - `grep -c "onRunAnalysis={handleRunAnalysis}" app/business/onboarding/page.tsx` returns 1
    - `grep -c "onRetry" app/business/onboarding/page.tsx` returns at least 3 (prop type + destructure + usage at the WaitingForAI call site)
    - `grep -c "MAX_POLLS = 20" app/business/onboarding/page.tsx` returns 1
    - `npx tsc --noEmit` reports no errors
    - `npm test` passes
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Clicking "Analysoi →" in the wizard actually starts a new analysis (POST fires); a failed/timed-out analysis shows a distinguishable failure state with an explicit retry button instead of silently reverting to the same button with no feedback.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the analysis flow now completes or fails visibly</name>
  <action>Pause for human verification. Present the three verification steps below, wait for the operator to manually exercise the wizard's "Analysoi →" retry flow on a live draft venue, and only resume on the "approved" signal. Do not auto-approve — this checkpoint confirms the blocker UAT gap (analysis hanging forever) is actually closed, not just type-checked.</action>
  <what-built>End-to-end fix for the stuck-analysis bug: the wizard's "Analysoi →" button now actually triggers analysis, the server pipeline writes a controlled failure instead of hanging forever, and the waiting screen offers a clear retry on failure.</what-built>
  <how-to-verify>
    1. Open `/business/onboarding` for a draft venue, reach wizard step 1, and click "Analysoi →" — confirm the page moves to the waiting/spinner screen AND (open the browser Network tab, or just wait) confirm a POST to `/api/business/analyze-website` actually fires this time.
    2. Let it run — confirm it either reaches the branding-pick step normally (fast site) or, within roughly 15-40 seconds, shows the new "Analyysi epäonnistui tai kesti liian kauan." failure screen with "Yritä uudelleen" and "Ohita" buttons — you should never be left waiting silently past that point with no feedback.
    3. Click "Yritä uudelleen" on the failure screen — confirm it restarts the analysis (spinner reappears, a new POST fires) rather than reusing the stuck state.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated business user → analyze-website Route Handler | Unchanged — POST/GET both still verify the JWT via `supabaseAdmin.auth.getUser(token)` before any read/write (T-45-04-01/T-45-04-04, unchanged by this plan) |
| GET handler now performs a write | Previously read-only; the new staleness self-heal issues an `UPDATE` on `business_branding` from within `GET` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-07-01 | Tampering (unauthorized row write from GET) | GET /api/business/analyze-website staleness self-heal | medium | mitigate | The self-heal `UPDATE` is scoped by `.eq('business_account_id', user.id).eq('paikka_id', paikkaId).eq('status', 'analyzing')` — identical ownership scoping (`business_account_id = user.id`, the JWT-verified caller) already enforced by every other query in this route; it can only ever flip the caller's own row, and only while it is still `'analyzing'`. |
| T-63-07-02 | Denial of Service (repeated retry-triggered re-analysis cost) | Client-side "Yritä uudelleen" retry button | low | accept | Retrying re-invokes the same POST path already protected by the existing ownership check (T-47-11) and business-account check (CR-04); a business owner can already re-trigger analysis today via the pre-existing step-0 auto-trigger and any future "Analysoi uudelleen" affordance — no new abuse surface, cost is bounded by the same Claude/scrape pipeline cost as before. |
| T-63-07-03 | Integrity (concurrent upload path collisions) | Parallelized logo-candidate/gallery-image uploads | low | mitigate | Each concurrent upload writes to a deterministic path keyed by its own stable array-position index (`logo.index` / slice `idx`), never a shared mutable counter — eliminates the race condition that a naive parallelization would introduce. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean after both tasks
- `npm test` full suite green (no regressions to existing onboarding/branding tests)
- Manual: 63-UAT.md Test 1's blocker complaint (analysis never finishes when editing a draft) is resolved per the checkpoint task
</verification>

<success_criteria>
- The "Analysoi →" button reliably starts a real analysis attempt.
- No `business_branding` row can remain stuck at `status='analyzing'` indefinitely — it always resolves to `'analyzed'` or `'failed'` within a bounded time.
- The user always gets a visible, actionable failure state (retry or skip) instead of a silent 60-second dead end.
- No regression to the existing step-0 auto-trigger AI-analysis path (`handleNimiUrlNext`) or to any already-passing analyze-website behavior.
</success_criteria>

<output>
Create `.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-07-SUMMARY.md` when done
</output>
