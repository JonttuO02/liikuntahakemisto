---
phase: 63-business-dashboardin-preview-n-kymien-uudistus
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/DiagonaalKortti.tsx
  - app/business/page.tsx
autonomous: false
gap_closure: true
requirements: [BIZPANEL-06, BIZPANEL-07]
must_haves:
  truths:
    - "The dashboard card's controls panel is always visibly two-sided (split layout) even when brandColor is unset, using the same neutral gray as the no-photo image placeholder — never the near-invisible .glass fallback"
    - "Copying the invite link gives a sighted mouse user a transient visible confirmation (icon swap), and only reports success when navigator.clipboard.writeText actually resolved"
    - "On desktop/wide viewports, /business's venue cards keep a fixed width and lay out in a responsive multi-column grid instead of stretching full-width"
  artifacts:
    - "app/components/DiagonaalKortti.tsx — neutral-gray panel fallback, Check icon swap on copied state, min-w-0 on the card root"
    - "app/business/page.tsx — max-w-5xl mx-auto wrapper, responsive grid venue-list wrapper, awaited/caught clipboard write"
  key_links:
    - "DashboardVenueCard's handleCopyInviteLink (app/business/page.tsx) awaits navigator.clipboard.writeText before setting copied=true, which flows into DiagonaalKortti's dashboardActions.copied prop and drives the Check-icon swap"
    - "app/business/page.tsx's venue-list wrapper grid classes rely on DiagonaalKortti's card root having min-w-0 to avoid CSS grid blowout from long venue names or price content"
---

<objective>
Close three UAT gaps (63-UAT.md Test 1, severities cosmetic/minor/minor) found while testing Phase 63's DiagonaalKortti dashboard cards on `/business`: (1) the controls panel collapses into an undifferentiated single panel when `brandColor` is unset because it falls back to the `.glass` class instead of a neutral gray; (2) the "copy invite link" button gives no visible confirmation to a sighted user (only the aria-label changes); (3) dashboard cards stretch full-width with no desktop grid, making the page unusable on wide viewports.

Purpose: Complete BIZPANEL-06 (status pill + controls-panel shade) and BIZPANEL-07 (permanent icon-button controls) for real-world usability across brand-color states and viewport widths — these are cosmetic/minor polish gaps on top of Plan 04/05's already-shipped dashboard card work, not new features.
Output: Updated `DiagonaalKortti.tsx` (neutral-gray fallback, Check-icon copy confirmation, grid-safe min-width) and `app/business/page.tsx` (responsive grid layout, awaited clipboard write).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Neutral-gray controls-panel fallback + copy-confirmation icon + grid-safe min-width (DiagonaalKortti.tsx)</name>
  <files>app/components/DiagonaalKortti.tsx</files>
  <read_first>
    - app/components/DiagonaalKortti.tsx (lucide-react import line 5; panelShade/panelShadeContrastText/panelChipBg derivation lines 72-78; RIGHT panel dashboardActions branch lines 239-243 and the copy-invite-link button lines 264-274; no-photo consumer placeholder lines 307-313 for the exact neutral-gray class to reuse; card root motion.div lines 104-109)
    - .planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-UAT.md (Gap 1 and Gap 2 under `## Gaps` — root_cause and missing items)
  </read_first>
  <action>
    Gap 1 (cosmetic, controls-panel fallback): In the RIGHT panel's dashboardActions branch (the wrapper rendering `panelShade ? '' : ' glass'` on its className, around line 241, with `style={panelShade ? { backgroundColor: panelShade } : undefined}`), replace the `.glass` fallback with the same neutral gray already used by the no-photo consumer placeholder at line 308 (`bg-[rgba(0,0,0,0.06)]`) — i.e. when `panelShade` is falsy, the wrapper's className should include `bg-[rgba(0,0,0,0.06)]` instead of `glass`. Do not touch the `panelShade`-set branch (brandColor present) — unaffected. Do not recompute `panelShadeContrastText`/`panelChipBg` — the 4 icon buttons already use the `glass-btn text-[rgba(17,17,17,0.5)] hover:text-[#111111]` classes (not inline color) whenever `panelShade` is falsy (lines ~250, ~259, ~269, ~280), and that dark-icon-on-light-background styling is already legible against this new flat light-gray background, matching how the same classes read against the identical `rgba(0,0,0,0.06)` tone used elsewhere in this file (e.g. the logo slot background at line 122).

    Gap 2 (minor, copy-link confirmation): Add `Check` to the existing `lucide-react` import on line 5 (alongside `MapPin, BookmarkCheck, Bookmark, Building2, Camera, BadgeCheck, Eye, Pencil, Link2, AlertCircle`). In the copy-invite-link button's icon (currently a bare `<Link2 className="w-3.5 h-3.5" />` inside the `dashboardActions.onCopyInviteLink` conditional block, lines 264-274), render `<Check className="w-3.5 h-3.5" />` when `dashboardActions.copied` is true, otherwise keep `<Link2 className="w-3.5 h-3.5" />` — the aria-label already toggles between `inviteLinkCopied`/`copyInviteLinkCta` correctly; only the icon glyph needs to follow `copied` too. No new i18n key needed.

    Gap 4 (minor, desktop grid — part 1 of 2, see Task 2 for the page.tsx side): Add `min-w-0` to the card's root `motion.div` className (currently `` `relative glass glass-hover rounded-2xl h-36${onOpen ? ' cursor-pointer' : ''}` `` on line 106) so the card cannot force its future CSS Grid track wider than intended once `app/business/page.tsx`'s venue-list wrapper becomes a responsive grid — prevents grid blowout from long venue names or unclamped price content inside the card.
  </action>
  <acceptance_criteria>
    - `grep -c "bg-\[rgba(0,0,0,0.06)\]" app/components/DiagonaalKortti.tsx` returns 3 (existing logo-slot background + existing no-photo placeholder + new controls-panel fallback)
    - `grep -c "<Check " app/components/DiagonaalKortti.tsx` returns 1
    - `grep -c "min-w-0" app/components/DiagonaalKortti.tsx` returns 3 (2 existing truncation containers + new card-root usage)
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>DiagonaalKortti's dashboardActions RIGHT panel always renders a visibly two-sided split (neutral gray when brandColor is unset, panelShade when set); the copy-invite-link button shows a Check icon while dashboardActions.copied is true; the card root has min-w-0.</done>
</task>

<task type="auto">
  <name>Task 2: Responsive desktop grid layout + awaited clipboard write (app/business/page.tsx)</name>
  <files>app/business/page.tsx</files>
  <read_first>
    - app/business/page.tsx (DashboardVenueCard's handleCopyInviteLink, lines 100-105; `<main>` element and its children, lines 219-304; venue-list wrapper div, line 237)
    - app/loading.tsx (the existing `max-w-5xl mx-auto` + `grid gap-4 sm:grid-cols-2 lg:grid-cols-3` pattern to mirror, lines 5, 11-12)
    - .planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-UAT.md (Gap 2 and Gap 4 under `## Gaps`)
  </read_first>
  <action>
    Gap 4 (minor, desktop grid — part 2 of 2): Wrap all of `<main>`'s existing children (StatusCard, the pending-access-request banner, the venue-list section, the add-venue section, the quick-actions section, the PreviewModal AnimatePresence block, and RejectionReasonPopup — everything currently between the `<main className="min-h-screen bg-white pt-16 px-4 pb-24">` opening tag on line 219 and `</main>` on line 304) in a single new `<div className="max-w-5xl mx-auto">`, mirroring `app/loading.tsx`'s `max-w-5xl mx-auto` pattern. This is safe for PreviewModal/RejectionReasonPopup because both use `fixed inset-0` positioning internally and are unaffected by an ancestor's max-width. Convert the venue-list wrapper (line 237, currently `<div className="flex flex-col gap-3">`) to a responsive grid: `<div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">`, mirroring `app/loading.tsx`'s skeleton grid breakpoints (keep `gap-3`, matching this list's existing gap value, not loading.tsx's `gap-4`).

    Gap 2 (minor, copy-link confirmation — second half, see Task 1 for the icon side): In `DashboardVenueCard`'s `handleCopyInviteLink` (lines 100-105), make the function `async`, wrap the `navigator.clipboard.writeText(url)` call in a `try`/`await`, and only call `setCopied(true)` plus start the existing 2-second `setTimeout(() => setCopied(false), 2000)` inside that `try` block on success. Add a `catch` that does nothing (no `copied` state change) so a failed clipboard write never shows a false-positive confirmation.
  </action>
  <acceptance_criteria>
    - `grep -c "max-w-5xl mx-auto" app/business/page.tsx` returns 1
    - `grep -c "grid gap-3 sm:grid-cols-2 lg:grid-cols-3" app/business/page.tsx` returns 1
    - `grep -c "async function handleCopyInviteLink" app/business/page.tsx` returns 1
    - `npx tsc --noEmit` reports no errors
    - `npm test` passes (full regression — no test currently targets this file's rendering, this confirms no unrelated breakage)
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>/business's content is capped at max-w-5xl and centered; the venue list renders as a responsive 1/2/3-column grid depending on viewport; clipboard-copy confirmation only fires after navigator.clipboard.writeText genuinely resolves.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify dashboard card visual and grid fixes</name>
  <action>Pause for human verification. Present the three verification steps below, wait for the operator to manually exercise the updated `/business` dashboard, and only resume on the "approved" signal. Do not auto-approve.</action>
  <what-built>Neutral-gray controls-panel fallback, visible copy-link confirmation (Check icon), and a responsive desktop grid layout for /business dashboard cards.</what-built>
  <how-to-verify>
    1. Open `/business` on a venue whose brand background color is NOT set — confirm the card's right-side controls panel now shows a flat light-gray split, clearly distinguishable from the left info panel, instead of blending into the card's own background.
    2. Click the "Kopioi kutsulinkki" (copy invite link) icon button on an approved venue's card — confirm the icon swaps to a checkmark for about 2 seconds before reverting to the link icon.
    3. Resize the browser to a wide/desktop viewport (or open on a desktop screen) — confirm dashboard cards keep a fixed width and lay out in 2-3 columns instead of stretching edge-to-edge, and the whole page content is capped/centered rather than spanning the full window width.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side rendering/layout fix plus an added try/catch around an existing browser Clipboard API call. No new input crosses a trust boundary, no new network call, no persistence change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-06-01 | Repudiation (false success feedback) | DashboardVenueCard.handleCopyInviteLink | low | mitigate | await + try/catch ensures the `copied` confirmation UI only fires after `navigator.clipboard.writeText` genuinely resolves, not merely after it was called — closes the false-positive gap the UAT report identified. |
| T-63-06-02 | Tampering | DiagonaalKortti/page.tsx layout classes | low | accept | Pure Tailwind class changes with no data, auth, or persistence impact; no mitigation required. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean after both tasks
- `npm test` full suite green (no regressions to existing business dashboard tests)
- Manual: 63-UAT.md Test 1's three remaining complaints (panel fallback color, dead-looking copy button, desktop stretch) are resolved per the checkpoint task
</verification>

<success_criteria>
- Dashboard cards are always visibly two-sided regardless of brandColor.
- Copy-invite-link gives genuine, visible success feedback.
- /business is usable on desktop/wide viewports with a capped-width, multi-column grid.
- No regression to any existing DiagonaalKortti call site (Etusivu, PreviewModal, LivePreviewPane, admin) or to the dashboard's existing status-pill/icon-button behavior.
</success_criteria>

<output>
Create `.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-06-SUMMARY.md` when done
</output>
