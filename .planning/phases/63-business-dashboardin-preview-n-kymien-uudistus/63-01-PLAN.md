---
phase: 63-business-dashboardin-preview-n-kymien-uudistus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/branding/brandingResult.ts
  - lib/branding/brandingResult.test.ts
  - app/components/CalloutCard.tsx
autonomous: true
requirements: [BIZPANEL-07]
must_haves:
  truths:
    - "getPanelShade(brandColor) returns a hex color that is never equal to brandColor for any valid 6-digit hex input"
    - "CalloutCard renders its accent ring identically after darkenHex/lightenHex are imported from the shared module (no visual regression)"
  artifacts:
    - "lib/branding/brandingResult.ts exports getPanelShade, darkenHex, lightenHex"
    - "lib/branding/brandingResult.test.ts covers getPanelShade direction (light→darken, dark→lighten) and inequality"
  key_links:
    - "CalloutCard.tsx imports darkenHex/lightenHex from @/lib/branding/brandingResult instead of declaring them privately"
---

<objective>
Establish the shared color-derivation foundation for the dashboard `DiagonaalKortti` variant's controls-panel background (D-03). Extract the currently-private `darkenHex`/`lightenHex` helpers from `CalloutCard.tsx` into `lib/branding/brandingResult.ts`, add the new exported `getPanelShade()` helper composed from them plus the existing `getContrastColor()`, and cover it with unit tests. Repoint `CalloutCard.tsx` to import the extracted helpers so there is a single source of truth.

Purpose: D-03 requires the controls panel to be a "computed complementary shade of brandColor" that visibly differs from the left panel. Plan 04 consumes `getPanelShade()`; this plan makes it exist without duplicating color math (RESEARCH.md Don't Hand-Roll).
Output: `getPanelShade` + extracted `darkenHex`/`lightenHex` exports, tests, and a de-duplicated `CalloutCard.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-RESEARCH.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-PATTERNS.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract color helpers and add getPanelShade to brandingResult.ts (with tests)</name>
  <files>lib/branding/brandingResult.ts, lib/branding/brandingResult.test.ts</files>
  <read_first>
    - lib/branding/brandingResult.ts (the target file — read getContrastColor's YIQ implementation and doc-comment style, lines ~59-87)
    - app/components/CalloutCard.tsx (source of the private darkenHex/lightenHex helpers, lines ~34-56 — copy verbatim including the clamping/regex-validation logic)
    - lib/branding/brandingResult.test.ts (existing test file — match its describe/it structure and import style)
    - .planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-PATTERNS.md (exact getPanelShade implementation, lines ~145-153)
  </read_first>
  <behavior>
    - getPanelShade('#ffffff') → darkens (getContrastColor('#ffffff') === '#000000' → light branch) → result !== '#ffffff'
    - getPanelShade('#000000') → lightens (dark branch) → result !== '#000000'
    - getPanelShade(brandColor) !== brandColor for a representative set of light and dark hexes (e.g. '#4f46e5', '#e2e8f0', '#111111')
    - getPanelShade returns the input unchanged only when the input is NOT a valid 6-digit hex (darkenHex/lightenHex regex-guard returns input as-is) — assert malformed input like 'not-a-color' passes through
    - darkenHex('#808080', 0.3) and lightenHex('#808080', 0.3) both clamp channels within 0-255 and return valid `#rrggbb`
  </behavior>
  <action>
    Move the `darkenHex(hex, amount)` and `lightenHex(hex, amount)` functions from `CalloutCard.tsx` into `lib/branding/brandingResult.ts` as named `export function` declarations (they are currently private/unexported in CalloutCard). Copy the bodies verbatim — the regex `/^#?([0-9a-f]{6})$/i` guard, the bit-shift channel math, and the `Math.max(0, Math.min(255, c))` clamping — do not rewrite the math. Then add `export function getPanelShade(brandColor: string, amount = 0.3): string` directly below `getContrastColor`, implemented exactly per D-03 / 63-PATTERNS.md line 147-151: return `getContrastColor(brandColor) === '#000000' ? darkenHex(brandColor, amount) : lightenHex(brandColor, amount)`. `amount = 0.3` is the locked default. Give `getPanelShade` a short doc-comment in the same YIQ-style as `getContrastColor`. In `brandingResult.test.ts`, add a `describe('getPanelShade')` block asserting the behaviors listed above, importing `getPanelShade`, `darkenHex`, `lightenHex` from `./brandingResult`.
  </action>
  <acceptance_criteria>
    - `grep -c "export function getPanelShade" lib/branding/brandingResult.ts` returns 1
    - `grep -c "export function darkenHex" lib/branding/brandingResult.ts` returns 1 AND `grep -c "export function lightenHex" lib/branding/brandingResult.ts` returns 1
    - `npx vitest run lib/branding/brandingResult.test.ts` passes with the new getPanelShade cases green
    - getPanelShade result is asserted `!==` its input for at least one light and one dark brandColor
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run lib/branding/brandingResult.test.ts</automated>
  </verify>
  <done>getPanelShade, darkenHex, lightenHex are exported from brandingResult.ts and the new unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint CalloutCard.tsx to the shared color helpers</name>
  <files>app/components/CalloutCard.tsx</files>
  <read_first>
    - app/components/CalloutCard.tsx (remove the now-duplicated private darkenHex/lightenHex, add the import; confirm no other private caller relies on a different signature)
    - lib/branding/brandingResult.ts (confirm the exported signatures match the removed private ones)
  </read_first>
  <action>
    Delete the private `darkenHex`/`lightenHex` function declarations from `CalloutCard.tsx` (the copies moved to brandingResult.ts in Task 1). Add them to the existing `import { getContrastColor } from '@/lib/branding/brandingResult'` line so it becomes `import { getContrastColor, darkenHex, lightenHex } from '@/lib/branding/brandingResult'`. All existing call sites inside CalloutCard (the accent-ring conic-gradient) keep calling `darkenHex(...)`/`lightenHex(...)` unchanged — only their definition source moves. Do not alter the conic-gradient logic or any visual output.
  </action>
  <acceptance_criteria>
    - `grep -c "function darkenHex" app/components/CalloutCard.tsx` returns 0 AND `grep -c "function lightenHex" app/components/CalloutCard.tsx` returns 0 (no private copies remain)
    - `grep -c "darkenHex" app/components/CalloutCard.tsx` is >= 1 (still called, now imported)
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>CalloutCard imports the two helpers from the shared module, no private duplicates remain, and the project type-checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan is a pure client-side/utility refactor. No new data crosses any trust boundary; `getPanelShade` is a deterministic function of a color string. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-01A | Tampering | getPanelShade input (untrusted brandColor from DB) | low | mitigate | darkenHex/lightenHex regex-guard non-hex input and return it unchanged; no `eval`/DOM/style-injection sink — the result is only ever assigned to a React `style.backgroundColor`, which React escapes |
</threat_model>

<verification>
- `npx vitest run lib/branding/brandingResult.test.ts` green
- `npx tsc --noEmit` clean
- Manual: no visual change to any existing `CalloutCard` render (accent ring identical) — spot-check one venue in `/business` preview after Plan 02/04 land
</verification>

<success_criteria>
- getPanelShade exported and unit-tested; darkenHex/lightenHex exported from the single shared module; CalloutCard de-duplicated and type-checking.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 01)
- New export: `getPanelShade(brandColor: string, amount?: number): string` in `lib/branding/brandingResult.ts`
- Promoted exports: `darkenHex(hex, amount)`, `lightenHex(hex, amount)` in `lib/branding/brandingResult.ts` (moved from CalloutCard private scope)
- New tests: `describe('getPanelShade')` in `lib/branding/brandingResult.test.ts`
</artifacts>

<output>
Create `.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-01-SUMMARY.md` when done
</output>
