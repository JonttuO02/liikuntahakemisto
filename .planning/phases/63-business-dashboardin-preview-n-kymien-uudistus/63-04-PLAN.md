---
phase: 63-business-dashboardin-preview-n-kymien-uudistus
plan: 04
type: execute
wave: 2
depends_on: ["63-01"]
files_modified:
  - messages/fi.json
  - messages/en.json
  - app/components/DiagonaalKortti.tsx
  - app/components/RejectionReasonPopup.tsx
autonomous: true
requirements: [BIZPANEL-06, BIZPANEL-07]
must_haves:
  truths:
    - "DiagonaalKortti's right panel renders a permanent no-photo controls panel (never revealed by hover/tap) whenever the dashboardActions prop is present, and renders the unchanged real-photo panel whenever it is absent"
    - "The controls panel's background is getPanelShade(brandColor) when brandColor is set, and the plain .glass fallback when it is not (D-04)"
    - "The dashboard variant's left info panel has zero click handlers and no click-catcher div — clicking it does nothing (D-10)"
    - "A rejected-status card exposes a rejection-info icon button that opens RejectionReasonPopup, whose CTA navigates (never fetches) to the edit flow"
  artifacts:
    - "app/components/DiagonaalKortti.tsx exports a dashboardActions?: {...} prop accepted by DiagonaalKorttiProps"
    - "app/components/RejectionReasonPopup.tsx is a new component with props { open, onClose, rejectionReason, editHref }"
    - "messages/fi.json and messages/en.json contain Business.fixDetailsCta and Business.showRejectionInfoLabel"
  key_links:
    - "DiagonaalKortti imports getPanelShade from @/lib/branding/brandingResult (Plan 01's export)"
    - "Every existing call site of DiagonaalKortti (Etusivu.tsx, PreviewModal.tsx, LivePreviewPane.tsx, admin/[id]/page.tsx) passes no dashboardActions prop and is therefore visually/behaviorally unchanged"
---

<objective>
Build the dashboard-only `DiagonaalKortti` variant (D-01 through D-05, D-09, D-10) and the new `RejectionReasonPopup` component (D-06) that Plan 05 will wire into `/business`. This is the phase's single largest new visual surface: a `dashboardActions` optional-prop bundle that swaps `DiagonaalKortti`'s right (photo) panel for a permanent icon-button controls panel with a status pill, following the codebase's existing "prop presence toggles behavior" convention exactly (no new mode enum). The rejection-reason popup is a new small component based on `AuthModal`'s lightweight dialog pattern.

Purpose: BIZPANEL-06 (status pill) and BIZPANEL-07 (permanent icon-button controls, no text buttons) require new UI that does not yet exist anywhere in the codebase (RESEARCH.md Pitfall 4) — this plan builds it in isolation before Plan 05 wires it into the live dashboard.
Output: `DiagonaalKortti.tsx` with the new `dashboardActions` prop, `RejectionReasonPopup.tsx`, and the two new i18n keys the UI-SPEC's Copywriting Contract requires.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-CONTEXT.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-RESEARCH.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-PATTERNS.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two new i18n keys required by the Copywriting Contract</name>
  <files>messages/fi.json, messages/en.json</files>
  <read_first>
    - messages/fi.json (Business namespace — locate `reapplyCta`/`rejectionReasonLabel` keys as the insertion neighborhood, match exact 4-space JSON indentation)
    - messages/en.json (Business namespace — same keys, English strings)
    - .planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-UI-SPEC.md (Copywriting Contract table — exact copy strings and the explicit "no other new strings needed" constraint)
  </read_first>
  <action>
    Add two new keys to the `Business` namespace in both `messages/fi.json` and `messages/en.json`, matching the existing indentation/quoting style exactly. `fixDetailsCta`: fi `"Korjaa tiedot"`, en `"Fix details"` (the rejection popup's CTA — replaces `reapplyCta` for this specific flow since this CTA navigates, it does not resubmit, per D-07). `showRejectionInfoLabel`: fi `"Näytä hylkäyssyy"`, en `"Show rejection reason"` (aria-label for the dashboard card's rejection-info icon button). Do not add, rename, or remove any other key — the UI-SPEC's Copywriting Contract explicitly states these are the only two new strings needed; every other piece of copy in this phase reuses an existing key verbatim (`dashboardStatusRejectedTitle`, `dashboardStatusRejectedBody`, `dashboardStatusRejectedBodyNoReason`, `esikatseluCta`, `muokkaaCta`, `jatkaCta`, `copyInviteLinkCta`, `inviteLinkCopied`, `statusKesken`, `statusApproved`, `statusRejected`, `statusPending`, `previewClose`).
  </action>
  <acceptance_criteria>
    - `node -e "console.log(require('./messages/fi.json').Business.fixDetailsCta, require('./messages/fi.json').Business.showRejectionInfoLabel)"` prints two non-empty strings
    - `node -e "console.log(require('./messages/en.json').Business.fixDetailsCta, require('./messages/en.json').Business.showRejectionInfoLabel)"` prints two non-empty strings
    - Both JSON files remain valid: `node -e "require('./messages/fi.json'); require('./messages/en.json'); console.log('ok')"` prints `ok`
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('./messages/fi.json'); require('./messages/en.json'); console.log('ok')"</automated>
  </verify>
  <done>Business.fixDetailsCta and Business.showRejectionInfoLabel exist in both locale files with valid JSON.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the dashboard-only controls-panel variant to DiagonaalKortti (D-01–D-05, D-09, D-10, BIZPANEL-06/07)</name>
  <files>app/components/DiagonaalKortti.tsx</files>
  <read_first>
    - app/components/DiagonaalKortti.tsx (the full target file — current props interface lines 27-39, contrast-derivation lines 52-56, RIGHT panel photo block lines 210-240, click-catcher lines 245-255, bottom-corner buttons lines 257-275)
    - app/business/page.tsx (current `VenueRow`'s 4-state status pill color/label logic, lines 122-138 — copy verbatim, do not invent new colors, per RESEARCH.md Pitfall 4)
    - lib/branding/brandingResult.ts (confirm `getPanelShade` and `getContrastColor` exact exported signatures from Plan 01)
    - .planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-UI-SPEC.md (Component Contract: Dashboard DiagonaalKortti Variant — locked structure, icon choices, sizing, Computed Color Contract's chip-contrast formula)
    - .planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-PATTERNS.md (exact prop-bundle signature and RIGHT-panel-swap guidance, lines 45-113)
  </read_first>
  <action>
    Add `Eye, Pencil, Link2, AlertCircle` to the existing `lucide-react` import line, and add `getPanelShade` to the existing `import { getContrastColor } from '@/lib/branding/brandingResult'` line. Extend `DiagonaalKorttiProps` with `dashboardActions?: { status: 'kesken' | 'approved' | 'rejected' | 'pending'; onPreview: () => void; onEditOrContinue: () => void; onCopyInviteLink?: () => void; copied?: boolean; onShowRejectionInfo?: () => void }` — this exact shape is the locked prop contract from 63-UI-SPEC.md (resolves CONTEXT.md's prop-naming discretion). Destructure `dashboardActions` in the component's function signature.

    Compute, alongside the existing `contrastText`/`accentContrastText` derivations: `panelShade = dashboardActions && brandColor ? getPanelShade(brandColor) : undefined`; `panelShadeContrastText = panelShade ? getContrastColor(panelShade) : undefined`; `panelChipBg = panelShade ? (panelShadeContrastText === '#000000' ? 'rgba(0,0,0,0.10)' : 'rgba(255,255,255,0.20)') : undefined` — this is the exact Computed Color Contract formula from 63-UI-SPEC.md's "Icon-button chip contrast" subsection.

    In the RIGHT panel block (the `<div className="absolute top-0 right-0 bottom-0 overflow-hidden" style={{left:'50%', clipPath:'polygon(14% 0, 100% 0, 100% 100%, 4% 100%)'}}>` wrapper), keep that wrapping div and its clip-path unchanged (D-01 — same diagonal silhouette), but make its children conditional: when `dashboardActions` is present, render a controls-panel `<div>` filling the wrapper (`w-full h-full flex items-center justify-center`) with `style={panelShade ? { backgroundColor: panelShade } : undefined}` and className including `'glass'` only when `panelShade` is undefined (D-04 fallback). Inside it, render a horizontal icon-button row (`flex items-center gap-2`, matching the existing `w-7 h-7` circle / `w-3.5 h-3.5` glyph sizing already used by `onShowMap`/`onToggleTodo`):
    - `Eye` button, always rendered, `aria-label={t('esikatseluCta')}`, `onClick` stops propagation and calls `dashboardActions.onPreview()`, gets `disabled` plus `opacity-40 pointer-events-none` classes when `dashboardActions.status === 'kesken'`.
    - `Pencil` button, always rendered, `aria-label={dashboardActions.status === 'kesken' ? t('jatkaCta') : t('muokkaaCta')}`, `onClick` stops propagation and calls `dashboardActions.onEditOrContinue()`.
    - `Link2` button, rendered only when `dashboardActions.onCopyInviteLink` is present, `aria-label={dashboardActions.copied ? t('inviteLinkCopied') : t('copyInviteLinkCta')}`, `onClick` stops propagation and calls `dashboardActions.onCopyInviteLink()`.
    - `AlertCircle` button, rendered only when `dashboardActions.onShowRejectionInfo` is present, `aria-label={t('showRejectionInfoLabel')}`, `onClick` stops propagation and calls `dashboardActions.onShowRejectionInfo()`.
    Each button's chip styling: when `panelShade` is defined, use inline `style={{ backgroundColor: panelChipBg, color: panelShadeContrastText }}`; when undefined, use the existing `glass-btn text-[rgba(17,17,17,0.5)] hover:text-[#111111]` classes (mirrors the existing `accentColor ? {...inline} : 'glass-btn ...'` conditional at lines 261-262).
    When `dashboardActions` is absent, render exactly the existing photo/`Camera`-fallback JSX unchanged (D-02 — every other call site keeps the real photo).

    Add the status pill (D-05): render it whenever `dashboardActions` is present, positioned `absolute bottom-3 right-3 z-20` (the same slot the photo variant's bookmark button occupies), using the exact 4-state color/label pairs copied verbatim from `app/business/page.tsx`'s `VenueRow` (lines 122-138): `kesken` → `bg-[rgba(17,17,17,0.08)] text-[rgba(17,17,17,0.55)]` + `t('statusKesken')`; `approved` → `bg-green-100 text-green-700` + `t('statusApproved')`; `rejected` → `bg-red-50 text-red-600` + `t('statusRejected')`; else (`pending`) → `bg-amber-100 text-amber-700` + `t('statusPending')`. Same `text-[10px] font-bold uppercase tracking-widest px-2 py-1 rounded-full` classes as the source.

    Gate the existing `{hasCoords && (...)}` "show on map" button additionally on `!dashboardActions` (change to `{hasCoords && !dashboardActions && (...)}`) — the dashboard variant never renders `onShowMap`/`onToggleTodo` (UI-SPEC: "not part of the 4 dashboard actions"); the status pill takes that button's `bottom-3 right-3` slot instead. `onToggleTodo` naturally won't render since the dashboard call site (Plan 05) never passes it.

    Change the whole-card click-catcher block: when `dashboardActions` is present, render nothing at all in that JSX position (no click-catcher div, not even the inert no-op filler) — per D-10 and 63-PATTERNS.md, the dashboard variant's left info panel is fully inert and needs no click-catcher. When `dashboardActions` is absent, keep the existing `onOpen ? (...) : (...)` ternary completely unchanged.

    Change the outer `motion.div`'s static `cursor-pointer` class to be conditional on `onOpen` (e.g. `` `relative glass glass-hover rounded-2xl h-36${onOpen ? ' cursor-pointer' : ''}` ``) so the dashboard variant (which never passes `onOpen`) does not show a misleading pointer cursor over its now-inert left panel. Leave `whileHover`/`whileTap` motion props unchanged — D-01 only drops a hover/tap *reveal* animation for hidden content, and no such reveal exists in this file today (RESEARCH.md Pitfall 4 confirms the status pill and controls panel are new UI, not a repositioning); the existing whole-card scale-on-hover treatment is a separate, unaffected visual.
  </action>
  <acceptance_criteria>
    - `grep -c "dashboardActions" app/components/DiagonaalKortti.tsx` is >= 8 (prop declaration + destructure + all conditional usages)
    - `grep -c "getPanelShade" app/components/DiagonaalKortti.tsx` >= 1
    - `grep -c "Eye\|Pencil\|Link2\|AlertCircle" app/components/DiagonaalKortti.tsx` >= 4 (imports + JSX usages)
    - `grep -c "hasCoords && !dashboardActions" app/components/DiagonaalKortti.tsx` returns 1
    - Region-scoped: within the RIGHT-panel block only, dashboardActions-present branch renders no `<img src={paikka.image_url` — confirm by reading the diff, the photo `<img>` remains reachable only via the `dashboardActions`-absent branch
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>DiagonaalKortti accepts an optional dashboardActions bundle that swaps the right panel to a controls panel with 2-4 icon buttons and a status pill; every existing call site (no dashboardActions passed) is visually and behaviorally unchanged; project type-checks.</done>
</task>

<task type="auto">
  <name>Task 3: Build RejectionReasonPopup component (D-06)</name>
  <files>app/components/RejectionReasonPopup.tsx</files>
  <read_first>
    - app/components/AuthModal.tsx (full lightweight-dialog pattern to mirror — Escape-key handler ~lines 49-61, backdrop+panel+close-button structure ~lines 141-178)
    - .planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-UI-SPEC.md (Component Contract: Rejection-Reason Popup — exact structure/classNames/durations, and the Copywriting Contract table mapping to real key names)
    - messages/fi.json (confirm exact key names: `Business.dashboardStatusRejectedTitle`, `Business.dashboardStatusRejectedBody`, `Business.dashboardStatusRejectedBodyNoReason`, `Business.fixDetailsCta`, `Business.previewClose`)
  </read_first>
  <action>
    Create a new client component `RejectionReasonPopup` with props `{ open: boolean; onClose: () => void; rejectionReason: string | null; editHref: string }`. Use `useTranslations('Business')`. Mirror `AuthModal.tsx`'s structure exactly: `AnimatePresence` wrapping a `fixed inset-0 z-[80] flex items-end sm:items-center justify-center` container with `aria-modal="true" role="dialog"`; a backdrop `motion.div` (`absolute inset-0 bg-[rgba(0,0,0,0.40)]`, opacity 0→1, `duration: 0.2`, `onClick={onClose}`); a panel `motion.div` (`relative glass rounded-2xl p-6 w-full max-w-sm mx-4`, opacity+y(24→0), `duration: 0.25`, `ease: [0.25, 0.1, 0.25, 1]`); a close `<button>` (`glass-btn w-7 h-7 rounded-full ... absolute top-4 right-4`, `<X className="w-3.5 h-3.5" />`, `aria-label={t('previewClose')}` — reuse this existing key verbatim, do not invent a new "close" key since the Copywriting Contract lists only two new keys total, both already added in Task 1). Add an `Escape`-key `useEffect` that calls `onClose` when `open` (mirror AuthModal's keydown-listener pattern, dropping AuthModal's `!loading` guard since this popup has no async state).

    Body content (inside the panel, below the close button): a heading `<h2 className="text-xl font-bold text-[#111111]">{t('dashboardStatusRejectedTitle')}</h2>`; a body `<p className="text-sm text-[rgba(17,17,17,0.6)]">` showing `rejectionReason ? t('dashboardStatusRejectedBody', { reason: rejectionReason }) : t('dashboardStatusRejectedBodyNoReason')` as plain JSX text interpolation — never `dangerouslySetInnerHTML` (Security Domain: rejection_reason is admin-authored free text, must render as escaped text); a CTA `<a href={editHref} className="mt-2 bg-[#111111] hover:bg-[#333333] text-white font-bold text-sm rounded-full h-10 px-6 flex items-center justify-center w-fit [transition:background-color_150ms_var(--ease-out)]">{t('fixDetailsCta')}</a>` — this MUST be a plain anchor navigation, never a `fetch()` call to any `/api/business/*` endpoint (RESEARCH.md Pitfall 1 — the status flip happens automatically on the next section save via `update-paikka`, per D-07/Plan 03; this popup's CTA only navigates). Dismissing via backdrop click, Escape, or the X button must only call `onClose` — no navigation, no side effect (D-06).
  </action>
  <acceptance_criteria>
    - `grep -c "dangerouslySetInnerHTML" app/components/RejectionReasonPopup.tsx` returns 0
    - `grep -c "fetch(" app/components/RejectionReasonPopup.tsx` returns 0 (CTA is a plain `<a href>`, never a fetch call)
    - `grep -c "api/business/reapply" app/components/RejectionReasonPopup.tsx` returns 0
    - `grep -c "role=\"dialog\"" app/components/RejectionReasonPopup.tsx` returns 1
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>RejectionReasonPopup renders the rejection reason as plain text with a navigation-only CTA, closes on Escape/backdrop/X without navigating, and type-checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin-authored rejection_reason → business owner's browser | `rejection_reason` is free text written by an admin during venue rejection; it is rendered inside RejectionReasonPopup to the business account holder |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-04A | Tampering (stored XSS) | RejectionReasonPopup rendering rejection_reason | high | mitigate | Render via plain JSX text interpolation (`{rejectionReason}` / next-intl `t(...,{reason})`) only — never `dangerouslySetInnerHTML`. React/next-intl escape interpolated values by default. Task 3's acceptance criteria greps for zero `dangerouslySetInnerHTML` occurrences. |
| T-63-04B | Tampering (unintended state mutation via popup) | RejectionReasonPopup CTA | medium | mitigate | CTA is a plain `<a href>` navigation, never a `fetch()`/API call — Task 3 acceptance criteria greps for zero `fetch(` and zero `api/business/reapply` references in this file |
| T-63-04C | Spoofing (misleading controls-panel affordance) | DiagonaalKortti dashboard variant left panel | low | accept | Left panel has no click handler and no click-catcher, and the outer card's `cursor-pointer` is now conditional on `onOpen` — no pointer cursor implies no click affordance on the inert region |
</threat_model>

<verification>
- `npx tsc --noEmit` clean after all three tasks
- Manual (once Plan 05 wires this): open `/business`, confirm each existing consumer-facing card (`Etusivu.tsx`, admin `/admin/[id]`) still shows the real photo unchanged (no dashboardActions regression)
</verification>

<success_criteria>
- DiagonaalKortti's dashboard variant (controls panel, status pill, whole-card inertness) and RejectionReasonPopup exist, type-check, and follow the locked UI-SPEC contract; every other DiagonaalKortti call site is unchanged.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 04)
- New prop: `dashboardActions?: { status: 'kesken' | 'approved' | 'rejected' | 'pending'; onPreview: () => void; onEditOrContinue: () => void; onCopyInviteLink?: () => void; copied?: boolean; onShowRejectionInfo?: () => void }` on `DiagonaalKorttiProps` (`app/components/DiagonaalKortti.tsx`)
- New component: `app/components/RejectionReasonPopup.tsx`, props `{ open: boolean; onClose: () => void; rejectionReason: string | null; editHref: string }`
- New i18n keys: `Business.fixDetailsCta`, `Business.showRejectionInfoLabel` (both `messages/fi.json` and `messages/en.json`)
</artifacts>

<output>
Create `.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-04-SUMMARY.md` when done
</output>
