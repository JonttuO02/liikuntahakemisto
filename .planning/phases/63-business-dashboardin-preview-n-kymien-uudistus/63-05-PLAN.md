---
phase: 63-business-dashboardin-preview-n-kymien-uudistus
plan: 05
type: execute
wave: 3
depends_on: ["63-04"]
files_modified:
  - app/business/page.tsx
  - app/api/business/reapply/route.ts
autonomous: true
requirements: [BIZPANEL-06, BIZPANEL-07]
must_haves:
  truths:
    - "The /business dashboard's venue list renders one DiagonaalKortti (dashboardActions variant) per venueLink instead of VenueRow"
    - "No 'Hae uudelleen' (reapply) button or fetch call to /api/business/reapply exists anywhere in the codebase after this plan"
    - "Rejected venues expose the rejection-info icon button, which opens RejectionReasonPopup with that venue's rejection_reason and an editHref of /business/{paikka_id}"
    - "Copy-invite-link still only appears for approved && !kesken venues and still works via navigator.clipboard.writeText"
  artifacts:
    - "app/business/page.tsx no longer defines VenueRow, handleReapply, or a reapply CTA inside StatusCard"
    - "app/api/business/reapply/route.ts is deleted"
  key_links:
    - "Each dashboard card's dashboardActions.onPreview opens the existing PreviewModal (setPreviewPaikka), unchanged from today's onPreview wiring"
    - "Each dashboard card's dashboardActions.onShowRejectionInfo opens a single page-level RejectionReasonPopup instance via new rejectionPopupLink state"
---

<objective>
Wire the new `DiagonaalKortti` dashboard variant (built in Plan 04) into `/business`, replacing the plain-text `VenueRow`/`StatusCard` reapply UI. Relocate `handleCopyInviteLink` verbatim into a small per-venue wrapper that also holds the `copied` toggle state (mirroring today's `VenueRow` local-state pattern). Wire the new `RejectionReasonPopup` as a single page-level instance. Remove `handleReapply` and every "Hae uudelleen" trigger (D-07), then delete `app/api/business/reapply/route.ts` — Claude's Discretion on its fate is resolved by RESEARCH.md's Pitfall 1/Assumption A2: delete it, since its only callers are removed in this plan and the cooldown it enforced is deliberately not ported (D-15).

Purpose: complete BIZPANEL-06 (status pill) and BIZPANEL-07 (permanent icon-button controls) by actually replacing the dashboard's rendered list — Plan 04 alone only builds the component, this plan makes it the dashboard.
Output: `/business` renders DiagonaalKortti-cards exclusively; `reapply/route.ts` is gone.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-CONTEXT.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-RESEARCH.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-PATTERNS.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace VenueRow/StatusCard's reapply UI with DiagonaalKortti-cards and RejectionReasonPopup</name>
  <files>app/business/page.tsx</files>
  <read_first>
    - app/business/page.tsx (the full target file — `StatusCard` lines 44-91, `VenueRow` lines 94-184, `handleReapply` lines 251-274, the render loop lines 316-333, and the `PreviewModal` `AnimatePresence` block lines 373-378)
    - app/components/DiagonaalKortti.tsx (confirm the exact `dashboardActions` prop shape built in Plan 04: `{ status, onPreview, onEditOrContinue, onCopyInviteLink?, copied?, onShowRejectionInfo? }`)
    - app/components/RejectionReasonPopup.tsx (confirm props: `{ open, onClose, rejectionReason, editHref }`)
    - lib/venueStatus.ts (`deriveVenueStatus` — already imported and used in this file, reuse unchanged)
    - .planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-PATTERNS.md (relocation guidance for `handleCopyInviteLink`, the render-loop replacement, and `handleReapply`/`StatusCard` removal, lines 222-296)
  </read_first>
  <action>
    Delete the `VenueRow` function entirely (lines 94-184) and replace it with a new `DashboardVenueCard` function taking `{ link, t, isKesken, onPreview, onShowRejectionInfo }: { link: VenueLink; t: TBusiness; isKesken: boolean; onPreview: (p: Liikuntapaikka) => void; onShowRejectionInfo: (link: VenueLink) => void }`. Inside it, keep the exact `copied`/`handleCopyInviteLink` local state and function relocated verbatim from the old `VenueRow` (lines 107-114: `useState(false)` + `navigator.clipboard.writeText(window.location.origin + '/business/liity?paikka_id=' + link.paikka_id)` + 2s timeout). Render a single `<DiagonaalKortti paikka={link.liikuntapaikat as unknown as Liikuntapaikka} dashboardActions={{ status: isKesken ? 'kesken' : (link.claim_status as 'approved' | 'rejected' | 'pending'), onPreview: () => { if (link.liikuntapaikat) onPreview(link.liikuntapaikat as unknown as Liikuntapaikka) }, onEditOrContinue: () => { window.location.href = isKesken ? '/business/onboarding?paikka_id=' + link.paikka_id : '/business/' + link.paikka_id }, onCopyInviteLink: link.claim_status === 'approved' && !isKesken ? handleCopyInviteLink : undefined, copied, onShowRejectionInfo: link.claim_status === 'rejected' ? () => onShowRejectionInfo(link) : undefined }} />` — do not pass `onOpen`, `onShowMap`, or `onToggleTodo` to this call (D-10, D-05 — dashboard variant never receives those). The `onCopyInviteLink` gate (`link.claim_status === 'approved' && !isKesken`) is D-08's exact visibility condition, copied verbatim from today's `VenueRow`, so the 4th icon button appears under the same rule as the old text button.

    In `StatusCard` (lines 44-91), remove the `onReapply` prop entirely from its signature and its only call site (`onClick={() => onReapply(rejectedLink.paikka_id)}` button in the `allRejected` branch, lines 74-81) — the title/body text of the `allRejected` banner may stay, but its CTA button must not remain (D-07). Update `StatusCard`'s call site (in the venueLinks-present render branch) to no longer pass `onReapply`.

    Delete `handleReapply` (lines 251-274) entirely — its only callers (the `StatusCard` CTA just removed, and the old `VenueRow`'s reapply button) are both gone. Do not port its fetch call anywhere new; D-07's replacement behavior is the automatic server-side flip already implemented in `update-paikka/route.ts` (Plan 03), triggered the next time the business owner saves any section via the existing edit flow.

    Add page-level state `const [rejectionPopupLink, setRejectionPopupLink] = useState<VenueLink | null>(null)`. Update the render loop (lines 316-333) to `venueLinks.map(link => <DashboardVenueCard key={link.paikka_id} link={link} t={t} isKesken={deriveVenueStatus(link.claim_status, keskenPaikkaIds.has(link.paikka_id), link.submitted_at) === 'kesken'} onPreview={setPreviewPaikka} onShowRejectionInfo={setRejectionPopupLink} />)` — reuse the exact `deriveVenueStatus(...)` call already present, unchanged. Keep the existing `<div className="flex flex-col gap-3">` wrapper.

    Add `import RejectionReasonPopup from '@/app/components/RejectionReasonPopup'` and render one page-level instance directly after the existing `PreviewModal` `AnimatePresence` block: `<RejectionReasonPopup open={!!rejectionPopupLink} onClose={() => setRejectionPopupLink(null)} rejectionReason={rejectionPopupLink?.rejection_reason ?? null} editHref={rejectionPopupLink ? '/business/' + rejectionPopupLink.paikka_id : ''} />` (a `claim_status === 'rejected'` venue is never simultaneously `kesken` per `deriveVenueStatus`'s draft-takes-priority logic, so the edit target is always the non-onboarding edit route `/business/{paikka_id}`).
  </action>
  <acceptance_criteria>
    - `grep -c "function VenueRow" app/business/page.tsx` returns 0
    - `grep -c "function DashboardVenueCard" app/business/page.tsx` returns 1
    - `grep -c "handleReapply" app/business/page.tsx` returns 0
    - `grep -c "reapplyCta" app/business/page.tsx` returns 0 (no reapply CTA text rendered anywhere in this file)
    - `grep -c "RejectionReasonPopup" app/business/page.tsx` is >= 2 (import + usage)
    - `grep -c "dashboardActions=" app/business/page.tsx` returns 1
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The /business venue list renders DashboardVenueCard (wrapping the new DiagonaalKortti dashboardActions variant) for every venueLink; StatusCard's and VenueRow's reapply CTAs and handleReapply are gone; RejectionReasonPopup is wired at the page level; project type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the now-dead /api/business/reapply route</name>
  <files>app/api/business/reapply/route.ts</files>
  <read_first>
    - app/api/business/reapply/route.ts (the file being deleted — confirm its cooldown/status-flip logic, already superseded by Plan 03's update-paikka branch)
    - app/business/page.tsx (confirm, post-Task-1, zero remaining references to `/api/business/reapply` or `handleReapply`)
  </read_first>
  <action>
    Delete the file `app/api/business/reapply/route.ts` (and its now-empty `app/api/business/reapply/` directory, if the filesystem leaves it empty). This resolves CONTEXT.md's "Claude's Discretion" item on the route's fate per RESEARCH.md Pitfall 1 / Assumption A2: after Task 1 removes both UI callers (`StatusCard`'s and the former `VenueRow`'s "Hae uudelleen" buttons) and D-07's replacement logic lives in `update-paikka/route.ts` (Plan 03), this route has zero remaining callers and is confirmed dead code. Do not delete `supabase/migrations/20260610000004_reapply_cooldown.sql` — per D-15 it is explicitly left in place, untouched (it only added a column/index, not a trigger, so it is harmless dead weight, not active code).
  </action>
  <acceptance_criteria>
    - File no longer exists: attempting to read `app/api/business/reapply/route.ts` returns a not-found error
    - `grep -rln "api/business/reapply" app --include="*.ts" --include="*.tsx"` (excluding `.claude/worktrees/`) returns zero matches
    - `npm test` passes (no test references the deleted route; `tests/api/update-paikka.test.ts` and the rest of the suite are unaffected)
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>app/api/business/reapply/route.ts is deleted, no remaining references exist in app/, and the full test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| business owner's browser → /business/{id} edit route | `onEditOrContinue`'s navigation target is derived from `link.paikka_id`, which comes from the already-server-scoped `venueLinks` query (`business_account_id = user.id`) — no new client-trust surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-05A | Repudiation | Deletion of /api/business/reapply | low | accept | Confirmed dead code (zero callers after Task 1) via `grep` acceptance criteria in Task 2; the replacement auto-resubmit behavior is already covered by Plan 03's server-side tests |
| T-63-05B | Tampering | Client-side `status`/`claim_status` used to decide which icon buttons render | low | accept | This is UI-affordance only (which buttons are visible); the authoritative enforcement is server-side in `update-paikka/route.ts` (Plan 03) and the existing ownership check — a business owner seeing an extra/missing button client-side cannot bypass server authorization |
</threat_model>

<verification>
- `npx tsc --noEmit` clean after Task 1
- `npm test` green after Task 2 (full suite, confirms no regressions from the route deletion)
- Manual: open `/business` with at least one approved, one pending, one rejected, and one kesken venue; confirm each renders as a DiagonaalKortti card with the correct status pill and the correct subset of icon buttons (rejected → rejection-info icon opens the popup; approved && !kesken → invite-link icon present; kesken → preview icon dimmed/disabled)
- Manual: click the rejection-info icon, confirm the popup shows the rejection reason (or the no-reason fallback) and that its CTA navigates to `/business/{paikka_id}` without any network request
</verification>

<success_criteria>
- `/business` dashboard is fully migrated to DiagonaalKortti dashboard cards; no reapply button/route remains anywhere in the codebase; full test suite green.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 05)
- New component: `DashboardVenueCard` (local to `app/business/page.tsx`) — replaces `VenueRow`
- New page-level state: `rejectionPopupLink` in `app/business/page.tsx`
- Removed: `VenueRow`, `handleReapply`, `StatusCard`'s reapply CTA (all from `app/business/page.tsx`)
- Deleted file: `app/api/business/reapply/route.ts`
</artifacts>

<output>
Create `.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-05-SUMMARY.md` when done
</output>
