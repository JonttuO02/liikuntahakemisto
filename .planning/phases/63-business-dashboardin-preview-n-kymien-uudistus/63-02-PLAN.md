---
phase: 63-business-dashboardin-preview-n-kymien-uudistus
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/PreviewModal.tsx
  - app/business/onboarding/LivePreviewPane.tsx
  - app/components/PaikkaSheet.tsx
autonomous: true
requirements: [PREV-04, LIVEPREV-05, PREV-05]
must_haves:
  truths:
    - "PreviewModal's stack is CalloutCard → DiagonaalKortti → PaikkaSheet(preview) — no PaikkaKortti rendered"
    - "LivePreviewPane renders a third PaikkaSheet(preview) section below CalloutCard and DiagonaalKortti, in both desktop sidebar and mobile toggle contexts"
    - "PaikkaSheet's booking-link button does not render when preview={true} — no navigation escape hatch in any preview surface"
  artifacts:
    - "app/components/PreviewModal.tsx imports CalloutCard, not PaikkaKortti"
    - "app/business/onboarding/LivePreviewPane.tsx imports and renders PaikkaSheet"
  key_links:
    - "PreviewModal passes lat/lng-shimmed venue ({ ...paikka, latitude: paikka.latitude ?? 0, longitude: paikka.longitude ?? 0 }) into CalloutCard"
    - "LivePreviewPane's existing DiagonaalKortti call is NOT given dashboardActions (keeps real photo — D-14)"
---

<objective>
Land the three purely-visual preview-surface changes: swap `PaikkaKortti`→`CalloutCard` in `PreviewModal` (PREV-04, D-11/D-12), add `PaikkaSheet` as a third stacked section in `LivePreviewPane` (LIVEPREV-05, D-13/D-14), and close the pre-existing PREV-05 click-through hole in `PaikkaSheet`'s booking link (RESEARCH.md Pitfall 3). Each is a compositional/wiring change to already-compliant components — no new colors, spacing, or typography.

Purpose: unify every preview surface on the same 3-section stack (CalloutCard → DiagonaalKortti → PaikkaSheet) and make all preview surfaces genuinely inert (PREV-05).
Output: `PreviewModal` on CalloutCard, `LivePreviewPane` with a venuepage section, and `PaikkaSheet` with the booking link suppressed in preview.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-RESEARCH.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-PATTERNS.md
@.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace PaikkaKortti with CalloutCard in PreviewModal (PREV-04, D-11/D-12)</name>
  <files>app/components/PreviewModal.tsx</files>
  <read_first>
    - app/components/PreviewModal.tsx (the target — current stack order: PaikkaKortti section lines ~47-53, then DiagonaalKortti, then PaikkaSheet; and the props signature, to know whether brandColor/accentColor are available)
    - app/business/onboarding/LivePreviewPane.tsx (analog — copy its CalloutCard section + `?? 0` shim, lines ~35-48)
    - app/components/CalloutCard.tsx (confirm the non-nullable `latitude`/`longitude` prop type and that brandColor/accentColor absence is handled gracefully)
    - .planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-PATTERNS.md (exact replacement JSX, lines ~300-334)
  </read_first>
  <action>
    Remove the PaikkaKortti section (the `{/* Section: PaikkaKortti */}` block with its `previewLabelCard` label and `<PaikkaKortti paikka={paikka} />`). Replace it with a CalloutCard section mirroring LivePreviewPane: a label `<span>` using `t('previewLabelCallout')` and `<CalloutCard p={{ ...paikka, latitude: paikka.latitude ?? 0, longitude: paikka.longitude ?? 0 }} .../>` — the `?? 0` shim is mandatory because CalloutCard's prop type requires non-nullable lat/lng (D-12, Pitfall 5). In the import block, remove `import PaikkaKortti from '@/app/components/PaikkaKortti'` and add `import CalloutCard from '@/app/components/CalloutCard'`. If PreviewModal already receives `brandColor`/`accentColor` (via its props or on the `paikka` object), thread them into CalloutCard; if not available on the current props, omit those two props (CalloutCard handles their absence). Do NOT change the existing DiagonaalKortti section (it keeps the real photo — consumer view) or the PaikkaSheet section. Resulting order (D-11): CalloutCard → DiagonaalKortti → PaikkaSheet. Do not delete `PaikkaKortti.tsx` itself — it is still used by the main list view.
  </action>
  <acceptance_criteria>
    - `grep -c "PaikkaKortti" app/components/PreviewModal.tsx` returns 0
    - `grep -c "CalloutCard" app/components/PreviewModal.tsx` is >= 1
    - `grep -c "latitude: paikka.latitude ?? 0" app/components/PreviewModal.tsx` returns 1 (shim present)
    - `npx tsc --noEmit` reports no errors (proves CalloutCard's non-nullable lat/lng type is satisfied)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>PreviewModal renders CalloutCard (with lat/lng shim) in place of PaikkaKortti; stack order is CalloutCard → DiagonaalKortti → PaikkaSheet; project type-checks.</done>
</task>

<task type="auto">
  <name>Task 2: Add PaikkaSheet as the third LivePreviewPane section (LIVEPREV-05, D-13/D-14)</name>
  <files>app/business/onboarding/LivePreviewPane.tsx</files>
  <read_first>
    - app/business/onboarding/LivePreviewPane.tsx (the target — existing 2-section stack lines ~33-58, `useLivePreview()` usage, and the existing DiagonaalKortti call which must stay unchanged per D-14)
    - app/components/PreviewModal.tsx (analog — its `<PaikkaSheet preview={true} .../>` call with the exact no-op onClose/onToggleTodo props)
    - app/components/PaikkaSheet.tsx (confirm the `preview` prop and required props: paikka, todo, onClose, onToggleTodo)
    - .planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-PATTERNS.md (exact 3rd-section JSX, lines ~338-361)
  </read_first>
  <action>
    Add a third stacked section below the existing DiagonaalKortti block, following the exact label-then-component wrapper pattern already used twice in this file: a `<span>` label using `t('previewLabelSheet')` then `<PaikkaSheet paikka={livePreviewPaikka} preview={true} todo={false} onClose={() => {}} onToggleTodo={() => {}} />`. Copy the no-op `onClose`/`onToggleTodo` props verbatim from PreviewModal — `preview={true}` already disables PaikkaSheet's interactive paths (drag-dismiss, close handle, show-on-map, and — after Plan 02 Task 3 — the booking link). Add `import PaikkaSheet from '@/app/components/PaikkaSheet'` to the import block. Per D-14, do NOT touch the existing DiagonaalKortti call in this file and do NOT pass it `dashboardActions` — live-preview must show the real photo (consumer view). The section works in both WizardInner contexts (desktop `w-[360px]` sidebar and mobile toggle) with no layout change beyond the column scrolling taller (D-13). If `t('previewLabelSheet')` does not yet exist as a key, reuse the closest existing venuepage label key already present in the `Business` namespace rather than inventing new copy; confirm by reading messages/fi.json.
  </action>
  <acceptance_criteria>
    - `grep -c "PaikkaSheet" app/business/onboarding/LivePreviewPane.tsx` is >= 2 (import + usage)
    - `grep -c "preview={true}" app/business/onboarding/LivePreviewPane.tsx` returns 1
    - `grep -c "dashboardActions" app/business/onboarding/LivePreviewPane.tsx` returns 0 (D-14 — real photo, no dashboard variant)
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>LivePreviewPane renders CalloutCard → DiagonaalKortti(real photo) → PaikkaSheet(preview) as one scrollable column; project type-checks.</done>
</task>

<task type="auto">
  <name>Task 3: Suppress PaikkaSheet booking link in preview mode (PREV-05, Pitfall 3)</name>
  <files>app/components/PaikkaSheet.tsx</files>
  <read_first>
    - app/components/PaikkaSheet.tsx (the target — the booking-link `<a target="_blank" href={paikka.varauslinkki}>` conditional ~lines 247-257, and the already-correct `!preview` guards on close/bookmark/"show on map" ~line 267 for the reference pattern)
    - .planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-PATTERNS.md (exact fix, lines ~365-388)
  </read_first>
  <action>
    Add `!preview` to the booking-link render conditional so the "Varaa aika" button is hidden in preview mode, matching the guards already applied to the close button, bookmark button, and "show on map" row. Change the conditional guarding the `<a href={paikka.varauslinkki!} target="_blank" ...>` from `{isSafeUrl(paikka.varauslinkki) && (` to `{isSafeUrl(paikka.varauslinkki) && !preview && (`. This is the only change in this file. Do not alter the button's styling or any non-preview behavior — in the live standalone/consumer render (`preview` falsy) the booking link must still appear exactly as today.
  </action>
  <acceptance_criteria>
    - `grep -c "isSafeUrl(paikka.varauslinkki) && !preview" app/components/PaikkaSheet.tsx` returns 1
    - The consumer-mode booking link is unchanged: `grep -c "target=\"_blank\"" app/components/PaikkaSheet.tsx` still returns the same count as before (the `<a>` still exists, only its guard changed)
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The booking-link button is gated behind `!preview`; it renders in consumer mode and is absent in every preview surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preview surface → external site | PaikkaSheet's booking link (`varauslinkki`, admin/venue-authored URL) could navigate the user away from a "purely visual" preview — the PREV-05 violation being closed here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-02A | Tampering | PaikkaSheet booking link in preview mode | medium | mitigate | Task 3 adds the `!preview` guard so the external-navigation `<a>` is not rendered in any preview surface; the existing `isSafeUrl()` check on `varauslinkki` (protocol allow-list) remains the guard for consumer mode |
| T-63-02B | Information Disclosure | CalloutCard rendering venue data in PreviewModal | low | accept | CalloutCard is an existing purely-presentational component with zero click handlers (verified in RESEARCH.md); passing shimmed lat/lng introduces no new data exposure |
</threat_model>

<verification>
- `npx tsc --noEmit` clean after all three tasks
- Manual click-audit (PREV-05 visual half): open PreviewModal and LivePreviewPane for a venue that has a `varauslinkki`; confirm no "Varaa aika" button appears and that clicking anywhere inside CalloutCard / DiagonaalKortti / PaikkaSheet neither navigates nor opens a new tab
- Manual (LIVEPREV-05): confirm the third PaikkaSheet section renders in both desktop sidebar and mobile toggle view
</verification>

<success_criteria>
- PreviewModal uses CalloutCard (PaikkaKortti removed); LivePreviewPane has the third PaikkaSheet section (real-photo DiagonaalKortti preserved); PaikkaSheet's booking link is inert in preview.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 02)
- Modified composition: `PreviewModal.tsx` stack = CalloutCard → DiagonaalKortti → PaikkaSheet (PaikkaKortti import removed, CalloutCard import added)
- New section: PaikkaSheet(preview) third block in `LivePreviewPane.tsx`
- Behavior change: `PaikkaSheet.tsx` booking-link render now gated on `!preview`
- No new exported symbols or files (pure wiring)
</artifacts>

<output>
Create `.planning/phases/63-business-dashboardin-preview-n-kymien-uudistus/63-02-SUMMARY.md` when done
</output>
