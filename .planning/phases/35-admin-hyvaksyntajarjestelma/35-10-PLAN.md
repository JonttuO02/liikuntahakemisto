---
phase: 35-admin-hyvaksyntajarjestelma
plan: 10
type: execute
wave: 4
depends_on: []
files_modified:
  - supabase/migrations/20260610000003_add_logo_url_to_liikuntapaikat.sql
  - app/api/business/onboarding/submit/route.ts
  - app/api/admin/applications/[id]/route.ts
autonomous: true
requirements:
  - ADMIN-02
gap_closure: true

must_haves:
  truths:
    - "logo_url column exists in liikuntapaikat table (TEXT NULL)"
    - "onboarding/submit writes logo_url: draft.media_urls?.logo ?? null to liikuntapaikat UPDATE"
    - "GET /api/admin/applications/[id] selects logo_url from liikuntapaikat"
    - "All three files compile without TypeScript errors"
  artifacts:
    - path: "supabase/migrations/20260610000003_add_logo_url_to_liikuntapaikat.sql"
      provides: "ALTER TABLE migration adding logo_url TEXT NULL column"
    - path: "app/api/business/onboarding/submit/route.ts"
      provides: "Updated liikuntapaikat UPDATE block writing logo_url from draft"
    - path: "app/api/admin/applications/[id]/route.ts"
      provides: "Updated select string including logo_url"
  key_links:
    - from: "app/api/business/onboarding/submit/route.ts"
      to: "liikuntapaikat.logo_url"
      via: "supabaseAdmin.from('liikuntapaikat').update({ logo_url: ... })"
      pattern: "logo_url"
    - from: "app/api/admin/applications/[id]/route.ts"
      to: "liikuntapaikat.logo_url"
      via: "select string"
      pattern: "logo_url"
---

<objective>
Close GAP A from 35-UAT.md: logo is missing in the admin detail view because the logo_url column was never added to the liikuntapaikat table and the onboarding/submit route never writes it.

Purpose: ADMIN-02 requires admin to see all uploaded media including the logo. The lib/types.ts Liikuntapaikka type and DiagonaalKortti/PaikkaSheet components already reference logo_url — only the DB column and write path are missing.

Output:
- supabase/migrations/20260610000003_add_logo_url_to_liikuntapaikat.sql — adds the column
- app/api/business/onboarding/submit/route.ts — writes logo_url in the liikuntapaikat UPDATE
- app/api/admin/applications/[id]/route.ts — selects logo_url so the admin detail view receives it
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/PROJECT.md
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/ROADMAP.md

## Manual setup note (NOT a task — run in Supabase SQL editor once before testing)

The admin access-control tests require is_admin=true for joona.orava@gmail.com. Run this in the Supabase Dashboard SQL editor:

  UPDATE profiles
  SET is_admin = true
  WHERE user_id = (SELECT id FROM auth.users WHERE email = 'joona.orava@gmail.com');

This is a one-time database seed; it is not automated because it requires the Supabase dashboard and is specific to the developer account.

## Migration numbering

Existing migration sequence ends at 20260610000002_admin_columns.sql. The new migration must use timestamp 20260610000003.

## onboarding/submit write block location

The liikuntapaikat UPDATE is at lines 66-78 of app/api/business/onboarding/submit/route.ts. The update object currently contains: hinta_kuvaus, aukioloajat, kuvaus, puhelin, varauslinkki, image_url, photo_urls, business_managed. Add logo_url as the entry after photo_urls.

The draft shape accessed: draft.media_urls is typed as unknown at runtime. The existing pattern draft.media_urls?.photos?.[0] shows the accessor pattern — use draft.media_urls?.logo ?? null for logo_url.

## applications/[id] select string location

The select string is at line 21-24 of app/api/admin/applications/[id]/route.ts. The liikuntapaikat subselect currently ends with: image_url, photo_urls, latitude, longitude. Add logo_url after photo_urls and before latitude.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 20260610000003 — add logo_url to liikuntapaikat</name>
  <files>supabase/migrations/20260610000003_add_logo_url_to_liikuntapaikat.sql</files>
  <read_first>
    supabase/migrations/20260610000001_add_photo_urls_to_liikuntapaikat.sql — use as the exact structural template (single ALTER TABLE … ADD COLUMN IF NOT EXISTS statement, no additional SQL)
  </read_first>
  <action>
    Create supabase/migrations/20260610000003_add_logo_url_to_liikuntapaikat.sql containing a single statement:

      ALTER TABLE liikuntapaikat ADD COLUMN IF NOT EXISTS logo_url TEXT NULL;

    No COMMENT, no index, no RLS policy — column only. Follow the identical minimal style used by 20260610000001_add_photo_urls_to_liikuntapaikat.sql.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS logo_url TEXT NULL" C:/ClaudeCodeTestit/liikuntahakemisto/supabase/migrations/20260610000003_add_logo_url_to_liikuntapaikat.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists at supabase/migrations/20260610000003_add_logo_url_to_liikuntapaikat.sql
    - File contains exactly: ALTER TABLE liikuntapaikat ADD COLUMN IF NOT EXISTS logo_url TEXT NULL;
    - No other SQL statements in the file
    - grep count returns 1
  </acceptance_criteria>
  <done>Migration file created with single ADD COLUMN statement for logo_url TEXT NULL</done>
</task>

<task type="auto">
  <name>Task 2: Update onboarding/submit — write logo_url to liikuntapaikat</name>
  <files>app/api/business/onboarding/submit/route.ts</files>
  <read_first>
    app/api/business/onboarding/submit/route.ts — read in full before editing; the UPDATE block is at lines 66-78
  </read_first>
  <action>
    In the supabaseAdmin.from('liikuntapaikat').update({...}) call (Step 4 block), add logo_url as a new entry in the update object, placed after the photo_urls line and before business_managed:

      logo_url: draft.media_urls?.logo ?? null,

    The line immediately before it is: photo_urls: draft.media_urls?.photos ?? null,
    The line immediately after it is: business_managed: true,

    Do not modify any other part of the file. The cast on draft.media_urls is already handled by the existing pattern (draft is typed from Supabase query response, .logo is accessed via optional chaining which TypeScript allows on unknown-ish shapes in this file).
  </action>
  <verify>
    <automated>grep -c "logo_url: draft.media_urls?.logo ?? null" C:/ClaudeCodeTestit/liikuntahakemisto/app/api/business/onboarding/submit/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - The update object in the supabaseAdmin.from('liikuntapaikat').update({...}) block contains the line: logo_url: draft.media_urls?.logo ?? null,
    - The entry appears between the photo_urls line and the business_managed line
    - grep count returns 1
    - No other lines in the file are changed
    - File compiles without TypeScript errors (run: npx tsc --noEmit from project root)
  </acceptance_criteria>
  <done>onboarding/submit writes logo_url from draft.media_urls?.logo ?? null to liikuntapaikat</done>
</task>

<task type="auto">
  <name>Task 3: Update admin applications/[id] route — select logo_url</name>
  <files>app/api/admin/applications/[id]/route.ts</files>
  <read_first>
    app/api/admin/applications/[id]/route.ts — read in full before editing; the select string is at lines 19-27
  </read_first>
  <action>
    In the supabaseAdmin.from('business_paikka_links').select(...) call, find the liikuntapaikat subselect line that currently reads:

      image_url, photo_urls, latitude, longitude

    Change it to:

      image_url, photo_urls, logo_url, latitude, longitude

    Insert logo_url between photo_urls and latitude. Do not change any other part of the select string or any other part of the file.
  </action>
  <verify>
    <automated>grep -c "photo_urls, logo_url, latitude" C:/ClaudeCodeTestit/liikuntahakemisto/app/api/admin/applications/[id]/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - The select string contains: photo_urls, logo_url, latitude, longitude
    - The string photo_urls, logo_url, latitude appears on one line within the template literal
    - grep count returns 1
    - No other lines in the file are changed
    - File compiles without TypeScript errors (run: npx tsc --noEmit from project root)
  </acceptance_criteria>
  <done>GET /api/admin/applications/[id] selects logo_url from liikuntapaikat and includes it in the JSON response</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. Migration file exists and contains the single ALTER TABLE statement: grep -c "ADD COLUMN IF NOT EXISTS logo_url" supabase/migrations/20260610000003_add_logo_url_to_liikuntapaikat.sql returns 1
2. Submit route writes logo_url: grep -c "logo_url: draft.media_urls?.logo" app/api/business/onboarding/submit/route.ts returns 1
3. Admin route selects logo_url: grep -c "logo_url" app/api/admin/applications/[id]/route.ts returns 1
4. TypeScript compiles: npx tsc --noEmit exits 0

Manual step (run in Supabase dashboard before testing):
  ALTER TABLE liikuntapaikat ADD COLUMN IF NOT EXISTS logo_url TEXT NULL;
  UPDATE profiles SET is_admin = true WHERE user_id = (SELECT id FROM auth.users WHERE email = 'joona.orava@gmail.com');
</verification>

<success_criteria>
- logo_url column exists in the liikuntapaikat table
- Completing onboarding/submit persists the logo URL from draft.media_urls.logo to liikuntapaikat.logo_url
- Admin detail view (/admin/[id]) receives logo_url in the JSON response from GET /api/admin/applications/[id]
- All three modified files compile without TypeScript errors
</success_criteria>

<output>
Create .planning/phases/35-admin-hyvaksyntajarjestelma/35-10-SUMMARY.md when done
</output>
