---
phase: 35-admin-hyvaksyntajarjestelma
plan: 11
type: execute
wave: 4
depends_on: []
files_modified:
  - app/api/business/reapply/route.ts
  - app/business/page.tsx
autonomous: true
requirements:
  - ADMIN-03
gap_closure: true

must_haves:
  truths:
    - "POST /api/business/reapply exists and requires JWT auth (same pattern as claim-paikka)"
    - "reapply finds existing rejected business_paikka_links row by business_account_id + paikka_id + claim_status='rejected'"
    - "reapply UPDATEs claim_status='pending' and rejection_reason=null on the found row"
    - "reapply sends admin notification email (non-critical, wrapped in try/catch)"
    - "reapply returns 404 when no rejected row found for that user+venue combination"
    - "Hae uudelleen button in /business/page.tsx calls POST /api/business/reapply with {paikka_id} instead of calling setShowAddVenue(true)"
    - "On reapply success, the venueLinks state entry for that paikka_id updates to claim_status='pending' without a page reload"
    - "Both files compile without TypeScript errors"
  artifacts:
    - path: "app/api/business/reapply/route.ts"
      provides: "POST route: JWT auth → find rejected row → UPDATE to pending → notify admin"
    - path: "app/business/page.tsx"
      provides: "Hae uudelleen button calls POST /api/business/reapply and updates local venueLinks state"
  key_links:
    - from: "app/business/page.tsx"
      to: "app/api/business/reapply/route.ts"
      via: "fetch POST /api/business/reapply"
      pattern: "/api/business/reapply"
    - from: "app/api/business/reapply/route.ts"
      to: "business_paikka_links"
      via: "supabaseAdmin.from('business_paikka_links').update"
      pattern: "claim_status.*pending"
---

<objective>
Close GAP B from 35-UAT.md: the "Hae uudelleen" button in /business is broken because clicking it opens ClaimSearchForm, which blocks re-selection of already-claimed venues (is_claimed=true) and the subsequent INSERT fails with 409 unique constraint violation.

Purpose: ADMIN-03 requires that a rejected business can reapply for the same venue. Reapply must UPDATE the existing rejected row to claim_status=pending — not INSERT a new row.

Output:
- app/api/business/reapply/route.ts — new POST route that performs the UPDATE
- app/business/page.tsx — "Hae uudelleen" button replaced with an async handler calling /api/business/reapply
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/PROJECT.md
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/ROADMAP.md

## JWT auth pattern (from claim-paikka/route.ts)

The exact pattern to replicate in reapply/route.ts:

  const authHeader = request.headers.get('Authorization')
  const token = authHeader?.replace('Bearer ', '') ?? ''
  const { data: { user }, error: authError } = await supabaseAdmin.auth.getUser(token)
  if (authError || !user) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }

## sendAdminNotificationEmail signature (from lib/email.ts via claim-paikka/route.ts)

  await sendAdminNotificationEmail({
    companyName: biz.company_name,
    venueName: paikka.nimi,
    linkType: 'claim',
    applicationId: link.id,
    submittedAt: new Date().toISOString(),
  })

Import: import { sendAdminNotificationEmail } from '@/lib/email'
Import: import { supabaseAdmin } from '@/lib/supabaseAdmin.server'

## business/page.tsx button location

The "Hae uudelleen" button is at lines 127-133. Currently:
  onClick={() => setShowAddVenue(true)}

It must be replaced with an async handler that:
1. Reads the session token from supabase (createBrowserSupabase is already imported)
2. POSTs to /api/business/reapply with Authorization header and body {paikka_id: link.paikka_id}
3. On success (response.ok): calls setVenueLinks(prev => prev.map(l => l.paikka_id === link.paikka_id ? {...l, claim_status: 'pending', rejection_reason: null} : l))
4. On failure: console.error only — do not block the UI

The supabase session token is retrieved with:
  const supabase = createBrowserSupabase()
  const { data: { session } } = await supabase.auth.getSession()
  const token = session?.access_token ?? ''

The button must remain type="button" and keep the same className.

## business/page.tsx state constraints

setVenueLinks is already available as a state setter (line 19: const [venueLinks, setVenueLinks] = useState<VenueLink[]>([])). Use it directly in the onClick handler via setVenueLinks(prev => ...).

The VenueLink type (lines 9-14) already has: paikka_id, claim_status, rejection_reason, liikuntapaikat. No type changes needed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/api/business/reapply/route.ts</name>
  <files>app/api/business/reapply/route.ts</files>
  <read_first>
    app/api/business/claim-paikka/route.ts — read in full; reapply follows the same JWT auth, body parse, supabaseAdmin, and email notification patterns exactly
  </read_first>
  <action>
    Create app/api/business/reapply/route.ts as a Next.js Route Handler with a single exported POST function. Structure:

    1. Imports: NextResponse from 'next/server', supabaseAdmin from '@/lib/supabaseAdmin.server', sendAdminNotificationEmail from '@/lib/email'

    2. JWT auth block: identical to claim-paikka (authHeader → token → supabaseAdmin.auth.getUser → 401 if no user)

    3. Body parse block: parse paikka_id with parseInt(body.paikka_id, 10), return 400 on NaN or JSON parse failure

    4. Find rejected row: SELECT id FROM business_paikka_links WHERE business_account_id=user.id AND paikka_id=paikkaId AND claim_status='rejected' using .maybeSingle(). If null, return 404 {error: 'No rejected application found'}.

    5. UPDATE the found row by id: SET claim_status='pending', rejection_reason=null. If updateError, return 500.

    6. Admin notification (non-critical, wrapped in try/catch identical to claim-paikka): fetch business_accounts.company_name where user_id=user.id, fetch liikuntapaikat.nimi where id=paikkaId, call sendAdminNotificationEmail with linkType: 'claim', applicationId: rejectedLink.id.

    7. Return NextResponse.json({ ok: true })

    Use the variable name rejectedLink for the result of step 4. Use paikkaId (not paikka_id) as the local variable after parsing.
  </action>
  <verify>
    <automated>grep -c "claim_status.*pending" C:/ClaudeCodeTestit/liikuntahakemisto/app/api/business/reapply/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at app/api/business/reapply/route.ts
    - Exports a named POST function
    - Contains supabaseAdmin.auth.getUser(token) for JWT verification
    - Contains .select('id') from business_paikka_links with .eq('claim_status', 'rejected')
    - Contains .update({ claim_status: 'pending', rejection_reason: null }) on business_paikka_links
    - Contains try/catch block calling sendAdminNotificationEmail
    - Returns { ok: true } on success
    - Returns status 404 when no rejected row found
    - Returns status 401 when auth fails
    - grep count for "claim_status.*pending" (or "claim_status: 'pending'") returns at least 1
    - File compiles without TypeScript errors (run: npx tsc --noEmit from project root)
  </acceptance_criteria>
  <done>POST /api/business/reapply finds rejected business_paikka_links row, updates claim_status to pending, sends admin notification, returns { ok: true }</done>
</task>

<task type="auto">
  <name>Task 2: Update business/page.tsx — Hae uudelleen button calls /api/business/reapply</name>
  <files>app/business/page.tsx</files>
  <read_first>
    app/business/page.tsx — read in full before editing; the Hae uudelleen button is at lines 127-133; createBrowserSupabase import is at line 6
  </read_first>
  <action>
    Replace the onClick handler of the "Hae uudelleen" button (currently onClick={() => setShowAddVenue(true)}) with an async inline handler that:

    1. Gets the session token: const supabase = createBrowserSupabase(); const { data: { session } } = await supabase.auth.getSession(); const token = session?.access_token ?? ''
    2. Calls fetch('/api/business/reapply', { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: 'Bearer ' + token }, body: JSON.stringify({ paikka_id: link.paikka_id }) })
    3. If response.ok: calls setVenueLinks(prev => prev.map(l => l.paikka_id === link.paikka_id ? { ...l, claim_status: 'pending', rejection_reason: null } : l))
    4. If not response.ok: console.error('[reapply] failed', await res.json()) — use variable name res for the fetch result to avoid shadowing

    The button element: keep type="button" and keep the existing className unchanged. Only the onClick prop changes.

    Do not add any new state variables. Do not modify the VenueLink type. Do not change any other part of the file.
  </action>
  <verify>
    <automated>grep -c "/api/business/reapply" C:/ClaudeCodeTestit/liikuntahakemisto/app/business/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The string /api/business/reapply appears in app/business/page.tsx (grep count >= 1)
    - The string setShowAddVenue(true) does NOT appear inside the Hae uudelleen button's onClick (the button at lines ~127-133; the other setShowAddVenue(true) calls elsewhere in the file may remain)
    - The string setVenueLinks(prev => appears in the file (inline state update after success)
    - No new useState calls added
    - VenueLink type definition unchanged (lines 9-14)
    - File compiles without TypeScript errors (run: npx tsc --noEmit from project root)
  </acceptance_criteria>
  <done>Hae uudelleen button calls POST /api/business/reapply; on success updates that venue's claim_status to 'pending' in local state without page reload</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Route file exists: test -f app/api/business/reapply/route.ts
2. Route has UPDATE to pending: grep -c "claim_status.*pending" app/api/business/reapply/route.ts returns >= 1
3. Page calls reapply: grep -c "/api/business/reapply" app/business/page.tsx returns >= 1
4. Page updates state: grep -c "setVenueLinks(prev" app/business/page.tsx returns >= 1
5. TypeScript compiles: npx tsc --noEmit exits 0

Manual test:
- Log in as a business user with a rejected venue on /business
- Click "Hae uudelleen" — status badge should change to pending (amber) without page reload
- Check Supabase: business_paikka_links row shows claim_status='pending', rejection_reason=null
</verification>

<success_criteria>
- POST /api/business/reapply accepts JWT-authenticated requests, finds the rejected row, updates it to pending, and notifies admin
- The "Hae uudelleen" button in /business no longer opens ClaimSearchForm; it directly triggers the reapply API
- Rejected venue's status updates to pending in the UI immediately after the reapply call succeeds
- No 409 unique constraint error on reapply (UPDATE replaces INSERT)
</success_criteria>

<output>
Create .planning/phases/35-admin-hyvaksyntajarjestelma/35-11-SUMMARY.md when done
</output>
