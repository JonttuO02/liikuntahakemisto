---
phase: 35-admin-hyvaksyntajarjestelma
plan: 04
type: execute
wave: 2
depends_on:
  - 35-01
  - 35-02
  - 35-03
files_modified: []
autonomous: false
requirements:
  - ADMIN-01

must_haves:
  truths:
    - "Migration 20260610000002_admin_columns applied to live Supabase project"
    - "RESEND_API_KEY present in .env.local"
    - "Dev server starts without errors after env var changes"
---

<objective>
Apply the Wave 1 migration to the live Supabase project and configure environment variables for Resend email integration.

This is a HUMAN CHECKPOINT — it requires manual steps in the terminal and Supabase dashboard. The agent assists with verification but cannot run supabase db push autonomously.

Prerequisites: 35-01 (migration file), 35-02 (i18n keys), 35-03 (email lib) all complete.

Output: No code changes. Migration applied to DB. Environment variables configured.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
## CHECKPOINT — Manual steps required

This plan has `autonomous: false`. Surface a checkpoint asking the user to complete the following steps before proceeding.

## Steps for user to complete:

### Step 1: Apply migration
```bash
npx supabase db push
```
Expected: Output shows `20260610000002_admin_columns` applied without errors.

### Step 2: Verify columns in Supabase dashboard
- `business_paikka_links` → `rejection_reason TEXT NULL` exists
- `business_accounts` → `role_in_company TEXT NULL` exists

### Step 3: Add env vars to .env.local
```
RESEND_API_KEY=re_...        # Obtain from resend.com dashboard (free tier)
EMAIL_FROM=onboarding@resend.dev  # Use onboarding@resend.dev until domain is verified
ADMIN_EMAIL=joona.orava@gmail.com
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### Step 4: Verify dev server restarts cleanly
```bash
npm run dev
```

## Checkpoint questions:
- [ ] Migration applied without errors?
- [ ] Both new columns visible in Supabase table editor?
- [ ] RESEND_API_KEY added to .env.local?
- [ ] Dev server starts without errors?

## SUMMARY format

After user confirms all steps are done, create SUMMARY.md marking this plan complete.
Note: No files were modified in this plan — only DB state and env vars changed.
</context>
