---
phase: 35-admin-hyvaksyntajarjestelma
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260610000002_admin_columns.sql
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02

must_haves:
  truths:
    - "supabase/migrations/20260610000002_admin_columns.sql exists and contains both ALTER TABLE statements"
    - "File contains ALTER TABLE business_paikka_links ADD COLUMN IF NOT EXISTS rejection_reason TEXT NULL"
    - "File contains ALTER TABLE business_accounts ADD COLUMN IF NOT EXISTS role_in_company TEXT NULL"
  artifacts:
    - path: "supabase/migrations/20260610000002_admin_columns.sql"
      provides: "DB migration adding rejection_reason to business_paikka_links and role_in_company to business_accounts"
---

<objective>
Add the two new columns that Phase 35 features depend on by creating a Supabase migration file.

Purpose: rejection_reason is stored in business_paikka_links so the /business page can display why an application was rejected (D-07). role_in_company is collected at registration so admin sees the applicant's role (D-04). Both columns are nullable with no constraints — no schema validation needed.

Output:
- supabase/migrations/20260610000002_admin_columns.sql — migration adding both columns
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
## Task 1: Create migration file

Create file `supabase/migrations/20260610000002_admin_columns.sql` with this exact content:

```sql
-- Phase 35: Admin approval system columns
-- D-07: rejection_reason stored in business_paikka_links so /business page can display it
-- D-04: role_in_company collected at registration so admin sees the applicant's role

ALTER TABLE business_paikka_links
  ADD COLUMN IF NOT EXISTS rejection_reason TEXT NULL;

ALTER TABLE business_accounts
  ADD COLUMN IF NOT EXISTS role_in_company TEXT NULL;
```

Constraints:
- Migration timestamp MUST be `20260610000002` (greater than the last migration)
- Use `ADD COLUMN IF NOT EXISTS` for idempotency
- Both columns are nullable TEXT — no CHECK constraints needed

## Verification

- File exists at `supabase/migrations/20260610000002_admin_columns.sql`
- Contains both `ALTER TABLE` statements
- No TypeScript changes in this plan
</context>
