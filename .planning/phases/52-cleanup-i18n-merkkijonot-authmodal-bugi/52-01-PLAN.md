---
phase: 52-cleanup-i18n-merkkijonot-authmodal-bugi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/AuthModal.tsx
  - app/business/rekisteroidy/page.tsx
  - app/components/__tests__/AuthModal.mapError.test.ts
  - package.json
  - .planning/phases/52-cleanup-i18n-merkkijonot-authmodal-bugi/52-VERIFICATION.md
autonomous: true
requirements:
  - CLEAN-06
  - CLEAN-07
must_haves:
  truths:
    - "An EN-locale user sees only English strings in AuthModal, CalloutCard, app/paikat/[id]/page.tsx, and DiagonaalKortti (no hardcoded Finnish user-visible strings, the deferred DiagonaalKortti.tsx alt text excepted per D-05)"
    - "mapError classifies a 'password ... 6' message as errorWeakPassword even when the 'Password should be at least' phrase is absent (proves (A || B) && C grouping)"
    - "mapError does NOT classify a 'password' message lacking '6' as errorWeakPassword (proves the old A || B && C bug is gone)"
    - "mapBusinessError exhibits the same correct precedence behavior"
    - "A verification artifact records file+line and git-commit evidence that CLEAN-06 and CLEAN-07 already hold true in current code"
  artifacts:
    - path: "app/components/__tests__/AuthModal.mapError.test.ts"
      provides: "Vitest regression test for mapError and mapBusinessError precedence"
      contains: "describe('mapError'"
      min_lines: 25
    - path: ".planning/phases/52-cleanup-i18n-merkkijonot-authmodal-bugi/52-VERIFICATION.md"
      provides: "CLEAN-06/CLEAN-07 evidence artifact with file+line citations and commit references"
      contains: "CLEAN-06"
    - path: "package.json"
      provides: "test script so npm test runs the Vitest suite"
      contains: "\"test\""
  key_links:
    - from: "app/components/__tests__/AuthModal.mapError.test.ts"
      to: "app/components/AuthModal.tsx"
      via: "imports the real exported mapError (not an inlined copy) via @/app/components/AuthModal"
      pattern: "import.*mapError.*from.*@/app/components/AuthModal"
    - from: "app/components/__tests__/AuthModal.mapError.test.ts"
      to: "app/business/rekisteroidy/page.tsx"
      via: "imports the real exported mapBusinessError via @/app/business/rekisteroidy/page"
      pattern: "import.*mapBusinessError.*from.*@/app/business/rekisteroidy/page"
---

<objective>
Close out CLEAN-06 and CLEAN-07 for Phase 52. RESEARCH.md independently re-confirmed (via fresh file reads, `git show`/`git log`, and grep) that both requirements already hold true in the current codebase: the four named files are fully i18n'd via next-intl, and the `mapError`/`mapBusinessError` operator-precedence bug was already fixed in commit `85eea7a8` (2026-06-04). There is no i18n diff and no precedence diff to apply.

This plan therefore (1) produces a verification artifact (`52-VERIFICATION.md`) capturing concrete file+line and git evidence that both criteria are satisfied, and (2) hardens the already-fixed precedence logic with the project's first automated regression test (RESEARCH.md Pitfall 3 / Open Question 1), which requires exporting the two currently-module-private classifier functions.

Purpose: Provide durable, machine-checkable evidence that CLEAN-06/CLEAN-07 are met, and prevent a future refactor from silently reintroducing the precedence bug.
Output: One Vitest test file, two added `export` keywords, a `test` script in `package.json`, and a verification evidence document.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cleanup-i18n-merkkijonot-authmodal-bugi/52-CONTEXT.md
@.planning/phases/52-cleanup-i18n-merkkijonot-authmodal-bugi/52-RESEARCH.md
@.planning/phases/52-cleanup-i18n-merkkijonot-authmodal-bugi/52-PATTERNS.md
@.planning/phases/52-cleanup-i18n-merkkijonot-authmodal-bugi/52-VALIDATION.md
@app/components/AuthModal.tsx
@app/business/rekisteroidy/page.tsx
@vitest.config.ts
</context>

<scope_lock>
Scope is locked by CONTEXT.md D-03 to exactly these four files for i18n verification: `app/components/AuthModal.tsx`, `app/components/CalloutCard.tsx`, `app/paikat/[id]/page.tsx`, `app/components/DiagonaalKortti.tsx`. Do NOT sweep the rest of the app for hardcoded Finnish strings.

The ONLY source-code changes permitted this phase are: adding the `export` keyword to `mapError` (AuthModal.tsx) and `mapBusinessError` (rekisteroidy/page.tsx), and adding a `test` script to package.json. No i18n string is added/changed/removed. The precedence logic is NOT modified — it is already correct.

DO NOT TOUCH `app/components/DiagonaalKortti.tsx` line ~224 `alt={\`Kuva: ${paikka.nimi}\`}`. It is a known hardcoded Finnish screen-reader string explicitly DEFERRED by the user (D-05). Do not create a task for it, do not "fix" it, do not mark it decorative. It is out of scope.
</scope_lock>

<artifacts_this_phase_produces>
- `app/components/__tests__/AuthModal.mapError.test.ts` (NEW) — Vitest regression suite covering `mapError` and `mapBusinessError` precedence behavior.
- `mapError` — newly EXPORTED named function in `app/components/AuthModal.tsx` (was module-private; the file's default export of the component is unchanged).
- `mapBusinessError` — newly EXPORTED named function in `app/business/rekisteroidy/page.tsx` (was module-private; the file's default export of the page component is unchanged).
- `package.json` `scripts.test` = `"vitest run"` (NEW script; `npm test` now runs the suite).
- `.planning/phases/52-cleanup-i18n-merkkijonot-authmodal-bugi/52-VERIFICATION.md` (NEW) — evidence artifact closing out CLEAN-06 and CLEAN-07.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify CLEAN-06 i18n coverage and produce evidence artifact</name>
  <files>.planning/phases/52-cleanup-i18n-merkkijonot-authmodal-bugi/52-VERIFICATION.md</files>
  <read_first>
    - app/components/AuthModal.tsx (confirm `useTranslations('Auth')` present, no hardcoded Finnish user-visible strings)
    - app/components/CalloutCard.tsx (confirm `useTranslations('PaikkaKortti')` / `useTranslations('Lajit')`)
    - app/paikat/[id]/page.tsx (confirm `await getTranslations('PaikkaPage')`)
    - app/components/DiagonaalKortti.tsx (confirm `useTranslations` present; note line ~224 alt text is the ONLY hardcoded Finnish string and is DEFERRED — do not change it)
    - .planning/phases/52-cleanup-i18n-merkkijonot-authmodal-bugi/52-RESEARCH.md (Phase Requirements table + Pitfall 2 — code comments are NOT violations)
    - .planning/phases/52-cleanup-i18n-merkkijonot-authmodal-bugi/52-CONTEXT.md (D-03 scope lock, D-05 deferred item)
  </read_first>
  <action>
    This is verification + documentation, NOT i18n implementation. Do not add or modify any next-intl call or message key — every user-visible string in the four files already routes through `t()`/`tLajit()`.

    Run the verification commands and capture their actual output as evidence:
    - Confirm next-intl wiring: grep for `useTranslations` and `getTranslations` across the four files (AuthModal.tsx, CalloutCard.tsx, app/paikat/[id]/page.tsx, DiagonaalKortti.tsx). Expect at least one hit per file.
    - Confirm no NEW user-visible hardcoded Finnish strings: grep each of the four files for Finnish-specific characters (the set a-with-diaeresis, o-with-diaeresis, a-with-ring, and their uppercase forms). For every match, classify it in the artifact as either a code comment (NOT a violation, per RESEARCH Pitfall 2) or the single deferred DiagonaalKortti alt attribute (line ~224, deferred per D-05). There must be zero matches that are user-visible AND not the deferred alt text.

    Create `52-VERIFICATION.md` recording, for CLEAN-06: each of the four files, the exact namespace it consumes, the line of its `useTranslations`/`getTranslations` call, and the grep classification result. Explicitly document the deferred `DiagonaalKortti.tsx` alt string as intentionally out of scope per D-05 (cite the decision), so a future reader knows it was a deliberate exclusion, not an oversight. Reference the verified EN namespaces in `messages/en.json` (Auth, PaikkaKortti, PaikkaPage, Lajit) per RESEARCH.md.

    Do not echo Finnish literal example strings into committed prose in a way that would itself trip a negative grep — describe matches by file and line number plus an English gloss rather than pasting the raw literal where avoidable.
  </action>
  <verify>
    <automated>grep -lE "useTranslations|getTranslations" app/components/AuthModal.tsx app/components/CalloutCard.tsx "app/paikat/[id]/page.tsx" app/components/DiagonaalKortti.tsx | grep -c . | grep -q '^4$' && test -f .planning/phases/52-cleanup-i18n-merkkijonot-authmodal-bugi/52-VERIFICATION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `52-VERIFICATION.md` exists and contains a CLEAN-06 section.
    - All four named files contain at least one `useTranslations`/`getTranslations` call (grep proves 4/4 files matched).
    - The artifact classifies every Finnish-character grep match in the four files as either a code comment or the single deferred `DiagonaalKortti.tsx` alt attribute — zero unexplained user-visible matches.
    - The artifact explicitly cites D-05 when noting the deferred DiagonaalKortti alt string is out of scope.
    - No next-intl call or message key was added, changed, or removed in any source file (this task touches only the verification doc).
  </acceptance_criteria>
  <done>CLEAN-06 is documented as already-satisfied with concrete file+line evidence, and the one deferred item is recorded as a deliberate D-05 exclusion. No source files changed by this task.</done>
</task>

<task type="auto">
  <name>Task 2: Export classifiers, add CLEAN-07 regression test, add test script, record CLEAN-07 evidence</name>
  <files>app/components/AuthModal.tsx, app/business/rekisteroidy/page.tsx, app/components/__tests__/AuthModal.mapError.test.ts, package.json, .planning/phases/52-cleanup-i18n-merkkijonot-authmodal-bugi/52-VERIFICATION.md</files>
  <read_first>
    - app/components/AuthModal.tsx (lines 20-35 — `mapError`, currently `function mapError`, module-private; default export of component at line 37)
    - app/business/rekisteroidy/page.tsx (lines 10-27 — `mapBusinessError`, currently `function mapBusinessError`, module-private)
    - vitest.config.ts (include glob requires test at `app/**/__tests__/*.test.ts`; `@` alias maps to project root)
    - .planning/phases/52-cleanup-i18n-merkkijonot-authmodal-bugi/52-PATTERNS.md (test structure, import-alias convention, the "export the real function, do not inline" instruction)
    - .planning/phases/52-cleanup-i18n-merkkijonot-authmodal-bugi/52-RESEARCH.md (Validation Architecture — exact run command, Pitfall 3, Code Examples showing current correct precedence)
  </read_first>
  <action>
    Step A — export the real classifiers (the one sanctioned source change). Change `function mapError(` to `export function mapError(` in `app/components/AuthModal.tsx`, and `function mapBusinessError(` to `export function mapBusinessError(` in `app/business/rekisteroidy/page.tsx`. These are behavior-preserving no-ops at runtime: no call site changes, the default exports stay exactly as they are. Do NOT touch the precedence logic itself — the `(A || B) && C` grouping is already correct (AuthModal lines 27-31; rekisteroidy lines 20-23) and is what the test will assert.

    Step B — create `app/components/__tests__/AuthModal.mapError.test.ts`. Import the REAL functions (do not inline/duplicate the logic): `mapError` from `@/app/components/AuthModal` and `mapBusinessError` from `@/app/business/rekisteroidy/page`. Use Vitest's `describe`/`it`/`expect`. Cover at minimum these cases for `mapError`:
    - invalid credentials message resolves to `errorInvalidCredentials`
    - duplicate-registration message resolves to `errorEmailInUse`
    - PRECEDENCE REGRESSION: a message containing the lowercase word `password` AND the digit `6` but NOT the phrase `Password should be at least` resolves to `errorWeakPassword` (proves the `(A || B)` left branch ANDs with `C`, i.e. `(A || B) && C` grouping)
    - PRECEDENCE GUARD: a message containing `password` but NO `6` resolves to `errorGeneric` (the old ungrouped `A || B && C` form would have wrongly returned `errorWeakPassword` here because `B && C` binds tighter than `||`; this case must NOT be weak-password)
    - an unrecognized message resolves to `errorGeneric`
    For `mapBusinessError`, cover at least the same two precedence cases (the regression case resolves to `errorWeakPassword`; the no-`6` case resolves to `errorGeneric`) plus the duplicate-registration case. Name the weak-password regression test so it is selectable via `-t "weak password"` per RESEARCH.md's cited command.

    Step C — add `"test": "vitest run"` to the `scripts` object in `package.json` so `npm test` runs the suite (currently no `test` script exists).

    Step D — append a CLEAN-07 section to `52-VERIFICATION.md` recording the git evidence (precedence fix shipped in commit `85eea7a8`, 2026-06-04), the current correct line locations (AuthModal.tsx 27-31, rekisteroidy/page.tsx 20-23), the fact that the two functions are now exported solely for testability (cite PATTERNS.md Option 1), and that the new regression test now guards the behavior.
  </action>
  <verify>
    <automated>npx vitest run app/components/__tests__/AuthModal.mapError.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/components/__tests__/AuthModal.mapError.test.ts` passes with all assertions green.
    - The test file imports `mapError` from `@/app/components/AuthModal` and `mapBusinessError` from `@/app/business/rekisteroidy/page` (real functions, not inlined copies).
    - `mapError` and `mapBusinessError` are both exported (`export function` prefix present in their source files); their default component/page exports are unchanged and the precedence expressions are byte-for-byte unchanged.
    - `npx vitest run app/components/__tests__/AuthModal.mapError.test.ts -t "weak password"` selects and passes the weak-password regression case.
    - `package.json` `scripts.test` equals `vitest run`.
    - `52-VERIFICATION.md` has a CLEAN-07 section citing commit `85eea7a8` and the current line locations.
  </acceptance_criteria>
  <done>The precedence behavior of both classifiers is covered by a passing automated regression test that exercises the real production functions, `npm test` is wired, and CLEAN-07 is documented as already-fixed-and-now-guarded. No precedence logic or i18n string was modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This phase introduces no new trust boundary. It exports two pure string-classification functions for testing, adds a test file, a `package.json` script, and a documentation artifact. No new input is parsed, no auth/session/token logic is touched, no network or storage surface changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Information Disclosure | `mapError` / `mapBusinessError` becoming exported | accept | Exporting a pure, side-effect-free function that maps a Supabase error string to a static i18n key reveals no secret and changes no runtime behavior; it is invoked identically to before. No sensitive data flows through these functions. |
| T-52-02 | Tampering | npm/pip/cargo installs | mitigate | No new packages are installed this phase (next-intl and Vitest are pre-existing dependencies; RESEARCH.md "Package Legitimacy Audit: Not applicable"). No install task exists, so no supply-chain checkpoint is required. |

**Rationale (per RESEARCH.md Security Domain):** ASVS V2 (Authentication), V3 (Session), V4 (Access Control), V5 (Input Validation), V6 (Cryptography) all assessed "no" — this phase only verifies localization display text (already done) and adds a regression test plus the `export` keyword for two pure functions. There is no applicable attack-surface change. This block is included to satisfy the active security gate (`security_asvs_level: 1`, `security_block_on: high`) and documents the "no applicable threats" conclusion explicitly rather than omitting it.
</threat_model>

<verification>
Phase-level checks (run before `/gsd-verify-work`):
- `npx vitest run` — full suite green (this plan's test is the first and only test in the repo).
- `npm test` — resolves via the new `scripts.test` and runs the same suite green.
- CLEAN-06: `52-VERIFICATION.md` exists with a CLEAN-06 evidence section; grep confirms 4/4 named files use next-intl; the only hardcoded Finnish string in scope is the documented, deferred `DiagonaalKortti.tsx` alt attribute (D-05, untouched).
- CLEAN-07: `52-VERIFICATION.md` has a CLEAN-07 section citing commit `85eea7a8`; the precedence regression test passes, including the `-t "weak password"` selector.
- Confirm DiagonaalKortti.tsx line ~224 was NOT modified (deferred per D-05).
- Confirm no message key was added/changed/removed in `messages/en.json` or `messages/fi.json`.
</verification>

<success_criteria>
- ROADMAP success criterion 1 (EN AuthModal loading + mode-toggle strings) — verified via file+line evidence in `52-VERIFICATION.md`; no diff needed (already i18n'd).
- ROADMAP success criterion 2 (EN CalloutCard, paikkasivu location row, DiagonaalKortti aria-label) — verified via file+line evidence; deferred alt string documented per D-05.
- ROADMAP success criterion 3 (precedence bug fixed) — git evidence (commit `85eea7a8`) recorded AND a passing automated regression test now guards both `mapError` and `mapBusinessError`.
- `npm test` runs the Vitest suite green; the project now has its first automated test.
</success_criteria>

<output>
Create `.planning/phases/52-cleanup-i18n-merkkijonot-authmodal-bugi/52-01-SUMMARY.md` when done.
</output>
