---
phase: 33-claim-paikan-luonti
plan: 03
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - app/api/business/claim-paikka/route.ts
  - app/api/business/create-paikka/route.ts
autonomous: true
requirements:
  - CLAIM-01
  - CLAIM-02
  - CLAIM-03

must_haves:
  truths:
    - "POST /api/business/claim-paikka validates JWT, inserts into business_paikka_links (link_type='claim', claim_status='pending'), sets is_claimed=true on liikuntapaikat"
    - "POST /api/business/create-paikka validates JWT, inserts into liikuntapaikat (published=false, business_managed=true), inserts into business_paikka_links (link_type='created', claim_status='pending'), sets is_claimed=true — all atomically"
    - "Both route handlers reject unauthenticated requests with 401"
    - "create-paikka validates that nimi and osoite are non-empty strings before inserting"
    - "claim-paikka returns 409 if UNIQUE(paikka_id) constraint is violated (already claimed)"
  artifacts:
    - path: "app/api/business/claim-paikka/route.ts"
      provides: "Claim route handler"
      exports: ["POST"]
    - path: "app/api/business/create-paikka/route.ts"
      provides: "Create venue route handler"
      exports: ["POST"]
  key_links:
    - from: "app/api/business/claim-paikka/route.ts"
      to: "supabaseAdmin"
      via: "import from @/lib/supabaseAdmin.server"
      pattern: "supabaseAdmin.from('business_paikka_links').insert"
    - from: "app/api/business/create-paikka/route.ts"
      to: "supabaseAdmin"
      via: "import from @/lib/supabaseAdmin.server"
      pattern: "supabaseAdmin.from('liikuntapaikat').insert"
---

<objective>
Create two POST Route Handlers following the exact pattern of app/api/business/register/route.ts:
- /api/business/claim-paikka: links an existing venue to the business account
- /api/business/create-paikka: creates a new hidden venue and links it to the business account

Purpose: CLAIM-01 and CLAIM-02 require server-side handlers that safely write to business_paikka_links using service role key (bypassing RLS). JWT from Authorization header is verified before any DB write to prevent spoofing of user_id.

Output: Two new route.ts files under app/api/business/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-claim-paikan-luonti/33-CONTEXT.md
@.planning/phases/31-db-skeema-storage-perusta/31-CONTEXT.md

<interfaces>
<!-- From app/api/business/register/route.ts — follow this pattern exactly: -->
import { NextResponse } from 'next/server'
import { supabaseAdmin } from '@/lib/supabaseAdmin.server'

export async function POST(request: Request) {
  // 1. Verify JWT from Authorization header
  const authHeader = request.headers.get('Authorization')
  const token = authHeader?.replace('Bearer ', '') ?? ''
  const { data: { user }, error: authError } = await supabaseAdmin.auth.getUser(token)
  if (authError || !user) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }
  // 2. Parse and validate body
  // 3. DB write using user.id (never body.user_id)
  // 4. Return NextResponse.json({ ok: true })
}

<!-- business_paikka_links schema (from 20260605000000_business_accounts.sql):
  id                  BIGSERIAL PRIMARY KEY
  business_account_id UUID NOT NULL REFERENCES business_accounts(user_id) ON DELETE CASCADE
  paikka_id           BIGINT NOT NULL REFERENCES liikuntapaikat(id) ON DELETE CASCADE
  claim_status        TEXT NOT NULL DEFAULT 'pending' CHECK (claim_status IN ('pending', 'approved', 'rejected'))
  link_type           TEXT NOT NULL CHECK (link_type IN ('claim', 'created'))
  created_at          TIMESTAMPTZ NOT NULL DEFAULT now()
  UNIQUE(paikka_id)
-->

<!-- liikuntapaikat columns needed for create-paikka INSERT:
  nimi TEXT, osoite TEXT, kaupunki TEXT,
  published BOOLEAN (set false — from migration 20260605000004),
  business_managed BOOLEAN (set true — from migration 20260605000001)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create POST /api/business/claim-paikka route handler</name>
  <files>app/api/business/claim-paikka/route.ts</files>
  <read_first>
    - app/api/business/register/route.ts — JWT verification + supabaseAdmin pattern to replicate exactly
    - lib/supabaseAdmin.server.ts — import path: @/lib/supabaseAdmin.server
    - .planning/phases/33-claim-paikan-luonti/33-CONTEXT.md — D-11 for claim logic
    - supabase/migrations/20260605000000_business_accounts.sql — business_paikka_links schema (link_type, claim_status values)
  </read_first>
  <behavior>
    - Request with no Authorization header → 401 { error: 'Unauthorized' }
    - Request with invalid token → 401 { error: 'Unauthorized' }
    - Request with missing paikka_id in body → 400 { error: 'Missing fields' }
    - Request with non-numeric paikka_id → 400 { error: 'Missing fields' }
    - Valid request with paikka_id already claimed (UNIQUE violation) → 409 { error: 'Already claimed' }
    - Valid request with available paikka_id → inserts business_paikka_links row + updates is_claimed, returns 200 { ok: true }
  </behavior>
  <action>
    Create app/api/business/claim-paikka/route.ts following the register/route.ts pattern exactly:

    1. JWT verification: same pattern as register — Authorization header → supabaseAdmin.auth.getUser(token) → 401 if error or no user.

    2. Body parsing: parse JSON, extract paikka_id. Validate: must be a number (parseInt, must not be NaN). If invalid, return 400 { error: 'Missing fields' }.

    3. INSERT into business_paikka_links using supabaseAdmin (service role — bypasses RLS):
       { business_account_id: user.id, paikka_id: parsedPaikkaId, link_type: 'claim', claim_status: 'pending' }
       
    4. Handle UNIQUE(paikka_id) constraint violation: if the error code is '23505' (PostgreSQL unique_violation), return 409 { error: 'Already claimed' }. For other errors, return 500.

    5. On successful INSERT: UPDATE liikuntapaikat SET is_claimed = true WHERE id = parsedPaikkaId using supabaseAdmin. If UPDATE fails, log but do not rollback (non-critical — the claim link exists; is_claimed is a denormalized cache).

    6. Return NextResponse.json({ ok: true }) on success.

    Do NOT change the existing venue's published status (per D-11 — claim-paikka pysyy näkyvänä).
    Use user.id from verified JWT for business_account_id — never trust body.user_id.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep "claim-paikka" || echo "TypeScript OK for claim-paikka"</automated>
  </verify>
  <done>app/api/business/claim-paikka/route.ts exists, exports POST, imports supabaseAdmin from @/lib/supabaseAdmin.server, handles 401/400/409/500 cases, inserts into business_paikka_links with link_type='claim', updates is_claimed=true. TypeScript compiles without errors in this file.</done>
</task>

<task type="auto">
  <name>Task 2: Create POST /api/business/create-paikka route handler</name>
  <files>app/api/business/create-paikka/route.ts</files>
  <read_first>
    - app/api/business/register/route.ts — JWT verification + atomicity rollback pattern
    - app/api/business/claim-paikka/route.ts — follow same structure as Task 1 output
    - .planning/phases/33-claim-paikan-luonti/33-CONTEXT.md — D-10 for create logic (atomic INSERT order)
    - supabase/migrations/20260605000004_published_is_claimed.sql — confirms published and is_claimed columns exist
  </read_first>
  <behavior>
    - Request with no Authorization header → 401 { error: 'Unauthorized' }
    - Request with missing nimi or empty nimi → 400 { error: 'Missing fields' }
    - Request with missing osoite or empty osoite → 400 { error: 'Missing fields' }
    - Request with missing kaupunki or empty kaupunki → 400 { error: 'Missing fields' }
    - Valid request → INSERT liikuntapaikat (published=false, business_managed=true) → INSERT business_paikka_links (link_type='created') → UPDATE is_claimed=true → 200 { ok: true, paikka_id: number }
    - liikuntapaikat INSERT fails → 500 { error: 'Insert failed' } (no business_paikka_links insert attempted)
    - business_paikka_links INSERT fails → DELETE newly created liikuntapaikat row (rollback), return 500
  </behavior>
  <action>
    Create app/api/business/create-paikka/route.ts following the register/route.ts pattern:

    1. JWT verification: same pattern — Authorization header → supabaseAdmin.auth.getUser(token) → 401 if error or no user.

    2. Body parsing: parse JSON, extract nimi, osoite, kaupunki. Each must be a non-empty string after trim() and slice(0, 500). If any is empty/missing, return 400 { error: 'Missing fields' }.

    3. Atomic INSERT sequence (per D-10):
       a. INSERT INTO liikuntapaikat: { nimi, osoite, kaupunki, published: false, business_managed: true }
          Use .select('id').single() to get the new paikka_id.
          If error → return 500 { error: 'Insert failed', detail: error.message }
       
       b. INSERT INTO business_paikka_links: { business_account_id: user.id, paikka_id: newPaikkaId, link_type: 'created', claim_status: 'pending' }
          If error → rollback by deleting the new liikuntapaikat row:
            supabaseAdmin.from('liikuntapaikat').delete().eq('id', newPaikkaId)
          Return 500 { error: 'Link insert failed', detail: error.message }
       
       c. UPDATE liikuntapaikat SET is_claimed = true WHERE id = newPaikkaId.
          If UPDATE fails, log but do not rollback (non-critical).

    4. Return NextResponse.json({ ok: true, paikka_id: newPaikkaId }) — paikka_id returned for future use by Phase 34 onboarding wizard.

    Use user.id from verified JWT for business_account_id — never trust body.user_id.
    Limit all string fields to 500 chars to prevent oversized INSERTs.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep "create-paikka" || echo "TypeScript OK for create-paikka"</automated>
  </verify>
  <done>app/api/business/create-paikka/route.ts exists, exports POST, handles all validation cases, performs atomic INSERT to liikuntapaikat then business_paikka_links, sets published=false and business_managed=true, includes rollback on link insert failure. TypeScript compiles without errors in this file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/business/claim-paikka | Untrusted: paikka_id from request body |
| client → /api/business/create-paikka | Untrusted: nimi, osoite, kaupunki from request body |
| Route Handler → Supabase DB | Trusted via service role key (server-side only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03-01 | Elevation of Privilege | claim-paikka: business_account_id | mitigate | user.id from JWT verification (supabaseAdmin.auth.getUser) — body.user_id never used |
| T-33-03-02 | Elevation of Privilege | create-paikka: business_account_id | mitigate | Same — user.id from JWT, not body |
| T-33-03-03 | Tampering | create-paikka: nimi/osoite injection | mitigate | trim() + slice(0,500); Supabase parameterized queries prevent SQL injection |
| T-33-03-04 | Denial of Service | claim-paikka: UNIQUE violation | mitigate | Returns 409, not 500; client shows errorClaimAlreadyTaken |
| T-33-03-05 | Tampering | create-paikka: published bypass | mitigate | published=false hardcoded in route handler INSERT, not from body |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
TypeScript compilation passes (npx tsc --noEmit) with no errors referencing claim-paikka or create-paikka. Both files export an async POST function. curl -X POST /api/business/claim-paikka with no Authorization header returns 401.
</verification>

<success_criteria>
Both route handlers exist and compile. claim-paikka inserts business_paikka_links with link_type='claim' and returns 409 on duplicate paikka_id. create-paikka inserts liikuntapaikat with published=false, business_managed=true, then inserts business_paikka_links with link_type='created', with rollback on link insert failure. Both set is_claimed=true on the liikuntapaikat row.
</success_criteria>

<output>
Create `.planning/phases/33-claim-paikan-luonti/33-03-SUMMARY.md` when done
</output>
