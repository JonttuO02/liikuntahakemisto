---
phase: 33-claim-paikan-luonti
plan: 04
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - app/page.tsx
  - app/api/hae-paikat/route.ts
autonomous: true
requirements:
  - CLAIM-03

must_haves:
  truths:
    - "app/page.tsx query includes .eq('published', true) so published=false venues are excluded from the homepage"
    - "All existing venues (published defaults to true from migration) continue to appear on the homepage"
  artifacts:
    - path: "app/page.tsx"
      provides: "Homepage query with published filter"
      contains: ".eq('published', true)"
  key_links:
    - from: "app/page.tsx"
      to: "liikuntapaikat table"
      via: "supabase query with .eq('published', true)"
      pattern: "eq\\('published', true\\)"
---

<objective>
Add .eq('published', true) filter to the Supabase query in app/page.tsx so that venues with published=false (created by businesses pending admin approval) are invisible to regular users.

Purpose: CLAIM-03 (D-09) — new venues created via create-paikka are stored with published=false. The user-facing homepage must not show them until admin approves. This is a single-line change to the existing select query.

Output: Modified app/page.tsx with published filter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-claim-paikan-luonti/33-CONTEXT.md

<interfaces>
<!-- Current app/page.tsx query (to be modified): -->
const { data: paikat, error } = await supabase
  .from('liikuntapaikat')
  .select('id, nimi, laji, osoite, kaupunki, latitude, longitude, hinta_min, hinta_max, varauslinkki, kuvaus, puhelin, aukioloajat, hinta_kuvaus, featured')
  .order('nimi')

<!-- After modification it must be: -->
const { data: paikat, error } = await supabase
  .from('liikuntapaikat')
  .select('id, nimi, laji, osoite, kaupunki, latitude, longitude, hinta_min, hinta_max, varauslinkki, kuvaus, puhelin, aukioloajat, hinta_kuvaus, featured')
  .eq('published', true)
  .order('nimi')

<!-- The supabase client is the anon key client from lib/supabase.ts (not supabaseAdmin).
     The published column is public-readable after migration 20260605000004. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .eq('published', true) filter to app/page.tsx</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx — read in full; confirm current query structure before editing
    - .planning/phases/33-claim-paikan-luonti/33-CONTEXT.md — D-09 confirms this filter must be added here
  </read_first>
  <action>
    Open app/page.tsx. Locate the Supabase query chain:
      .from('liikuntapaikat')
      .select('...')
      .order('nimi')

    Insert .eq('published', true) between .select(...) and .order('nimi'). The result must be:
      .from('liikuntapaikat')
      .select('id, nimi, laji, osoite, kaupunki, latitude, longitude, hinta_min, hinta_max, varauslinkki, kuvaus, puhelin, aukioloajat, hinta_kuvaus, featured')
      .eq('published', true)
      .order('nimi')

    No other changes to the file. The supabase import, Suspense wrapper, Etusivu component, and error handling remain identical.
  </action>
  <verify>
    <automated>grep -c "\.eq('published', true)" app/page.tsx</automated>
  </verify>
  <done>.eq('published', true) is present in the query chain in app/page.tsx. grep count returns 1. The file still imports supabase from @/lib/supabase and renders Etusivu. TypeScript compilation passes.</done>
</task>

<task type="auto">
  <name>Task 2: Verify app/api/hae-paikat/route.ts per D-09</name>
  <files>app/api/hae-paikat/route.ts</files>
  <read_first>
    - app/api/hae-paikat/route.ts — read in full; confirm the route's purpose and whether it has any user-facing SELECT that could leak published=false venues
    - .planning/phases/33-claim-paikan-luonti/33-CONTEXT.md — D-09 requires published filter on this route
  </read_first>
  <action>
    Read app/api/hae-paikat/route.ts and verify it contains:
    1. Authorization header check: `req.headers.get('authorization') !== 'Bearer ${process.env.ADMIN_SECRET}'` — this is an admin-only sync route, not user-facing
    2. No SELECT that returns venues to end users — it only calls supabaseAdmin.from('liikuntapaikat').upsert(...)
    
    Since this route only performs admin-protected upserts (it syncs Tampere venues FROM Google Places API to the DB) and has no user-facing SELECT, D-09's published filter requirement is satisfied by the user-facing query filter in Task 1 (app/page.tsx). No code change is needed in this file.
    
    If the route does contain a user-facing SELECT (i.e., it returns liikuntapaikat rows to end users without ADMIN_SECRET protection), add .eq('published', true) to that query. Otherwise confirm and move on.
  </action>
  <verify>
    <automated>grep -c "ADMIN_SECRET" app/api/hae-paikat/route.ts</automated>
  </verify>
  <done>app/api/hae-paikat/route.ts has been read and confirmed. Either: (a) the route is admin-protected and performs only upsert operations — no user-facing SELECT exists, no code change needed, D-09 filter requirement covered by app/page.tsx Task 1; OR (b) if a user-facing SELECT exists, .eq('published', true) has been added to it. The ADMIN_SECRET check count is ≥ 1 confirming admin-only access.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase anon query → liikuntapaikat | Public read RLS; published filter enforced at query level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04-01 | Information Disclosure | published=false venues | mitigate | .eq('published', true) in query; Supabase RLS public_read does not expose published column value to filter bypassing |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
grep -c ".eq('published', true)" app/page.tsx returns 1. npx tsc --noEmit passes. The homepage renders the same venue list as before (all existing venues have published=true by default from migration).
</verification>

<success_criteria>
app/page.tsx query filters by published=true. All existing venues still appear (DEFAULT true from migration). New venues with published=false created by businesses do not appear on homepage.
</success_criteria>

<output>
Create `.planning/phases/33-claim-paikan-luonti/33-04-SUMMARY.md` when done
</output>
