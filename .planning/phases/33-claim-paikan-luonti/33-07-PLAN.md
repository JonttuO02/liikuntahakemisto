---
phase: 33-claim-paikan-luonti
plan: 07
type: execute
wave: 5
depends_on:
  - 33-01
  - 33-02
  - 33-03
  - 33-04
  - 33-05
  - 33-06
files_modified: []
autonomous: false
requirements:
  - CLAIM-01
  - CLAIM-02
  - CLAIM-03

must_haves:
  truths:
    - "supabase db push completes without errors"
    - "liikuntapaikat table has published BOOLEAN NOT NULL DEFAULT true column in Supabase"
    - "liikuntapaikat table has is_claimed BOOLEAN NOT NULL DEFAULT false column in Supabase"
    - "All existing venues are visible on homepage (published defaults to true)"
    - "Business user can complete claim flow end-to-end: search → select → Ota haltuun → Path B"
    - "Business user can complete create flow end-to-end: search → Luo uusi paikka → submit → Path B"
  artifacts:
    - path: "supabase/migrations/20260605000004_published_is_claimed.sql"
      provides: "Pushed to Supabase DB"
      contains: "published BOOLEAN"
  key_links:
    - from: "supabase/migrations/20260605000004_published_is_claimed.sql"
      to: "Supabase DB liikuntapaikat table"
      via: "npx supabase db push"
      pattern: "published"
---

<objective>
Push the Phase 33 migration to Supabase and run a full smoke test verifying both the claim and create flows work end-to-end. This is the blocking final plan — Phase 33 cannot be marked complete without the schema push succeeding.

Purpose: Migrations are applied to Supabase. The published filter in app/page.tsx, the is_claimed flag in route handlers, and the ClaimSearchForm's claim status display all depend on these columns existing in the live DB.

Output: Supabase DB updated with published + is_claimed columns. Smoke test pass confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/33-claim-paikan-luonti/33-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push migration to Supabase</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260605000004_published_is_claimed.sql — verify file content before pushing
    - .planning/phases/31-db-skeema-storage-perusta/31-CONTEXT.md — confirm supabase db push is the correct push command (same as Phase 31)
  </read_first>
  <action>
    Run: npx supabase db push

    This pushes all pending migrations including 20260605000004_published_is_claimed.sql.

    If the command fails:
    - "column already exists" → migration used IF NOT EXISTS guard so this should not occur; if it does, the column was added manually — verify columns exist and continue
    - "permission denied" → check SUPABASE_ACCESS_TOKEN is set or run `npx supabase login` first
    - Any other error → report the exact error message; do not attempt to fix schema manually

    After successful push, verify the columns exist by running this SQL in Supabase SQL editor or via the CLI:
    SELECT column_name, data_type, column_default
    FROM information_schema.columns
    WHERE table_name = 'liikuntapaikat'
      AND column_name IN ('published', 'is_claimed')
    ORDER BY column_name;

    Expected result: 2 rows returned, both data_type = 'boolean', published default = 'true', is_claimed default = 'false'.
  </action>
  <verify>
    <automated>npx supabase db push 2>&1 | tail -5</automated>
  </verify>
  <done>npx supabase db push completes with no errors. The published and is_claimed columns are confirmed present in liikuntapaikat via information_schema query.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test — claim and create flows end-to-end</name>
  <what-built>
    Phase 33 complete implementation:
    - DB: published + is_claimed columns on liikuntapaikat
    - API: /api/business/claim-paikka and /api/business/create-paikka route handlers
    - UI: /business page with ClaimSearchForm (3 steps) and status placeholder
    - Filter: app/page.tsx filters by published=true
  </what-built>
  <how-to-verify>
    Run the dev server: npm run dev

    **Test 1: Homepage published filter**
    1. Visit http://localhost:3000
    2. Confirm all previously visible venues still appear (all have published=true by default)

    **Test 2: Claim flow (Path A → Path B via claim)**
    1. Log in as a business user (one registered via /business/rekisteroidy in Phase 32)
    2. Visit http://localhost:3000/business
    3. Confirm: Path A renders — "Hae tai luo paikka" heading + search form visible
    4. Type at least 2 characters in the search field
    5. Confirm: results appear (max 8, showing nimi, osoite, laji)
    6. If any result shows "JO HALLITTU" badge — it should be non-interactive (cursor-not-allowed)
    7. Click "Valitse" on an unclaimed venue
    8. Confirm: Step 2 appears with "Valittu paikka" label, venue name, and "Ota haltuun" button
    9. Click "Ota haltuun"
    10. Confirm: Page refreshes and now shows Path B — "Hakemuksesi on vastaanotettu" + venue name + "Odottaa admin-hyväksyntää..."

    **Test 3: Create flow (Path A → Path B via create)**
    If you need to test with a fresh business account (no existing links):
    1. Register a new business account at /business/rekisteroidy
    2. Visit http://localhost:3000/business — Path A should show
    3. Type at least 2 characters in search field
    4. Click "Luo uusi paikka sen sijaan →"
    5. Confirm: Step 3 appears with nimi, osoite, kaupunki fields and "Luo paikka" button
    6. Fill in: nimi="Testi Sali", osoite="Testikatu 1", kaupunki=Tampere
    7. Click "Luo paikka"
    8. Confirm: Page shows Path B — "Hakemuksesi on vastaanotettu" + "Paikka: Testi Sali" + pending message

    **Test 4: New venue hidden on homepage**
    1. Visit http://localhost:3000
    2. Confirm "Testi Sali" (created in Test 3) does NOT appear in the venue list
    3. Verify in Supabase SQL editor: SELECT published FROM liikuntapaikat WHERE nimi='Testi Sali' → should return false

    **Test 5: is_claimed flag set correctly**
    In Supabase SQL editor, verify claimed venues:
    SELECT id, nimi, is_claimed FROM liikuntapaikat WHERE is_claimed = true;
    The venue claimed in Test 2 should appear with is_claimed = true.

    Report any failures with exact error messages or screenshots.
  </how-to-verify>
  <resume-signal>Type "approved" when all 5 tests pass, or describe specific failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npx supabase db push → Supabase DB | Network: migration pushed via Supabase Management API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07-01 | Tampering | Migration push | mitigate | IF NOT EXISTS guards prevent double-application; migration file is version-controlled |
| T-33-07-02 | Denial of Service | DB push timeout | accept | Rare; retry is safe due to IF NOT EXISTS guards |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | supabase CLI already installed |
</threat_model>

<verification>
npx supabase db push exits 0. information_schema query returns both columns. Smoke tests confirm claim and create flows work end-to-end. New venue (published=false) does not appear on homepage. is_claimed=true set on claimed venue.
</verification>

<success_criteria>
Supabase DB has published and is_claimed columns. Homepage shows only published venues. Business user can claim an existing venue (Path B shows pending status). Business user can create a new venue (Path B shows pending status; venue not visible on homepage). Human verifier approves all 5 smoke tests.
</success_criteria>

<output>
Create `.planning/phases/33-claim-paikan-luonti/33-07-SUMMARY.md` when done
</output>
