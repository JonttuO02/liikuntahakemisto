---
phase: 33-claim-paikan-luonti
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260605000004_published_is_claimed.sql
autonomous: true
requirements:
  - CLAIM-03

must_haves:
  truths:
    - "A migration file exists that adds published BOOLEAN NOT NULL DEFAULT true to liikuntapaikat"
    - "A migration file exists that adds is_claimed BOOLEAN NOT NULL DEFAULT false to liikuntapaikat"
    - "New venues created with published = false are invisible to user-facing queries (enforced at app layer)"
    - "The is_claimed column is public-readable so anon queries can show 'Jo hallittu' status"
  artifacts:
    - path: "supabase/migrations/20260605000004_published_is_claimed.sql"
      provides: "DB columns for visibility and claim status tracking"
      contains: "published BOOLEAN"
  key_links:
    - from: "supabase/migrations/20260605000004_published_is_claimed.sql"
      to: "app/page.tsx"
      via: ".eq('published', true) filter added in Plan 33-04"
      pattern: "published"
    - from: "supabase/migrations/20260605000004_published_is_claimed.sql"
      to: "app/api/business/create-paikka/route.ts"
      via: "INSERT sets published = false"
      pattern: "is_claimed"
---

<objective>
Add two new columns to liikuntapaikat: published (controls user-facing visibility of new venues) and is_claimed (enables claim status display without RLS workarounds).

Purpose: CLAIM-03 requires that new venues created by businesses are hidden (published = false) until admin approval, and that claim-paikka pysyy näkyvänä. The is_claimed flag (D-07) lets anon queries check if a venue is already claimed without needing to query business_paikka_links (which has RLS restricting per-business reads).

Output: One new migration file — 20260605000004_published_is_claimed.sql
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-claim-paikan-luonti/33-CONTEXT.md
@.planning/phases/31-db-skeema-storage-perusta/31-CONTEXT.md

<interfaces>
<!-- From supabase/migrations/20260605000001_business_managed.sql (migration pattern to replicate) -->
ALTER TABLE liikuntapaikat ADD COLUMN IF NOT EXISTS business_managed BOOLEAN NOT NULL DEFAULT false;

<!-- From supabase/migrations/20260605000000_business_accounts.sql -->
-- business_paikka_links schema (for reference — is_claimed mirrors paikka_id presence there)
-- UNIQUE(paikka_id) constraint means each venue can have at most one business link

<!-- No RLS policy needed for new columns:
     supabase/migrations/20260519000001_enable_rls.sql already grants
     public_read, authenticated_insert, authenticated_update, authenticated_delete
     to liikuntapaikat — these existing policies cover all columns including new ones.
     The is_claimed column is intentionally public-readable (same as nimi, osoite, etc.) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 20260605000004_published_is_claimed.sql</name>
  <files>supabase/migrations/20260605000004_published_is_claimed.sql</files>
  <read_first>
    - supabase/migrations/20260605000001_business_managed.sql — ALTER TABLE pattern to follow exactly
    - supabase/migrations/20260519000001_enable_rls.sql — confirm existing RLS policies on liikuntapaikat cover new columns (they do: public_read policy has no column list, covers all)
    - .planning/phases/33-claim-paikan-luonti/33-CONTEXT.md — D-09 (published default true, new venues set to false), D-07 (is_claimed boolean)
  </read_first>
  <action>
    Create the migration file with these two ALTER statements:

    1. Add published column: ALTER TABLE liikuntapaikat ADD COLUMN IF NOT EXISTS published BOOLEAN NOT NULL DEFAULT true;
       Rationale: DEFAULT true ensures all existing venues remain visible immediately after migration. New venues created by the create-paikka route handler will explicitly set published = false.

    2. Add is_claimed column: ALTER TABLE liikuntapaikat ADD COLUMN IF NOT EXISTS is_claimed BOOLEAN NOT NULL DEFAULT false;
       Rationale: DEFAULT false — all existing venues start unclaimed. The claim-paikka and create-paikka route handlers will UPDATE this to true after inserting into business_paikka_links. This column is readable by anon key (same public_read RLS as the rest of liikuntapaikat) — no new RLS policy needed.

    Include a decision log comment at the top of the file citing D-07 and D-09 from 33-CONTEXT.md, and noting why no new RLS policy is needed (existing public_read policy in 20260519000001_enable_rls.sql covers all columns).

    Do NOT add any RLS policies or CREATE POLICY statements. The existing policies already cover these columns.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS published" supabase/migrations/20260605000004_published_is_claimed.sql && grep -c "ADD COLUMN IF NOT EXISTS is_claimed" supabase/migrations/20260605000004_published_is_claimed.sql</automated>
  </verify>
  <done>Migration file exists at supabase/migrations/20260605000004_published_is_claimed.sql with both ALTER TABLE statements. grep confirms both "published" and "is_claimed" column additions are present. No RLS statements in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → Supabase DB | SQL runs with service role privileges via supabase db push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | published column | mitigate | DEFAULT true protects existing venues; only service role key (Route Handler) can set published = false via INSERT |
| T-33-02 | Information Disclosure | is_claimed column | accept | Intentionally public — venue claim status is not sensitive; shows only true/false, no business identity |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
Run after migration push (Plan 33-07):
- SELECT column_name, data_type, column_default FROM information_schema.columns WHERE table_name = 'liikuntapaikat' AND column_name IN ('published', 'is_claimed');
- Both rows must appear with data_type 'boolean' and defaults 'true' / 'false' respectively
</verification>

<success_criteria>
Migration file exists and is syntactically valid SQL. Both columns use IF NOT EXISTS guard. published defaults to true (existing venues visible). is_claimed defaults to false (no venues claimed by default). No new RLS policies added.
</success_criteria>

<output>
Create `.planning/phases/33-claim-paikan-luonti/33-01-SUMMARY.md` when done
</output>
