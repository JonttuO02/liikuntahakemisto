---
phase: 55-ai-lajiluokitus-sivuanalyysiin
plan: 03
type: execute
wave: 3
depends_on: ["55-01", "55-02"]
files_modified:
  - app/business/onboarding/AnalysoiSivusto.tsx
  - app/business/onboarding/page.tsx
autonomous: false
requirements: [AI-06]

must_haves:
  truths:
    - "The preview screen shows a distinct laji suggestion element (separate from logo/color/gallery pickers) with Vahvista + Vaihda actions (AI-06 criterion 2, D-01)"
    - "When suggested_laji is null/invalid the card renders an unconfirmed state with no pre-confirmed pick, forcing the user into the picker (D-03)"
    - "Vaihda opens a picker listing all 9 lib/lajit.ts categories plus a bounded free-text input (D-02)"
    - "'Analysoi uudelleen' resets the laji selection alongside the existing logo/color resets (D-05)"
    - "Both the quick-accept and 'Jatka velhoon' paths persist the confirmed laji via save-step (D-04)"
    - "The 'Ohita' (skip-analysis) path presents the same manual picker before final submit, with no AI badge (D-06)"
  artifacts:
    - path: "app/business/onboarding/AnalysoiSivusto.tsx"
      provides: "laji suggestion card, extracted LajiPicker, laji state, reanalyze reset, quick-accept laji field, onConfirm laji arg"
      contains: "lajiKonfig"
    - path: "app/business/onboarding/page.tsx"
      provides: "handleConfirm laji save-step write + skip-path manual picker wiring (D-06)"
      contains: "field: 'laji'"
  key_links:
    - from: "app/business/onboarding/AnalysoiSivusto.tsx"
      to: "lib/branding/brandingResult.ts"
      via: "seeds confirmedLaji from brandingResult.suggested_laji in the selectionInitialisedRef-guarded effect"
      pattern: "suggested_laji"
    - from: "app/business/onboarding/page.tsx"
      to: "app/api/business/onboarding/save-step/route.ts"
      via: "handleConfirm POSTs { field: 'laji', value } before navigating to wizard"
      pattern: "field: 'laji'"
    - from: "app/business/onboarding/AnalysoiSivusto.tsx"
      to: "lib/lajit.ts"
      via: "import { lajiKonfig }; picker grid maps Object.entries(lajiKonfig)"
      pattern: "Object\\.entries\\(lajiKonfig\\)"
---

<objective>
Build the user-facing confirm/change/skip UI: a distinct laji suggestion card in `AnalysoiSivusto.tsx`'s preview phase (Suggested / Confirmed / Unconfirmed states per the UI-SPEC), an extracted `LajiPicker` (9-category grid + bounded free text) reused by both the Vaihda flow and the D-06 skip path, reset-on-reanalyze, and the confirmed-value save-step writes on both the quick-accept and full-wizard paths.

Purpose: AI-06 criterion 2 (distinct suggestion element, confirm-or-change), criterion 3 (explicit confirmation before any write), and D-01/D-02/D-03/D-05/D-06.

Output: AnalysoiSivusto suggestion card + LajiPicker + state/reset/quick-accept changes; page.tsx handleConfirm laji write + skip-path picker.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-CONTEXT.md
@.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-RESEARCH.md
@.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-PATTERNS.md
@.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-UI-SPEC.md
@.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- `app/business/onboarding/AnalysoiSivusto.tsx`:
  - `LajiPicker` sub-component (taxonomy grid + free-text input + Käytä/Peruuta) — extracted so Vaihda and the D-06 skip path share one implementation
  - laji local state: `confirmedLaji`, `suggestedLajiKey`, `lajiState` ('suggested'|'confirmed'|'unconfirmed'), `lajiPickerOpen`, free-text input + error state
  - laji suggestion card block (Suggested/Confirmed/Unconfirmed render states) inserted between Galleria and Hinnat
  - laji seeding in the `selectionInitialisedRef`-guarded `useEffect` (from `brandingResult.suggested_laji`)
  - laji resets added to the `onReanalyze` reset block
  - `'laji'` added to `handleQuickAccept`'s `fieldsToWrite` union + array
  - `onConfirm` call-site + `AnalysoiSivustoProps.onConfirm` type widened to carry `laji`
- `app/business/onboarding/page.tsx`:
  - `handleConfirm` second `save-step` write for `field: 'laji'` (await before navigate, only when set); `selections` type + `PrePhase.onConfirm` type widened
  - D-06 skip-path manual picker wiring (reusing `LajiPicker`) presented before final submit
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Suggestion card + extracted LajiPicker + state/reset/quick-accept in AnalysoiSivusto</name>
  <files>app/business/onboarding/AnalysoiSivusto.tsx</files>
  <read_first>
    - app/business/onboarding/AnalysoiSivusto.tsx (full file — specifically: AnalysoiSivustoProps.onConfirm type ~lines 21-24; the editContent JSX blocks Logo 191-233, Brändivärit 235-353 including the color armed-slot 276-324 and custom-hex free-text input 326-346, Galleria 355-407, Hinnat 409-423; the "Jatka velhoon →" onConfirm call-site 460-474; the selectionInitialisedRef-guarded init useEffect ~549-585; handleQuickAccept fieldsToWrite 705-710; onReanalyze reset block 955-971. These line numbers are verified in 55-RESEARCH.md and 55-PATTERNS.md.)
    - lib/lajit.ts (lajiKonfig — 9 keys + label/color config the picker grid maps over)
    - lib/branding/brandingResult.ts (BrandingResult.suggested_laji — produced in Plan 01; the seed source)
    - .planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-UI-SPEC.md ("Interaction States" table + "Copywriting Contract" — exact Finnish copy and the 4 render states; "Color"/"Typography" — reuse LabelCaps/PrimaryButton/MutedButton, no new visual language)
    - .planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-PATTERNS.md ("app/business/onboarding/AnalysoiSivusto.tsx" section — verbatim JSX for the suggestion card, picker grid, free-text input, onConfirm widening, fieldsToWrite, onReanalyze reset)
  </read_first>
  <action>
    Import `lajiKonfig` from `@/lib/lajit`. Add local state: `confirmedLaji: string | null`, `suggestedLajiKey: string | null`, `lajiState: 'suggested' | 'confirmed' | 'unconfirmed'`, `lajiPickerOpen: boolean`, plus a free-text input value + error (mirror the existing `customHexError` idiom). Seed these in the existing `selectionInitialisedRef`-guarded `useEffect` keyed on `brandingResult`: if `brandingResult.suggested_laji` is a valid `lajiKonfig` key, set `suggestedLajiKey` to it and `lajiState='suggested'`; otherwise `lajiState='unconfirmed'`, `suggestedLajiKey=null` (D-03 — never default to 'liikunta'/'liikuntahalli'/'Muu').

    Extract a `LajiPicker` sub-component (taxonomy button grid + free-text input + Käytä + Peruuta) so the Vaihda flow AND the D-06 skip path (Plan task 2 / page.tsx) render the identical JSX/validation — do NOT inline two divergent copies. The grid maps `Object.entries(lajiKonfig)` to buttons styled exactly like the logo-candidate grid (`flex flex-row flex-wrap gap-2`, `border rounded-lg p-2 text-sm`, selected state `border-[#111111] ring-2 ring-[#111111]`). The free-text input mirrors the custom-hex input block (`border-[rgba(0,0,0,0.12)] rounded-lg h-10 px-3 text-sm`, placeholder "Muu laji…", a "Käytä" MutedButton, validation: non-empty + ≤100 chars, error "Anna lajin nimi (enintään 100 merkkiä)" as `text-sm text-red-600 role="alert"`). Picker heading "Valitse lajikategoria", a "Peruuta" MutedButton that closes without change. `LajiPicker` should accept the current value + an `onPick(value: string)` callback + an `onCancel` callback (or equivalent), so it carries no AI-suggestion framing of its own. Export it (or place it module-level in this file) so page.tsx can import it for D-06.

    Insert the suggestion card block between the Galleria block (ends ~407) and the Hinnat block (starts ~409), using a `LabelCaps` "Laji" header and rendering the three states per the UI-SPEC Interaction States table:
    - Suggested: body "Ehdotettu laji: {lajiKonfig[suggestedLajiKey].label}" in `text-sm font-bold text-[#111111]`; a `flex flex-row gap-2` row with Vahvista (PrimaryButton → sets confirmedLaji=suggestedLajiKey, lajiState='confirmed') and Vaihda (MutedButton → opens LajiPicker).
    - Confirmed: body "Laji: {label}" (drops "Ehdotettu"); only Vaihda (MutedButton).
    - Unconfirmed: body "Lajia ei tunnistettu automaattisesti — valitse lajikategoria" in `text-sm text-[rgba(17,17,17,0.45)]` (muted, not bold); a single "Valitse laji" PrimaryButton that opens LajiPicker.
    When `lajiPickerOpen`, render `LajiPicker` in place (inline expansion, not a modal); picking a category or submitting valid free text sets `confirmedLaji`, `lajiState='confirmed'`, closes the picker.

    Widen `AnalysoiSivustoProps.onConfirm`'s type and the "Jatka velhoon →" call-site (lines 460-474) so the second argument carries `laji: confirmedLaji` (i.e. `{ logoUrl, gallery, laji }`). Add `'laji'` to `handleQuickAccept`'s `fieldsToWrite` union type and a `{ field: 'laji', value: confirmedLaji }` entry to the array (inherits the existing idempotent-UPSERT-retry semantics — no new error handling). Add the laji state resets to the `onReanalyze` reset block: `setLajiState(...)` back to its initial sentinel, `setSuggestedLajiKey(null)`, `setConfirmedLaji(null)`, `setLajiPickerOpen(false)`, plus clearing the free-text input/error (D-05).

    Reuse the existing `LabelCaps`/`PrimaryButton`/`MutedButton`/`Spinner` sub-components and the `text-sm text-red-600 role="alert"` error idiom verbatim — introduce no new colors, font sizes, or weights (CLAUDE.md 4-size/2-weight, UI-SPEC Color/Typography). Do NOT add an immediate PATCH /api/business/branding write for laji — the confirmed value only ever travels through save-step (Plan 02) per D-04.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i 'AnalysoiSivusto' | head -5; echo "exit-check"; npx vitest run lib/branding/analyzer.test.ts >/dev/null 2>&1 && echo "suite-still-green"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'lajiKonfig' app/business/onboarding/AnalysoiSivusto.tsx` and the picker grid maps `Object.entries(lajiKonfig)` (all 9 categories rendered)
    - A `LajiPicker` sub-component exists and is reusable (single implementation, importable/usable by page.tsx for D-06)
    - The suggestion card renders three distinct states (Suggested / Confirmed / Unconfirmed) with the exact UI-SPEC Finnish copy; unconfirmed state shows no Vahvista and no pre-confirmed pick (D-03)
    - `'laji'` is present in `handleQuickAccept`'s `fieldsToWrite` array and its union type
    - The `onReanalyze` reset block clears all laji state (suggestion key, confirmed value, picker open, free-text) (D-05)
    - The `onConfirm` call-site passes the confirmed laji; `AnalysoiSivustoProps.onConfirm` type is updated to match
    - No `PATCH /api/business/branding` call is added for laji (deferred-to-submit invariant)
    - `npx tsc --noEmit` shows no new type errors originating in AnalysoiSivusto.tsx
    - `npm test` (existing suite) stays green
  </acceptance_criteria>
  <done>The preview screen shows a distinct, three-state laji suggestion element with a reusable picker; reanalyze resets it; quick-accept persists the confirmed value via save-step; no premature DB write.</done>
</task>

<task type="auto">
  <name>Task 2: handleConfirm laji write + D-06 skip-path manual picker in page.tsx</name>
  <files>app/business/onboarding/page.tsx</files>
  <read_first>
    - app/business/onboarding/page.tsx (full file — handleConfirm lines 171-212 with its media_urls await-before-navigate save-step write; the PrePhase onConfirm prop type ~lines 109-112; handleSkip lines 214-217 ({ setBrandingData(null); setPagePhase('wizard') }); the pagePhase state machine 'paikka' → 'analyze' → 'wizard' and where each phase renders; StepPaikkaPrePhase wrapper. Verified in 55-RESEARCH.md.)
    - app/business/onboarding/AnalysoiSivusto.tsx (the LajiPicker extracted in Task 1 — import it for the skip path; the onConfirm signature widened in Task 1 — keep all three declarations in sync)
    - .planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-PATTERNS.md ("app/business/onboarding/page.tsx" section — verbatim handleConfirm extension with the second laji save-step write)
    - .planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-UI-SPEC.md ("Skip-path picker (D-06)" note + the "Valitse paikan lajikategoria ennen jatkamista" intro copy)
  </read_first>
  <action>
    Widen `handleConfirm`'s `selections` param type to `{ logoUrl: string | null; gallery: string[]; laji: string | null }` and update the `PrePhase.onConfirm` prop type to match (keep all three onConfirm declarations — AnalysoiSivustoProps from Task 1, PrePhase here, and handleConfirm here — in sync). After the existing `media_urls` save-step write inside the same `if (paikkaId !== null)` try block, add a second sequential `await fetch('/api/business/onboarding/save-step', ...)` POSTing `{ paikka_id: paikkaId, step: 0, field: 'laji', value: selections.laji }` — but ONLY when `selections.laji` is truthy (never POST null/empty; save-step would 400 on empty anyway). Both writes are independent fields on the same idempotent UPSERT route and run safely in sequence; keep the existing non-blocking single try/catch (a failed write still allows navigation).

    For D-06 (skip path): implement the manual picker so it is presented before the user can reach final submit. RECOMMENDED placement (planner's discretion per CONTEXT.md, but pick this unless a blocker emerges): render a small intermediate UI on the skip path rather than wiring it deep into the wizard. Concretely — when `handleSkip` fires, instead of immediately `setPagePhase('wizard')`, show the extracted `LajiPicker` (no AI badge, no "Ehdotettu"/Vahvista framing) introduced by the copy "Valitse paikan lajikategoria ennen jatkamista"; on pick/free-text submit, write the chosen value via the same `save-step` `field: 'laji'` POST (await before navigate), then `setBrandingData(null)` and `setPagePhase('wizard')`. This can be a new transient `pagePhase` value (e.g. `'laji-skip'`) or an inline conditional block — implementation choice — but it MUST reuse the `LajiPicker` from Task 1 (no second picker implementation) and MUST write through `save-step` field `'laji'` before final submit. Per UI-SPEC, this is a strongly-prompted one-time picker, not a hard block; if the user proceeds without picking, laji stays at its create-paikka default (acceptable per onboarding's "nothing literally blocks submit" philosophy).
  </action>
  <verify>
    <automated>grep -q "field: 'laji'" app/business/onboarding/page.tsx && grep -q 'LajiPicker' app/business/onboarding/page.tsx && echo OK; npx tsc --noEmit 2>&1 | grep -i 'page.tsx' | head -5; echo "tsc-checked"</automated>
  </verify>
  <acceptance_criteria>
    - `handleConfirm` POSTs a second save-step write with `field: 'laji'` (awaited before navigate), guarded so it only fires when `selections.laji` is truthy
    - `selections` type and `PrePhase.onConfirm` type both carry `laji: string | null`; all three onConfirm declarations stay in sync (no tsc signature-mismatch errors)
    - The D-06 skip path presents a manual category picker reusing the imported `LajiPicker` (no duplicated picker implementation, no AI/Ehdotettu framing) before final submit, introduced by the "Valitse paikan lajikategoria ennen jatkamista" copy
    - The skip-path pick writes through `save-step` `field: 'laji'`
    - `npx tsc --noEmit` shows no new type errors originating in page.tsx
  </acceptance_criteria>
  <done>Both the full-wizard and skip paths persist a confirmed laji via save-step before reaching submit; the skip path reuses the single LajiPicker implementation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — confirm/change/skip/reanalyze flows + end-to-end persistence</name>
  <action>Human verification of the laji UX. See how-to-verify steps below — no code change in this task.</action>
  <what-built>
    The full laji confirm/change/skip UX in onboarding: a distinct suggestion card (Suggested/Confirmed/Unconfirmed), a 9-category + free-text picker (Vaihda and skip-path), reanalyze reset, and persistence through quick-accept and "Jatka velhoon" and the skip path. Criterion 2, D-03, D-05, D-06 have no automated coverage (no component test infra) and require manual UAT.
  </what-built>
  <how-to-verify>
    1. Start the app and begin business onboarding's create-from-scratch flow through to the AnalysoiSivusto preview phase (after a real or representative website analysis).
    2. Confirm the laji suggestion card appears between the Galleria and Hinnat blocks and is visually distinct (bold "Ehdotettu laji: {label}" + Vahvista/Vaihda row) from the logo/color/gallery pickers. (AI-06 criterion 2 / D-01)
    3. Click Vahvista — body changes to "Laji: {label}" and only Vaihda remains. Click Vaihda — the picker opens with all 9 categories (padel, tennis, jooga, kuntosali, uinti, kiipeily, jääkiekko, liikuntahalli, liikunta) plus a "Muu laji…" free-text input. Pick a different category; confirm it sticks. (D-02)
    4. Trigger an analysis where laji is absent/invalid (or temporarily mock suggested_laji=null): confirm the card shows the unconfirmed state ("Lajia ei tunnistettu automaattisesti — valitse lajikategoria"), NO Vahvista, only "Valitse laji" → picker. (D-03)
    5. Confirm a laji, click "Analysoi uudelleen": the laji card returns to its fresh/unconfirmed state, not the prior pick. (D-05)
    6. Run BOTH the "Hyväksy ja lähetä" (quick-accept) and "Jatka velhoon →" (full-wizard, complete the wizard) paths to submit; then inspect liikuntapaikat.laji for the venue — it must equal the confirmed value in both cases. (AI-06 criterion 3)
    7. Restart onboarding and click "Ohita" at the analysis step: confirm the manual category picker (no AI badge) appears before you can finish; pick a category, finish, and verify liikuntapaikat.laji reflects it. (D-06)
    8. Confirm a venue whose laji was already set, then resubmit a flow that does NOT revisit the laji picker — verify the laji does NOT revert (criterion 4; the route test already guards this, this is the end-to-end check).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any state/copy/persistence issues observed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client laji input → save-step | Free-text and selected category cross from the browser into the staged draft |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-05 | Tampering | LajiPicker free-text → save-step | mitigate | Client-side ≤100-char/non-empty check in LajiPicker is UX defense-in-depth; the authoritative bound is the server-side save-step validator (Plan 02, T-55-03). Value rendered as plain JSX text — no HTML sink, no dangerouslySetInnerHTML. |
| T-55-06 | Information Disclosure | unconfirmed AI suggestion leaking to DB | mitigate | The suggestion only ever writes to liikuntapaikat after an explicit Vahvista/Vaihda → save-step → submit; no immediate PATCH for laji (D-04), so an unconfirmed pick cannot leak. Enforced by construction in Task 1/Task 2 (no branding-PATCH path for laji). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (onConfirm signatures in sync across all three declarations)
- `npm test` full suite green (Plans 01+02 tests unaffected)
- Manual UAT checkpoint covers criterion 2, D-03, D-05, D-06, and end-to-end persistence (criterion 3/4)
</verification>

<success_criteria>
- AI-06 criterion 2: distinct suggestion element; user can confirm or change before save
- AI-06 criterion 3: laji written only on explicit confirmation, via save-step → submit
- D-02/D-03/D-05/D-06 all observable in manual UAT
- No new visual language; CLAUDE.md glassmorphism + 4-size/2-weight respected
</success_criteria>

<output>
Create `.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-03-SUMMARY.md` when done
</output>
