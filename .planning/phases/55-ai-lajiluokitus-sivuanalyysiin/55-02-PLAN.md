---
phase: 55-ai-lajiluokitus-sivuanalyysiin
plan: 02
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - app/api/business/onboarding/save-step/route.ts
  - app/api/business/onboarding/submit/route.ts
  - tests/api/save-step.test.ts
  - tests/api/submit.test.ts
autonomous: true
requirements: [AI-06]

must_haves:
  truths:
    - "save-step accepts a 'laji' field and rejects empty / >100-char values (bounds the D-02 free-text escape hatch, Pitfall 5)"
    - "submit writes liikuntapaikat.laji ONLY when draft.laji is set — and omits the key entirely when it is falsy, never clobbering the existing value (AI-06 criterion 3+4, Pitfall 2)"
    - "the confirmed value never reaches liikuntapaikat without travelling through save-step → submit (deferred-to-submit invariant, success criterion 3)"
  artifacts:
    - path: "app/api/business/onboarding/save-step/route.ts"
      provides: "'laji' in ALLOWED_FIELDS + field-specific length/non-empty validator"
      contains: "'laji'"
    - path: "app/api/business/onboarding/submit/route.ts"
      provides: "conditional-spread laji write into the liikuntapaikat UPDATE"
      contains: "draft.laji"
    - path: "tests/api/submit.test.ts"
      provides: "regression tests: writes laji when set; omits laji key when falsy"
    - path: "tests/api/save-step.test.ts"
      provides: "validation tests: rejects empty / >100-char laji; accepts valid"
  key_links:
    - from: "app/api/business/onboarding/submit/route.ts"
      to: "onboarding_draft.laji"
      via: "draft.laji read from the select('*') row; conditional-spread into liikuntapaikat UPDATE"
      pattern: "draft\\.laji"
    - from: "app/api/business/onboarding/save-step/route.ts"
      to: "onboarding_draft.laji"
      via: "'laji' member of ALLOWED_FIELDS written via [field]: value UPSERT"
      pattern: "ALLOWED_FIELDS"
---

<objective>
Wire the confirmed sport category through the deferred-to-submit persistence path: `save-step` accepts and bounds a `laji` field into `onboarding_draft.laji`, and `submit` copies `draft.laji` into `liikuntapaikat.laji` using a conditional spread that never overwrites the existing value when the draft never set it.

Purpose: AI-06 criterion 3 (no write to `liikuntapaikat.laji` without explicit confirmation — guaranteed by construction via the draft staging) and criterion 4 (no null-overwrite regression). Closes RESEARCH.md Pitfall 1 (submit currently never touches laji) and Pitfall 2 (null-overwrite) and Pitfall 5 (unbounded free text).

Output: save-step ALLOWED_FIELDS + validator, submit UPDATE conditional spread, two new route test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-CONTEXT.md
@.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-RESEARCH.md
@.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-PATTERNS.md
@.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-VALIDATION.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- `app/api/business/onboarding/save-step/route.ts` — `'laji'` added to `ALLOWED_FIELDS`; new `if (field === 'laji')` validation block (non-empty string, max 100 chars)
- `app/api/business/onboarding/submit/route.ts` — `...(draft.laji ? { laji: draft.laji } : {})` conditional spread added to the `liikuntapaikat` UPDATE payload
- `tests/api/submit.test.ts` (new) — tests for "writes confirmed laji" and "does not overwrite laji"
- `tests/api/save-step.test.ts` (new) — tests for laji field validation (empty rejected, >100 rejected, valid accepted)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 'laji' to save-step ALLOWED_FIELDS with bounded validation</name>
  <files>app/api/business/onboarding/save-step/route.ts, tests/api/save-step.test.ts</files>
  <read_first>
    - app/api/business/onboarding/save-step/route.ts (ALLOWED_FIELDS const at line 6; the AllowedField type; the field-specific validation block for aukioloajat/hinnasto at lines 79-88; the ownership check at lines 90-102; the [field]: value UPSERT at lines 107-118 — all read in this conversation)
    - tests/api/update-paikka.test.ts (the project's Route Handler test pattern: mock next/server's NextResponse.json, mock @/lib/supabaseAdmin.server with a chainable builder keyed by table name, mock supabaseAdmin.auth.getUser, import the route's exported POST handler AFTER mocks are set up)
  </read_first>
  <behavior>
    - Test: POST with `field: 'laji'`, `value: 'padel'`, valid auth + owned paikka → 200 and the UPSERT receives `laji: 'padel'`
    - Test: POST with `field: 'laji'`, `value: ''` (empty) → 400 with a laji error message, no UPSERT
    - Test: POST with `field: 'laji'`, `value` a 101-char string → 400, no UPSERT
    - Test: POST with `field: 'laji'`, `value: 123` (non-string) → 400, no UPSERT
  </behavior>
  <action>
    In `save-step/route.ts`: add `'laji'` to the `ALLOWED_FIELDS` tuple (so it becomes `['media_urls', 'hinnasto', 'aukioloajat', 'yhteystiedot', 'laji'] as const`). Add a field-specific validation block after the existing `hinnasto` block (around lines 83-88), mirroring the hinnasto shape: `if (field === 'laji') { if (typeof value !== 'string' || value.trim().length === 0 || value.length > 100) { return NextResponse.json({ error: 'laji: invalid value' }, { status: 400 }) } }`. The 100-char cap mirrors the `nimi`/`osoite` bounding in create-paikka. No other change needed — the JWT verify, the `business_paikka_links` ownership check, and the `[field]: value` computed-property UPSERT all apply to `laji` for free (it is an ALLOWED_FIELDS member like the others).

    Create `tests/api/save-step.test.ts` mirroring `tests/api/update-paikka.test.ts`'s mock-builder pattern. Mock `supabaseAdmin.auth.getUser` to return a valid user, mock the `business_paikka_links` lookup to return an owning link, and mock the `onboarding_draft` UPSERT. Cover the cases in `<behavior>`. Name at least one test so it matches `-t "laji"`.
  </action>
  <verify>
    <automated>npx vitest run tests/api/save-step.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'laji'" app/api/business/onboarding/save-step/route.ts` and `'laji'` is a member of the ALLOWED_FIELDS tuple
    - A `if (field === 'laji')` validation block exists rejecting non-string, empty (post-trim), and >100-char values with a 400
    - `npx vitest run tests/api/save-step.test.ts` passes all cases in `<behavior>`
    - `npx vitest run tests/api/save-step.test.ts -t "laji"` matches and passes
  </acceptance_criteria>
  <done>save-step accepts a bounded `laji` value into onboarding_draft.laji and rejects empty/oversized/non-string input; covered by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: submit writes draft.laji into liikuntapaikat via conditional spread (no null-overwrite)</name>
  <files>app/api/business/onboarding/submit/route.ts, tests/api/submit.test.ts</files>
  <read_first>
    - app/api/business/onboarding/submit/route.ts (the draft fetch select('*', joined liikuntapaikat ...) at lines 29-34; the liikuntapaikat UPDATE payload object literal at lines 75-87; the updateError handling at lines 89-92 — all read in this conversation. Note: draft.laji is the onboarding_draft column added in Plan 01, distinct from the joined liikuntapaikat(... laji ...) read.)
    - tests/api/update-paikka.test.ts (mock-builder pattern: mock next/server NextResponse.json, mock @/lib/supabaseAdmin.server chainable builder keyed by table name, mock supabaseAdmin.auth.getUser, import POST after mocks)
    - .planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-RESEARCH.md (Pitfall 2 — why `laji: draft.laji ?? null` is forbidden and the conditional spread `...(draft.laji ? { laji: draft.laji } : {})` is mandatory)
  </read_first>
  <behavior>
    - Test: `draft.laji = 'padel'` set → the mocked `liikuntapaikat` `.update(...)` call args include `laji: 'padel'`
    - Test: `draft.laji` is `null`/absent → the mocked `.update(...)` call args object does NOT contain a `laji` key at all (assert `!('laji' in updateArgs)`, not `laji === null`) — Pitfall 2 / criterion 4
    - Test: existing fields (hinta_kuvaus, aukioloajat, image_url, etc.) are still written in both cases (no regression)
  </behavior>
  <action>
    In `submit/route.ts`'s `liikuntapaikat` `.update({...})` payload (lines 75-87), add exactly one line inside the object literal: `...(draft.laji ? { laji: draft.laji } : {})`. This conditional spread omits the `laji` key entirely when `draft.laji` is falsy — the only safe form, because Supabase's `.update()` writes every key present in the object, so an unconditional `laji: draft.laji ?? null` would clobber the create-paikka-seeded value back to null for any flow that bypassed the picker (criterion 4 regression). Do NOT use `laji: draft.laji ?? undefined` as a plain key — a present `undefined` risks driver-level inconsistency. No change to the updateError handling, the draft-delete step, or any other field.

    Create `tests/api/submit.test.ts` mirroring `tests/api/update-paikka.test.ts`'s mock-builder pattern. Set up mocks so the `onboarding_draft` select returns a draft row (with `laji` set in one test, absent/null in another), the `business_paikka_links` lookup returns an owning link, and capture the args passed to the `liikuntapaikat` `.update(...)`. Cover the cases in `<behavior>`. Name tests so `-t "writes confirmed laji"` and `-t "does not overwrite laji"` both match.
  </action>
  <verify>
    <automated>npx vitest run tests/api/submit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'draft.laji' app/api/business/onboarding/submit/route.ts` and the usage is a conditional spread `...(draft.laji ? { laji: draft.laji } : {})`, NOT an unconditional key with `?? null` / `?? undefined`
    - `npx vitest run tests/api/submit.test.ts -t "writes confirmed laji"` passes (update args include laji when draft.laji set)
    - `npx vitest run tests/api/submit.test.ts -t "does not overwrite laji"` passes (laji key absent from update args when draft.laji falsy)
    - The tests assert key-absence (`!('laji' in args)`), not `laji === null`
  </acceptance_criteria>
  <done>submit copies a confirmed draft.laji into liikuntapaikat.laji and provably omits the key when unset, guarding criterion 4 against null-overwrite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → save-step | Business user free-text laji input (D-02 escape hatch) crosses into stored JSONB-adjacent column |
| onboarding_draft → liikuntapaikat | Draft value is committed to the public-facing venue row at submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-03 | Tampering | save-step laji field | mitigate | Server-side length (≤100) + non-empty + string-type validation bounds the free-text Vaihda escape hatch (Task 1). Value is rendered as plain category text downstream (React JSX escaping; no dangerouslySetInnerHTML), so no XSS sink. |
| T-55-04 | Elevation of Privilege | save-step / submit IDOR | mitigate | Existing `business_paikka_links` ownership check on both routes applies uniformly to every ALLOWED_FIELDS member including `laji`; verified user.id from JWT is used, never body-supplied account id. No new code needed (inherited). |
</threat_model>

<verification>
- `npx vitest run tests/api/save-step.test.ts` green
- `npx vitest run tests/api/submit.test.ts` green
- `npm test` full suite green
- `npx tsc --noEmit` clean (AllowedField union widened; draft.laji typed via select('*'))
</verification>

<success_criteria>
- AI-06 criterion 3: liikuntapaikat.laji is only ever written via the explicit save-step → submit path (deferred-to-submit invariant holds by construction)
- AI-06 criterion 4: a draft without laji leaves the existing liikuntapaikat.laji untouched (key-absence asserted)
- Free-text Vaihda input is bounded server-side (Pitfall 5 closed)
</success_criteria>

<output>
Create `.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-02-SUMMARY.md` when done
</output>
