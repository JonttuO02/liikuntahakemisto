---
phase: 55-ai-lajiluokitus-sivuanalyysiin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260623190347_business_branding_suggested_laji.sql
  - supabase/migrations/20260623190348_onboarding_draft_add_laji.sql
  - lib/branding/prompt.ts
  - lib/branding/analyzer.ts
  - lib/branding/brandingResult.ts
  - app/api/business/analyze-website/route.ts
  - lib/branding/analyzer.test.ts
autonomous: true
requirements: [AI-06]

must_haves:
  truths:
    - "Claude's analysis returns a sport-category suggestion drawn only from the lib/lajit.ts 9-key taxonomy, never free text (AI-06 criterion 1, D-07)"
    - "An out-of-taxonomy or missing laji from Claude is discarded to null, not defaulted to a sentinel (D-03)"
    - "Existing logo/color/price/hours extraction stays intact when Claude's response omits laji (AI-06 criterion 4)"
    - "The raw AI suggestion is persisted to business_branding.suggested_laji and returned in the GET BrandingResult"
  artifacts:
    - path: "supabase/migrations/20260623190347_business_branding_suggested_laji.sql"
      provides: "business_branding.suggested_laji TEXT column"
      contains: "ADD COLUMN IF NOT EXISTS suggested_laji"
    - path: "supabase/migrations/20260623190348_onboarding_draft_add_laji.sql"
      provides: "onboarding_draft.laji TEXT column (confirmed value staging for Plan 02/03)"
      contains: "ADD COLUMN IF NOT EXISTS laji"
    - path: "lib/branding/analyzer.ts"
      provides: "suggested_laji allowlist validation on analyzeWithClaude result"
      contains: "VALID_LAJI_KEYS"
    - path: "lib/branding/brandingResult.ts"
      provides: "suggested_laji field on the client-safe BrandingResult type"
      contains: "suggested_laji"
  key_links:
    - from: "lib/branding/analyzer.ts"
      to: "lib/lajit.ts"
      via: "import { lajiKonfig }; VALID_LAJI_KEYS = Object.keys(lajiKonfig)"
      pattern: "Object\\.keys\\(lajiKonfig\\)"
    - from: "app/api/business/analyze-website/route.ts"
      to: "lib/branding/analyzer.ts"
      via: "runAnalysis persists analyzeWithClaude().suggested_laji into business_branding UPSERT"
      pattern: "suggested_laji"
---

<objective>
Add the AI sport-category suggestion source to the existing branding-analysis pipeline: Claude proposes a `lib/lajit.ts` taxonomy key, the server validates it against the live 9-key allowlist (discarding anything invalid to `null`), and the raw suggestion is persisted to `business_branding.suggested_laji` and surfaced in the GET `BrandingResult`. Also lands the two additive schema migrations this whole phase depends on.

Purpose: AI-06 criterion 1 ("ehdotettu lajikategoria taksonomiasta, ei vapaata tekstiä") and the schema foundation for the confirmed-value staging used by Plans 02/03. This is the only plan that touches the AI/analyzer layer.

Output: Two migrations, prompt schema field, analyzer validation + type, brandingResult type field, analyze-website persist+select, and extended analyzer unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-CONTEXT.md
@.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-RESEARCH.md
@.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-PATTERNS.md
@.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-VALIDATION.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- `supabase/migrations/20260623190347_business_branding_suggested_laji.sql` — adds `business_branding.suggested_laji TEXT`
- `supabase/migrations/20260623190348_onboarding_draft_add_laji.sql` — adds `onboarding_draft.laji TEXT`
- `lib/branding/prompt.ts` — `laji` key added to `BRANDING_ANALYSIS_PROMPT` JSON schema + field rules (enum interpolated from `Object.keys(lajiKonfig)`)
- `lib/branding/analyzer.ts` — new `VALID_LAJI_KEYS` const, `suggested_laji: string | null` on `BrandingAnalysisResult`, `laji?: unknown` on the raw parse cast, discard-to-null validation, `suggested_laji` in the return object
- `lib/branding/brandingResult.ts` — `suggested_laji: string | null` field on `BrandingResult`
- `app/api/business/analyze-website/route.ts` — `suggested_laji` written into the `business_branding` UPSERT in `runAnalysis`; `suggested_laji` added to the GET handler `.select(...)` column list and response shape
- `lib/branding/analyzer.test.ts` — new `laji` test cases (valid-passes, invalid-string→null, missing→null, omission-does-not-break-other-fields)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Schema migrations for suggested_laji and onboarding_draft.laji</name>
  <files>supabase/migrations/20260623190347_business_branding_suggested_laji.sql, supabase/migrations/20260623190348_onboarding_draft_add_laji.sql</files>
  <read_first>
    - supabase/migrations/20260616110000_business_branding_selected_logo_url.sql (exact analog: bare nullable TEXT additive column on business_branding, ADD COLUMN IF NOT EXISTS, header comment style)
    - supabase/migrations/20260606000000_onboarding.sql (defines onboarding_draft columns; its own ALTER TABLE business_accounts ADD COLUMN IF NOT EXISTS onboarding_completed idiom is the analog for the onboarding_draft.laji add)
    - supabase/migrations/20260622120000_remove_google_places_data.sql (most recent migration — confirm timestamp ordering so the new files sort last)
  </read_first>
  <action>
    Create two additive migration files following the verified naming convention `YYYYMMDDHHMMSS_description.sql`. File one, `20260623190347_business_branding_suggested_laji.sql`: a header comment block (`-- Phase 55 Task 1: additive column for Claude's raw laji suggestion (AI-06).`, `-- Analog source: 20260616110000_business_branding_selected_logo_url.sql`, plus a note that this holds the UNCONFIRMED suggestion — the user-confirmed value lives separately in onboarding_draft.laji per D-04 and is never read from this column), then `ALTER TABLE business_branding ADD COLUMN IF NOT EXISTS suggested_laji TEXT;`. File two, `20260623190348_onboarding_draft_add_laji.sql`: header comment block (`-- Phase 55 Task 1: additive column for the user-confirmed sport category (AI-06, D-04).`, `-- Analog source: 20260606000000_onboarding.sql`, note that this holds the CONFIRMED laji written exclusively via POST /api/business/onboarding/save-step field 'laji' after explicit Vahvista/Vaihda, copied into liikuntapaikat.laji at submit), then `ALTER TABLE onboarding_draft ADD COLUMN IF NOT EXISTS laji TEXT;`. Both columns are bare nullable TEXT with no default. Always `ADD COLUMN IF NOT EXISTS` (idempotent under retried migration runs) — never a bare ADD COLUMN. Do not apply the migration (no `supabase db push`) — file-authoring only; the column takes effect on the next local/CI db push, which the analyzer/route tests do not require since they mock the DB.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260623190347_business_branding_suggested_laji.sql && test -f supabase/migrations/20260623190348_onboarding_draft_add_laji.sql && grep -q 'ADD COLUMN IF NOT EXISTS suggested_laji TEXT' supabase/migrations/20260623190347_business_branding_suggested_laji.sql && grep -q 'ADD COLUMN IF NOT EXISTS laji TEXT' supabase/migrations/20260623190348_onboarding_draft_add_laji.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260623190347_business_branding_suggested_laji.sql` exists and contains `ALTER TABLE business_branding` and `ADD COLUMN IF NOT EXISTS suggested_laji TEXT`
    - File `supabase/migrations/20260623190348_onboarding_draft_add_laji.sql` exists and contains `ALTER TABLE onboarding_draft` and `ADD COLUMN IF NOT EXISTS laji TEXT`
    - Both files carry a header comment citing the phase, AI-06, and the analog source migration
    - Neither file uses a bare `ADD COLUMN` without `IF NOT EXISTS`
  </acceptance_criteria>
  <done>Both migrations exist as additive nullable-TEXT columns following the verified analog style; not yet applied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prompt schema + analyzer allowlist validation for suggested_laji</name>
  <files>lib/branding/prompt.ts, lib/branding/analyzer.ts, lib/branding/brandingResult.ts, lib/branding/analyzer.test.ts</files>
  <read_first>
    - lib/branding/prompt.ts (BRANDING_ANALYSIS_PROMPT — the JSON schema section and field-rules section where logos[].type / colors[].role enums are declared; see 55-PATTERNS.md "lib/branding/prompt.ts" for exact line locations)
    - lib/branding/analyzer.ts (VALID_LOGO_TYPES/VALID_COLOR_ROLES allowlist consts at lines 41-42; BrandingAnalysisResult interface lines 28-39; the raw-result parse cast; the validation steps 6-7; the return object — all read in this conversation, exact verbatim shapes in 55-PATTERNS.md "lib/branding/analyzer.ts")
    - lib/branding/brandingResult.ts (BrandingResult type, logo_type field placement — see 55-PATTERNS.md "lib/branding/brandingResult.ts")
    - lib/lajit.ts (lajiKonfig Record export, exactly 9 keys: padel, tennis, jooga, kuntosali, uinti, kiipeily, jääkiekko, liikuntahalli, liikunta — no 'Muu' key)
    - lib/branding/analyzer.test.ts (existing 'defaults an invalid logo type to "unknown"' / 'filters non-hex colors and defaults an invalid role to "unknown"' cases — the exact mock-Claude-response shape to extend)
  </read_first>
  <behavior>
    - Test: when the mocked Claude JSON response includes `"laji": "padel"` (a valid taxonomy key), `analyzeWithClaude(...).suggested_laji === 'padel'`
    - Test: when the mocked response includes `"laji": "sulkapallo"` (a non-taxonomy string), `suggested_laji === null` (discard, NOT default to a sentinel) — D-03
    - Test: when the mocked response includes `"laji": "Padel ja tennis"` (free text), `suggested_laji === null`
    - Test: when the mocked response omits `laji` entirely, `suggested_laji === null` AND `logos`/`colors`/`prices`/`opening_hours` remain correctly shaped (does not throw) — AI-06 criterion 4 regression guard
    - Test: when the mocked response sets `"laji": null`, `suggested_laji === null`
  </behavior>
  <action>
    In `lib/branding/prompt.ts`: import `lajiKonfig` from `@/lib/lajit` (prompt.ts is server-only, imported only by analyzer.ts, so this is safe). Add a sibling top-level `laji` key to the JSON schema section of `BRANDING_ANALYSIS_PROMPT`, mirroring the existing `logos[].type`/`colors[].role` enum-constrained declarations — build the enum value list at module load by interpolating `Object.keys(lajiKonfig).join('" | "')` into the template literal so it never drifts from lib/lajit.ts (do NOT hand-type the 9 strings — RESEARCH.md anti-pattern). Add a `laji:` field-rules block: instruct Claude to infer the single most likely sport/activity category, choose ONLY from the interpolated key list, return `null` (not an empty string, not a guess) when uncertain or the site gives no clear sport signal, and never return free text outside the listed keys. Note in the rules that `laji` is a scalar — use `null`, not `[]`.

    In `lib/branding/analyzer.ts`: add `import { lajiKonfig } from '@/lib/lajit'`. Add `const VALID_LAJI_KEYS: string[] = Object.keys(lajiKonfig)` next to the existing `VALID_LOGO_TYPES`/`VALID_COLOR_ROLES` consts. Add `suggested_laji: string | null` to the `BrandingAnalysisResult` interface (after `logo_index`, additive). Add `laji?: unknown` to the raw-result parse cast object type. Add the discard-to-null validation: `const rawLaji = typeof result.laji === 'string' ? result.laji : null` then `const suggested_laji: string | null = rawLaji && VALID_LAJI_KEYS.includes(rawLaji) ? rawLaji : null`. This MUST discard to `null` on mismatch (NOT default to a sentinel string like the logo `'unknown'` path) because the UI's unconfirmed state (D-03) keys on `suggested_laji === null`. Add `suggested_laji` to the function's return object. No new try/catch — the derivation is a pure string check that cannot throw.

    In `lib/branding/brandingResult.ts`: add `suggested_laji: string | null` to the `BrandingResult` type, placed next to `logo_type` (mirrors its nullability/placement exactly). Do not touch `getContrastColor` or `buildBrandingPreview`.

    In `lib/branding/analyzer.test.ts`: extend the existing suite with the cases in `<behavior>`, reusing the exact mock-Claude-response pattern already used for the logo-type/color-role allowlist tests — add `laji` to the mocked JSON and assert `result.suggested_laji`. Name at least one test so it matches `-t "laji"`.
  </action>
  <verify>
    <automated>npx vitest run lib/branding/analyzer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/branding/analyzer.test.ts` passes including the new `laji` cases
    - `npx vitest run lib/branding/analyzer.test.ts -t "laji"` matches and passes at least one named test
    - `grep -q 'VALID_LAJI_KEYS' lib/branding/analyzer.ts` and the const is assigned `Object.keys(lajiKonfig)` (not a hardcoded array)
    - `grep -q 'suggested_laji' lib/branding/brandingResult.ts`
    - `lib/branding/prompt.ts` references `lajiKonfig` and interpolates `Object.keys(lajiKonfig)` into the prompt enum (no hand-typed 9-string list)
    - An out-of-taxonomy mocked Claude `laji` yields `suggested_laji === null` (asserted in test), never a sentinel
  </acceptance_criteria>
  <done>analyzeWithClaude returns a taxonomy-validated `suggested_laji` (null on any mismatch/omission); the prompt instructs Claude with the live 9-key enum; BrandingResult carries the field; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Persist suggested_laji in business_branding and surface in GET</name>
  <files>app/api/business/analyze-website/route.ts</files>
  <read_first>
    - app/api/business/analyze-website/route.ts (the `runAnalysis` background pipeline's `business_branding` UPSERT — where existing columns like selected_logo_url / logo_type are written; and the GET handler's `.select(...)` column list + the object it returns as BrandingResult. See 55-RESEARCH.md "Canonical References" and "System Architecture Diagram" for the exact integration points.)
    - lib/branding/analyzer.ts (confirm analyzeWithClaude now returns suggested_laji — produced in Task 2)
    - lib/branding/brandingResult.ts (confirm BrandingResult now has suggested_laji — produced in Task 2)
  </read_first>
  <action>
    In `runAnalysis`, add `suggested_laji: result.suggested_laji` (where `result` is the `analyzeWithClaude(...)` return) to the `business_branding` UPSERT payload object, alongside the existing analyzed-status columns. Do NOT add any new error handling — the existing UPSERT error path covers the widened payload. In the GET handler, add `suggested_laji` to the `.select(...)` column-list string for `business_branding`, and add `suggested_laji: <row>.suggested_laji ?? null` to the `BrandingResult` object the handler returns, mirroring how `selected_logo_url`/`logo_type` are selected and mapped. Do not change the response shape for any other field. The confirmed value is NOT touched here — that lives in onboarding_draft.laji (D-04) and is handled by Plans 02/03.
  </action>
  <verify>
    <automated>grep -c 'suggested_laji' app/api/business/analyze-website/route.ts | { read n; [ "$n" -ge 2 ] && echo OK || echo "expected >=2 suggested_laji refs, got $n"; }</automated>
  </verify>
  <acceptance_criteria>
    - `suggested_laji` appears in the `business_branding` UPSERT payload inside `runAnalysis`
    - `suggested_laji` appears in the GET handler's `.select(...)` column list
    - The GET handler's returned `BrandingResult` object includes `suggested_laji`
    - No other field's persistence or response mapping is changed (logo/colors/prices/hours untouched)
    - `npx tsc --noEmit` reports no new type errors in this file (BrandingResult now requires the field)
  </acceptance_criteria>
  <done>The validated AI suggestion is persisted to business_branding.suggested_laji by the background pipeline and returned in the GET BrandingResult, ready for the client to render.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped site → Claude → analyzer | Untrusted scraped HTML/screenshot content can steer Claude's free-form output |
| Claude response → business_branding | LLM output is untrusted; must not pass through unvalidated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Tampering | `analyzer.ts` suggested_laji derivation | mitigate | Server-side allowlist validation against `Object.keys(lajiKonfig)`; any out-of-taxonomy or non-string value is discarded to `null` (Task 2), identical pattern to existing logo.type/color.role validation. Prompt-injection via scraped content cannot produce a stored non-taxonomy laji. |
| T-55-02 | Tampering | analyze-website GET/UPSERT | accept | `suggested_laji` is rendered as plain category text downstream (no HTML sink); the value is already constrained to the 9-key allowlist by T-55-01, so the GET surface carries no additional injection risk. |
</threat_model>

<verification>
- `npx vitest run lib/branding/analyzer.test.ts` green (allowlist + criterion-4 omission cases)
- `npm test` full suite green (no regression in existing branding tests)
- `npx tsc --noEmit` clean (BrandingAnalysisResult/BrandingResult field additions type-check)
- Both migration files present with correct DDL
</verification>

<success_criteria>
- AI-06 criterion 1: Claude's suggestion is constrained to the lib/lajit.ts taxonomy; invalid output discarded to null
- AI-06 criterion 4: logo/color/price/hours extraction unaffected when laji is omitted (unit-asserted)
- suggested_laji persisted and returned via GET for the client (Plan 03) to consume
- onboarding_draft.laji column exists for the confirmed-value staging (Plan 02/03)
</success_criteria>

<output>
Create `.planning/phases/55-ai-lajiluokitus-sivuanalyysiin/55-01-SUMMARY.md` when done
</output>
