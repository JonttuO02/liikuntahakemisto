---
phase: 20-navigaatio-korjaukset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/NavPill.tsx
  - app/suosikit/SuosikitClient.tsx
autonomous: true
requirements:
  - NAV-04
  - NAV-05
must_haves:
  truths:
    - "NavPill expanded menu contains no link to /?nakyma=lista or any Haku / Search entry"
    - "NavPill expanded menu contains exactly: Profiili, Suosikit, Kirjaudu (tai Kirjaudu ulos)"
    - "SuosikitClient unauthenticated-state 'Takaisin hakemistoon' navigates to /"
    - "SuosikitClient empty-state 'Selaa hakemistoa' navigates to /"
    - "SuosikitClient authenticated-state 'Takaisin hakemistoon' navigates to /"
  artifacts:
    - path: "app/components/NavPill.tsx"
      provides: "NavPill without Haku link"
      contains: "no /?nakyma=lista"
    - path: "app/suosikit/SuosikitClient.tsx"
      provides: "SuosikitClient with corrected back-links"
      contains: "href=\"/\""
  key_links:
    - from: "app/components/NavPill.tsx"
      to: "/"
      via: "Profiili and Suosikit links remain"
      pattern: "href=\"/profiili\""
    - from: "app/suosikit/SuosikitClient.tsx"
      to: "/"
      via: "three Link elements"
      pattern: "href=\"/\""
---

<objective>
Remove the dead "Haku" navigation link from NavPill (NAV-04) and fix three broken back-links in SuosikitClient that still point to the removed /?nakyma=lista route (NAV-05).

Purpose: Eliminates two classes of broken navigation — the NavPill Haku link goes nowhere useful, and SuosikitClient's back-buttons land on a dead route.
Output: NavPill.tsx with Haku block deleted; SuosikitClient.tsx with three href values corrected.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-navigaatio-korjaukset/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove "Haku" link from NavPill dropdown (NAV-04)</name>
  <files>app/components/NavPill.tsx</files>
  <read_first>app/components/NavPill.tsx — read in full before editing; the current file is 109 lines</read_first>
  <action>
    Delete the entire Link block at lines 57–60 that renders the "Haku" entry in the expanded NavPill menu. The block is:

      Link href="/?nakyma=lista" onClick={() => setOpen(false)} className={BTN}
        Search className="w-3.5 h-3.5" /
        Haku
      /Link

    After deleting this block, also remove Search from the lucide-react import on line 6 — the import currently is:
      import { Search, Heart, User, LogOut, MoreHorizontal, X } from 'lucide-react'
    Remove Search from this import since it is only used by the deleted block. The resulting import should be:
      import { Heart, User, LogOut, MoreHorizontal, X } from 'lucide-react'

    The remaining expanded menu items (Profiili link, Suosikit link, Kirjaudu button) are unchanged. Per D-09.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/components/NavPill.tsx does not contain the string "nakyma=lista"
    - app/components/NavPill.tsx does not contain "Haku" as a JSX text node
    - app/components/NavPill.tsx import line does not import Search from lucide-react
    - app/components/NavPill.tsx still contains href="/profiili" and href="/suosikit"
    - npm run build exits 0
  </acceptance_criteria>
  <done>NavPill expanded menu has no Haku/Search entry; dead /?nakyma=lista link is gone; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix three back-link hrefs in SuosikitClient (NAV-05)</name>
  <files>app/suosikit/SuosikitClient.tsx</files>
  <read_first>app/suosikit/SuosikitClient.tsx — read in full before editing; the current file is 142 lines</read_first>
  <action>
    Replace all three occurrences of href="/?nakyma=lista" with href="/" in the file. The three locations are:

    1. Line 73 — unauthenticated state, Link text "Takaisin hakemistoon": change href from "/?nakyma=lista" to "/"
    2. Line 104 — empty-state (authenticated, no favorites), Link text "Selaa hakemistoa": change href from "/?nakyma=lista" to "/"
    3. Line 134 — authenticated state with favorites, Link text "Takaisin hakemistoon": change href from "/?nakyma=lista" to "/"

    No other changes to this file. The Link classNames, text content, and surrounding JSX are unchanged. Per D-10.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/suosikit/SuosikitClient.tsx contains zero occurrences of "nakyma=lista"
    - app/suosikit/SuosikitClient.tsx contains exactly three Link elements with href="/"
    - The three link texts are preserved: "Takaisin hakemistoon" (×2) and "Selaa hakemistoa" (×1)
    - npm run build exits 0
  </acceptance_criteria>
  <done>All three back-links in SuosikitClient point to "/" — no broken /?nakyma=lista references remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan makes only JSX/href changes — no data flows cross any trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | NavPill.tsx / SuosikitClient.tsx | accept | Pure client-side JSX edits; no data, no auth, no PII involved |
| T-20-SC | Tampering | npm installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:
- Grep app/components/NavPill.tsx for "nakyma=lista" — must return zero matches
- Grep app/suosikit/SuosikitClient.tsx for "nakyma=lista" — must return zero matches
- npm run build exits 0
</verification>

<success_criteria>
- NAV-04: NavPill dropdown contains Profiili, Suosikit, and Kirjaudu — no Haku link
- NAV-05: All three SuosikitClient back-links navigate to "/" (homepage)
- Build passes with no TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/20-navigaatio-korjaukset/20-01-SUMMARY.md` when done
</output>
