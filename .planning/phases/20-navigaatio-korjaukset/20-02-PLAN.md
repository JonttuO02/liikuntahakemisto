---
phase: 20-navigaatio-korjaukset
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Etusivu.tsx
  - app/components/DiagonaalKortti.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
must_haves:
  truths:
    - "Homepage loads with bottom sheet in closed (pill) state, then animates open immediately without user interaction"
    - "When /?id=X is in the URL, auto-open effect does not fire — focusId effect handles the sheet"
    - "Navigating to /paikat/ID from a DiagonaalKortti stores scroll position + all search filters in sessionStorage key 'etusivu-scroll-state'"
    - "Returning to / from a venue profile page restores the search overlay open, the previous filter state, and scrolls the list to the previous position"
    - "sessionStorage key 'etusivu-scroll-state' is deleted immediately after being read on mount — not re-applied on subsequent visits"
    - "'Näytä kartalla' from /paikat/[id] already uses /?id=${paikka.id} and triggers focusId effect (no code changes needed for NAV-02)"
  artifacts:
    - path: "app/components/Etusivu.tsx"
      provides: "sheetPhase init 'closed', auto-open effect, scroll restore effect, handleCardClick, searchResultsRef, onCardClick prop on DiagonaalKortti"
      contains: "useState<'open' | 'sliding' | 'closed'>('closed')"
    - path: "app/components/DiagonaalKortti.tsx"
      provides: "onCardClick prop in interface + destructuring + onClick on Link"
      contains: "onCardClick?: () => void"
  key_links:
    - from: "app/components/DiagonaalKortti.tsx"
      to: "sessionStorage"
      via: "onCardClick?.() fires in Link onClick before navigation"
      pattern: "onCardClick\\?\\.(\\(\\))"
    - from: "app/components/Etusivu.tsx"
      to: "searchResultsRef.current.scrollTop"
      via: "requestAnimationFrame in mount restore effect"
      pattern: "searchResultsRef\\.current\\.scrollTop"
    - from: "app/components/Etusivu.tsx"
      to: "sheetPhase"
      via: "auto-open useEffect with focusId guard"
      pattern: "if \\(!focusId\\) setSheetPhase"
---

<objective>
Implement bottom-sheet auto-open animation on homepage load (NAV-03) and scroll+state persistence for back-navigation from venue profiles (NAV-01). Confirm NAV-02 requires no code changes.

Purpose: Users returning from a venue profile land at their exact previous list position with filters restored. The homepage bottom sheet now slides up gracefully on load instead of appearing pre-rendered open.
Output: Etusivu.tsx with sheetPhase init changed + two new effects + handleCardClick + searchResultsRef + onCardClick prop wired; DiagonaalKortti.tsx with onCardClick prop added to interface and Link.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-navigaatio-korjaukset/20-CONTEXT.md
@.planning/phases/20-navigaatio-korjaukset/20-PATTERNS.md
</context>

<interfaces>
Current state of key types and signatures the executor must respect:

From app/components/DiagonaalKortti.tsx (lines 34–40):
  interface DiagonaalKorttiProps {
    paikka: Liikuntapaikka
    distanceStr?: string
    onShowMap?: (paikka: Liikuntapaikka) => void
  }
  export default function DiagonaalKortti({ paikka, distanceStr, onShowMap }: DiagonaalKorttiProps)

From app/components/Etusivu.tsx (line 167):
  const [sheetPhase, setSheetPhase] = useState<'open' | 'sliding' | 'closed'>('open')

From app/components/Etusivu.tsx (lines 188–191) — existing refs:
  const inFlight = useRef<Set<number>>(new Set())
  const debounceRef = useRef<ReturnType<typeof setTimeout> | null>(null)
  const pendingValittuRef = useRef<Liikuntapaikka | null>(null)
  const zoomRef = useRef(14)

From app/components/Etusivu.tsx (lines 181–186) — search state variables:
  const [searchOpen, setSearchOpen]           = useState(false)
  const [searchHaku, setSearchHaku]           = useState('')
  const [searchLaji, setSearchLaji]           = useState('Kaikki')
  const [searchAukinyt, setSearchAukinyt]     = useState(false)
  const [searchKertakaynti, setSearchKertakaynti] = useState(false)
  const [searchKaupunki, setSearchKaupunki]   = useState('Kaikki')

From app/components/Etusivu.tsx (lines 359–366) — existing focusId effect (must remain unchanged):
  useEffect(() => {
    if (!focusId) return
    const id = Number(focusId)
    const target = paikat.find(p => p.id === id)
    if (!target || target.latitude == null || target.longitude == null) return
    setAutoZoomTarget({ lat: target.latitude, lng: target.longitude })
    setSheetPhase('sliding')
  }, [focusId, paikat]) // eslint-disable-line react-hooks/exhaustive-deps

From app/components/Etusivu.tsx (line 57 in DiagonaalKortti.tsx) — current Link:
  Link href={`/paikat/${paikka.id}`} className="absolute inset-0 block"

From app/components/Etusivu.tsx (lines 980–991) — current DiagonaalKortti usage:
  DiagonaalKortti
    key={p.id}
    paikka={p}
    distanceStr={distancesMap[p.id] != null ? formatDistance(distancesMap[p.id]) : undefined}
    onShowMap={(paikka) => {
      setSearchOpen(false)
      if (paikka.latitude != null && paikka.longitude != null) {
        setAutoZoomTarget({ lat: paikka.latitude, lng: paikka.longitude })
      }
    }}
  /DiagonaalKortti
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DiagonaalKortti — add onCardClick prop to interface and Link (NAV-01)</name>
  <files>app/components/DiagonaalKortti.tsx</files>
  <read_first>app/components/DiagonaalKortti.tsx — read in full before editing; 140-ish lines</read_first>
  <action>
    Make three changes to DiagonaalKortti.tsx:

    1. Extend DiagonaalKorttiProps interface (lines 34–38) by adding the optional onCardClick prop:
       Add "onCardClick?: () => void" as a new line inside the interface, after the onShowMap line.

    2. Destructure onCardClick in the function signature (line 40):
       Change "{ paikka, distanceStr, onShowMap }" to "{ paikka, distanceStr, onShowMap, onCardClick }".

    3. Add onClick to the Link element (line 57 — the full-card Link with className="absolute inset-0 block"):
       Add "onClick={() => onCardClick?.()}" as a prop on this Link element.
       Use the nullish-safe optional chain form "onCardClick?.()" — same pattern as existing "onShowMap?.(paikka)" at line 134.
       No preventDefault is needed — navigation must proceed normally.

    Per D-01, D-03.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - DiagonaalKortti.tsx interface block contains "onCardClick?: () => void"
    - DiagonaalKortti.tsx function signature destructures onCardClick
    - DiagonaalKortti.tsx Link element (absolute inset-0 block) has onClick prop calling onCardClick?.()
    - npm run build exits 0 (TypeScript accepts the new optional prop)
  </acceptance_criteria>
  <done>DiagonaalKortti accepts onCardClick callback; Link fires it before navigating to /paikat/ID.</done>
</task>

<task type="auto">
  <name>Task 2: Etusivu — sheetPhase init, auto-open effect, scroll restore effect, handleCardClick, ref (NAV-01 + NAV-03)</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    app/components/Etusivu.tsx — read lines 160–400 before editing to see current state init, refs, effects, and DiagonaalKortti usage
    app/components/DiagonaalKortti.tsx — already modified in Task 1; verify onCardClick prop exists
  </read_first>
  <action>
    Make six targeted edits to Etusivu.tsx. Read the current file carefully before each edit.

    EDIT 1 — sheetPhase initial value (NAV-03):
    Change line 167 from:
      useState<'open' | 'sliding' | 'closed'>('open')
    to:
      useState<'open' | 'sliding' | 'closed'>('closed')

    EDIT 2 — add searchResultsRef after the existing refs block (NAV-01):
    After the existing "const zoomRef = useRef(14)" line (line 191), add:
      const searchResultsRef = useRef<HTMLDivElement>(null)

    EDIT 3 — add handleCardClick function after openSearch function (NAV-01):
    The openSearch function ends around line 236 (it closes with the setSearchOpen(true) call).
    After the closing brace of openSearch, add the handleCardClick function:
      function handleCardClick() {
        try {
          const scrollTop = searchResultsRef.current?.scrollTop ?? 0
          const state = {
            scrollTop,
            searchHaku,
            searchLaji,
            searchKertakaynti,
            searchAukinyt,
            searchKaupunki,
            searchOpen: true,
          }
          sessionStorage.setItem('etusivu-scroll-state', JSON.stringify(state))
        } catch {}
      }
    Per D-01, D-02, D-03, D-05.

    EDIT 4 — add sessionStorage restore effect as the FIRST useEffect in the component (NAV-01):
    The first useEffect in the component is currently the auth/suosikit effect around line 275.
    Insert a new useEffect immediately BEFORE it:
      useEffect(() => {
        try {
          const raw = sessionStorage.getItem('etusivu-scroll-state')
          if (!raw) return
          sessionStorage.removeItem('etusivu-scroll-state')
          const s = JSON.parse(raw)
          setSearchHaku(s.searchHaku ?? '')
          setSearchLaji(s.searchLaji ?? 'Kaikki')
          setSearchKertakaynti(s.searchKertakaynti ?? false)
          setSearchAukinyt(s.searchAukinyt ?? false)
          setSearchKaupunki(s.searchKaupunki ?? 'Kaikki')
          if (s.searchOpen) setSearchOpen(true)
          if (s.scrollTop) {
            requestAnimationFrame(() => {
              if (searchResultsRef.current) {
                searchResultsRef.current.scrollTop = s.scrollTop
              }
            })
          }
        } catch {}
      }, []) // eslint-disable-line react-hooks/exhaustive-deps
    Per D-04, D-05. The removeItem call MUST happen before any state setters.

    EDIT 5 — add auto-open effect IMMEDIATELY BEFORE the existing focusId effect (NAV-03):
    The focusId effect is at lines 359–366. Insert a new useEffect immediately before it:
      useEffect(() => {
        if (!focusId) setSheetPhase('open')
      }, []) // eslint-disable-line react-hooks/exhaustive-deps
    Per D-06, D-07, D-08. The focusId guard ensures this does not fire when /?id=X is in the URL.
    Do NOT modify the existing focusId effect.

    EDIT 6 — add ref to search-results motion.div and onCardClick to DiagonaalKortti usage (NAV-01):
    (a) The search-results motion.div (around line 920) currently starts with:
          motion.div key="search-results" initial={{ opacity: 0 }}
        Add ref={searchResultsRef} as a prop on this motion.div element.
    (b) The DiagonaalKortti usage (around lines 981–991) currently passes paikka, distanceStr, and onShowMap.
        Add "onCardClick={handleCardClick}" as an additional prop.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Etusivu.tsx line containing sheetPhase useState reads useState<'open' | 'sliding' | 'closed'>('closed')
    - Etusivu.tsx contains "const searchResultsRef = useRef<HTMLDivElement>(null)"
    - Etusivu.tsx contains "sessionStorage.getItem('etusivu-scroll-state')" in a useEffect
    - Etusivu.tsx contains "sessionStorage.removeItem('etusivu-scroll-state')" called before any state setter in that same effect
    - Etusivu.tsx contains "requestAnimationFrame" for the scrollTop restore
    - Etusivu.tsx contains "if (!focusId) setSheetPhase('open')" in a useEffect immediately before the focusId effect
    - Etusivu.tsx contains "ref={searchResultsRef}" on the search-results motion.div
    - Etusivu.tsx DiagonaalKortti usage includes "onCardClick={handleCardClick}"
    - Etusivu.tsx contains "sessionStorage.setItem('etusivu-scroll-state'" inside handleCardClick
    - The existing focusId effect at lines 359–366 is unchanged (still has setSheetPhase('sliding') and [focusId, paikat] deps)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Etusivu loads with sheet closed (pill), animates open immediately unless /?id=X is present; returning from a venue profile restores full search state and scroll position.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessionStorage → Etusivu state | UI state (scroll offset + filter strings + booleans) written and read within the same tab origin; no server involvement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | Information Disclosure | sessionStorage key 'etusivu-scroll-state' | accept | Stores only scroll offset and filter UI state — no PII, no credentials; tab-scoped storage, cleared on read (D-04) |
| T-20-03 | Tampering | sessionStorage.getItem parse | mitigate | JSON.parse is wrapped in try/catch (established pattern); all restored values use nullish defaults (??); no eval, no dynamic code execution |
| T-20-SC | Tampering | npm installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:
- npm run build exits 0 with no TypeScript errors
- Manual: Homepage at / loads with map visible (sheet in pill/closed state), then sheet slides up via spring animation (~damping 28, stiffness 280, delay 0.1)
- Manual: Homepage at /?id=5 loads with sheet in sliding/closed state (auto-open does NOT fire) and map centers on venue
- Manual: Open search overlay, scroll down, tap a DiagonaalKortti → navigate to /paikat/ID → tap "Takaisin hakemistoon" → land at exact scroll position with filters restored and search overlay open
- Manual (NAV-02 confirmation — no code change needed): "Näytä kartalla" from any /paikat/[id] page uses /?id=X URL and triggers map centering without GPS activation; sheet stays in sliding/closed state
</verification>

<success_criteria>
- NAV-01: Back navigation from venue profile restores scroll position + all 5 search filters + searchOpen state
- NAV-02: "Näytä kartalla" already uses /?id=X — confirmed correct, no regression
- NAV-03: Bottom sheet starts closed ('closed' initial state), auto-open effect fires on mount setting phase to 'open', producing a slide-up animation
- Build passes with no TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/20-navigaatio-korjaukset/20-02-SUMMARY.md` when done
</output>
