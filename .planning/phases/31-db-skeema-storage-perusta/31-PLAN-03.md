---
phase: 31-db-skeema-storage-perusta
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/admin/__tests__/sync-paikat-filter.test.ts
  - app/api/admin/sync-paikat/route.ts
autonomous: true
requirements:
  - DATA-09
must_haves:
  truths:
    - "sync-paikat route fetches existing business_managed=true place_ids before building the upsert batch"
    - "Only place_ids NOT in the managedSet are passed to fetchPlaceDetails and the upsert"
    - "allResults.length is still reported as loydetty in the response (pre-filter count from Places API)"
    - "Unit test covers: managed place_id is excluded from syncResults; non-managed place_id is included"
  artifacts:
    - path: "app/api/admin/__tests__/sync-paikat-filter.test.ts"
      provides: "Unit tests for the business_managed pre-filter logic"
      min_lines: 30
    - path: "app/api/admin/sync-paikat/route.ts"
      provides: "Edited sync route with managedSet pre-filter"
      contains: "managedSet"
  key_links:
    - from: "sync-paikat route"
      to: "liikuntapaikat.business_managed"
      via: "supabaseAdmin.from('liikuntapaikat').select('place_id').eq('business_managed', true)"
      pattern: "business_managed.*true"
---

<objective>
Add the Wave 0 test file for DATA-09 automated verification, then edit the sync-paikat route to pre-filter business-managed venues before the upsert. These are independent of the migration files — the filter logic can be tested with mocked Supabase responses before the column even exists in the live DB.

Purpose: Closes the Wave 0 gap identified in VALIDATION.md; satisfies DATA-09's automated verification requirement. Prevents Google Places sync from overwriting venues that a business owns.
Output: app/api/admin/__tests__/sync-paikat-filter.test.ts (new), app/api/admin/sync-paikat/route.ts (edited)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-db-skeema-storage-perusta/31-CONTEXT.md
@.planning/phases/31-db-skeema-storage-perusta/31-RESEARCH.md
@.planning/phases/31-db-skeema-storage-perusta/31-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs for the sync route edit and test authorship. -->

Current sync route structure (app/api/admin/sync-paikat/route.ts):
  Line 2:   import { supabaseAdmin } from '@/lib/supabaseAdmin.server'
  Lines 137–151: Deduplication loop builds allResults array
  Line 149:  if (allResults.length === 0) early return
  Line 154:  const details = await Promise.all(allResults.map(p => fetchPlaceDetails(p.place_id)))
  Line 156:  const rivit = allResults.map((p, i) => ({ ... }))
  Line 169:  const websiteLoydetty = ...
  Lines 171–174: supabaseAdmin.from('liikuntapaikat').upsert(rivit, ...).select('id')
  Line 183:  loydetty: allResults.length  ← keep allResults here (Places API total, before filter)

Pre-filter insertion point: after line 151 (after allResults.length === 0 check), before line 154.
After insertion, replace all uses of allResults in lines 154, 156 with syncResults.
Keep allResults.length for the loydetty response field.

Pre-filter block to insert:
  const { data: managedRows } = await supabaseAdmin
    .from('liikuntapaikat')
    .select('place_id')
    .eq('business_managed', true)
  const managedSet = new Set((managedRows ?? []).map(r => r.place_id))
  const syncResults = allResults.filter(r => !managedSet.has(r.place_id))
  if (syncResults.length === 0) {
    return NextResponse.json({ loydetty: allResults.length, tallennettu: 0, website_loydetty: 0 })
  }

Test file location: app/api/admin/__tests__/sync-paikat-filter.test.ts
Test framework: vitest (vitest.config.ts exists; no new packages needed)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Wave 0 unit test for sync-paikat business_managed filter</name>
  <files>app/api/admin/__tests__/sync-paikat-filter.test.ts</files>
  <read_first>
    - app/api/admin/sync-paikat/route.ts — understand the allResults array shape (PlacesResult & { assignedLaji: string }) and the managedSet filter logic
    - vitest.config.ts — verify test discovery pattern (should find **/__tests__/*.test.ts)
  </read_first>
  <behavior>
    - Test 1: managedSet correctly excludes a place_id that appears in managedRows
      Given managedRows = [{ place_id: 'managed_place_1' }]
      And allResults = [{ place_id: 'managed_place_1', name: 'Gym A' }, { place_id: 'open_place_2', name: 'Gym B' }]
      When buildSyncResults(allResults, managedRows) is called
      Then syncResults contains only { place_id: 'open_place_2' }
      And syncResults.length === 1

    - Test 2: all results pass through when managedRows is empty
      Given managedRows = []
      And allResults = [{ place_id: 'place_1' }, { place_id: 'place_2' }]
      When buildSyncResults(allResults, managedRows) is called
      Then syncResults.length === 2

    - Test 3: null managedRows (Supabase error path) treated as empty — no crash
      Given managedRows = null
      And allResults = [{ place_id: 'place_1' }]
      When buildSyncResults(allResults, managedRows) is called
      Then syncResults.length === 1

    - Test 4: allResults.length is preserved as the loydetty count regardless of filter
      Given managedRows = [{ place_id: 'place_1' }]
      And allResults = [{ place_id: 'place_1' }, { place_id: 'place_2' }]
      Then allResults.length === 2 (loydetty) while syncResults.length === 1 (tallennettu)
  </behavior>
  <action>
Create app/api/admin/__tests__/ directory and write sync-paikat-filter.test.ts.

The test targets the filter logic in isolation. Extract the pure function by defining a testable helper locally in the test file (do NOT import from route.ts — the route has side effects and cannot be imported in unit tests without full mocking). Define the helper inline:

function buildSyncResults(
  allResults: Array<{ place_id: string; [key: string]: unknown }>,
  managedRows: Array<{ place_id: string }> | null
): Array<{ place_id: string; [key: string]: unknown }> {
  const managedSet = new Set((managedRows ?? []).map(r => r.place_id))
  return allResults.filter(r => !managedSet.has(r.place_id))
}

Write the four test cases described in the behavior block. Use describe('buildSyncResults') and it() blocks. No mocking of Supabase needed — the function is pure.

Import pattern: import { describe, it, expect } from 'vitest'
  </action>
  <verify>
    <automated>npx vitest run app/api/admin/__tests__/sync-paikat-filter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - app/api/admin/__tests__/sync-paikat-filter.test.ts exists
    - File contains describe('buildSyncResults') or equivalent grouping
    - File contains at least 4 it() or test() blocks
    - All four test cases from behavior block are covered
    - npx vitest run app/api/admin/__tests__/sync-paikat-filter.test.ts exits 0 with all tests passing
    - Full suite npx vitest run also exits 0 (no regressions)
  </acceptance_criteria>
  <done>Test file exists with 4 passing tests covering: exclusion of managed place_id, pass-through of empty managedRows, null-safety, and loydetty/tallennettu count distinction</done>
</task>

<task type="auto">
  <name>Task 2: Edit sync-paikat route to pre-filter business_managed venues</name>
  <files>app/api/admin/sync-paikat/route.ts</files>
  <read_first>
    - app/api/admin/sync-paikat/route.ts — read the full file to understand current line numbers for the deduplication block and the exact insertion point
  </read_first>
  <action>
Edit app/api/admin/sync-paikat/route.ts.

Insertion point: after the existing `if (allResults.length === 0)` early return block and BEFORE the `const details = await Promise.all(allResults.map(...))` line.

Insert these lines (verbatim logic per PATTERNS.md):
  // Pre-filter: exclude venues managed by businesses (business_managed = true)
  // Note: upsert() cannot be WHERE-filtered — must exclude in TypeScript before building rivit
  const { data: managedRows } = await supabaseAdmin
    .from('liikuntapaikat')
    .select('place_id')
    .eq('business_managed', true)

  const managedSet = new Set((managedRows ?? []).map(r => r.place_id))
  const syncResults = allResults.filter(r => !managedSet.has(r.place_id))

  if (syncResults.length === 0) {
    return NextResponse.json({ loydetty: allResults.length, tallennettu: 0, website_loydetty: 0 })
  }

After inserting, update references in the lines that follow:
- Change: const details = await Promise.all(allResults.map(p => fetchPlaceDetails(p.place_id)))
  To:     const details = await Promise.all(syncResults.map(p => fetchPlaceDetails(p.place_id)))
- Change: const rivit = allResults.map((p, i) => ({
  To:     const rivit = syncResults.map((p, i) => ({

Keep allResults.length in the final response body's loydetty field — this is intentional (reports total from Places API, before the business filter).

Do NOT change any other logic, error handling, imports, or type definitions. Do NOT alter the deduplication loop. Do NOT change the ADMIN_SECRET auth check.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - app/api/admin/sync-paikat/route.ts contains "managedSet"
    - File contains "const { data: managedRows } = await supabaseAdmin"
    - File contains ".eq('business_managed', true)"
    - File contains "const syncResults = allResults.filter"
    - File contains "syncResults.map(p => fetchPlaceDetails"
    - File contains "syncResults.map((p, i) =>"
    - File still contains "loydetty: allResults.length" (not syncResults.length)
    - npx vitest run exits 0 (full suite green including the new filter tests)
    - TypeScript: the file has no new type errors (the supabaseAdmin .select chain returns typed data compatible with place_id extraction)
  </acceptance_criteria>
  <done>sync-paikat route pre-filters business_managed=true venues before the fetchPlaceDetails and upsert calls; allResults.length still used for loydetty response field; npx vitest run green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync-paikat route → liikuntapaikat | Pre-filter query uses service-role (supabaseAdmin) — reads all managed rows regardless of RLS |
| Google Places API results → upsert | Managed venues are excluded from the upsert batch in TypeScript before data reaches the DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-09 | Tampering | sync-paikat upsert overwriting business_managed venues | mitigate | Pre-filter fetches all place_ids with business_managed=true via service role, builds exclusion Set, filters allResults before fetchPlaceDetails and rivit construction |
| T-31-10 | Denial of Service | managedRows query latency slowing sync | accept | One additional SELECT on liikuntapaikat before the existing N parallel Place Detail fetches; overhead is negligible compared to outbound API calls |
| T-31-SC | Tampering | npm/pip installs | accept | No npm installs — TypeScript edit only |
</threat_model>

<verification>
After this plan executes:
- grep -c "managedSet" app/api/admin/sync-paikat/route.ts
  Expected: at least 2
- grep -c "business_managed" app/api/admin/sync-paikat/route.ts
  Expected: at least 1
- grep -c "syncResults.map" app/api/admin/sync-paikat/route.ts
  Expected: at least 2 (fetchPlaceDetails call + rivit map)
- grep -c "loydetty: allResults.length" app/api/admin/sync-paikat/route.ts
  Expected: 1 (unchanged)
- npx vitest run app/api/admin/__tests__/sync-paikat-filter.test.ts
  Expected: 4 passing tests
- npx vitest run
  Expected: all tests pass
</verification>

<success_criteria>
- sync-paikat-filter.test.ts has 4 passing tests covering the filter logic
- sync-paikat/route.ts: managedSet pre-filter inserted between allResults.length===0 check and fetchPlaceDetails call
- syncResults (not allResults) drives fetchPlaceDetails and rivit construction
- allResults.length still used for loydetty response field
- npx vitest run green
</success_criteria>

<output>
Create .planning/phases/31-db-skeema-storage-perusta/31-03-SUMMARY.md when done
</output>
