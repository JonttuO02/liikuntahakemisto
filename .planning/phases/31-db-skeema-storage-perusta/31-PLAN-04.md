---
phase: 31-db-skeema-storage-perusta
plan: 04
type: execute
wave: 2
depends_on:
  - "31-PLAN-01"
  - "31-PLAN-02"
  - "31-PLAN-03"
files_modified: []
autonomous: false
requirements:
  - BIZ-02
  - DATA-09
  - DATA-10
must_haves:
  truths:
    - "business_accounts and business_paikka_links tables exist in the live Supabase database"
    - "liikuntapaikat.business_managed column exists in live DB"
    - "profiles.is_admin column exists in live DB"
    - "business-media Storage bucket exists and is public"
    - "is_admin = true is set for joona.orava@gmail.com"
    - "RLS on business_accounts and business_paikka_links is active (anon read returns 0 rows)"
  artifacts:
    - path: "supabase/migrations/20260605000000_business_accounts.sql"
      provides: "Pushed to live DB — tables exist"
    - path: "supabase/migrations/20260605000001_business_managed.sql"
      provides: "Pushed to live DB — column exists"
    - path: "supabase/migrations/20260605000002_profiles_is_admin.sql"
      provides: "Pushed to live DB — column exists"
    - path: "supabase/sql-editor/20260605_business_media_bucket.sql"
      provides: "Executed manually in SQL Editor — bucket + RLS exist"
  key_links:
    - from: "npx supabase db push"
      to: "live Supabase project"
      via: "SUPABASE_ACCESS_TOKEN env var"
      pattern: "supabase db push"
---

<objective>
Push all three migration files to the live Supabase project, then guide the user through two manual steps: running the Storage bucket SQL in the Supabase SQL Editor, and setting is_admin=true for joona.orava@gmail.com.

Purpose: This is the [BLOCKING] schema push required before any Phase 31 verification can pass. Without the live tables and Storage bucket, no downstream v1.7 phase can proceed.
Output: Live database updated; manual verification checklist confirmed by user.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-db-skeema-storage-perusta/31-CONTEXT.md
@.planning/phases/31-db-skeema-storage-perusta/31-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push migrations to live Supabase</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260605000000_business_accounts.sql — verify file exists before push
    - supabase/migrations/20260605000001_business_managed.sql — verify file exists before push
    - supabase/migrations/20260605000002_profiles_is_admin.sql — verify file exists before push
  </read_first>
  <action>
Run the database push command to apply all three new migration files to the linked Supabase project (ref: odkrnesnmrpuegccgovy).

Pre-flight check: Confirm the three migration files exist:
  supabase/migrations/20260605000000_business_accounts.sql
  supabase/migrations/20260605000001_business_managed.sql
  supabase/migrations/20260605000002_profiles_is_admin.sql

Then run: npx supabase db push

If the push requires interactive confirmation, set the SUPABASE_ACCESS_TOKEN environment variable first (retrieve from Supabase Dashboard → Account → Access Tokens). The access token makes the push non-interactive:
  SUPABASE_ACCESS_TOKEN=<token> npx supabase db push

If the push fails with "relation already exists" errors, the tables were already applied manually — check the migration history in Supabase Dashboard → Database → Migrations and mark those migrations as applied if needed.

If the push fails with "relation paikat does not exist" — that is the known stale migration 20260530000000_add_image_url_to_paikat.sql; it has already been applied (or ignored). This error does NOT prevent the new migrations from applying.

After a successful push, run the SQL verification query in the Supabase SQL Editor:
  SELECT table_name FROM information_schema.tables
  WHERE table_name IN ('business_accounts', 'business_paikka_links')
  AND table_schema = 'public';
Expected result: 2 rows.

Also verify columns:
  SELECT column_name FROM information_schema.columns
  WHERE table_name = 'liikuntapaikat' AND column_name = 'business_managed';
Expected: 1 row.

  SELECT column_name FROM information_schema.columns
  WHERE table_name = 'profiles' AND column_name = 'is_admin';
Expected: 1 row.
  </action>
  <verify>
    <automated>npx supabase db push</automated>
  </verify>
  <acceptance_criteria>
    - npx supabase db push exits with code 0
    - Supabase SQL Editor query: SELECT count(*) FROM information_schema.tables WHERE table_name IN ('business_accounts', 'business_paikka_links') AND table_schema = 'public' returns 2
    - Supabase SQL Editor query: SELECT count(*) FROM information_schema.columns WHERE table_name = 'liikuntapaikat' AND column_name = 'business_managed' returns 1
    - Supabase SQL Editor query: SELECT count(*) FROM information_schema.columns WHERE table_name = 'profiles' AND column_name = 'is_admin' returns 1
  </acceptance_criteria>
  <done>supabase db push exits 0; all three new migration files applied; tables and columns confirmed via SQL editor queries</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual — run Storage SQL + set is_admin in Supabase SQL Editor</name>
  <what-built>
    Plan 02 created supabase/sql-editor/20260605_business_media_bucket.sql with:
    - INSERT INTO storage.buckets for 'business-media' (public=true)
    - storage.business_owns_paikka() security-definer function
    - 4 RLS policies on storage.objects (public SELECT, authenticated INSERT/UPDATE/DELETE)

    Plan 02 also documented the is_admin manual UPDATE in the migration comment.
  </what-built>
  <how-to-verify>
    Step 1 — Run the Storage SQL:
    1. Open Supabase Dashboard for project odkrnesnmrpuegccgovy
    2. Go to SQL Editor
    3. Open file supabase/sql-editor/20260605_business_media_bucket.sql in your code editor
    4. Copy the entire file contents and paste into the SQL Editor
    5. Click Run
    6. Expected: "Success. No rows returned" or similar — no errors
    7. Go to Storage → confirm "business-media" bucket appears in the list as Public

    Step 2 — Set is_admin for your account:
    8. In Supabase SQL Editor, run:
       UPDATE profiles SET is_admin = true
       WHERE user_id = (SELECT id FROM auth.users WHERE email = 'joona.orava@gmail.com');
    9. Expected: "1 row affected"
    10. Verify: SELECT is_admin FROM profiles WHERE user_id = (SELECT id FROM auth.users WHERE email = 'joona.orava@gmail.com');
        Expected: true

    Step 3 — Verify RLS on business_accounts:
    11. Using the Supabase Table Editor, switch to "anon" role (or use the anon key in a browser console):
        const { data } = await supabase.from('business_accounts').select('*')
        Expected: empty array [] (RLS blocks anon reads)
  </how-to-verify>
  <resume-signal>Type "approved" after confirming: (1) business-media bucket visible in Storage tab, (2) is_admin=true confirmed for joona.orava@gmail.com, (3) anon read on business_accounts returns empty array</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npx supabase db push → live DB | Uses local Supabase CLI credentials; requires SUPABASE_ACCESS_TOKEN for non-TTY |
| SQL Editor execution | Runs as service role — bypasses RLS; correct for bucket/policy setup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-11 | Tampering | Accidental migration re-apply | accept | All migrations use IF NOT EXISTS / ON CONFLICT DO NOTHING — safe to re-run |
| T-31-12 | Elevation of Privilege | is_admin SQL executed with wrong user email | accept | The UPDATE WHERE clause scopes to exact email joona.orava@gmail.com; incorrect email results in 0 rows affected (safe no-op) |
| T-31-SC | Tampering | npm/pip installs | accept | No new packages; npx supabase is already in use |
</threat_model>

<verification>
Phase 31 is complete when ALL of the following are true:

Automated:
- npx vitest run — all tests pass (including sync-paikat-filter.test.ts)

Manual (Supabase SQL Editor):
- SELECT count(*) FROM information_schema.tables WHERE table_name IN ('business_accounts','business_paikka_links') AND table_schema='public' → 2
- SELECT count(*) FROM information_schema.columns WHERE table_name='liikuntapaikat' AND column_name='business_managed' → 1
- SELECT count(*) FROM information_schema.columns WHERE table_name='profiles' AND column_name='is_admin' → 1
- SELECT count(*) FROM information_schema.tables WHERE table_schema='storage' AND table_name='buckets' AND EXISTS (SELECT 1 FROM storage.buckets WHERE id='business-media') → use Storage tab instead: bucket "business-media" visible and marked Public
- SELECT is_admin FROM profiles WHERE user_id = (SELECT id FROM auth.users WHERE email='joona.orava@gmail.com') → true
- Anon client SELECT on business_accounts → empty array (RLS active)
</verification>

<success_criteria>
- supabase db push exits 0; migration history shows 3 new entries
- business_accounts and business_paikka_links exist in public schema with correct columns
- liikuntapaikat.business_managed and profiles.is_admin columns exist
- business-media Storage bucket is public; RLS policies visible in Supabase Storage → Policies
- is_admin=true confirmed for joona.orava@gmail.com
- npx vitest run green
</success_criteria>

<output>
Create .planning/phases/31-db-skeema-storage-perusta/31-04-SUMMARY.md when done
</output>
