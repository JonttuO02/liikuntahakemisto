---
phase: 30-i18n-fien
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - app/components/NavPill.tsx
  - app/components/Etusivu.tsx
  - app/components/PaikkaKortti.tsx
  - app/components/DiagonaalKortti.tsx
autonomous: true
requirements:
  - I18N-03

must_haves:
  truths:
    - "NavPill shows translated Profiili/Kirjaudu/Kirjaudu ulos labels for both locales"
    - "Etusivu CombinedFilterPill aria-labels and RecenterButton aria-label are translated"
    - "Todo overlay strings (TO DO title, empty state, Ohita, TÄHTIARVOSANA, etc.) are translated"
    - "PaikkaKortti shows translated status/price/CTA labels for both locales"
    - "DiagonaalKortti shows translated open/closed/price labels for both locales"
    - "'Kaikki' state sentinel in Etusivu is NOT replaced by a translation call — only display label uses t('Filters.all')"
  artifacts:
    - path: "app/components/NavPill.tsx"
      provides: "Translated nav labels via useTranslations('Nav')"
      contains: "useTranslations"
    - path: "app/components/Etusivu.tsx"
      provides: "Translated filter/todo/auth strings via useTranslations"
      contains: "useTranslations"
    - path: "app/components/PaikkaKortti.tsx"
      provides: "Translated card labels via useTranslations('PaikkaKortti')"
      contains: "useTranslations"
    - path: "app/components/DiagonaalKortti.tsx"
      provides: "Translated card labels via useTranslations('PaikkaKortti')"
      contains: "useTranslations"
  key_links:
    - from: "app/components/Etusivu.tsx"
      to: "searchKaupunki state"
      via: "sentinel value 'Kaikki' unchanged"
      pattern: "searchKaupunki === 'Kaikki'"
---

<objective>
Migrate NavPill, Etusivu, PaikkaKortti, and DiagonaalKortti to use next-intl useTranslations. All Finnish hard-coded strings in these four files are replaced with translation key lookups. The 'Kaikki' sentinel in Etusivu is preserved — only the display label is translated.

Purpose: Core UI — these components are visible on every user interaction and form the primary i18n surface area.
Output: 4 modified components; Finnish and English rendering verified via locale toggle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-i18n-fien/30-PATTERNS.md
@.planning/phases/30-i18n-fien/30-RESEARCH.md
@.planning/phases/30-i18n-fien/30-01-SUMMARY.md
</context>

<interfaces>
<!-- Contracts established by Plan 30-01 that this plan builds on -->

From messages/fi.json (namespaces used in this plan):
- Nav: { profile, signIn, signOut, openMenu, closeMenu }
- PaikkaKortti: { sponsored, dropIn, openNow, closed, hoursUnknown, hoursComingSoon, membershipOnly, priceComingSoon, showDetails, addToTodo, removeFromTodo }
- Filters: { all, searchPlaceholder, filterBySport, filterByCity, searchAndFilters, showList }
- Todo: { title, empty, emptyHint, visited, yes, no, reviewSaved, skip, ratingLabel, commentLabel, commentPlaceholder, openList, closeList }
- Map: { recenter }

From app/actions/locale.ts:
```typescript
'use server'
export async function changeLocaleAction(locale: 'fi' | 'en'): Promise<void>
```

Hook pattern for all client components:
```typescript
import { useTranslations } from 'next-intl'
// Inside component function:
const t = useTranslations('NamespaceName')
// Usage: {t('keyName')}, aria-label={t('keyName')}, placeholder={t('keyName')}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: NavPill.tsx — translate nav labels</name>
  <files>app/components/NavPill.tsx</files>
  <read_first>
    - app/components/NavPill.tsx — read the FULL file before editing; locate exact string positions for Profiili (line ~65), Kirjaudu ulos (~72), Kirjaudu (~76-78), aria-label open/close valikko (~89)
    - .planning/phases/30-i18n-fien/30-PATTERNS.md lines 327–357 — exact string mapping table and import pattern for NavPill
  </read_first>
  <action>
    Add import: `import { useTranslations } from 'next-intl'` to the existing import block (after all other imports).

    Add hook call inside the NavPill component function (before JSX return, after existing useState/useEffect hooks):
    `const t = useTranslations('Nav')`

    Replace hard-coded strings:
    - "Profiili" (link text) → {t('profile')}
    - "Kirjaudu ulos" (button text) → {t('signOut')}
    - "Kirjaudu" (link text for unauthenticated) → {t('signIn')}
    - aria-label={open ? 'Sulje valikko' : 'Avaa valikko'} → aria-label={open ? t('closeMenu') : t('openMenu')}

    Do NOT change any className, event handlers, or logic — only string literals.
  </action>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck 2>&1 | grep -v "^$" | head -10</automated>
  </verify>
  <done>
    NavPill.tsx contains `useTranslations('Nav')` and no bare Finnish string literals for "Profiili", "Kirjaudu ulos", "Kirjaudu", "Sulje valikko", "Avaa valikko". tsc --noEmit --skipLibCheck reports no NEW errors from this file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Etusivu.tsx — translate filter, map, todo, and auth strings</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    - app/components/Etusivu.tsx — read the full file (it is large; focus reads on: lines 61-72 RecenterButton aria-label; lines 415-453 CombinedFilterPill city/sport aria-labels; lines 447-460 sport filter aria-label; lines 465-480 search placeholder; lines 1210-1270 todo overlay strings; lines 1315-1395 lista/navpill sign-in strings; lines 590-600 searchKaupunki state init)
    - .planning/phases/30-i18n-fien/30-RESEARCH.md lines 342-388 — Etusivu strings inventory
    - .planning/phases/30-i18n-fien/30-RESEARCH.md lines 369-380 — 'Kaikki' sentinel critical note
    - .planning/phases/30-i18n-fien/30-PATTERNS.md lines 211-213 — sentinel preservation instruction
  </read_first>
  <action>
    Add import: `import { useTranslations } from 'next-intl'` to the existing import block.

    Etusivu is a large component. Add useTranslations hooks at the top of the Etusivu component function:
    - `const tFilters = useTranslations('Filters')`
    - `const tTodo = useTranslations('Todo')`
    - `const tMap = useTranslations('Map')`

    The CombinedFilterPill sub-component is defined inside Etusivu.tsx but is a separate function — add its own hook:
    - Inside CombinedFilterPill function body: `const tFilters = useTranslations('Filters')`

    SENTINEL SAFETY — CRITICAL: The line `const [searchKaupunki, setSearchKaupunki] = useState('Kaikki')` MUST NOT be changed. The string 'Kaikki' in this state variable is the sentinel. The comparisons `searchKaupunki === 'Kaikki'` and `newCity === 'Kaikki'` and `kaupunki !== 'Kaikki'` MUST all remain unchanged. Only translate DISPLAY labels.

    Replace hard-coded display strings:
    RecenterButton (lines ~68):
    - aria-label="Palaa omalle sijainnille" → aria-label={tMap('recenter')}
    Note: RecenterButton is a sub-function — add `const tMap = useTranslations('Map')` inside it.

    CombinedFilterPill sub-component:
    - aria-label="Suodata kaupungin mukaan" → aria-label={tFilters('filterByCity')}
    - aria-label="Suodata lajin mukaan" → aria-label={tFilters('filterBySport')}
    - placeholder="Hae liikuntapaikkaa..." → placeholder={tFilters('searchPlaceholder')}
    - aria-label="Haku ja filtterit" (if present) → aria-label={tFilters('searchAndFilters')}

    Etusivu main component todo overlay area:
    - "TO DO" (title label, ~line 1217) → {tTodo('title')}
    - "Lista on tyhjä" (~line 1221) → {tTodo('empty')}
    - "Lisää paikkoja kirjanmerkkipainikkeella" (if present as hint text) → {tTodo('emptyHint')}
    - "Arvostelu tallennettu" (~line 1243) → {tTodo('reviewSaved')}
    - "TÄHTIARVOSANA" (~line 1247) → {tTodo('ratingLabel')}
    - "Ohita" (~line 1257) → {tTodo('skip')}
    - "Kyllä" and "Ei" (visited? buttons) → {tTodo('yes')} and {tTodo('no')}
    - "Vapaaehtoinen kommentti" placeholder → placeholder={tTodo('commentPlaceholder')}
    - "KOMMENTTI" label (if present) → {tTodo('commentLabel')}
    - aria-label="Näytä lista" (~line 1320) → aria-label={tFilters('showList')}
    - aria-label={todoOpen ? 'Sulje TO DO -lista' : 'Avaa TO DO -lista'} (~line 1427) → aria-label={todoOpen ? tTodo('closeList') : tTodo('openList')}
    - "TO DO" (the tab button label, ~line 1367) → {tTodo('title')}
    - "Kirjaudu ulos" (~line 1382, inline in Etusivu if present) → this string is in NavPill — skip if not in Etusivu
    - "Kirjaudu" (~line 1390, inline in Etusivu if present) → only replace if it is a hard-coded string not delegated to NavPill

    After completing replacements, verify no Finnish string literals remain for the above list.
    Do NOT translate the city names displayed in the filter carousel (those come from the Supabase data).
    Do NOT translate sport labels — they are proper nouns (Padel, Tennis, etc.).
  </action>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck 2>&1 | grep "Etusivu" | head -10</automated>
  </verify>
  <done>
    - Etusivu.tsx imports useTranslations from 'next-intl'
    - searchKaupunki state still initialised with 'Kaikki' (grep: `useState('Kaikki')` still present)
    - All comparisons `searchKaupunki === 'Kaikki'` still present (grep confirms)
    - RecenterButton, CombinedFilterPill, and todo overlay areas use t() calls for all strings in scope
    - tsc --noEmit --skipLibCheck has no errors referencing Etusivu.tsx
  </done>
</task>

<task type="auto">
  <name>Task 3: PaikkaKortti.tsx + DiagonaalKortti.tsx — translate card labels</name>
  <files>app/components/PaikkaKortti.tsx, app/components/DiagonaalKortti.tsx</files>
  <read_first>
    - app/components/PaikkaKortti.tsx — read full file; locate string positions per PATTERNS.md lines 361–388
    - app/components/DiagonaalKortti.tsx — read full file; locate string positions per PATTERNS.md lines 390–412
    - .planning/phases/30-i18n-fien/30-PATTERNS.md lines 360–412 — exact string-to-key mapping tables for both files
    - messages/fi.json PaikkaKortti namespace — keys: sponsored, dropIn, openNow, closed, hoursUnknown, hoursComingSoon, membershipOnly, priceComingSoon, showDetails, addToTodo, removeFromTodo
  </read_first>
  <action>
    PaikkaKortti.tsx:
    Add import: `import { useTranslations } from 'next-intl'` after all existing imports.
    Add inside the PaikkaKortti component function: `const t = useTranslations('PaikkaKortti')`

    Replace string literals:
    - 'Poista TO DO -listalta' (aria-label) → t('removeFromTodo')
    - 'Lisää TO DO -listalle' (aria-label) → t('addToTodo')
    - "Sponsoroitu" → {t('sponsored')}
    - "Kertakäynti OK" → {t('dropIn')}
    - "Auki nyt" in open status string — replace only the "Auki nyt" substring, keep the " · {openStatus.hours}" concatenation: `{t('openNow')} · {openStatus.hours}` (or whatever the current expression is — preserve the hours part)
    - "Suljettu" (closed label) → {t('closed')}
    - 'Aukioloajat tuntematon' → {t('hoursUnknown')}
    - 'Aukioloajat lisätään pian' → {t('hoursComingSoon')}
    - "vain jäsenyys" → {t('membershipOnly')}
    - "Lisätään pian" (price coming soon) → {t('priceComingSoon')}
    - "Näytä tiedot" → {t('showDetails')}

    DiagonaalKortti.tsx:
    Add import: `import { useTranslations } from 'next-intl'` after all existing imports.
    Add inside the DiagonaalKortti component function: `const t = useTranslations('PaikkaKortti')`
    (DiagonaalKortti shares the PaikkaKortti namespace — verified in PATTERNS.md.)

    Replace string literals:
    - 'Poista TO DO -listalta' / 'Lisää TO DO -listalle' (aria-labels) → t('removeFromTodo') / t('addToTodo')
    - "vain jäsenyys" → {t('membershipOnly')}
    - "Lisätään pian" → {t('priceComingSoon')}
    - "Auki · {hours}" display → `{t('openNow')} · {hours}` (preserve the hours variable)
    - "Suljettu" → {t('closed')}
  </action>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "PaikkaKortti|DiagonaalKortti" | head -10</automated>
  </verify>
  <done>
    - Both files import useTranslations from 'next-intl'
    - Both files call useTranslations('PaikkaKortti') inside the component function
    - No bare Finnish string literals for the listed strings remain in either file
    - tsc --noEmit --skipLibCheck has no errors referencing PaikkaKortti.tsx or DiagonaalKortti.tsx
    - npx vitest run passes (full suite, existing tests unaffected)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Translation key → rendered output | Keys are compile-time safe via global.d.ts AppConfig augmentation; wrong keys cause TS error not runtime injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Tampering | useTranslations — interpolated values in translated strings | accept | next-intl escapes interpolated variables automatically; no dangerouslySetInnerHTML used with t() output |
| T-30-05 | Tampering | 'Kaikki' sentinel replacement | mitigate | Plan explicitly requires sentinel string to remain unchanged; acceptance criteria verify grep presence of `useState('Kaikki')` and `=== 'Kaikki'` |
| T-30-SC | Tampering | npm packages | accept | No new packages installed in this plan; next-intl already audited in plan 30-01 |
</threat_model>

<verification>
After all 3 tasks complete:

```bash
npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "NavPill|Etusivu|PaikkaKortti|DiagonaalKortti" | head -20
```
Expected: zero lines output (no TS errors in migrated files).

```bash
npx vitest run
```
Expected: full suite green.

Sentinel check:
```bash
grep -v '^#' app/components/Etusivu.tsx | grep -c "useState('Kaikki')"
```
Expected: 1 (sentinel preserved).

```bash
grep -v '^#' app/components/Etusivu.tsx | grep -c "searchKaupunki === 'Kaikki'"
```
Expected: 2 or more (comparison logic unchanged).
</verification>

<success_criteria>
- [ ] NavPill.tsx: useTranslations('Nav') present; no bare Finnish strings for nav labels
- [ ] Etusivu.tsx: sentinel `useState('Kaikki')` unchanged; RecenterButton, CombinedFilterPill, todo overlay all translated
- [ ] PaikkaKortti.tsx: useTranslations('PaikkaKortti') present; all 11 card strings translated
- [ ] DiagonaalKortti.tsx: useTranslations('PaikkaKortti') present; all card strings translated
- [ ] tsc --noEmit --skipLibCheck: no errors in the 4 migrated files
- [ ] vitest run: full suite green
</success_criteria>

<output>
Create `.planning/phases/30-i18n-fien/30-02-SUMMARY.md` when done.
</output>
