---
phase: 30-i18n-fien
plan: 04
type: execute
wave: 3
depends_on:
  - 30-02
  - 30-03
files_modified:
  - app/components/PaikkaSheet.tsx
  - app/components/AuthModal.tsx
  - app/paikat/[id]/page.tsx
  - app/not-found.tsx
autonomous: false
requirements:
  - I18N-03

must_haves:
  truths:
    - "PaikkaSheet section headings (Hinta, Aukioloajat, Puhelin, Kuvaus, Varaa aika, Arvostelut) render in selected locale"
    - "PaikkaSheet review count handles singular/plural ('1 review' in EN, 'N arvostelua' in FI)"
    - "AuthModal modal title, placeholders, and button labels render in selected locale"
    - "app/paikat/[id]/page.tsx server component uses getTranslations for all section labels"
    - "app/not-found.tsx server component uses getTranslations for title, description, back-link"
    - "npx tsc --noEmit passes with zero errors across all modified files"
  artifacts:
    - path: "app/components/PaikkaSheet.tsx"
      provides: "Translated sheet labels via useTranslations('PaikkaSheet')"
      contains: "useTranslations"
    - path: "app/components/AuthModal.tsx"
      provides: "Translated auth modal labels via useTranslations('Auth')"
      contains: "useTranslations"
    - path: "app/paikat/[id]/page.tsx"
      provides: "Translated page labels via getTranslations (server component)"
      contains: "getTranslations"
    - path: "app/not-found.tsx"
      provides: "Translated 404 labels via getTranslations (server component)"
      contains: "getTranslations"
  key_links:
    - from: "app/paikat/[id]/page.tsx"
      to: "getTranslations('PaikkaPage')"
      via: "async server component"
      pattern: "getTranslations"
    - from: "app/not-found.tsx"
      to: "getTranslations('NotFound')"
      via: "async server component"
      pattern: "getTranslations"
---

<objective>
Migrate the remaining UI-facing components: PaikkaSheet (client), AuthModal (client), app/paikat/[id]/page.tsx (server), and app/not-found.tsx (server). Complete the full i18n surface area. Run tsc --noEmit as the final type-safety gate for the entire phase, and close with a visual verification checkpoint.

Purpose: Completes I18N-03 — all UI texts in all components render in the selected locale. The tsc gate confirms no translation key typos or missing imports remain.
Output: 4 migrated components; tsc --noEmit passes; human visual check across FI/EN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-i18n-fien/30-PATTERNS.md
@.planning/phases/30-i18n-fien/30-RESEARCH.md
@.planning/phases/30-i18n-fien/30-02-SUMMARY.md
@.planning/phases/30-i18n-fien/30-03-SUMMARY.md
</context>

<interfaces>
<!-- Contracts from prior plans used in this plan -->

From messages/fi.json (namespaces used in this plan):
- PaikkaSheet: { price, hours, phone, description, bookNow, reviews, noReviews, reviewCountSingular, reviewCountPlural }
  Note: RESEARCH.md recommends TWO keys for plural: reviewCountSingular ("1 arvostelu") and reviewCountPlural ("{count} arvostelua"). If fi.json was written with reviewCount as a single ICU string, adjust the component logic to use count === 1 ? t('reviewCountSingular') : t('reviewCountPlural').
- Auth: { signIn, signUp, emailPlaceholder, passwordPlaceholder, continueWithGoogle, or, close }
- PaikkaPage: { backToDirectory, price, hours, phone, description, bookNow }
- NotFound: { title, description, backHome }

Server component pattern:
```typescript
import { getTranslations } from 'next-intl/server'
// Inside async component function:
const t = await getTranslations('NamespaceName')
// Then: t('key') — same as useTranslations but awaited
```

Client component pattern:
```typescript
import { useTranslations } from 'next-intl'
// Inside component function:
const t = useTranslations('NamespaceName')
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: PaikkaSheet.tsx + AuthModal.tsx — translate client component strings</name>
  <files>app/components/PaikkaSheet.tsx, app/components/AuthModal.tsx</files>
  <read_first>
    - app/components/PaikkaSheet.tsx — read FULL file; locate: section headings (Hinta, Aukioloajat, Puhelin, Kuvaus, Varaa aika lines ~160+), review section (Arvostelut, Ei arvosteluja, N arvostelua), bookmark aria-labels, close button aria-label
    - app/components/AuthModal.tsx — read FULL file; locate: h2 title (Kirjaudu sisään / Luo tili), Google button (Jatka Googlella), TAI divider, email/password placeholders, submit button, close aria-label (~lines 152, 178, 187, 204, 210, 229, 240, 268-275)
    - .planning/phases/30-i18n-fien/30-PATTERNS.md lines 414–466 — exact string mapping tables for PaikkaSheet and AuthModal
    - messages/fi.json — check whether reviewCount is one key or two (reviewCountSingular/reviewCountPlural)
  </read_first>
  <action>
    PaikkaSheet.tsx:
    Add import: `import { useTranslations } from 'next-intl'` after all existing imports.
    Add inside PaikkaSheet component function: `const t = useTranslations('PaikkaSheet')`

    Also add PaikkaKortti namespace for shared bookmark aria-labels:
    `const tKortti = useTranslations('PaikkaKortti')`

    Replace string literals:
    - "Hinta" (section heading) → {t('price')}
    - "Aukioloajat" → {t('hours')}
    - "Puhelin" → {t('phone')}
    - "Kuvaus" → {t('description')}
    - "Varaa aika" → {t('bookNow')}
    - "Arvostelut" → {t('reviews')}
    - "Ei arvosteluja" → {t('noReviews')}
    - "{N} arvostelua" — check the existing expression in the file. If it is a template literal like `${count} arvostelua`, replace with:
      `{count === 1 ? t('reviewCountSingular') : t('reviewCountPlural', { count })}`
      where reviewCountSingular = "1 arvostelu" and reviewCountPlural = "{count} arvostelua" in fi.json.
      If fi.json was written with only reviewCount (not singular/plural), also add reviewCountSingular key to both fi.json and en.json: fi: "1 arvostelu", en: "1 review"; and update reviewCountPlural (or reviewCount) accordingly.
    - 'Poista TO DO -listalta' / 'Lisää TO DO -listalle' (aria-labels) → tKortti('removeFromTodo') / tKortti('addToTodo')
    - aria-label="Sulje" on close button → aria-label={t('close')} — if this key is not in PaikkaSheet namespace, check the PaikkaSheet section in fi.json and add "close": "Sulje" / "Close" if missing

    AuthModal.tsx:
    Add import: `import { useTranslations } from 'next-intl'` after all existing imports.
    Add inside AuthModal component function: `const t = useTranslations('Auth')`

    Replace string literals:
    - aria-label={mode === 'signin' ? 'Kirjaudu sisään' : 'Luo tili'} (modal title aria) → aria-label={mode === 'signin' ? t('signIn') : t('signUp')}
    - h2 text: {mode === 'signin' ? 'Kirjaudu sisään' : 'Luo tili'} → {mode === 'signin' ? t('signIn') : t('signUp')}
    - "Jatka Googlella" → {t('continueWithGoogle')}
    - "TAI" (divider) → {t('or')}
    - placeholder="Sähköpostiosoite" → placeholder={t('emailPlaceholder')}
    - placeholder="Salasana" → placeholder={t('passwordPlaceholder')}
    - submit button label: {mode === 'signin' ? 'Kirjaudu' : 'Luo tili'} → {mode === 'signin' ? t('signIn') : t('signUp')}
    - aria-label="Sulje" (close X button) → aria-label={t('close')}
  </action>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "PaikkaSheet|AuthModal" | head -10</automated>
  </verify>
  <done>
    - PaikkaSheet.tsx imports useTranslations; all section headings use t() calls; review count uses singular/plural logic
    - AuthModal.tsx imports useTranslations; all modal strings use t() calls
    - tsc --noEmit --skipLibCheck: zero lines output for these two files
  </done>
</task>

<task type="auto">
  <name>Task 2: app/paikat/[id]/page.tsx + app/not-found.tsx — translate server components</name>
  <files>app/paikat/[id]/page.tsx, app/not-found.tsx</files>
  <read_first>
    - app/paikat/[id]/page.tsx — read FULL file; this is an async server component; note current imports, data fetching logic, and string positions (~lines 54, 99, 104, 114, 123-124, 137)
    - app/not-found.tsx — read full file; note whether the function is currently async or not (must be async to call getTranslations)
    - .planning/phases/30-i18n-fien/30-PATTERNS.md lines 552–611 — exact implementations for both server components
  </read_first>
  <action>
    app/paikat/[id]/page.tsx:
    Add import: `import { getTranslations } from 'next-intl/server'`
    (The file already imports `cookies` from `next/headers` — add getTranslations import alongside it or in the next-intl section.)

    Inside the async page function, after data fetching and before JSX, add:
    `const t = await getTranslations('PaikkaPage')`

    Replace string literals:
    - "Takaisin hakemistoon" (link text, ~line 54) → {t('backToDirectory')}
    - label="Aukioloajat" (Row component prop, ~line 99) → label={t('hours')}
    - label="Puhelin" (~line 104) → label={t('phone')}
    - label="Hinta" (~line 114) → label={t('price')}
    - label="Varaussivu" or "Varaa aika" (~line 124) → label={t('bookNow')}
    - label="Kuvaus" (~line 137) → label={t('description')}
    - Open/closed status text (if hard-coded Finnish open/closed strings present) → use PaikkaKortti namespace: `import { getTranslations } from 'next-intl/server'` and add `const tKortti = await getTranslations('PaikkaKortti')`, then replace "Auki nyt" / "Suljettu" strings

    app/not-found.tsx:
    Add import: `import { getTranslations } from 'next-intl/server'`
    Change function signature to async: `export default async function NotFound()`
    Add inside function: `const t = await getTranslations('NotFound')`

    Replace string literals:
    - "Sivua ei löydy." → {t('title')}
    - "Etsimääsi sivua ei ole olemassa..." (exact text from the file) → {t('description')}
    - "Palaa etusivulle" → {t('backHome')}
  </action>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck 2>&1 | grep -E "paikat|not-found" | head -10</automated>
  </verify>
  <done>
    - app/paikat/[id]/page.tsx imports getTranslations; awaits t = await getTranslations('PaikkaPage'); all 6 section labels use t() calls
    - app/not-found.tsx is async; imports getTranslations; 3 strings replaced with t() calls
    - tsc --noEmit --skipLibCheck: zero lines output for these two files
  </done>
</task>

<task type="auto">
  <name>Task 3: Full TypeScript gate — npx tsc --noEmit must pass</name>
  <files></files>
  <read_first>
    - No files to read — this task runs a command and fixes any remaining issues
  </read_first>
  <action>
    Run `npx tsc --noEmit` (without --skipLibCheck) and fix any remaining TypeScript errors in files modified during Phase 30.

    Common sources of errors at this stage:
    1. Translation key typo: `t('wrongKey')` — check against messages/fi.json AppConfig augmentation
    2. Missing namespace in useTranslations call: ensure every t() call uses a key that exists in the correct namespace
    3. getTranslations called in a non-async function — ensure not-found.tsx and paikat/[id]/page.tsx are async
    4. global.d.ts AppConfig import path incorrect — should be `import messages from './messages/fi.json'` at project root
    5. i18n/request.ts import path for messages: `../messages/${locale}.json` is correct when i18n/ is at project root

    If tsc reports errors from files NOT modified in Phase 30, check if they are pre-existing (present before Phase 30) by running `git stash && npx tsc --noEmit && git stash pop` — pre-existing errors do not block this gate.

    The gate PASSES when: `npx tsc --noEmit` exits with code 0, OR all remaining errors are in files not touched in Phase 30 and identical to pre-Phase-30 baseline.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    `npx tsc --noEmit` exits 0 (zero type errors), OR only pre-existing errors remain in files not modified by Phase 30. The output of `npx tsc --noEmit 2>&1 | grep -E "i18n|i18nUtils|layout|actions/locale|PaikkaSheet|AuthModal|ProfiiliClient|NavPill|Etusivu|PaikkaKortti|DiagonaalKortti|not-found|paikat" | head -20` returns zero lines.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete i18n migration — all 9 namespaces active, all components translated, language toggle on /profiili, NEXT_LOCALE cookie persists.</what-built>
  <how-to-verify>
    Start dev server: `npm run dev`

    FI → EN toggle flow:
    1. Open http://localhost:3000 — confirm UI is in Finnish (card labels, filter aria-labels in source)
    2. Navigate to http://localhost:3000/profiili
    3. Click "Switch to English" — page re-renders; confirm button now reads "Switch to Finnish"
    4. Navigate to http://localhost:3000 — confirm UI labels switched to English:
       - RecenterButton aria-label = "Return to my location" (inspect element)
       - PaikkaKortti "Näytä tiedot" → "Show details"
       - PaikkaKortti "Auki nyt" → "Open now"
    5. Open a PaikkaSheet — confirm "Price", "Opening hours", "Phone", "Description", "Book now" headings
    6. Open AuthModal (click sign-in) — confirm "Sign in" title, "Email address" placeholder, "Continue with Google" button
    7. Navigate to http://localhost:3000/paikat/1 (or any valid venue ID) — confirm "Back to directory", "Price", "Opening hours" labels in English
    8. Navigate to http://localhost:3000/ei-olemassa (a nonexistent path) — confirm "Page not found." and "Back to home" in English
    9. Reload the page at http://localhost:3000 — confirm English persists (NEXT_LOCALE=en in DevTools cookies)

    State preservation check:
    10. On http://localhost:3000, set a sport filter (e.g. Padel) and select a city if available
    11. Navigate to /profiili, click "Switch to Finnish"
    12. Navigate back to http://localhost:3000 — confirm sport filter is STILL set, city filter still set
    13. Confirm map viewport has not reset (same zoom and center as before language switch)

    NEXT_LOCALE cookie check:
    14. DevTools → Application → Cookies → localhost; confirm NEXT_LOCALE=en is present with Path=/ and Max-Age ~31536000 (1 year)
  </how-to-verify>
  <resume-signal>Type "approved" if all 14 checks pass, or describe which check number failed and what was observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| getTranslations in server components | Called during SSR — locale comes from NEXT_LOCALE cookie via i18n/request.ts; no user-controlled input reaches string rendering |
| AuthModal user inputs | email/password placeholders are translated; actual input values are not translated (user data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-08 | Tampering | PaikkaSheet — reviewCount interpolation | accept | Review count is a number from Supabase; no user-controlled string in the count interpolation path |
| T-30-09 | Elevation of Privilege | AuthModal — translated error messages | accept | Error messages are from Supabase auth API, not from translation keys; translated only the static UI chrome |
| T-30-10 | Tampering | app/not-found.tsx — making function async | accept | Not-found pages are not on a security boundary; async adds no new attack surface |
| T-30-SC | Tampering | No new npm installs in this plan | accept | — |
</threat_model>

<verification>
TypeScript gate (Task 3):

```bash
npx tsc --noEmit 2>&1 | tail -3
```
Expected: exit code 0, or only pre-existing errors.

Phase-wide translation completeness:
```bash
grep -rn "useTranslations\|getTranslations" app/components/ app/profiili/ app/paikat/ app/not-found.tsx | grep -v ".test." | wc -l
```
Expected: at least 8 lines (NavPill, Etusivu, PaikkaKortti, DiagonaalKortti, PaikkaSheet, AuthModal, ProfiiliClient, paikat/[id]/page.tsx, not-found.tsx).

Full vitest run:
```bash
npx vitest run
```
Expected: all tests pass including the 6 resolveLocale unit tests from plan 30-01.
</verification>

<success_criteria>
- [ ] PaikkaSheet.tsx: useTranslations('PaikkaSheet') present; review count plural handled; all section headings translated
- [ ] AuthModal.tsx: useTranslations('Auth') present; modal title, placeholders, buttons translated
- [ ] app/paikat/[id]/page.tsx: getTranslations('PaikkaPage') present; all 6 section labels translated
- [ ] app/not-found.tsx: async function, getTranslations('NotFound') present, 3 strings translated
- [ ] npx tsc --noEmit: zero errors in Phase 30 files
- [ ] npx vitest run: full suite green
- [ ] Human checkpoint: EN mode shows all 9 component areas in English; locale persists on reload; map/filter state preserved on language toggle
</success_criteria>

<output>
Create `.planning/phases/30-i18n-fien/30-04-SUMMARY.md` when done.
</output>
