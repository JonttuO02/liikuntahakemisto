---
phase: 40-wizard-konsolidointi-cleanup
plan: 03
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - app/business/WizardInner.tsx
  - app/business/onboarding/page.tsx
  - app/business/[id]/page.tsx
files_deleted:
  - app/business/onboarding/OnboardingWizardInner.tsx
  - app/business/[id]/EditWizardInner.tsx
autonomous: true
requirements:
  - CLEAN-02

must_haves:
  truths:
    - "app/business/WizardInner.tsx exists and is the single exported component with mode: 'onboarding' | 'edit' discriminated union props"
    - "WizardInner uses private sub-components OnboardingMode and EditMode within the same file to keep React hooks rules (no conditional hook calls at top level)"
    - "OnboardingMode contains all logic from OnboardingWizardInner.tsx unchanged — 6 steps, ProgressBar, maxReachedStep guard, draft loading, saveAndAdvance"
    - "EditMode contains all logic from EditWizardInner.tsx unchanged — tab bar, 5 tabs, free navigation, PreviewModal, local state from paikka prop"
    - "app/business/onboarding/page.tsx imports WizardInner from '../WizardInner' and renders <WizardInner mode='onboarding' />"
    - "app/business/[id]/page.tsx imports WizardInner from '../WizardInner' and renders <WizardInner mode='edit' paikka={paikka} paikkaId={paikkaId} />"
    - "OnboardingWizardInner.tsx and EditWizardInner.tsx no longer exist — deleted"
    - "TypeScript compiles cleanly — npx tsc --noEmit reports no errors related to WizardInner or the updated pages"
  artifacts:
    - path: "app/business/WizardInner.tsx"
      provides: "Unified wizard component for onboarding and edit modes"
      exports: ["WizardInner (default)"]
  key_links:
    - from: "app/business/onboarding/page.tsx"
      to: "app/business/WizardInner.tsx"
      via: "import WizardInner from '../WizardInner'"
      pattern: "WizardInner.*mode.*onboarding"
    - from: "app/business/[id]/page.tsx"
      to: "app/business/WizardInner.tsx"
      via: "import WizardInner from '../WizardInner'"
      pattern: "WizardInner.*mode.*edit"
---

<objective>
Merge OnboardingWizardInner and EditWizardInner into a single WizardInner component at app/business/WizardInner.tsx. The goal is single-file maintenance: one place to look when debugging, one place to fix when a bug is found. UX behavior is NOT changed.

The implementation uses private sub-components (OnboardingMode + EditMode) within WizardInner.tsx to satisfy React hooks rules — this avoids conditional hook calls while keeping both modes in one file.

Purpose: Satisfies CLEAN-02 — removes code duplication and eliminates the two-file maintenance burden.
Output: app/business/WizardInner.tsx (new), updated onboarding/page.tsx, updated [id]/page.tsx, deleted OnboardingWizardInner.tsx and EditWizardInner.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/40-wizard-konsolidointi-cleanup/40-CONTEXT.md

<source_files>
Read both source files completely before writing WizardInner.tsx:
- app/business/onboarding/OnboardingWizardInner.tsx — full source (becomes OnboardingMode)
- app/business/[id]/EditWizardInner.tsx — full source (becomes EditMode)
</source_files>

<design_decisions>
D-01: File location: app/business/WizardInner.tsx (business root level — neutral, imported by both child directories)
D-02: Merge strategy: rename + absorb. OnboardingWizardInner → OnboardingMode (private). EditWizardInner → EditMode (private). WizardInner is the exported shell that routes to the correct mode.
D-03: Single-file maintenance is the goal — not visual UX unification.
D-04: Onboarding mode keeps all its current behavior unchanged.
D-05: Edit mode keeps all its current behavior unchanged.
D-08: EditMode receives paikka: Liikuntapaikka and paikkaId: number as props (matches current EditWizardInner).

HOOKS RULE: React hooks cannot be called conditionally. Use private sub-components to keep each mode's hooks in its own component function.
</design_decisions>

<props_interface>
type WizardInnerProps =
  | { mode: 'onboarding' }
  | { mode: 'edit'; paikka: Liikuntapaikka; paikkaId: number }
</props_interface>

<file_structure>
app/business/WizardInner.tsx structure:

'use client'

[imports — merged from both files, deduped]

type PaikkaInfo = { ... }  // from OnboardingWizardInner, stays local

type WizardInnerProps =
  | { mode: 'onboarding' }
  | { mode: 'edit'; paikka: Liikuntapaikka; paikkaId: number }

// ─── Onboarding mode ────────────────────────────────────────────────────────
function OnboardingMode() {
  // Entire body of OnboardingWizardInner (minus the export default declaration) — copied verbatim
}

// ─── Edit mode ───────────────────────────────────────────────────────────────
function EditMode({ paikka, paikkaId }: { paikka: Liikuntapaikka; paikkaId: number }) {
  // Entire body of EditWizardInner (minus the export default declaration and props destructuring) — copied verbatim
}

// ─── Exported component ───────────────────────────────────────────────────────
export default function WizardInner(props: WizardInnerProps) {
  if (props.mode === 'onboarding') return &lt;OnboardingMode /&gt;
  return &lt;EditMode paikka={props.paikka} paikkaId={props.paikkaId} /&gt;
}
</file_structure>

<import_merge>
Combined imports needed in WizardInner.tsx:

From OnboardingWizardInner:
  import { useState, useEffect } from 'react'
  import { useSearchParams, useRouter } from 'next/navigation'
  import { AnimatePresence, motion } from 'framer-motion'
  import { createBusinessBrowserClient } from '@/lib/supabase-business'
  import { useTranslations } from 'next-intl'
  import type { OnboardingDraft } from '@/lib/onboardingUtils'
  import ProgressBar from './onboarding/ProgressBar'
  import StepPaikka from './onboarding/StepPaikka'
  import StepMediat from './onboarding/StepMediat'
  import StepHinnasto from './onboarding/StepHinnasto'
  import StepAukioloajat from './onboarding/StepAukioloajat'
  import StepYhteystiedot from './onboarding/StepYhteystiedot'
  import StepEsikatselu from './onboarding/StepEsikatselu'

From EditWizardInner (additional/changed):
  import type { Liikuntapaikka } from '@/lib/types'
  import PreviewModal from '@/app/components/PreviewModal'

Note: AnimatePresence is already imported; motion is used only by OnboardingMode.
Step imports change paths: since WizardInner.tsx is at app/business/ (not app/business/onboarding/),
  all step imports must use './onboarding/StepPaikka' etc. (one extra path segment).
  ProgressBar import: './onboarding/ProgressBar'
  EditWizardInner's step imports were already '../onboarding/StepMediat' etc. — from app/business/[id]/,
  but from app/business/ they become './onboarding/StepMediat'. Adjust ALL step import paths.
  PreviewModal import: '@/app/components/PreviewModal' — unchanged (absolute import).
</import_merge>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Read source files</name>
  <files>
    app/business/onboarding/OnboardingWizardInner.tsx
    app/business/[id]/EditWizardInner.tsx
  </files>
  <read_first>
    - app/business/onboarding/OnboardingWizardInner.tsx — full file
    - app/business/[id]/EditWizardInner.tsx — full file
  </read_first>
  <action>
Read both files completely. Note exact content for Task 2. No changes in this task.
  </action>
  <acceptance_criteria>
    - Both source files read successfully
  </acceptance_criteria>
  <done>Source files read — ready to create WizardInner.tsx.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create app/business/WizardInner.tsx</name>
  <files>app/business/WizardInner.tsx</files>
  <read_first>
    - app/business/onboarding/OnboardingWizardInner.tsx — read in Task 1
    - app/business/[id]/EditWizardInner.tsx — read in Task 1
  </read_first>
  <action>
Create app/business/WizardInner.tsx as a new file. Follow the file_structure and import_merge specifications exactly:

1. Start with 'use client' directive.

2. Write merged imports (deduped): include all imports from both source files, adjusting paths as specified in import_merge. Key path adjustments since this file lives at app/business/:
   - Step components: './onboarding/StepPaikka', './onboarding/StepMediat', './onboarding/StepHinnasto', './onboarding/StepAukioloajat', './onboarding/StepYhteystiedot', './onboarding/StepEsikatselu'
   - ProgressBar: './onboarding/ProgressBar'
   - PreviewModal: '@/app/components/PreviewModal' (keep absolute)
   - Liikuntapaikka type: 'import type { Liikuntapaikka } from "@/lib/types"'

3. Define PaikkaInfo type (local — copy from OnboardingWizardInner verbatim).

4. Define WizardInnerProps discriminated union:
   type WizardInnerProps =
     | { mode: 'onboarding' }
     | { mode: 'edit'; paikka: Liikuntapaikka; paikkaId: number }

5. Write function OnboardingMode() — copy the entire body of OnboardingWizardInner verbatim (everything inside the function, including all state, effects, helper functions, and JSX). Do NOT rename any variables or change any logic.

6. Write function EditMode({ paikka, paikkaId }: { paikka: Liikuntapaikka; paikkaId: number }) — copy the entire body of EditWizardInner verbatim (everything inside the function, including state, helper functions, and JSX return). Do NOT rename any variables or change any logic.

7. Write the exported shell:
   export default function WizardInner(props: WizardInnerProps) {
     if (props.mode === 'onboarding') return <OnboardingMode />
     return <EditMode paikka={props.paikka} paikkaId={props.paikkaId} />
   }

Important: copy logic VERBATIM. Do not simplify, refactor, or add abstractions beyond the structural change described above.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "wizardinner\|WizardInner"</automated>
  </verify>
  <acceptance_criteria>
    - app/business/WizardInner.tsx exists
    - File exports default function WizardInner
    - File contains private function OnboardingMode (no export)
    - File contains private function EditMode (no export)
    - File contains: type WizardInnerProps = ...| { mode: 'onboarding' }...| { mode: 'edit'...
    - All step imports use './onboarding/' prefix (not '../onboarding/')
    - TypeScript: no errors related to this file — grep output from npx tsc --noEmit is empty for WizardInner
    - OnboardingMode and EditMode preserve exact behavior (no logic changes)
  </acceptance_criteria>
  <done>app/business/WizardInner.tsx created with OnboardingMode + EditMode sub-components and WizardInner shell. TypeScript clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update app/business/onboarding/page.tsx</name>
  <files>app/business/onboarding/page.tsx</files>
  <read_first>
    - app/business/onboarding/page.tsx — full file
  </read_first>
  <action>
Update app/business/onboarding/page.tsx:
1. Replace: import OnboardingWizardInner from './OnboardingWizardInner'
   With:     import WizardInner from '../WizardInner'
2. Replace: &lt;OnboardingWizardInner /&gt;
   With:     &lt;WizardInner mode="onboarding" /&gt;
3. Keep everything else exactly as is (Suspense wrapper, fallback spinner).
  </action>
  <acceptance_criteria>
    - page.tsx imports WizardInner from '../WizardInner'
    - page.tsx renders &lt;WizardInner mode="onboarding" /&gt;
    - Suspense wrapper and fallback spinner are unchanged
    - No import of OnboardingWizardInner remains
  </acceptance_criteria>
  <done>onboarding/page.tsx updated to use WizardInner.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update app/business/[id]/page.tsx</name>
  <files>app/business/[id]/page.tsx</files>
  <read_first>
    - app/business/[id]/page.tsx — full file
  </read_first>
  <action>
Update app/business/[id]/page.tsx:
1. Replace: import EditWizardInner from './EditWizardInner'
   With:     import WizardInner from '../WizardInner'
2. Replace: &lt;EditWizardInner paikka={paikka} paikkaId={paikkaId} /&gt;
   With:     &lt;WizardInner mode="edit" paikka={paikka} paikkaId={paikkaId} /&gt;
3. Keep everything else exactly as is (RSC data fetch, Suspense wrapper, notFound logic).

Note: The comment about auth guard being client-side can be left as is or removed since Phase 39 middleware now handles auth — Claude's discretion.
  </action>
  <acceptance_criteria>
    - [id]/page.tsx imports WizardInner from '../WizardInner'
    - [id]/page.tsx renders &lt;WizardInner mode="edit" paikka={paikka} paikkaId={paikkaId} /&gt;
    - RSC data fetch logic unchanged
    - No import of EditWizardInner remains
  </acceptance_criteria>
  <done>[id]/page.tsx updated to use WizardInner.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Delete old wizard files</name>
  <files>
    app/business/onboarding/OnboardingWizardInner.tsx
    app/business/[id]/EditWizardInner.tsx
  </files>
  <read_first>
    - Confirm no other file imports OnboardingWizardInner or EditWizardInner before deleting
  </read_first>
  <action>
Before deleting, search for any remaining imports:
- grep -r "OnboardingWizardInner" app/ — must return empty (only page.tsx was the importer; already updated in Task 3)
- grep -r "EditWizardInner" app/ — must return empty (only [id]/page.tsx was the importer; already updated in Task 4)

If both grep results are empty: delete both files.
If any import remains: fix it before deleting.

Delete app/business/onboarding/OnboardingWizardInner.tsx
Delete app/business/[id]/EditWizardInner.tsx
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - app/business/onboarding/OnboardingWizardInner.tsx does not exist
    - app/business/[id]/EditWizardInner.tsx does not exist
    - grep -r "OnboardingWizardInner" app/ returns empty
    - grep -r "EditWizardInner" app/ returns empty
    - npx tsc --noEmit reports no errors (TypeScript compiles cleanly after deletion)
  </acceptance_criteria>
  <done>OnboardingWizardInner.tsx and EditWizardInner.tsx deleted. TypeScript clean. No remaining imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React hooks | Private sub-components ensure hooks are never called conditionally |
| Import paths | All step imports adjusted for new file location — incorrect paths cause runtime errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03-01 | Tampering | Import path resolution | mitigate | Explicit path verification in acceptance criteria — './onboarding/' prefix required |
| T-40-03-02 | Denial of Service | Deleted file still referenced | mitigate | grep check before deletion confirms no remaining imports |
| T-40-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages — all imports use existing dependencies |
</threat_model>

<verification>
After all tasks complete:
- `test -f "app/business/WizardInner.tsx" && echo "EXISTS"` returns EXISTS
- `test -f "app/business/onboarding/OnboardingWizardInner.tsx" && echo "EXISTS" || echo "DELETED"` returns DELETED
- `test -f "app/business/[id]/EditWizardInner.tsx" && echo "EXISTS" || echo "DELETED"` returns DELETED
- `grep "mode=\"onboarding\"" app/business/onboarding/page.tsx` returns a match
- `grep "mode=\"edit\"" "app/business/[id]/page.tsx"` returns a match
- `npx tsc --noEmit 2>&1 | grep -i "wizard"` returns empty (no TypeScript errors)
</verification>

<success_criteria>
app/business/WizardInner.tsx exists with OnboardingMode + EditMode private sub-components and WizardInner default export. Both parent pages updated to use WizardInner. Old files deleted. TypeScript compiles cleanly. Onboarding and edit UX behavior is identical to pre-merge — single-file maintenance goal achieved.
</success_criteria>

<output>
Create .planning/phases/40-wizard-konsolidointi-cleanup/40-03-SUMMARY.md when done
</output>
