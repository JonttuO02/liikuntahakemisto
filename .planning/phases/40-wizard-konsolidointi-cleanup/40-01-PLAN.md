---
phase: 40-wizard-konsolidointi-cleanup
plan: 01
type: verify
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - CLEAN-03
  - CLEAN-04
  - CLEAN-05

must_haves:
  truths:
    - "CLEAN-03 confirmed: POST /api/business/update-paikka ownership check uses no claim_status filter — any linked business_account_id may save regardless of claim_status"
    - "CLEAN-04 confirmed: OnboardingWizardInner has maxReachedStep guard (lines 159–166) that redirects step > maxReachedStep+1 back to maxReachedStep+1"
    - "CLEAN-05 confirmed: /api/business/onboarding/submit/route.ts has no onboarding_completed write; migration 20260611000000_drop_onboarding_completed.sql already dropped the column"
    - "All three requirements are fully satisfied by existing code — zero code changes needed"
  artifacts: []
  key_links: []
---

<objective>
Verify that CLEAN-03, CLEAN-04, and CLEAN-05 are fully implemented in the current codebase — confirm no code changes are required for these three requirements.

Purpose: Validate pre-existing implementations before starting the code-change plans (40-02 and 40-03). If any check fails, add targeted minimal fixes.
Output: Confirmation checklist written to this plan's summary. Zero file modifications if all checks pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/40-wizard-konsolidointi-cleanup/40-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify CLEAN-03 — update-paikka accepts all claim_status values</name>
  <files>app/api/business/update-paikka/route.ts</files>
  <read_first>
    - app/api/business/update-paikka/route.ts — full file
  </read_first>
  <action>
Read app/api/business/update-paikka/route.ts. Check the ownership query (~line 39):
- It must query business_paikka_links with .eq('business_account_id', user.id) and .eq('paikka_id', paikka_id) only
- There must be NO .eq('claim_status', ...) or .filter('claim_status', ...) anywhere in the ownership check
- Any linked claimant (pending, approved, or rejected) must be allowed to save

If the ownership check has a claim_status filter: remove it.
If already clean: record "CLEAN-03: PASS — no claim_status filter in ownership check" in the task output.
  </action>
  <acceptance_criteria>
    - app/api/business/update-paikka/route.ts ownership query has no claim_status filter
    - Any business_account with a business_paikka_links row for the paikka_id can save, regardless of claim_status
  </acceptance_criteria>
  <done>CLEAN-03 verified: ownership check uses only business_account_id + paikka_id — no claim_status restriction.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify CLEAN-04 — OnboardingWizardInner has step-forward guard</name>
  <files>app/business/onboarding/OnboardingWizardInner.tsx</files>
  <read_first>
    - app/business/onboarding/OnboardingWizardInner.tsx — lines 159–166
  </read_first>
  <action>
Read app/business/onboarding/OnboardingWizardInner.tsx lines 159–166. Check for a useEffect that:
- Reads the current URL step param
- Compares it to maxReachedStep
- Redirects to maxReachedStep+1 if step > maxReachedStep+1

If the guard is absent: add it (mirror lines 159–166 from CONTEXT.md D-14).
If already present: record "CLEAN-04: PASS — maxReachedStep guard present at lines 159–166" in the task output.
  </action>
  <acceptance_criteria>
    - OnboardingWizardInner has a useEffect guard that redirects URL step > maxReachedStep+1 to maxReachedStep+1
    - The guard runs only when loading is false (guard condition: if (loading) return)
  </acceptance_criteria>
  <done>CLEAN-04 verified: maxReachedStep guard present — URL step manipulation cannot skip past unfinished steps.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify CLEAN-05 — submit route has no onboarding_completed write</name>
  <files>app/api/business/onboarding/submit/route.ts</files>
  <read_first>
    - app/api/business/onboarding/submit/route.ts — full file
    - supabase/migrations/20260611000000_drop_onboarding_completed.sql — confirm column is dropped
  </read_first>
  <action>
Read app/api/business/onboarding/submit/route.ts. Grep the full file for "onboarding_completed":
- There must be ZERO occurrences of "onboarding_completed" anywhere in the file

Also read supabase/migrations/20260611000000_drop_onboarding_completed.sql:
- The migration must contain ALTER TABLE business_accounts DROP COLUMN onboarding_completed

If the route still writes onboarding_completed: remove that write.
If already clean: record "CLEAN-05: PASS — no onboarding_completed write; column already dropped" in the task output.
  </action>
  <acceptance_criteria>
    - app/api/business/onboarding/submit/route.ts contains no "onboarding_completed" string
    - supabase/migrations/20260611000000_drop_onboarding_completed.sql exists and drops the column
  </acceptance_criteria>
  <done>CLEAN-05 verified: submit route has no onboarding_completed write; migration already dropped the column.</done>
</task>

</tasks>

<verification>
After all tasks complete:
- `grep -r "onboarding_completed" app/api/business/` returns empty
- `grep "claim_status" app/api/business/update-paikka/route.ts` returns empty (no claim_status filter)
- `grep "maxReachedStep" app/business/onboarding/OnboardingWizardInner.tsx` returns at least 3 matches
</verification>

<success_criteria>
All three verification tasks pass with no code changes required. CLEAN-03, CLEAN-04, and CLEAN-05 are confirmed implemented. Any unexpected failures are fixed with minimal targeted changes.
</success_criteria>

<output>
Create .planning/phases/40-wizard-konsolidointi-cleanup/40-01-SUMMARY.md when done
</output>
