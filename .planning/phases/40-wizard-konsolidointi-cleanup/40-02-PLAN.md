---
phase: 40-wizard-konsolidointi-cleanup
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - supabase/migrations/20260612000000_cleanup_test_accounts.sql
autonomous: true
requirements:
  - CLEAN-01

must_haves:
  truths:
    - "supabase/migrations/20260612000000_cleanup_test_accounts.sql exists and deletes all rows from auth.users where id IN (SELECT user_id FROM business_accounts)"
    - "The migration cascades automatically to business_accounts, business_paikka_links, and onboarding_draft via ON DELETE CASCADE FKs"
    - "Migration does NOT drop onboarding_completed — that column was already dropped in 20260611000000_drop_onboarding_completed.sql"
    - "The migration is idempotent: if business_accounts is already empty, the DELETE is a no-op"
  artifacts:
    - path: "supabase/migrations/20260612000000_cleanup_test_accounts.sql"
      provides: "Test account cleanup — deletes all business test users and cascades to dependent tables"
  key_links: []
---

<objective>
Create the SQL migration that deletes all test business accounts. All rows in business_accounts are test data (no production business users exist). Deleting from auth.users cascades to business_accounts, business_paikka_links, and onboarding_draft via ON DELETE CASCADE foreign keys — one DELETE statement cleans everything.

Purpose: Satisfies CLEAN-01 — no test account rows remain in business_accounts or auth.users after the migration runs.
Output: supabase/migrations/20260612000000_cleanup_test_accounts.sql
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/40-wizard-konsolidointi-cleanup/40-CONTEXT.md

<schema_refs>
business_accounts:
  user_id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE
  (→ deleting from auth.users cascades to business_accounts)

business_paikka_links:
  business_account_id UUID REFERENCES business_accounts(user_id) ON DELETE CASCADE
  (→ deleting from business_accounts cascades to business_paikka_links)

onboarding_draft:
  business_account_id UUID REFERENCES business_accounts(user_id) ON DELETE CASCADE
  (→ deleting from business_accounts cascades to onboarding_draft)

onboarding_completed: ALREADY DROPPED in migration 20260611000000_drop_onboarding_completed.sql
  — do NOT attempt to drop it again
</schema_refs>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Read existing migration timestamps to determine correct filename</name>
  <files>supabase/migrations/</files>
  <read_first>
    - supabase/migrations/ — list files to confirm latest timestamp
  </read_first>
  <action>
List files in supabase/migrations/ to confirm the latest existing timestamp. The new migration must sort AFTER the latest existing file.

Latest known migration: 20260611000002_approval_trigger_not_found.sql
New migration filename: 20260612000000_cleanup_test_accounts.sql (today is 2026-06-12)

Confirm no file named 20260612000000_cleanup_test_accounts.sql exists yet.
  </action>
  <acceptance_criteria>
    - Latest existing migration timestamp confirmed
    - New filename 20260612000000_cleanup_test_accounts.sql sorts after all existing migrations
  </acceptance_criteria>
  <done>Migration timestamp confirmed — 20260612000000 is the correct next filename.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create cleanup migration</name>
  <files>supabase/migrations/20260612000000_cleanup_test_accounts.sql</files>
  <read_first>
    - supabase/migrations/20260611000000_drop_onboarding_completed.sql — confirm onboarding_completed already dropped (do not re-drop)
    - supabase/migrations/20260605000000_business_accounts.sql — confirm FK cascade relationships
  </read_first>
  <action>
Create supabase/migrations/20260612000000_cleanup_test_accounts.sql with the following content:

The migration must:
1. Delete all auth.users rows where id exists in business_accounts — this cascades automatically to business_accounts (ON DELETE CASCADE), which cascades to business_paikka_links and onboarding_draft (both have ON DELETE CASCADE from business_accounts.user_id)
2. NOT drop onboarding_completed (already dropped in 20260611000000_drop_onboarding_completed.sql)
3. Be wrapped in a DO block that captures the user IDs first to avoid empty-subquery edge case (or use a direct DELETE with subquery — both are correct since the subquery evaluates before the DELETE executes)
4. Include a comment explaining this is a one-time cleanup of test data, not a recurring pattern

Use a direct DELETE with subquery pattern (simpler and equally safe):
```sql
-- Phase 40 CLEAN-01: Delete all test business accounts.
-- All business_accounts rows are test data — no production business users exist.
-- Deleting from auth.users cascades via ON DELETE CASCADE to:
--   business_accounts → business_paikka_links → (link children)
--   business_accounts → onboarding_draft
DELETE FROM auth.users
WHERE id IN (SELECT user_id FROM business_accounts);
```

The subquery `SELECT user_id FROM business_accounts` evaluates before any row is deleted, so the DELETE correctly targets all current business user IDs even though business_accounts will be empty after the cascade.
  </action>
  <acceptance_criteria>
    - supabase/migrations/20260612000000_cleanup_test_accounts.sql exists
    - File contains: DELETE FROM auth.users WHERE id IN (SELECT user_id FROM business_accounts)
    - File contains no DROP COLUMN for onboarding_completed
    - File contains an explanatory comment about cascades
  </acceptance_criteria>
  <done>Migration created: deletes all test business accounts via auth.users cascade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration runs as postgres superuser | Can write to auth schema — correct for this operation |
| Subquery evaluation order | Evaluated before DELETE — safe, no race condition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02-01 | Tampering | auth.users DELETE scope | mitigate | WHERE clause limits deletion to business_accounts user_ids only — consumer accounts not affected |
| T-40-02-02 | Denial of Service | Accidental production deletion | accept | All business_accounts are confirmed test data; migration comment documents this assumption |
</threat_model>

<verification>
After task completes:
- `cat supabase/migrations/20260612000000_cleanup_test_accounts.sql` shows the DELETE statement
- No DROP COLUMN in the migration file
- File sorts after 20260611000002 alphabetically
</verification>

<success_criteria>
supabase/migrations/20260612000000_cleanup_test_accounts.sql exists with the correct DELETE statement. After manual application via Supabase Dashboard (supabase db push or SQL editor), business_accounts and the corresponding auth.users rows will be empty.
</success_criteria>

<output>
Create .planning/phases/40-wizard-konsolidointi-cleanup/40-02-SUMMARY.md when done
</output>
