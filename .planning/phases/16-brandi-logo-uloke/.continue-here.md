---
context: phase
phase: 16-brandi-logo-uloke
task: null
status: paused
last_updated: 2026-05-29
---

_No blocking constraints._

<current_state>
Etusivu.tsx has been reverted to commit 3d1cd6c — the pre-bump state. This means:
- No clip-path bump geometry
- No AktiiviLogo wired in
- No gradIndex state
- Sheet uses old pill animation (HANDLE_H=44, pillInset, left/right/borderRadius animation)
- Standard drag bar (w-10 h-1) still in place

AktiiviLogo.tsx component still exists and is correct (plan 02 artifact).
Plans 01 and 02 remain complete. Plan 03 wiring is reverted.

The revert is staged but not yet committed — HANDOFF commit covers it.
</current_state>

<completed_work>
- Plan 01: AKTIIVI rebrand (layout, manifest, tietosuoja) ✓
- Plan 02: AktiiviLogo.tsx SVG component with gradient sweep ✓
- Plan 03: Etusivu wiring — REVERTED (bump approach abandoned)
</completed_work>

<remaining_work>
- Plan 04: New uloke design — start from scratch with a fresh approach
  - What should the tab/handle look like?
  - How does AktiiviLogo integrate without the bump geometry?
  - Shadow technique: box-shadow on inner div at shoulder level WORKS (see decisions)
</remaining_work>

<decisions_made>
- Bump clip-path approach abandoned — visual integration was never satisfying
- Etusivu.tsx reverted to pre-bump state (3d1cd6c)
- PROVEN: box-shadow on a non-clipped child div at `top: TAB_H` creates visible shadow at the sheet/map boundary. Values that worked: `boxShadow: '0 0 40px 20px rgba(0,0,0,0.55)'`
- filter:drop-shadow does NOT work when backdrop-filter is present (compositing conflict)
- SVG sibling stroke visible as separate during animation — do not retry
</decisions_made>

<blockers>
- Design direction: need to decide what the new uloke/logo tab looks like before coding
</blockers>

## Required Reading
1. `app/components/AktiiviLogo.tsx` — the logo component that needs to be placed somewhere
2. `.planning/phases/16-brandi-logo-uloke/16-DISCUSSION-LOG.md` — original design intent

<context>
The user wants a fresh start on how the logo tab works. The previous approach (clip-path bump protruding above the sheet) was technically implemented but never looked "connected" to the sheet. Start with design discussion before any code.
</context>

<next_action>
Discuss with user: what should the new logo tab design look like? Options to consider: (A) standard rounded-t-3xl top with AktiiviLogo replacing the drag bar inside the sheet, (B) a simpler tab shape that doesn't require clip-path, (C) something else entirely.
</next_action>
