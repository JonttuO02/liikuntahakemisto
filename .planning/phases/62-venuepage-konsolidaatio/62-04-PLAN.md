---
phase: 62-venuepage-konsolidaatio
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Etusivu.tsx
  - .planning/phases/62-venuepage-konsolidaatio/62-UAT.md
autonomous: true
gap_closure: true
requirements: [VENUEPAGE-03]
must_haves:
  truths:
    - "Tapping a card in the search results list opens PaikkaSheet on top of the still-mounted search list; closing the sheet returns the user to the search list, not the base map/sheet state."
    - "Tapping a saved card in the TO DO overlay opens PaikkaSheet on top of the still-mounted TO DO overlay; closing the sheet returns the user to the TO DO overlay."
  artifacts:
    - app/components/Etusivu.tsx
  key_links:
    - "Etusivu.tsx onOpen handlers (search list + TO DO overlay) set only the selected venue and leave the overlay-open flags untouched, so the AnimatePresence-wrapped overlay stays mounted beneath PaikkaSheet (zIndex 65/66 > 59/62)."
---

<objective>
Fix the Phase 62 UAT Test 2 regression: opening PaikkaSheet from the search results list or the TO DO (favorites) overlay currently unmounts that overlay, forcing the user to reopen the list after every venue view. The overlay must stay mounted underneath PaikkaSheet so the user resumes browsing where they left off when the sheet closes.

Purpose: Restore uninterrupted browse-open-close-browse flow for VENUEPAGE-03 (internal paths open the bottom sheet the same way as a CalloutCard click, without collapsing the surrounding navigation surface).
Output: Two one-line handler edits in Etusivu.tsx plus a UAT Test 1 wording correction to reconcile the reversed product intent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Full root-cause evidence (diagnose-only debug session)
@.planning/debug/paikkasheet-dismisses-search-todo-overlay.md
@.planning/phases/62-venuepage-konsolidaatio/62-UAT.md

# Target file — the two onOpen handlers live at ~line 1026 (TO DO overlay)
# and ~line 1422 (search results list). PaikkaSheet render + onClose is at ~line 1452.
@app/components/Etusivu.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stop the onOpen handlers from unmounting their overlay</name>
  <files>app/components/Etusivu.tsx</files>
  <action>
Two DiagonaalKortti `onOpen` handlers currently clear the overlay-open flag in the same handler that selects the venue, which unmounts the AnimatePresence-wrapped overlay instead of layering PaikkaSheet over it. Remove only the flag-clearing call from each handler; keep the `setValittu(clicked)` selection call.

1. TO DO overlay card handler (~line 1026): the inline handler currently clears the TO DO overlay flag before selecting the venue. Delete the overlay-flag-clearing call so the handler becomes `onOpen={(clicked) => { setValittu(clicked) }}`.
<!-- planner-discipline-allow: setTodoOpen(false) -->

2. Search results list card handler (~lines 1422-1425): the multi-line handler currently clears the search-open flag before selecting the venue. Delete that flag-clearing line so the handler body is only `setValittu(clicked)`.
<!-- planner-discipline-allow: setSearchOpen(false) -->

Do NOT touch:
- The separate `onShowMap` handlers (search onShowMap at ~line 1416 legitimately closes the search list to reveal the map — leave it).
- `openSearch`, `openTodoOverlay`, `closeOverlays` (~lines 462-489) — those intentionally clear flags when switching surfaces.
- PaikkaSheet's `onClose` at ~line 1458 (`setValittu(null)`). It correctly leaves the overlay flags untouched, so the overlay remains mounted and re-appears when the sheet closes. No change needed there.

Root cause reference: PaikkaSheet renders at zIndex 65/66, above the search list (59) and TO DO overlay (62), so removing the two flag-clearing calls is sufficient for correct layering — no z-index or stacking-context changes required (per debug session, evidence 2026-07-01T00:12:00Z).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit && rg -Uq --multiline-dotall 'onOpen=\{\(clicked\) => \{[^}]*setSearchOpen\(false\)' app/components/Etusivu.tsx && echo FAIL || rg -Uq --multiline-dotall 'onOpen=\{\(clicked\) => \{[^}]*setTodoOpen\(false\)' app/components/Etusivu.tsx && echo FAIL || echo PASS</automated>
    <human-check>Open the app (Etusivu). Search for a venue so the search list shows results; tap a card's info panel. PaikkaSheet opens ON TOP of the still-visible search list. Close the sheet — you land back in the search list, not the base map. Repeat from the TO DO overlay with a saved card: sheet layers over the TO DO overlay, closing returns you to the TO DO overlay.</human-check>
  </verify>
  <done>Neither onOpen handler clears its overlay flag; tsc passes; the region-anchored greps for `onOpen={(clicked) => { ... setSearchOpen(false)` and `... setTodoOpen(false)` return no match. PaikkaSheet layers over the originating overlay and closing it resumes browsing.</done>
</task>

<task type="auto">
  <name>Task 2: Correct UAT Test 1 wording to match the fixed intent</name>
  <files>.planning/phases/62-venuepage-konsolidaatio/62-UAT.md</files>
  <action>
Test 1 (CR-01 regression confirmation) currently asserts the old dismiss-on-open behavior that Test 2 has since established is wrong. Its true intent was only that tapping the info panel must trigger `onOpen` and open PaikkaSheet at all — not that the overlay must be dismissed.

In Test 1's `expected:` field, replace the clause stating that the overlay "should be dismissed rather than remaining visible underneath the sheet" with the corrected expectation: PaikkaSheet should open for the tapped venue in both cases, layering ON TOP of the overlay while it remains mounted underneath (so closing the sheet resumes browsing). This aligns Test 1 with Test 2 and the fix.

Do not change Test 1's `result: pass` line or the Summary counts — this is a wording reconciliation of an already-passing behavioral assertion, not a re-test.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && rg -q 'should be dismissed rather than remaining visible underneath the sheet' .planning/phases/62-venuepage-konsolidaatio/62-UAT.md && echo FAIL || echo PASS</automated>
  </verify>
  <done>Test 1's expected wording no longer asserts the overlay is dismissed on open; it describes layering over a still-mounted overlay, consistent with Test 2 and the code fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This change only removes two client-side React state mutations; no new input crosses a trust boundary, no data flow, network call, or persistence changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-04-01 | Tampering | Etusivu.tsx view state | low | accept | Change is a pure client-side render-order fix with no auth, data, or persistence impact; no mitigation required. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (no type/syntax breakage from the handler edits).
- Region-anchored greps confirm neither onOpen handler clears its overlay flag.
- Manual: PaikkaSheet layers over the search list and TO DO overlay; closing the sheet resumes browsing in the originating surface.
- 62-UAT.md Test 1 wording reconciled with Test 2.
</verification>

<success_criteria>
- Opening PaikkaSheet from the search results list leaves the search list mounted underneath; closing the sheet returns to the search list.
- Opening PaikkaSheet from the TO DO overlay leaves the overlay mounted underneath; closing the sheet returns to the TO DO overlay.
- No regression to onShowMap (map view) behavior or to surface-switching helpers.
- VENUEPAGE-03 browse-continuity restored.
</success_criteria>

<output>
Create `.planning/phases/62-venuepage-konsolidaatio/62-04-SUMMARY.md` when done.
</output>
