---
phase: 62-venuepage-konsolidaatio
plan: 03
type: execute
wave: 2
depends_on: ["62-01", "62-02"]
files_modified:
  - app/components/Etusivu.tsx
  - app/paikat/[id]/page.tsx
  - messages/fi.json
  - messages/en.json
autonomous: true
requirements: [VENUEPAGE-01, VENUEPAGE-03, VENUEPAGE-04]
must_haves:
  truths:
    - "Clicking a DiagonaalKortti in the search list opens PaikkaSheet and dismisses the search overlay (no page navigation)"
    - "Clicking a DiagonaalKortti in the TO DO overlay opens PaikkaSheet and dismisses the TO DO overlay"
    - "Visiting /paikat/123 returns a Next.js 404 with no redirect"
    - "npm run build passes with no references to the deleted route and no PaikkaPage namespace remaining"
  artifacts:
    - "app/components/Etusivu.tsx wiring onOpen on both DiagonaalKortti usages and with handleCardClick removed"
    - "app/paikat/[id]/ directory deleted"
    - "messages/fi.json and messages/en.json with the PaikkaPage namespace removed"
  key_links:
    - "DiagonaalKortti.onOpen -> Etusivu setValittu(p) -> PaikkaSheet renders (same path as CalloutCard click)"
    - "Route 404 is automatic on directory deletion (D-08) — no not-found.tsx, no redirect"
---

<objective>
Complete the consolidation: wire DiagonaalKortti's new `onOpen` callback to `setValittu(p)` at both Etusivu usage sites (search list and TO DO overlay), remove the now-dead `handleCardClick` scroll-state function and its wiring (D-06), delete the `app/paikat/[id]/` directory entirely so the route auto-404s (D-08, VENUEPAGE-01/04), and remove the orphaned `PaikkaPage` i18n namespace from both message files. This plan depends on Plan 02 (the `onOpen` prop must exist) and Plan 01 (the show-on-map content must be migrated before deletion).

Purpose: Cards open the sheet inline, the separate venue page is gone, and the route returns 404.
Output: Rewired `Etusivu.tsx`, deleted `app/paikat/[id]/`, cleaned message files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-venuepage-konsolidaatio/62-CONTEXT.md
@.planning/phases/62-venuepage-konsolidaatio/62-RESEARCH.md
@.planning/phases/62-venuepage-konsolidaatio/62-PATTERNS.md
@.planning/phases/62-venuepage-konsolidaatio/62-UI-SPEC.md
@app/components/DiagonaalKortti.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire onOpen at both DiagonaalKortti sites and remove handleCardClick</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    - app/components/Etusivu.tsx (targeted reads: the `handleCardClick` function (~lines 492-505), the search-list `<DiagonaalKortti>` usage that passes `onCardClick={handleCardClick}` (~lines 1460-1473), the TO DO overlay `<DiagonaalKortti>` usage (~line 1074), the existing `onShowMap` handlers for the pattern of calling `setSearchOpen(false)` first, and the `setValittu` usage by CalloutCard (~line 916) as the reference open-sheet call)
    - app/components/DiagonaalKortti.tsx (confirm the `onOpen` prop signature added in Plan 02 and that `onCardClick` no longer exists)
    - .planning/phases/62-venuepage-konsolidaatio/62-PATTERNS.md (Etusivu section — exact handler snippets)
    - .planning/phases/62-venuepage-konsolidaatio/62-RESEARCH.md (Pitfalls 2 and 5 — dismiss overlay before opening; remove the stale onCardClick call site)
  </read_first>
  <action>
    At the search-list `<DiagonaalKortti>` usage (~line 1460), remove the `onCardClick={handleCardClick}` prop and add an `onOpen` prop whose handler first calls `setSearchOpen(false)` (to dismiss the search overlay so it does not layer over the sheet — RESEARCH pitfall 2) and then calls `setValittu(p)` with the card's `paikka`. Keep the existing `onShowMap`, `key`, `paikka`, and `distanceStr` props unchanged.

    At the TO DO overlay `<DiagonaalKortti>` usage (~line 1074), add an `onOpen` prop whose handler first calls `setTodoOpen(false)` (dismiss the TO DO overlay) and then calls `setValittu(p)`. Keep the existing `paikka`, `isSaved`, `onShowMap`, and `onToggleTodo` props unchanged. (Confirm the exact TO DO overlay state setter name while reading; it is the state that gates the TO DO overlay's visibility — use that setter, not a guessed name.)

    Delete the `handleCardClick` function (~lines 492-505) entirely — it only wrote `etusivu-scroll-state` to sessionStorage before a navigation that no longer occurs; after removing the `onCardClick={handleCardClick}` call site it has no remaining references (D-06).

    Discretion decision (D-06, recorded): do the MINIMAL cleanup — remove only `handleCardClick` and its single call site. LEAVE the scroll-restore `useEffect` (~lines 598-622) and `searchResultsRef` (~line 427, still attached at ~line 1438) in place as harmless dead code: the restore effect early-returns because the sessionStorage key is never written again, and `searchResultsRef` stays referenced so no unused-variable error appears. Do NOT remove the restore effect or the ref in this plan — that broader churn is out of scope and risks touching the scroll container wiring.

    Do not change any other Etusivu logic, state, or the `/?id=` focusId handler (the show-on-map link relies on it).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes without TypeScript errors (no unknown `onCardClick` prop, no undefined `handleCardClick` reference)
    - Source assertion: `grep -c "handleCardClick" app/components/Etusivu.tsx` returns 0
    - Source assertion: `grep -c "onOpen" app/components/Etusivu.tsx` returns 2 (the two DiagonaalKortti sites)
    - Source assertion: the search-list onOpen handler calls `setSearchOpen(false)` before `setValittu`; the TO DO onOpen handler dismisses the TO DO overlay before `setValittu`
    - Behavior assertion (manual smoke): clicking a card in the search list opens PaikkaSheet and the search overlay is gone (not layered on top); clicking a card in the TO DO overlay opens PaikkaSheet
  </acceptance_criteria>
  <done>Both DiagonaalKortti sites pass `onOpen` wired to `setValittu` (with the correct overlay dismissed first), `handleCardClick` and its call site are removed, and the build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete app/paikat/[id]/ and remove the PaikkaPage i18n namespace</name>
  <files>app/paikat/[id]/page.tsx, messages/fi.json, messages/en.json</files>
  <read_first>
    - app/paikat/[id]/page.tsx (confirm it uses `getTranslations('PaikkaPage')` and that the show-on-map content it renders is already migrated to PaikkaSheet by Plan 01 — verify nothing else unique remains before deleting)
    - messages/fi.json (locate the `PaikkaPage` object — distinct from `PaikkaSheet` — including its surrounding commas and the sibling block after it, e.g. `NotFound`)
    - messages/en.json (locate the `PaikkaPage` object — same structure)
    - .planning/phases/62-venuepage-konsolidaatio/62-CONTEXT.md (D-07 — PaikkaKortti `/paikat/` links are intentionally left to 404 in this phase; do NOT touch PaikkaKortti.tsx)
  </read_first>
  <action>
    Delete the entire `app/paikat/[id]/` directory (its only file is `page.tsx`). Use a filesystem delete (for example `git rm -r "app/paikat/[id]"`). Do NOT leave a `not-found.tsx`, a stub, or a redirect — Next.js App Router auto-returns 404 for the removed route (D-08, VENUEPAGE-04). If the parent `app/paikat/` directory becomes empty after this, removing the empty `app/paikat/` directory too is acceptable; if other files remain under `app/paikat/`, leave them.

    In `messages/fi.json`, remove the entire `PaikkaPage` object (the key and its full body, including the comma that joins it to its sibling) so the JSON remains valid. Do the same in `messages/en.json`. Do NOT touch the `PaikkaSheet` namespace (it now carries the migrated `location`/`showOnMap` keys from Plan 01).

    Do NOT modify `app/components/PaikkaKortti.tsx`: its two `/paikat/` links are deliberately deferred to Phase 63 (D-07). They will 404 after this deletion — an accepted, business-side-only consequence (PreviewModal context).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes without errors (no import resolves to the deleted page; both message files remain valid JSON)
    - Source assertion: `ls "app/paikat/[id]" 2>/dev/null` reports the directory does not exist
    - Source assertion: `grep -rc "PaikkaPage" messages/fi.json messages/en.json` returns 0 for both files
    - Source assertion: `grep -rn "PaikkaPage" app/ --include="*.tsx" --include="*.ts"` returns no results (the only user was the deleted page)
    - Behavior assertion (manual UAT): navigating the browser to `/paikat/1` shows the Next.js 404 page (no redirect)
  </acceptance_criteria>
  <done>The `app/paikat/[id]/` directory is deleted, the route 404s, the `PaikkaPage` namespace is gone from both message files, and the build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user interaction -> sheet state | DiagonaalKortti `onOpen` triggers Etusivu `setValittu` to open PaikkaSheet |
| public web -> deleted route | A direct request to `/paikat/[id]` now hits the App Router 404 path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-05 | Information Disclosure | deleted venue route | low | accept | The route rendered only public venue data; removing it reduces surface area, discloses nothing new; 404 leaks no information |
| T-62-06 | Tampering | residual PaikkaKortti `/paikat/` links | low | accept | Deferred to Phase 63 (D-07); links are internal and 404 (not external redirects); no security impact, business-side PreviewModal only |
| T-62-07 | Denial of Service | malformed JSON after PaikkaPage removal | low | mitigate | `npm run build` fails fast if either message file becomes invalid JSON, blocking a broken release |
| T-62-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed in this plan or phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npm run build` passes after both tasks.
- `app/paikat/[id]/` does not exist; `/paikat/1` returns 404 (manual).
- No `PaikkaPage` references remain in `app/` or in either message file.
- Both DiagonaalKortti sites are wired to `onOpen` -> `setValittu`; `handleCardClick` removed.
</verification>

<success_criteria>
- VENUEPAGE-01: `app/paikat/[id]` deleted.
- VENUEPAGE-03: both internal navigation paths open PaikkaSheet inline (no page navigation).
- VENUEPAGE-04: direct URL to the deleted route returns 404 with no redirect.
- Build is green; PaikkaKortti deferral (D-07) is the only accepted residual.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- `app/components/Etusivu.tsx`: `onOpen` wired on the search-list DiagonaalKortti (`setSearchOpen(false)` then `setValittu(p)`) and on the TO DO overlay DiagonaalKortti (dismiss TO DO overlay then `setValittu(p)`); `handleCardClick` function and its `onCardClick={handleCardClick}` call site removed
- `app/paikat/[id]/` directory: DELETED (route now auto-404s)
- `messages/fi.json` / `messages/en.json`: `PaikkaPage` namespace removed
</artifacts_produced>

<output>
Create `.planning/phases/62-venuepage-konsolidaatio/62-03-SUMMARY.md` when done.
</output>
