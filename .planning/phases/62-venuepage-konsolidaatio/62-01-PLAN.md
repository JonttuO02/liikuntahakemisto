---
phase: 62-venuepage-konsolidaatio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/PaikkaSheet.tsx
  - messages/fi.json
  - messages/en.json
autonomous: true
requirements: [VENUEPAGE-02]
must_haves:
  truths:
    - "PaikkaSheet shows a Nayta-kartalla row when the venue has latitude and longitude and the sheet is not in preview mode"
    - "The Nayta-kartalla row is absent when coordinates are null or when preview=true"
    - "Clicking the Nayta-kartalla link navigates to /?id=<paikka.id>"
  artifacts:
    - "app/components/PaikkaSheet.tsx with MapPin imported and a conditional Nayta-kartalla SheetRow"
    - "messages/fi.json PaikkaSheet namespace with location + showOnMap keys"
    - "messages/en.json PaikkaSheet namespace with location + showOnMap keys"
  key_links:
    - "The new anchor href=/?id=${paikka.id} reuses the existing Etusivu focusId handler that centers the map"
    - "t('location') and t('showOnMap') resolve via useTranslations('PaikkaSheet')"
---

<objective>
Migrate the only unique content of the soon-to-be-deleted venue page (`app/paikat/[id]/page.tsx`) — the "Nayta kartalla" (show-on-map) link — into PaikkaSheet as a new conditional SheetRow, and add its two i18n keys to the `PaikkaSheet` namespace in both message files. This is the content-migration prerequisite (VENUEPAGE-02, per D-01/D-02/D-03) that MUST land before the route is deleted in Plan 03.

Purpose: PaikkaSheet becomes feature-complete so deleting `app/paikat/[id]/` loses no user-facing capability.
Output: One new SheetRow in PaikkaSheet plus `location` and `showOnMap` keys in the `PaikkaSheet` namespace (fi + en).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-venuepage-konsolidaatio/62-CONTEXT.md
@.planning/phases/62-venuepage-konsolidaatio/62-RESEARCH.md
@.planning/phases/62-venuepage-konsolidaatio/62-PATTERNS.md
@.planning/phases/62-venuepage-konsolidaatio/62-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add MapPin import and the show-on-map SheetRow to PaikkaSheet</name>
  <files>app/components/PaikkaSheet.tsx</files>
  <read_first>
    - app/components/PaikkaSheet.tsx (FULL read — confirm the lucide-react import line, the `preview` prop on the component, the existing Description SheetRow block guarded by `paikka.kuvaus` using `label={t('description')}`, the SheetRow function definition, and the Reviews section that follows Description)
    - app/paikat/[id]/page.tsx (read the show-on-map link block — the source being migrated — to confirm the visibility condition uses `latitude != null && longitude != null` and the `/?id=` href shape)
    - .planning/phases/62-venuepage-konsolidaatio/62-PATTERNS.md (PaikkaSheet section — exact SheetRow snippet and the icon-size rule)
  </read_first>
  <action>
    Append `MapPin` to the existing `lucide-react` import in PaikkaSheet.tsx (the list currently ends with `BadgeCheck`). Do not rename or reorder existing icons — append `MapPin` only.

    Insert a new conditional SheetRow immediately AFTER the Description SheetRow block (the block guarded by `paikka.kuvaus`) and BEFORE the Reviews section. Guard the row with the exact condition `paikka.latitude != null && paikka.longitude != null && !preview` — all three clauses, in that order. Reuse the existing `SheetRow` component (do NOT define a new component). Pass `icon={<MapPin className="w-4 h-4" />}` and `label={t('location')}`. Use `w-4 h-4` for the icon to match every existing SheetRow icon (Info, Phone, Clock, CircleDollarSign all use `w-4 h-4`). Do NOT use `w-5 h-5`: the UI-SPEC code sample shows `w-5 h-5`, but it contradicts the established SheetRow icon size and the CLAUDE.md consistency rule wins (RESEARCH anti-pattern 3). The SheetRow wrapper already applies the muted icon color, so add no text-color class to the MapPin glyph (match the existing `<Info className="w-4 h-4" />` usage).

    The SheetRow child is a single anchor: an `<a>` whose `href` is a template literal of `/?id=` followed by `${paikka.id}`, and whose visible text is `{t('showOnMap')}`. Apply the className `text-[#111111] hover:text-[rgba(17,17,17,0.6)] text-sm font-bold underline underline-offset-2 [transition:color_150ms_var(--ease-out)]`. Use a plain `<a>` tag, NOT a Next.js `<Link>` — the click triggers a full URL context change that the Etusivu focusId handler consumes (D-01 behavior); this is intentional.

    Do not modify any other row, the hero area, or the SheetRow function definition. The row goes in the sheet body with the other data rows (D-02), not in the hero corner.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes without TypeScript errors
    - Source assertion: `grep -n "MapPin" app/components/PaikkaSheet.tsx` shows MapPin in the import line and the SheetRow icon className is `w-4 h-4` (not `w-5 h-5`)
    - Source assertion: PaikkaSheet.tsx contains the substring `paikka.latitude != null && paikka.longitude != null && !preview`
    - Source assertion: PaikkaSheet.tsx contains `label={t('location')}` and `{t('showOnMap')}`
    - Behavior assertion (manual smoke at end of wave): PaikkaSheet for a venue WITH coordinates shows a SIJAINTI row with a show-on-map link; a venue WITHOUT coordinates and any preview render show no such row
  </acceptance_criteria>
  <done>MapPin is imported, the coordinate-and-preview-guarded show-on-map SheetRow renders in the sheet body after Description, and the build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add location + showOnMap keys to the PaikkaSheet i18n namespace (fi + en)</name>
  <files>messages/fi.json, messages/en.json</files>
  <read_first>
    - messages/fi.json (read the `PaikkaSheet` namespace block near the top — keys `price` through `close` — confirm `"close"` is the last key and needs a trailing comma added)
    - messages/en.json (read the `PaikkaSheet` namespace block near the top — same structure)
    - .planning/phases/62-venuepage-konsolidaatio/62-RESEARCH.md (i18n key migration section — exact values; the FI showOnMap value omits the trailing arrow the old PaikkaPage key had)
  </read_first>
  <action>
    In `messages/fi.json`, inside the `PaikkaSheet` object ONLY (near the top of the file, NOT the `PaikkaPage` object lower down), add two keys after the existing `"close"` key (add a trailing comma to `"close"` first): a `location` key with the Finnish value Sijainti, and a `showOnMap` key with the Finnish value Nayta kartalla (use the proper Finnish word with the a-umlaut). The FI showOnMap value must NOT include any trailing arrow glyph — the old PaikkaPage version had one; the new key omits it per UI-SPEC.

    In `messages/en.json`, inside the `PaikkaSheet` object ONLY, add the same two keys after `"close"`: a `location` key with the English value Location, and a `showOnMap` key with the English value Show on map.

    Do NOT touch the `PaikkaPage` namespace in either file in this plan — that block is removed later in Plan 03. Place both keys under `PaikkaSheet` exclusively; PaikkaSheet.tsx reads them via `useTranslations('PaikkaSheet')`, so keys added under `PaikkaPage` would not resolve at runtime (RESEARCH pitfall 4).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes (both JSON files remain valid JSON; an invalid JSON breaks the next-intl build)
    - Source assertion: `grep -n "location" messages/fi.json` and `grep -n "showOnMap" messages/fi.json` show the keys are inside the `PaikkaSheet` block (above the `PaikkaPage` block, which still exists at this point)
    - Source assertion: `grep -c "showOnMap" messages/fi.json` returns 2 (one in PaikkaSheet, one still in PaikkaPage) and `grep -c "showOnMap" messages/en.json` returns 2
    - Behavior assertion: at runtime `t('location')` resolves to Sijainti (fi) / Location (en) and `t('showOnMap')` resolves without a trailing arrow
  </acceptance_criteria>
  <done>Both message files have `location` and `showOnMap` under the `PaikkaSheet` namespace; the build passes and the FI showOnMap value has no trailing arrow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser -> URL navigation | The show-on-map anchor builds a `/?id=X` URL from `paikka.id` |
| build pipeline -> message JSON | next-intl loads `messages/*.json` at build/render time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-01 | Tampering | show-on-map anchor href `/?id=${paikka.id}` | low | accept | `paikka.id` is a numeric Supabase primary key from trusted DB data, not user input; no open-redirect surface (the URL stays same-origin) |
| T-62-02 | Denial of Service | malformed JSON in messages/*.json | low | mitigate | `npm run build` fails fast on invalid JSON, blocking a broken release |
| T-62-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed in this plan or phase (RESEARCH Package Legitimacy Audit: none); nothing to verify |
</threat_model>

<verification>
- `npm run build` passes after both tasks.
- PaikkaSheet renders the show-on-map SheetRow only when coordinates exist and `preview` is false.
- Both message files are valid JSON and carry the two new `PaikkaSheet` keys.
</verification>

<success_criteria>
- VENUEPAGE-02 unique content (show-on-map) exists in PaikkaSheet with correct visibility gating.
- i18n keys live under the `PaikkaSheet` namespace in fi + en.
- Build is green; no regressions to existing SheetRows.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- `app/components/PaikkaSheet.tsx`: new `MapPin` import; new conditional `SheetRow` (guard `paikka.latitude != null && paikka.longitude != null && !preview`) rendering an `<a href="/?id=${paikka.id}">` labeled via `t('location')` / `t('showOnMap')`
- `messages/fi.json`: `PaikkaSheet.location` = "Sijainti", `PaikkaSheet.showOnMap` = "Nayta kartalla" (with umlaut, no arrow)
- `messages/en.json`: `PaikkaSheet.location` = "Location", `PaikkaSheet.showOnMap` = "Show on map"
</artifacts_produced>

<output>
Create `.planning/phases/62-venuepage-konsolidaatio/62-01-SUMMARY.md` when done.
</output>
