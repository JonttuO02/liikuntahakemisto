---
phase: 62-venuepage-konsolidaatio
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/DiagonaalKortti.tsx
autonomous: true
requirements: [VENUEPAGE-03]
must_haves:
  truths:
    - "DiagonaalKortti calls onOpen(paikka) on click when the onOpen prop is provided, with no page navigation"
    - "DiagonaalKortti renders an inert non-navigating overlay when onOpen is absent (preview contexts)"
    - "DiagonaalKortti no longer imports or renders a next/link Link, and no longer accepts onCardClick"
  artifacts:
    - "app/components/DiagonaalKortti.tsx with onOpen?: (paikka: Liikuntapaikka) => void prop replacing onCardClick"
  key_links:
    - "onOpen is the callback Etusivu (Plan 03) wires to setValittu(p) to open PaikkaSheet"
    - "The LEFT info panel and RIGHT photo panel become siblings of the overlay element, not children of it"
---

<objective>
Convert DiagonaalKortti from a navigating card (invisible `<Link href="/paikat/${id}">` overlay) into a context-aware card driven by an `onOpen` callback. When `onOpen` is provided (consumer views), clicking the card fires `onOpen(paikka)`; when it is absent (preview contexts: PreviewModal, LivePreviewPane, admin/[id]), the card renders an inert overlay `<div>` with no navigation and no dead link. This delivers the card-side of VENUEPAGE-03 (D-04/D-05) and removes the last consumer-facing path to the route deleted in Plan 03.

Purpose: Cards open the bottom sheet inline instead of navigating to a separate page.
Output: Updated `DiagonaalKortti.tsx` props interface and overlay element; `Link` import removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-venuepage-konsolidaatio/62-CONTEXT.md
@.planning/phases/62-venuepage-konsolidaatio/62-RESEARCH.md
@.planning/phases/62-venuepage-konsolidaatio/62-PATTERNS.md
@.planning/phases/62-venuepage-konsolidaatio/62-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace onCardClick with onOpen and the Link overlay with a conditional overlay div</name>
  <files>app/components/DiagonaalKortti.tsx</files>
  <read_first>
    - app/components/DiagonaalKortti.tsx (FULL read — critical: the invisible `<Link href={\`/paikat/${paikka.id}\`}>` at line ~91 OPENS the overlay and its matching `</Link>` CLOSES at line ~246, wrapping BOTH the LEFT info panel and the RIGHT photo panel as children. Also read the props interface (lines ~28-40), the destructuring on the `export default function` line (~42), the `next/link` import (line ~5), and the `MapPin` import (line ~6))
    - .planning/phases/62-venuepage-konsolidaatio/62-PATTERNS.md (DiagonaalKortti section — exact interface and overlay snippets)
    - .planning/phases/62-venuepage-konsolidaatio/62-RESEARCH.md (Pitfall 1 — the Link wraps the panels; the replacement div is a SIBLING of the panels, not a wrapper)
  </read_first>
  <action>
    In the `DiagonaalKorttiProps` interface, remove the `onCardClick?: () => void` line and add `onOpen?: (paikka: Liikuntapaikka) => void` in its place (mirror the existing `onShowMap?: (paikka: Liikuntapaikka) => void` signature). Keep `paikka`, `distanceStr`, `isSaved`, `onShowMap`, `onToggleTodo`, `brandColor`, `accentColor` unchanged.

    Update the `export default function DiagonaalKortti({ ... })` destructuring: remove `onCardClick`, add `onOpen` (place it where `onCardClick` was, after `onShowMap`).

    Replace the invisible `<Link>` element. The current `<Link href={\`/paikat/${paikka.id}\`} className="absolute inset-0 block z-10" onClick={() => onCardClick?.()}>` opens the overlay and its `</Link>` closes after the RIGHT photo panel — the LEFT and RIGHT panels are its children. Remove BOTH the opening `<Link ...>` tag and the closing `</Link>` tag, and insert in place of the opening tag a prop-conditional overlay element that does NOT wrap the panels. When `onOpen` is truthy: render a `<div role="button" tabIndex={0} className="absolute inset-0 block z-10 cursor-pointer" onClick={() => onOpen(paikka)} onKeyDown={...}>` where the keydown handler invokes `onOpen(paikka)` on Enter or Space. When `onOpen` is falsy: render `<div className="absolute inset-0 block z-10" />` with no handlers (inert no-op, D-05). After this change the LEFT info panel and the RIGHT photo panel must be direct siblings of this overlay div inside the existing container `<div className="absolute inset-0 rounded-2xl overflow-hidden">`, at the same nesting depth they had inside the old Link — do NOT make them children of the new overlay div, and do NOT change their own markup. Card content must still render (RESEARCH warning sign: if content disappears, the panels were wrongly nested).

    Remove the now-unused `Link` import from `next/link` (the import line). Keep the `MapPin` import — it is still used by the show-on-map button inside the card. Leave the `whileHover`/`whileTap` scale animations and all other markup unchanged.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes without TypeScript errors (no unused `Link` import, no unknown-prop usage)
    - Source assertion: `grep -c "next/link" app/components/DiagonaalKortti.tsx` returns 0
    - Source assertion: `grep -c "/paikat/" app/components/DiagonaalKortti.tsx` returns 0 (the navigation href is gone)
    - Source assertion: `grep -c "onCardClick" app/components/DiagonaalKortti.tsx` returns 0
    - Source assertion: DiagonaalKortti.tsx contains `onOpen?: (paikka: Liikuntapaikka) => void` and an overlay with `onClick={() => onOpen(paikka)}`
    - Behavior assertion (manual smoke at end of wave): in a consumer view the card body still renders fully (LEFT + RIGHT panels visible); a card given no onOpen renders but does nothing on click
  </acceptance_criteria>
  <done>DiagonaalKortti exposes `onOpen`, fires it on click, renders an inert overlay without `onOpen`, no longer imports `Link`, and the build passes with panels rendering correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user interaction -> card callback | Card click/keydown invokes the parent-provided `onOpen(paikka)` callback |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-03 | Elevation of Privilege | onOpen callback | low | accept | `onOpen` is a parent-supplied function receiving the card's own `paikka` object; no privileged action or data crossing; preview contexts pass no callback (inert) |
| T-62-04 | Tampering | removed Link overlay | low | accept | Removing the `<Link>` eliminates a navigation surface; the inert `<div>` performs no action |
| T-62-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed in this plan or phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npm run build` passes.
- No `next/link`, no `/paikat/`, no `onCardClick` remain in DiagonaalKortti.tsx.
- Card renders fully in consumer view; inert in preview contexts.
</verification>

<success_criteria>
- DiagonaalKortti delivers the card-side of VENUEPAGE-03: click opens the sheet via `onOpen` (when wired by Plan 03), inert otherwise.
- The last consumer-facing `/paikat/` navigation path is removed.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- `app/components/DiagonaalKortti.tsx`: new prop `onOpen?: (paikka: Liikuntapaikka) => void` (replaces removed `onCardClick?: () => void`); conditional overlay element (`role="button"` interactive div when `onOpen` present, inert `<div>` otherwise); `next/link` `Link` import removed
</artifacts_produced>

<output>
Create `.planning/phases/62-venuepage-konsolidaatio/62-02-SUMMARY.md` when done.
</output>
