---
phase: "15"
plan: "04"
type: execute
wave: 3
depends_on:
  - "15-02"
  - "15-03"
files_modified:
  - app/paikat/[id]/page.tsx
autonomous: false
requirements:
  - REVIEW-01
  - REVIEW-02
  - REVIEW-03
  - REVIEW-04

must_haves:
  truths:
    - "app/paikat/[id]/page.tsx imports `ReviewSection` from `@/app/components/ReviewSection` and renders it after the existing `.glass` content card div, inside the page wrapper (D-01)"
    - "page.tsx fetches reviews via `supabase.from('reviews').select('id, rating, teksti, is_anonymous, reviewer_name, created_at').eq('paikka_id', id).order('created_at', { ascending: false })` (D-06)"
    - "page.tsx computes `avgRating` in JS via `computeAvgRating` from `@/lib/reviewUtils` and passes `paikkaId={id}`, `initialReviews={reviewList}`, `avgRating={avgRating}`, `reviewCount={reviewList.length}` to ReviewSection (REVIEW-04)"
    - "Anonymous user visiting `/paikat/[id]` sees existing reviews and the locked form with the 'Kirjaudu arvostellaksesi' CTA (REVIEW-01, REVIEW-02 — verified manually)"
    - "Logged-in user can submit a 1–5 star review with optional text, anonymous toggle, visit date, and crowd rating; the star average updates without a manual reload (router.refresh round-trip works) (REVIEW-01..04 — verified manually)"
    - "Submitting a second review for the same paikka updates the existing row (D-03); the venue card never shows two reviews from the same user"
    - "npx vitest run, npx tsc --noEmit, and a clean dev-server start are all green before the human verification step runs"
  artifacts:
    - path: "app/paikat/[id]/page.tsx"
      provides: "Integration of ReviewSection into the venue profile page"
      contains: "import ReviewSection from '@/app/components/ReviewSection'"
  key_links:
    - from: "app/paikat/[id]/page.tsx"
      to: "app/components/ReviewSection.tsx"
      via: "import + JSX render"
      pattern: "<ReviewSection"
    - from: "app/paikat/[id]/page.tsx"
      to: "lib/reviewUtils.ts"
      via: "import"
      pattern: "from '@/lib/reviewUtils'"
    - from: "app/paikat/[id]/page.tsx"
      to: "reviews table (live Supabase)"
      via: "createServerSupabase(cookies()).from('reviews').select(...)"
      pattern: "from('reviews')"
---

<objective>
Wire the reviews feature into the venue profile page and run the end-to-end human verification that closes Phase 15. After this plan, REVIEW-01 through REVIEW-04 are observable in the running application.

Purpose: Plans 02 and 03 produce components that have not yet been mounted on a real page. Plan 04 performs the small server-side query, computes the average via the Plan 01 helper, and slots `<ReviewSection />` into `app/paikat/[id]/page.tsx`. The blocking checkpoint then walks the user through the full feature against the live Supabase project so we catch RLS, router.refresh, and copy issues before `/gsd:verify-work 15`.

Output: Modified `app/paikat/[id]/page.tsx` with the reviews query + ReviewSection render, plus a signed-off human verification covering all 5 ROADMAP success criteria for Phase 15.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-arvostelut/15-CONTEXT.md
@.planning/phases/15-arvostelut/15-RESEARCH.md
@.planning/phases/15-arvostelut/15-PATTERNS.md
@.planning/phases/15-arvostelut/15-UI-SPEC.md
@.planning/phases/15-arvostelut/15-01-SUMMARY.md
@.planning/phases/15-arvostelut/15-02-SUMMARY.md
@.planning/phases/15-arvostelut/15-03-SUMMARY.md

<interfaces>
<!-- ReviewSection contract from Plan 02. -->
From app/components/ReviewSection.tsx:
  export type ReviewRow = { id: number; rating: number; teksti: string; is_anonymous: boolean; reviewer_name: string | null; created_at: string }
  export default function ReviewSection(props: { paikkaId: number; initialReviews: ReviewRow[]; avgRating: number | null; reviewCount: number }): JSX.Element

<!-- Aggregate helper from Plan 01. -->
From lib/reviewUtils.ts:
  export function computeAvgRating(ratings: number[]): number | null

<!-- Existing server supabase client. -->
From lib/supabaseSSR.ts:
  export function createServerSupabase(cookieStore: ReadonlyRequestCookies): SupabaseClient

<!-- Existing page structure (anchor for the insertion point). -->
From app/paikat/[id]/page.tsx:
  // ...existing imports...
  // const { data: paikka } = await supabase.from('liikuntapaikat')...
  // return ( <div className="min-h-screen bg-white"> <NavPill /> <section>hero</section>
  //   <div className="max-w-2xl mx-auto px-4 pt-6 pb-10"> <div className="glass rounded-2xl overflow-hidden"> ...content card... </div>
  //   {/* ReviewSection slots here */}
  //   </div>
  // )
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire ReviewSection into app/paikat/[id]/page.tsx</name>
  <files>app/paikat/[id]/page.tsx</files>
  <read_first>
    - app/paikat/[id]/page.tsx (full file — locate the closing `</div>` of the `.glass rounded-2xl overflow-hidden` content card and the outer `max-w-2xl mx-auto px-4 pt-6 pb-10` wrapper)
    - app/components/ReviewSection.tsx (signature + ReviewRow type)
    - lib/reviewUtils.ts (computeAvgRating signature)
    - .planning/phases/15-arvostelut/15-PATTERNS.md `app/paikat/[id]/page.tsx (modified)` section (exact insertion guidance)
    - .planning/phases/15-arvostelut/15-RESEARCH.md Pattern 2 + Common Pitfalls 1, 6
    - .planning/phases/15-arvostelut/15-UI-SPEC.md "Integration Target" section
  </read_first>
  <behavior>
    - After the existing `paikka` fetch and before the `return (`, the file declares a second supabase query selecting only the public review columns (id, rating, teksti, is_anonymous, reviewer_name, created_at) for the current `id`, ordered by `created_at DESC`, results stored in `reviewList = data ?? []`
    - `avgRating` is computed by `computeAvgRating(reviewList.map(r => r.rating))` — never by an inline reduce (the helper is the single source of truth)
    - `<ReviewSection paikkaId={id} initialReviews={reviewList} avgRating={avgRating} reviewCount={reviewList.length} />` is rendered AFTER the closing `</div>` of the existing `.glass rounded-2xl overflow-hidden` content card. ReviewSection owns its own `max-w-2xl mx-auto px-4 pb-10` wrapper (per Plan 02), so the JSX placement does NOT add an additional wrapper.
    - All existing routing, hero, content card, NavPill, and notFound() behavior remains unchanged — no edits outside the imports block, the `paikka` query area, and the JSX insertion point.
  </behavior>
  <action>
    Modify `app/paikat/[id]/page.tsx` in three localized edits — make no other changes.

    Edit 1 — imports block, in the same group as the existing `@/app/components/*` imports:
    - Add: `import ReviewSection from '@/app/components/ReviewSection'`
    - Add: `import { computeAvgRating } from '@/lib/reviewUtils'`

    Edit 2 — between the `if (!paikka) notFound()` line and the start of the existing `const laji = …` block (or wherever the post-fetch derived data block begins), insert the reviews query and aggregate:
    - `const { data: reviewsData } = await supabase.from('reviews').select('id, rating, teksti, is_anonymous, reviewer_name, created_at').eq('paikka_id', id).order('created_at', { ascending: false })`
    - `const reviewList = reviewsData ?? []`
    - `const avgRating = computeAvgRating(reviewList.map(r => r.rating))`

    Edit 3 — JSX insertion. Locate the closing `</div>` of the existing inner content card `<div className="glass rounded-2xl overflow-hidden">` AND the closing `</div>` of the outer wrapper `<div className="max-w-2xl mx-auto px-4 pt-6 pb-10">`. The ReviewSection renders AFTER the inner content card closes but BEFORE the outer wrapper closes — OR (cleaner) AFTER the outer wrapper closes but BEFORE the top-level `<div className="min-h-screen bg-white">` closes. Either placement is acceptable because ReviewSection owns its own `max-w-2xl mx-auto px-4 pb-10` wrapper (Plan 02). Choose the placement that minimises diff churn — outside the existing wrapper is preferred.

    The exact JSX to insert:
    `<ReviewSection paikkaId={id} initialReviews={reviewList} avgRating={avgRating} reviewCount={reviewList.length} />`

    DO NOT add a new `<section>` element. DO NOT add Tailwind padding/margin classes around the component (ReviewSection owns its layout). DO NOT change the existing `paikka` query, NavPill placement, hero markup, or notFound() handling. DO NOT re-fetch reviews client-side (Pitfall 1) — this is the only place reviews are fetched and the data flows down as props.

    The reviews query must NOT select `user_id` — that column stays server-side only (T-15-02 hardening).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('app/paikat/[id]/page.tsx','utf8');const must=[`import ReviewSection from '@/app/components/ReviewSection'`,`import { computeAvgRating } from '@/lib/reviewUtils'`,`from('reviews')`,'reviewer_name','order(\\'created_at\\'','ascending: false','computeAvgRating','<ReviewSection','paikkaId={id}','initialReviews={reviewList}','avgRating={avgRating}','reviewCount={reviewList.length}'];const missing=must.filter(m=>!s.includes(m));if(missing.length){console.error('MISSING:',missing);process.exit(1)}if(/select\\([^)]*user_id/.test(s.split('reviews')[1]||'')){console.error('reviews query selected user_id — must be omitted (T-15-02)');process.exit(1)}console.log('OK')" && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `app/paikat/[id]/page.tsx` contains both new imports verbatim
    - The reviews query uses `.from('reviews')`, selects exactly the 6 public columns (id, rating, teksti, is_anonymous, reviewer_name, created_at) and NOT `user_id`, and orders by `created_at` descending
    - `computeAvgRating` from `@/lib/reviewUtils` is called with the array of ratings — no inline reduce
    - The JSX renders `<ReviewSection paikkaId={id} initialReviews={reviewList} avgRating={avgRating} reviewCount={reviewList.length} />` with no surrounding wrapper added
    - The verify script exits 0 and prints `OK`
    - `npx tsc --noEmit` exits 0 (clean type-check across StarPicker, ReviewSection, ReviewForm, page.tsx)
    - `npx vitest run` exits 0 (no regression from Plan 01)
  </acceptance_criteria>
  <done>Integration edits applied; page.tsx type-checks; full vitest suite green; dev server can serve `/paikat/[id]` without runtime errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 15 end-to-end</name>
  <what-built>
    - `reviews` table + RLS in the live Supabase project (Plan 01)
    - `lib/reviewUtils.ts` helpers + vitest unit suite (Plan 01)
    - `app/components/StarPicker.tsx` controlled star input (Plan 02)
    - `app/components/ReviewSection.tsx` shell with star average, list, and "Näytä kaikki" expansion (Plan 02)
    - `app/components/ReviewForm.tsx` auth-gated form with INSERT/UPDATE upsert + `router.refresh()` (Plan 03)
    - `app/paikat/[id]/page.tsx` server-side reviews fetch + ReviewSection mount (Plan 04 Task 1)
  </what-built>
  <how-to-verify>
    Start the dev server (`npm run dev`) and open `http://localhost:3000` in a browser. Then walk through these checks **in order** — each maps to a ROADMAP success criterion.

    **A. Logged-OUT path (REVIEW-04 + D-02):**
    1. Make sure you are signed out (open NavPill → if "Kirjaudu ulos" appears, click it).
    2. Open any venue: `http://localhost:3000/paikat/1` (or pick a real id from the homepage).
    3. Scroll past the existing info card. You should see a NEW `.glass` card titled by the star-average line. If the venue has no reviews yet, you should see exactly `Ei vielä arvosteluja` (D-05 zero-state).
    4. Below the average, the form area must show greyed-out fields with a visible `Kirjaudu arvostellaksesi` button centered on top. The fields must NOT be interactive (try clicking the textarea — nothing should happen).
    5. Click the CTA → confirm the same AuthModal that HeartButton uses opens.
    6. Dismiss without logging in. Confirm the reviews section is still visible (form locked, not hidden).

    **B. Logged-IN happy path (REVIEW-01, REVIEW-02, REVIEW-03):**
    7. Sign in via the modal (use the existing email you already use for testing).
    8. Reload `/paikat/<same id>`.
    9. The form is now interactive. Fields visible: `TÄHTIARVOSANA` (5 stars), `KOMMENTTI` (textarea), `KÄYNTIPÄIVÄ` (date input with today as max), `RUUHKATILANNE` (three pills `Hiljaista` / `Sopivasti` / `Ruuhkaista`), an anonymity toggle (`Näytä nimeni` / `Anonyymi` — `Näytä nimeni` is the default active pill).
    10. Hover the 4th star — stars 1–4 should turn amber **without** clicking. Move the mouse out — they revert to whatever was selected (0 on first visit, so all empty).
    11. Click the 4th star, type a short comment, set today's date, tap `Sopivasti`, leave `Näytä nimeni` active, click `Jätä arvostelu`.
    12. Confirm: green `Arvostelu tallennettu` flashes for ~2.5s and the star average above flips to show your rating without you reloading the page (this proves `router.refresh()` works).
    13. The form area now shows the read-only "your existing review" view with a `Muokkaa arvostelu` outlined button. The author label of your review should display **the email prefix** (everything before `@`), NEVER your full email or a UUID.

    **C. UPDATE path (REVIEW-01 + D-03):**
    14. Click `Muokkaa arvostelu`. The full form returns, pre-filled with your previous answers, and the submit label is now `Tallenna muutokset`.
    15. Change the star rating to 5, toggle to `Anonyymi`, click `Tallenna muutokset`.
    16. Confirm a second row was NOT created — the star-average count remains the same as before this edit; your review now displays `Anonyymi` as the author (T-15-02 mitigation visible to the user).
    17. Try opening the Supabase dashboard → Table editor → `reviews`. Confirm there is exactly one row for this `(user_id, paikka_id)` pair, `is_anonymous=true`, `rating=5`, and `reviewer_name` contains the email prefix (not the full email, not the user_id).

    **D. List expansion (REVIEW-04 + D-07):**
    18. (Optional — needs >5 rows.) If the venue has more than five reviews, confirm only the first 5 render and a `Näytä kaikki (N arvostelua)` button appears. Click it → the rest of the reviews appear; the button disappears.

    **E. Future date guard (T-15-04):**
    19. Click the date input. Confirm the browser-native picker does NOT allow a date later than today (the `max` attribute is honoured).

    Report any visible bug, copy mismatch, console error, or DB row anomaly exactly as observed.
  </how-to-verify>
  <resume-signal>Type "approved" when all checklist items pass, or paste the failing step + the exact error/console message.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server component → Supabase `reviews` SELECT | Public read; only the 6 non-PII columns are queried (user_id excluded) — T-15-02 reinforced at the SELECT layer |
| Browser → ReviewForm submit | Re-tested end-to-end in the manual checklist; depends on the Plan 01 RLS and Plan 03 upsert logic |
| Manual verification environment | Trusted operator on localhost; no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Information Disclosure | page.tsx reviews SELECT | mitigate | The query string lists only public columns; the verify script blocks `user_id` from being added to the SELECT list |
| T-15-06 | Repudiation | Auth-state desync between server render and client | accept | Server component renders public review list (no auth required); ReviewForm runs `subscribeToAuthUser` and re-queries existing review per user — covered in manual step B |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages added |
</threat_model>

<verification>
- Task 1 verify script exits 0 and prints `OK`
- `npx tsc --noEmit` exits 0 across the project
- `npx vitest run` exits 0
- Human signs off Task 2 with "approved", confirming all sections A–E pass
<human-check>
All A–E checklist items in Task 2 confirmed by user.
</human-check>
</verification>

<success_criteria>
- `app/paikat/[id]/page.tsx` renders `<ReviewSection />` with server-fetched reviews and helper-computed average
- All 5 ROADMAP success criteria for Phase 15 are observable in the running app:
  1. Logged-in user can leave a 1–5 star review with text; second review to the same venue updates instead of duplicating
  2. Reviewer can choose between named (email prefix) and Anonyymi display
  3. Review captures visit date and crowd rating (DB has the columns; UI accepts the inputs)
  4. Venue profile shows all reviews and the calculated star average
  5. Logged-out user sees existing reviews but the form is locked behind the `Kirjaudu arvostellaksesi` CTA
- Phase 15 is ready for `/gsd:verify-work 15`
</success_criteria>

<output>
Create `.planning/phases/15-arvostelut/15-04-SUMMARY.md` when done
</output>
