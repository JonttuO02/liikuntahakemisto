---
phase: "15"
plan: "02"
type: execute
wave: 1
depends_on:
  - "15-01"
files_modified:
  - app/components/StarPicker.tsx
  - app/components/ReviewSection.tsx
autonomous: true
requirements:
  - REVIEW-04

must_haves:
  truths:
    - "StarPicker renders 5 motion.button stars in a horizontal row; hovering star N tints stars 1..N text-amber-400 and leaving the container reverts to the selected `value` (D-04)"
    - "StarPicker is a controlled component: it never owns `value`; clicking star N invokes `onChange(n)`"
    - "ReviewSection is a 'use client' component receiving `paikkaId`, `initialReviews`, `avgRating`, `reviewCount` props and renders a single `.glass rounded-2xl p-6 sm:p-8` card under `max-w-2xl mx-auto px-4 pb-10` (D-01)"
    - "StarAverage renders '★★★★☆ 4.2 (N arvostelua)' format when `reviewCount > 0` and 'Ei vielä arvosteluja' when `reviewCount === 0` (D-05, REVIEW-04)"
    - "Reviews are shown newest-first; only the first 5 render until `showAll` is true; if `initialReviews.length > 5` a button labelled 'Näytä kaikki (N arvostelua)' appears and reveals the rest (D-06, D-07)"
    - "Each ReviewCard renders ONLY: filled stars (amber-400) + author name resolved via resolveDisplayName(is_anonymous, reviewer_name) + review text — visit_date and crowd_rating are NOT rendered (D-08, T-15-02)"
    - "ReviewSection renders a `<ReviewForm paikkaId={paikkaId} />` slot between StarAverage and the list — ReviewForm itself is implemented in Plan 03 and consumed unchanged here"
  artifacts:
    - path: "app/components/StarPicker.tsx"
      provides: "Controlled 5-star input with hover preview and whileTap"
      exports: ["default"]
    - path: "app/components/ReviewSection.tsx"
      provides: "ReviewSection shell + StarAverage + ReviewCard list + 'Näytä kaikki' toggle"
      exports: ["default"]
  key_links:
    - from: "app/components/ReviewSection.tsx"
      to: "lib/reviewUtils.ts"
      via: "import"
      pattern: "from '@/lib/reviewUtils'"
    - from: "app/components/ReviewSection.tsx"
      to: "app/components/ReviewForm"
      via: "import (Plan 03 file)"
      pattern: "from './ReviewForm'"
    - from: "app/paikat/[id]/page.tsx (Plan 04)"
      to: "app/components/ReviewSection.tsx"
      via: "import + JSX render below content card"
      pattern: "from '@/app/components/ReviewSection'"
---

<objective>
Build the visual scaffold for the reviews feature: the reusable `StarPicker` (consumed by `ReviewForm` in Plan 03 and as a read-only star renderer here) and the `ReviewSection` shell that the venue profile page (Plan 04) will slot in below the existing content card.

Purpose: Decouples the design-system rendering work from the auth-machine work in Plan 03. After this plan the venue page can already display reviews and average from server-fetched data — only the submission form remains to be wired.

Output: Two new client components — `StarPicker.tsx` and `ReviewSection.tsx` — both rendering correctly against mock props and ready for the `ReviewForm` import that Plan 03 produces.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-arvostelut/15-CONTEXT.md
@.planning/phases/15-arvostelut/15-RESEARCH.md
@.planning/phases/15-arvostelut/15-PATTERNS.md
@.planning/phases/15-arvostelut/15-UI-SPEC.md
@.planning/phases/15-arvostelut/15-01-SUMMARY.md

<interfaces>
<!-- Helpers provided by Plan 01 (lib/reviewUtils.ts). -->
From lib/reviewUtils.ts:
  export function resolveDisplayName(isAnonymous: boolean, reviewerName: string | null | undefined): string
  export function computeAvgRating(ratings: number[]): number | null

<!-- Filter pill toggle pattern reused for "Näytä kaikki" / inactive pill style. -->
From app/components/Etusivu.tsx (filter pill, ~line 786):
  <motion.button whileTap={{ scale: 0.96 }}
    className={active ? 'h-8 px-3 rounded-full text-xs font-bold bg-[#111111] text-white' : 'h-8 px-3 rounded-full text-xs font-bold glass text-[rgba(17,17,17,0.6)] hover:text-[#111111]'}>

<!-- whileTap animation contract per CLAUDE.md. -->
StarPicker whileTap: { scale: 0.95, transition: { duration: 0.12, ease: 'easeOut' } }

<!-- ReviewForm import the integration consumes — file is created by Plan 03. -->
From app/components/ReviewForm.tsx (Plan 03):
  export default function ReviewForm(props: { paikkaId: number }): JSX.Element

<!-- Shape of each review row coming from page.tsx (Plan 04 server query). -->
type ReviewRow = {
  id: number
  rating: number
  teksti: string
  is_anonymous: boolean
  reviewer_name: string | null
  created_at: string
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StarPicker.tsx (controlled 5-star input with hover preview)</name>
  <files>app/components/StarPicker.tsx</files>
  <read_first>
    - app/components/HeartButton.tsx (motion.button + whileTap structure, 'use client' header)
    - app/components/Etusivu.tsx lines 780–800 (filter pill whileTap reference)
    - CLAUDE.md "Animation Principles" + "Card hover" sections (whileTap rules, no spring, no y-movement)
    - .planning/phases/15-arvostelut/15-UI-SPEC.md "Sub-Widget: StarPicker" + "Accessibility Contract"
    - .planning/phases/15-arvostelut/15-PATTERNS.md "app/components/StarPicker.tsx" section
  </read_first>
  <behavior>
    - Component is named-default-exported and accepts `{ value: number; onChange: (n: number) => void }`
    - On first render with value=0, all five stars render with class `text-[rgba(17,17,17,0.2)]` (empty)
    - With value=3 and no hover, stars 1..3 render with `text-amber-400` and stars 4..5 render with `text-[rgba(17,17,17,0.2)]`
    - On `mouseenter` of star N (or focus on touch), stars 1..N render amber-400 regardless of `value`
    - On `mouseleave` of the container, hover preview clears and rendering reverts to `value`
    - Clicking star N calls `onChange(n)` with the integer N (1..5); component does not store its own value
    - Each button declares `aria-label="${n} tähteä"` and the container declares `role="group" aria-label="Tähtiarvosana"`
    - Each `motion.button` uses `whileTap={{ scale: 0.95, transition: { duration: 0.12, ease: 'easeOut' } }}`
    - Star glyph is the literal Unicode `★` character (matches D-05 display format)
  </behavior>
  <action>
    Create `app/components/StarPicker.tsx`:
    - Top line: `'use client'`
    - Imports: `useState` from 'react'; `motion` from 'framer-motion'. No supabase, no lucide-react (Unicode ★ is the glyph per RESEARCH.md Pattern 6).
    - Local constant: `const STARS = [1, 2, 3, 4, 5] as const`
    - Component signature: `export default function StarPicker({ value, onChange }: { value: number; onChange: (n: number) => void })`
    - Internal state: `const [hovered, setHovered] = useState(0)`; `const display = hovered || value`
    - Render: container `<div className="flex gap-1" role="group" aria-label="Tähtiarvosana" onMouseLeave={() => setHovered(0)}>` wrapping a `STARS.map(n => <motion.button ...>)`.
    - Each `motion.button`:
      - `key={n}`, `type="button"`, `aria-label={`${n} tähteä`}`
      - `whileTap={{ scale: 0.95, transition: { duration: 0.12, ease: 'easeOut' } }}`
      - `onMouseEnter={() => setHovered(n)}`, `onFocus={() => setHovered(n)}`, `onClick={() => onChange(n)}`
      - className: `"text-2xl leading-none focus:outline-none"`
      - Children: a single `<span>` with className `n <= display ? 'text-amber-400' : 'text-[rgba(17,17,17,0.2)]'` and the text `★`.
    - No `Heart`/lucide icons; no spring physics; no y-translation; no layout animation.

    Do NOT introduce a 5th typography size — `text-2xl` here is an icon-size utility on a glyph, documented in 15-UI-SPEC.md "Icon-sizing note".
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('app/components/StarPicker.tsx','utf8');const must=[`'use client'`,'export default function StarPicker','value: number','onChange','useState','motion.button','whileTap','scale: 0.95','onMouseEnter','onMouseLeave','role=\"group\"','aria-label=\"Tähtiarvosana\"','text-amber-400','text-[rgba(17,17,17,0.2)]','★','text-2xl'];const missing=must.filter(m=>!s.includes(m));if(missing.length){console.error('MISSING:',missing);process.exit(1)}if(s.includes('lucide-react')){console.error('UNEXPECTED lucide-react import');process.exit(1)}console.log('OK')" && npx tsc --noEmit 2>&1 | findstr /R "StarPicker.tsx" & exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/StarPicker.tsx` exists and the first non-comment line is `'use client'`
    - File default-exports a function named `StarPicker` with parameter `{ value, onChange }`
    - File imports `useState` from React and `motion` from `framer-motion`; no lucide-react import
    - File contains `whileTap` with `scale: 0.95` and `duration: 0.12`
    - File contains `role="group"` and `aria-label="Tähtiarvosana"` on the container
    - File contains the Unicode `★` character literal
    - File contains both `text-amber-400` and `text-[rgba(17,17,17,0.2)]` class strings
    - `npx tsc --noEmit` produces no errors mentioning `app/components/StarPicker.tsx`
  </acceptance_criteria>
  <done>StarPicker component created, satisfies the verify script, compiles cleanly under TypeScript strict mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ReviewSection.tsx (glass card shell + StarAverage + ReviewCard list + "Näytä kaikki")</name>
  <files>app/components/ReviewSection.tsx</files>
  <read_first>
    - app/paikat/[id]/page.tsx (existing content card surrounding container — `max-w-2xl mx-auto px-4 pt-6 pb-10`, `.glass rounded-2xl overflow-hidden`, p-6 sm:p-8 inner)
    - app/profiili/ProfiiliClient.tsx (label-caps and muted-text class strings; underline link style)
    - CLAUDE.md "Card Structure (PaikkaKortti)" + "Color System" + "Typography" sections
    - .planning/phases/15-arvostelut/15-UI-SPEC.md (Component Inventory, Sub-Widget: StarAverage, ReviewCard, "Näytä kaikki" button)
    - .planning/phases/15-arvostelut/15-PATTERNS.md `app/components/ReviewSection.tsx` section
    - .planning/phases/15-arvostelut/15-RESEARCH.md Pattern 8 (StarAverage) + "Rendering the ReviewSection in page.tsx"
    - lib/reviewUtils.ts (signatures of resolveDisplayName, computeAvgRating from Plan 01)
  </read_first>
  <behavior>
    - Component is named-default-exported as `ReviewSection` and accepts props `{ paikkaId: number; initialReviews: ReviewRow[]; avgRating: number | null; reviewCount: number }`
    - `ReviewRow` type is declared in this file (or re-exported alongside) with the exact shape listed in `<interfaces>` (id, rating, teksti, is_anonymous, reviewer_name, created_at)
    - Renders `<div className="max-w-2xl mx-auto px-4 pb-10"><div className="glass rounded-2xl p-6 sm:p-8 flex flex-col gap-5">…</div></div>` (D-01 — new card below content card)
    - First child: a `StarAverage` inline section showing `★★★★☆ 4.2 (N arvostelua)` when `reviewCount > 0`; when `reviewCount === 0` renders `<p className="text-sm text-[rgba(17,17,17,0.45)]">Ei vielä arvosteluja</p>` (D-05)
    - Numeric average is rounded to 1 decimal via `(Math.round(avgRating * 10) / 10).toFixed(1)`; the filled-star count is `Math.round(avgRating)`; the empty-star count is `5 - filled`
    - Second child: a slot for `<ReviewForm paikkaId={paikkaId} />` (imported from `./ReviewForm` — file is produced by Plan 03; Plan 02 only ensures the import path is correct)
    - Third child: list of reviews — `const displayed = showAll ? initialReviews : initialReviews.slice(0, 5)` (D-07); each rendered by `ReviewCard` (defined in this file)
    - "Näytä kaikki" button: rendered only when `!showAll && initialReviews.length > 5`; label `Näytä kaikki (${initialReviews.length} arvostelua)`; click sets `showAll=true`; styled as a self-start text link `text-sm font-bold text-[#111111] underline underline-offset-2`
    - `ReviewCard` is an internal component (no separate file) accepting `{ review: ReviewRow }`. It renders `flex flex-col gap-2 py-3 border-b border-[rgba(0,0,0,0.07)]` (last child should not have a border — use `last:border-0`). Inside: a row with filled `★` glyphs colored `text-amber-400 text-sm` repeated `review.rating` times, followed by `<span className="font-bold text-sm text-[#111111]">{resolveDisplayName(review.is_anonymous, review.reviewer_name)}</span>`; below it a paragraph `<p className="text-sm text-[rgba(17,17,17,0.65)] leading-relaxed">{review.teksti}</p>` rendered only when `review.teksti.trim() !== ''`
    - Visit date and crowd rating MUST NOT appear in ReviewCard (D-08, scope guard)
  </behavior>
  <action>
    Create `app/components/ReviewSection.tsx`:
    - First line `'use client'`.
    - Imports:
      - `import { useState } from 'react'`
      - `import { resolveDisplayName } from '@/lib/reviewUtils'`
      - `import ReviewForm from './ReviewForm'` (file is provided by Plan 03 — type-check will only succeed after Plan 03 lands; this is intentional — wave 2 depends on Plan 01 only for typing, integration verification happens in Plan 04)
    - Declare and export type alias:
      `export type ReviewRow = { id: number; rating: number; teksti: string; is_anonymous: boolean; reviewer_name: string | null; created_at: string }`
    - Declare default export `ReviewSection` with the props signature above.
    - Internal state: `const [showAll, setShowAll] = useState(false)`.
    - Implement an inline `StarAverage` block (no separate export) using the rounding rule above; for the filled/empty star strings use `'★'.repeat(filled) + '☆'.repeat(5 - filled)` rendered inside `<span className="text-amber-400 tracking-tight">`, followed by `<span className="font-bold text-sm text-[#111111]">{rounded}</span>` and `<span className="text-sm text-[rgba(17,17,17,0.45)]">({reviewCount} arvostelua)</span>`. Wrap in `<div className="flex items-center gap-2">`.
    - Slot the `<ReviewForm paikkaId={paikkaId} />`.
    - Render the list: `<div className="flex flex-col">{displayed.map(r => <ReviewCard key={r.id} review={r} />)}{!showAll && initialReviews.length > 5 && (<button onClick={() => setShowAll(true)} className="mt-4 text-sm font-bold text-[#111111] underline underline-offset-2 self-start">{`Näytä kaikki (${initialReviews.length} arvostelua)`}</button>)}</div>`.
    - Define `function ReviewCard({ review }: { review: ReviewRow })` inside the same file. Implementation per the behavior contract. Do NOT render visit_date or crowd_rating.
    - Use `last:border-0` (Tailwind v3 native pseudo) on the ReviewCard root to remove the bottom border on the final row.
    - DO NOT import or render anything that displays `review.user_id` — that field is not even in `ReviewRow` (T-15-02 enforcement).
    - DO NOT introduce semibold (font-weight 600); DO NOT add a 5th font size; DO NOT animate y/height.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('app/components/ReviewSection.tsx','utf8');const must=[`'use client'`,'export default function ReviewSection','paikkaId','initialReviews','avgRating','reviewCount','useState','resolveDisplayName','ReviewForm','glass rounded-2xl','max-w-2xl mx-auto px-4 pb-10','flex flex-col gap-5','Ei vielä arvosteluja','arvostelua','Näytä kaikki','text-amber-400','text-[rgba(17,17,17,0.45)]','★','☆','export type ReviewRow'];const missing=must.filter(m=>!s.includes(m));if(missing.length){console.error('MISSING:',missing);process.exit(1)}const banned=['visit_date','crowd_rating','user_id','font-semibold'];const found=banned.filter(b=>s.includes(b));if(found.length){console.error('BANNED tokens present:',found);process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/ReviewSection.tsx` exists and first line is `'use client'`
    - File default-exports `ReviewSection` accepting `{ paikkaId, initialReviews, avgRating, reviewCount }`
    - File exports a `ReviewRow` type alias with exactly the 6 fields listed in `<interfaces>` (no `user_id`, no `visit_date`, no `crowd_rating` on the row type)
    - File imports `resolveDisplayName` from `@/lib/reviewUtils` and `ReviewForm` from `./ReviewForm`
    - File contains the glass-card wrapper classes `glass rounded-2xl`, `p-6 sm:p-8`, `flex flex-col gap-5`
    - File contains both the average format ('Näytä kaikki (...) arvostelua' button label and the rounded-decimal numeric block) and the empty-state literal `Ei vielä arvosteluja`
    - File does NOT contain the strings `visit_date`, `crowd_rating`, `user_id`, or `font-semibold` (scope guard for D-08 + design system)
    - The verify command exits 0 and prints `OK`
  </acceptance_criteria>
  <done>ReviewSection renders the glass card with average, ReviewForm slot, and review list per spec; verify script reports OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-rendered review props → ReviewSection client | Props are pre-sanitised by Postgres CHECK constraints; rendered as React text nodes (no `dangerouslySetInnerHTML`) |
| StarPicker user input → onChange callback | Integer 1..5 only; bounded by the 5 buttons; consumer (ReviewForm in Plan 03) re-validates before submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Information Disclosure | ReviewCard author rendering | mitigate | `resolveDisplayName(is_anonymous, reviewer_name)` resolves to 'Anonyymi' when anonymous; `ReviewRow` type omits `user_id` so the field cannot be rendered by mistake |
| T-15-05 | Tampering | Review text rendering — stored XSS | mitigate | Text rendered via React text node (`{review.teksti}`) — never `dangerouslySetInnerHTML`; React auto-escapes |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages added; framer-motion and React are already installed |
</threat_model>

<verification>
- Both verify scripts exit 0
- `npx tsc --noEmit` exits 0 once Plan 03 lands and the `./ReviewForm` import resolves — until then a single missing-module error against `ReviewSection.tsx` is expected and acceptable (Plan 04 verification re-runs the full type-check)
- Manual smoke (optional): render `<ReviewSection paikkaId={1} initialReviews={[]} avgRating={null} reviewCount={0} />` in any sandbox page; confirm the empty-state text appears inside the glass card
</verification>

<success_criteria>
- StarPicker and ReviewSection components exist with the exact contracts described
- No banned tokens appear in ReviewSection (visit_date, crowd_rating, user_id, font-semibold)
- ReviewRow type is exported and consumed by Plan 04 from this file
</success_criteria>

<output>
Create `.planning/phases/15-arvostelut/15-02-SUMMARY.md` when done
</output>
