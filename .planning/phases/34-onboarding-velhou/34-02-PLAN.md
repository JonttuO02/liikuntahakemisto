---
phase: 34-onboarding-velhou
plan: 02
type: execute
wave: 1
depends_on:
  - "34-01"
files_modified:
  - supabase/migrations/20260606000000_onboarding.sql
autonomous: true
requirements:
  - ONBOARD-01
  - ONBOARD-03
  - ONBOARD-04
  - ONBOARD-05
  - ONBOARD-06
  - ONBOARD-07

must_haves:
  truths:
    - "onboarding_draft table exists in Supabase with all required columns and RLS"
    - "business_accounts has onboarding_completed BOOLEAN NOT NULL DEFAULT false column"
    - "Draft data is isolated from liikuntapaikat until Step 6 atomic commit"
  artifacts:
    - path: "supabase/migrations/20260606000000_onboarding.sql"
      provides: "onboarding_draft table + onboarding_completed ALTER TABLE + RLS policies"
      contains: "CREATE TABLE IF NOT EXISTS onboarding_draft"
  key_links:
    - from: "onboarding_draft"
      to: "business_accounts"
      via: "business_account_id UUID REFERENCES business_accounts(user_id)"
      pattern: "REFERENCES business_accounts"
    - from: "onboarding_draft"
      to: "liikuntapaikat"
      via: "paikka_id BIGINT REFERENCES liikuntapaikat(id)"
      pattern: "REFERENCES liikuntapaikat"
---

<objective>
Create the database migration that adds the onboarding_draft table and onboarding_completed column.

Purpose: The wizard's draft isolation strategy (D-05) requires onboarding_draft as a staging table. The onboarding gate (D-03) requires onboarding_completed boolean on business_accounts. No wizard code can function without this schema in place.

Output:
- supabase/migrations/20260606000000_onboarding.sql — full DDL for onboarding_draft + ALTER business_accounts
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-onboarding-velhou/34-RESEARCH.md
@.planning/phases/34-onboarding-velhou/34-CONTEXT.md

<interfaces>
<!-- Verified existing migration patterns from supabase/migrations/20260605000000_business_accounts.sql -->

From 20260605000000_business_accounts.sql (header + RLS pattern):
```sql
-- CREATE TABLE IF NOT EXISTS business_accounts (
--   user_id         UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
--   company_name    TEXT NOT NULL,
--   approval_status TEXT NOT NULL DEFAULT 'pending',
--   created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
-- );
-- ALTER TABLE business_accounts ENABLE ROW LEVEL SECURITY;
-- CREATE POLICY "Business reads own account" ON business_accounts FOR SELECT USING (auth.uid() = user_id);
-- CREATE POLICY "Business inserts own account" ON business_accounts FOR INSERT WITH CHECK (auth.uid() = user_id);
-- CREATE POLICY "Business updates own account" ON business_accounts FOR UPDATE USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);
```

onboarding_draft schema required (from RESEARCH.md):
- id BIGSERIAL PRIMARY KEY
- business_account_id UUID NOT NULL REFERENCES business_accounts(user_id) ON DELETE CASCADE
- paikka_id BIGINT NOT NULL REFERENCES liikuntapaikat(id) ON DELETE CASCADE
- media_urls JSONB — { logo: string | null, photos: string[] }
- hinnasto JSONB — Array of { kategoria, hinta, lisatieto }
- aukioloajat JSONB — same shape as liikuntapaikat.aukioloajat
- yhteystiedot JSONB — { puhelin, email, website, kuvaus }
- current_step INT NOT NULL DEFAULT 1
- updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
- UNIQUE(business_account_id, paikka_id)

RLS required: SELECT, INSERT, UPDATE, DELETE — all USING (auth.uid() = business_account_id)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create supabase/migrations/20260606000000_onboarding.sql</name>
  <files>supabase/migrations/20260606000000_onboarding.sql</files>
  <read_first>
    - supabase/migrations/20260605000000_business_accounts.sql — header comment convention, RLS triple policy pattern, CREATE TABLE syntax
    - supabase/migrations/20260605000004_published_is_claimed.sql — ALTER TABLE ADD COLUMN IF NOT EXISTS pattern
    - .planning/phases/34-onboarding-velhou/34-RESEARCH.md — "Existing DB Schema" section for onboarding_draft column list and RLS spec
    - .planning/phases/34-onboarding-velhou/34-PATTERNS.md — migration pattern section for header comment and RLS policy names
  </read_first>
  <action>
    Create supabase/migrations/20260606000000_onboarding.sql with this exact structure:

    SECTION 1 — Header comment block (same convention as business_accounts.sql):
    - Description: what the migration does
    - Decision log: D-03, D-05, D-06, D-07, D-11, D-14, D-16
    - Not included: atomic commit logic (Route Handler), Step 6 submit (code)

    SECTION 2 — ALTER TABLE business_accounts:
    ALTER TABLE business_accounts
      ADD COLUMN IF NOT EXISTS onboarding_completed BOOLEAN NOT NULL DEFAULT false;
    (implements D-03)

    SECTION 3 — CREATE TABLE IF NOT EXISTS onboarding_draft with columns:
    - id BIGSERIAL PRIMARY KEY
    - business_account_id UUID NOT NULL REFERENCES business_accounts(user_id) ON DELETE CASCADE
    - paikka_id BIGINT NOT NULL REFERENCES liikuntapaikat(id) ON DELETE CASCADE
    - media_urls JSONB
    - hinnasto JSONB
    - aukioloajat JSONB
    - yhteystiedot JSONB
    - current_step INT NOT NULL DEFAULT 1
    - updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
    - CONSTRAINT onboarding_draft_unique_business_paikka UNIQUE (business_account_id, paikka_id)

    SECTION 4 — Enable RLS:
    ALTER TABLE onboarding_draft ENABLE ROW LEVEL SECURITY;

    SECTION 5 — Four RLS policies (SELECT, INSERT, UPDATE, DELETE — all keyed on business_account_id):
    CREATE POLICY "Business reads own draft"
      ON onboarding_draft FOR SELECT
      USING (auth.uid() = business_account_id);

    CREATE POLICY "Business inserts own draft"
      ON onboarding_draft FOR INSERT
      WITH CHECK (auth.uid() = business_account_id);

    CREATE POLICY "Business updates own draft"
      ON onboarding_draft FOR UPDATE
      USING (auth.uid() = business_account_id)
      WITH CHECK (auth.uid() = business_account_id);

    CREATE POLICY "Business deletes own draft"
      ON onboarding_draft FOR DELETE
      USING (auth.uid() = business_account_id);

    No IF EXISTS guards on policies — these run once in a clean migration.
    File naming follows YYYYMMDDNNNNNN_description.sql pattern (14 digits).
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS onboarding_draft" supabase/migrations/20260606000000_onboarding.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists at supabase/migrations/20260606000000_onboarding.sql
    - Contains "ALTER TABLE business_accounts" and "ADD COLUMN IF NOT EXISTS onboarding_completed BOOLEAN NOT NULL DEFAULT false"
    - Contains "CREATE TABLE IF NOT EXISTS onboarding_draft" with all 9 columns (id, business_account_id, paikka_id, media_urls, hinnasto, aukioloajat, yhteystiedot, current_step, updated_at)
    - Contains UNIQUE constraint on (business_account_id, paikka_id)
    - Contains all four RLS policies: SELECT, INSERT, UPDATE, DELETE
    - All policies use "auth.uid() = business_account_id" predicate
  </acceptance_criteria>
  <done>Migration file is syntactically valid SQL; grep for "CREATE TABLE IF NOT EXISTS onboarding_draft" returns 1; grep for "ALTER TABLE business_accounts" returns 1; four RLS policy CREATE POLICY statements exist</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase RLS → anon client | onboarding_draft rows are readable/writable only by the business that owns them via auth.uid() = business_account_id |
| supabaseAdmin → onboarding_draft | Service role bypasses RLS — used only in server Route Handlers after JWT verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02-01 | Tampering | onboarding_draft RLS | mitigate | SELECT/INSERT/UPDATE/DELETE policies all check auth.uid() = business_account_id — anon user cannot read or modify another business's draft |
| T-34-02-02 | Elevation of Privilege | business_accounts.onboarding_completed | mitigate | Column is false by default; only submit Route Handler (JWT-verified, supabaseAdmin) sets it to true — cannot be set via anon client due to RLS UPDATE policy |
| T-34-02-SC | Tampering | supabase db push (Plan 04) | mitigate | Schema push reviewed before execution; blocking checkpoint in Plan 04 |
</threat_model>

<verification>
grep -c "CREATE TABLE IF NOT EXISTS onboarding_draft" supabase/migrations/20260606000000_onboarding.sql
Expected output: 1

grep -c "onboarding_completed" supabase/migrations/20260606000000_onboarding.sql
Expected output: at least 1
</verification>

<success_criteria>
- Migration file created at supabase/migrations/20260606000000_onboarding.sql
- ALTER TABLE business_accounts adds onboarding_completed BOOLEAN NOT NULL DEFAULT false (per D-03)
- onboarding_draft table has all required columns including UNIQUE(business_account_id, paikka_id)
- RLS enabled with SELECT + INSERT + UPDATE + DELETE policies all scoped to auth.uid() = business_account_id
</success_criteria>

<output>
Create .planning/phases/34-onboarding-velhou/34-02-SUMMARY.md when done
</output>
