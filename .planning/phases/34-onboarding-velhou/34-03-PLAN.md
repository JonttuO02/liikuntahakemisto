---
phase: 34-onboarding-velhou
plan: 03
type: execute
wave: 1
depends_on:
  - "34-01"
files_modified:
  - messages/fi.json
  - messages/en.json
  - app/components/ClaimSearchForm.tsx
  - app/business/page.tsx
autonomous: true
requirements:
  - ONBOARD-01
  - ONBOARD-02

must_haves:
  truths:
    - "All 34 wizard i18n keys exist in fi.json and en.json under Business namespace"
    - "ClaimSearchForm redirects to /business/onboarding (not window.location.reload()) on claim/create success"
    - "business/page.tsx checks onboarding_completed and redirects to /business/onboarding when false"
  artifacts:
    - path: "messages/fi.json"
      provides: "Finnish wizard strings under Business namespace"
      contains: "onboardingTitle"
    - path: "messages/en.json"
      provides: "English wizard strings under Business namespace"
      contains: "onboardingTitle"
    - path: "app/components/ClaimSearchForm.tsx"
      provides: "Updated success redirect logic (D-01)"
    - path: "app/business/page.tsx"
      provides: "onboarding_completed gate + router.push to /business/onboarding (D-03)"
  key_links:
    - from: "app/components/ClaimSearchForm.tsx"
      to: "/business/onboarding"
      via: "router.push on claim/create success"
      pattern: "router.push.*business/onboarding"
    - from: "app/business/page.tsx"
      to: "/business/onboarding"
      via: "onboarding_completed check in useEffect"
      pattern: "onboarding_completed"
---

<objective>
Add all wizard i18n strings (34 new keys) to fi.json and en.json, fix the ClaimSearchForm post-success redirect from window.location.reload() to router.push('/business/onboarding'), and add the onboarding_completed gate to business/page.tsx.

Purpose: This plan delivers the two integration-point fixes (D-01, D-03) and the full i18n vocabulary that all wizard components depend on. Both can be done without the DB schema being live, so this runs in Wave 1 parallel with the migration file plan.

Output:
- messages/fi.json — 34 new keys under Business namespace (wizard strings per UI-SPEC Copywriting Contract)
- messages/en.json — same 34 keys in English
- app/components/ClaimSearchForm.tsx — handleClaim and handleCreate redirect to /business/onboarding
- app/business/page.tsx — onboarding_completed check added to checkLinks() useEffect
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-onboarding-velhou/34-CONTEXT.md
@.planning/phases/34-onboarding-velhou/34-UI-SPEC.md
@.planning/phases/34-onboarding-velhou/34-PATTERNS.md

<interfaces>
<!-- Key code patterns extracted from source files. -->

From messages/fi.json Business namespace (current last key):
```json
"pendingBody": "Odottaa admin-hyväksyntää. Lähetimme vahvistuksen sähköpostiisi."
```
New keys must be appended inside the "Business": { } block after "pendingBody".

From app/components/ClaimSearchForm.tsx — handleClaim success (lines 114–117, current):
```tsx
if (res.ok) {
  window.location.reload()
  return
}
```

From app/components/ClaimSearchForm.tsx — handleCreate success (lines 166–169, current):
```tsx
if (res.ok) {
  window.location.reload()
  return
}
```

router is already imported: import { useRouter } from 'next/navigation'
router is used in the component: const router = useRouter()

create-paikka route returns { ok: true, paikka_id: newPaikkaId } — paikka_id available in res.json().

From app/business/page.tsx — checkLinks useEffect (current, lines 14–32):
```tsx
useEffect(() => {
  async function checkLinks() {
    const supabase = createBrowserSupabase()
    const { data: { user } } = await supabase.auth.getUser()
    if (!user) { setLoading(false); return }
    const { data: links } = await supabase
      .from('business_paikka_links')
      .select('paikka_id, liikuntapaikat(nimi)')
      .eq('business_account_id', user.id)
      .limit(1)
    if (links && links.length > 0) {
      setHasLinks(true)
      ...
    }
    setLoading(false)
  }
  checkLinks()
}, [])
```
useRouter is NOT currently imported in app/business/page.tsx — must be added.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 34 wizard i18n keys to fi.json and en.json</name>
  <files>messages/fi.json, messages/en.json</files>
  <read_first>
    - messages/fi.json — read full file; locate the "Business" namespace closing brace; understand current key count and last key (pendingBody)
    - messages/en.json — same
    - .planning/phases/34-onboarding-velhou/34-UI-SPEC.md — Copywriting Contract table (all 34 keys with Finnish copy and key names)
  </read_first>
  <action>
    In messages/fi.json, inside the "Business": { } object, append the following 34 keys after the existing "pendingBody" key. Preserve valid JSON (trailing comma on pendingBody before new keys, no trailing comma on last new key before closing brace).

    Finnish values (per UI-SPEC Copywriting Contract):
    "onboardingTitle": "Tietojesi täydentäminen",
    "stepPlaceName": "Paikkasi",
    "stepMedia": "Mediat",
    "stepPricing": "Hinnasto",
    "stepHours": "Aukioloajat",
    "stepContact": "Yhteystiedot",
    "stepPreview": "Esikatselu",
    "nextCta": "Seuraava",
    "prevCta": "Edellinen",
    "submitCta": "Lähetä hyväksyttäväksi",
    "submitting": "Lähetetään...",
    "logoDropLabel": "Logo (neliö, max 2 MB)",
    "imagesDropLabel": "Kuvat 1–5 (max 5 MB / kuva)",
    "dropActiveHint": "Pudota tiedosto tähän",
    "uploadingLabel": "Ladataan...",
    "addPriceRowCta": "+ Lisää hintarivi",
    "pricingCategoryDrop": "Kertakäynti",
    "pricingCategoryMonthly": "Kuukausijäsenyys",
    "pricingCategory10x": "10-kerran kortti",
    "pricingCategoryAnnual": "Vuosijäsenyys",
    "pricingHeaderPrice": "Hinta (€)",
    "pricingHeaderNotes": "Lisätieto (vapaaehtoinen)",
    "pricingRowNamePlaceholder": "Hinnan nimi",
    "hoursPrefilledLabel": "Esitäytetty Google Places -datasta",
    "hoursClosed": "Suljettu",
    "hoursToggleLabel": "Auki",
    "contactPhonePlaceholder": "Puhelinnumero",
    "contactEmailPlaceholder": "Sähköposti",
    "contactWebsitePlaceholder": "Website (https://...)",
    "contactDescPlaceholder": "Lyhyt kuvaus palvelustanne (max 300 merkkiä)",
    "contactDescCount": "{n}/300",
    "previewLabelCard": "LISTAKORTTI",
    "previewLabelDiag": "DIAGONAALIKORTTI",
    "previewLabelSheet": "PROFIILISIVU",
    "errorNoPriceRow": "Lisää vähintään yksi hintarivi ennen jatkamista.",
    "errorUploadFailed": "Tiedoston lataus epäonnistui. Yritä uudelleen.",
    "errorFileTooLarge": "Tiedosto on liian suuri (max {max} MB).",
    "errorSubmitFailed": "Lähetys epäonnistui. Tarkista tiedot ja yritä uudelleen."

    Note: fi.json already has "errorGeneric" — do NOT duplicate it.

    In messages/en.json, append the same keys with English translations:
    "onboardingTitle": "Complete your information",
    "stepPlaceName": "Your venue",
    "stepMedia": "Media",
    "stepPricing": "Pricing",
    "stepHours": "Opening hours",
    "stepContact": "Contact details",
    "stepPreview": "Preview",
    "nextCta": "Next",
    "prevCta": "Previous",
    "submitCta": "Submit for approval",
    "submitting": "Submitting...",
    "logoDropLabel": "Logo (square, max 2 MB)",
    "imagesDropLabel": "Images 1–5 (max 5 MB / image)",
    "dropActiveHint": "Drop file here",
    "uploadingLabel": "Uploading...",
    "addPriceRowCta": "+ Add price row",
    "pricingCategoryDrop": "Single visit",
    "pricingCategoryMonthly": "Monthly membership",
    "pricingCategory10x": "10-visit card",
    "pricingCategoryAnnual": "Annual membership",
    "pricingHeaderPrice": "Price (€)",
    "pricingHeaderNotes": "Notes (optional)",
    "pricingRowNamePlaceholder": "Price name",
    "hoursPrefilledLabel": "Pre-filled from Google Places",
    "hoursClosed": "Closed",
    "hoursToggleLabel": "Open",
    "contactPhonePlaceholder": "Phone number",
    "contactEmailPlaceholder": "Email address",
    "contactWebsitePlaceholder": "Website (https://...)",
    "contactDescPlaceholder": "Short description of your service (max 300 characters)",
    "contactDescCount": "{n}/300",
    "previewLabelCard": "LIST CARD",
    "previewLabelDiag": "DIAGONAL CARD",
    "previewLabelSheet": "VENUE PAGE",
    "errorNoPriceRow": "Add at least one price row before continuing.",
    "errorUploadFailed": "File upload failed. Please try again.",
    "errorFileTooLarge": "File is too large (max {max} MB).",
    "errorSubmitFailed": "Submission failed. Check your information and try again."

    Note: en.json already has "errorGeneric" — do NOT duplicate it.
    Ensure both files remain valid JSON after edits.
  </action>
  <verify>
    <automated>node -e "const f = require('./messages/fi.json'); console.log(f.Business.onboardingTitle)"</automated>
  </verify>
  <acceptance_criteria>
    - node -e "require('./messages/fi.json')" exits 0 (valid JSON)
    - node -e "require('./messages/en.json')" exits 0 (valid JSON)
    - node -e "const f=require('./messages/fi.json'); console.log(f.Business.onboardingTitle)" prints "Tietojesi täydentäminen"
    - node -e "const f=require('./messages/en.json'); console.log(f.Business.submitCta)" prints "Submit for approval"
    - grep -c "errorNoPriceRow" messages/fi.json returns 1 (no duplicates)
    - fi.json Business namespace does NOT contain duplicate "errorGeneric" key
  </acceptance_criteria>
  <done>Both fi.json and en.json are valid JSON; all 34+ new wizard keys present under Business namespace; no duplicate keys</done>
</task>

<task type="auto">
  <name>Task 2: Fix ClaimSearchForm redirect (D-01) and add onboarding gate to business/page.tsx (D-03)</name>
  <files>app/components/ClaimSearchForm.tsx, app/business/page.tsx</files>
  <read_first>
    - app/components/ClaimSearchForm.tsx — read full file; locate handleClaim (lines ~94–129) and handleCreate (lines ~131–177); confirm router is already imported and used; confirm create-paikka response shape ({ ok, paikka_id })
    - app/business/page.tsx — read full file; locate checkLinks useEffect; confirm useRouter is NOT yet imported; understand current import list
    - .planning/phases/34-onboarding-velhou/34-RESEARCH.md — Integration Points §2 and §3 (exact change description) and §1 (business/page.tsx change)
    - .planning/phases/34-onboarding-velhou/34-PATTERNS.md — ClaimSearchForm modification section and business/page.tsx modification section
  </read_first>
  <action>
    In app/components/ClaimSearchForm.tsx:

    In handleClaim, replace the `if (res.ok) { window.location.reload(); return }` block with:
    ```
    if (res.ok) {
      router.push('/business/onboarding')
      return
    }
    ```
    Note: claim-paikka route does NOT return paikka_id — no query param needed here. The wizard will look up paikka_id from business_paikka_links on mount.

    In handleCreate, replace the `if (res.ok) { window.location.reload(); return }` block with:
    ```
    if (res.ok) {
      const data = await res.json()
      router.push(`/business/onboarding${data.paikka_id ? `?paikka_id=${data.paikka_id}` : ''}`)
      return
    }
    ```
    create-paikka returns { ok: true, paikka_id: number } — pass paikka_id as query param so wizard avoids a DB roundtrip on first load.

    In app/business/page.tsx:

    1. Add useRouter to the import from 'next/navigation':
       Change: import { useEffect, useState } from 'react'
       Add below: import { useRouter } from 'next/navigation'

    2. Add `const router = useRouter()` as first line inside BusinessPage() function, before the useState declarations.

    3. Inside checkLinks(), after the `const { data: { user } } = await supabase.auth.getUser()` line and before the business_paikka_links query, add the onboarding_completed check (per D-03):
    ```
    const { data: account } = await supabase
      .from('business_accounts')
      .select('onboarding_completed')
      .eq('user_id', user.id)
      .maybeSingle()

    if (account && !account.onboarding_completed) {
      router.push('/business/onboarding')
      return
    }
    ```
    This check runs before the business_paikka_links query. If user has no business_accounts row, account is null and the check is skipped (falls through to existing logic).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "ClaimSearchForm|business/page" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "router.push.*business/onboarding" app/components/ClaimSearchForm.tsx returns 2 (one for handleClaim, one for handleCreate)
    - grep -c "window.location.reload" app/components/ClaimSearchForm.tsx returns 0
    - grep -c "onboarding_completed" app/business/page.tsx returns at least 1
    - grep -c "useRouter" app/business/page.tsx returns at least 1 (import + usage)
    - npx tsc --noEmit produces no errors in ClaimSearchForm.tsx or business/page.tsx
  </acceptance_criteria>
  <done>ClaimSearchForm has no window.location.reload() calls; both handleClaim and handleCreate use router.push('/business/onboarding'); business/page.tsx checks onboarding_completed before showing any panel content; TypeScript compiles without errors in both files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /business | Client-side onboarding_completed check — redirects to /business/onboarding if wizard not complete |
| ClaimSearchForm → /api/business/claim-paikka | JWT authenticated; response only contains { ok } |
| ClaimSearchForm → /api/business/create-paikka | JWT authenticated; response contains { ok, paikka_id } |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03-01 | Tampering | paikka_id URL param in /business/onboarding redirect | accept | paikka_id is a URL hint only — wizard always re-verifies ownership via business_paikka_links on mount before rendering; crafted param cannot grant access |
| T-34-03-02 | Elevation of Privilege | onboarding_completed client-side check | accept | This check is a UX gate, not a security gate; the actual security boundary is the submit Route Handler which re-verifies JWT + business_paikka_links before touching liikuntapaikat |
| T-34-03-SC | Tampering | npm (no new packages) | accept | No new packages; existing dependencies only |
</threat_model>

<verification>
After both tasks:
- grep -c "router.push.*business/onboarding" app/components/ClaimSearchForm.tsx — must return 2
- grep -c "window.location.reload" app/components/ClaimSearchForm.tsx — must return 0
- grep -c "onboarding_completed" app/business/page.tsx — must return at least 1
- node -e "require('./messages/fi.json')" — must exit 0
- npx tsc --noEmit — no errors in modified files
</verification>

<success_criteria>
- messages/fi.json and en.json both valid JSON with all 34+ new wizard keys under Business namespace (per D-01 i18n requirement)
- ClaimSearchForm.tsx: window.location.reload() replaced by router.push('/business/onboarding') in handleClaim; router.push with optional paikka_id param in handleCreate (per D-01)
- business/page.tsx: onboarding_completed check added to checkLinks() with router.push('/business/onboarding') when false (per D-03)
- TypeScript compiles without errors
</success_criteria>

<output>
Create .planning/phases/34-onboarding-velhou/34-03-SUMMARY.md when done
</output>
