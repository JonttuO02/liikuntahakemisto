---
phase: 34-onboarding-velhou
plan: 08
type: execute
wave: 4
depends_on:
  - "34-06"
  - "34-07"
files_modified:
  - app/business/onboarding/StepHinnasto.tsx
  - app/business/onboarding/StepAukioloajat.tsx
  - app/business/onboarding/OnboardingWizardInner.tsx
autonomous: true
requirements:
  - ONBOARD-04
  - ONBOARD-05

must_haves:
  truths:
    - "StepHinnasto shows 4 fixed category rows plus dynamic custom rows"
    - "Seuraava is disabled in StepHinnasto until at least one price row has a non-empty hinta value"
    - "StepAukioloajat stores data with English day keys (monday...sunday), not Finnish"
    - "StepAukioloajat pre-fills from liikuntapaikat.aukioloajat if available in paikkaInfo"
    - "Both steps save via POST /api/business/onboarding/save-step with JWT"
  artifacts:
    - path: "app/business/onboarding/StepHinnasto.tsx"
      provides: "Step 3 pricing table with fixed + dynamic rows and validation"
    - path: "app/business/onboarding/StepAukioloajat.tsx"
      provides: "Step 4 opening hours editor with 7-day toggles and time inputs"
  key_links:
    - from: "StepHinnasto.tsx"
      to: "/api/business/onboarding/save-step"
      via: "fetch with JWT, field='hinnasto'"
      pattern: "field.*hinnasto"
    - from: "StepAukioloajat.tsx"
      to: "/api/business/onboarding/save-step"
      via: "fetch with JWT, field='aukioloajat', English day keys"
      pattern: "field.*aukioloajat"
    - from: "StepAukioloajat.tsx"
      to: "lib/onboardingUtils.ts"
      via: "FI_TO_EN and ORDERED_DAYS imports"
      pattern: "FI_TO_EN"
---

<objective>
Create Step 3 (Hinnasto — pricing) and Step 4 (Aukioloajat — opening hours), then wire them into OnboardingWizardInner's step-switcher.

Purpose: Step 3 implements the pricing validation gate (D-12, D-13, D-14): Seuraava stays disabled until at least one price row has a value. Step 4 implements the Google Places pre-fill (D-15, D-16) and the critical FI_TO_EN day key conversion that must happen before saving — lib/aukiolo.ts only understands English keys.

Output:
- app/business/onboarding/StepHinnasto.tsx — fixed category rows + dynamic rows + validation
- app/business/onboarding/StepAukioloajat.tsx — 7-row hours editor with toggles, time inputs, and pre-fill
- app/business/onboarding/OnboardingWizardInner.tsx — updated to render steps 3 and 4
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-onboarding-velhou/34-CONTEXT.md
@.planning/phases/34-onboarding-velhou/34-RESEARCH.md
@.planning/phases/34-onboarding-velhou/34-PATTERNS.md
@.planning/phases/34-onboarding-velhou/34-UI-SPEC.md

<interfaces>
<!-- Patterns extracted from existing source files and PATTERNS.md -->

From app/business/rekisteroidy/page.tsx (input/button class constants):
```tsx
const inputClass = 'border border-[rgba(0,0,0,0.12)] rounded-lg h-10 px-3 text-sm text-[#111111] placeholder:text-[rgba(17,17,17,0.35)] bg-white focus:outline-none focus:border-[rgba(0,0,0,0.25)] disabled:opacity-60 w-full'
const submitButtonClass = 'bg-[#111111] hover:bg-[#333333] text-white font-bold text-sm rounded-full h-10 [transition:background-color_150ms_var(--ease-out)] disabled:opacity-60 disabled:pointer-events-none'
```

From lib/onboardingUtils.ts (FI_TO_EN, ORDERED_DAYS):
```typescript
export const FI_TO_EN: Record<string, string> = {
  'Ma': 'monday', 'Ti': 'tuesday', 'Ke': 'wednesday',
  'To': 'thursday', 'Pe': 'friday', 'La': 'saturday', 'Su': 'sunday'
}
export const ORDERED_DAYS: string[] = ['monday','tuesday','wednesday','thursday','friday','saturday','sunday']
```

Finnish day labels for display (derived from ORDERED_DAYS):
```typescript
const EN_TO_FI: Record<string, string> = {
  monday: 'Ma', tuesday: 'Ti', wednesday: 'Ke', thursday: 'To',
  friday: 'Pe', saturday: 'La', sunday: 'Su'
}
```

From PATTERNS.md — Auki toggle pattern (role="switch"):
```tsx
<button
  type="button"
  role="switch"
  aria-checked={isOpen}
  aria-label={t('hoursToggleLabel')}
  onClick={() => toggleDay(dayKey)}
  className={`w-10 h-6 rounded-full [transition:background-color_150ms_var(--ease-out)] ${
    isOpen ? 'bg-[#111111]' : 'bg-[rgba(17,17,17,0.12)]'
  }`}
>
  <span className={`w-5 h-5 rounded-full bg-white block transition-transform ${
    isOpen ? 'translate-x-[18px]' : 'translate-x-0.5'
  }`} />
</button>
```

StepHinnasto pricing row types:
- Fixed: kategoria is a fixed string (non-editable), hinta is an input, lisatieto is an input
- Dynamic (custom): kategoria is also an editable input, hinta and lisatieto are inputs; user can delete these rows

save-step call for hinnasto: POST { paikka_id, step: 3, field: 'hinnasto', value: [ { kategoria, hinta, lisatieto } ] }
save-step call for aukioloajat: POST { paikka_id, step: 4, field: 'aukioloajat', value: { monday: { open, close }, ... } (only open days included) }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StepHinnasto.tsx (Step 3 — pricing table with validation)</name>
  <files>app/business/onboarding/StepHinnasto.tsx</files>
  <read_first>
    - app/business/rekisteroidy/page.tsx — full file; input class, button class, AnimatePresence error pattern, fetch+JWT pattern (lines 92–96, 67–76)
    - app/components/ClaimSearchForm.tsx lines 98–103 — JWT token extraction (getSession)
    - .planning/phases/34-onboarding-velhou/34-UI-SPEC.md — Step 3 Hinnasto visual anatomy (table structure, row structure, "+ Lisää hintarivi")
    - .planning/phases/34-onboarding-velhou/34-PATTERNS.md — StepHinnasto section (fixed rows, dynamic rows, X delete button, disabled button)
  </read_first>
  <action>
    Create app/business/onboarding/StepHinnasto.tsx:
    - 'use client'
    - Props: { paikkaId: number, onNext: () => void, onPrev: () => void }
    - Imports: useState from 'react'; motion, AnimatePresence from 'framer-motion'; createBrowserSupabase from '@/lib/supabaseSSR'; useTranslations from 'next-intl'; X from 'lucide-react'

    Types:
    - PricingRow: { id: string, kategoria: string, hinta: string, lisatieto: string, isFixed: boolean }

    State:
    - rows: PricingRow[] — initialized with 4 fixed rows (ids: 'fixed-1' through 'fixed-4'); categories from t('pricingCategoryDrop'), t('pricingCategoryMonthly'), t('pricingCategory10x'), t('pricingCategoryAnnual'); hinta: '', lisatieto: '', isFixed: true
    - loading: boolean (false)
    - error: string | null

    Derived state:
    - hasAnyPrice: rows.some(r => r.hinta.trim() !== '') — used to gate the Next button

    addPriceRow: append PricingRow { id: `custom-${Date.now()}`, kategoria: '', hinta: '', lisatieto: '', isFixed: false }

    removeRow(id): filter out row with matching id (only callable for non-fixed rows)

    updateRow(id, field, value): map rows, update matching row

    handleNext async:
    1. setLoading(true); setError(null)
    2. Get JWT session
    3. POST to /api/business/onboarding/save-step with { paikka_id, step: 3, field: 'hinnasto', value: rows.filter(r => r.hinta.trim() !== '').map(({ kategoria, hinta, lisatieto }) => ({ kategoria, hinta, lisatieto })) }
    4. On success: onNext()
    5. On error: setError(t('errorGeneric'))
    6. finally: setLoading(false)

    Render: glass rounded-2xl p-6 w-full max-w-xl mx-auto
    - h2.text-xl.font-bold: t('stepPricing')
    - table.w-full:
      - thead: tr with caps-label headers — t('pricingHeaderPrice'), t('pricingHeaderNotes') columns + delete column
      - tbody.divide-y.divide-[rgba(0,0,0,0.07)]:
        - For each row: tr with py-3 cells
          - Category cell: if isFixed → text-sm text-[#111111]; if dynamic → input with same border style as inputClass but w-full, placeholder=t('pricingRowNamePlaceholder')
          - Price cell: input type="text" inputMode="decimal" className="border border-[rgba(0,0,0,0.12)] focus:border-[rgba(0,0,0,0.25)] rounded-lg h-9 px-2 text-sm w-28 tabular-nums outline-none [transition:border-color_150ms_var(--ease-out)]"
          - Notes cell: input type="text" className="border border-[rgba(0,0,0,0.12)] focus:border-[rgba(0,0,0,0.25)] rounded-lg h-9 px-2 text-sm w-full outline-none [transition:border-color_150ms_var(--ease-out)]"
          - Delete cell (only for non-fixed rows): button with X icon, className="text-[rgba(17,17,17,0.35)] hover:text-red-600 [transition:color_150ms_var(--ease-out)]"
    - "+ Lisää hintarivi" button: text-sm text-[#111111] font-bold underline-offset-2 hover:underline w-fit mt-2, onClick=addPriceRow
    - AnimatePresence error: same pattern as rekisteroidy (key="price-error")
    - footer: back button (text-sm text-[rgba(17,17,17,0.45)] hover:text-[#111111]) | Next button (disabled={!hasAnyPrice || loading}, px-6 variant of submitButtonClass): t('nextCta') or loading state

    The Next button's disabled state depends on hasAnyPrice — this is the ONBOARD-04 validation gate.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "StepHinnasto" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - StepHinnasto.tsx exists; contains 'use client'; 4 initial fixed rows; "+ Lisää hintarivi" button adds rows
    - grep -c "hasAnyPrice" app/business/onboarding/StepHinnasto.tsx returns at least 1
    - grep -c "disabled.*hasAnyPrice\|hasAnyPrice.*disabled" app/business/onboarding/StepHinnasto.tsx returns at least 1 (Next button gated)
    - grep -c "save-step" app/business/onboarding/StepHinnasto.tsx returns at least 1
    - grep -c "hinnasto" app/business/onboarding/StepHinnasto.tsx returns at least 1 (field name in save-step body)
    - npx tsc --noEmit produces no errors in StepHinnasto.tsx
  </acceptance_criteria>
  <done>StepHinnasto renders pricing table with 4 fixed rows; custom rows add/delete; Seuraava disabled until at least one row has a non-empty hinta; data saved via save-step with field='hinnasto'; TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Create StepAukioloajat.tsx (Step 4 — hours editor with pre-fill and English key mapping)</name>
  <files>app/business/onboarding/StepAukioloajat.tsx, app/business/onboarding/OnboardingWizardInner.tsx</files>
  <read_first>
    - lib/onboardingUtils.ts — FI_TO_EN and ORDERED_DAYS exports (paths and names, used in StepAukioloajat)
    - lib/aukiolo.ts — verify ORDERED_DAYS key format and confirm aukioloajat shape is Record&lt;string, { open: string; close: string }&gt;
    - app/business/onboarding/OnboardingWizardInner.tsx — current stub steps 3–6 (replace stub 3 and 4 with real imports)
    - .planning/phases/34-onboarding-velhou/34-UI-SPEC.md — Step 4 Aukioloajat visual anatomy (7-row layout, toggle, time inputs, pre-fill label)
    - .planning/phases/34-onboarding-velhou/34-PATTERNS.md — StepAukioloajat section (toggle pattern, day key mapping, pre-fill label)
  </read_first>
  <action>
    Create app/business/onboarding/StepAukioloajat.tsx:
    - 'use client'
    - Props: { paikkaId: number, existingAukioloajat: Record&lt;string, { open: string; close: string }&gt; | null | undefined, onNext: () => void, onPrev: () => void }
    - Imports: useState, useEffect from 'react'; motion, AnimatePresence from 'framer-motion'; createBrowserSupabase from '@/lib/supabaseSSR'; useTranslations from 'next-intl'; ORDERED_DAYS from '@/lib/onboardingUtils'

    Types:
    - DayState: { isOpen: boolean, open: string, close: string }
    - HoursState: Record&lt;string, DayState&gt; — keyed by English day names

    EN_TO_FI map (local constant, for display only):
    { monday: 'Ma', tuesday: 'Ti', wednesday: 'Ke', thursday: 'To', friday: 'Pe', saturday: 'La', sunday: 'Su' }

    State:
    - hours: HoursState — initialized from existingAukioloajat in useEffect (if prop present, set isOpen=true and fill times); default: all days { isOpen: false, open: '', close: '' }
    - wasPreFilled: boolean — true if existingAukioloajat had any data
    - loading: boolean
    - error: string | null

    useEffect on existingAukioloajat:
    If prop is non-null and has keys, build HoursState from it: for each English day in ORDERED_DAYS, if key exists in existingAukioloajat → { isOpen: true, open: data.open, close: data.close }. Set wasPreFilled(true).

    toggleDay(dayKey): setHours(prev => ({ ...prev, [dayKey]: { ...prev[dayKey], isOpen: !prev[dayKey].isOpen } }))

    updateTime(dayKey, field: 'open'|'close', value: string): update hours[dayKey][field]

    handleNext async:
    1. setLoading(true)
    2. Build save value: filter ORDERED_DAYS to only isOpen days, construct: { [dayKey]: { open: hours[dayKey].open, close: hours[dayKey].close } }
    3. Get JWT session
    4. POST to save-step: { paikka_id, step: 4, field: 'aukioloajat', value: openDaysObject }
    5. On success: onNext(); on error: setError(t('errorGeneric'))
    6. finally: setLoading(false)

    Render: glass rounded-2xl p-6 w-full max-w-xl mx-auto
    - h2.text-xl.font-bold: t('stepHours')
    - If wasPreFilled: span.text-[10px].font-bold.uppercase.tracking-widest.text-[rgba(17,17,17,0.45)]: t('hoursPrefilledLabel')
    - For each dayKey in ORDERED_DAYS: div.flex.items-center.gap-3.py-3.border-b.border-[rgba(0,0,0,0.07)]:
      - span.text-sm.font-bold.text-[#111111].w-8: EN_TO_FI[dayKey]
      - Auki toggle button (role="switch", aria-checked, aria-label=t('hoursToggleLabel'), onClick=toggleDay(dayKey))
      - If isOpen: two time inputs (type="time", className="border border-[rgba(0,0,0,0.12)] focus:border-[rgba(0,0,0,0.25)] rounded-lg h-9 px-2 text-sm w-24 outline-none [transition:border-color_150ms_var(--ease-out)]") + em dash span between them + screen-reader-only labels
      - If !isOpen: span.text-sm.text-[rgba(17,17,17,0.35)]: t('hoursClosed')
    - AnimatePresence error
    - footer: back button | Next button (disabled={loading})

    CRITICAL: The saved aukioloajat value uses English keys only. The FI day labels (Ma/Ti/...) are display-only derived from EN_TO_FI. Storage keys are always 'monday', 'tuesday', etc. (per RESEARCH.md Pitfall 5).

    After creating StepAukioloajat.tsx, update app/business/onboarding/OnboardingWizardInner.tsx:
    - Add import of StepHinnasto from './StepHinnasto' (replace null stub for step 3)
    - Add import of StepAukioloajat from './StepAukioloajat' (replace null stub for step 4)
    - In the step-switcher, render StepHinnasto for step===3 with paikkaId + onNext (calls saveAndAdvance(3)) + onPrev (calls goToStep(2))
    - In the step-switcher, render StepAukioloajat for step===4 with paikkaId + existingAukioloajat=paikkaInfo?.aukioloajat + onNext (calls saveAndAdvance(4)) + onPrev (calls goToStep(3))
    - Steps 5 and 6 remain null stubs

    Note: StepHinnasto and StepAukioloajat handle their own save-step calls internally. OnboardingWizardInner.saveAndAdvance(n) calls goToStep(n+1) only — no additional save logic needed at orchestrator level for these steps.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "StepAukioloajat|StepHinnasto" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - StepAukioloajat.tsx exists; contains 'use client'; imports ORDERED_DAYS from '@/lib/onboardingUtils'
    - grep -c "FI_TO_EN\|EN_TO_FI" app/business/onboarding/StepAukioloajat.tsx returns at least 1 (day key display mapping)
    - grep -c "monday" app/business/onboarding/StepAukioloajat.tsx returns at least 2 (ORDERED_DAYS iteration uses English keys)
    - grep -c "role.*switch" app/business/onboarding/StepAukioloajat.tsx returns at least 1 (Auki toggle)
    - grep -c "aukioloajat" app/business/onboarding/StepAukioloajat.tsx returns at least 1 (field name in save-step)
    - StepHinnasto.tsx TypeScript compiles without errors
    - StepAukioloajat.tsx TypeScript compiles without errors
    - OnboardingWizardInner.tsx imports and renders StepHinnasto for step 3 and StepAukioloajat for step 4
  </acceptance_criteria>
  <done>StepAukioloajat renders 7-day rows with toggle + time inputs; pre-fills from existingAukioloajat using English day keys; saves with English keys; StepHinnasto integrated into OnboardingWizardInner; TypeScript compiles without errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StepHinnasto → /api/business/onboarding/save-step | JWT Authorization; server validates token before UPSERT |
| StepAukioloajat → /api/business/onboarding/save-step | JWT Authorization; English day keys enforced client-side before send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-08-01 | Tampering | arbitrary hinnasto/aukioloajat JSONB values | accept | JSONB is schema-less; values are business-supplied pricing/hours data; no code evaluation of JSONB values; priceItemList() and formatGroupedHours() parse defensively |
| T-34-08-02 | Tampering | Day key injection in aukioloajat | mitigate | ORDERED_DAYS whitelist enforced: only english day keys from the whitelist are included in the saved object; arbitrary keys from the UI cannot be injected |
| T-34-08-03 | Denial of Service | Excessive custom price rows | accept | No hard limit enforced on custom rows (UI concern only); JSONB stored in DB; Supabase enforces 8KB row limit which prevents extreme abuse |
| T-34-08-SC | Tampering | npm (no new packages) | accept | No new packages; X icon from lucide-react already installed |
</threat_model>

<verification>
npx vitest run lib/onboardingUtils.test.ts — pricing and hours tests must pass (from Plan 01).

npx tsc --noEmit — no errors in StepHinnasto.tsx, StepAukioloajat.tsx, or updated OnboardingWizardInner.tsx.

Manual: Visit /business/onboarding?step=3 — pricing table shows 4 fixed rows; Seuraava disabled; enter a price — Seuraava enables.
Manual: Visit /business/onboarding?step=4 — 7 day rows render; Ma toggle turns on — time inputs appear; La/Su start closed.
</verification>

<success_criteria>
- StepHinnasto: 4 fixed category rows + dynamic row add/delete; Seuraava disabled until hasAnyPrice; saves { field: 'hinnasto', value: [...] } via save-step
- StepAukioloajat: 7-day rows with Auki toggles; time inputs shown for open days; pre-fills from existingAukioloajat; saves with English day keys { field: 'aukioloajat', value: { monday: {...}, ... } }; wasPreFilled shows Google Places label
- OnboardingWizardInner renders StepHinnasto for step 3 and StepAukioloajat for step 4
- npx tsc --noEmit passes
</success_criteria>

<output>
Create .planning/phases/34-onboarding-velhou/34-08-SUMMARY.md when done
</output>
