---
phase: 34-onboarding-velhou
plan: 04
type: execute
wave: 2
depends_on:
  - "34-02"
  - "34-03"
files_modified: []
autonomous: false
requirements:
  - ONBOARD-01
  - ONBOARD-03
  - ONBOARD-04
  - ONBOARD-05
  - ONBOARD-06
  - ONBOARD-07

must_haves:
  truths:
    - "onboarding_draft table exists in live Supabase instance with all columns"
    - "business_accounts.onboarding_completed column exists with DEFAULT false"
    - "RLS policies are active on onboarding_draft"
  artifacts:
    - path: "supabase/migrations/20260606000000_onboarding.sql"
      provides: "Applied to Supabase — schema is live"
  key_links:
    - from: "Supabase DB"
      to: "onboarding_draft table"
      via: "supabase db push"
      pattern: "supabase db push"
---

<objective>
[BLOCKING] Push the Phase 34 migration to the live Supabase instance. No wizard code can function until this schema is applied.

Purpose: The onboarding_draft table and onboarding_completed column from Plan 02 must be live in Supabase before the Route Handlers in Plan 05 can query them. This plan is a human-supervised checkpoint — the executor runs the push command, the human verifies in the Supabase dashboard.

Output:
- onboarding_draft table live in Supabase
- business_accounts.onboarding_completed column live
- Checkpoint sign-off confirming the push succeeded
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-onboarding-velhou/34-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Run supabase db push</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260606000000_onboarding.sql — verify the migration file exists and is complete before pushing
    - supabase/config.toml — confirm project is linked (look for project_id)
  </read_first>
  <action>
    Run the following command to push the Phase 34 migration to the live Supabase instance:

      supabase db push

    If this fails with an interactive prompt (asking for confirmation), re-run with:

      supabase db push --linked

    If the CLI reports authentication required, the SUPABASE_ACCESS_TOKEN environment variable may need to be set. In that case, retrieve the token from the Supabase dashboard (Account → Access Tokens) and set it before re-running.

    Expected output: "Applying migration 20260606000000_onboarding.sql... done" or similar success message.

    If the command exits with an error (e.g., "already applied"), check supabase migration list to confirm the migration is in the applied list — if it is, the schema is already live and no further action is needed.
  </action>
  <verify>
    <automated>supabase migration list 2>&1 | grep "20260606000000"</automated>
  </verify>
  <acceptance_criteria>
    - supabase migration list shows 20260606000000_onboarding in the applied column
    - supabase db push exits 0 or reports the migration was already applied
  </acceptance_criteria>
  <done>supabase migration list shows 20260606000000_onboarding as applied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Ran supabase db push to apply the Phase 34 migration (20260606000000_onboarding.sql) which adds: (1) onboarding_draft table with all required columns and RLS, (2) onboarding_completed BOOLEAN column on business_accounts.</what-built>
  <how-to-verify>
    1. Open the Supabase dashboard for this project.
    2. Navigate to Table Editor — confirm "onboarding_draft" table exists with columns: id, business_account_id, paikka_id, media_urls, hinnasto, aukioloajat, yhteystiedot, current_step, updated_at.
    3. Navigate to Authentication → Policies — confirm 4 RLS policies exist on onboarding_draft (SELECT, INSERT, UPDATE, DELETE).
    4. Navigate to Table Editor → business_accounts — confirm "onboarding_completed" column exists with type boolean and default false.
  </how-to-verify>
  <resume-signal>Type "approved" if schema looks correct, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supabase CLI → Supabase project | Migration push uses project credentials; service role is not exposed in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04-01 | Tampering | supabase db push | mitigate | Migration SQL was reviewed in Plan 02 before push; RLS policies verified in checkpoint |
| T-34-04-SC | Tampering | supabase CLI | mitigate | Official Supabase CLI from project; human checkpoint verifies schema in dashboard before proceeding |
</threat_model>

<verification>
Human checkpoint: Supabase dashboard shows onboarding_draft table with correct schema and 4 RLS policies; business_accounts has onboarding_completed column.
</verification>

<success_criteria>
- supabase migration list shows 20260606000000_onboarding as applied
- Human confirms in dashboard: onboarding_draft table exists with all required columns, RLS enabled with 4 policies
- Human confirms: business_accounts.onboarding_completed column exists
</success_criteria>

<output>
Create .planning/phases/34-onboarding-velhou/34-04-SUMMARY.md when done
</output>
