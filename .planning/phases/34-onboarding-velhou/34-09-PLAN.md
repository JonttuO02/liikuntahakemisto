---
phase: 34-onboarding-velhou
plan: 09
type: execute
wave: 4
depends_on:
  - "34-06"
  - "34-07"
files_modified:
  - app/business/onboarding/StepYhteystiedot.tsx
  - app/business/onboarding/StepEsikatselu.tsx
  - app/business/onboarding/OnboardingWizardInner.tsx
autonomous: true
requirements:
  - ONBOARD-06
  - ONBOARD-07

must_haves:
  truths:
    - "StepYhteystiedot collects puhelin, email, website, and kuvaus (max 300 chars)"
    - "Description character counter turns text-red-600 at the 300-char limit"
    - "StepEsikatselu renders PaikkaKortti and DiagonaalKortti with draft data"
    - "StepEsikatselu does NOT render full PaikkaSheet (position:fixed would break wizard layout)"
    - "StepEsikatselu Laheta button POSTs to submit Route Handler and redirects to /business on success"
    - "buildDraftAsPaikka from lib/onboardingUtils constructs the Liikuntapaikka preview object"
  artifacts:
    - path: "app/business/onboarding/StepYhteystiedot.tsx"
      provides: "Step 5 contact form with 4 fields and 300-char description limit"
    - path: "app/business/onboarding/StepEsikatselu.tsx"
      provides: "Step 6 preview (PaikkaKortti + DiagonaalKortti + simplified sheet content) + submit"
  key_links:
    - from: "StepEsikatselu.tsx"
      to: "lib/onboardingUtils.ts"
      via: "buildDraftAsPaikka import"
      pattern: "buildDraftAsPaikka"
    - from: "StepEsikatselu.tsx"
      to: "/api/business/onboarding/submit"
      via: "fetch with JWT Authorization"
      pattern: "submit"
    - from: "StepEsikatselu.tsx"
      to: "app/components/PaikkaKortti.tsx"
      via: "paikka prop of type Liikuntapaikka"
      pattern: "PaikkaKortti"
---

<objective>
Create Step 5 (Yhteystiedot — contact details) and Step 6 (Esikatselu — preview + submit), then wire both into OnboardingWizardInner.

Purpose: Step 5 is the final data-collection step — contact info with a 300-char description cap. Step 6 is the preview and submission gate: it fetches the full draft, constructs a Liikuntapaikka preview object using buildDraftAsPaikka, renders PaikkaKortti and DiagonaalKortti, and provides the "Lähetä hyväksyttäväksi" button that triggers the atomic submit Route Handler. PaikkaSheet is intentionally omitted from the preview because it uses position:fixed and would break the wizard layout.

Output:
- app/business/onboarding/StepYhteystiedot.tsx — contact form with phone, email, website, description (max 300)
- app/business/onboarding/StepEsikatselu.tsx — preview cards + submit button + router.push to /business
- app/business/onboarding/OnboardingWizardInner.tsx — wired with steps 5 and 6 (all 6 steps complete)
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-onboarding-velhou/34-CONTEXT.md
@.planning/phases/34-onboarding-velhou/34-RESEARCH.md
@.planning/phases/34-onboarding-velhou/34-PATTERNS.md
@.planning/phases/34-onboarding-velhou/34-UI-SPEC.md

<interfaces>
<!-- Key patterns from existing source files and PATTERNS.md -->

From app/business/rekisteroidy/page.tsx:
```tsx
const inputClass = 'border border-[rgba(0,0,0,0.12)] rounded-lg h-10 px-3 text-sm text-[#111111] placeholder:text-[rgba(17,17,17,0.35)] bg-white focus:outline-none focus:border-[rgba(0,0,0,0.25)] disabled:opacity-60 w-full'
```

From PATTERNS.md — StepEsikatselu buildDraftAsPaikka usage:
```typescript
import { buildDraftAsPaikka, OnboardingDraft } from '@/lib/onboardingUtils'
import type { Liikuntapaikka } from '@/lib/types'
const draftAsPaikka: Liikuntapaikka = buildDraftAsPaikka(draft, paikka)
```

From PATTERNS.md — StepEsikatselu preview section:
```tsx
<div className="flex flex-col gap-2">
  <span className="text-[10px] font-bold uppercase tracking-widest text-[rgba(17,17,17,0.45)]">LISTAKORTTI</span>
  <PaikkaKortti paikka={draftAsPaikka} />
</div>
<div className="flex flex-col gap-2">
  <span className="text-[10px] font-bold uppercase tracking-widest text-[rgba(17,17,17,0.45)]">DIAGONAALIKORTTI</span>
  <DiagonaalKortti paikka={draftAsPaikka} />
</div>
```

CRITICAL: Do NOT render PaikkaSheet in preview. It uses position:fixed and height:calc(100dvh) — would overlay the entire screen. Instead render a simplified ".glass rounded-2xl p-4" card with price/hours/contact info inline. (RESEARCH.md Pitfall 1)

From PATTERNS.md — submit button style:
```tsx
className="bg-[#111111] hover:bg-[#333333] text-white font-bold text-sm rounded-full h-10 px-6 [transition:background-color_150ms_var(--ease-out)] disabled:opacity-60 disabled:pointer-events-none"
```

contactDescCount key format: "{n}/300" — use t('contactDescCount').replace('{n}', kuvaus.length.toString())

Description textarea: aria-describedby pointing to the counter element
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StepYhteystiedot.tsx (Step 5 — contact form)</name>
  <files>app/business/onboarding/StepYhteystiedot.tsx</files>
  <read_first>
    - app/business/rekisteroidy/page.tsx — full file; inputClass, submitButtonClass, error AnimatePresence pattern, JWT fetch pattern
    - .planning/phases/34-onboarding-velhou/34-UI-SPEC.md — Step 5 contact fields list, Form Validation Contract (all optional, description max 300), contactDescCount key
    - .planning/phases/34-onboarding-velhou/34-PATTERNS.md — StepYhteystiedot section (exact inputClass reuse, back button class)
  </read_first>
  <action>
    Create app/business/onboarding/StepYhteystiedot.tsx:
    - 'use client'
    - Props: { paikkaId: number, onNext: () => void, onPrev: () => void }
    - Imports: useState from 'react'; motion, AnimatePresence from 'framer-motion'; createBrowserSupabase from '@/lib/supabaseSSR'; useTranslations from 'next-intl'

    State:
    - puhelin: string ('')
    - email: string ('')
    - website: string ('')
    - kuvaus: string ('')
    - loading: boolean (false)
    - error: string | null

    Derived: descCount = kuvaus.length; isAtLimit = descCount >= 300

    handleNext async:
    1. setLoading(true); setError(null)
    2. Get JWT session (createBrowserSupabase + getSession)
    3. POST to save-step: { paikka_id, step: 5, field: 'yhteystiedot', value: { puhelin: puhelin.trim(), email: email.trim(), website: website.trim(), kuvaus: kuvaus.trim() } }
    4. On success: onNext(); on error: setError(t('errorGeneric'))
    5. finally: setLoading(false)

    Render: glass rounded-2xl p-6 w-full max-w-xl mx-auto
    - h2.text-xl.font-bold: t('stepContact')
    - flex.flex-col.gap-4:
      - Phone input: type="tel", placeholder=t('contactPhonePlaceholder'), value=puhelin, onChange, className=inputClass
      - Email input: type="email", placeholder=t('contactEmailPlaceholder'), value=email, onChange, className=inputClass
      - Website input: type="url", placeholder=t('contactWebsitePlaceholder'), value=website, onChange, className=inputClass
      - Description section:
        - textarea: placeholder=t('contactDescPlaceholder'), value=kuvaus, onChange, maxLength={300}, rows={4}, id="kuvaus-textarea", aria-describedby="kuvaus-counter", className="border border-[rgba(0,0,0,0.12)] rounded-lg px-3 py-2 text-sm text-[#111111] placeholder:text-[rgba(17,17,17,0.35)] bg-white focus:outline-none focus:border-[rgba(0,0,0,0.25)] w-full resize-none [transition:border-color_150ms_var(--ease-out)]"
        - char counter: span id="kuvaus-counter", className=isAtLimit ? "text-[10px] font-bold text-red-600" : "text-[10px] font-bold text-[rgba(17,17,17,0.45)]": t('contactDescCount').replace('{n}', String(descCount))
      - AnimatePresence error
    - footer: back button (t('prevCta')) | Next button (disabled={loading}, t('nextCta'))

    All fields are optional — no form validation beyond the 300-char maxLength.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "StepYhteystiedot" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - StepYhteystiedot.tsx exists; contains 'use client'
    - grep -c "maxLength.*300\|maxLength={300}" app/business/onboarding/StepYhteystiedot.tsx returns 1
    - grep -c "kuvaus-counter" app/business/onboarding/StepYhteystiedot.tsx returns at least 2 (id + aria-describedby)
    - grep -c "yhteystiedot" app/business/onboarding/StepYhteystiedot.tsx returns at least 1 (field name in save-step body)
    - grep -c "text-red-600" app/business/onboarding/StepYhteystiedot.tsx returns at least 1 (limit reached indicator)
    - npx tsc --noEmit produces no errors in StepYhteystiedot.tsx
  </acceptance_criteria>
  <done>StepYhteystiedot renders 4 contact fields; description textarea has maxLength=300 with character counter that turns red at limit; all fields optional; saves via save-step with field='yhteystiedot'; TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Create StepEsikatselu.tsx (Step 6 — preview + submit) and wire all steps into OnboardingWizardInner</name>
  <files>app/business/onboarding/StepEsikatselu.tsx, app/business/onboarding/OnboardingWizardInner.tsx</files>
  <read_first>
    - lib/onboardingUtils.ts — buildDraftAsPaikka function signature and OnboardingDraft type
    - lib/types.ts — Liikuntapaikka type (confirm all fields that buildDraftAsPaikka produces)
    - app/components/PaikkaKortti.tsx — component props (paikka: Liikuntapaikka required); read lines 1–30 for prop shape
    - app/components/DiagonaalKortti.tsx — component props (paikka: Liikuntapaikka required); read lines 1–20
    - app/components/PaikkaSheet.tsx — first 5 lines only to confirm position:fixed is present (do NOT render it)
    - app/business/onboarding/OnboardingWizardInner.tsx — current state; locate step-switcher to add steps 5 and 6
    - .planning/phases/34-onboarding-velhou/34-UI-SPEC.md — Step 6 Esikatselu visual anatomy (preview labels, column layout)
    - .planning/phases/34-onboarding-velhou/34-PATTERNS.md — StepEsikatselu section (preview label pattern, submit button, "PROFIILISIVU" simplified card)
    - .planning/phases/34-onboarding-velhou/34-RESEARCH.md — Pitfall 1 (PaikkaSheet), Pattern 4 (buildDraftAsPaikka)
  </read_first>
  <action>
    Create app/business/onboarding/StepEsikatselu.tsx:
    - 'use client'
    - Props: { draft: OnboardingDraft | null, paikkaInfo: { nimi: string; laji: string; osoite: string | null; kaupunki: string | null; latitude: number | null; longitude: number | null; aukioloajat?: Record&lt;string, { open: string; close: string }&gt; | null } | null, onPrev: () => void }
    - Imports: useState from 'react'; useRouter from 'next/navigation'; motion, AnimatePresence from 'framer-motion'; createBrowserSupabase from '@/lib/supabaseSSR'; useTranslations from 'next-intl'; buildDraftAsPaikka, OnboardingDraft from '@/lib/onboardingUtils'; PaikkaKortti from '@/app/components/PaikkaKortti'; DiagonaalKortti from '@/app/components/DiagonaalKortti'

    CRITICAL: Do NOT import PaikkaSheet — it uses position:fixed and will break the wizard layout (RESEARCH.md Pitfall 1).

    State: loading: boolean, error: string | null

    Derived: draftAsPaikka — computed when both draft and paikkaInfo are non-null: buildDraftAsPaikka(draft, paikkaInfo). If either is null, show spinner.

    handleSubmit async:
    1. setLoading(true); setError(null)
    2. Get JWT session
    3. POST to /api/business/onboarding/submit: { } (empty body — server reads draft from user.id)
    4. On success: const data = await res.json(); if (data.ok) { router.push('/business') }
    5. On error: setError(t('errorSubmitFailed'))
    6. finally: setLoading(false)

    Render: glass rounded-2xl p-6 w-full max-w-xl mx-auto
    - h2.text-xl.font-bold: t('stepPreview')
    - If !draftAsPaikka: loading spinner
    - If draftAsPaikka:
      - flex.flex-col.gap-6:
        - LISTAKORTTI section: div.flex.flex-col.gap-2
          - span.text-[10px].font-bold.uppercase.tracking-widest.text-[rgba(17,17,17,0.45)]: t('previewLabelCard')
          - PaikkaKortti paikka={draftAsPaikka}
        - DIAGONAALIKORTTI section: div.flex.flex-col.gap-2
          - span caps label: t('previewLabelDiag')
          - DiagonaalKortti paikka={draftAsPaikka}
        - PROFIILISIVU section (simplified — NOT PaikkaSheet): div.flex.flex-col.gap-2
          - span caps label: t('previewLabelSheet')
          - div.glass.rounded-2xl.p-4.flex.flex-col.gap-3:
            - h3.text-xl.font-bold.text-[#111111]: draftAsPaikka.nimi
            - if draftAsPaikka.hinta_kuvaus: p.text-sm.text-[#111111] showing hinta_kuvaus (whitespace-pre-line for newlines)
            - if draftAsPaikka.kuvaus: p.text-sm.text-[rgba(17,17,17,0.45)].line-clamp-4: draftAsPaikka.kuvaus
            - if draftAsPaikka.puhelin: p.text-sm: draftAsPaikka.puhelin
      - AnimatePresence error (key="submit-error")
    - footer: back button (t('prevCta')) | Submit button (disabled={loading}, type="button", onClick=handleSubmit):
      loading ? t('submitting') : t('submitCta')
      className="bg-[#111111] hover:bg-[#333333] text-white font-bold text-sm rounded-full h-10 px-6 [transition:background-color_150ms_var(--ease-out)] disabled:opacity-60 disabled:pointer-events-none"

    After creating StepEsikatselu.tsx, update app/business/onboarding/OnboardingWizardInner.tsx:
    - Add import of StepYhteystiedot from './StepYhteystiedot' (replace null stub for step 5)
    - Add import of StepEsikatselu from './StepEsikatselu' (replace null stub for step 6)
    - In the step-switcher:
      - step===5: StepYhteystiedot (paikkaId + onNext calls saveAndAdvance(5) + onPrev calls goToStep(4))
      - step===6: StepEsikatselu (draft=draft, paikkaInfo=paikkaInfo, onPrev=() => goToStep(5))
    - Ensure saveAndAdvance(5) calls goToStep(6) (not goToStep(6) after save — StepYhteystiedot calls onNext which calls saveAndAdvance(5) after internal save-step call)
    - All 6 steps are now wired — no more null stubs

    StepEsikatselu does NOT have an onNext prop — submission redirects instead of advancing a step.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "StepEsikatselu|StepYhteystiedot" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - StepEsikatselu.tsx exists; contains 'use client'; imports buildDraftAsPaikka; imports PaikkaKortti and DiagonaalKortti
    - grep -c "PaikkaSheet" app/business/onboarding/StepEsikatselu.tsx returns 0 (PaikkaSheet NOT imported — fixed position would break layout)
    - grep -c "buildDraftAsPaikka" app/business/onboarding/StepEsikatselu.tsx returns at least 1
    - grep -c "submit" app/business/onboarding/StepEsikatselu.tsx returns at least 2 (/api/business/onboarding/submit call + submitCta button)
    - grep -c "router.push.*business" app/business/onboarding/StepEsikatselu.tsx returns at least 1 (redirect after submit)
    - StepYhteystiedot.tsx maxLength=300 check confirmed
    - OnboardingWizardInner.tsx imports and renders all 6 steps (no null stubs remaining for steps 5, 6)
    - npx tsc --noEmit produces no errors in any modified/created file
  </acceptance_criteria>
  <done>StepEsikatselu renders PaikkaKortti + DiagonaalKortti + simplified sheet content using buildDraftAsPaikka; submit button POSTs to /api/business/onboarding/submit and redirects to /business on success; PaikkaSheet not used; all 6 wizard steps wired in OnboardingWizardInner; TypeScript compiles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StepEsikatselu → /api/business/onboarding/submit | JWT Authorization; submit Route Handler does ownership check before liikuntapaikat write |
| StepYhteystiedot → /api/business/onboarding/save-step | JWT Authorization; server validates token before UPSERT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-09-01 | Tampering | XSS via kuvaus/puhelin/website stored in DB | mitigate | React renders these as text nodes (not innerHTML) in PaikkaKortti, DiagonaalKortti, and simplified sheet card — no dangerouslySetInnerHTML in wizard or preview components |
| T-34-09-02 | Elevation of Privilege | StepEsikatselu submit without ownership | mitigate | submit Route Handler (Plan 05) verifies business_paikka_links before updating liikuntapaikat; client cannot bypass this server-side check |
| T-34-09-03 | Tampering | PaikkaSheet layout escape | mitigate | PaikkaSheet explicitly NOT imported in StepEsikatselu (position:fixed architectural blocker documented in RESEARCH.md Pitfall 1) |
| T-34-09-SC | Tampering | npm (no new packages) | accept | PaikkaKortti and DiagonaalKortti are existing project components; no new registry downloads |
</threat_model>

<verification>
npx vitest run lib/onboardingUtils.test.ts — all tests pass (draft mapping for ONBOARD-07).

npx tsc --noEmit — no errors in StepYhteystiedot.tsx, StepEsikatselu.tsx, or OnboardingWizardInner.tsx.

Manual end-to-end:
1. Log in as new business, complete Phase 33 claim/create
2. Wizard auto-navigates to /business/onboarding?step=1 — venue name visible
3. Complete steps 1–5; on step 6 preview — PaikkaKortti and DiagonaalKortti render with entered data
4. Click "Lähetä hyväksyttäväksi" — redirects to /business; /business shows pending state
5. Supabase: onboarding_draft row deleted; business_accounts.onboarding_completed = true; liikuntapaikat updated
</verification>

<success_criteria>
- StepYhteystiedot: 4 optional contact fields; description maxLength=300 with red counter at limit; saves { field: 'yhteystiedot', value: { puhelin, email, website, kuvaus } }
- StepEsikatselu: PaikkaKortti + DiagonaalKortti + simplified sheet card with draft data; no PaikkaSheet import; "Lähetä hyväksyttäväksi" POSTs to /api/business/onboarding/submit and redirects to /business on success
- OnboardingWizardInner: all 6 steps wired with correct props; no null stubs
- Full unit test suite passes: npx vitest run exits 0
- npx tsc --noEmit passes
</success_criteria>

<output>
Create .planning/phases/34-onboarding-velhou/34-09-SUMMARY.md when done
</output>
