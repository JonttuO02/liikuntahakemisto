---
phase: 34-onboarding-velhou
plan: 10
type: execute
wave: 10
depends_on: ["34-09"]
files_modified:
  - app/components/PaikkaSheet.tsx
  - app/business/onboarding/StepEsikatselu.tsx
autonomous: true
requirements:
  - ONBOARD-07

must_haves:
  truths:
    - "Step 6 preview renders PaikkaSheet alongside PaikkaKortti and DiagonaalKortti"
    - "PaikkaSheet in preview mode does not use position fixed and does not overlay the wizard"
    - "PaikkaSheet in preview mode does not load live reviews from Supabase"
    - "PaikkaSheet in preview mode does not show a drag handle and cannot be dragged closed"
    - "PaikkaSheet in preview mode renders inline, scrollable within the wizard container"
  artifacts:
    - path: "app/components/PaikkaSheet.tsx"
      provides: "preview prop on Props interface"
      contains: "preview?: boolean"
    - path: "app/business/onboarding/StepEsikatselu.tsx"
      provides: "PaikkaSheet import and render"
      contains: "PaikkaSheet"
  key_links:
    - from: "app/business/onboarding/StepEsikatselu.tsx"
      to: "app/components/PaikkaSheet.tsx"
      via: "named import + JSX render with preview prop"
      pattern: "PaikkaSheet.*preview"
    - from: "PaikkaSheet preview mode"
      to: "Supabase reviews query"
      via: "useEffect skipped when preview is true"
      pattern: "if.*preview.*return"
---

<objective>
Close gap ONBOARD-07: add a preview prop to PaikkaSheet that renders it inline (not fixed-position) for use inside the wizard, then wire it into StepEsikatselu replacing the simplified inline glass card.

Purpose: REQUIREMENTS.md ONBOARD-07 and CONTEXT.md D-17 both require PaikkaSheet in the Step 6 preview. The current implementation substitutes a minimal inline card that satisfies neither the requirement text nor the user decision.

Output: PaikkaSheet gains a preview?: boolean prop that switches positioning, height, drag, close button, and review loading. StepEsikatselu imports and renders PaikkaSheet with preview={true}.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-onboarding-velhou/34-CONTEXT.md

<interfaces>
<!-- Extracted from app/components/PaikkaSheet.tsx -->

Current Props interface (lines 16-22):
interface Props {
  paikka: Liikuntapaikka
  todo: boolean
  distanceKm?: number
  onClose: () => void
  onToggleTodo: (id: number) => void
}

Current motion.div style (lines 62-68):
style={{
  position: 'fixed',
  left: 0, right: 0, bottom: 0,
  height: 'calc(100dvh - 116px)',
  zIndex: 66,
  overflow: 'hidden',
}}

Current drag props (lines 70-75):
drag="y"
dragConstraints={{ top: 0, bottom: 0 }}
dragElastic={{ top: 0, bottom: 0.15 }}
onDragEnd={(_, info) => {
  if (info.velocity.y > 400 || info.offset.y > 100) onClose()
}}

Reviews useEffect (lines 41-47):
useEffect(() => {
  const sb = createBrowserSupabase()
  sb.from('reviews')
    .select('id, rating, teksti, is_anonymous, reviewer_name, created_at')
    .eq('paikka_id', paikka.id)
    .order('created_at', { ascending: false })
    .then(({ data }) => setReviews(data ?? []))
}, [paikka.id])

Drag indicator (line 84):
<div className="absolute top-3 left-1/2 -translate-x-1/2 z-20 w-10 h-1 bg-[rgba(255,255,255,0.5)] rounded-full" />

Close button (lines 99-105):
<button
  onClick={onClose}
  aria-label={t('close')}
  className="glass-btn w-8 h-8 rounded-full flex items-center justify-center ..."
>
  <X className="w-4 h-4" />
</button>

<!-- Extracted from app/business/onboarding/StepEsikatselu.tsx -->

Current imports (lines 8-10):
import { buildDraftAsPaikka, type OnboardingDraft, type PaikkaBase } from '@/lib/onboardingUtils'
import PaikkaKortti from '@/app/components/PaikkaKortti'
import DiagonaalKortti from '@/app/components/DiagonaalKortti'

Simplified inline card block (lines 106-127):
{/* PROFIILISIVU — simplified inline card */}
<div className="flex flex-col gap-2">
  <span className="text-[10px] font-bold uppercase tracking-widest text-[rgba(17,17,17,0.45)]">
    {t('previewLabelSheet')}
  </span>
  <div className="glass rounded-2xl p-4 flex flex-col gap-3">
    <h3 ...>{draftAsPaikka.nimi}</h3>
    {draftAsPaikka.hinta_kuvaus && <p ...>{draftAsPaikka.hinta_kuvaus}</p>}
    {draftAsPaikka.kuvaus && <p ...>{draftAsPaikka.kuvaus}</p>}
    {draftAsPaikka.puhelin && <p ...>{draftAsPaikka.puhelin}</p>}
  </div>
</div>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add preview prop to PaikkaSheet</name>
  <files>app/components/PaikkaSheet.tsx</files>

  <read_first>
    - app/components/PaikkaSheet.tsx — read in full before any edits; all line numbers referenced below are from the current file
  </read_first>

  <action>
Modify app/components/PaikkaSheet.tsx to accept a preview?: boolean prop and gate four behaviors on it.

1. Extend the Props interface (currently lines 16-22) to add preview?: boolean. Also make onClose optional with a default no-op: change onClose: () => void to onClose?: () => void. The function signature destructuring becomes: { paikka, todo, distanceKm, onClose = () => {}, onToggleTodo, preview = false }.

2. Reviews useEffect (currently lines 41-47): wrap the entire Supabase query inside an early-return guard — if (preview) { setReviews([]); return; }. Place this as the first line of the useEffect body, before creating the Supabase client. This ensures no DB call is made in preview mode and the reviews section shows the "no reviews" empty state without querying.

3. motion.div style (currently lines 62-68): replace the static style object with a conditional. When preview is false keep existing style (position: 'fixed', left: 0, right: 0, bottom: 0, height: 'calc(100dvh - 116px)', zIndex: 66, overflow: 'hidden'). When preview is true use: { position: 'relative', height: 'auto', maxHeight: '600px', overflow: 'hidden', borderRadius: '1rem' }. Use a ternary: style={preview ? { position: 'relative', height: 'auto', maxHeight: '600px', overflow: 'hidden', borderRadius: '1rem' } : { position: 'fixed', left: 0, right: 0, bottom: 0, height: 'calc(100dvh - 116px)', zIndex: 66, overflow: 'hidden' }}.

4. Drag props (currently lines 70-75): disable all drag-related props when preview is true. Add drag={preview ? false : 'y'} and conditionally include dragConstraints, dragElastic, and onDragEnd only when !preview. The cleanest approach: keep drag="y" dragConstraints dragElastic onDragEnd as-is but add drag={preview ? false : ('y' as const)} and wrap the three drag callback props in spread: {...(!preview && { dragConstraints: { top: 0, bottom: 0 }, dragElastic: { top: 0, bottom: 0.15 }, onDragEnd: (_: unknown, info: { velocity: { y: number }; offset: { y: number } }) => { if (info.velocity.y > 400 || info.offset.y > 100) onClose() } })}. Remove the original dragConstraints, dragElastic, onDragEnd attributes and replace with this spread pattern.

5. Drag indicator (line 84 — the white pill at top-center of hero): hide when preview is true. Wrap with {!preview && <div className="absolute top-3 left-1/2 ..." />}.

6. Close button (lines 99-105 — the X button in the hero top-right area): hide when preview is true. Wrap with {!preview && <button onClick={onClose} ...>...</button>}. Keep the bookmark button (onToggleTodo) visible — it is unaffected by preview mode.

Do NOT change anything else — SheetRow, HoursTable, ReviewSection, pricing display, hours display, phone, booking link, or description sections remain untouched.
  </action>

  <verify>
    <automated>grep -n "preview" app/components/PaikkaSheet.tsx</automated>
  </verify>

  <acceptance_criteria>
    - PaikkaSheet.tsx Props interface contains: preview?: boolean
    - PaikkaSheet.tsx Props interface contains: onClose?: () => void (optional, not required)
    - PaikkaSheet.tsx function signature destructures preview with default false and onClose with default no-op
    - PaikkaSheet.tsx reviews useEffect first line contains an early-return when preview is true that calls setReviews([])
    - PaikkaSheet.tsx motion.div style is conditional: position 'relative' when preview, position 'fixed' when not preview
    - PaikkaSheet.tsx motion.div does not have unconditional drag="y" — drag prop evaluates to false when preview is true
    - PaikkaSheet.tsx drag indicator pill (w-10 h-1 ... rounded-full) is wrapped in !preview guard
    - PaikkaSheet.tsx close button (X icon) is wrapped in !preview guard
    - PaikkaSheet.tsx has no new fenced code blocks or import changes — only the six targeted modifications above
  </acceptance_criteria>

  <done>PaikkaSheet accepts preview?: boolean; in preview mode it renders inline (position relative, max-h-[600px]), skips Supabase review query, disables drag, and hides the drag indicator and close button.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire PaikkaSheet into StepEsikatselu</name>
  <files>app/business/onboarding/StepEsikatselu.tsx</files>

  <read_first>
    - app/business/onboarding/StepEsikatselu.tsx — read in full before any edits; the simplified inline card block is at lines 106-127
    - app/components/PaikkaSheet.tsx — confirm the updated Props interface from Task 1 before referencing it here
  </read_first>

  <action>
Modify app/business/onboarding/StepEsikatselu.tsx to replace the simplified inline card with PaikkaSheet rendered in preview mode.

1. Add the PaikkaSheet import after the existing DiagonaalKortti import line (currently line 10):
   import PaikkaSheet from '@/app/components/PaikkaSheet'

2. Remove the NOTE comment block (lines 12-15) that explains why PaikkaSheet is not imported. It is now imported.

3. Replace the entire PROFIILISIVU section (currently lines 106-127) with:
   A wrapper div with className="flex flex-col gap-2" containing:
   - The existing label span: text-[10px] font-bold uppercase tracking-widest text-[rgba(17,17,17,0.45)] with {t('previewLabelSheet')} content
   - A PaikkaSheet element with props: paikka={draftAsPaikka} preview={true} todo={false} onClose={() => {}} onToggleTodo={() => {}}

   The PaikkaSheet element must use the preview prop as a boolean attribute (preview not preview={true} is acceptable JSX shorthand, but preview={true} is also correct — either form is fine).

Do NOT change anything else in StepEsikatselu.tsx — the handleSubmit function, loading/error state, PaikkaKortti section, DiagonaalKortti section, AnimatePresence error block, and footer buttons are untouched.
  </action>

  <verify>
    <automated>grep -n "PaikkaSheet" app/business/onboarding/StepEsikatselu.tsx</automated>
  </verify>

  <acceptance_criteria>
    - StepEsikatselu.tsx imports PaikkaSheet from '@/app/components/PaikkaSheet'
    - StepEsikatselu.tsx does NOT contain the simplified inline glass card (the div with h3 for draftAsPaikka.nimi and p tags for hinta_kuvaus/kuvaus/puhelin)
    - StepEsikatselu.tsx renders PaikkaSheet with paikka={draftAsPaikka}
    - StepEsikatselu.tsx renders PaikkaSheet with preview (boolean prop present)
    - StepEsikatselu.tsx renders PaikkaSheet with todo={false}
    - StepEsikatselu.tsx renders PaikkaSheet with onClose={() => {}}
    - StepEsikatselu.tsx renders PaikkaSheet with onToggleTodo={() => {}}
    - StepEsikatselu.tsx does NOT contain the NOTE comment about PaikkaSheet using fixed positioning
    - StepEsikatselu.tsx still imports and renders PaikkaKortti and DiagonaalKortti (unchanged)
    - TypeScript compiles without errors: run npx tsc --noEmit and confirm no new errors in these two files
  </acceptance_criteria>

  <done>StepEsikatselu renders PaikkaSheet with preview={true} in the profile preview section. The simplified inline card is gone. ONBOARD-07 requirement satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard UI → PaikkaSheet | preview prop is a boolean passed from the wizard parent; no external input, no sanitization needed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-10-01 | Information Disclosure | PaikkaSheet preview — Supabase review query | mitigate | Skip the reviews query entirely when preview=true; setReviews([]) immediately in useEffect early return — no DB call, no data exposure |
| T-34-10-02 | Tampering | PaikkaSheet onClose prop | accept | onClose defaults to no-op in preview mode; no state mutation path; low risk |
</threat_model>

<verification>
After both tasks complete:

1. TypeScript check — run in project root:
   npx tsc --noEmit
   Expected: zero new type errors in PaikkaSheet.tsx or StepEsikatselu.tsx.

2. Grep check — PaikkaSheet prop added:
   grep -n "preview" app/components/PaikkaSheet.tsx
   Expected: lines for the interface declaration, function parameter default, useEffect guard, style conditional, drag conditional, drag indicator guard, close button guard.

3. Grep check — inline card removed:
   grep -c "draftAsPaikka.nimi" app/business/onboarding/StepEsikatselu.tsx
   Expected: 0 (the inline h3 tag referencing draftAsPaikka.nimi is gone).

4. Grep check — PaikkaSheet wired:
   grep -n "PaikkaSheet" app/business/onboarding/StepEsikatselu.tsx
   Expected: at least 2 lines (import line + JSX render line).

5. Grep check — NOTE comment removed:
   grep -c "position:fixed" app/business/onboarding/StepEsikatselu.tsx
   Expected: 0.
</verification>

<success_criteria>
- PaikkaSheet.tsx: preview?: boolean prop exists; in preview mode position is relative not fixed; reviews useEffect skips DB query; drag is disabled; drag indicator and close button are hidden
- StepEsikatselu.tsx: imports PaikkaSheet; renders it with preview todo={false} onClose={() => {}} onToggleTodo={() => {}}; simplified inline card is removed
- ONBOARD-07 gap closed: Step 6 now renders PaikkaKortti + DiagonaalKortti + PaikkaSheet as required by both REQUIREMENTS.md and CONTEXT.md D-17
- TypeScript compiles clean
</success_criteria>

<output>
Create `.planning/phases/34-onboarding-velhou/34-10-SUMMARY.md` when done.
</output>
