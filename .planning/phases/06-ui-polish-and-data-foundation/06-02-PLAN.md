---
phase: 06-ui-polish-and-data-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/tietosuoja/page.tsx
autonomous: true
requirements:
  - LEGAL-01
must_haves:
  truths:
    - "User can navigate to /tietosuoja and read the GDPR privacy policy before logging in"
    - "The page renders as a server component with no 'use client' directive"
    - "Six prose sections appear in the order specified by D-04"
  artifacts:
    - path: "app/tietosuoja/page.tsx"
      provides: "Static GDPR privacy page (Tietosuojaseloste)"
      contains: "Tietosuojaseloste"
      min_lines: 40
  key_links:
    - from: "app/tietosuoja/page.tsx"
      to: "Next.js App Router /tietosuoja route"
      via: "default exported React component"
      pattern: "export default function"
---

<objective>
Create the GDPR privacy page at `app/tietosuoja/page.tsx` — pure server component, no data fetching, six prose sections per D-04 with placeholder tokens for controller identity (D-01) and contact email (D-02). Page is reachable at `/tietosuoja` and must be live before Phase 9 (auth) ships.

Purpose: Satisfies LEGAL-01. Listed in CLAUDE.md key constraints: "LEGAL-01 must be live (Phase 6) before auth (Phase 9) ships." Phase 6 builds the page; Plan 06 wires the footer link.

Output: One new file `app/tietosuoja/page.tsx` containing the GDPR prose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md
@.planning/phases/06-ui-polish-and-data-foundation/06-RESEARCH.md
@.planning/phases/06-ui-polish-and-data-foundation/06-PATTERNS.md
@.planning/phases/06-ui-polish-and-data-foundation/06-UI-SPEC.md
@CLAUDE.md
@app/paikat/[id]/page.tsx

<interfaces>
From app/paikat/[id]/page.tsx (closest analog — server-component page shell):
- Default export: `export default async function PaikkaPage({ params }: ...)` — async because it fetches; the new tietosuoja page is non-async (no fetch)
- Page shell: `<div className="min-h-screen bg-white">` wrapper with `max-w-2xl mx-auto px-4` content container
- Display heading: `<h1 className="font-serif text-3xl ... font-bold text-[#111111] leading-tight tracking-tight">`
- Uses Inter (sans) + system serif (`font-serif`) — same project font system

Style tokens from CLAUDE.md (do not redefine):
- Page bg: `bg-white`
- Foreground: `text-[#111111]`, muted body: `text-[rgba(17,17,17,0.65)] leading-relaxed`
- Section H2: `text-sm font-bold text-[#111111] uppercase tracking-widest mt-8 mb-2` (or `font-serif text-xl font-bold ...` per UI-SPEC variant — choose one consistently)
- No glassmorphism, no hero section (per UI-SPEC LEGAL-01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static GDPR page at app/tietosuoja/page.tsx</name>
  <files>app/tietosuoja/page.tsx</files>
  <read_first>
    - app/paikat/[id]/page.tsx (role-match analog — page shell, max-w-2xl container, h1 styling, font-serif pattern)
    - app/components/NavBar.tsx (so the back-link/Link import pattern matches the project)
    - .planning/phases/06-ui-polish-and-data-foundation/06-UI-SPEC.md (LEGAL-01 section — exact layout, classes, six section list)
    - .planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md (D-01 to D-05 — placeholder tokens and scope)
    - CLAUDE.md (color tokens, typography rules: 4 sizes only, 400/700 only, no 600)
  </read_first>
  <action>
    Create a new file `app/tietosuoja/page.tsx` as a pure server component. Do NOT add `'use client'`. Do NOT import `supabase` or fetch any data. Default export is a non-async function named `TietosuojaPage` that returns the page JSX.

    Page shell: `<div className="min-h-screen bg-white">` wrapping `<div className="max-w-2xl mx-auto px-4 pt-10 pb-16">` (per UI-SPEC LEGAL-01 layout block).

    Heading: a single `<h1 className="font-serif text-3xl font-bold text-[#111111]">Tietosuojaseloste</h1>` followed immediately by an "updated date" line `<p className="text-sm text-[rgba(17,17,17,0.45)] mt-1">Päivitetty 22.5.2026</p>` (UI-SPEC LEGAL-01 layout block lists this `<p>`).

    Add a back link at the top (above h1) using `next/link` to `/` with `ChevronLeft` from `lucide-react`. Style consistent with the project muted text pattern (`text-sm text-[rgba(17,17,17,0.45)] hover:text-[#111111] [transition:color_150ms_var(--ease-out)]`).

    Body: six `<section>` blocks in the order locked by D-04. Each section uses `<h2 className="font-serif text-xl font-bold text-[#111111] mt-8 mb-3">` followed by one or more `<p className="text-sm text-[rgba(17,17,17,0.65)] leading-relaxed mb-4">`. Section order and headings:
      1. Rekisterinpitäjä — body uses the literal placeholder token `[Rekisterinpitäjä]` (per D-01, do NOT replace with a real name).
      2. Mitä tietoja kerätään — state honestly that the service currently collects no personal data; sessionStorage is used only for the AI widget cache (D-03).
      3. Evästeet ja selaintallennus — clarify sessionStorage usage (AI widget cache, key scoped to calendar day YYYY-MM-DD); no third-party cookies.
      4. Käyttäjän oikeudet — describe GDPR rights (access, deletion, rectification) in general terms; mention that since no personal data is currently stored, no requests apply yet.
      5. Yhteydenotot — include the literal placeholder token `[yhteyssähköposti@esimerkki.fi]` (per D-02, do NOT invent an email).
      6. Muutokset — note that the policy will be updated when user accounts are added in Phase 9 (per D-03).

    Do NOT describe planned auth features as if they are live (D-03 explicit prohibition). Do NOT use `dangerouslySetInnerHTML` anywhere. All Finnish copy. Typography rules: only `font-bold` (700) and default (400); no `font-semibold` (600). Sizes restricted to `text-[10px]`, `text-sm`, `text-xl`, `text-3xl` (4 sizes per CLAUDE.md).

    Use only typed JSX — no TypeScript-language extensions beyond standard React.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/tietosuoja/page.tsx','utf8'); const must=['Tietosuojaseloste','Rekisterinpitäjä','[Rekisterinpitäjä]','[yhteyssähköposti@esimerkki.fi]','Evästeet','Käyttäjän oikeudet','Yhteydenotot','Muutokset','export default function']; for(const t of must){ if(!s.includes(t)) throw new Error('missing: '+t) } if(s.includes('use client')) throw new Error('must NOT be a client component'); if(s.includes('dangerouslySetInnerHTML')) throw new Error('dangerouslySetInnerHTML forbidden'); if(s.includes('font-semibold')) throw new Error('font-semibold forbidden (use font-bold)'); console.log('OK')"</automated>
  </verify>
  <done>File `app/tietosuoja/page.tsx` exists, exports a non-async default function, has zero `'use client'` directives, contains all six section headings in order, contains both placeholder tokens verbatim, compiles with `npx tsc --noEmit`, and `npx next build` produces a route for `/tietosuoja`.</done>
  <acceptance_criteria>
    - Source assertion: app/tietosuoja/page.tsx exists
    - Source assertion: file does NOT contain the substring `'use client'`
    - Source assertion: file contains literal `Tietosuojaseloste`, `[Rekisterinpitäjä]`, and `[yhteyssähköposti@esimerkki.fi]`
    - Source assertion: file contains all six Finnish section keywords: `Rekisterinpitäjä`, `Mitä tietoja kerätään`, `Evästeet`, `Käyttäjän oikeudet`, `Yhteydenotot`, `Muutokset`
    - Source assertion: file does NOT contain `dangerouslySetInnerHTML` or `font-semibold`
    - Build assertion: `npx tsc --noEmit` exits 0
    - Behavior assertion: `npx next build` lists `/tietosuoja` in the route output
    - Behavior assertion (manual smoke): `npx next dev` then `curl -s http://localhost:3000/tietosuoja | grep -q Tietosuojaseloste`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous → Server | Any visitor can GET /tietosuoja — no auth, no input parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering (XSS) | GDPR prose page | mitigate | Pure server component with no `dangerouslySetInnerHTML`; React escapes all string content. Validated by acceptance criterion forbidding `dangerouslySetInnerHTML` in the source. |
| T-06-04 | Information Disclosure | Placeholder tokens `[Rekisterinpitäjä]` and `[yhteyssähköposti@esimerkki.fi]` | accept | Tokens are public placeholders agreed in D-01/D-02; user fills before Phase 9 ships. No PII at this stage. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx next build` includes `/tietosuoja` in route listing
- Manual smoke (one-shot): start dev server, visit /tietosuoja, see "Tietosuojaseloste" heading and six section headings in order
</verification>

<success_criteria>
GET /tietosuoja returns 200 with a server-rendered HTML page titled "Tietosuojaseloste" containing six prose sections in the order locked by D-04, including the two literal placeholder tokens.
</success_criteria>

<output>
Create `.planning/phases/06-ui-polish-and-data-foundation/06-02-SUMMARY.md` when done.
</output>
