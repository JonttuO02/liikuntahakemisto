---
phase: 06-ui-polish-and-data-foundation
plan: 07
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - app/components/Etusivu.tsx
autonomous: true
requirements:
  - AI-04
  - ADS-02
must_haves:
  truths:
    - "The AI weather widget displays the city name (Tampere) as muted text immediately after the temperature reading"
    - "When the map bottom-sheet shows a featured venue, the Sponsoroitu amber badge appears in the badge row independently from PaikkaKortti (Pitfall 1)"
    - "A WEATHER_CITY constant is defined at the top of Etusivu.tsx to make Phase 10 update trivial (D-24)"
    - "The bottom-sheet Varaa → CTA remains unchanged (D-17)"
  artifacts:
    - path: "app/components/Etusivu.tsx"
      provides: "WEATHER_CITY constant, city name in weather row, Sponsoroitu badge in bottom-sheet"
      contains: "WEATHER_CITY"
  key_links:
    - from: "Etusivu.tsx weather row"
      to: "WEATHER_CITY constant"
      via: "JSX expression after saa.temp"
      pattern: "WEATHER_CITY"
    - from: "Etusivu.tsx bottom-sheet badge row"
      to: "valittu.featured boolean"
      via: "conditional render guard"
      pattern: "valittu\\.featured"
---

<objective>
Apply two coordinated edits to `app/components/Etusivu.tsx`:
  1. Add a top-level `WEATHER_CITY` constant (Tampere) and render it as muted inline text immediately after `{saa.temp}°` in the AI widget weather row (AI-04, D-24).
  2. Add the Sponsoroitu amber badge to the bottom-sheet badge area when `valittu.featured` is true — independently from PaikkaKortti per Pitfall 1 / D-07 (ADS-02 map portion). The bottom-sheet Varaa → CTA is NOT touched (D-17).

Purpose: Closes AI-04 entirely and closes the map half of ADS-02. Depends on Plan 01 (`featured` reaches the client through `paikat`, and `valittu` is selected from that array).

Output: One file modified. No new imports needed (lucide icons used are already imported in Etusivu.tsx; the amber badge is a plain span).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md
@.planning/phases/06-ui-polish-and-data-foundation/06-RESEARCH.md
@.planning/phases/06-ui-polish-and-data-foundation/06-PATTERNS.md
@.planning/phases/06-ui-polish-and-data-foundation/06-UI-SPEC.md
@CLAUDE.md
@app/components/Etusivu.tsx
@lib/types.ts

<interfaces>
From lib/types.ts (Liikuntapaikka):
  - featured?: boolean | null  (now reaches client after Plan 01)

Existing Etusivu.tsx structure (verified via PATTERNS.md):
  - 'use client' at top
  - Imports framer-motion, lucide-react icons (X, MapPin, Moon, Sun, etc.), useState/useEffect/useMemo
  - Weather display block at lines ~186–195: a flex row with the weather emoji span + the temperature span
  - Bottom-sheet for selected venue (valittu) is inline JSX at lines ~448–503
  - Inside the bottom-sheet, the sport pill is rendered via an IIFE that returns a single span (lines ~448–461) inside a wrapping `<div className="px-5 pt-2 pb-2">`
  - The bottom-sheet Varaa → CTA appears later (lines ~485–502) — DO NOT change it per D-17

WEATHER_CITY constant placement:
  - After the NAV_H constant declaration near the top of the file (per CONTEXT specifics)
  - `const WEATHER_CITY = 'Tampere'`

City label JSX (per UI-SPEC AI-04 and D-24):
  - Inserted inline within the existing temperature span: `{saa.temp}° <span className="font-normal text-[rgba(17,17,17,0.45)]">{WEATHER_CITY}</span>`

Sponsoroitu badge JSX (identical to PaikkaKortti badge per UI-SPEC ADS-02):
  - `<span className="inline-flex items-center gap-1 text-[10px] font-bold px-2 py-1 rounded-full bg-amber-100 text-amber-700 border border-amber-200">Sponsoroitu</span>`
  - Inserted as a sibling immediately after the sport-pill IIFE closing `})()`, inside the wrapping `<div className="px-5 pt-2 pb-2">`
  - Add a `ml-1.5` modifier on the Sponsoroitu span so it does not visually butt into the sport pill (matches PATTERNS.md snippet)

Typography rules (CLAUDE.md):
  - 400 and 700 only — the city label uses `font-normal` (= 400), the rest of the temperature span stays `font-bold` (700)
  - Do NOT change the existing temperature span weight from `font-semibold` if it is currently `font-semibold` — the UI-SPEC typography update is enforced where this plan TOUCHES the line. Convert the temperature span's `font-semibold` to `font-bold` (700) when editing the line.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WEATHER_CITY constant and render city name after temperature (AI-04)</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    - app/components/Etusivu.tsx (full file — locate `const NAV_H` near top, weather display block at lines ~186–195)
    - .planning/phases/06-ui-polish-and-data-foundation/06-PATTERNS.md (Etusivu section — exact constant placement and weather span replacement)
    - .planning/phases/06-ui-polish-and-data-foundation/06-UI-SPEC.md (AI-04 visual contract — class names, font weight)
    - .planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md (D-24 — WEATHER_CITY constant naming, hardcoded Tampere)
    - CLAUDE.md (typography 400/700 only — replace any font-semibold encountered on touched lines)
  </read_first>
  <action>
    1) Add a new top-level constant `const WEATHER_CITY = 'Tampere'` immediately after the existing `const NAV_H = 56` declaration (or wherever the file currently declares its top-level constants). Keep the export structure of the file unchanged.

    2) Locate the temperature span inside the weather display block (the `<span className="text-sm font-semibold text-[#111111] tabular-nums">{saa.temp}°</span>` element). Replace it with the AI-SPEC-compliant variant:
       - Outer span className becomes `text-sm font-bold text-[#111111] tabular-nums`
       - Inner content: `{saa.temp}°` followed by a single space (JSX expression `{' '}`) followed by a nested `<span className="font-normal text-[rgba(17,17,17,0.45)]">{WEATHER_CITY}</span>`

    3) Do NOT change the weather emoji span, the surrounding flex row, or any other line in the file in this task. Do NOT introduce a translation or i18n lookup — D-24 hardcodes Tampere.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/components/Etusivu.tsx','utf8'); if(!/const WEATHER_CITY = ['\"]Tampere['\"]/.test(s)) throw new Error('WEATHER_CITY constant missing'); if(!/\{WEATHER_CITY\}/.test(s)) throw new Error('WEATHER_CITY not rendered in JSX'); if(!/font-normal text-\[rgba\(17,17,17,0\.45\)\]/.test(s)) throw new Error('muted city label class missing'); console.log('OK')"</automated>
  </verify>
  <done>WEATHER_CITY constant declared at module scope; the temperature span renders `{saa.temp}°` immediately followed by the muted city name. `npx tsc --noEmit` exits 0.</done>
  <acceptance_criteria>
    - Source assertion: grep `const WEATHER_CITY = 'Tampere'` returns 1 match
    - Source assertion: grep `{WEATHER_CITY}` returns at least 1 match (rendered in JSX)
    - Source assertion: the temperature span uses className containing `font-bold` (not `font-semibold`)
    - Source assertion: the inner span containing WEATHER_CITY uses className containing `font-normal` and `text-[rgba(17,17,17,0.45)]`
    - Build assertion: `npx tsc --noEmit` exits 0
    - Behavior assertion (manual smoke): on the homepage (map view), the AI weather widget shows e.g. `7° Tampere` with the city in muted gray
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Sponsoroitu badge to map bottom-sheet (ADS-02 map portion)</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    - app/components/Etusivu.tsx (bottom-sheet block ~lines 448–503; sport-pill IIFE returns a single span; the wrapping `<div className="px-5 pt-2 pb-2">` contains the IIFE)
    - .planning/phases/06-ui-polish-and-data-foundation/06-PATTERNS.md (Etusivu section — Bottom-sheet badge insertion snippet, sibling placement after the IIFE)
    - .planning/phases/06-ui-polish-and-data-foundation/06-UI-SPEC.md (ADS-02 — same badge class as PaikkaKortti; Etusivu bottom-sheet placement)
    - .planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md (D-06, D-07 — independent from PaikkaKortti; D-17 — Varaa → CTA unchanged)
    - .planning/phases/06-ui-polish-and-data-foundation/06-RESEARCH.md (Pitfall 1 — badge must be added independently here, not via PaikkaKortti)
  </read_first>
  <action>
    1) Within the bottom-sheet `<div className="px-5 pt-2 pb-2">` wrapper that already contains the sport-pill IIFE `{(() => { ... return (<span ...>{laji.label}</span>); })()}`, append (as a sibling immediately AFTER the IIFE closing `})()`) the following conditional JSX:

        {valittu.featured && (
          <span className="inline-flex items-center gap-1 text-[10px] font-bold px-2 py-1 rounded-full bg-amber-100 text-amber-700 border border-amber-200 ml-1.5">
            Sponsoroitu
          </span>
        )}

    2) Do NOT alter the venue name `<h2>`, the address `<p>`, or the existing Varaa → CTA (D-17 prohibits). Do NOT refactor the IIFE — keep the existing structure. Do NOT replace the bottom-sheet inline JSX with `PaikkaKortti` (D-07 keeps them independent).

    3) Verify that `valittu` is the variable name used in the bottom-sheet for the selected venue. If the variable has a different name in the current file, use the existing name (do NOT rename).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/components/Etusivu.tsx','utf8'); const sponsoroitu=s.indexOf('Sponsoroitu'); const varaa=s.indexOf('Varaa →'); if(sponsoroitu===-1) throw new Error('Sponsoroitu badge missing'); if(!/valittu\.featured/.test(s)) throw new Error('valittu.featured guard missing'); if(!/bg-amber-100/.test(s)) throw new Error('amber-100 badge class missing'); if(varaa===-1) throw new Error('Varaa → CTA must remain (D-17 prohibits removal)'); console.log('OK')"</automated>
  </verify>
  <done>The bottom-sheet renders a Sponsoroitu amber badge guarded by `valittu.featured`, the Varaa → CTA remains intact, and `npx tsc --noEmit` exits 0.</done>
  <acceptance_criteria>
    - Source assertion: grep `Sponsoroitu` in Etusivu.tsx returns at least 1 match
    - Source assertion: grep `valittu.featured` returns at least 1 match
    - Source assertion: grep `bg-amber-100` returns at least 1 match (badge styling)
    - Source assertion: grep `Varaa →` in Etusivu.tsx returns at least 1 match (CTA preserved per D-17)
    - Build assertion: `npx tsc --noEmit` exits 0
    - Behavior assertion (manual smoke): tap a featured venue's map pin → bottom-sheet rises → amber Sponsoroitu badge is visible next to the sport pill; the Varaa → CTA still works
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → Client (Etusivu) | valittu.featured and saa.temp flow from server into client JSX |
| External weather API → Server route | /api/saasuositus fetches weather; not modified in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Tampering (XSS) | WEATHER_CITY constant rendered in JSX | mitigate | Constant is a compile-time string literal (`'Tampere'`); React text node auto-escapes. No user input on this path. |
| T-06-15 | Tampering | valittu.featured controls Sponsoroitu badge visibility | accept | Boolean flag; RLS keeps anon role read-only; no privilege granted by badge visibility. |
| T-06-16 | Information Disclosure | Bottom-sheet exposes featured venues to anonymous map users | accept | Intentional — ADS-02 explicitly requires public visibility of sponsored badges. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` exits 0 (lib tests still green; no Etusivu-specific test)
- grep assertions per acceptance criteria pass
- Manual smoke: homepage shows `{temp}° Tampere`; tapping a featured pin shows the Sponsoroitu badge in the bottom-sheet; Varaa → CTA still present
</verification>

<success_criteria>
The AI weather widget shows the city name next to the temperature; the map bottom-sheet shows the Sponsoroitu amber badge for featured venues; the existing Varaa → CTA in the bottom-sheet is unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-ui-polish-and-data-foundation/06-07-SUMMARY.md` when done.
</output>
