---
phase: 06-ui-polish-and-data-foundation
plan: 05
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-04
files_modified:
  - app/components/PaikkaKortti.tsx
autonomous: true
requirements:
  - UI-05
  - UI-06
  - UI-07
  - ADS-02
must_haves:
  truths:
    - "Featured venues render a Sponsoroitu amber badge in the badge row of every list card"
    - "Price block sits at card position 4 (after open-status, before address) and renders walk-in price, multi-line price, 'vain jäsenyys', or 'Lisätään pian' per D-11/D-12/D-13"
    - "List cards always render the outlined 'Näytä tiedot' link as CTA, never the 'Varaa aika →' button (UI-07)"
    - "Distance string remains on the right side of the bottom row when GPS is granted"
  artifacts:
    - path: "app/components/PaikkaKortti.tsx"
      provides: "Updated card with Sponsoroitu badge, price-at-top, no Varaa aika button"
      contains: "Sponsoroitu"
  key_links:
    - from: "PaikkaKortti.tsx"
      to: "lib/priceUtils.ts (isMembershipOnly)"
      via: "named import"
      pattern: "isMembershipOnly"
    - from: "PaikkaKortti.tsx"
      to: "paikka.featured boolean"
      via: "conditional render guard"
      pattern: "paikka\\.featured"
---

<objective>
Apply four interlocking edits to `app/components/PaikkaKortti.tsx`:
  1. Add "Sponsoroitu" amber badge to the badge row after "Kertakäynti OK" (ADS-02 list portion, D-08).
  2. Move the price display from the bottom row to a new position 4 (between open-status and address), implement multi-line price split, and use the `isMembershipOnly` helper to render the muted "vain jäsenyys" fallback only when D-11 evidence exists (UI-05, UI-06, D-10..D-13).
  3. Remove the entire `varauslinkki`-conditional "Varaa aika →" branch from the bottom row; always render the outlined "Näytä tiedot" Link (UI-07 list portion, D-15).
  4. Update the bottom row to contain only the CTA and the distance string (D-14).

Purpose: Closes UI-05, UI-06, UI-07 (list half), and ADS-02 (list half) in a single coherent edit so the card layout is consistent and reviewable. Depends on Plan 01 (`featured` reaches client) and Plan 04 (`isMembershipOnly` exists with tests).

Output: One file modified. New named import from `@/lib/priceUtils`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md
@.planning/phases/06-ui-polish-and-data-foundation/06-RESEARCH.md
@.planning/phases/06-ui-polish-and-data-foundation/06-PATTERNS.md
@.planning/phases/06-ui-polish-and-data-foundation/06-UI-SPEC.md
@CLAUDE.md
@app/components/PaikkaKortti.tsx
@lib/priceUtils.ts
@lib/utils.ts
@lib/types.ts

<interfaces>
From lib/priceUtils.ts (provided by Plan 04):
  - isMembershipOnly(p: Pick<Liikuntapaikka,'hinta_kuvaus'|'hinta_min'|'hinta_max'>): boolean

From lib/utils.ts (existing):
  - hintateksti(min: number | null, max: number | null): string

From lib/types.ts (Liikuntapaikka):
  - featured?: boolean | null
  - hinta_min / hinta_max: number | null
  - hinta_kuvaus: string | null
  - varauslinkki: string | null

New card section order after this plan (D-10):
  1. Badge row: [sport pill] [Sponsoroitu? amber] [Kertakäynti OK?]
  2. Venue name (Link)
  3. Open status indicator
  4. Price block (new position; replaces former bottom-row price)
  5. Address row (existing)
  6. Description (line-clamp-2, existing)
  7. Bottom row: outlined "Näytä tiedot" CTA + optional distance string

Existing animation contract (do not change):
  - whileHover scale 1.02 (or y:-2 per UI-SPEC — keep whatever PaikkaKortti currently uses; this plan does not touch the motion wrapper)
  - whileTap scale 0.97 on the CTA Link

Token tightening from UI-SPEC § Spacing Scale (apply where applicable):
  - Sponsoroitu badge padding: `py-1` (4px) — NOT `py-0.5`
  - Bottom-row separator: `pt-3` (12px) replaces previous `pt-2.5`
  - Typography: `font-bold` (700) and default (400) only; do NOT introduce `font-semibold` or `font-medium`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply ADS-02 badge, UI-05/06 price-at-top, UI-07 CTA simplification to PaikkaKortti</name>
  <files>app/components/PaikkaKortti.tsx</files>
  <read_first>
    - app/components/PaikkaKortti.tsx (full file — current badge row at lines ~56–70, current price derivation at lines ~42–44, current bottom row at lines ~112–148)
    - lib/priceUtils.ts (the new isMembershipOnly helper consumed here)
    - lib/utils.ts (hintateksti — still used for numeric-range fallback)
    - lib/types.ts (Liikuntapaikka field types — featured is `boolean | null | undefined` in the conditional guard)
    - .planning/phases/06-ui-polish-and-data-foundation/06-PATTERNS.md (PaikkaKortti section — exact replacement snippets and section order)
    - .planning/phases/06-ui-polish-and-data-foundation/06-UI-SPEC.md (ADS-02, UI-05, UI-06, UI-07 visual contracts)
    - .planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md (D-08, D-10..D-15)
    - CLAUDE.md (typography: only 400 and 700; never 600; sport colors only from lajiKonfig)
  </read_first>
  <action>
    Edit `app/components/PaikkaKortti.tsx` in place. Maintain the `'use client'` directive, existing motion wrappers, and existing imports for `framer-motion`, `next/link`, lucide icons, `lajiKonfig`, `hintateksti`, `getOpenStatus`, and the `Liikuntapaikka` type.

    1) Add the import: `import { isMembershipOnly } from '@/lib/priceUtils'` (use the project's `@/` alias as already used by other lib imports).

    2) Replace the existing three-line price derivation (`hasDropIn`, `hintaTeksti`, `priceToShow`) with derivations that the new layout uses. Keep `hasDropIn` and `hintaTeksti` since `hasDropIn` still drives the "Kertakäynti OK" badge in the badge row. Replace `priceToShow` logic with:
       - call `isMembershipOnly(paikka)` and assign to a const `membershipOnly`
       - derive `priceLines: string[] | null` = if `paikka.hinta_kuvaus` includes `\n` then split on `\n`, else null
       - derive `priceText: string | null` = `paikka.hinta_kuvaus ?? (hintaTeksti !== '' ? hintaTeksti : null)`

    3) Badge row (after the existing sport-pill span and AFTER the "Kertakäynti OK" badge — per D-08 the order is `[Sport pill] [Sponsoroitu] [Kertakäynti OK]`; PATTERNS.md shows Sponsoroitu placement after Kertakäynti as an alternative; UI-SPEC ADS-02 specifies AFTER Sport pill, BEFORE Kertakäynti OK — follow UI-SPEC which is the design-of-record). Insert the badge between the sport span and the Kertakäynti OK conditional:

        {paikka.featured && (
          <span className="inline-flex items-center gap-1 text-[10px] font-bold px-2 py-1 rounded-full bg-amber-100 text-amber-700 border border-amber-200">
            Sponsoroitu
          </span>
        )}

       Final visual order in the badge row: sport pill (first) → Sponsoroitu (if featured) → Kertakäynti OK (if hasDropIn).

    4) Insert a NEW price block as JSX between the open-status row and the address row (position 4 per D-10). The block renders one of four states in priority order:
       - if `membershipOnly`: render `<span className="text-sm text-[rgba(17,17,17,0.5)]">vain jäsenyys</span>` (muted, D-12)
       - else if `priceLines` is non-null: render a `<span className="text-sm font-bold text-[#111111] tabular-nums">` containing each line wrapped in `<span className="block">{line}</span>` (D-13)
       - else if `priceText` is non-null: render `<span className="text-sm font-bold text-[#111111] tabular-nums">{priceText}</span>`
       - else: render `<span className="text-xs text-[rgba(17,17,17,0.35)]">Lisätään pian</span>` (existing fallback copy)
       Wrap the block in `<div>` so it occupies its own card row.

    5) Bottom row replacement (UI-07, D-14, D-15): remove the entire `paikka.varauslinkki ? <motion.a>Varaa aika →</motion.a> : <motion.div>Näytä tiedot</motion.div>` ternary. Always render the `Näytä tiedot` outlined Link with `whileTap={{ scale: 0.97, transition: { duration: 0.1 } }}`. Remove the bottom-row `<div className="flex flex-col items-end gap-0.5 shrink-0">` block that previously held `priceToShow`. Keep the distance string `distanceStr` on the right side, replicating only the distance span (with `MapPin` icon) from the old block. Outer bottom row becomes a `flex items-center justify-between gap-3 pt-3 border-t border-[rgba(0,0,0,0.07)]` row (note: `pt-3` per UI-SPEC spacing tightening — was `pt-2.5`). Use `mt-auto` to keep CTA pinned to bottom.

    6) Verify the CTA Link text is exactly `Näytä tiedot` and its className uses `font-bold` (not `font-medium` — per UI-SPEC typography). Match the class to UI-SPEC UI-07 PaikkaKortti spec verbatim: `border border-[rgba(0,0,0,0.12)] text-[rgba(17,17,17,0.6)] hover:text-[#111111] hover:border-[rgba(0,0,0,0.25)] text-sm font-bold py-2 px-4 rounded-full [transition:color_150ms_var(--ease-out),border-color_150ms_var(--ease-out)]`.

    7) Do NOT modify the venue-name Link, the open-status block, the address row, the description (`line-clamp-2`) block, the `motion.div` shell, `korttiVariants`, or the `glass`/`glass-hover` classes. Do NOT inline any sport color hex — sport color must continue to come from `lajiKonfig[laji].color`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/components/PaikkaKortti.tsx','utf8'); const checks={'Sponsoroitu present':s.includes('Sponsoroitu'), 'amber-100 badge bg':s.includes('bg-amber-100'), 'isMembershipOnly imported':/isMembershipOnly/.test(s), 'vain jäsenyys copy':s.includes('vain jäsenyys'), 'Näytä tiedot copy':s.includes('Näytä tiedot'), 'Varaa aika removed':!s.includes('Varaa aika'), 'no font-semibold':!s.includes('font-semibold'), 'no font-medium':!s.includes('font-medium'), 'priceLines split':/split\(['\"]\\n['\"]?\)/.test(s) || /\\n/.test(s)}; for(const k of Object.keys(checks)){ if(!checks[k]) throw new Error('FAIL: '+k) } console.log('OK')" && npx vitest run lib/priceUtils.test.ts</automated>
  </verify>
  <done>app/components/PaikkaKortti.tsx contains: the Sponsoroitu amber badge guarded by `paikka.featured`, the new price block at position 4 using `isMembershipOnly`, multi-line price split on `\n`, no occurrence of the string "Varaa aika", and the always-rendered "Näytä tiedot" outlined Link. `npx tsc --noEmit` exits 0 and `npx vitest run` exits 0.</done>
  <acceptance_criteria>
    - Source assertion: grep `Sponsoroitu` in PaikkaKortti.tsx returns at least 1 match
    - Source assertion: grep `bg-amber-100` in PaikkaKortti.tsx returns at least 1 match (badge styling)
    - Source assertion: grep `Varaa aika` in PaikkaKortti.tsx returns 0 matches
    - Source assertion: grep `isMembershipOnly` in PaikkaKortti.tsx returns at least 1 match (import + call)
    - Source assertion: grep `vain jäsenyys` in PaikkaKortti.tsx returns 1 match (single muted fallback span)
    - Source assertion: grep `Näytä tiedot` in PaikkaKortti.tsx returns 1 match (always-shown CTA, no longer behind a ternary)
    - Source assertion: file does NOT contain `font-semibold` or `font-medium`
    - Source order assertion: in the file, the substring `Sponsoroitu` appears AFTER the line containing the sport pill `style={{ backgroundColor: laji.color }}` and BEFORE the line containing `Kertakäynti OK`
    - Source order assertion: the new price `<div>` appears AFTER the open-status block and BEFORE the address row (`MapPin` outside the bottom row, used for the address line)
    - Build assertion: `npx tsc --noEmit` exits 0
    - Test assertion: `npx vitest run` exits 0 (priceUtils + cityFilter tests still green)
    - Behavior assertion (manual smoke): list view with a `featured=true` venue shows the amber Sponsoroitu badge in card; the same card shows `Näytä tiedot` (never `Varaa aika`); price appears between open-status and address; a venue whose `hinta_kuvaus` contains "jäsenyys" with both `hinta_min/max=null` shows muted `vain jäsenyys`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → Client component | `paikka.featured`, `paikka.hinta_kuvaus`, `paikka.varauslinkki` rendered into JSX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering (XSS) | `paikka.hinta_kuvaus` rendered into multi-line price spans | mitigate | React text nodes auto-escape; `<span>{line}</span>` does NOT use `dangerouslySetInnerHTML`. Acceptance criteria forbid that API in this file. |
| T-06-10 | Tampering | `paikka.featured` boolean controls amber badge | accept | Conditional render only flips visibility — no privileges granted. RLS keeps anon role read-only. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` exits 0 (lib helpers still pass)
- grep checks per acceptance criteria all pass
- Manual smoke: render list view in dev, scan for badge / price / CTA changes
</verification>

<success_criteria>
The card matches the new section order from D-10, the amber Sponsoroitu badge appears for featured venues, "Varaa aika →" is fully gone from list cards, and the muted "vain jäsenyys" fallback appears only when the D-11 evidence rule passes.
</success_criteria>

<output>
Create `.planning/phases/06-ui-polish-and-data-foundation/06-05-SUMMARY.md` when done.
</output>
