---
phase: 21-todo-lista
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/BookmarkButton.tsx
  - app/components/PaikkaSheet.tsx
  - app/components/Etusivu.tsx
  - app/paikat/[id]/page.tsx
autonomous: true
requirements:
  - TODO-01

must_haves:
  truths:
    - "BookmarkButton.tsx exists and exports BookmarkButton (not HeartButton)"
    - "HeartButton.tsx no longer exists on disk (file renamed)"
    - "BookmarkButton renders Bookmark icon when not saved, BookmarkCheck when saved"
    - "PaikkaSheet accepts todo and onToggleTodo props (suosikki/onToggleSuosikki removed)"
    - "Etusivu passes todo={todoIds.has(...)} and onToggleTodo={toggleTodo} to PaikkaSheet"
    - "paikat/[id]/page.tsx imports BookmarkButton from BookmarkButton (not HeartButton)"
    - "No Heart import remains in any of the four modified files"
    - "TypeScript compiles without errors across all four files"
  artifacts:
    - path: "app/components/BookmarkButton.tsx"
      provides: "Bookmark toggle button (was HeartButton)"
      contains: "BookmarkButtonProps"
    - path: "app/components/PaikkaSheet.tsx"
      provides: "Sheet panel with renamed props"
      contains: "onToggleTodo"
    - path: "app/components/Etusivu.tsx"
      provides: "Map view with renamed state and function"
      contains: "todoIds"
    - path: "app/paikat/[id]/page.tsx"
      provides: "Venue detail page importing BookmarkButton"
      contains: "BookmarkButton"
  key_links:
    - from: "app/components/Etusivu.tsx"
      to: "app/components/PaikkaSheet.tsx"
      via: "JSX props"
      pattern: "onToggleTodo.*toggleTodo"
    - from: "app/paikat/[id]/page.tsx"
      to: "app/components/BookmarkButton.tsx"
      via: "import"
      pattern: "import BookmarkButton from.*BookmarkButton"
---

<objective>
Rename HeartButton to BookmarkButton and execute the PaikkaSheet+Etusivu atomic props rename.

Purpose: The heart icon is replaced with a bookmark icon across the map flow. PaikkaSheet's interface (suosikki/onToggleSuosikki) and its sole caller (Etusivu) must be updated in the same task to avoid a TypeScript compile error. The venue detail page import is updated in the same plan to keep BookmarkButton's usage consistent.

Output: BookmarkButton.tsx (new file replacing HeartButton.tsx), updated PaikkaSheet.tsx props, updated Etusivu.tsx state/function names and PaikkaSheet call site, updated paikat/[id]/page.tsx import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-todo-lista/21-CONTEXT.md
@.planning/phases/21-todo-lista/21-RESEARCH.md
@.planning/phases/21-todo-lista/21-PATTERNS.md
@.planning/phases/21-todo-lista/21-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BookmarkButton.tsx and delete HeartButton.tsx</name>
  <files>app/components/BookmarkButton.tsx, app/components/HeartButton.tsx</files>

  <read_first>
    - app/components/HeartButton.tsx — read the entire file; this is the source to transform
  </read_first>

  <action>
    Create app/components/BookmarkButton.tsx as a transformation of HeartButton.tsx with these exact changes:

    Imports: Replace `import { Heart } from 'lucide-react'` with `import { Bookmark, BookmarkCheck } from 'lucide-react'`. All other imports stay identical.

    Props interface: Rename `HeartButtonProps` to `BookmarkButtonProps`. The `paikkaId: number` field is unchanged.

    Component name: `HeartButton` → `BookmarkButton` in the function declaration and default export.

    State: `isSuosikki`/`setIsSuosikki` → `isTodo`/`setIsTodo` everywhere in the file.

    Console log prefixes: `[HeartButton]` → `[BookmarkButton]` in both error log strings.

    Auth check in `onSuccess`: `setIsSuosikki(true)` → `setIsTodo(true)`.

    Button aria-label: `isSuosikki ? 'Poista suosikeista' : 'Lisää suosikkeihin'` → `isTodo ? 'Poista TO DO -listalta' : 'Lisää TO DO -listalle'` (per D-02).

    Icon render: Replace the single Heart JSX with a conditional:
    - When `isTodo` is true: `<BookmarkCheck className="w-5 h-5 fill-[#111111] text-[#111111]" />` (both fill- and text- classes required — Pitfall 5)
    - When `isTodo` is false: `<Bookmark className="w-5 h-5 text-[rgba(17,17,17,0.35)]" />`

    All other logic (useEffect, toggle function, Supabase calls on table 'suosikit', AuthModal integration, subscribeToAuthUser, createBrowserSupabase) is copied verbatim from HeartButton.tsx with only the state variable names and log prefixes changed.

    After creating BookmarkButton.tsx, delete app/components/HeartButton.tsx using the shell: `Remove-Item app/components/HeartButton.tsx`.

    Do NOT use `cn()` wrapper around the icon className strings — the existing HeartButton pattern uses cn() there; keep it: `cn('w-5 h-5 fill-[#111111] text-[#111111]')` for filled state and `cn('w-5 h-5 text-[rgba(17,17,17,0.35)]')` for unsaved state.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - app/components/BookmarkButton.tsx exists on disk
    - app/components/HeartButton.tsx does NOT exist on disk
    - BookmarkButton.tsx line 1: `'use client'`
    - BookmarkButton.tsx contains `interface BookmarkButtonProps`
    - BookmarkButton.tsx contains `export default function BookmarkButton`
    - BookmarkButton.tsx imports `{ Bookmark, BookmarkCheck }` from 'lucide-react' — no `Heart` import
    - BookmarkButton.tsx contains `aria-label={isTodo ? 'Poista TO DO -listalta' : 'Lisää TO DO -listalle'}`
    - BookmarkButton.tsx contains `fill-[#111111]` on the BookmarkCheck className
    - BookmarkButton.tsx contains zero occurrences of the string `Heart` (import or JSX)
    - BookmarkButton.tsx contains zero occurrences of `isSuosikki` or `suosikit` as a variable name (the Supabase table name 'suosikit' in the query string is acceptable)
  </acceptance_criteria>

  <done>BookmarkButton.tsx is a functional bookmark-toggle button replacing HeartButton.tsx; HeartButton.tsx is deleted; TypeScript reports no new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Rename PaikkaSheet props and update Etusivu call site atomically</name>
  <files>app/components/PaikkaSheet.tsx, app/components/Etusivu.tsx, app/paikat/[id]/page.tsx</files>

  <read_first>
    - app/components/PaikkaSheet.tsx — read the entire file; focus on the Props interface (lines ~17–23) and toggle button (lines ~89–95)
    - app/components/Etusivu.tsx — read to find: state declaration `suosikitIds` (~line 171), function `toggleSuosikki` (~lines 255–293), useMemo `suosikitSizeAndIds` (~lines 379–399), PaikkaSheet JSX call site (~lines 1124–1130), AuthModal onSuccess (~lines 1109–1111)
    - app/paikat/[id]/page.tsx — read lines 1–20 to locate the HeartButton import and its usage
  </read_first>

  <action>
    Edit PaikkaSheet.tsx — three changes only:

    1. Lucide import: Remove `Heart`, add `Bookmark` and `BookmarkCheck`. Example current import pattern from PATTERNS.md line 340: `import { X, MapPin, Phone, ExternalLink, Clock, CircleDollarSign, Info, Heart } from 'lucide-react'` → replace `Heart` with `Bookmark, BookmarkCheck` in that same import.

    2. Props interface rename (per D-05):
       - `suosikki: boolean` → `todo: boolean`
       - `onToggleSuosikki: (id: number) => void` → `onToggleTodo: (id: number) => void`
       Update the destructuring in the function signature to match: `{ paikka, todo, distanceKm, onClose, onToggleTodo }`.

    3. Toggle button (per D-04, D-05): Replace `onClick={() => onToggleSuosikki(paikka.id)}` with `onClick={() => onToggleTodo(paikka.id)}`. Replace the Heart icon JSX with:
       - When `todo` is true: `<BookmarkCheck className="w-4 h-4 fill-[#111111] text-[#111111]" />`
       - When `todo` is false: `<Bookmark className="w-4 h-4 text-[rgba(17,17,17,0.35)]" />`
       Replace `suosikki` condition variable with `todo` in the icon conditional.

    Edit Etusivu.tsx — five changes (per D-06):

    1. Lucide import: Remove `Heart` from the lucide-react import; add `Bookmark` (for any Heart usage in Etusivu; confirm by reading what Heart was used for before removing — if Heart is only in the import and not rendered in Etusivu JSX itself, just remove it from the import).

    2. State declaration: `const [suosikitIds, setSuosikitIds] = useState<Set<number>>(new Set())` → `const [todoIds, setTodoIds] = useState<Set<number>>(new Set())`. Update ALL usages of `suosikitIds`/`setSuosikitIds` throughout the file to `todoIds`/`setTodoIds`.

    3. Function rename: `async function toggleSuosikki(id: number)` → `async function toggleTodo(id: number)`. Update ALL occurrences of `toggleSuosikki` in the function body and at call sites within Etusivu.tsx to `toggleTodo`. Update console log prefix `[toggleSuosikki]` → `[toggleTodo]`.

    4. useMemo / AI fetch variable: `suosikkiNimet` → `todoNimet` everywhere it appears. The JSON payload key stays `suosikit` (keep `body: JSON.stringify({ suosikit: todoNimet, ... })` — only the local variable name changes, not the wire key, per D-06 and RESEARCH.md Pattern 4).

    5. PaikkaSheet call site: Update the JSX to pass `todo={todoIds.has(valittu.id)}` and `onToggleTodo={toggleTodo}`. Remove `suosikki={...}` and `onToggleSuosikki={...}` props. Also update `onSuccess={id => { if (id) toggleTodo(id); ... }}` in AuthModal.

    Edit app/paikat/[id]/page.tsx — two changes only (per D-03):

    1. Import line: `import HeartButton from '@/app/components/HeartButton'` → `import BookmarkButton from '@/app/components/BookmarkButton'`
    2. JSX usage: `<HeartButton paikkaId={paikka.id} />` → `<BookmarkButton paikkaId={paikka.id} />`

    No other changes to paikat/[id]/page.tsx.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - PaikkaSheet.tsx Props interface contains `todo: boolean` and `onToggleTodo: (id: number) => void`
    - PaikkaSheet.tsx does NOT contain `suosikki: boolean` or `onToggleSuosikki` anywhere
    - PaikkaSheet.tsx does NOT contain `Heart` anywhere (import or JSX)
    - PaikkaSheet.tsx toggle button contains `onClick={() => onToggleTodo(paikka.id)}`
    - PaikkaSheet.tsx contains `BookmarkCheck` and `Bookmark` in the toggle button JSX
    - Etusivu.tsx contains `todoIds` state declaration (not `suosikitIds`)
    - Etusivu.tsx contains `async function toggleTodo` (not `toggleSuosikki`)
    - Etusivu.tsx PaikkaSheet JSX contains `todo={todoIds.has(valittu.id)}` and `onToggleTodo={toggleTodo}`
    - Etusivu.tsx does NOT contain `suosikitIds`, `setSuosikitIds`, or `toggleSuosikki` anywhere
    - Etusivu.tsx JSON payload still contains the key string `suosikit` (wire key unchanged)
    - Etusivu.tsx contains `todoNimet` (not `suosikkiNimet`)
    - app/paikat/[id]/page.tsx imports from `'@/app/components/BookmarkButton'` (not HeartButton)
    - app/paikat/[id]/page.tsx JSX contains `<BookmarkButton paikkaId={paikka.id} />`
    - TypeScript compiles without errors (tsc --noEmit exits 0)
  </acceptance_criteria>

  <done>PaikkaSheet interface and Etusivu call site are fully renamed; the PaikkaSheet props rename compiles without TypeScript errors; paikat/[id]/page.tsx uses BookmarkButton.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Supabase | Browser client sends delete/insert to suosikit table — row-level security enforced by Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21A-01 | Tampering | BookmarkButton toggle (insert/delete) | accept | RLS on suosikit table enforces user_id match; anon key is read-only; existing pattern unchanged |
| T-21A-02 | Spoofing | subscribeToAuthUser in BookmarkButton | accept | Auth state sourced from Supabase session cookie; existing pattern unchanged |
</threat_model>

<verification>
Run `npx tsc --noEmit` — must exit 0 with no errors related to HeartButton, BookmarkButton, PaikkaSheet props, or Etusivu state.

Run `grep -r "HeartButton" app/ --include="*.tsx" --include="*.ts"` — must return zero results.

Run `grep -r "suosikitIds\|toggleSuosikki\|onToggleSuosikki\|suosikki:" app/components/Etusivu.tsx app/components/PaikkaSheet.tsx` — must return zero results.

Run `grep -r " Heart" app/components/BookmarkButton.tsx app/components/PaikkaSheet.tsx app/components/Etusivu.tsx app/paikat` — must return zero results (no Heart import or JSX in these four files).
</verification>

<success_criteria>
1. BookmarkButton.tsx exists; HeartButton.tsx does not.
2. PaikkaSheet.tsx Props interface uses `todo` and `onToggleTodo`; no `Heart` icon.
3. Etusivu.tsx state is `todoIds`, function is `toggleTodo`, call site passes `todo`/`onToggleTodo`; AI payload key wire string `suosikit` unchanged.
4. app/paikat/[id]/page.tsx imports and renders BookmarkButton.
5. `npx tsc --noEmit` exits 0.
</success_criteria>

<output>
Create `.planning/phases/21-todo-lista/21-01-SUMMARY.md` when done.
</output>
