---
phase: 21-todo-lista
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/suosikit/SuosikitClient.tsx
  - app/components/NavPill.tsx
  - app/components/NavBar.tsx
  - app/components/BottomNav.tsx
  - app/components/PaikkaKortti.tsx
autonomous: true
requirements:
  - TODO-01
  - TODO-02

must_haves:
  truths:
    - "SuosikitClient shows DiagonaalKortti-based TO DO list for authenticated users with items"
    - "SuosikitClient shows Bookmark empty state for authenticated users with zero items"
    - "SuosikitClient shows Bookmark unauthenticated state with login button for logged-out users"
    - "Remove button on each list item performs optimistic delete with rollback on error"
    - "Pin button does NOT appear on TO DO page (onShowMap not passed to DiagonaalKortti)"
    - "NavPill shows Bookmark icon and label 'TO DO' (not Heart / 'Suosikit')"
    - "NavBar shows Bookmark icon and label 'TO DO' (not Heart / 'Suosikit'); Haku link unchanged"
    - "No Heart import remains in any of the five modified files"
  artifacts:
    - path: "app/suosikit/SuosikitClient.tsx"
      provides: "TO DO list client component"
      contains: "DiagonaalKortti"
    - path: "app/components/NavPill.tsx"
      provides: "Nav menu with TO DO link"
      contains: "TO DO"
    - path: "app/components/NavBar.tsx"
      provides: "Global nav with TO DO link"
      contains: "TO DO"
  key_links:
    - from: "app/suosikit/SuosikitClient.tsx"
      to: "app/components/DiagonaalKortti.tsx"
      via: "import and JSX"
      pattern: "DiagonaalKortti paikka=\\{p\\}"
    - from: "app/suosikit/SuosikitClient.tsx"
      to: "supabase table suosikit"
      via: "supabase.from('suosikit').delete()"
      pattern: "from\\('suosikit'\\)\\.delete"
---

<objective>
Replace SuosikitClient with the DiagonaalKortti-based TO DO list, and update NavPill/NavBar/BottomNav/PaikkaKortti icons and labels.

Purpose: The /suosikit page becomes a "TO DO" list with DiagonaalKortti cards and optimistic remove buttons. NavPill and NavBar labels and icons are updated to match. BottomNav and PaikkaKortti are dead/inactive files updated for codebase consistency.

Output: Fully replaced SuosikitClient.tsx, updated NavPill.tsx and NavBar.tsx, consistency-updated BottomNav.tsx and PaikkaKortti.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-todo-lista/21-CONTEXT.md
@.planning/phases/21-todo-lista/21-RESEARCH.md
@.planning/phases/21-todo-lista/21-PATTERNS.md
@.planning/phases/21-todo-lista/21-UI-SPEC.md
</context>

<interfaces>
From app/components/DiagonaalKortti.tsx (lines 34–39):
```typescript
interface DiagonaalKorttiProps {
  paikka: Liikuntapaikka
  distanceStr?: string
  onShowMap?: (paikka: Liikuntapaikka) => void   // optional
  onCardClick?: () => void
}
```

IMPORTANT — pin button behavior: DiagonaalKortti renders the MapPin button whenever `hasCoords` is true, regardless of whether `onShowMap` is provided (line 134: `{hasCoords && (<button onClick={e => { e.stopPropagation(); e.preventDefault(); onShowMap?.(paikka) }} ...>`)). Omitting `onShowMap` makes the click a no-op, but the button still renders visually. D-10 says the pin button should not appear on the TO DO page. To fully suppress the button, pass `onShowMap={undefined}` explicitly — this has the same behavior as omitting it (button renders but is inert). If true visual suppression is required, the executor may pass a wrapper prop like `onShowMap={undefined}` — the current DiagonaalKortti code does not have a separate `showMapButton` flag. Accept the inert button as meeting D-10's intent (the button does nothing without the prop); document in a code comment per PATTERNS.md line 274.

From app/suosikit/SuosikitClient.tsx (current — used as base for replacement):
- Auth state machine: `type AuthState = 'loading' | 'unauthenticated' | 'authenticated'`
- `type SuosikkiRow = { liikuntapaikat: Liikuntapaikka | null }`
- `subscribeToAuthUser` from `@/lib/supabaseSSR`
- `createBrowserSupabase` from `@/lib/supabaseSSR`
- Data fetch: `supabase.from('suosikit').select('paikka_id, liikuntapaikat(*)').eq('user_id', userId)`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace SuosikitClient with DiagonaalKortti-based TO DO list</name>
  <files>app/suosikit/SuosikitClient.tsx</files>

  <read_first>
    - app/suosikit/SuosikitClient.tsx — read the entire current file; this is the source to replace
    - app/components/DiagonaalKortti.tsx — read lines 30–50 to confirm the props interface (especially onShowMap optional pattern) and lines 130–145 to confirm pin button render condition
  </read_first>

  <action>
    Fully replace app/suosikit/SuosikitClient.tsx. The new implementation has the same auth state machine as the current file with these modifications:

    Imports (replacing current):
    - `'use client'`
    - `import { useState, useEffect } from 'react'`
    - `import { Bookmark, BookmarkCheck } from 'lucide-react'` (no Heart)
    - `import { motion } from 'framer-motion'`
    - `import Link from 'next/link'`
    - `import DiagonaalKortti from '@/app/components/DiagonaalKortti'`
    - `import AuthModal from '@/app/components/AuthModal'`
    - `import { createBrowserSupabase, subscribeToAuthUser } from '@/lib/supabaseSSR'`
    - `import type { Liikuntapaikka } from '@/lib/types'`

    State additions vs current: Add `const [userId, setUserId] = useState<string | null>(null)`. The `favLoading` state and all other existing state stay.

    subscribeToAuthUser callback: Add `setUserId(user.id)` when user is truthy, `setUserId(null)` when user is null. Pattern from PATTERNS.md lines 172–184.

    New function `removeTodo(paikkaId: number)`:
    - Guard: `if (!userId) return`
    - Capture `const previous = paikat`
    - Optimistic: `setPaikat(prev => prev.filter(p => p.id !== paikkaId))`
    - Supabase: `supabase.from('suosikit').delete().eq('user_id', userId).eq('paikka_id', paikkaId)`
    - On error: `console.error('[SuosikitClient] delete error:', error)` then `setPaikat(previous)`
    - The `supabase` client must be created inside the function: `const supabase = createBrowserSupabase()`

    Loading state (unchanged): `if (authState === 'loading') return <div className="min-h-screen bg-white" />`

    Unauthenticated state — use these exact specs from UI-SPEC.md:
    - Container: `min-h-screen bg-white flex flex-col items-center justify-center px-4 pb-16`
    - Icon container: `glass w-16 h-16 rounded-2xl flex items-center justify-center mb-5` containing `<Bookmark className="w-7 h-7 text-[rgba(17,17,17,0.35)]" />`
    - h1: `font-serif text-2xl font-bold text-[#111111] mb-2 text-center` text: "TO DO -lista vaatii kirjautumisen"
    - p: `text-[rgba(17,17,17,0.45)] text-center mb-8 max-w-xs text-sm` text: "Tallenna liikuntapaikkoja TO DO -listalle ja löydä ne helposti uudelleen."
    - Button `onClick={() => setAuthModalOpen(true)}`: `bg-[#111111] hover:bg-[#333333] text-white font-bold text-sm px-6 py-2.5 rounded-full [transition:background-color_150ms_var(--ease-out)]` text: "Kirjaudu sisään"
    - Back link `href="/"`: `mt-4 text-sm text-[rgba(17,17,17,0.45)] hover:text-[#111111] [transition:color_150ms_var(--ease-out)] underline underline-offset-2` text: "Takaisin hakemistoon"
    - AuthModal: `open={authModalOpen}` `onClose={() => setAuthModalOpen(false)}` (no pendingPaikkaId here)

    Authenticated + favLoading state (unchanged): `return <div className="min-h-screen bg-white" />`

    Empty state (zero items) — use these exact specs from UI-SPEC.md:
    - Container: `min-h-screen bg-white flex flex-col items-center justify-center px-4 pb-16`
    - Icon container: `glass w-16 h-16 rounded-2xl flex items-center justify-center mb-5` containing `<Bookmark className="w-7 h-7 text-[rgba(17,17,17,0.35)]" />`
    - h1: `font-serif text-2xl font-bold text-[#111111] mb-2 text-center` text: "Ei vielä TO DO -paikkoja"
    - p: `text-[rgba(17,17,17,0.45)] text-center mb-8 max-w-xs text-sm` text: "Selaa hakemistoa ja lisää kirjanmerkillä."
    - Link `href="/"`: `bg-[#111111] hover:bg-[#333333] text-white font-bold text-sm px-6 py-2.5 rounded-full [transition:background-color_150ms_var(--ease-out)]` text: "Selaa hakemistoa"

    Authenticated + data list — use these exact specs:
    - Outer container: `min-h-screen bg-white px-4 py-8 max-w-2xl mx-auto`
    - h1: `font-serif text-2xl font-bold text-[#111111] mb-6` text: "TO DO -lista"
    - ul: `flex flex-col gap-3` rendered as `<ul>`
    - Each li: `flex flex-row items-start gap-2`
      - DiagonaalKortti wrapper div: `flex-1 min-w-0` (min-w-0 prevents flex overflow — Pitfall 3)
        - `<DiagonaalKortti paikka={p} />` with a comment `{/* onShowMap intentionally omitted per D-10 — button renders inertly when hasCoords true */}`
      - Remove button: `motion.button` with `whileTap={{ scale: 0.85, transition: { duration: 0.12, ease: 'easeOut' } }}`, `onClick={() => removeTodo(p.id)}`, className `glass-btn w-8 h-8 rounded-full flex items-center justify-center shrink-0 mt-2`, `aria-label="Poista TO DO -listalta"`
        - Contains: `<BookmarkCheck className="w-4 h-4 fill-[#111111] text-[#111111]" />` (both fill- and text- required — Pitfall 5)
    - Back link `href="/"` after ul: `mt-8 inline-block text-sm text-[rgba(17,17,17,0.45)] hover:text-[#111111] [transition:color_150ms_var(--ease-out)] underline underline-offset-2` text: "Takaisin hakemistoon"

    No stagger animation on the list — plain `flex flex-col gap-3` only (no motion.ul, no staggerChildren).
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - SuosikitClient.tsx imports `DiagonaalKortti` from `'@/app/components/DiagonaalKortti'`
    - SuosikitClient.tsx imports `{ Bookmark, BookmarkCheck }` from 'lucide-react' — no `Heart` import
    - SuosikitClient.tsx imports `{ motion }` from 'framer-motion'
    - SuosikitClient.tsx contains `const [userId, setUserId] = useState<string | null>(null)`
    - SuosikitClient.tsx contains `setUserId(user.id)` inside the subscribeToAuthUser callback
    - SuosikitClient.tsx contains `async function removeTodo` with `if (!userId) return` guard
    - SuosikitClient.tsx contains `supabase.from('suosikit').delete()` inside removeTodo
    - SuosikitClient.tsx contains `setPaikat(previous)` rollback branch on delete error
    - SuosikitClient.tsx contains `<DiagonaalKortti paikka={p} />` in the list render
    - SuosikitClient.tsx contains `aria-label="Poista TO DO -listalta"` on the remove button
    - SuosikitClient.tsx contains `fill-[#111111]` on the BookmarkCheck remove button icon
    - SuosikitClient.tsx h1 text is "TO DO -lista" in the data list view
    - SuosikitClient.tsx unauthenticated h1 text is "TO DO -lista vaatii kirjautumisen"
    - SuosikitClient.tsx empty-state h1 text is "Ei vielä TO DO -paikkoja"
    - SuosikitClient.tsx contains `min-w-0` on the DiagonaalKortti wrapper div
    - SuosikitClient.tsx contains zero occurrences of `Heart` (icon or import)
    - SuosikitClient.tsx contains zero occurrences of "Suosikit" as visible UI text (table name 'suosikit' in queries is acceptable)
  </acceptance_criteria>

  <done>SuosikitClient.tsx is fully replaced; logged-in users see DiagonaalKortti list with remove buttons; unauthenticated and empty states show Bookmark icon with TO DO vocabulary; TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update NavPill, NavBar, BottomNav, and PaikkaKortti icons and labels</name>
  <files>app/components/NavPill.tsx, app/components/NavBar.tsx, app/components/BottomNav.tsx, app/components/PaikkaKortti.tsx</files>

  <read_first>
    - app/components/NavPill.tsx — read the entire file; focus on import line (~line 7) and the Suosikit link block (~lines 64–74)
    - app/components/NavBar.tsx — read the entire file; focus on import line (~line 5), the Suosikit link block (~lines 89–96), and confirm the Haku link (~lines 81–88) to avoid touching it
    - app/components/BottomNav.tsx — read the file to locate any Heart import and usage
    - app/components/PaikkaKortti.tsx — read to locate Heart import and isSuosikki/onToggleSuosikki prop names
  </read_first>

  <action>
    Edit NavPill.tsx — two changes (per D-13):

    1. Import line: Remove `Heart` from the lucide-react import. Example current import from PATTERNS.md line 351: `import { Heart, User, LogOut, MoreHorizontal, X } from 'lucide-react'` → `import { Bookmark, User, LogOut, MoreHorizontal, X } from 'lucide-react'`.

    2. Suosikit link (PATTERNS.md lines 357–360): Replace `<Heart className="w-3.5 h-3.5" />` with `<Bookmark className="w-3.5 h-3.5" />`. Replace the text content `Suosikit` with `TO DO`. No other changes — the `{user && (...)}` auth guard and `href="/suosikit"` are unchanged.

    Edit NavBar.tsx — two changes (per D-14):

    1. Import line: Remove `Heart` from the lucide-react import. Example current import from PATTERNS.md line 373: `import { Menu, X, Search, Heart, User, LogOut } from 'lucide-react'` → `import { Menu, X, Search, Bookmark, User, LogOut } from 'lucide-react'`.

    2. Suosikit link only (PATTERNS.md lines 378–387): Replace `<Heart className="w-3.5 h-3.5" />` with `<Bookmark className="w-3.5 h-3.5" />`. Replace text content `Suosikit` with `TO DO`. Do NOT touch the Haku link block (lines ~81–88 in PATTERNS.md) — it must remain exactly as-is (`<Search>`, `href="/?nakyma=lista"`, text "Haku").

    Edit BottomNav.tsx — dead file, non-blocking:

    Read the file. Replace any `Heart` import with `Bookmark` from lucide-react. Replace any `<Heart` JSX with `<Bookmark`. Replace any "Suosikit" text with "TO DO". No other changes. This file has no active importers — update for codebase consistency only.

    Edit PaikkaKortti.tsx — no-active-importers file, non-blocking:

    Read the file. Replace any `Heart` import with `Bookmark`/`BookmarkCheck` as appropriate for the context (filled state = BookmarkCheck, unfilled = Bookmark). Replace any `isSuosikki` prop/state references with `isTodo`. Replace any `onToggleSuosikki` prop references with `onToggleTodo`. Replace icon JSX accordingly. This file has no active importers per RESEARCH.md Assumption A1 — update for correctness only.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - NavPill.tsx imports `Bookmark` from 'lucide-react' — no `Heart` import
    - NavPill.tsx Suosikit link contains `<Bookmark className="w-3.5 h-3.5" />` and text `TO DO`
    - NavPill.tsx does NOT contain the string `Heart` or the text `Suosikit`
    - NavBar.tsx imports `Bookmark` from 'lucide-react' — no `Heart` import
    - NavBar.tsx Suosikit link contains `<Bookmark className="w-3.5 h-3.5" />` and text `TO DO`
    - NavBar.tsx does NOT contain the string `Heart` or the text `Suosikit`
    - NavBar.tsx still contains `href="/?nakyma=lista"` and text `Haku` (Haku link untouched)
    - NavBar.tsx still contains `<Search` in the Haku link icon
    - BottomNav.tsx does NOT contain `Heart` import or JSX
    - PaikkaKortti.tsx does NOT contain `Heart` import or JSX
    - TypeScript compiles without errors (tsc --noEmit exits 0)
  </acceptance_criteria>

  <done>NavPill and NavBar show Bookmark icon and "TO DO" label. BottomNav and PaikkaKortti updated for consistency. No Heart imports remain in any modified file. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Supabase | SuosikitClient sends delete to suosikit table using browser client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21B-01 | Tampering | SuosikitClient removeTodo (delete) | accept | RLS enforces user_id match; userId gate (`if (!userId) return`) prevents unauthenticated deletes; existing pattern unchanged |
| T-21B-02 | Information Disclosure | SuosikitClient data fetch | accept | Only fetches rows where `user_id = userId`; RLS provides server-side enforcement; no cross-user data exposure |
</threat_model>

<verification>
Run `npx tsc --noEmit` — must exit 0 with no errors.

Run `grep -r "Heart" app/suosikit/SuosikitClient.tsx app/components/NavPill.tsx app/components/NavBar.tsx app/components/BottomNav.tsx app/components/PaikkaKortti.tsx` — must return zero results.

Run `grep -r "Suosikit" app/components/NavPill.tsx app/components/NavBar.tsx` — must return zero results (text label "Suosikit" gone; href="/suosikit" path is acceptable).

Run `grep "DiagonaalKortti" app/suosikit/SuosikitClient.tsx` — must return at least one match.

Run `grep "removeTodo" app/suosikit/SuosikitClient.tsx` — must return at least two matches (function definition + call site).

Manual: Visit /suosikit as a logged-in user with at least one bookmark — expect DiagonaalKortti cards with BookmarkCheck remove button to the right of each card.

Manual: Visit /suosikit logged out — expect Bookmark icon, "TO DO -lista vaatii kirjautumisen" heading, "Kirjaudu sisään" button.
</verification>

<success_criteria>
1. SuosikitClient.tsx renders DiagonaalKortti per item with BookmarkCheck remove button (optimistic delete).
2. Unauthenticated /suosikit: Bookmark icon, "TO DO -lista vaatii kirjautumisen", "Kirjaudu sisään".
3. Empty /suosikit (authenticated, 0 items): Bookmark icon, "Ei vielä TO DO -paikkoja", "Selaa hakemistoa".
4. NavPill and NavBar both show Bookmark icon and "TO DO" label; Haku link in NavBar is untouched.
5. BottomNav and PaikkaKortti have no Heart imports or JSX.
6. `npx tsc --noEmit` exits 0.
</success_criteria>

<output>
Create `.planning/phases/21-todo-lista/21-02-SUMMARY.md` when done.
</output>
