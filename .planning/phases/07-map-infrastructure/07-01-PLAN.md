---
phase: 07-map-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Etusivu.tsx
  - lib/mapStyles.ts
  - lib/sportPins.ts
  - .env.local.example
autonomous: true
requirements:
  - MAP-04

must_haves:
  truths:
    - "Both map instances render venue pins as AdvancedMarker with img child, not legacy Marker"
    - "User location marker renders as AdvancedMarker with HTML div, not legacy Marker"
    - "Both Map components receive mapId from env vars, switching on isDark"
    - "MapStyleController is removed — no setOptions({ styles }) calls remain"
    - "New env var names are documented in .env.local.example"
  artifacts:
    - path: "app/components/Etusivu.tsx"
      provides: "Migrated map component — AdvancedMarker + mapId"
      contains: "AdvancedMarker"
    - path: ".env.local.example"
      provides: "Developer onboarding — env var documentation"
      contains: "NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_DAY"
  key_links:
    - from: "app/components/Etusivu.tsx"
      to: "@vis.gl/react-google-maps"
      via: "AdvancedMarker import"
      pattern: "AdvancedMarker"
    - from: "app/components/Etusivu.tsx"
      to: "process.env"
      via: "mapId prop"
      pattern: "NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID"
---

<objective>
Migrate both map instances in Etusivu.tsx from the legacy `Marker` component to `AdvancedMarker`. Switch `<Map>` instances to use day/night Map IDs from env vars, replacing MapStyleController. Document the two new env vars.

Purpose: AdvancedMarker is a prerequisite for all Phase 8 map features. The legacy Marker API does not support the AdvancedMarker children-based pin model needed for zoom-based pin→card in Phase 8.
Output: Etusivu.tsx with zero `Marker` usages; .env.local.example with both new Map ID vars documented.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-map-infrastructure/07-CONTEXT.md
@.planning/phases/07-map-infrastructure/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate venue and user-location Markers to AdvancedMarker</name>
  <files>app/components/Etusivu.tsx, lib/sportPins.ts</files>

  <read_first>
    - app/components/Etusivu.tsx — full file; understand the two Marker render sites (preview map ~line 263, fullscreen map ~line 330) and the existing import on line 6
    - lib/sportPins.ts — understand pinUrl() and userLocationPinUrl() signatures; pinUrl() must NOT be changed
    - .planning/phases/07-map-infrastructure/07-CONTEXT.md — decisions D-01 through D-03 and the specifics block for exact HTML structure
    - .planning/phases/07-map-infrastructure/07-UI-SPEC.md — Component Contracts section for gmap-pin CSS class and data-active attribute requirements
  </read_first>

  <action>
    In app/components/Etusivu.tsx:

    1. Change the @vis.gl/react-google-maps import on line 6 from `{ Map, Marker, useMap }` to `{ Map, AdvancedMarker, useMap }`.

    2. Change the lib/sportPins.ts import on line 18 from `{ pinUrl, userLocationPinUrl }` to `{ pinUrl }` (per D-03, userLocationPinUrl usage is replaced by HTML; delete the named import).

    3. Preview map venue pins (inside the first Map, ~line 263-271): replace each `<Marker>` with:
       ```
       <AdvancedMarker key={p.id} position={{ lat: p.latitude, lng: p.longitude }}>
         <img src={pinUrl(color, p.laji)} width={28} height={38} alt="" className="gmap-pin" data-active={valittu?.id === p.id ? "true" : undefined} />
       </AdvancedMarker>
       ```
       The preview map has no onClick handlers — do not add any. No zIndex needed for the preview.

    4. Preview map user-location Marker (~line 273-279): replace with:
       ```
       {coords && (
         <AdvancedMarker position={coords} zIndex={20}>
           <div style={{ width: 24, height: 24, position: 'relative' }}>
             <div style={{ position: 'absolute', inset: 0, borderRadius: '50%', background: 'rgba(66,133,244,0.18)' }} />
             <div style={{ position: 'absolute', inset: 3, borderRadius: '50%', background: '#4285F4', border: '2.5px solid white' }} />
           </div>
         </AdvancedMarker>
       )}
       ```

    5. Fullscreen map venue pins (~line 330-339): replace each `<Marker>` with:
       ```
       <AdvancedMarker key={p.id} position={{ lat: p.latitude, lng: p.longitude }} zIndex={valittu?.id === p.id ? 10 : 1} onClick={() => setValittu(p)}>
         <img src={pinUrl(color, p.laji)} width={28} height={38} alt="" className="gmap-pin" data-active={valittu?.id === p.id ? "true" : undefined} />
       </AdvancedMarker>
       ```

    6. Fullscreen map user-location Marker (~line 341-347): replace with the same HTML div pattern as step 4.

    In lib/sportPins.ts:
    7. Delete the entire `userLocationPinUrl()` function (lines 16-19) — its usage in Etusivu.tsx has been replaced by the inline HTML div in steps 4 and 6. The pinUrl() function must NOT be changed.
  </action>

  <verify>
    <automated>grep -r "from '@vis.gl/react-google-maps'" C:/ClaudeCodeTestit/liikuntahakemisto/app/components/Etusivu.tsx</automated>
    <automated>npm run build --prefix C:/ClaudeCodeTestit/liikuntahakemisto 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Etusivu.tsx does NOT import `Marker` from @vis.gl/react-google-maps (string "Marker" must not appear in the import line from that package)
    - Etusivu.tsx imports `AdvancedMarker` from @vis.gl/react-google-maps
    - Etusivu.tsx does NOT import `userLocationPinUrl` from lib/sportPins
    - Etusivu.tsx contains exactly 0 occurrences of `<Marker` (the JSX element)
    - Etusivu.tsx contains `className="gmap-pin"` on venue pin img elements
    - Etusivu.tsx contains `data-active=` on venue pin img elements
    - lib/sportPins.ts does NOT contain `userLocationPinUrl`
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>Both map instances render venue pins as AdvancedMarker with img child carrying gmap-pin class and data-active attribute; user location renders as AdvancedMarker with HTML div; no Marker JSX elements remain in Etusivu.tsx; userLocationPinUrl deleted from sportPins.ts; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add day/night mapId switching and remove MapStyleController</name>
  <files>app/components/Etusivu.tsx, lib/mapStyles.ts, .env.local.example</files>

  <read_first>
    - app/components/Etusivu.tsx — after Task 1 changes; focus on the MapStyleController function (~line 40-47), its two render sites inside the Map components (~line 281 and ~line 350), and the DAY_MAP_STYLES/NIGHT_MAP_STYLES import on line 14
    - lib/mapStyles.ts — understand the three exports: isNightHour (MUST be kept), DAY_MAP_STYLES, NIGHT_MAP_STYLES (may be deleted)
    - .planning/phases/07-map-infrastructure/07-CONTEXT.md — decisions D-04 through D-07 and the env var documentation requirement
    - .planning/phases/07-map-infrastructure/07-UI-SPEC.md — Map ID Switching component contract
  </read_first>

  <action>
    In app/components/Etusivu.tsx:

    1. Replace the import from lib/mapStyles.ts on line 14. Currently: `import { DAY_MAP_STYLES, NIGHT_MAP_STYLES, isNightHour } from '@/lib/mapStyles'`. Change to: `import { isNightHour } from '@/lib/mapStyles'`. (DAY_MAP_STYLES and NIGHT_MAP_STYLES are no longer needed.)

    2. Add these two module-level constants immediately after the import block, before the EASE_DRAWER constant:
       ```
       const DAY_ID = process.env.NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_DAY
       const NIGHT_ID = process.env.NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_NIGHT
       ```

    3. Delete the entire `MapStyleController` function (approx lines 40-47 — the function that calls `map.setOptions({ styles: ... })`).

    4. Preview Map component: add `mapId={isDark ? NIGHT_ID : DAY_ID}` to the `<Map>` props and remove the `<MapStyleController isDark={isDark} />` child element inside it.

    5. Fullscreen Map component: add `mapId={isDark ? NIGHT_ID : DAY_ID}` to the `<Map>` props and remove the `<MapStyleController isDark={isDark} />` child element inside it.

    In lib/mapStyles.ts:
    6. Delete the `DAY_MAP_STYLES` constant (lines 7-22) and `NIGHT_MAP_STYLES` constant (lines 25-49). Keep `isNightHour` (lines 1-4) unchanged. The file should only contain `isNightHour` after this change.

    Create .env.local.example:
    7. Create the file C:/ClaudeCodeTestit/liikuntahakemisto/.env.local.example with this content — comment-only lines explaining each var, with placeholder values:
       ```
       # Supabase
       NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
       NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
       SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

       # Google Maps
       NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-js-api-key
       GOOGLE_PLACES_API_KEY=your-google-places-api-key-server-only

       # Google Maps Map IDs (create in Google Cloud Console -> Maps -> Map Management)
       # Day theme Map ID — configure styles in Cloud Console for light/day appearance
       NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_DAY=your-day-map-id
       # Night theme Map ID — configure styles in Cloud Console for dark/night appearance
       NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_NIGHT=your-night-map-id

       # AI
       ANTHROPIC_API_KEY=your-anthropic-api-key

       # Admin
       ADMIN_SECRET=your-admin-secret
       ```
  </action>

  <verify>
    <automated>npm run build --prefix C:/ClaudeCodeTestit/liikuntahakemisto 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Etusivu.tsx does NOT import `DAY_MAP_STYLES` or `NIGHT_MAP_STYLES` from lib/mapStyles
    - Etusivu.tsx imports `isNightHour` from lib/mapStyles
    - Etusivu.tsx contains `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_DAY` (as part of the const DAY_ID line)
    - Etusivu.tsx contains `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_NIGHT` (as part of the const NIGHT_ID line)
    - Etusivu.tsx contains `mapId={isDark ? NIGHT_ID : DAY_ID}` — must appear twice (once per Map instance)
    - Etusivu.tsx does NOT contain `MapStyleController` (the function definition is removed)
    - Etusivu.tsx does NOT contain `setOptions` (the call inside MapStyleController is gone)
    - lib/mapStyles.ts contains `isNightHour` function
    - lib/mapStyles.ts does NOT contain `DAY_MAP_STYLES`
    - lib/mapStyles.ts does NOT contain `NIGHT_MAP_STYLES`
    - .env.local.example exists and contains `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_DAY`
    - .env.local.example exists and contains `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_NIGHT`
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>Both Map instances pass mapId based on isDark; MapStyleController function deleted; DAY_MAP_STYLES/NIGHT_MAP_STYLES removed from lib/mapStyles.ts; isNightHour kept; .env.local.example created with both Map ID vars documented; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars → client bundle | NEXT_PUBLIC_* vars are baked into the client bundle at build time — values are public |
| user GPS → map.panTo | coords value comes from browser Geolocation API, used only for map centering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_DAY/NIGHT | accept | Map IDs are non-secret; they are public identifiers with HTTP referrer restrictions set in Cloud Console |
| T-07-02 | Spoofing | userLocation AdvancedMarker | accept | GPS coords come from the browser's Geolocation API; no user-controlled input path |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase — @vis.gl/react-google-maps and lucide-react already in package.json |
</threat_model>

<verification>
After both tasks complete:
1. `npm run build` exits 0 with no TypeScript errors
2. `grep -r "Marker" app/components/Etusivu.tsx` — should only return lines containing `AdvancedMarker`, no bare `Marker` JSX
3. `grep "MapStyleController" app/components/Etusivu.tsx` — returns nothing
4. `grep "DAY_MAP_STYLES\|NIGHT_MAP_STYLES" app/components/Etusivu.tsx` — returns nothing
5. `.env.local.example` exists and contains both `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_DAY` and `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID_NIGHT`
</verification>

<success_criteria>
- Zero `<Marker` JSX elements in Etusivu.tsx
- `AdvancedMarker` used for all pins (venue + user location) in both map instances
- Both `<Map>` instances pass `mapId` prop switching on `isDark`
- `MapStyleController` function and its render sites deleted
- `DAY_MAP_STYLES` and `NIGHT_MAP_STYLES` removed from lib/mapStyles.ts; `isNightHour` intact
- `userLocationPinUrl()` deleted from lib/sportPins.ts
- `.env.local.example` created with both new Map ID env vars documented
- `npm run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/07-map-infrastructure/07-01-SUMMARY.md` when done
</output>
