---
phase: 58-admin-p-sy-kartta-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/[id]/page.tsx
autonomous: false
requirements:
  - ADMIN-07
must_haves:
  truths:
    - "Admin detail page (/admin/[id]) shows a new 'Sijainti' section above the Listakortti preview"
    - "The Sijainti map centers on the venue's latitude/longitude at fixed zoom 15"
    - "The map is zoomable and pannable via standard Google Maps gestures (not a static image)"
    - "Clicking the SportPin reveals the CalloutCard popup"
    - "Clicking the CalloutCard does nothing — no navigation, no PaikkaSheet, no action (D-08)"
  artifacts:
    - path: "app/admin/[id]/page.tsx"
      provides: "New 'Sijainti' map section rendering Map + AdvancedMarker + SportPin + CalloutCard"
      contains: "Sijainti"
  key_links:
    - from: "app/admin/[id]/page.tsx"
      to: "@vis.gl/react-google-maps"
      via: "import of Map and AdvancedMarker"
      pattern: "from '@vis.gl/react-google-maps'"
    - from: "app/admin/[id]/page.tsx"
      to: "app/components/SportPin.tsx"
      via: "SportPin import and render inside AdvancedMarker"
      pattern: "SportPin"
    - from: "app/admin/[id]/page.tsx"
      to: "app/components/CalloutCard.tsx"
      via: "CalloutCard import, rendered with NO onClick wrapper"
      pattern: "CalloutCard"
---

<objective>
Add a read-only "Sijainti" (location) map section to the admin application detail page (`app/admin/[id]/page.tsx`) that shows the single venue being reviewed on an interactive Google Map, so the admin can visually verify the venue's pin position before approving/rejecting it. (ADMIN-07; implements D-03 through D-10.)

Purpose: Give the admin a spatial check of venue placement directly in the approval flow, reusing the exact map primitives from the customer-facing map.
Output: A modified `app/admin/[id]/page.tsx` with a new "Sijainti" section composed of `Map` + `AdvancedMarker` + `SportPin` + `CalloutCard`, plus a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-admin-p-sy-kartta-qa/58-CONTEXT.md
@.planning/phases/58-admin-p-sy-kartta-qa/58-PATTERNS.md
@.planning/phases/58-admin-p-sy-kartta-qa/58-UI-SPEC.md
</context>

## Artifacts this phase produces

This phase modifies one file and produces these new symbols. The executor MAY extract a small `AdminVenueMap` component or inline the markup in the page — extraction is left to executor discretion (D-11 / CONTEXT.md "Claude's Discretion"). Whichever path is chosen, the following must exist:

- A new **"Sijainti" section** in `app/admin/[id]/page.tsx`, placed as the FIRST of the preview-style sections (before "Listakortti"), inside the `{paikka && (...)}` guard.
- A **single-venue interactive map**: one `Map` (from `@vis.gl/react-google-maps`) containing exactly one `AdvancedMarker`.
- A **local boolean toggle state** (e.g. `useState<boolean>`) controlling CalloutCard visibility on pin click — no zoom-level / clustering / AnimatePresence machinery.
- If extracted: a new file `app/components/AdminVenueMap.tsx` (a `'use client'` component taking `paikka` as a prop). If inlined: the same markup placed directly in `page.tsx`. Either is acceptable — name it explicitly in the SUMMARY.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add read-only "Sijainti" venue-location map section to admin detail page</name>
  <files>app/admin/[id]/page.tsx (and optionally a new app/components/AdminVenueMap.tsx if extracted)</files>
  <read_first>
    - app/admin/[id]/page.tsx — the file being modified; see the existing SectionLabel pattern (lines 204-206), the `{paikka && (...)}` guard (line 175), the Listakortti/Diagonaalikortti/Profiilisivu sections (lines 177-198), and the `const paikka = link.liikuntapaikat` destructure (line 97). The new section goes FIRST, before Listakortti, inside the existing `{paikka && (` fragment.
    - app/components/SijaintiPicker.tsx — reference for the EXACT map container markup and styling (lines 13, 115-122): the outer `div` classes, the 320px height, and the `MAP_ID = process.env.NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID` module constant.
    - app/components/Etusivu.tsx — reference for the AdvancedMarker + SportPin + CalloutCard composition (lines 883-930). NOTE: line 911's onClick (which opens PaikkaSheet via setValittu) is exactly the handler that must NOT be replicated here.
    - app/components/SportPin.tsx — pin component reused as-is; takes a `laji: string` prop.
    - app/components/CalloutCard.tsx — popup component reused as-is; its prop type is `p: Liikuntapaikka & { latitude: number; longitude: number }` (lines 58-63), so lat/lng must be narrowed to non-null before passing.
    - lib/types.ts — confirm `Liikuntapaikka.latitude`/`longitude` are typed `number | null` (the reason a narrowing guard/assertion is needed even though D-10 confirms they are always populated).
    - .planning/phases/58-admin-p-sy-kartta-qa/58-UI-SPEC.md — the Interaction Contract and Container & Layout Contract are the binding spec.
  </read_first>
  <action>
Add a new "Sijainti" section to `app/admin/[id]/page.tsx`. Place it as the FIRST child inside the existing `{paikka && (` fragment, before the "Listakortti" section (i.e. between the "Toiminnot" approve/reject block that closes around line 172 and the current first preview section at line 177).

Section wrapper: a bare `<div className="flex flex-col gap-2">` containing `<SectionLabel>Sijainti</SectionLabel>` followed by the map container. Do NOT wrap the map in a `glass rounded-2xl p-5` card — the map container supplies its own border, and a glass wrapper would double-border it (UI-SPEC Spacing Exceptions).

Map container: copy the exact outer-div from `SijaintiPicker.tsx` line 115 — className `relative rounded-2xl overflow-hidden border border-[rgba(0,0,0,0.07)]` with inline `style={{ width: '100%', height: '320px' }}`.

Inside it render a `Map` from `@vis.gl/react-google-maps` with: `mapId={MAP_ID}` where `MAP_ID = process.env.NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID` (add this as a module-level const, same as SijaintiPicker line 13); `defaultCenter={{ lat: paikka.latitude, lng: paikka.longitude }}`; `defaultZoom={15}` (fixed close zoom per D-07 — NOT 13, and NO auto-fit-bounds logic); `gestureHandling="greedy"`; and the same `style={{ width: '100%', height: '320px' }}`.

Imports to add at the top of the file (the file already uses the `@/app/components/...` alias, e.g. line 6): `import { Map, AdvancedMarker } from '@vis.gl/react-google-maps'`, `import SportPin from '@/app/components/SportPin'`, `import CalloutCard from '@/app/components/CalloutCard'`. Also import `useState` if not already imported (it is — line 3).

Inside the `Map`, render exactly ONE `AdvancedMarker` at `position={{ lat: paikka.latitude, lng: paikka.longitude }}`. No `.map()` over a collection, no cluster branch, no Supercluster, no other pins (D-05). Inside the marker, render `<SportPin laji={paikka.laji} />` and a conditionally-shown `<CalloutCard p={...} />`.

Pin↔card interaction: add a local `useState<boolean>` (e.g. `showCallout`) that starts `false`. Clicking the SportPin sets it `true` (revealing CalloutCard); the SportPin is shown when the callout is hidden and the CalloutCard when shown — a simple show/hide. Do NOT replicate Etusivu's zoom-level / nearestCardId / AnimatePresence machinery (UI-SPEC Interaction Contract: a simple toggle is sufficient since there is only one marker).

CRITICAL — CalloutCard click behavior (D-08): The `CalloutCard` (and its wrapping element) must receive NO `onClick` prop at all. Do not add an onClick that opens PaikkaSheet, do not add a no-op `() => {}`, and do not add a handler that only calls `stopPropagation`/`preventDefault`. Implement the read-only behavior as the literal ABSENCE of any click handler on the card. Do NOT add `pointer-events-none` — the card must look identical to the main map's version, it simply has no wired behavior. (The pin's own click handler that toggles `showCallout` is fine and required; the prohibition is specifically the card.)

CalloutCard prop narrowing: `CalloutCard` requires `p: Liikuntapaikka & { latitude: number; longitude: number }`, but `paikka.latitude`/`longitude` are typed `number | null`. Narrow before passing — either a guard (`paikka.latitude != null && paikka.longitude != null &&` around the marker render) or a non-null assertion when constructing the prop object. D-10 confirms values are always populated, so no placeholder/fallback UI is needed (do NOT build an empty/missing-coordinate state).

Optional (executor discretion, not required): an `onClick` on the `Map` that hides the CalloutCard (sets `showCallout` false), mirroring Etusivu's dismiss-on-background-click. Skip if it complicates the single-marker toggle.

Extraction is optional: you MAY extract this into `app/components/AdminVenueMap.tsx` (a `'use client'` component taking `paikka` as a prop) and render `<AdminVenueMap paikka={paikka} />` in the section, OR inline the markup directly in `page.tsx`. Either is acceptable per D-11.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with no new type errors (confirms the lat/lng narrowing for CalloutCard's non-null prop type is correct).
    - `grep -n "Sijainti" "app/admin/[id]/page.tsx"` matches the new SectionLabel (the section exists).
    - The new section appears BEFORE the "Listakortti" SectionLabel in source order (verify line number of "Sijainti" < line number of "Listakortti").
    - The file imports `Map` and `AdvancedMarker` from `@vis.gl/react-google-maps`, and `SportPin` + `CalloutCard` from `@/app/components/...`.
    - The `Map` is rendered with `defaultZoom={15}` and `gestureHandling="greedy"` (D-06/D-07). There is no `fitBounds` / auto-fit-bounds call anywhere in the new code.
    - Exactly one `AdvancedMarker` is rendered in the new section (no `.map(` over a venue collection, no cluster branch).
    - The `CalloutCard` element in the new section has NO `onClick` prop on it or any wrapping element around it (D-08) — verified by source inspection: the card is rendered as a plain `<CalloutCard p={...} />` with no click handler and no `pointer-events-none`.
    - The pin (SportPin / its wrapper) DOES have a click handler that toggles the local boolean callout-visibility state.
    - No `glass rounded-2xl p-5` wrapper is applied to the new section's outer div (it uses the bare `flex flex-col gap-2` + self-bordered map container pattern).
    - The map container div uses `style={{ width: '100%', height: '320px' }}` and className includes `rounded-2xl overflow-hidden border border-[rgba(0,0,0,0.07)]` (D-09).
    - No null-coordinate placeholder/fallback UI is added (D-10) — the section renders inside the existing `{paikka && (...)}` guard only.
  </acceptance_criteria>
  <done>
The admin detail page renders a new "Sijainti" section (first preview section) with an interactive, zoomable Google Map centered on the venue at zoom 15, showing one SportPin whose click reveals a CalloutCard that has no wired click behavior. TypeScript compiles cleanly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the admin venue-location map renders and behaves read-only</name>
  <action>Pause for human verification. Present the eight verification steps below, wait for the operator to manually exercise the new "Sijainti" map section on a live `/admin/[id]` page, and only resume on the "approved" signal. Do not auto-approve — this checkpoint confirms the two behaviors easiest to get subtly wrong: the D-08 read-only CalloutCard (clicking it does nothing) and the D-07 fixed zoom 15.</action>
  <what-built>A read-only "Sijainti" map section on `/admin/[id]` showing the single venue pin (SportPin) on an interactive Google Map at zoom 15, with a CalloutCard on pin click that does nothing when itself clicked.</what-built>
  <how-to-verify>
1. Start the dev server (`npm run dev`) and sign in as the admin (joona.orava@gmail.com).
2. Navigate to `/admin`, open any pending or approved application detail page (`/admin/[id]`).
3. Confirm a new "Sijainti" section appears ABOVE the "Listakortti" preview, with a 320px-tall bordered, rounded map.
4. Confirm the map is centered on the venue and zoomed in close (street-level, ~zoom 15), showing exactly one blue SportPin at the venue location.
5. Confirm the map is zoomable and pannable (scroll/drag/pinch work) — it is not a static image.
6. Click the SportPin → the CalloutCard popup appears.
7. Click the CalloutCard itself → NOTHING happens: no bottom sheet (PaikkaSheet) opens, no navigation, no page change. This is the key read-only behavior (D-08).
8. Confirm there is only one pin (no other venues, no clustering, no user-location dot, no recenter button).
  </how-to-verify>
  <resume-signal>Type "approved" if all eight checks pass, or describe what is wrong (e.g. "clicking the callout opens the sheet", "map shows at wrong zoom").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/admin/applications/[id] | Admin client fetches venue data (incl. latitude/longitude) across this boundary; existing route enforces auth |
| browser → Google Maps JS API | Map tiles/markers loaded client-side using the public `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Information Disclosure | venue lat/lng shown on admin map | accept | Data is already returned to this page by the existing `/api/admin/applications/[id]` GET route, which enforces `supabaseAdmin.auth.getUser(token)` + a `profiles.is_admin` check (route lines 6-14) and the page redirects on non-OK (page line 43). This phase adds NO new data path or fetch — it only renders coordinates already present in the guarded `paikka` object. Do not weaken or bypass the existing admin guard. |
| T-58-02 | Information Disclosure | NEXT_PUBLIC_GOOGLE_MAPS_API_KEY exposed client-side | accept | Pre-existing, documented client-side key (CLAUDE.md env table) with HTTP-referrer restrictions; identical to the customer map and SijaintiPicker. Not a new threat introduced by this phase. |
| T-58-03 | Tampering | npm/pip/cargo installs | accept | This phase introduces ZERO new dependencies (UI-SPEC Registry Safety) — all primitives (`@vis.gl/react-google-maps`, `SportPin`, `CalloutCard`) are already in package.json and previously audited. No install step, so the slopcheck/legitimacy gate is not triggered. |
| T-58-SC | Tampering | supply-chain (package installs) | accept | No package installs in this phase; T-58-SC carried for register completeness only. |

No high-severity threats found; nothing meets the `block_on: high` threshold. The phase is a read-only viewer behind the existing admin auth guard.
</threat_model>

<verification>
- `npx tsc --noEmit` passes (no new type errors from the lat/lng narrowing).
- Source inspection confirms: one `AdvancedMarker`, `defaultZoom={15}`, no `fitBounds`, no `onClick` on `CalloutCard`, no `glass` wrapper on the section.
- Human-verify checkpoint (Task 2) confirms the eight observable behaviors, especially the D-08 read-only CalloutCard and the D-07 fixed zoom.
</verification>

<success_criteria>
1. `/admin/[id]` has a new "Sijainti" section showing the venue pin on a map in the same SportPin/CalloutCard style as the main map (ROADMAP success criterion 1).
2. The map is zoomable/pannable and centered on the venue's coordinates at fixed close zoom ~15 (ROADMAP success criterion 2; D-06/D-07).
3. Clicking the pin shows the CalloutCard, but the CalloutCard does NOT open the venue page or trigger any navigation (ROADMAP success criterion 3; D-08).
</success_criteria>

<output>
Create `.planning/phases/58-admin-p-sy-kartta-qa/58-01-SUMMARY.md` when done. In the SUMMARY, explicitly state whether the map was extracted into `app/components/AdminVenueMap.tsx` or inlined in `app/admin/[id]/page.tsx`, and which lat/lng narrowing approach (guard vs. non-null assertion) was used.
</output>
