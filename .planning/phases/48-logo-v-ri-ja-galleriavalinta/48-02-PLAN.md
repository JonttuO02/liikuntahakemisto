---
phase: 48-logo-v-ri-ja-galleriavalinta
plan: 02
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - app/business/onboarding/page.tsx
  - app/business/onboarding/AnalysoiSivusto.tsx
autonomous: true
requirements: [ONBOARD-14, ONBOARD-15]
must_haves:
  truths:
    - "AnalysoiSivusto resolves a paikka_id and passes it on every analyze-website GET/POST and every branding PATCH call (currently those calls omit the now-required paikka_id and fail at runtime)"
    - "When multiple logo candidates exist, the user sees them all as radio-style cards with the first pre-selected, and can pick exactly one"
    - "The user picks a background color and an accent color from up to 6 swatches (slots pre-filled from each color's role), or enters a custom #rrggbb hex"
    - "Each selection change autosaves immediately via PATCH /api/business/branding"
    - "A failed autosave shows the inline Finnish error and does not silently lose the selection"
  artifacts:
    - path: "app/business/onboarding/AnalysoiSivusto.tsx"
      provides: "Logo picker, color picker (swatch + slots + custom hex), autosave PATCH wiring, paikka_id resolution"
      contains: "patchBranding"
    - path: "app/business/onboarding/page.tsx"
      provides: "paikka_id resolution passed into AnalysoiSivusto"
      contains: "paikkaId"
  key_links:
    - from: "app/business/onboarding/AnalysoiSivusto.tsx"
      to: "/api/business/branding"
      via: "PATCH autosave per selection"
      pattern: "fetch\\(['\"]/api/business/branding"
    - from: "app/business/onboarding/AnalysoiSivusto.tsx"
      to: "/api/business/analyze-website"
      via: "GET/POST now include paikka_id"
      pattern: "paikka_id"
---

<objective>
Turn the read-only `'preview'` block of `AnalysoiSivusto.tsx` into interactive logo and color pickers with immediate autosave, and fix the pre-existing `paikka_id` gap so the pre-vaihe actually works against Phase 47's now-required `paikka_id` parameter.

Purpose: Delivers ONBOARD-14 (explicit logo pick) and ONBOARD-15 (background+accent color pick). Without resolving `paikka_id` here, the GET/POST analyze-website calls (and the new PATCH) all 400 — so this plan also closes that integration gap as a prerequisite for any selection to persist.
Output: A reworked `AnalysoiSivusto.tsx` preview block and a `page.tsx` that supplies `paikka_id`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-logo-v-ri-ja-galleriavalinta/48-CONTEXT.md
@.planning/phases/48-logo-v-ri-ja-galleriavalinta/48-PATTERNS.md
@.planning/phases/48-logo-v-ri-ja-galleriavalinta/48-UI-SPEC.md

<interfaces>
<!-- Reshaped BrandingResult (delivered by Plan 01) — AnalysoiSivusto consumes these: -->
BrandingResult.logo_candidates: Array<{ url: string; type: string }> | null
BrandingResult.colors:          Array<{ hex: string; role: string }> | null
BrandingResult.selected_background_color: string | null
BrandingResult.selected_accent_color:     string | null

<!-- PATCH /api/business/branding (delivered by Plan 01) accepts an autosave partial body: -->
{ paikka_id: number,
  selected_background_color?: string, background_color_source?: 'ai'|'custom',
  selected_accent_color?: string,     accent_color_source?: 'ai'|'custom',
  image_urls?: string[] }   // gallery handled in Plan 03; this plan sends color fields

<!-- analyze-website GET/POST NOW REQUIRE paikka_id (Phase 47):
     GET  /api/business/analyze-website?paikka_id=<n>
     POST body: { url, paikka_id }   -->

<!-- Existing reusable primitives in AnalysoiSivusto.tsx (do not replace):
     Spinner (line 28), LabelCaps (37), PrimaryButton (45, has `loading` prop), MutedButton (69),
     getAuthToken (18). getContrastColor importable from '@/lib/branding/brandingResult'. -->
</interfaces>

<copywriting>
<!-- Verbatim Finnish copy from 48-UI-SPEC.md Copywriting Contract: -->
Logo section header (LabelCaps):        Logo
Logo helper (only if >1 candidate):     Valitse logo, jota käytetään profiilissasi
Color section header (LabelCaps):       Brändivärit
Color slot labels:                      Tausta  /  Aksentti
Color helper:                           Valitse taustaväri ja aksenttiväri löydetyistä sävyistä, tai syötä oma värikoodi
Custom hex placeholder:                 #rrggbb
Custom hex aria-label:                  Oma värikoodi
Empty — no logos:                       Logoa ei löytynyt automaattisesti — voit lisätä sen myöhemmin velhon Mediat-vaiheessa
Empty — 0 colors:                       Värejä ei löytynyt — syötä omat värikoodit alla
Empty — 1 color (accent placeholder):   Valitse aksenttiväri
Autosave failure (role="alert"):        Valinnan tallennus epäonnistui. Yritä uudelleen.
Custom hex invalid:                     Anna värikoodi muodossa #rrggbb
</copywriting>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Resolve and thread paikka_id through page.tsx and AnalysoiSivusto's analyze-website calls</name>
  <files>app/business/onboarding/page.tsx, app/business/onboarding/AnalysoiSivusto.tsx</files>
  <read_first>
    - app/business/onboarding/page.tsx (full file — currently renders `<AnalysoiSivusto onConfirm onSkip />` with no paikka_id; handleConfirm/handleSkip at lines 14-22)
    - app/business/onboarding/AnalysoiSivusto.tsx (full file — the three fetch call sites that now need paikka_id: checkStatus GET lines 110-112, poll GET lines 169-171, handleSubmit POST lines 229-236)
    - app/business/WizardInner.tsx (OnboardingMode lines 35-144 — the exact paikka_id resolution logic to mirror: URL `paikka_id` param first, then `business_paikka_links` lookup `.eq('business_account_id', user.id).limit(1).maybeSingle()`)
    - app/api/business/analyze-website/route.ts (POST requires `paikka_id` at lines 167-169; GET requires `?paikka_id=` at lines 233-235)
    - lib/supabase-business.ts (createBusinessBrowserClient — used for the links lookup)
  </read_first>
  <action>
    Resolve `paikka_id` once in `page.tsx` and pass it to `AnalysoiSivusto` as a required `paikkaId: number` prop. In `OnboardingWizardPage`, add an effect that: reads `paikka_id` from `useSearchParams()` first; if absent, looks it up via `createBusinessBrowserClient()` from `business_paikka_links` `.select('paikka_id').eq('business_account_id', user.id).limit(1).maybeSingle()` (the same resolution WizardInner's OnboardingMode uses at lines 70-85). Hold it in state; render a small spinner (reuse the existing inline spinner markup already in page.tsx's Suspense fallback) until resolved; render `AnalysoiSivusto` only once `paikkaId !== null`. Note: `page.tsx` is currently a plain client component without `useSearchParams`; add `import { useSearchParams } from 'next/navigation'` and keep `'use client'`.

    In `AnalysoiSivusto.tsx`, add `paikkaId: number` to `AnalysoiSivustoProps` and destructure it. Update all three analyze-website fetches: append `?paikka_id=${paikkaId}` to the two GET URLs (checkStatus, poll) and add `paikka_id: paikkaId` to the POST body in `handleSubmit`. Do NOT change `onSkip`'s behavior. Do NOT change `onConfirm`'s signature in this task (Plan 03 extends it).

    Because `page.tsx` reads searchParams, ensure the page still renders under Suspense if Next requires it — if `useSearchParams` triggers a CSR-bailout build warning, wrap the `AnalysoiSivusto`-rendering subtree in the existing `<Suspense>` already present in the file (extend its usage rather than adding a new boundary).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "AnalysoiSivusto|onboarding/page" || echo "paikka_id wiring typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - `AnalysoiSivustoProps` includes `paikkaId: number` (source assertion)
    - All three analyze-website fetches reference `paikka_id`: grep `grep -c "paikka_id" app/business/onboarding/AnalysoiSivusto.tsx` returns ≥ 3
    - `page.tsx` resolves paikka_id from `useSearchParams` then `business_paikka_links` and passes `paikkaId` to `<AnalysoiSivusto>` (grep: `paikkaId={` present)
    - `AnalysoiSivusto` is not rendered while paikkaId is null (a guard/spinner precedes it)
    - `npx tsc --noEmit` reports no new errors in either file
  </acceptance_criteria>
  <done>The pre-vaihe resolves paikka_id and every analyze-website GET/POST sends it, so the screen functions against Phase 47's required parameter.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace the read-only logo/color blocks with interactive pickers + autosave PATCH</name>
  <files>app/business/onboarding/AnalysoiSivusto.tsx</files>
  <read_first>
    - app/business/onboarding/AnalysoiSivusto.tsx (the 'preview' render block lines 344-429 — logo block 350-360, colors block 362-377, footer 412-427; local primitives LabelCaps/PrimaryButton/Spinner/getAuthToken)
    - .planning/phases/48-logo-v-ri-ja-galleriavalinta/48-UI-SPEC.md (Component Inventory rows for Logo picker, Color picker; Color section reserved-accent rules; Spacing exceptions for 44px tap targets; Animation row for whileTap)
    - .planning/phases/48-logo-v-ri-ja-galleriavalinta/48-CONTEXT.md (D-12 color slot layout, D-13 role pre-fill, D-14 fewer-than-2-colors fallback, D-07 immediate autosave, D-09 custom hex override)
    - lib/branding/brandingResult.ts (reshaped BrandingResult + getContrastColor for swatch text legibility — delivered by Plan 01)
    - CLAUDE.md (glassmorphism palette, 4-size/2-weight typography, accent #111111 reserved for selected/CTA states only)
  </read_first>
  <action>
    Add local selection state to the component: `selectedLogoUrl: string | null`, `bgColor: string | null`, `accentColor: string | null`, `bgSource: 'ai'|'custom'`, `accentSource: 'ai'|'custom'`, `armedSlot: 'tausta'|'aksentti'|null`, `saveError: string | null`, and a `savingSection: 'logo'|'colors'|null` for the inline spinner. Initialize from `brandingResult` when it resolves (in the existing checkStatus/poll success paths or a derived effect): `selectedLogoUrl` = `brandingResult.logo_candidates?.[0]?.url ?? brandingResult.logo_url`; `bgColor`/`bgSource` from `selected_background_color` (source 'ai') else first `colors[].role==='background'` (source 'ai') else null; `accentColor`/`accentSource` from `selected_accent_color` else first `colors[].role==='accent'` else null (D-13).

    Add a `patchBranding(partial)` helper modeled on `handleSubmit`'s fetch shape: `getAuthToken()` → `fetch('/api/business/branding', { method: 'PATCH', headers: { 'Content-Type':'application/json', Authorization: 'Bearer '+token }, body: JSON.stringify({ paikka_id: paikkaId, ...partial }) })`; on `!res.ok` set `saveError` to `Valinnan tallennus epäonnistui. Yritä uudelleen.`; clear `saveError` on a subsequent success. Show the inline `Spinner className="w-4 h-4"` next to the relevant LabelCaps while in flight (per UI-SPEC Animation row, fade via the existing AnimatePresence opacity pattern).

    Logo picker (replaces lines 350-360): render only when `logo_candidates?.length` (else show the empty copy `Logoa ei löytynyt automaattisesti — voit lisätä sen myöhemmin velhon Mediat-vaiheessa` OR hide the section). Map `logo_candidates` to radio-style `<button>` cards (`border border-[rgba(0,0,0,0.12)] rounded-lg p-2`, full card is the tap target ≥44px), each showing the candidate image at `h-12 w-auto object-contain` plus its `type` as a small caption. Selected card gets `border-[#111111] ring-2 ring-[#111111]`. Clicking sets `selectedLogoUrl` (no PATCH — there is no logo column; logo selection is client-state carried forward by Plan 03). Show the helper `Valitse logo, jota käytetään profiilissasi` only when `length > 1`. At `length === 1` still render the single card pre-selected (no separate code path).

    Color picker (replaces lines 362-377): LabelCaps `Brändivärit` + helper `Valitse taustaväri ja aksenttiväri löydetyistä sävyistä, tai syötä oma värikoodi`. Row 1 = swatch row: up to 6 `<button>` swatches (`w-8 h-8 rounded-full border border-[rgba(0,0,0,0.07)]`, wrapped in a ≥44px tap target via padding) from `colors`; the swatch matching `bgColor` or `accentColor` gets `ring-2 ring-[#111111] ring-offset-2`. Clicking a swatch assigns its hex to the currently-armed slot (`armedSlot`), sets that slot's source to `'ai'`, then calls `patchBranding({ selected_background_color, background_color_source }` or the accent equivalent`)`. Row 2 = two slot mini-cards `Tausta` and `Aksentti`, each showing a `w-6 h-6 rounded-full` preview of its current color + the hex in `text-sm` (use `getContrastColor` only if rendering text on top of the swatch); clicking a slot "arms" it. Below both rows, an always-visible custom hex input (styled text field matching the existing URL input styling at line 289, `aria-label="Oma värikoodi"`, placeholder `#rrggbb`); on a valid `/^#[0-9a-fA-F]{6}$/` value it assigns to the armed slot with source `'custom'` and PATCHes; on invalid show `Anna värikoodi muodossa #rrggbb`. D-14 fallbacks: 0 colors → no swatch row, show `Värejä ei löytynyt — syötä omat värikoodit alla`, both slots use custom hex; 1 color → background pre-filled, accent slot shows custom input with placeholder context `Valitse aksenttiväri`. Color selection NEVER disables the footer buttons.

    Keep the existing Prices and Opening hours read-only blocks (lines 379-410) unchanged. Keep the footer's existing `Analysoi uudelleen` + `Jatka velhoon →` (Plan 03 adds quick-accept). Surface `saveError` inline with `text-red-600 role="alert"` near the pickers. Respect CLAUDE.md: only 4 font sizes, 2 weights; accent `#111111` only on selected/CTA states; use `whileTap={{ scale: 0.95 }}` at most for feedback (no hover scale).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "AnalysoiSivusto" || echo "pickers typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - Logo candidates render as selectable `<button>` cards; selected card class includes `ring-2 ring-[#111111]` (grep present)
    - Color swatches render from `colors` as buttons; a `patchBranding` helper exists and is called on color selection (grep: `patchBranding` ≥ 2 occurrences — definition + call)
    - PATCH targets `/api/business/branding` with method `PATCH` (grep: `'/api/business/branding'` and `method: 'PATCH'`)
    - Custom hex input present with `aria-label="Oma värikoodi"` and validates against `/^#[0-9a-fA-F]{6}$/`
    - Autosave failure copy `Valinnan tallennus epäonnistui. Yritä uudelleen.` present with `role="alert"`
    - Footer is never disabled by color state (no `disabled` bound to bgColor/accentColor)
    - All new copy strings match the verbatim Finnish in `<copywriting>` (grep each header/helper string)
    - `npx tsc --noEmit` reports no new errors in the file
  </acceptance_criteria>
  <done>Preview block lets the user pick one logo and assign background+accent colors (swatch or custom hex), each color change autosaving via PATCH with inline error handling; prices/hours/footer otherwise intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → PATCH /api/business/branding | User selection values cross to the server; server-side validation (Plan 01) is authoritative — client validation is UX only |
| browser → analyze-website GET/POST | paikka_id now sent from the client; server re-checks ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-06 | Tampering | client-supplied paikka_id | mitigate | Server (analyze-website + branding routes) re-validates ownership against `business_paikka_links` for the verified JWT; a forged paikka_id in the client cannot escalate (defense lives in Plan 01 / Phase 47, not the client) |
| T-48-07 | Tampering | custom hex / AI-source flag from client | mitigate | Client sends `*_source` and hex, but Plan 01's PATCH route re-validates membership/format server-side; client checks are convenience only |
| T-48-08 | Information disclosure | logo/image src rendered from URLs | accept | logo_candidates/image_urls are already same-origin Supabase Storage URLs sanitized server-side (SEC-46-02 in GET handler); no new exposure |
| T-48-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; no install task → gate not triggered |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no new errors.
- Manual: with a venue that has multiple logo candidates and ≥2 colors, all candidates render, first logo is pre-selected, background/accent slots pre-fill from role, and changing any color fires a PATCH (Network tab shows PATCH /api/business/branding 200).
- Manual: with 0 colors, the empty copy shows and both slots accept custom hex; footer stays enabled.
</verification>

<success_criteria>
- ONBOARD-14: user sees every logo candidate, AI top pick pre-selected, picks exactly one.
- ONBOARD-15: user assigns background + accent from the palette (or custom hex), not auto-assigned.
- paikka_id flows on every analyze-website and branding request — the pre-vaihe functions end to end.
- Each color change autosaves; failures are surfaced, not swallowed.
</success_criteria>

<output>
Create `.planning/phases/48-logo-v-ri-ja-galleriavalinta/48-02-SUMMARY.md` when done.
</output>
