---
phase: 48-logo-v-ri-ja-galleriavalinta
plan: 03
type: execute
wave: 3
depends_on: [48-01, 48-02]
files_modified:
  - app/business/onboarding/AnalysoiSivusto.tsx
  - app/business/onboarding/page.tsx
autonomous: true
requirements: [ONBOARD-16, FLOW-02, FLOW-03]
must_haves:
  truths:
    - "The preview screen shows a checkable grid of up to 8 scraped images; the first 5 (or fewer) start checked, capped at 5 selected"
    - "Gallery selections are persisted (autosave PATCH) and, when the user continues to the wizard, appear pre-filled in the Mediat step's photo grid"
    - "handleConfirm AWAITS the save-step write of media_urls BEFORE calling setPagePhase('wizard'), so WizardInner's on-mount draft fetch cannot race the write and StepMediat always renders the prefilled gallery"
    - "A 'Hyväksy ja lähetä' button maps the AI analysis + the user's logo/color/gallery selections into the onboarding_draft and then calls the existing submit route unmodified"
    - "The logo written into media_urls.logo is the already-server-validated selectedLogoUrl (validated by Plan 01's PATCH against logo_candidates) — no unvalidated client logo URL reaches save-step or submit"
    - "Quick-accept passes through the same ownership/validation/draft-cleanup as a full-wizard submission — no parallel write path; submit/route.ts is unchanged"
    - "Quick-accept failure shows a Finnish error offering the full-wizard fallback; a partial save-step failure is recoverable (idempotent UPSERT per field, retry re-applies) and the user is shown a clear retry path"
  artifacts:
    - path: "app/business/onboarding/AnalysoiSivusto.tsx"
      provides: "Gallery checkbox picker + autosave + quick-accept submit logic"
      contains: "Hyväksy ja lähetä"
    - path: "app/business/onboarding/page.tsx"
      provides: "handleConfirm persists gallery selection into onboarding_draft.media_urls (awaited) before entering the wizard"
      contains: "media_urls"
  key_links:
    - from: "app/business/onboarding/AnalysoiSivusto.tsx"
      to: "/api/business/onboarding/submit"
      via: "quick-accept calls existing submit unmodified after draft mapping"
      pattern: "/api/business/onboarding/submit"
    - from: "app/business/onboarding/page.tsx (handleConfirm)"
      to: "/api/business/onboarding/save-step"
      via: "AWAITS the media_urls.photos write before navigating into wizard"
      pattern: "await fetch\\(['\"]/api/business/onboarding/save-step"
---

<objective>
Add the gallery picker (ONBOARD-16) to the preview screen and wire its selections into the wizard's Mediat step, and add the "Hyväksy ja lähetä" quick-accept path (FLOW-02/FLOW-03) that maps AI results + selections into the existing `onboarding_draft` and calls the existing submit route unmodified.

Purpose: Completes the phase — gallery prefill and one-action submission. FLOW-03's invariant (no second write path) is honored by reusing save-step + submit exactly as the full wizard does.
Output: Final `AnalysoiSivusto.tsx` (gallery picker + quick-accept) and `page.tsx` (handleConfirm gallery persist + selection forwarding).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-logo-v-ri-ja-galleriavalinta/48-CONTEXT.md
@.planning/phases/48-logo-v-ri-ja-galleriavalinta/48-PATTERNS.md
@.planning/phases/48-logo-v-ri-ja-galleriavalinta/48-UI-SPEC.md

<interfaces>
<!-- save-step route (app/api/business/onboarding/save-step/route.ts) — UNCHANGED, reused:
     POST body: { paikka_id, step (1-6), field, value }
     ALLOWED_FIELDS = ['media_urls','hinnasto','aukioloajat','yhteystiedot']
     media_urls shape: { logo: string|null, photos: string[] }  (OnboardingDraft)
     Each call is an independent idempotent UPSERT on (business_account_id, paikka_id) — retrying a
     field that already succeeded simply re-writes the same value (no duplication, no corruption). -->

<!-- submit route (app/api/business/onboarding/submit/route.ts) — UNCHANGED, reused:
     POST body: { paikka_id }
     reads onboarding_draft joined with liikuntapaikat, requires business_paikka_links (ownership-only,
     NO claim_status filter), resets claim_status to 'pending', deletes draft, sends admin email.
     Internally calls hinnastaToHintaKuvaus(draft.hinnasto ?? []) — so hinnasto must be the ARRAY, not the string.
     Writes liikuntapaikat.logo_url = draft.media_urls?.logo (line 85). -->

<!-- raw_analysis → draft mapping (verbatim shapes from 48-PATTERNS.md):
     hinnasto = raw_analysis.prices.map(p => ({ kategoria: p.label, hinta: p.price, lisatieto: '' }))
     aukioloajat = Object.fromEntries(raw_analysis.opening_hours.map(e => [e.day, {open:e.open, close:e.close}]))
     yhteystiedot = { website: raw_analysis.website_url }
     media_urls = { logo: <selectedLogoUrl, already validated by Plan 01 PATCH>, photos: <selected gallery URLs capped at 5> } -->

<!-- PATCH /api/business/branding (Plan 01) accepts image_urls?: string[] (validated membership + 5-cap)
     and selected_logo_url?: string (validated against logo_candidates membership). The logo URL Plan 02
     already PATCHed is the SAME value carried here into media_urls.logo — it has passed server-side
     membership validation, so submit's write of liikuntapaikat.logo_url is on a validated value. -->

<!-- Existing StepMediat reads initialDraft?.media_urls?.photos unconditionally (line 43), and
     WizardInner re-fetches the onboarding_draft from Supabase ON MOUNT. CRITICAL race: if handleConfirm
     fires the save-step write WITHOUT awaiting it and then immediately calls setPagePhase('wizard'),
     WizardInner can mount and complete its draft fetch BEFORE the save-step write lands — StepMediat
     then renders with the stale (pre-write) draft and the gallery prefill silently disappears
     (no compile error, intermittent). handleConfirm MUST await the save-step fetch before navigating.
     This makes the integration point page.tsx's handleConfirm with ZERO StepMediat changes. -->
</interfaces>

<copywriting>
<!-- Verbatim Finnish copy from 48-UI-SPEC.md: -->
Gallery section header (LabelCaps):     Galleria
Gallery helper:                         Valitse enintään 5 kuvaa galleriaan
Gallery selection count:                {n}/5 valittu     (e.g. "3/5 valittu")
Gallery over-cap error:                 Valitse enintään 5 kuvaa
Empty — no gallery images:              Kuvia ei löytynyt automaattisesti — voit lisätä kuvia myöhemmin velhon Mediat-vaiheessa
Quick-accept CTA:                       Hyväksy ja lähetä
Quick-accept loading:                   Lähetetään...
Quick-accept failure (role="alert"):    Lähetys epäonnistui. Yritä uudelleen tai jatka velhon kautta.
Continue CTA (existing, unchanged):     Jatka velhoon →
</copywriting>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Gallery checkbox picker with autosave + prefill into the wizard's Mediat step</name>
  <files>app/business/onboarding/AnalysoiSivusto.tsx, app/business/onboarding/page.tsx</files>
  <read_first>
    - app/business/onboarding/AnalysoiSivusto.tsx (current state after Plan 02 — the picker state including selectedLogoUrl, patchBranding helper, and preview block; the onConfirm call in the footer)
    - app/business/onboarding/StepMediat.tsx (existing existingPhotoUrls initializer line 40-44, 5-photo cap `photosAtMax = totalPhotos >= 5` line 56, delete-badge corner-overlay pattern lines 384-395 — mirror this with a Check icon for selection)
    - app/business/onboarding/page.tsx (handleConfirm — where gallery selection must be written into the draft before navigation; note Plan 02 may have restructured the pre-phase into a Suspense-descendant child, so handleConfirm now receives selections from that child)
    - app/business/WizardInner.tsx (confirm the on-mount draft fetch — this is what handleConfirm's awaited save-step write must complete BEFORE, to avoid the prefill race)
    - .planning/phases/48-logo-v-ri-ja-galleriavalinta/48-CONTEXT.md (D-04/D-05/D-06 gallery rules: checkable grid of up to 8 image_urls, first 5 pre-checked, cap at 5, selections flow into StepMediat as existingPhotoUrls)
    - .planning/phases/48-logo-v-ri-ja-galleriavalinta/48-UI-SPEC.md (Gallery picker Component Inventory row: grid, w-16/w-20 thumbnails, corner Check badge, dim+disable unselected at 5)
  </read_first>
  <action>
    Gallery picker in `AnalysoiSivusto.tsx`'s preview block (place after the color picker, before Prices). Add state `selectedGallery: string[]` initialized when `brandingResult` resolves to `image_urls.slice(0, 5)` (first 5 pre-checked per D-05). Render LabelCaps `Galleria` + helper `Valitse enintään 5 kuvaa galleriaan` only when `image_urls?.length`; else show the empty copy `Kuvia ei löytynyt automaattisesti — voit lisätä kuvia myöhemmin velhon Mediat-vaiheessa` or hide the section. Render a grid (`grid grid-cols-4 gap-2` or the existing `flex flex-wrap gap-2` thumbnail idiom) of up to 8 thumbnails (`w-16 h-16 object-cover rounded-lg`, or `w-20 h-20`). Each cell has a top-right corner badge mirroring StepMediat's delete badge (`absolute -top-1 -right-1 bg-[#111111] text-white rounded-full w-5 h-5`) but with the lucide `Check` icon shown only when selected (import `Check` from `lucide-react`). Clicking a thumbnail toggles membership in `selectedGallery`. Enforce the cap: when 5 are selected, dim unselected thumbnails (`opacity-50`) and make them non-clickable; show `{n}/5 valittu` near the grid. After each toggle, autosave via the existing `patchBranding({ image_urls: selectedGallery })` helper (the PATCH route validates membership + 5-cap). The full thumbnail is the ≥44px tap target.

    Prefill into the wizard (D-06, ONBOARD-16) — integration point is `page.tsx`'s `handleConfirm`, ZERO StepMediat changes. Extend `onConfirm`'s signature to carry the user's selections forward: change `onConfirm` to `(brandingData: BrandingResult, selections: { logoUrl: string | null; gallery: string[] }) => void | Promise<void>` and call it from the existing `Jatka velhoon →` button with `{ logoUrl: selectedLogoUrl, gallery: selectedGallery }`. The `logoUrl` carried here is the server-validated value already PATCHed in Plan 02 (validated against logo_candidates), so no unvalidated logo URL enters the draft.

    In `page.tsx`, make `handleConfirm` an `async` function and AWAIT the save-step write BEFORE navigating. The exact sequence MUST be: (1) `const token = await getAuthToken()` (or the session-token equivalent already used in page.tsx / mirroring StepMediat's save-step call at lines 183-201); (2) `await fetch('/api/business/onboarding/save-step', { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: 'Bearer ' + token }, body: JSON.stringify({ paikka_id: paikkaId, step: 2, field: 'media_urls', value: { logo: selections.logoUrl, photos: selections.gallery } }) })` — this fetch MUST be `await`-ed; (3) ONLY AFTER that promise resolves, call `setPagePhase('wizard')`. Do NOT fire the fetch and navigate synchronously: `setPagePhase('wizard')` must not run until the awaited save-step fetch has settled. Reason: `WizardInner` re-fetches the `onboarding_draft` on mount; if navigation happens before the write lands, the on-mount fetch reads the stale draft and StepMediat renders without the gallery prefill (silently breaking ONBOARD-16/D-06 — no compile error, intermittent in testing). Add a short code comment on the await noting it prevents the WizardInner on-mount draft-fetch race. (Optional: if the awaited save-step rejects/`!res.ok`, surface a non-blocking error and still allow navigation, since StepMediat lets the user re-add photos — but the await itself is mandatory.)

    Because StepMediat already reads `initialDraft?.media_urls?.photos`, and WizardInner re-fetches the draft from Supabase on mount AFTER the awaited write, the selections appear in the unified grid with no StepMediat edit. Keep `onSkip` unchanged.

    Update `AnalysoiSivustoProps.onConfirm` type accordingly (allowing the async/Promise return). Respect CLAUDE.md design rules (accent only on selected/badge, 2 weights, glassmorphism).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "AnalysoiSivusto|onboarding/page" || echo "gallery + prefill typecheck"</automated>
    <automated>grep -Pzo "await fetch\(['\"]/api/business/onboarding/save-step[\s\S]*?setPagePhase\(['\"]wizard" app/business/onboarding/page.tsx >/dev/null && echo "OK: save-step is awaited before setPagePhase('wizard')" || (echo "FAIL: save-step write not awaited before navigating to wizard" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - Gallery thumbnails render from `image_urls`; selected cells show a `Check` badge; `selectedGallery` toggling is wired (grep: `selectedGallery` ≥ 3 occurrences)
    - First 5 images start selected (`image_urls.slice(0, 5)` initializer present)
    - Cap enforced: unselected thumbnails get `opacity-50` and are non-clickable once 5 selected; `{n}/5 valittu` indicator present (grep: `valittu`)
    - Gallery toggle autosaves via `patchBranding({ image_urls` (grep present)
    - `onConfirm` signature carries `{ logoUrl, gallery }`; footer `Jatka velhoon →` passes the selections (grep: `gallery:` in the onConfirm call)
    - `handleConfirm` is `async` and `await`s the `POST /api/business/onboarding/save-step` (field `media_urls`) BEFORE `setPagePhase('wizard')` — grep gate: `await fetch('/api/business/onboarding/save-step'` appears textually before `setPagePhase('wizard')` in the same function (the Task 1 multiline grep verify must pass)
    - `page.tsx handleConfirm` POSTs to `/api/business/onboarding/save-step` with `field: 'media_urls'` before entering the wizard (grep both)
    - StepMediat.tsx is NOT in this plan's files_modified and is not edited (git status shows no StepMediat change)
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>The preview screen offers a 5-cap checkable gallery that autosaves; continuing to the wizard pre-fills those images into the Mediat grid via an AWAITED save-step write in handleConfirm (completed before setPagePhase('wizard'), so WizardInner's on-mount fetch sees the write), with no StepMediat code change.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Quick-accept ("Hyväksy ja lähetä") — map to draft, call existing submit unmodified</name>
  <files>app/business/onboarding/AnalysoiSivusto.tsx</files>
  <read_first>
    - app/business/onboarding/AnalysoiSivusto.tsx (current state after Task 1 — selection state, patchBranding, footer with Jatka velhoon, getAuthToken, PrimaryButton with loading prop)
    - app/api/business/onboarding/save-step/route.ts (ALLOWED_FIELDS, step must be 1-6, media_urls/hinnasto/aukioloajat/yhteystiedot shapes — the write target, UNMODIFIED; each call is an idempotent UPSERT)
    - app/api/business/onboarding/submit/route.ts (the call target — read to confirm body is `{ paikka_id }`, that it calls hinnastaToHintaKuvaus(draft.hinnasto ?? []) so hinnasto must be the array, that it resets claim_status to pending and deletes the draft; DO NOT MODIFY)
    - lib/onboardingUtils.ts (OnboardingDraft shape lines 5-17, hinnastaToHintaKuvaus lines 65-75 — confirms field shapes; the mapping is the inverse direction, store the array not the string)
    - .planning/phases/48-logo-v-ri-ja-galleriavalinta/48-CONTEXT.md (D-10 zero changes to submit/route.ts, D-11 draft-mapping-then-submit sequence)
    - .planning/phases/48-logo-v-ri-ja-galleriavalinta/48-PATTERNS.md (Quick-accept draft-mapping section: exact hinnasto/aukioloajat/yhteystiedot/media_urls mapping + write-then-submit sequence)
  </read_first>
  <action>
    Add a `handleQuickAccept` async function and a second `PrimaryButton` labeled `Hyväksy ja lähetä` in the footer next to `Jatka velhoon →` (both `bg-[#111111]` equal weight per D-02/UI-SPEC; on narrow widths stack vertically with 8px gap). Add `submittingQuick: boolean` state to drive the button's `loading` prop (showing `Lähetetään...`) and a `quickError: string | null`.

    `handleQuickAccept` builds the draft fields from `brandingResult.raw_analysis` + selections, then writes each field via the UNCHANGED save-step route, then calls the UNCHANGED submit route (D-10/D-11 — no parallel write path):
      - `hinnasto = (raw_analysis.prices ?? []).map(p => ({ kategoria: p.label, hinta: p.price, lisatieto: '' }))` — store the ARRAY (submit calls hinnastaToHintaKuvaus itself).
      - `aukioloajat = Object.fromEntries((raw_analysis.opening_hours ?? []).map(e => [e.day, { open: e.open, close: e.close }]))`.
      - `yhteystiedot = { website: raw_analysis.website_url ?? '' }`.
      - `media_urls = { logo: selectedLogoUrl, photos: selectedGallery.slice(0, 5) }` — `selectedLogoUrl` is the server-validated logo (PATCHed + membership-checked in Plan 02); no unvalidated URL is written.
    Sequence: get session token (getAuthToken). For each of the four fields, `await POST /api/business/onboarding/save-step` with `{ paikka_id: paikkaId, step: 6, field, value }` (step 6 = final, bypasses stepped wizard per PATTERNS.md). Each save-step MUST be awaited. If any save-step returns `!res.ok`, set `quickError` to `Lähetys epäonnistui. Yritä uudelleen tai jatka velhon kautta.` and STOP (do not proceed to submit).

    PARTIAL-FAILURE behavior (acceptable + recoverable — must be explicitly handled, not ignored): the four save-step writes are sequential and NOT transactional. If call N fails after calls 1..N-1 succeeded, the draft is left partially populated. This is RECOVERABLE by design: each save-step is an idempotent UPSERT on (business_account_id, paikka_id), so (a) re-clicking `Hyväksy ja lähetä` re-writes all four fields (the succeeded ones harmlessly overwrite with the same value) and then submits, OR (b) the user clicks `Jatka velhoon →` and finishes via the full wizard, where the partially-written fields appear pre-filled. The `quickError` copy (`...Yritä uudelleen tai jatka velhon kautta.`) explicitly surfaces BOTH recovery paths to the user. Do NOT attempt rollback/compensation — the idempotent-retry + wizard-fallback path is the intended recovery. Add a brief code comment noting this partial-write recoverability so a future reader does not mistake it for a bug.

    Then `await POST /api/business/onboarding/submit` with `{ paikka_id: paikkaId }`; on `!res.ok` set the same `quickError`. On success, navigate the user to the post-submit destination the full wizard uses (read submit's success handling / StepEsikatselu to match; if it redirects to a confirmation/admin-queue route, push there via `useRouter`). Guard against double-submit by disabling both footer buttons while `submittingQuick`.

    Color/logo selections are already persisted via PATCH (Plan 02) and gallery via Task 1, so quick-accept does not re-PATCH branding; it only maps raw_analysis + the in-memory selections into the draft and submits. Do NOT modify submit/route.ts or save-step/route.ts (D-10). Surface `quickError` inline with `text-red-600 role="alert"`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "AnalysoiSivusto" || echo "quick-accept typechecks"; git diff --name-only app/api/business/onboarding/submit/route.ts | grep . && echo "VIOLATION: submit modified" || echo "submit unchanged"</automated>
  </verify>
  <acceptance_criteria>
    - A `Hyväksy ja lähetä` button exists in the footer with a `Lähetetään...` loading state (grep both strings)
    - `handleQuickAccept` maps `raw_analysis.prices` → `{kategoria,hinta,lisatieto:''}` array and stores the array (not the serialized string) — grep `kategoria:` present, no `hinnastaToHintaKuvaus` call in this file
    - Quick-accept `await`s `/api/business/onboarding/save-step` (4 fields, step 6) then `/api/business/onboarding/submit` (grep both endpoints; each fetch is awaited)
    - On a save-step failure, the flow STOPS before calling submit (no submit POST after a failed save-step) and sets `quickError`
    - A code comment documents the partial-write recoverability (idempotent retry + wizard fallback) — grep for a comment near the save-step loop mentioning idempotent/recover/retry
    - submit/route.ts and save-step/route.ts are UNCHANGED (git diff shows no changes to either file)
    - Quick-accept failure copy `Lähetys epäonnistui. Yritä uudelleen tai jatka velhon kautta.` present with `role="alert"`
    - Both footer buttons disabled while `submittingQuick` (no double-submit)
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>"Hyväksy ja lähetä" maps the AI results + user selections into onboarding_draft via save-step, then runs the existing submit route exactly as a full-wizard completion — same ownership/validation/cleanup, no second write path, submit/save-step untouched. Partial save-step failure stops before submit, surfaces a Finnish error with both recovery paths, and is recoverable via idempotent retry or the full wizard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → save-step → onboarding_draft | Quick-accept writes draft fields; save-step re-validates ownership + field allowlist + shapes (unchanged route) |
| browser → submit → liikuntapaikat | Quick-accept triggers the same submit invariants as the full wizard (ownership, claim_status reset, draft deletion) |
| browser → PATCH branding (gallery/logo) | Gallery image_urls + logo URL validated for membership server-side (Plan 01); the validated logo is the value carried into media_urls.logo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-09 | Elevation/IDOR | quick-accept submit | mitigate | submit route (unchanged) re-checks `business_paikka_links` ownership for the verified JWT before any liikuntapaikat write — quick-accept gains no privilege over the full wizard (satisfies ROADMAP success criterion 6) |
| T-48-10 | Tampering | quick-accept draft fields | mitigate | save-step (unchanged) enforces ALLOWED_FIELDS allowlist + per-field shape validation (aukioloajat day/time regex, hinnasto ≤20 rows); malformed mapped data is rejected at the route, not trusted from the client |
| T-48-11 | Tampering | gallery image_urls injection | mitigate | PATCH branding (Plan 01) requires every submitted gallery URL to exist in stored `image_urls`; arbitrary URLs cannot be persisted |
| T-48-12 | Repudiation/DoS | double-submit | mitigate | Footer buttons disabled while `submittingQuick`; submit deletes the draft, so a replayed submit finds no draft and no-ops |
| T-48-13 | Tampering / IDOR | logo URL into liikuntapaikat.logo_url | mitigate | The `media_urls.logo` carried into the draft is `selectedLogoUrl`, already validated against logo_candidates by Plan 01's PATCH; submit's write of `liikuntapaikat.logo_url` is therefore on a server-validated value, not an arbitrary client URL |
| T-48-14 | Tampering / availability | partial save-step write before submit | accept | Sequential non-transactional save-step writes can leave the draft partially populated on mid-sequence failure; flow STOPS before submit so no half-mapped row is committed to liikuntapaikat. Recoverable by idempotent retry or full-wizard completion; user is shown both paths. Accepted (low impact, self-healing) — no compensation logic added. |
| T-48-15 | Availability | gallery prefill lost to draft-fetch race | mitigate | handleConfirm AWAITS the media_urls save-step write before setPagePhase('wizard'); WizardInner's on-mount draft fetch therefore always reads the written value, so the prefill cannot be silently dropped by a fire-and-forget race |
| T-48-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (`lucide-react` already a project dependency per shadcn config / 48-UI-SPEC.md); no install task → gate not triggered |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no new errors.
- `git diff --name-only` shows NO changes to `submit/route.ts`, `save-step/route.ts`, or `StepMediat.tsx`.
- The Task 1 multiline grep gate confirms `await fetch('/api/business/onboarding/save-step'` precedes `setPagePhase('wizard')` in the same handleConfirm function.
- Manual: select ≤5 gallery images, click `Jatka velhoon →`, advance to Mediat step — selected images appear in the grid pre-filled (no flicker/loss from the race).
- Manual: click `Hyväksy ja lähetä` — draft fields written, submit runs, user lands on the same confirmation the full wizard reaches; claim_status becomes pending and the draft is deleted.
- Manual (partial failure): simulate a mid-sequence save-step failure (e.g. offline after 2 of 4 writes) — flow stops before submit, `quickError` shows with the dual recovery copy, and re-clicking succeeds (idempotent retry).
</verification>

<success_criteria>
- ONBOARD-16: scraped images appear as selectable options and pre-fill the Mediat step (reliably — the prefill write is awaited before wizard navigation).
- FLOW-02: user quick-accepts and lands directly in the admin approval flow without stepping through remaining wizard screens.
- FLOW-03: quick-accept maps results into onboarding_draft then runs the existing submit route unmodified — same ownership/validation/cleanup, no parallel write path.
- Logo written to the venue is the server-validated selection; partial-failure during quick-accept is recoverable and clearly surfaced.
</success_criteria>

<output>
Create `.planning/phases/48-logo-v-ri-ja-galleriavalinta/48-03-SUMMARY.md` when done.
</output>
</content>
