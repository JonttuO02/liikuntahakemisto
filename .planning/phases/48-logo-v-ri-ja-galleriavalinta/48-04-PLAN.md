---
phase: 48-logo-v-ri-ja-galleriavalinta
plan: 04
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - app/business/onboarding/page.tsx
  - app/business/onboarding/StepEsikatselu.tsx
autonomous: true
requirements: [ONBOARD-16, ONBOARD-14, ONBOARD-15]
must_haves:
  truths:
    - "After clicking 'Jatka velhoon →', the user lands ON Step 2 (StepMediat / Mediat step) where the prefilled gallery images and logo render — Step 2 is NOT skipped (closes CR-01, restores ROADMAP Success Criterion 3 / ONBOARD-16)"
    - "When no explicit background selection exists yet, the Step 6 preview's DiagonaalKortti background uses a color whose semantic role is 'background', not an arbitrary colors[0] that may be an accent/text color (closes CR-02, restores the phase goal's 'explicitly choose what represents their brand' guarantee)"
    - "When selected_background_color is already set, the Step 6 preview background is unchanged (no regression to the explicit-selection path)"
  artifacts:
    - path: "app/business/onboarding/page.tsx"
      provides: "handleConfirm save-step call that lands the wizard on Step 2 (Media), not past it"
      contains: "step: 1"
    - path: "app/business/onboarding/StepEsikatselu.tsx"
      provides: "Role-aware brandColor fallback mirroring AnalysoiSivusto"
      contains: "role === 'background'"
  key_links:
    - from: "app/business/onboarding/page.tsx handleConfirm"
      to: "app/business/WizardInner.tsx auto-resume (savedStep > 1 && step === 1)"
      via: "save-step writes current_step = step + 1; step:1 → current_step:2 → resume lands on Step 2"
      pattern: "step:\\s*1"
    - from: "app/business/onboarding/StepEsikatselu.tsx brandColor"
      to: "DiagonaalKortti brandColor prop"
      via: "role-aware find on colors array"
      pattern: "role === 'background'"
---

<objective>
Close the two unresolved gaps from 48-VERIFICATION.md (CR-01 and CR-02 in 48-REVIEW.md) that
leave Phase 48 at 4/6 success criteria. Both are confirmed present at HEAD and both are
narrow, deterministic, single-expression fixes in files Phase 48 already touched.

- **CR-01 (gap 1):** `page.tsx`'s `handleConfirm` writes `save-step` with `step: 2`. The
  save-step route sets `current_step = step + 1` (→ 3). `WizardInner`'s on-mount resume
  (`savedStep > 1 && step === 1` → redirect to `?step=savedStep`) then sends every
  "Jatka velhoon →" user straight to Step 3 (Hinnasto), permanently skipping Step 2 (Media)
  — the only place the prefilled gallery/logo render in the wizard. Fix: write `step: 1` so
  `current_step` becomes 2 and the user lands ON Step 2.
- **CR-02 (gap 2):** `StepEsikatselu.tsx`'s `brandColor` fallback uses `colors?.[0]?.hex`,
  ignoring the color's semantic `role`. Fix: mirror `AnalysoiSivusto.tsx`'s role-aware
  fallback (`colors?.find(c => c.role === 'background')?.hex`).

Purpose: Restore ROADMAP Success Criterion 3 (gallery images appear as selectable options in
the Mediat step) and the phase goal's "explicitly choose what represents their brand" clause.
Output: Two single-expression edits. No new public symbols, no schema changes, no new files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-logo-v-ri-ja-galleriavalinta/48-VERIFICATION.md
@.planning/phases/48-logo-v-ri-ja-galleriavalinta/48-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CR-01 — handleConfirm writes step:1 so the wizard lands on Step 2 (Media), not past it</name>
  <files>app/business/onboarding/page.tsx</files>
  <read_first>
    - app/business/onboarding/page.tsx — the file being modified. The relevant code is
      `handleConfirm` (lines ~88-126); the save-step fetch body is at lines 112-117 and
      currently contains `step: 2`.
    - app/business/WizardInner.tsx (lines ~117-125) — confirm the auto-resume redirect:
      `const savedStep = existingDraft?.current_step ?? 0; setMaxReachedStep(savedStep);
      if (savedStep > 1 && step === 1) { ...router.push(?step=savedStep) }`. This is the
      mechanism: `step:1` → save-step sets `current_step:2` → on mount the user resumes to
      `?step=2` (StepMediat), which is the intended Media step. `step:2` would set
      `current_step:3` and skip Step 2 entirely.
  </read_first>
  <action>
    In `app/business/onboarding/page.tsx`, inside `handleConfirm`'s `fetch('/api/business/onboarding/save-step', ...)`
    body (the `JSON.stringify({ ... })` at lines 112-117), change `step: 2` to `step: 1`.
    Do NOT change `field: 'media_urls'` or the `value: { logo: ..., photos: ... }` shape —
    only the `step` number changes. The save-step route's `current_step = step + 1` arithmetic
    then yields `current_step: 2`, and WizardInner's `savedStep > 1 && step === 1` resume
    redirects the user to `?step=2` (StepMediat), where the prefilled gallery/logo render.
    Update the adjacent explanatory comment if it references the old `step: 2` reasoning so it
    stays accurate (per CR-01 / D-gap fix). Keep the `await` ordering before `setPagePhase('wizard')`
    intact — that race fix (T-48-15) is correct and must not regress.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx`'s `handleConfirm` save-step fetch body contains `step: 1` and does NOT contain `step: 2`.
    - The `field: 'media_urls'` key and `value: { logo: ..., photos: ... }` shape are unchanged.
    - The `await fetch(...)` still precedes `setPagePhase('wizard')` (race fix preserved).
    - Manually tracing the flow: save-step writes `current_step = 1 + 1 = 2`; WizardInner mounts
      with no `step` param (step defaults to 1), reads `savedStep = 2`, evaluates
      `savedStep > 1 && step === 1` as true, and redirects to `?step=2` → StepMediat (Step 2),
      NOT StepHinnasto (Step 3). Step 2 is reachable, not skipped.
    - `npx tsc --noEmit` produces no errors.
  </acceptance_criteria>
  <done>
    The "Jatka velhoon →" continuation path lands the user on Step 2 (Mediat), where the
    prefilled gallery images and logo are selectable — closing CR-01 and restoring ROADMAP
    Success Criterion 3 / ONBOARD-16.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix CR-02 — StepEsikatselu brandColor fallback is role-aware (background-role), mirroring AnalysoiSivusto</name>
  <files>app/business/onboarding/StepEsikatselu.tsx</files>
  <read_first>
    - app/business/onboarding/StepEsikatselu.tsx (lines ~44-48) — the file being modified. The
      `brandColor` const currently reads:
      `brandingData?.selected_background_color ?? brandingData?.colors?.[0]?.hex ?? undefined`.
    - app/business/onboarding/AnalysoiSivusto.tsx (lines ~140-150) — the canonical role-aware
      pattern to mirror: `const bgCandidate = colors.find(c => c.role === 'background')`. The
      `colors` array elements carry a semantic `role` field ('background' | 'accent' | 'text' |
      ...); the fallback must select the 'background'-role entry, not the first array element.
  </read_first>
  <action>
    In `app/business/onboarding/StepEsikatselu.tsx`, change the `brandColor` fallback expression
    (lines ~47-48) from:
    `brandingData?.selected_background_color ?? brandingData?.colors?.[0]?.hex ?? undefined`
    to:
    `brandingData?.selected_background_color ?? brandingData?.colors?.find(c => c.role === 'background')?.hex ?? undefined`.
    Only the middle fallback term changes (`colors?.[0]?.hex` → `colors?.find(c => c.role === 'background')?.hex`).
    The leading `selected_background_color` term and the trailing `?? undefined` are unchanged,
    so when an explicit background selection exists the result is identical (no behavior change
    on the explicit-selection path). Update the adjacent comment so it states the fallback is the
    background-ROLE color, not "the first AI-extracted color" (per CR-02). If TypeScript cannot
    infer the `role` field on the color element type, confirm the color element type in
    `lib/branding/brandingResult.ts` exposes `role` (AnalysoiSivusto already uses it without a
    cast, so it should) — do not add an `any` cast.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `StepEsikatselu.tsx`'s `brandColor` expression contains `colors?.find(c => c.role === 'background')` and does NOT contain `colors?.[0]?.hex`.
    - The expression still begins with `brandingData?.selected_background_color ??` and ends with `?? undefined` — i.e. no behavior change when `selected_background_color` is already set.
    - No `any` cast or `// @ts-ignore` is introduced.
    - `npx tsc --noEmit` produces no errors.
  </acceptance_criteria>
  <done>
    When no explicit background selection exists, the Step 6 DiagonaalKortti background uses the
    'background'-role color (matching AnalysoiSivusto's own initialization), not an arbitrary
    accent/text color — closing CR-02 and restoring the phase goal's "explicit choice over
    silent auto-pick" guarantee.
  </done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

This gap-closure plan produces **no new public symbols, files, types, exports, routes, or
schema changes**. Both tasks are in-place single-expression edits to existing files:

- `app/business/onboarding/page.tsx` — `handleConfirm` save-step body changes `step: 2` → `step: 1`.
- `app/business/onboarding/StepEsikatselu.tsx` — `brandColor` fallback changes `colors?.[0]?.hex` → `colors?.find(c => c.role === 'background')?.hex`.

No downstream plan consumes a new interface from this plan. The contracts of `save-step`,
`WizardInner`, `DiagonaalKortti`, and `brandingResult.ts` are all unchanged.
</artifacts_produced>

<verification>
- `npx tsc --noEmit` is clean across the whole project (it was clean at verification time;
  these edits do not add types or change signatures, so it must remain clean).
- Grep confirms `step: 1` (not `step: 2`) in `page.tsx`'s `handleConfirm` save-step body.
- Grep confirms `role === 'background'` (not `colors?.[0]?.hex`) in `StepEsikatselu.tsx`'s `brandColor`.
- Trace check (CR-01): step:1 → save-step current_step:2 → WizardInner resumes to ?step=2 (StepMediat). Step 2 reachable.
- Trace check (CR-02): with no `selected_background_color`, `brandColor` resolves to the
  background-role color; with `selected_background_color` set, the result is unchanged.
</verification>

<success_criteria>
- ROADMAP Success Criterion 3 (ONBOARD-16) restored: the "Jatka velhoon →" flow lands on Step 2
  (Mediat) and the prefilled gallery/logo render there.
- Phase goal's "explicitly choose what represents their brand" clause restored: the preview
  fallback never silently surfaces a non-background-role color as the panel background.
- No regression on the explicit-selection path (CR-02) and no regression on the save-step
  race fix (CR-01 / T-48-15).
- `npx tsc --noEmit` clean.
</success_criteria>

<output>
Create `.planning/phases/48-logo-v-ri-ja-galleriavalinta/48-04-SUMMARY.md` when done
</output>
