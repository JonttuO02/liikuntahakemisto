---
phase: 13-uusi-korttimalli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/DiagonaalKortti.tsx
autonomous: true
requirements:
  - UI-11
tags:
  - ui
  - card
  - clip-path
  - static-maps

must_haves:
  truths:
    - "DiagonaalKortti.tsx exists in app/components/ and exports a default React component"
    - "Component renders a fixed-height (h-32) card with a CSS clip-path diagonal split between a left info panel (60%) and a right map panel (40%)"
    - "When paikka.latitude and paikka.longitude are both non-null, the right panel renders a Google Static Maps <img> with a red marker pin"
    - "When latitude or longitude is null, the right panel renders a solid lajiKonfig.color background with a centered LucideIcon (w-8 h-8) instead of a broken image"
    - "Entire card surface is wrapped in <Link href={`/paikat/${paikka.id}`}> so clicks anywhere navigate to the venue profile"
    - "Outer wrapper is a motion.div with whileHover scale 1.02 and whileTap scale 0.98 — no y-lift"
    - "Component does NOT accept aukinyt, isSuosikki, or onToggleSuosikki props — only paikka and distanceStr"
  artifacts:
    - path: "app/components/DiagonaalKortti.tsx"
      provides: "Diagonal split card component with clip-path + Static Maps thumbnail"
      contains: "export default function DiagonaalKortti"
      min_lines: 90
  key_links:
    - from: "app/components/DiagonaalKortti.tsx"
      to: "lib/lajit.ts"
      via: "lajiKonfig import (sport pill background + fallback color)"
      pattern: "from '@/lib/lajit'"
    - from: "app/components/DiagonaalKortti.tsx"
      to: "lib/aukiolo.ts"
      via: "getOpenStatus(paikka.aukioloajat)"
      pattern: "from '@/lib/aukiolo'"
    - from: "app/components/DiagonaalKortti.tsx"
      to: "Google Static Maps API"
      via: "<img src=staticMapsUrl(...)> with NEXT_PUBLIC_GOOGLE_MAPS_API_KEY"
      pattern: "maps.googleapis.com/maps/api/staticmap"
---

<objective>
Create `app/components/DiagonaalKortti.tsx` — a new React component that renders a diagonal split card: left panel (60%) shows venue info (sport pill, name, open status, price, distance), right panel (40%) shows a Google Static Maps thumbnail with a pin marker (or a sport-color fallback when coordinates are missing). The entire card is a Link to the venue profile page.

Purpose: Implements UI-11 by introducing a denser, visually distinctive card model for the Etusivu search-results list. The visual diagonal split + map thumbnail differentiates this card from the existing PaikkaKortti (which Phase 12 still uses in the search panel overlay).

Output: A single new file `app/components/DiagonaalKortti.tsx` (no other files modified in this plan). The component compiles, types check, and existing vitest suite remains green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/13-uusi-korttimalli/13-CONTEXT.md
@.planning/phases/13-uusi-korttimalli/13-RESEARCH.md
@.planning/phases/13-uusi-korttimalli/13-PATTERNS.md
@app/components/PaikkaKortti.tsx
@lib/lajit.ts
@lib/types.ts
@lib/aukiolo.ts
@lib/priceUtils.ts
@lib/utils.ts
@app/globals.css

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From lib/types.ts:
- `Liikuntapaikka` shape: `id: number`, `nimi: string`, `laji: string`, `osoite: string | null`, `kaupunki: string | null`, `latitude: number | null`, `longitude: number | null`, `hinta_min: number | null`, `hinta_max: number | null`, `hinta_kuvaus?: string | null`, `aukioloajat?: Record<string, {open: string; close: string}> | null`, `featured?: boolean | null`
- CRITICAL: `latitude` and `longitude` are `number | null` — fallback (D-06) is mandatory, not optional

From lib/lajit.ts:
- `lajiKonfig: Record<string, LajiKonfig>` where `LajiKonfig = { label: string; badgeTw: string; accentBg: string; color: string }`
- Available keys: `padel`, `tennis`, `jooga`, `kuntosali`, `uinti`, `kiipeily`, `jääkiekko`, `liikuntahalli`, `liikunta`
- `lajiKonfig[paikka.laji]` may be `undefined` for unknown laji — fall back to `{ label: paikka.laji, badgeTw: 'text-white', accentBg: '', color: '#6b7280' }`

From lib/aukiolo.ts:
- `getOpenStatus(aukioloajat)` returns a discriminated union: `{ status: 'open'; hours: string } | { status: 'closed'; hours: string | null } | { status: 'no-data' }`

From lib/priceUtils.ts:
- `isMembershipOnly(paikka): boolean` — true only when `hinta_kuvaus` contains 'jäsenyys' AND hinta_min/hinta_max are both null

From lib/utils.ts:
- `hintateksti(hinta_min, hinta_max): string` — returns '' when both null
- `cn(...inputs: ClassValue[]): string` — Tailwind class merge

From PaikkaKortti.tsx (lines 13-32):
- Exports `korttiVariants = { hidden: { opacity: 0, y: 14 }, show: { opacity: 1, y: 0, transition: { duration: 0.22, ease: EASE_OUT } } }` where `EASE_OUT: [number, number, number, number] = [0.23, 1, 0.32, 1]`
- `SPORT_ICONS` map: `{ padel: Zap, kuntosali: Dumbbell, jooga: Leaf, uinti: Waves, tennis: Target, liikuntahalli: Building2, liikunta: Activity }` (from lucide-react)
- Fallback icon: `Activity`

Glassmorphism (from app/globals.css):
- `.glass` — gradient background + backdrop blur + box shadow + box-shadow/transform transition
- `.glass-hover` — hover-state box-shadow upgrade
- Apply `.glass .glass-hover rounded-2xl overflow-hidden` ONLY on the outer motion.div — never on inner clip-path panels

Environment:
- `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` — already configured in .env.local, used by APIProvider; same key reused here for Static Maps `<img>` requests
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create DiagonaalKortti.tsx scaffold with imports, constants, and props interface</name>
  <files>app/components/DiagonaalKortti.tsx</files>
  <read_first>
    - app/components/PaikkaKortti.tsx (reference structure — copy imports/constants verbatim per D-05; this analog drives every pattern in DiagonaalKortti)
    - .planning/phases/13-uusi-korttimalli/13-CONTEXT.md (decisions D-01 through D-10 — locked, non-negotiable)
    - .planning/phases/13-uusi-korttimalli/13-PATTERNS.md (pattern map showing exact lines of PaikkaKortti to mirror)
    - lib/lajit.ts (lajiKonfig shape — used for sport pill bg color and D-06 fallback)
    - lib/types.ts (Liikuntapaikka shape — latitude/longitude are number | null)
    - lib/aukiolo.ts (getOpenStatus return type — discriminated union)
    - lib/priceUtils.ts (isMembershipOnly signature)
    - lib/utils.ts (hintateksti, cn)
    - CLAUDE.md (Design Guidelines section — color system, animation principles, Tailwind v3 constraint)
  </read_first>
  <action>
    Create new file `app/components/DiagonaalKortti.tsx`. Start the file with `'use client'` on line 1.

    Copy the import block from PaikkaKortti.tsx (lines 3-11) verbatim per D-05, with three changes:
    1. Drop `Heart` from the lucide-react import (no HeartButton, per D-09)
    2. Keep `MapPin, Dumbbell, Waves, Leaf, Building2, Zap, Target, Activity` from lucide-react
    3. Keep `type { LucideIcon }`, `motion` from framer-motion, `Link` from next/link, `lajiKonfig` from '@/lib/lajit', `hintateksti, cn` from '@/lib/utils', `getOpenStatus` from '@/lib/aukiolo', `isMembershipOnly` from '@/lib/priceUtils', and `type { Liikuntapaikka }` from '@/lib/types'

    Define module-level constants (mirror PaikkaKortti lines 13-32):
    - `EASE_OUT: [number, number, number, number] = [0.23, 1, 0.32, 1]`
    - Export `diagonaalKorttiVariants = { hidden: { opacity: 0, y: 14 }, show: { opacity: 1, y: 0, transition: { duration: 0.22, ease: EASE_OUT } } }` (exported so a future stagger container in Etusivu can pick it up — see RESEARCH.md Pattern 8)
    - `SPORT_ICONS: Record<string, LucideIcon>` with the exact same 7 entries as PaikkaKortti.tsx lines 24-32: padel→Zap, kuntosali→Dumbbell, jooga→Leaf, uinti→Waves, tennis→Target, liikuntahalli→Building2, liikunta→Activity
    - `API_KEY = process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY ?? ''` (module-level, per D-07)

    Add a module-level helper function `staticMapsUrl(lat: number, lng: number): string` that returns the URL:
    `https://maps.googleapis.com/maps/api/staticmap?center={lat},{lng}&zoom=15&size=200x128&scale=2&maptype=roadmap&markers=color:red%7C{lat},{lng}&key={API_KEY}`
    Use template literal with the `%7C` URL-encoding for the `|` pipe character (per RESEARCH.md Pitfall 6). zoom=15, scale=2, maptype=roadmap, marker color=red — all per D-07 and RESEARCH.md Pattern 2.

    Define `interface DiagonaalKorttiProps { paikka: Liikuntapaikka; distanceStr?: string }` — only two props, no aukinyt/isSuosikki/onToggleSuosikki (per D-09).

    Create a default-exported function component `DiagonaalKortti({ paikka, distanceStr }: DiagonaalKorttiProps)` that returns a placeholder JSX node (a single `<div>Diagonal placeholder for {paikka.nimi}</div>`) — Task 2 fills in the real rendering. Stop here to keep the task contained.

    Do NOT use Tailwind clip-path utilities — Tailwind v3 has none for polygon values (Task 2 uses inline `style={{ clipPath: '...' }}`).
    Do NOT add the `Heart` import.
    Do NOT add `aukinyt`, `isSuosikki`, `onToggleSuosikki` to the props interface.
  </action>
  <verify>
    <automated>cd C:/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | grep -i "diagonaalkortti\|error" || echo "OK_TYPECHECK"</automated>
  </verify>
  <acceptance_criteria>
    - File `app/components/DiagonaalKortti.tsx` exists
    - First non-empty line is exactly `'use client'`
    - File imports `motion` from `'framer-motion'`, `Link` from `'next/link'`, `lajiKonfig` from `'@/lib/lajit'`, `getOpenStatus` from `'@/lib/aukiolo'`, `isMembershipOnly` from `'@/lib/priceUtils'`, `hintateksti, cn` from `'@/lib/utils'`, `type { Liikuntapaikka }` from `'@/lib/types'`
    - File does NOT import `Heart` from `'lucide-react'`
    - File imports `MapPin, Dumbbell, Waves, Leaf, Building2, Zap, Target, Activity` from `'lucide-react'` and `type { LucideIcon }` from `'lucide-react'`
    - File contains `export const diagonaalKorttiVariants` with `hidden` and `show` keys
    - File contains `const SPORT_ICONS: Record<string, LucideIcon>` with exactly 7 entries (padel, kuntosali, jooga, uinti, tennis, liikuntahalli, liikunta)
    - File contains a module-level `staticMapsUrl` function whose return string contains the literal substrings `'maps.googleapis.com/maps/api/staticmap'`, `'zoom=15'`, `'size=200x128'`, `'scale=2'`, `'maptype=roadmap'`, `'%7C'`, and `'key='`
    - File contains `interface DiagonaalKorttiProps` with exactly two property names: `paikka` and `distanceStr` (no `aukinyt`, no `isSuosikki`, no `onToggleSuosikki`)
    - File default-exports a function named `DiagonaalKortti`
    - `npx tsc --noEmit` reports no errors involving `DiagonaalKortti.tsx`
  </acceptance_criteria>
  <done>Skeleton file created with correct imports, constants, helpers, and props interface — ready for Task 2 to fill in the JSX body. Type check passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement clip-path diagonal layout with left info panel and right map/fallback panel</name>
  <files>app/components/DiagonaalKortti.tsx</files>
  <read_first>
    - app/components/DiagonaalKortti.tsx (current scaffold from Task 1 — do not re-import; only fill in the body)
    - app/components/PaikkaKortti.tsx (reference for sport pill, open status, price, distance row markup — lines 73-170)
    - .planning/phases/13-uusi-korttimalli/13-PATTERNS.md (section "Clip-path diagonal split" and "Static Maps image" — exact JSX structure)
    - .planning/phases/13-uusi-korttimalli/13-RESEARCH.md (Pitfalls 1, 2, 5, 6 — clip-path seam overlap, overflow-hidden, line-clamp, %7C encoding)
    - .planning/phases/13-uusi-korttimalli/13-CONTEXT.md (D-01 through D-10 — clip-path values, h-32, animations)
    - CLAUDE.md (color system table — `text-[#111111]`, `text-[rgba(17,17,17,0.45)]`, `text-green-700`; animation principles — scale not y-lift)
  </read_first>
  <action>
    Replace the placeholder JSX body in `DiagonaalKortti.tsx` (inside the default-exported function) with the full implementation.

    Add a data-derivation block at the top of the function body (mirror PaikkaKortti lines 43-55 but simplified for the 128px constraint):
    - `const laji = lajiKonfig[paikka.laji] ?? { label: paikka.laji, badgeTw: 'text-white', accentBg: '', color: '#6b7280' }`
    - `const openStatus = getOpenStatus(paikka.aukioloajat)`
    - `const hintaTeksti = hintateksti(paikka.hinta_min, paikka.hinta_max)`
    - `const membershipOnly = isMembershipOnly(paikka)`
    - `const priceText = membershipOnly ? null : (paikka.hinta_kuvaus?.split('\n')[0] ?? (hintaTeksti !== '' ? hintaTeksti : null))` — single-line only, no priceLines array (128px height has no room for multiline)
    - `const hasCoords = paikka.latitude != null && paikka.longitude != null` — mandatory guard (latitude/longitude are number | null per lib/types.ts)
    - `const Icon = SPORT_ICONS[paikka.laji] ?? Activity`

    Build the JSX as three nested layers:

    OUTER (motion.div):
    - className `relative glass glass-hover rounded-2xl overflow-hidden h-32 cursor-pointer`
    - `variants={diagonaalKorttiVariants}`
    - `whileHover={{ scale: 1.02, transition: { duration: 0.18, ease: 'easeOut' } }}`
    - `whileTap={{ scale: 0.98, transition: { duration: 0.12, ease: 'easeOut' } }}`
    - The `overflow-hidden` and `relative` classes are mandatory — `overflow-hidden` clips clip-path children to rounded corners (Pitfall 2); `relative` provides positioning context for absolutely-positioned children.

    MIDDLE (Link):
    - `<Link href={`/paikat/${paikka.id}`} className="absolute inset-0 block">`
    - Whole card is the link target (D-08)

    LEFT PANEL (div, child of Link):
    - className `absolute inset-0 z-10 flex flex-col gap-1 p-3`
    - `style={{ clipPath: 'polygon(0 0, 62% 0, 57% 100%, 0 100%)' }}` (per D-01 and RESEARCH.md Pattern 1 — the 5% top-to-bottom diff creates the diagonal lean)
    - `z-10` so it sits above the right panel in the overlap zone
    - Content rows (in this order, mirroring PATTERNS.md):
      1. Sport pill: `<span className="inline-flex items-center gap-1 text-[10px] font-bold px-2 py-0.5 rounded-full text-white self-start truncate max-w-full" style={{ backgroundColor: laji.color }}>` containing `<Icon className="w-3 h-3 shrink-0" />` and `{laji.label}`
      2. Venue name: `<p className="font-bold text-[#111111] text-sm leading-snug line-clamp-1 overflow-hidden">{paikka.nimi}</p>` — pair `line-clamp-1` with `overflow-hidden` per Pitfall 5
      3. Open status (conditional):
         - if `openStatus.status === 'open'`: `<span className="text-xs font-bold text-green-700 truncate">Auki · {openStatus.hours}</span>`
         - if `openStatus.status === 'closed'`: `<span className="text-xs text-[rgba(17,17,17,0.45)] truncate">Suljettu</span>`
         - omit `no-data` branch (no room in 128px)
      4. Price (conditional):
         - if `membershipOnly`: `<span className="text-xs text-[rgba(17,17,17,0.5)]">vain jäsenyys</span>`
         - else if `priceText`: `<span className="text-xs font-bold text-[#111111] tabular-nums truncate">{priceText}</span>`
         - else: `<span className="text-xs text-[rgba(17,17,17,0.35)]">Lisätään pian</span>`
      5. Distance (conditional on `distanceStr`): `<div className="flex items-center gap-1 text-xs text-[rgba(17,17,17,0.4)] mt-auto"><MapPin className="w-3 h-3 shrink-0" /><span className="tabular-nums">{distanceStr}</span></div>` — `mt-auto` pins it to the panel bottom
    - Do NOT render `osoite` (address) — explicitly omitted per phase context summary (128px height)
    - Do NOT render `paikka.kuvaus` (description) — explicitly omitted
    - Do NOT render featured/Sponsoroitu badge or Kertakäynti OK badge — DiagonaalKortti is content-tight; PaikkaKortti keeps those

    RIGHT PANEL (div, sibling of left panel inside Link):
    - className `absolute inset-0 overflow-hidden`
    - `style={{ clipPath: 'polygon(57% 0, 100% 0, 100% 100%, 52% 100%)' }}` (per D-01 — the mirror diagonal; the 5% overlap with the left panel (57/62 top, 52/57 bottom) prevents the 1px anti-alias seam per Pitfall 1)
    - Conditional content:
      - if `hasCoords`: render `<img src={staticMapsUrl(paikka.latitude!, paikka.longitude!)} alt={`Karttakuva: ${paikka.nimi}`} className="w-full h-full object-cover" loading="lazy" />` — `loading="lazy"` per D-07 / RESEARCH.md Pattern 2
      - else: render `<div className="w-full h-full flex items-center justify-center" style={{ backgroundColor: laji.color }}><Icon className="w-8 h-8 text-white opacity-80" /></div>` — D-06 fallback

    Do NOT apply `.glass` to inner panels (RESEARCH.md anti-patterns — glassmorphism is on outer wrapper only).
    Do NOT use Tailwind clip-path utilities — use inline `style={{ clipPath: '...' }}` (Tailwind v3 has no polygon utilities).
    Do NOT add `transition-transform` class on the motion.div — conflicts with Framer Motion transform (RESEARCH.md Pattern 4).
    Do NOT include HeartButton, suosikki logic, or aukinyt-based text (D-09).
  </action>
  <verify>
    <automated>cd C:/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | (grep -i "error" || echo "OK_TYPECHECK") && npx vitest run lib/ --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `app/components/DiagonaalKortti.tsx` exists and is at least 90 lines
    - `npx tsc --noEmit` returns no errors for `DiagonaalKortti.tsx`
    - `npx vitest run lib/` exits with code 0 (existing lib tests still pass — no regression)
    - File contains the literal substring `clipPath: 'polygon(0 0, 62% 0, 57% 100%, 0 100%)'` (left panel — D-01 geometry)
    - File contains the literal substring `clipPath: 'polygon(57% 0, 100% 0, 100% 100%, 52% 100%)'` (right panel — D-01 geometry; 5% overlap prevents Pitfall 1 seam)
    - File contains the literal substring `h-32` on the outer motion.div className (D-03 fixed height)
    - File contains the literal substring `overflow-hidden` on the outer motion.div className (Pitfall 2 prevention)
    - File contains the literal substring `whileHover={{ scale: 1.02` and `whileTap={{ scale: 0.98` (D-10)
    - File contains the literal substring `href={`/paikat/${paikka.id}`}` AND the literal substring `className="absolute inset-0` on the Link (D-08 — whole card is the link)
    - File contains the literal substring `paikka.latitude != null && paikka.longitude != null` OR equivalent `!= null` guard before any `staticMapsUrl(` call
    - File contains `loading="lazy"` on the `<img>` element (D-07 / lazy image)
    - File contains `alt={` followed by `paikka.nimi` reference in the img element (Finnish accessibility label)
    - File contains the literal substring `backgroundColor: laji.color` inside the fallback `<div>` branch (D-06 — sport color background)
    - File contains the literal substring `w-8 h-8` on a `<Icon` element inside the fallback branch (D-06 — large centered icon)
    - File does NOT contain the substring `Heart` (no HeartButton, D-09)
    - File does NOT contain the substring `osoite` (address row omitted)
    - File does NOT contain the substring `paikka.kuvaus` (description omitted)
    - File does NOT contain the substring `featured` or `Sponsoroitu` (badge omitted)
    - File does NOT contain the substring `aukinyt` (no aukinyt prop or display)
    - File does NOT contain the substring `isSuosikki` or `onToggleSuosikki`
    - File does NOT contain the substring `transition-transform` (conflict avoidance)
    - File contains `line-clamp-1` on the venue name `<p>` element AND `overflow-hidden` is on the same element OR its ancestor panel (Pitfall 5)
    - File contains `mt-auto` on the distance row container (pins distance to panel bottom)
  </acceptance_criteria>
  <done>DiagonaalKortti.tsx is complete: clip-path diagonal split renders correctly in structure (left info panel at 60% with 5-row vertical layout, right map/fallback panel at 40% with mirror clip-path). Type check passes, lib tests still green, no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → googleapis.com/staticmap | Client-side `<img src>` request constructed from `paikka.latitude`, `paikka.longitude` (database-sourced `number \| null` fields) and `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` |
| browser → /paikat/{id} | Internal Next.js route from `<Link href>` using `paikka.id` (database-sourced number) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `staticMapsUrl(lat, lng)` URL builder | mitigate | TypeScript enforces `lat: number, lng: number` parameters; `hasCoords = paikka.latitude != null && paikka.longitude != null` guard ensures only typed numbers flow into the URL — no string concatenation of user-controlled data possible |
| T-13-02 | Information Disclosure | `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` in client bundle | accept | Key is intentionally client-side per CLAUDE.md env table; restricted by HTTP referrer + API service scope in Google Cloud Console (existing v1.1 control — same key used by APIProvider in MapProvider.tsx) |
| T-13-03 | Denial of Service | Static Maps API quota | accept | `loading="lazy"` defers image fetches to viewport; quota managed at Google Cloud level — no in-app rate limiting needed for read-only thumbnails |
</threat_model>

<verification>
1. `npx tsc --noEmit` — no type errors in DiagonaalKortti.tsx
2. `npx vitest run lib/` — existing lib tests pass (no regression)
3. Source assertions per `<acceptance_criteria>` — clip-path values, h-32, scale animations, lazy loading, fallback rendering all literal-string-present in the file
4. File length > 90 lines (must_haves.artifacts.min_lines)
</verification>

<success_criteria>
- `app/components/DiagonaalKortti.tsx` exists with full implementation
- Component exports default `DiagonaalKortti` and named export `diagonaalKorttiVariants`
- Component is a `'use client'` React component using framer-motion, next/link, lucide-react
- Diagonal split via inline `style={{ clipPath: 'polygon(...)' }}` on both panels with 5% overlap
- Right panel renders Static Maps `<img loading="lazy">` when coordinates exist, else fallback solid-color panel with centered LucideIcon
- Animation matches CLAUDE.md: scale 1.02 hover, 0.98 tap, no y-lift
- No props for HeartButton/suosikki/aukinyt (D-09)
- Type check green, lib suite green
</success_criteria>

<output>
Create `.planning/phases/13-uusi-korttimalli/13-01-SUMMARY.md` when done, listing:
- File created: `app/components/DiagonaalKortti.tsx` (line count)
- Key implementation choices for Claude's discretion items (clip-path exact percentages used, alt text, padding)
- Any deviations from the plan and why
- Confirmation that lib tests still pass
</output>
</content>
</invoke>