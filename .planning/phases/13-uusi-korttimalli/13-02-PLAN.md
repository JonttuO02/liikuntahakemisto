---
phase: 13-uusi-korttimalli
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - app/components/Etusivu.tsx
autonomous: false
requirements:
  - UI-11
tags:
  - ui
  - integration
  - etusivu
  - static-maps

must_haves:
  truths:
    - "Etusivu.tsx imports DiagonaalKortti and no longer imports PaikkaKortti"
    - "The search-results card list at the bottom-sheet card-list section renders DiagonaalKortti for every item in searchSuodatettu"
    - "The DiagonaalKortti invocation passes only `key`, `paikka`, and `distanceStr` props — no aukinyt, isSuosikki, or onToggleSuosikki"
    - "The MAP-06 zoom-cards (inline motion.div inside AdvancedMarker around lines 396-411) are not modified and continue to render unchanged"
    - "Static Maps thumbnails load in the running browser when paikka has latitude and longitude (verified by human via DevTools Network tab)"
    - "Clicking anywhere on a DiagonaalKortti navigates the browser to /paikat/{id} for that venue"
  artifacts:
    - path: "app/components/Etusivu.tsx"
      provides: "Etusivu uses DiagonaalKortti for the search-results list"
      contains: "import DiagonaalKortti from './DiagonaalKortti'"
  key_links:
    - from: "app/components/Etusivu.tsx"
      to: "app/components/DiagonaalKortti.tsx"
      via: "default import + <DiagonaalKortti /> JSX usage"
      pattern: "import DiagonaalKortti from './DiagonaalKortti'"
---

<objective>
Swap `PaikkaKortti` for `DiagonaalKortti` at the single integration point in `app/components/Etusivu.tsx` — the bottom-sheet search-results card list (lines 797-809). Remove the props that DiagonaalKortti does not accept (`aukinyt`, `isSuosikki`, `onToggleSuosikki`). Then run a human-verify checkpoint to confirm visual rendering, Static Maps image loading, click navigation, and that the MAP-06 zoom-cards remain unaffected.

Purpose: Activates UI-11 in the running app by replacing the existing card component at the integration point Phase 12 created. Without this swap the new card is dead code.

Output: Etusivu.tsx with one import line changed and one JSX block changed; human checkpoint confirms all three roadmap success criteria visually.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/13-uusi-korttimalli/13-CONTEXT.md
@.planning/phases/13-uusi-korttimalli/13-RESEARCH.md
@.planning/phases/13-uusi-korttimalli/13-PATTERNS.md
@.planning/phases/13-uusi-korttimalli/13-01-SUMMARY.md
@app/components/Etusivu.tsx
@app/components/DiagonaalKortti.tsx

<interfaces>
<!-- The DiagonaalKortti component contract created in Plan 01 -->

From app/components/DiagonaalKortti.tsx (Plan 01 output):
```
interface DiagonaalKorttiProps {
  paikka: Liikuntapaikka
  distanceStr?: string
}
export default function DiagonaalKortti(props: DiagonaalKorttiProps): JSX.Element
export const diagonaalKorttiVariants: { hidden: {...}; show: {...} }
```

From app/components/Etusivu.tsx (existing — pre-swap state):
- Line 27: `import PaikkaKortti from './PaikkaKortti'` (only PaikkaKortti import in this file)
- Lines 797-809: PaikkaKortti is invoked exactly once, inside `{searchSuodatettu.length > 0 ? (<div className="flex flex-col gap-3">{searchSuodatettu.map(p => (<PaikkaKortti key={p.id} paikka={p} distanceStr={distancesMap[p.id] != null ? formatDistance(distancesMap[p.id]) : undefined} aukinyt={searchAukinyt} isSuosikki={suosikitIds.has(p.id)} onToggleSuosikki={toggleSuosikki} />))}</div>) : (...)}`
- MAP-06 zoom-cards live around lines 396-411 inside `<AdvancedMarker>` JSX — these are inline `motion.div` elements, not PaikkaKortti, and must be left untouched
- `formatDistance`, `distancesMap`, `searchSuodatettu` are all in-scope locals already used by the existing PaikkaKortti call
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Swap PaikkaKortti → DiagonaalKortti import and JSX in Etusivu.tsx</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    - app/components/Etusivu.tsx (lines 1-40 for imports, lines 390-420 for MAP-06 zoom-cards context to confirm they are not PaikkaKortti, lines 790-830 for the swap site)
    - app/components/DiagonaalKortti.tsx (Plan 01 output — confirm DiagonaalKorttiProps shape: only `paikka` and `distanceStr?`)
    - .planning/phases/13-uusi-korttimalli/13-01-SUMMARY.md (Plan 01 outcome — any deviations to be aware of)
    - .planning/phases/13-uusi-korttimalli/13-PATTERNS.md (section "Etusivu.tsx import swap" — exact before/after)
    - .planning/phases/13-uusi-korttimalli/13-RESEARCH.md (Pattern 7 — integration point details)
  </read_first>
  <action>
    Make exactly two edits to `app/components/Etusivu.tsx`:

    Edit 1 — Replace the PaikkaKortti import at line 27:
    Change `import PaikkaKortti from './PaikkaKortti'` to `import DiagonaalKortti from './DiagonaalKortti'`. Keep the line in the same position relative to other imports. Do not add a separate import for `PaikkaKortti` anywhere else — it is no longer used in this file.

    Edit 2 — Replace the `<PaikkaKortti ... />` JSX block (currently at lines 800-807) with a `<DiagonaalKortti ... />` block. Keep the `key={p.id}` and `paikka={p}` and `distanceStr={distancesMap[p.id] != null ? formatDistance(distancesMap[p.id]) : undefined}` props. Remove the three props `aukinyt={searchAukinyt}`, `isSuosikki={suosikitIds.has(p.id)}`, and `onToggleSuosikki={toggleSuosikki}` — DiagonaalKortti does not accept them (per D-09).

    Do not modify:
    - The outer `{searchSuodatettu.length > 0 ? (` ternary
    - The `<div className="flex flex-col gap-3">` wrapper (do NOT convert it to a `motion.div` with staggerChildren — that is RESEARCH.md open question 2 and explicitly OUT of UI-11 scope)
    - The empty-state branch (`<p className="glass rounded-2xl px-6 py-4 ...">Ei tuloksia</p>` and clear-filters button)
    - The MAP-06 zoom-cards around lines 396-411 — these are inline `motion.div` elements inside `<AdvancedMarker>`, NOT PaikkaKortti, and roadmap success criterion #3 requires they remain unchanged
    - Any other Etusivu logic, state, or layout

    Do not delete the `searchAukinyt`, `suosikitIds`, or `toggleSuosikki` identifiers from the file — they are still used elsewhere (e.g., the "Auki nyt" filter toggle button at line 783-790 reads/writes `searchAukinyt`; the suosikit modal and other UI may use `suosikitIds`/`toggleSuosikki`). Only stop passing them to the card component.

    After editing, the only remaining references to `PaikkaKortti` in the file should be zero. Verify by mentally grep'ing the buffer.
  </action>
  <verify>
    <automated>cd C:/ClaudeCodeTestit/liikuntahakemisto && (grep -n "PaikkaKortti" app/components/Etusivu.tsx | grep -v "^#" | wc -l | awk '$1==0{print "OK_NO_PAIKKAKORTTI"; exit 0} {print "FAIL: PaikkaKortti still referenced"; exit 1}') && grep -c "DiagonaalKortti" app/components/Etusivu.tsx && npx tsc --noEmit 2>&1 | (grep -i "etusivu.*error" || echo "OK_TYPECHECK") && npx vitest run lib/ --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/Etusivu.tsx` contains exactly 0 occurrences of the substring `PaikkaKortti` (no import, no JSX usage)
    - `app/components/Etusivu.tsx` contains at least 2 occurrences of the substring `DiagonaalKortti` (1 import + 1 JSX usage)
    - The import line `import DiagonaalKortti from './DiagonaalKortti'` appears in the file
    - The DiagonaalKortti JSX call passes exactly three named props in some order: `key`, `paikka`, `distanceStr` — and does NOT pass `aukinyt`, `isSuosikki`, or `onToggleSuosikki`
    - The `<div className="flex flex-col gap-3">` wrapper around the `.map()` call is unchanged (not converted to motion.div)
    - The MAP-06 zoom-cards around lines 396-411 (inline `motion.div` inside `<AdvancedMarker>`) are unchanged — diff for that line range is empty
    - The identifiers `searchAukinyt`, `suosikitIds`, and `toggleSuosikki` still exist somewhere in the file (used by other UI like the "Auki nyt" filter button)
    - `npx tsc --noEmit` reports no new type errors in `app/components/Etusivu.tsx`
    - `npx vitest run lib/` exits with code 0 (no regression)
  </acceptance_criteria>
  <done>Etusivu.tsx renders DiagonaalKortti instead of PaikkaKortti for the search-results card list. PaikkaKortti has zero references in this file. MAP-06 zoom-cards untouched. Type check + lib tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — visual diagonal split, Static Maps loading, click navigation, MAP-06 unaffected</name>
  <what-built>
    Plan 01 created `DiagonaalKortti.tsx` with a clip-path diagonal split layout, Static Maps `<img>` thumbnail, and fallback for missing coordinates. Plan 02 Task 1 swapped it into the Etusivu search-results list. This checkpoint confirms the running app renders correctly and that the three roadmap success criteria for Phase 13 are observable.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` from the project root.

    2. Open `http://localhost:3000/` in Chrome (the homepage / Etusivu — map view is default).

    3. Open the bottom sheet to expose the search-results card list (the sheet that Phase 12 added in the left toolbar / search panel area). Trigger the search panel via the search icon in the left toolbar and apply a filter that produces at least 5 results (e.g., laji = "Padel" or kaupunki = "Tampere"). Confirm the card list section renders below the search controls.

    4. **Diagonal split — Success Criterion #1 (vasen tiedot, oikea kartta):**
       - For each visible card, confirm a clear diagonal split between a left info panel and a right map panel
       - Left panel shows in order from top: sport pill (colored, with sport name and icon), venue name (bold, one line), open status row (green "Auki · HH:MM" or grey "Suljettu"), price row, distance row (with MapPin icon) pinned to the bottom
       - There should be NO address row, NO description, NO Sponsoroitu badge, NO "Näytä tiedot" CTA button, NO heart/suosikki icon
       - The diagonal join between the two panels should look clean — no 1px white seam line. If you see a thin white line, zoom Chrome to 110% (Ctrl+Shift+=) and 90% (Ctrl+Shift+-) and re-check; report at which zoom levels the seam appears.
       - Card height should be visually identical for every card in the list (h-32 = 128px).

    5. **Static Maps image — Success Criterion #2 (oikealla Static Maps -kuvakaappaus pin-ikonin kanssa):**
       - Open DevTools → Network tab → filter by "staticmap"
       - Confirm one `staticmap` request is made per visible card that has coordinates, each returning status 200 with a PNG/JPEG image
       - In the rendered card, the right panel should show a small zoomed-in map of the venue location with a red pin marker
       - If any request returns 403 / image fails to load: STOP and report the exact error. The likely cause is HTTP referrer restrictions on `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` not allowing `localhost`. Resume signal "blocked by referrer" will trigger a follow-up plan to configure the key in Google Cloud Console.

    6. **Fallback (no coordinates) — D-06:**
       - If your filter produces any place without lat/lng, confirm its right panel shows a solid sport-colored background with a large centered white sport icon (no broken image icon)
       - If no such place exists in the current data, this branch can be confirmed code-side from Plan 01's acceptance criteria; mark this step as "no-data-skipped" rather than failed.

    7. **Click navigation — D-08 (whole card is a Link):**
       - Click on any part of a card (the sport pill area, the name, the price area, the distance area, the map image area)
       - Each click must navigate the browser to `/paikat/{id}` for that venue's profile page
       - Use the browser back button to return; repeat with at least 3 different click targets on the same card

    8. **MAP-06 zoom-cards unchanged — Success Criterion #3 (kartan zoom-kortit pysyvät ennallaan):**
       - Close the search panel / bottom sheet to expose the map fully
       - Zoom into the map until pin-to-card transitions trigger (MAP-06 behavior)
       - Confirm the zoom-cards (inline cards attached to map markers) render with the SAME visual style as before Phase 13 — they should NOT have the diagonal split layout. They are unchanged because they are separate inline `motion.div` elements inside `<AdvancedMarker>`, not PaikkaKortti.

    9. **Animation feel — D-10:**
       - Hover a card with the mouse — it should scale up subtly (≈2%) with a snappy 180ms ease-out
       - Click and hold a card without releasing — it should scale down to 98%
       - The motion should be scale only, no upward/downward lift
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved` — all 6 visual checks pass; Phase 13 ready for `/gsd:verify-work`
    - `blocked by referrer` — Static Maps returns 403 / images fail; a follow-up plan will be created to configure API key restrictions
    - `seam visible at {zoom%}` — 1px white line appears at specific Chrome zoom levels; clip-path percentages may need tweaking
    - `MAP-06 broken` — zoom-cards changed unexpectedly; revisit Plan 02 Task 1 diff
    - or describe any other issue observed; the planner will create a gap-closure plan
  </resume-signal>
  <files>app/components/Etusivu.tsx, app/components/DiagonaalKortti.tsx (read-only — running app uses these)</files>
  <action>Human-verify checkpoint — see <what-built> and <how-to-verify> for full instructions. The executor must run `npm run dev`, open the browser, walk through the 9 verification steps in order, then capture the user’s resume-signal response. Do NOT modify any source files in this task — Task 1 already did the swap. This task is purely observational.</action>
  <verify><human-check>9-step browser verification in <how-to-verify>; user replies with one of the resume-signal values</human-check></verify>
  <done>User has replied with `approved` (or a documented issue resume-signal that triggers a follow-up gap plan). All 9 verification steps recorded in the 13-02-SUMMARY.md output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /paikat/{id} | `<Link href>` navigation using `paikka.id` (database-sourced `number`); already covered by Next.js dynamic route handling |
| browser → googleapis.com/staticmap | Same boundary as Plan 01; reaffirmed by Task 2 human verification of the actual HTTP requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | Etusivu prop swap — removal of `isSuosikki`/`onToggleSuosikki` from DiagonaalKortti call | accept | Suosikki state is still maintained server-side (Supabase) and in `suosikitIds` local state; the DiagonaalKortti view simply does not surface it (D-09 — feature out of scope for this card variant). No data leak. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero new packages (RESEARCH.md "No new packages required") — Package Legitimacy Gate not applicable; legitimacy audit table noted as N/A in RESEARCH.md |
</threat_model>

<verification>
1. Source assertion: `grep PaikkaKortti app/components/Etusivu.tsx` returns 0 matches
2. Source assertion: `grep -c DiagonaalKortti app/components/Etusivu.tsx` returns ≥ 2
3. Type check: `npx tsc --noEmit` no errors
4. Regression: `npx vitest run lib/` exits 0
5. Human visual verification of the 6 success criteria listed in Task 2
6. Network tab confirms Static Maps requests return 200
</verification>

<success_criteria>
- Etusivu.tsx imports DiagonaalKortti, not PaikkaKortti
- Search-results card list renders DiagonaalKortti for each item with only `key`, `paikka`, `distanceStr` props
- All three roadmap criteria for Phase 13 are observable in the running app:
  1. Etusivun korttilistassa jokainen kortti näyttää vasemmalla puolella paikan nimen, lajin, hinnan, aukioloajan ja etäisyyden — VERIFIED
  2. Kortissa on oikealla puolella Google Static Maps -kuvakaappaus paikan sijainnista pin-ikonin kanssa — VERIFIED (200 response in Network tab)
  3. Kartan zoom-kortit (MAP-06) pysyvät ennallaan — VERIFIED (zoom-cards unchanged)
- Click anywhere on a card navigates to /paikat/{id}
- No clip-path seam at 100% browser zoom
- Human checkpoint resolved with `approved` resume signal
</success_criteria>

<output>
Create `.planning/phases/13-uusi-korttimalli/13-02-SUMMARY.md` when done, listing:
- Line numbers of the two edits applied to Etusivu.tsx
- Confirmation that `grep PaikkaKortti app/components/Etusivu.tsx` returns 0
- Human checkpoint resume-signal value received (`approved` / specific issue)
- Network tab observation: number of staticmap requests, HTTP status codes
- Any Claude's discretion adjustments made (e.g., clip-path % tweaks if seam was reported)
- Confirmation that MAP-06 zoom-cards still render unchanged
</output>
</content>
</invoke>