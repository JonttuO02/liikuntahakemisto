# Plan 12-03: Delete LiikuntapaikatLista

**Phase:** 12 — Haku & korttilistaus etusivulle
**Depends on:** 12-01 (no more import in page.tsx), 12-02 (filtering logic migrated to Etusivu)
**Goal:** Remove the `LiikuntapaikatLista` component file from the codebase entirely

---

## Context

After 12-01: `app/page.tsx` no longer imports or renders `LiikuntapaikatLista`.
After 12-02: all filtering logic from `LiikuntapaikatLista.tsx` has been migrated into `Etusivu.tsx`.

The file is now orphaned — no imports anywhere in the codebase reference it.

---

## Tasks

### T1 — Delete the component file

Delete: `app/components/LiikuntapaikatLista.tsx`

No other changes are needed — the file is not imported anywhere after 12-01 and 12-02.

### T2 — Confirm no remaining references

Run a search for any remaining references to `LiikuntapaikatLista` in the codebase. If any are found, remove them. Expected: zero references after deletion.

---

## Verification

- `app/components/LiikuntapaikatLista.tsx` does not exist
- No file in the project imports `LiikuntapaikatLista`
- TypeScript compiles without errors (`npx tsc --noEmit`)
- Dev server starts without errors (`npm run dev`)
- All 4 phase success criteria are now met:
  1. Search icon in left toolbar opens overlay → real-time filtering works
  2. All filter-matching venues shown as scrollable card list in overlay
  3. `/?nakyma=lista` redirects to `/` (301)
  4. `LiikuntapaikatLista` component deleted from codebase
