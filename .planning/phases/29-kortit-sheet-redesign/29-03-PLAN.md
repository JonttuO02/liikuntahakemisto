---
phase: 29-kortit-sheet-redesign
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/DiagonaalKortti.tsx
autonomous: false
requirements: [UI-26, UI-27]
must_haves:
  truths:
    - "DiagonaalKortti shows a 40×40 gray logo placeholder with a Building2 icon to the left of the sport pill"
    - "When a venue has no image, the right panel shows a gray placeholder with a Camera icon instead of the sport color + sport icon"
    - "Venues WITH an image_url still render the real image (real-image branch and onError fallback wiring intact)"
  artifacts:
    - path: "app/components/DiagonaalKortti.tsx"
      provides: "Logo placeholder in badge row + gray/camera right-panel fallback"
      contains: "Building2"
  key_links:
    - from: "app/components/DiagonaalKortti.tsx"
      to: "lucide-react"
      via: "named import of Building2 and Camera"
      pattern: "import.*\\{[^}]*Building2[^}]*Camera[^}]*\\}.*lucide-react"
    - from: "DiagonaalKortti img onError"
      to: "[data-fallback] div"
      via: "querySelector on parentElement"
      pattern: "data-fallback"
---

<objective>
Update DiagonaalKortti's two placeholder slots per D-21–D-24: add a 40×40 gray logo box (Building2 icon) to the left of the sport pill, and replace the sport-color + SportIcon right-panel fallback with a gray box + Camera icon. The real-image branch (`paikka.image_url`) and its `onError` → `[data-fallback]` wiring stay intact.

Purpose: Prepares DiagonaalKortti's visual layout for future real logos and venue photos (deferred to a later milestone) while shipping neutral placeholders now (UI-26, UI-27).
Output: Modified DiagonaalKortti.tsx with two new Lucide imports and two restructured DOM regions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-kortit-sheet-redesign/29-CONTEXT.md
@.planning/phases/29-kortit-sheet-redesign/29-RESEARCH.md
@.planning/phases/29-kortit-sheet-redesign/29-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Current DiagonaalKortti import line (line 5) — extend it -->
From app/components/DiagonaalKortti.tsx:
```tsx
import { MapPin, Check } from 'lucide-react'
```

<!-- Current badge row (lines 57–63) — wrap into a flex row with logo box -->
<!-- Current right-panel fallback div (lines 113–121) — restyle to gray + Camera -->
<!-- See 29-PATTERNS.md "DiagonaalKortti.tsx" section for exact before/after markup -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add logo placeholder to the left of the sport pill (UI-26)</name>
  <files>app/components/DiagonaalKortti.tsx</files>
  <read_first>
    - app/components/DiagonaalKortti.tsx (full file — note line 5 imports, the left-panel clipPath on line 55, and the badge `<span>` at lines 57–63)
    - .planning/phases/29-kortit-sheet-redesign/29-PATTERNS.md (DiagonaalKortti badge-row before/after; Pattern 5 sizing + clipPath constraint)
    - CLAUDE.md (color system — `rgba(0,0,0,0.06)` background, icon muted tones)
  </read_first>
  <action>
    Add `Building2` to the lucide-react import on line 5 (becomes `import { MapPin, Check, Building2 } from 'lucide-react'`). Wrap the existing sport-pill `<span>` (lines 57–63) inside a new flex container `<div className="flex items-start gap-2 self-start">`. As the FIRST child of that container, add the logo placeholder: a `<div className="w-10 h-10 rounded-lg bg-[rgba(0,0,0,0.06)] flex items-center justify-center shrink-0">` containing `<Building2 size={20} className="text-[rgba(0,0,0,0.25)]" />`. Keep the sport pill as the SECOND child unchanged except: remove `self-start` from the span (the wrapper now owns alignment) and add `mt-1` to vertically center it against the taller logo box. Use `items-start` (not items-center) on the wrapper so the 40px box and pill align to the top — this prevents the badge row from pushing the venue name out of the `h-32` card. The logo box (40px) + gap (8px) + pill fits within the ~57–62% left-panel clipPath width at p-3 padding (see Pattern 5: ~128px of ~190px available at 375px screen).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Line 5 import includes `Building2`
    - DiagonaalKortti.tsx contains `<Building2 size={20}` with className `text-[rgba(0,0,0,0.25)]`
    - The logo box div uses classes `w-10 h-10 rounded-lg bg-[rgba(0,0,0,0.06)]` and `shrink-0`
    - The logo box and sport pill are siblings inside one `flex items-start gap-2 self-start` wrapper, logo box first
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A 40×40 gray Building2 logo box renders to the left of the sport pill in the left panel's top row.</done>
</task>

<task type="auto">
  <name>Task 2: Replace right-panel fallback with gray + Camera (UI-27)</name>
  <files>app/components/DiagonaalKortti.tsx</files>
  <read_first>
    - app/components/DiagonaalKortti.tsx (right-panel region lines 94–122 — note the real-image `<img>` with onError on lines 99–112 and the fallback `<div>` on lines 113–121)
    - .planning/phases/29-kortit-sheet-redesign/29-PATTERNS.md (Pattern 6 — exact fallback before/after; data-fallback constraint)
  </read_first>
  <action>
    Add `Camera` to the lucide-react import on line 5 (final form: `import { MapPin, Check, Building2, Camera } from 'lucide-react'`). In the fallback `<div>` (lines 113–121): remove the inline `style={{ backgroundColor: laji.color }}` entirely and add `bg-[rgba(0,0,0,0.06)]` to its className (final: `className="w-full h-full flex items-center justify-center bg-[rgba(0,0,0,0.06)]"`). Replace the `<SportIcon laji={paikka.laji} size={32} className="text-white opacity-80" />` child with `<Camera size={24} className="text-[rgba(0,0,0,0.2)]" />`. Keep `aria-hidden`, `data-fallback`, and `hidden={!!paikka.image_url}` exactly as-is — the `onError` handler on the `<img>` (lines 105–110) queries `[data-fallback]` and unsets its `hidden`, so that attribute and the marker must survive. Leave the real-image `<img>` branch (lines 99–112) completely unchanged. Do NOT remove `SportIcon` from the import on line 7 — it is still used by the left-panel sport pill.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The fallback div className contains `bg-[rgba(0,0,0,0.06)]` and no longer has `style={{ backgroundColor: laji.color }}`
    - The fallback div renders `<Camera size={24}` with className `text-[rgba(0,0,0,0.2)]`
    - `data-fallback`, `aria-hidden`, and `hidden={!!paikka.image_url}` are all still present on the fallback div
    - The `<img>` real-image branch and its `onError` handler are unchanged
    - `SportIcon` is still imported (line 7) and still used in the left-panel sport pill
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Right panel shows gray + Camera when no image; real images and onError fallback wiring still work.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Logo placeholder (Building2) left of the sport pill, and gray+Camera right-panel fallback replacing the sport-color+SportIcon fallback in DiagonaalKortti.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app; switch to the map/card list view that renders DiagonaalKortti.
    2. Find a venue WITHOUT an image: confirm the right panel is a light gray box with a centered camera icon (NOT a sport-colored panel with a sport icon).
    3. Confirm the left panel's top row shows a 40×40 gray rounded box with a building icon to the LEFT of the sport pill, both aligned to the top.
    4. Confirm the venue name below the badge row is not clipped/pushed off the 128px-tall card on a narrow (mobile) viewport.
    5. If any venue has a real image_url, confirm that image still renders in the right panel (placeholder only shows when there is no image).
  </how-to-verify>
  <resume-signal>Type "approved" or describe layout issues (e.g. name clipped, box overflowing clipPath)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Changes are static visual placeholders; no data input, no network calls.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | Tampering | npm installs | mitigate | No packages installed; Building2/Camera already present in installed lucide-react 1.16.0 (RESEARCH.md verified) |
| T-29-04 | Denial of service | clipPath layout overflow on narrow screens | accept | Pattern 5 verified ~128px content fits ~190px available; human checkpoint confirms no clipping |
</threat_model>

<verification>
- `npx tsc --noEmit` → 0 errors
- Human checkpoint confirms: logo box left of pill, gray+camera fallback, real images intact, no name clipping
</verification>

<success_criteria>
- Building2 logo placeholder renders left of the sport pill (UI-26)
- Right-panel fallback is gray + Camera, replacing sport-color + SportIcon, with real-image branch and onError wiring preserved (UI-27)
- tsc clean
</success_criteria>

<output>
Create `.planning/phases/29-kortit-sheet-redesign/29-03-SUMMARY.md` when done
</output>
