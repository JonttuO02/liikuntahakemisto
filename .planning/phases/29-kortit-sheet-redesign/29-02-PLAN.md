---
phase: 29-kortit-sheet-redesign
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - app/components/PaikkaKortti.tsx
autonomous: false
requirements: [UI-25]
must_haves:
  truths:
    - "When hinta_kuvaus has 2+ non-empty lines (and not membership-only), PaikkaKortti shows a leftward-scrolling marquee of the price lines separated by ·"
    - "When hinta_kuvaus has 1 line, is null, or membershipOnly is true, the card shows the existing static price text (no marquee)"
    - "The marquee row has a top border separating it from the content above"
  artifacts:
    - path: "app/components/PaikkaKortti.tsx"
      provides: "Marquee price row using @keyframes marquee + marqueePriceLines() guard"
      contains: "marquee"
  key_links:
    - from: "app/components/PaikkaKortti.tsx"
      to: "lib/priceUtils.ts marqueePriceLines"
      via: "named import"
      pattern: "import.*marqueePriceLines.*priceUtils"
    - from: "PaikkaKortti marquee div"
      to: "globals.css @keyframes marquee"
      via: "inline animation style"
      pattern: "animation:.*marquee"
---

<objective>
Replace PaikkaKortti's static price block with a conditional marquee: when `hinta_kuvaus` has 2+ non-empty lines (and the venue is not membership-only), render a leftward-scrolling marquee of the price lines (two-copy seamless loop, `·` separators); otherwise fall back to the existing static price text. Uses the `marqueePriceLines()` guard and `@keyframes marquee` from Plan 01.

Purpose: UI-25 — show richer multi-line pricing without consuming vertical card space, matching the FilterCarouselPill scrolling-animation pattern.
Output: Modified PaikkaKortti.tsx — updated price logic and a new marquee row with the existing static fallbacks intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-kortit-sheet-redesign/29-CONTEXT.md
@.planning/phases/29-kortit-sheet-redesign/29-RESEARCH.md
@.planning/phases/29-kortit-sheet-redesign/29-PATTERNS.md
@CLAUDE.md
@.planning/phases/29-kortit-sheet-redesign/29-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — the guard this plan consumes -->
From lib/priceUtils.ts:
```typescript
export function marqueePriceLines(
  hintaKuvaus: string | null | undefined,
  membershipOnly: boolean
): string[] | null   // returns 2+ non-empty lines, else null
```

<!-- From Plan 01 — the keyframe this plan animates with -->
From app/globals.css:
```css
@keyframes marquee { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }
```

<!-- Current PaikkaKortti price logic (lines 37–43) and price block (lines 115–130) — see 29-PATTERNS.md PaikkaKortti section for exact before/after -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire marqueePriceLines guard + render marquee row (UI-25)</name>
  <files>app/components/PaikkaKortti.tsx</files>
  <read_first>
    - app/components/PaikkaKortti.tsx (full file — note import block lines 1–11, priceLines computation lines 38–40, priceText line 41–43, the price block lines 115–130, and the bottom-row border-t separator line 146)
    - lib/priceUtils.ts (the marqueePriceLines signature from Plan 01)
    - .planning/phases/29-kortit-sheet-redesign/29-PATTERNS.md (PaikkaKortti section — exact replacement markup and two-copy loop)
    - .planning/phases/29-kortit-sheet-redesign/29-RESEARCH.md (Pattern 3 + Pitfall 2 — two-copy loop must not append a trailing separator that unbalances the copies)
    - CLAUDE.md (animation: no spring for non-drag elements; price typography text-sm font-bold tabular-nums; border-[rgba(0,0,0,0.07)] separator)
  </read_first>
  <action>
    Add `marqueePriceLines` to the existing `import { isMembershipOnly } from '@/lib/priceUtils'` on line 9 (becomes `import { isMembershipOnly, marqueePriceLines } from '@/lib/priceUtils'`).

    Replace the current `priceLines` computation (lines 38–40) with `const priceLines = marqueePriceLines(paikka.hinta_kuvaus, membershipOnly)`. This returns a string[] of 2+ non-empty lines or null — the guard now lives in the helper. Keep `priceText` (lines 41–43) unchanged as the single-line/static fallback.

    Replace the price block (lines 115–130) with a conditional: (a) if `membershipOnly` → existing `<span className="text-sm text-[rgba(17,17,17,0.5)]">vain jäsenyys</span>`; (b) else if `priceLines && priceLines.length >= 2` → a marquee row: an outer `<div className="border-t border-[rgba(0,0,0,0.07)] overflow-hidden pt-2">` (overflow-hidden clips the scroll; border-t matches the bottom-row separator) wrapping an inner `<div className="flex whitespace-nowrap text-sm font-bold text-[#111111] tabular-nums" style={{ animation: 'marquee 8s linear infinite', willChange: 'transform' }}>`. Inside, render `[...priceLines, ...priceLines].map((line, i) => ...)` — TWO copies for the seamless loop (Pitfall 2: both copies must be identical and balanced). Each item is a `<span key={i} className="flex items-center shrink-0">{line}<span className="mx-2 text-[rgba(17,17,17,0.3)]">·</span></span>` (the `·` after every item keeps both copies symmetric so the -50% wrap is seamless); (c) else if `priceText` → existing `<span className="text-sm font-bold text-[#111111] tabular-nums">{priceText}</span>`; (d) else → existing `<span className="text-xs text-[rgba(17,17,17,0.35)]">Lisätään pian</span>`.

    Per D-16, the marquee replaces the price block at its current position (between open-status and address). Do not move the bottom CTA/distance row (line 146) — it stays at the very bottom with its own border-t. Speed ~40px/s → 8s duration default (tunable; see RESEARCH.md A4).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Line 9 import includes `marqueePriceLines`
    - `priceLines` is computed via `marqueePriceLines(paikka.hinta_kuvaus, membershipOnly)` (no inline split/includes logic remains)
    - The marquee branch renders an outer div with `border-t border-[rgba(0,0,0,0.07)]` and `overflow-hidden`, and an inner div with inline `style` `animation: 'marquee 8s linear infinite'`
    - The marquee maps `[...priceLines, ...priceLines]` (two copies) with a `·` separator (`text-[rgba(17,17,17,0.3)]`) after each item
    - The membershipOnly, single-line priceText, and "Lisätään pian" static fallbacks are all preserved
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 (Plan 01 tests still green)
  </acceptance_criteria>
  <done>Marquee renders for 2+ price lines using the shared keyframe + guard; all static fallbacks preserved; tsc + tests clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Conditional marquee price row in PaikkaKortti: leftward-scrolling two-copy loop for 2+ price lines, static text otherwise.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the card list.
    2. Find a venue whose `hinta_kuvaus` has 2+ lines (newline-separated). Confirm the price row scrolls leftward continuously and seamlessly (no visible jump/stutter at the loop point — Pitfall 2 check), items separated by `·`.
    3. Find a venue with a single-line price: confirm it shows STATIC text (no animation, no scrolling).
    4. Find a membership-only venue: confirm it shows "vain jäsenyys" (no marquee).
    5. Find a venue with no price: confirm "Lisätään pian".
    6. Confirm a thin top border separates the marquee row from the content above, and the bottom CTA/distance row is unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. marquee stutters at loop, single-line scrolls, speed too fast/slow)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. The marquee renders already-trusted `hinta_kuvaus` via a pure helper; pure CSS animation, no input handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08 | Tampering | npm installs | mitigate | No packages installed in this plan |
| T-29-09 | Information disclosure | hinta_kuvaus rendered in marquee | accept | Public venue pricing; already displayed on the card; no PII |
</threat_model>

<verification>
- `npx tsc --noEmit` → 0 errors
- `npx vitest run` → all green (Plan 01 marqueePriceLines tests)
- Human checkpoint confirms seamless leftward scroll for 2+ lines, static for 1 line / membership / none
</verification>

<success_criteria>
- Marquee activates only for 2+ non-empty price lines (via marqueePriceLines), scrolling left seamlessly with `·` separators (UI-25)
- Static fallbacks (membership, single line, none) preserved
- tsc + tests clean
</success_criteria>

<output>
Create `.planning/phases/29-kortit-sheet-redesign/29-02-SUMMARY.md` when done
</output>
