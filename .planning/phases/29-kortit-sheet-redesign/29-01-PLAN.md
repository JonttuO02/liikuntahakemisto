---
phase: 29-kortit-sheet-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - lib/priceUtils.ts
  - lib/priceUtils.test.ts
autonomous: true
requirements: [UI-25]
must_haves:
  truths:
    - "A leftward CSS marquee keyframe named `marquee` exists in globals.css"
    - "A pure, unit-tested function decides whether a price string should marquee (2+ non-empty lines)"
  artifacts:
    - path: "app/globals.css"
      provides: "@keyframes marquee (0 → translateX(-50%), leftward)"
      contains: "@keyframes marquee"
    - path: "lib/priceUtils.ts"
      provides: "marqueePriceLines() helper — returns string[] of 2+ non-empty lines, else null"
      exports: ["marqueePriceLines"]
    - path: "lib/priceUtils.test.ts"
      provides: "Unit tests for marquee guard logic"
      contains: "marqueePriceLines"
  key_links:
    - from: "lib/priceUtils.ts"
      to: "lib/priceUtils.test.ts"
      via: "named import in test"
      pattern: "import.*marqueePriceLines"
---

<objective>
Create the two shared foundations the PaikkaKortti marquee depends on: (1) the leftward `@keyframes marquee` animation in globals.css, and (2) a pure, unit-tested `marqueePriceLines()` helper in lib/priceUtils.ts that decides when the marquee activates.

Purpose: The marquee guard logic (split `hinta_kuvaus` by `\n`, filter empty lines, require 2+) is the only automatable behavior in this otherwise visual phase. Extracting it as a pure function makes UI-25 testable (Wave 0 requirement) and keeps PaikkaKortti.tsx (Plan 02) free of inline parsing logic.
Output: A new keyframe in globals.css, a new exported function in lib/priceUtils.ts, and passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-kortit-sheet-redesign/29-CONTEXT.md
@.planning/phases/29-kortit-sheet-redesign/29-RESEARCH.md
@.planning/phases/29-kortit-sheet-redesign/29-PATTERNS.md

<interfaces>
<!-- Existing priceUtils.ts export (the marquee helper must live alongside this) -->
From lib/priceUtils.ts:
```typescript
export function isMembershipOnly(
  p: Pick<Liikuntapaikka, 'hinta_kuvaus' | 'hinta_min' | 'hinta_max'>
): boolean
```

<!-- Existing globals.css keyframe — DO NOT REUSE (wrong direction). New keyframe goes immediately after it. -->
From app/globals.css (lines 144–147):
```css
@keyframes tickerScrollRight {
  0%   { transform: translateX(-50%); }
  100% { transform: translateX(0%); }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add marqueePriceLines() helper + unit tests (Wave 0)</name>
  <files>lib/priceUtils.ts, lib/priceUtils.test.ts</files>
  <read_first>
    - lib/priceUtils.ts (current file — see existing isMembershipOnly signature and JSDoc style)
    - lib/priceUtils.test.ts (current test file — match the vitest describe/it style and Finnish test descriptions)
    - .planning/phases/29-kortit-sheet-redesign/29-RESEARCH.md (Open Question 3 — filter empty lines before counting)
  </read_first>
  <behavior>
    marqueePriceLines(hinta_kuvaus, membershipOnly) → string[] | null:
    - Test 1: "8€/kerta\n50€/kk" with membershipOnly=false → ['8€/kerta', '50€/kk'] (length 2 → array returned)
    - Test 2: "8€/kerta" (single line) with membershipOnly=false → null (only 1 non-empty line)
    - Test 3: null hinta_kuvaus → null
    - Test 4: undefined hinta_kuvaus → null
    - Test 5: "8€/kerta\n\n50€/kk\n   " (blank + whitespace-only lines) → ['8€/kerta', '50€/kk'] (whitespace-only lines filtered, length 2)
    - Test 6: "8€\n   " (one real line + one whitespace line) → null (filtered length 1, not 2)
    - Test 7: "8€/kerta\n50€/kk" with membershipOnly=true → null (membership overrides marquee)
  </behavior>
  <action>
    Add an exported pure function `marqueePriceLines(hintaKuvaus: string | null | undefined, membershipOnly: boolean): string[] | null` to lib/priceUtils.ts, placed after the existing `isMembershipOnly` export. Logic: if `membershipOnly` is true OR `hintaKuvaus` is falsy, return null. Otherwise split on `\n`, apply `.filter(l => l.trim().length > 0)`, and return the resulting array only when its length is >= 2; otherwise return null. Use a JSDoc block matching the existing isMembershipOnly comment style (Finnish prose, reference UI-25 and D-17/D-18). In lib/priceUtils.test.ts, add a new `describe('marqueePriceLines', ...)` block with the 7 cases from <behavior>, importing `marqueePriceLines` alongside the existing `isMembershipOnly` import on line 2. Keep the existing isMembershipOnly tests untouched.
  </action>
  <verify>
    <automated>npx vitest run lib/priceUtils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/priceUtils.ts` contains `export function marqueePriceLines`
    - `lib/priceUtils.test.ts` line 2 imports both `isMembershipOnly` and `marqueePriceLines`
    - `npx vitest run lib/priceUtils.test.ts` exits 0 with the new describe block reporting 7 passing cases plus the 9 existing isMembershipOnly cases
    - Behavior: marqueePriceLines('8€\n   ', false) returns null (whitespace-only second line filtered, leaving length 1)
    - Behavior: marqueePriceLines('8€/kerta\n50€/kk', true) returns null
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>marqueePriceLines is exported, fully typed, and all 16 priceUtils tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add leftward @keyframes marquee to globals.css</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (lines 138–160 — see the existing tickerScrollRight keyframe and the .pill-orbit-ring block that follows it)
    - .planning/phases/29-kortit-sheet-redesign/29-PATTERNS.md (globals.css section — exact keyframe to add and insertion point)
  </read_first>
  <action>
    Insert a new keyframe immediately after the existing `tickerScrollRight` block (which ends at line 147) and before the `.pill-orbit-ring` rule (line 149). The new keyframe must be named exactly `marquee` and go LEFTWARD: `0% { transform: translateX(0); }` and `100% { transform: translateX(-50%); }`. Do NOT modify or reuse `tickerScrollRight` (it goes the opposite direction, -50% → 0, and is consumed by FilterCarouselPill). The `-50%` endpoint pairs with the two-copy seamless-loop pattern used in Plan 02.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('app/globals.css','utf8'); const m=c.match(/@keyframes marquee\s*\{[^}]*\}/); if(!m) throw new Error('marquee keyframe missing'); if(!/translateX\(-50%\)/.test(m[0])) throw new Error('marquee missing -50% endpoint'); if(!/translateX\(0\)/.test(m[0])) throw new Error('marquee missing 0 start'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/globals.css` contains a `@keyframes marquee` block distinct from `@keyframes tickerScrollRight`
    - The marquee block contains both `translateX(0)` (at 0%) and `translateX(-50%)` (at 100%)
    - `tickerScrollRight` is unchanged (still `-50%` at 0% and `0%` at 100%)
    - The verify node command prints `OK` and exits 0
  </acceptance_criteria>
  <done>A leftward `marquee` keyframe exists in globals.css; tickerScrollRight is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan introduces no new trust boundaries. All changes are static CSS and a pure string-parsing function operating on already-trusted database content (`hinta_kuvaus`).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | npm/pip/cargo installs | mitigate | No packages installed in this phase (RESEARCH.md Package Legitimacy Audit confirms zero new deps); nothing to verify |
| T-29-02 | Information disclosure | marqueePriceLines on hinta_kuvaus | accept | hinta_kuvaus is public venue pricing already rendered elsewhere; no PII, pure client-side string split |
</threat_model>

<verification>
- `npx vitest run lib/priceUtils.test.ts` → 16 passing (9 existing + 7 new)
- `npx tsc --noEmit` → 0 errors
- globals.css contains both `@keyframes marquee` (leftward) and the untouched `@keyframes tickerScrollRight`
</verification>

<success_criteria>
- marqueePriceLines() is exported from lib/priceUtils.ts, returns string[] only for 2+ non-empty lines (null otherwise), and respects membershipOnly
- The `marquee` keyframe exists in globals.css scrolling leftward (0 → -50%)
- All priceUtils tests pass and tsc is clean
</success_criteria>

<output>
Create `.planning/phases/29-kortit-sheet-redesign/29-01-SUMMARY.md` when done
</output>
