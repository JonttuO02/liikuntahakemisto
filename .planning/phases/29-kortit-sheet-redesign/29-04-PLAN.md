---
phase: 29-kortit-sheet-redesign
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/PaikkaSheet.tsx
autonomous: false
requirements: [SHEET-01, SHEET-02, SHEET-03]
must_haves:
  truths:
    - "PaikkaSheet opens to a 16:9 hero carousel of 3 gray Camera placeholder slides at the very top"
    - "Drag handle floats over the hero top-center; close + bookmark buttons float top-right over the hero"
    - "Venue name and address render over the bottom of the hero with a dark-to-transparent gradient overlay"
    - "3 dot indicators below the hero track the active slide as the user swipes"
    - "Pricing appears as a SheetRow (CircleDollarSign, label Hinta) immediately below the hero + dots"
    - "Review widget is collapsed by default and expands smoothly on tap; shows a non-tappable static row when 0 reviews"
    - "The sport badge pill is gone from PaikkaSheet"
  artifacts:
    - path: "app/components/PaikkaSheet.tsx"
      provides: "Hero carousel, floating controls, gradient overlay, dot indicators, pricing SheetRow, collapsible review widget"
      contains: "aspect-video"
  key_links:
    - from: "PaikkaSheet hero carousel container"
      to: "motion.div drag=y close gesture"
      via: "onPointerDown stopPropagation"
      pattern: "onPointerDown=\\{e => e.stopPropagation\\(\\)\\}"
    - from: "PaikkaSheet carousel onScroll"
      to: "activeSlide dot indicators"
      via: "scrollLeft / offsetWidth index math"
      pattern: "setActiveSlide"
    - from: "PaikkaSheet collapsed review header"
      to: "ReviewSection"
      via: "AnimatePresence height auto expand"
      pattern: "AnimatePresence"
---

<objective>
Redesign PaikkaSheet per SHEET-01/02/03: replace the text-only header with a 16:9 hero image carousel (3 gray Camera placeholder slides + dot indicators), float the drag handle and close/bookmark controls over the hero, render venue name + address as a dark-gradient overlay at the bottom of the hero, move pricing to a SheetRow directly below the hero, and wrap ReviewSection in a collapsible widget that is collapsed by default. The sport badge pill is removed entirely (D-05).

Purpose: Modernize the venue sheet into a mobile-first hero layout, reduce initial scroll depth (collapsed reviews), and prepare image slots for future real photos (deferred). All slots are placeholders in v1.6.
Output: Heavily restructured PaikkaSheet.tsx with new state (activeSlide, reviewOpen, carouselRef), new imports (Camera, ChevronDown, AnimatePresence), and three new DOM regions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-kortit-sheet-redesign/29-CONTEXT.md
@.planning/phases/29-kortit-sheet-redesign/29-RESEARCH.md
@.planning/phases/29-kortit-sheet-redesign/29-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Current PaikkaSheet imports (lines 2–4) — extend framer-motion and lucide-react -->
From app/components/PaikkaSheet.tsx:
```tsx
import { motion } from 'framer-motion'
import { X, MapPin, Phone, ExternalLink, Clock, CircleDollarSign, Info, Bookmark, BookmarkCheck } from 'lucide-react'
```

<!-- ReviewSection contract (unchanged — wrap it, do not edit it) -->
From app/components/ReviewSection.tsx:
```tsx
// Props: paikkaId: number, initialReviews: ReviewRow[], avgRating: number | null, reviewCount: number
```

<!-- SheetRow helper already defined at bottom of PaikkaSheet.tsx (lines 197–209) — reuse verbatim -->
<!-- See 29-PATTERNS.md "PaikkaSheet.tsx" section for full before/after markup of every region -->
</interfaces>

<critical_constraints>
<!-- From 29-PATTERNS.md Critical Constraints Summary — these MUST survive the edit -->
- `layoutId={`vc-${paikka.id}`}` MUST stay on the outer motion.div (line 50) — drives card→sheet expand animation
- `overflow: 'hidden'` MUST stay in the outer motion.div style (line 60) — clips rounded-t-3xl corners
- `drag="y"` on the outer motion.div stays — the hero carousel needs `onPointerDown={e => e.stopPropagation()}` to avoid closing the sheet on horizontal swipe (RESEARCH.md Pitfall 4)
- The outer drag-handle div (lines 72–74) MUST keep its 32px height (`pt-3 pb-1`) so `height: 'calc(100% - 32px)'` on the scrollRef div (line 77) stays valid (RESEARCH.md Pitfall 1)
- The hero block goes INSIDE the scrollRef div as its FIRST child (not between the drag handle and scrollRef) — preserves the calc and the corner clipping
</critical_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero carousel + floating controls + gradient overlay + pricing SheetRow (SHEET-01, SHEET-02)</name>
  <files>app/components/PaikkaSheet.tsx</files>
  <read_first>
    - app/components/PaikkaSheet.tsx (full file — note outer motion.div lines 49–70, drag handle lines 72–74, scrollRef line 77, header row lines 80–106, name/address lines 108–120, price SheetRow lines 139–147, SheetRow helper lines 197–209)
    - .planning/phases/29-kortit-sheet-redesign/29-PATTERNS.md (PaikkaSheet section — exact hero structure markup, what to REMOVE, pricing SheetRow placement)
    - .planning/phases/29-kortit-sheet-redesign/29-RESEARCH.md (Pattern 1, Pattern 2, Pitfalls 1 & 4)
    - CLAUDE.md (glass-btn usage, animation durations, color system, typography — name uses font-bold text-white, no new font sizes beyond the 4 allowed)
  </read_first>
  <action>
    Extend imports: change line 3 to `import { motion, AnimatePresence } from 'framer-motion'` and add `Camera, ChevronDown` to the lucide-react import on line 4. Add new state after line 26 (alongside scrollRef): `const [activeSlide, setActiveSlide] = useState(0)`, `const [reviewOpen, setReviewOpen] = useState(false)`, and `const carouselRef = useRef<HTMLDivElement>(null)` (ChevronDown/reviewOpen are consumed in Task 2 but declare them here is fine OR defer reviewOpen to Task 2 — declare activeSlide and carouselRef now).

    REMOVE the entire header-row div (lines 80–106): the sport badge span (D-05, gone entirely, not moved) and the old top-right close/bookmark cluster — both move into the hero overlay. REMOVE the standalone name/address div (lines 108–120) — name and address move into the hero gradient overlay.

    Keep the outer drag-handle div (lines 72–74) at its 32px height unchanged. As the FIRST child of the scrollRef div (line 77, inside the `px-4 pb-8 flex flex-col gap-4` wrapper — note: the hero must be full-bleed so place the hero BEFORE/outside that px-4 padding wrapper, directly under the scrollRef div, then keep the padded content wrapper for the rows below), insert the hero per 29-PATTERNS.md:
    - A `relative aspect-video w-full overflow-hidden` container.
    - Floating drag indicator: `absolute top-3 left-1/2 -translate-x-1/2 z-20 w-10 h-1 bg-[rgba(255,255,255,0.5)] rounded-full`.
    - Floating controls `absolute top-3 right-3 z-20 flex items-center gap-1.5`: the bookmark motion.button (whileTap scale 0.85, calls onToggleTodo(paikka.id), BookmarkCheck when todo else Bookmark, `glass-btn w-8 h-8 rounded-full`) and the close button (calls onClose, X icon, `glass-btn w-8 h-8 rounded-full`) — reuse the exact button markup that currently lives at lines 88–104.
    - Carousel: a `ref={carouselRef}` div `flex overflow-x-auto snap-x snap-mandatory w-full h-full` with inline `style={{ scrollbarWidth: 'none', WebkitOverflowScrolling: 'touch' }}`, `onPointerDown={e => e.stopPropagation()}` (Pitfall 4), and an `onScroll` that sets activeSlide via `Math.round(carouselRef.current.scrollLeft / carouselRef.current.offsetWidth)`. Render 3 slides via `[0,1,2].map`: each `snap-start shrink-0 w-full h-full bg-[rgba(0,0,0,0.08)] flex items-center justify-center` containing `<Camera size={32} className="text-[rgba(255,255,255,0.4)]" />`.
    - Gradient overlay: `absolute bottom-0 inset-x-0 px-4 pb-3 pt-8` with inline `style={{ background: 'linear-gradient(to top, rgba(0,0,0,0.65) 0%, transparent 60%)' }}`. Inside: `<h2 className="font-bold text-white text-lg leading-tight">{paikka.nimi}</h2>` and, when osoite/kaupunki exist, `<p className="text-sm text-white/70 mt-0.5">{[paikka.osoite, paikka.kaupunki].filter(Boolean).join(', ')}</p>`.

    Below the hero (outside the aspect-video container, in normal flow), add dot indicators: `flex justify-center gap-1.5 py-2`, three `[0,1,2].map` spans `w-1.5 h-1.5 rounded-full transition-colors duration-150` with `bg-[#111111]` when `activeSlide === i` else `bg-[rgba(0,0,0,0.15)]`.

    Pricing (SHEET-02, D-09/D-10): keep the existing price SheetRow (lines 139–147) with CircleDollarSign icon and label "Hinta" and its unchanged inner content, but ensure it renders as the first content row immediately below the dot indicators (above the Aukioloajat/Hours row). The open-status paragraph (lines 122–130) stays in the padded content area below the hero. Preserve the `distanceKm` display — it currently lives in the removed name/address block; re-add the distance as part of the address line in the hero overlay OR append it to the open-status / a small row in the padded content. Per CONTEXT the overlay holds name+address; append `· {formatDistance(distanceKm)}` to the overlay address `<p>` when `distanceKm != null` so the distance is not lost.

    Do NOT delete the SheetRow helper (lines 197–209) or the Hours/Phone/Booking/Description rows. Do NOT remove the MapPin import (still used... verify: if MapPin is no longer used after removing the old address block, remove it from the import to keep tsc clean — the overlay address is plain text without the MapPin icon per 29-PATTERNS.md).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Line 3 imports `AnimatePresence` from framer-motion; line 4 lucide import includes `Camera` and `ChevronDown`
    - PaikkaSheet.tsx contains a `relative aspect-video w-full overflow-hidden` hero container
    - The carousel container has `onPointerDown={e => e.stopPropagation()}` and an `onScroll` calling `setActiveSlide`
    - Exactly 3 carousel slides each render `<Camera size={32}` with `bg-[rgba(0,0,0,0.08)]`
    - The gradient overlay uses `linear-gradient(to top, rgba(0,0,0,0.65) 0%, transparent 60%)` and renders `paikka.nimi` in `text-white`
    - 3 dot indicators use `bg-[#111111]` for active and `bg-[rgba(0,0,0,0.15)]` for inactive driven by `activeSlide`
    - The pricing SheetRow with `CircleDollarSign` and label "Hinta" renders below the dot indicators
    - No sport badge span (`backgroundColor: lajiKonfig`) remains in PaikkaSheet
    - The outer motion.div still has `layoutId={`vc-${paikka.id}`}`, `overflow: 'hidden'`, and `drag="y"`
    - The drag-handle div still uses `pt-3 pb-1`; scrollRef still uses `height: 'calc(100% - 32px)'`
    - `distanceKm` is still displayed somewhere (overlay address line) when provided
    - `npx tsc --noEmit` exits 0 (no unused-import errors — drop MapPin/lajiKonfig if now unused)
  </acceptance_criteria>
  <done>Hero carousel with floating controls, gradient overlay, dot indicators, and a pricing SheetRow render below it; sport badge removed; critical animation/height constraints preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Collapsible review widget (SHEET-03)</name>
  <files>app/components/PaikkaSheet.tsx</files>
  <read_first>
    - app/components/PaikkaSheet.tsx (current ReviewSection block lines 181–189; avgRating computation line 46; SheetRow helper lines 197–209)
    - .planning/phases/29-kortit-sheet-redesign/29-PATTERNS.md (collapsible review widget before/after — exact collapsed header + AnimatePresence markup)
    - .planning/phases/29-kortit-sheet-redesign/29-RESEARCH.md (Pattern 4 height:auto, Pitfalls 3 & 6)
    - CLAUDE.md (animation: avoid height:auto generally — but D-14 mandates it here; duration 0.25, ease [0.25,0.1,0.25,1]; label caps pattern)
  </read_first>
  <action>
    Ensure `reviewOpen` state and `ChevronDown` import exist (added in Task 1; if not, add them). Replace the existing ReviewSection block (lines 181–189) with a collapsible widget, keeping the entire block wrapped in `{reviews !== null && (...)}` to match the existing async-load guard (Pitfall 6).

    Collapsed header: a div `flex items-center gap-3 border-t border-[rgba(0,0,0,0.07)] pt-4` with `cursor-pointer` and `onClick={() => reviews.length > 0 && setReviewOpen(prev => !prev)}` (D-13: no toggle when 0 reviews). Left icon slot: `w-8 h-8 rounded-lg glass flex items-center justify-center shrink-0 text-[rgba(17,17,17,0.5)]` containing a `★` text span (matches SheetRow icon-slot styling; Star is not imported — use the unicode char). Middle: a `flex-1 min-w-0` div with the label-caps `<p className="text-[10px] font-bold text-[rgba(17,17,17,0.4)] uppercase tracking-widest mb-1">Arvostelut</p>` and then: when `reviews.length === 0`, a static `<span className="text-sm text-[rgba(17,17,17,0.45)]">☆ Ei arvosteluja</span>` (D-13); else a `<span className="text-sm text-[#111111]">` showing filled/empty stars via `'★'.repeat(Math.round(avgRating ?? 0))` + `'☆'.repeat(5 - Math.round(avgRating ?? 0))`, then `(avgRating ?? 0).toFixed(1)` + `· {reviews.length} arvostelua` (D-12). Right: only when `reviews.length > 0`, a `<ChevronDown className={cn('w-4 h-4 text-[rgba(17,17,17,0.4)] transition-transform duration-200', reviewOpen && 'rotate-180')} />`.

    Expandable content: `<AnimatePresence initial={false}>` wrapping `{reviewOpen && (<motion.div key="reviews" initial={{ height: 0, opacity: 0 }} animate={{ height: 'auto', opacity: 1 }} exit={{ height: 0, opacity: 0 }} transition={{ duration: 0.25, ease: [0.25, 0.1, 0.25, 1] }} style={{ overflow: 'hidden' }}> ... </motion.div>)}`. The `style={{ overflow: 'hidden' }}` is REQUIRED (Pitfall 3) so content is clipped during the height animation. `initial={false}` prevents an entry animation on first mount. Inside the motion.div, render the existing `<ReviewSection paikkaId={paikka.id} initialReviews={reviews} avgRating={avgRating} reviewCount={reviews.length} />` unchanged. Do NOT edit ReviewSection.tsx (D-15: the collapsible lives in PaikkaSheet only).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The collapsible block is wrapped in `{reviews !== null && (...)}`
    - The collapsed header onClick is `() => reviews.length > 0 && setReviewOpen(prev => !prev)` (no toggle at 0 reviews)
    - When `reviews.length === 0`, the header shows `☆ Ei arvosteluja` and renders no ChevronDown
    - When `reviews.length > 0`, the header shows the star string + `(avgRating ?? 0).toFixed(1)` + `arvostelua` and a ChevronDown that gets `rotate-180` when reviewOpen
    - Expandable content uses `<AnimatePresence initial={false}>` and a motion.div with `animate={{ height: 'auto', opacity: 1 }}`, `transition` duration `0.25` ease `[0.25, 0.1, 0.25, 1]`, and `style={{ overflow: 'hidden' }}`
    - ReviewSection is rendered unchanged inside the motion.div; ReviewSection.tsx is NOT modified
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Reviews are collapsed by default, expand/collapse smoothly via height:auto, show a non-tappable static row at 0 reviews, and ReviewSection is untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full PaikkaSheet redesign: 16:9 hero carousel (3 gray Camera slides), floating drag handle + close/bookmark over the image, name+address gradient overlay, dot indicators, pricing SheetRow below the hero, and a collapsible review widget (collapsed by default). Sport badge removed.</what-built>
  <how-to-verify>
    1. Run `npm run dev`, open the app, tap a venue card to open PaikkaSheet.
    2. Hero: confirm a 16:9 area at the top with a gray slide + centered camera icon; the drag-handle bar floats centered over the top of the image; close (X) and bookmark chips float top-right over the image.
    3. Swipe the hero left/right: slides change with snap, the 3 dots below update to track the active slide, and the SHEET DOES NOT CLOSE on horizontal swipe (Pitfall 4 check).
    4. Overlay: venue name (white, bold) and address (white/70) appear over the bottom of the image with a dark gradient; distance is shown when available.
    5. Below the dots: a "Hinta" SheetRow (dollar icon) shows pricing. No sport-color badge pill appears anywhere in the sheet.
    6. Reviews: scroll down — the review widget is COLLAPSED by default showing stars + "X.X · N arvostelua" + a chevron. Tap it: it expands smoothly (no jank, content clipped during animation), chevron rotates. Tap again: collapses.
    7. Open a venue with 0 reviews: confirm "☆ Ei arvosteluja" shows with NO chevron and tapping does nothing.
    8. Drag the sheet down by the handle: it still closes (drag-to-close intact).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. swipe closes sheet, reviews visible before tap, height jank, distance missing)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. PaikkaSheet renders already-trusted venue data and an existing reviews query (unchanged). No new input handling.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05 | Tampering | npm installs | mitigate | No packages installed; Camera/ChevronDown/AnimatePresence already present (RESEARCH.md verified) |
| T-29-06 | Denial of service | drag="y" vs horizontal carousel swipe gesture conflict | mitigate | `onPointerDown={e => e.stopPropagation()}` on carousel container (Pitfall 4); human checkpoint verifies sheet stays open on swipe |
| T-29-07 | Tampering | height:auto animation paint overflow | mitigate | `style={{ overflow: 'hidden' }}` on the animating motion.div (Pitfall 3) |
</threat_model>

<verification>
- `npx tsc --noEmit` → 0 errors
- Human checkpoint confirms hero carousel, floating controls, gradient overlay, dot tracking, no swipe-close, pricing row, collapsed-by-default reviews with smooth expand and 0-review static state
</verification>

<success_criteria>
- Hero carousel with 3 Camera placeholder slides, floating drag handle + close/bookmark, gradient name/address overlay, and slide-tracking dots (SHEET-01)
- Pricing SheetRow directly below the hero (SHEET-02)
- Review widget collapsed by default, smooth height:auto expand, static no-chevron row at 0 reviews (SHEET-03)
- Sport badge removed (D-05); layoutId / overflow:hidden / drag="y" / height calc all preserved
- tsc clean
</success_criteria>

<output>
Create `.planning/phases/29-kortit-sheet-redesign/29-04-SUMMARY.md` when done
</output>
