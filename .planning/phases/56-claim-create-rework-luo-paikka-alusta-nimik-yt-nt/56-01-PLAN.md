---
phase: 56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/normalizeNimi.ts
  - app/api/business/create-paikka/route.ts
  - app/api/business/claim-paikka/route.ts
  - lib/normalizeNimi.test.ts
autonomous: true
requirements: [CLAIM-04, CLAIM-05]

must_haves:
  truths:
    - "normalizeNimi trims, collapses internal whitespace, and caps at 200 chars without changing casing (D-01, D-02)"
    - "create-paikka accepts yritysNimi + toimipisteNimi, writes combined nimi to liikuntapaikat, and writes yritysNimi to business_accounts.company_name (D-05, D-06, D-07)"
    - "When toimipisteNimi is empty, nimi equals yritysNimi with no trailing space (D-08)"
    - "A second business creating against an already-linked paikka_id receives HTTP 409 via the 23505 branch (D-11)"
    - "The claim-paikka route file no longer exists (D-10)"
    - "No new DB column or migration was created — company_name and nimi already exist (D-09)"
    - "create-paikka performs no duplicate-venue detection (no name/address similarity check) — accepted trade-off, deferred to a future access-request phase, not bolted on here (D-13)"
  artifacts:
    - path: "lib/normalizeNimi.ts"
      provides: "Shared name-normalization helper normalizeNimi(value: string): string"
      exports: ["normalizeNimi"]
      contains: "export function normalizeNimi"
    - path: "lib/normalizeNimi.test.ts"
      provides: "Unit tests for normalizeNimi (trim, collapse, cap, casing-preserve, empty)"
    - path: "app/api/business/create-paikka/route.ts"
      provides: "Create-only venue route with two name fields + company_name write + 23505→409"
      contains: "normalizeNimi"
  key_links:
    - from: "app/api/business/create-paikka/route.ts"
      to: "lib/normalizeNimi.ts"
      via: "import { normalizeNimi } and call on yritysNimi/toimipisteNimi before DB write"
      pattern: "normalizeNimi\\("
    - from: "app/api/business/create-paikka/route.ts"
      to: "business_accounts.company_name"
      via: "supabaseAdmin.from('business_accounts').update({ company_name }) keyed on verified user.id"
      pattern: "company_name"
---

<objective>
Backend half of the claim/create rework. Build the first shared name-normalization helper (`lib/normalizeNimi.ts`), reshape `create-paikka/route.ts` to accept two distinct name fields (`yritysNimi`, `toimipisteNimi`), compute the combined `liikuntapaikat.nimi`, write `yritysNimi` to `business_accounts.company_name`, add the missing `23505 → 409` conflict branch, and delete the now-orphaned `claim-paikka/route.ts`.

Purpose: CLAIM-04 removes the claim path entirely (the claim route is dead once search is gone); CLAIM-05 introduces the separate company/branch name fields with consistent normalization. This plan owns every backend/server-side change so the frontend plan (56-02) can consume a stable new request contract.
Output: New helper + tests, reworked create route, deleted claim route. Zero new DB columns, zero migrations (D-09).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-CONTEXT.md
@.planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/normalizeNimi.ts shared helper + unit tests</name>
  <files>lib/normalizeNimi.ts, lib/normalizeNimi.test.ts</files>
  <read_first>
    - lib/sanitizeKotikaupunki.ts (the structural analog: JSDoc header + single exported pure string helper)
    - .planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-PATTERNS.md (section "lib/normalizeNimi.ts" — exact signature and divergence from the analog)
    - .planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-CONTEXT.md (D-01, D-02, D-03, D-04)
  </read_first>
  <behavior>
    - normalizeNimi("  FitLife   Oy  ") returns "FitLife Oy" (trim + collapse internal whitespace via /\s+/g)
    - normalizeNimi("CrossFit") returns "CrossFit" (casing preserved exactly — no toLowerCase/toUpperCase, D-01)
    - normalizeNimi("") returns "" and normalizeNimi(null as never) returns "" (falsy guard, returns plain string not undefined — caller tests truthiness)
    - normalizeNimi("a".repeat(250)) returns a 200-char string (cap at 200, D-02)
    - A leading/trailing newline or tab is treated as whitespace and trimmed/collapsed
  </behavior>
  <action>
    Create `lib/normalizeNimi.ts` exporting a single pure function `normalizeNimi(value: string): string`. Mirror the file shape of `lib/sanitizeKotikaupunki.ts`: JSDoc header block, single guard, single transform chain, return. Divergence from the analog per D-01/D-02: NO character allowlist regex (preserve all characters and casing), cap at 200 chars (not 80), and return empty string for falsy/blank input (NOT undefined) — the create-paikka required-field check does `if (!yritysNimi)`, so a plain string is needed. Transform chain: guard `if (!value) return ''`, then `value.trim()`, collapse internal whitespace with `.replace(/\s+/g, ' ')`, then `.slice(0, 200)`. The JSDoc must state forward-only intent (D-04: apply only to new writes, never backfill) and that casing is preserved. Do NOT add a casing transformation. Then create `lib/normalizeNimi.test.ts` (Vitest, mirroring existing `lib/*.test.ts` style — describe/it/expect) covering all five behaviors above.
  </action>
  <verify>
    <automated>npx vitest run lib/normalizeNimi.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/normalizeNimi.test.ts` passes with all listed behavior cases.
    - Source assertion: `lib/normalizeNimi.ts` contains `export function normalizeNimi` and `.replace(/\s+/g, ' ')` and `.slice(0, 200)` and contains neither `toLowerCase` nor `toUpperCase`.
    - normalizeNimi returns `string` (never undefined) for all inputs including empty/null.
  </acceptance_criteria>
  <done>Helper exists with the exact signature, casing is preserved, 200-char cap enforced, empty-string-on-blank behavior, and the test file is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rework create-paikka route — two name fields, company_name write, 23505→409</name>
  <files>app/api/business/create-paikka/route.ts</files>
  <read_first>
    - app/api/business/create-paikka/route.ts (the file being modified — current single-nimi body shape, JWT block, liikuntapaikat insert, link insert + rollback, is_claimed update, admin email)
    - app/api/business/claim-paikka/route.ts (lines 50-54 — the 23505→409 pattern to copy)
    - app/api/business/register/route.ts (company_name trim/slice precedent on the same column)
    - lib/normalizeNimi.ts (the helper created in Task 1 — import and call it)
    - .planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-PATTERNS.md (section "create-paikka/route.ts" — body-parse, combine, company_name UPDATE, 23505 branch)
    - .planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-CONTEXT.md (D-05, D-06, D-07, D-08, D-09, D-11)
  </read_first>
  <behavior>
    - POST with {yritysNimi:"FitLife Oy", toimipisteNimi:"Keskusta", osoite, kaupunki, latitude, longitude} inserts liikuntapaikat with nimi="FitLife Oy Keskusta" (combined, space-separated, D-07)
    - POST with toimipisteNimi empty/omitted inserts nimi="FitLife Oy" with no trailing space (D-08)
    - POST missing yritysNimi (after normalization) returns 400 Missing fields
    - After a successful create, business_accounts.company_name for the verified user equals the normalized yritysNimi (D-05)
    - When the business_paikka_links insert fails with Postgres code 23505, the orphaned liikuntapaikat row is deleted and the response is 409 (D-11)
    - latitude/longitude allowlist parse and the published:false / laji:'Muu' insert shape are unchanged from the current route
  </behavior>
  <action>
    Modify `app/api/business/create-paikka/route.ts`. Keep the JWT-verification block (Authorization → supabaseAdmin.auth.getUser(token) → 401) and the business_accounts existence check (→ 403) verbatim. Import `normalizeNimi` from `@/lib/normalizeNimi`. Replace the single `nimi` body field: read `body.yritysNimi` and `body.toimipisteNimi` as strings, run each through `normalizeNimi(...)` (not inline trim/slice). Keep `osoite`/`kaupunki` on their existing inline `.trim().slice(0, 500)` convention (only name fields route through the helper per PATTERNS "Inline trim+slice → shared helper migration"). Keep the latitude/longitude allowlist parse unchanged. Change the required-field guard to `if (!yritysNimi || !osoite || !kaupunki || latitude === null || longitude === null)` → 400 (toimipisteNimi is optional, D-08). Compute combined name per D-06/D-07/D-08: `const nimi = toimipisteNimi ? yritysNimi + ' ' + toimipisteNimi : yritysNimi`. Use that `nimi` in the unchanged `liikuntapaikat` insert (keep `laji: 'Muu'`, `published: false`). In the `business_paikka_links` link-insert error branch, ADD the missing conflict mapping BEFORE the generic 500: if `linkError.code === '23505'`, delete the orphaned liikuntapaikat row (preserve the existing rollback) and return 409 with `{ error: 'Already claimed' }` — mirroring claim-paikka lines 50-54 plus the existing create-paikka rollback. Keep the generic-error rollback + 500 for all other link errors. ADD a `business_accounts.company_name` UPDATE (D-05) keyed on the verified `user.id` (never body): `supabaseAdmin.from('business_accounts').update({ company_name: yritysNimi }).eq('user_id', user.id)`. Treat its failure as non-critical (console.error + continue), consistent with the existing is_claimed/email log-don't-rollback pattern. Leave the is_claimed UPDATE and admin-email block intact. Do NOT add any new DB column or migration (D-09).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: route imports `normalizeNimi` from `@/lib/normalizeNimi` and calls it on the two name fields.
    - Source assertion: route body no longer reads a top-level `body.nimi`; it reads `body.yritysNimi` and `body.toimipisteNimi`.
    - Source assertion: the combined-name expression produces `yritysNimi` alone when `toimipisteNimi` is falsy (no trailing space).
    - Source assertion: the link-insert error branch contains a `linkError.code === '23505'` check returning status 409, and still deletes the orphaned liikuntapaikat row on conflict.
    - Source assertion: route contains a `business_accounts` `.update({ company_name` keyed on `.eq('user_id', user.id)`.
    - `npx tsc --noEmit` reports no new type errors for this file.
  </acceptance_criteria>
  <done>create-paikka accepts the two-name body, writes the combined nimi + company_name, returns 409 on 23505 with rollback, and type-checks clean. No migration added.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the orphaned claim-paikka route</name>
  <files>app/api/business/claim-paikka/route.ts</files>
  <read_first>
    - app/api/business/claim-paikka/route.ts (confirm this is the search-to-claim write path being removed)
    - .planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-CONTEXT.md (D-10, D-12 — reapply route is unaffected and must NOT be deleted)
  </read_first>
  <action>
    Delete the file `app/api/business/claim-paikka/route.ts` entirely (D-10 — claim/search is removed; this route has no remaining caller once 56-02 strips the claim path from the component). Before deleting, grep the repo for any remaining importer or fetch reference to `/api/business/claim-paikka` and confirm the only caller is `ClaimSearchForm.tsx`'s `handleClaim` (which 56-02 removes). Do NOT touch `app/api/business/reapply/route.ts` — it is a separate concern and stays (D-12). Do NOT delete the create-paikka route. If the empty `app/api/business/claim-paikka/` directory remains after file deletion, remove the directory too.
  </action>
  <verify>
    <automated>test ! -f app/api/business/claim-paikka/route.ts && echo DELETED</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/business/claim-paikka/route.ts` does not exist.
    - `app/api/business/reapply/route.ts` still exists (D-12 unaffected).
    - Repo grep for the string `api/business/claim-paikka` returns only matches inside `.planning/` docs (no live `app/` or component code references) — note 56-02 removes the ClaimSearchForm reference; if executed before 56-02 the only live reference is in ClaimSearchForm.tsx and that is expected.
  </acceptance_criteria>
  <done>claim-paikka route file (and empty dir) removed; reapply and create-paikka untouched.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

New files:
- `lib/normalizeNimi.ts` — exports `normalizeNimi(value: string): string`
- `lib/normalizeNimi.test.ts` — Vitest unit tests for the helper

Modified files:
- `app/api/business/create-paikka/route.ts` — new request body fields `yritysNimi` (required) and `toimipisteNimi` (optional); writes combined `liikuntapaikat.nimi`; writes `business_accounts.company_name`; adds `23505 → 409` branch

Deleted files:
- `app/api/business/claim-paikka/route.ts` (and its now-empty directory)

New symbols:
- Function `normalizeNimi(value: string): string`
- Request contract change: `create-paikka` POST body `{ yritysNimi, toimipisteNimi?, osoite, kaupunki, latitude, longitude }`

No new DB columns, no migration (D-09 — `company_name` and `nimi` already exist).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/business/create-paikka | Untrusted JSON body (name/address/coords) crosses into a service-role-key write path that bypasses RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Spoofing / Elevation | create-paikka identity | mitigate | Identity for both the liikuntapaikat link and the company_name UPDATE comes from `supabaseAdmin.auth.getUser(token)` verified `user.id`, never from the request body (existing JWT block preserved). |
| T-56-02 | Tampering / DoS | name field length | mitigate | `normalizeNimi` caps both name fields at 200 chars; osoite/kaupunki keep the existing 500-char slice. Oversized inserts rejected pre-write. |
| T-56-03 | Tampering | UNIQUE(paikka_id) constraint | mitigate | Constraint left intact (D-11); 23505 violation maps to 409 with orphan-row rollback. Constraint must NOT be loosened (PITFALLS Pitfall 7). |
| T-56-04 | Information disclosure | Places raw data | accept/mitigate | Only allowlisted lat/lng parsed (existing SIJAINTI-03 guard); no place_id or raw Places fields read from body. Unchanged by this plan. |
</threat_model>

<verification>
- `npx vitest run lib/normalizeNimi.test.ts` passes.
- `npx tsc --noEmit` shows no new errors in `create-paikka/route.ts`.
- `app/api/business/claim-paikka/route.ts` is gone; `reapply/route.ts` remains.
- No new files under `supabase/migrations/` were created by this plan (D-09 structural no-op).
</verification>

<success_criteria>
- normalizeNimi normalizes per D-01/D-02 and preserves casing.
- create-paikka accepts and persists the two name fields per D-05/D-06/D-07/D-08 and returns 409 on conflict per D-11.
- claim-paikka route deleted per D-10; reapply untouched per D-12.
- Zero new columns/migrations per D-09.
</success_criteria>

<output>
Create `.planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-01-SUMMARY.md` when done.
</output>
