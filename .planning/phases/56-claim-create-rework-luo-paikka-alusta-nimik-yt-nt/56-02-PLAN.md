---
phase: 56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - app/components/ClaimSearchForm.tsx
  - messages/fi.json
  - messages/en.json
autonomous: false
requirements: [CLAIM-04, CLAIM-05]

must_haves:
  truths:
    - "The venue form has no search field and no claim step — it is create-only (CLAIM-04, D-10)"
    - "The form has two separate name inputs: Yrityksen nimi (required) and Toimipisteen nimi (optional, with helper text) (CLAIM-05, D-08)"
    - "Submitting POSTs { yritysNimi, toimipisteNimi, osoite, kaupunki, latitude, longitude } to /api/business/create-paikka (matches 56-01 contract)"
    - "A 409 response surfaces the reworded support-pointer error copy (D-11, UI-SPEC)"
    - "All claim/search-only i18n keys are removed from both fi.json and en.json (D-10, UI-SPEC)"
    - "The page heading reads Luo paikka / Create a venue (search framing dropped, UI-SPEC)"
  artifacts:
    - path: "app/components/ClaimSearchForm.tsx"
      provides: "Create-only venue form with two name fields, reusing SijaintiPicker"
      contains: "yritysNimi"
    - path: "messages/fi.json"
      provides: "Updated Business namespace: new name-field keys, reworded heading/409, deleted claim keys"
      contains: "yritysNimiLabel"
    - path: "messages/en.json"
      provides: "EN equivalents of the same Business-namespace changes"
      contains: "yritysNimiLabel"
  key_links:
    - from: "app/components/ClaimSearchForm.tsx"
      to: "app/api/business/create-paikka/route.ts"
      via: "fetch POST with { yritysNimi, toimipisteNimi, osoite, kaupunki, latitude, longitude }"
      pattern: "yritysNimi.*toimipisteNimi"
    - from: "app/components/ClaimSearchForm.tsx"
      to: "messages/fi.json"
      via: "useTranslations('Business') t('yritysNimiLabel') etc."
      pattern: "t\\('yritysNimi"
---

<objective>
Frontend half of the claim/create rework. Strip the `search` and `claim` steps from `ClaimSearchForm.tsx` so it is create-only (CLAIM-04), split the single name input into two fields `yritysNimi` (required) and `toimipisteNimi` (optional), submit the new body shape to the 56-01 create route, handle the 409 conflict with reworded copy, and update both i18n files — adding the new name-field keys, rewording the heading and the 409 message, and deleting every claim/search-only key.

Purpose: Delivers the user-visible CLAIM-04 (no search, always create) and CLAIM-05 (two name fields) behavior and finalizes the copywriting contract from the approved UI-SPEC.
Output: A create-only form wired to the new backend contract, plus a clean Business i18n namespace in both locales.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-CONTEXT.md
@.planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-PATTERNS.md
@.planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-UI-SPEC.md
@app/api/business/create-paikka/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ClaimSearchForm.tsx as a create-only two-name form</name>
  <files>app/components/ClaimSearchForm.tsx</files>
  <read_first>
    - app/components/ClaimSearchForm.tsx (the file being rewritten — current 3-step component; keep INPUT_CLASS/CTA_CLASS, SijaintiPicker usage, handleCreate skeleton, error AnimatePresence block)
    - app/business/page.tsx (lines ~290-346 — the two call sites: embedded dashboard branch line ~301 and standalone branch line ~342; both render <ClaimSearchForm /> with no props)
    - app/api/business/create-paikka/route.ts (the 56-01 contract this form must POST to — field names yritysNimi/toimipisteNimi/osoite/kaupunki/latitude/longitude)
    - .planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-PATTERNS.md (section "ClaimSearchForm.tsx" — exact state to keep/delete, handleCreate body shape, render block)
    - .planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-UI-SPEC.md (Copywriting Contract + Step Structure sections — field labels, helper text, optional suffix, deleted back button, transition rules)
    - .planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-CONTEXT.md (D-08 optional branch, D-10 deletions, D-11 409 handling)
    - app/components/SijaintiPicker.tsx (the reusable location picker — do NOT rebuild; reuse the existing onChange contract { lat, lng, address, city })
  </read_first>
  <action>
    Rewrite `app/components/ClaimSearchForm.tsx` to be create-only. DELETE the `search` and `claim` steps and everything exclusive to them: the `Step`/`SearchResult` types, the `step`/`query`/`kaupunki`/`results`/`selectedVenue` state, the debounce `useEffect` (search query), `handleClaim`, the `SELECT_CLASS` constant (only the deleted city dropdown used it), the createBrowserSupabase import if it becomes unused, and the back button (`backToSearch`) — per UI-SPEC the back button has no destination-step left; the embedded call site already has its own "← Takaisin" (`backToVenues`) wrapper. KEEP verbatim: `INPUT_CLASS`, `CTA_CLASS`, the `createBusinessBrowserClient` session/token fetch, the `SijaintiPicker` import and its `onChange` wiring into createLat/createLng/createOsoite/createKaupunki, and the error `AnimatePresence` block. Replace the single `createNimi` state with two: `const [yritysNimi, setYritysNimi] = useState('')` and `const [toimipisteNimi, setToimipisteNimi] = useState('')`. Render two text inputs above the SijaintiPicker, both using `INPUT_CLASS` inside the existing `flex flex-col gap-3` form: first input placeholder `t('yritysNimiPlaceholder')` / aria-label `t('yritysNimiLabel')` bound to yritysNimi; second input placeholder `t('toimipisteNimiPlaceholder')` / aria-label `t('toimipisteNimiLabel')` bound to toimipisteNimi, followed by a muted helper line `<p className="text-sm text-[rgba(17,17,17,0.45)]">{t('toimipisteNimiHelper')}</p>`. In `handleCreate`: change the required-field check from `createNimi` to `if (!yritysNimi.trim())` → `setError(t('errorNameRequired'))` (now company-name-specific copy); keep the osoite and lat/lng checks. Change the POST body to `{ yritysNimi: yritysNimi.trim(), toimipisteNimi: toimipisteNimi.trim(), osoite: createOsoite.trim(), kaupunki: createKaupunki, latitude: createLat, longitude: createLng }`. ADD 409 handling (currently absent in handleCreate — D-11): on `res.status === 409` set `t('errorClaimAlreadyTaken')` (reworded value), else on other non-ok set `t('errorCreateFailed')`. Keep the success redirect to `/business/onboarding?paikka_id=...`. For the in-form `<h2>` heading (`createTitle`): per UI-SPEC, drop the redundant inner `<h2>` since the page-level `<h1>{t('claimTitle')}</h1>` already sits directly above it (discretion granted) — remove it to avoid stacked duplicate headings. Per UI-SPEC Step Structure: if only one render branch remains, `AnimatePresence mode="wait"` for step-switching is no longer needed and may be removed; do NOT introduce any new transition — reuse only the existing `initial/animate/exit opacity` + existing durations (0.2 / 0.15) if any transition is kept (e.g. the error block keeps its `duration: 0.15` crossfade). Do NOT add props to the component — both call sites render `<ClaimSearchForm />` propless.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: ClaimSearchForm.tsx contains `yritysNimi` and `toimipisteNimi` state and inputs, and no longer contains `handleClaim`, `selectedVenue`, `setStep`, or `createNimi`.
    - Source assertion: the fetch body includes both `yritysNimi` and `toimipisteNimi` and posts to `/api/business/create-paikka`.
    - Source assertion: handleCreate branches on `res.status === 409` and calls `t('errorClaimAlreadyTaken')`.
    - Source assertion: no reference to `t('backToSearch')`, `t('searchNamePlaceholder')`, `t('createInstead')`, or `t('selectedVenueLabel')` remains.
    - Source assertion: no new framer-motion duration/easing value is introduced (only 0.2 and/or 0.15 appear).
    - `npx tsc --noEmit` reports no new type errors.
  </acceptance_criteria>
  <done>Create-only form with two name fields, helper text, SijaintiPicker reused, new POST body, 409 handling, no search/claim remnants, type-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update Business namespace in messages/fi.json and messages/en.json</name>
  <files>messages/fi.json, messages/en.json</files>
  <read_first>
    - messages/fi.json (Business namespace, lines ~98-145 — current claim/search/create key set)
    - messages/en.json (Business namespace, lines ~98-145 — EN equivalents)
    - .planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-UI-SPEC.md (Copywriting Contract table — exact FI/EN strings for every changed/new key)
    - .planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-PATTERNS.md (section messages/*.json — keys to delete/modify/add)
    - app/components/ClaimSearchForm.tsx (after Task 1 — confirm every t('...') key referenced exists in both locale files)
  </read_first>
  <action>
    Edit the `Business` namespace in BOTH `messages/fi.json` and `messages/en.json`, keeping the flat-key-under-Business convention (no nesting) and the existing key ordering style.
    ADD new keys (FI / EN):
      - `yritysNimiLabel`: "Yrityksen nimi" / "Company name"
      - `yritysNimiPlaceholder`: "esim. FitLife Oy" / "e.g. FitLife Oy"
      - `toimipisteNimiLabel`: "Toimipisteen nimi (valinnainen)" / "Branch name (optional)"
      - `toimipisteNimiPlaceholder`: "esim. Keskusta" / "e.g. Downtown"
      - `toimipisteNimiHelper`: "Täytä tämä jos yrityksellä on useita toimipisteitä." / "Fill this in if your company has multiple locations."
    MODIFY values (same key):
      - `claimTitle`: "Luo paikka" / "Create a venue" (was "Hae tai luo paikka" / "Find or create a venue")
      - `errorNameRequired`: "Yrityksen nimi on pakollinen." / "Company name is required." (was venue-name framing)
      - `errorClaimAlreadyTaken`: "Tämä paikka on jo rekisteröity. Ota yhteyttä tukeen, jos uskot tämän olevan virhe." / "This venue is already registered. Contact support if you believe this is a mistake."
    DELETE keys (both files) — all belong to the removed search/claim steps: `searchNamePlaceholder`, `searchAllCities`, `searchHelperText`, `createInstead`, `searchNoResults`, `searchMinChars`, `resultSelectCta`, `resultAlreadyClaimed`, `backToSearch`, `selectedVenueLabel`, `claimCta`, `claiming`, `errorClaimFailed`, `createNamePlaceholder`, `createAddressPlaceholder`, `createTitle`.
    KEEP unchanged: `createCta`, `creating`, `errorCreateFailed`, `errorAddressRequired`, `sijaintiLabel`, `sijaintiPakollinen`, and all non-claim Business keys (register*, dashboard*, onboarding*, etc.).
    Note: `createTitle` and `createNamePlaceholder`/`createAddressPlaceholder` are deleted because Task 1 removes the inner heading and the single-name/address text inputs (address comes from SijaintiPicker). If Task 1 keeps `createTitle` for the embedded-only context, retain it in both files instead of deleting — keep i18n and component in sync. Verify the final JSON is valid (no trailing commas, both files parse).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('messages/fi.json','utf8')); JSON.parse(require('fs').readFileSync('messages/en.json','utf8')); console.log('VALID')"</automated>
  </verify>
  <acceptance_criteria>
    - Both `messages/fi.json` and `messages/en.json` parse as valid JSON.
    - Source assertion: both files contain `yritysNimiLabel`, `yritysNimiPlaceholder`, `toimipisteNimiLabel`, `toimipisteNimiPlaceholder`, `toimipisteNimiHelper`.
    - Source assertion: `claimTitle` value is "Luo paikka" (fi) / "Create a venue" (en); `errorClaimAlreadyTaken` is the reworded support-pointer copy.
    - Source assertion: neither file contains `searchNamePlaceholder`, `claimCta`, `backToSearch`, `selectedVenueLabel`, or `resultAlreadyClaimed`.
    - Every `t('...')` key referenced in the rewritten ClaimSearchForm.tsx exists in both locale files (no missing-key gaps).
  </acceptance_criteria>
  <done>Both locale files have the new name-field keys, reworded heading + 409 copy, all claim/search keys removed, valid JSON, and full key parity with the component.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Create-only venue form: two name fields (Yrityksen nimi required, Toimipisteen nimi optional with helper), location via SijaintiPicker, submitting creates a venue and redirects to onboarding. Both FI and EN copy updated; search/claim UI removed.</what-built>
  <how-to-verify>
    1. Run the app (`npm run dev`) and sign in as a business account, go to `/business`.
    2. On the no-venues standalone view (or via "+ Lisää paikka" on the dashboard), confirm the heading reads "Luo paikka" (FI) and there is NO search box and NO "Luo uusi paikka sen sijaan" link.
    3. Confirm two name inputs appear: "Yrityksen nimi" (placeholder "esim. FitLife Oy") and "Toimipisteen nimi (valinnainen)" (placeholder "esim. Keskusta") with the helper line "Täytä tämä jos yrityksellä on useita toimipisteitä." below the second.
    4. Submit with only company name + a map location (leave branch empty) → venue is created and you land on `/business/onboarding?paikka_id=...`. In Supabase, the new `liikuntapaikat.nimi` equals the company name alone (no trailing space) and `business_accounts.company_name` is updated.
    5. Create another with both names filled → `liikuntapaikat.nimi` equals "Company Branch" (space-separated).
    6. Submit with company name blank → inline error "Yrityksen nimi on pakollinen." appears, no request sent.
    7. Switch language to EN (profiili language toggle) and confirm heading "Create a venue", labels "Company name" / "Branch name (optional)", helper "Fill this in if your company has multiple locations.".
    8. Confirm no console errors about missing i18n keys.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

Modified files:
- `app/components/ClaimSearchForm.tsx` — create-only form; new state `yritysNimi`, `toimipisteNimi`; new POST body shape; 409 handling; search/claim steps removed
- `messages/fi.json` — Business namespace: + `yritysNimiLabel`, `yritysNimiPlaceholder`, `toimipisteNimiLabel`, `toimipisteNimiPlaceholder`, `toimipisteNimiHelper`; reworded `claimTitle`, `errorNameRequired`, `errorClaimAlreadyTaken`; deleted all search/claim keys
- `messages/en.json` — same Business-namespace changes in English

New symbols:
- i18n keys: `yritysNimiLabel`, `yritysNimiPlaceholder`, `toimipisteNimiLabel`, `toimipisteNimiPlaceholder`, `toimipisteNimiHelper`
- Component state: `yritysNimi`, `toimipisteNimi`

Note: the component may be kept named `ClaimSearchForm` (rename is discretionary per CONTEXT.md; both call sites import it by that name — a rename would require updating `app/business/page.tsx` imports at lines ~301/~342).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → form state → create-paikka POST | Client-collected names/address/coords cross to the server route; server (56-01) re-validates and normalizes — client trim is convenience only, not a security control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-05 | Tampering | client-side name trim | accept | Client `.trim()` is cosmetic; the authoritative normalization + length cap happens server-side via `normalizeNimi` in 56-01. No trust placed in client values. |
| T-56-06 | Information disclosure | 409 copy | mitigate | Reworded 409 message is a generic support-pointer (no owner identity / no "someone else claimed it" detail), avoiding leaking that a specific venue/owner exists (D-11, UI-SPEC). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for ClaimSearchForm.tsx.
- Both messages JSON files parse and contain the new keys; deleted keys absent in both.
- Human checkpoint confirms the create-only flow, two-name behavior (combined vs company-only), 409/required-field copy, and EN locale.
</verification>

<success_criteria>
- Form is create-only with two name fields per CLAIM-04/CLAIM-05 and D-08/D-10.
- POST matches the 56-01 contract; 409 handled with reworded copy (D-11).
- i18n: new keys added, heading + 409 reworded, all claim/search keys deleted in both locales (UI-SPEC).
</success_criteria>

<output>
Create `.planning/phases/56-claim-create-rework-luo-paikka-alusta-nimik-yt-nt/56-02-SUMMARY.md` when done.
</output>
