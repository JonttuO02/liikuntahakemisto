---
phase: 49-esikatselu-ja-kontrastikorjaukset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/ContrastSafeLogo.tsx
  - app/business/onboarding/AnalysoiSivusto.tsx
autonomous: false
requirements: [PREV-03]
must_haves:
  truths:
    - "A white or transparent logo candidate is visibly framed (not invisible) against the picker's white card in AnalysoiSivusto's 'preview' phase"
    - "Each logo candidate renders inside a fixed rgba(0,0,0,0.06) backdrop box at the same h-12 footprint as before"
    - "The candidate button's selected-state ring, type label, and click-to-select behavior are unchanged"
  artifacts:
    - path: "app/components/ContrastSafeLogo.tsx"
      provides: "Shared contrast-safe logo display primitive (rgba(0,0,0,0.06) backdrop)"
      contains: "ContrastSafeLogo"
      min_lines: 7
    - path: "app/business/onboarding/AnalysoiSivusto.tsx"
      provides: "Logo-candidate picker using ContrastSafeLogo instead of a bare <img>"
      contains: "ContrastSafeLogo"
  key_links:
    - from: "app/business/onboarding/AnalysoiSivusto.tsx"
      to: "app/components/ContrastSafeLogo.tsx"
      via: "import + render inside logo-candidate .map() button"
      pattern: "ContrastSafeLogo src=\\{candidate.url\\}"
---

<objective>
Fix the only real logo-contrast bug in the app (PREV-03): in `AnalysoiSivusto.tsx`'s logo-candidate picker, each candidate's logo currently renders as a bare `<img>` sitting directly on the white `.glass` card, so white/transparent-background logos vanish. Introduce one shared presentational primitive, `ContrastSafeLogo`, that wraps the logo in a fixed `rgba(0,0,0,0.06)` mid-gray backdrop box (the exact tint already used by `DiagonaalKortti`'s logo box), and use it at the single bug call site.

Per D-03, this is the ONLY call site this phase touches. Per D-05, the requirement was narrowed (REQUIREMENTS.md PREV-03, ROADMAP Phase 49 criterion 2) to "AnalysoiSivusto's logo-candidate picker specifically", NOT "everywhere logo_url renders". `DiagonaalKortti.tsx` and `PaikkaSheet.tsx` already render logos against a non-white brandColor/translucent backdrop from Phase 48 and are explicitly NOT modified (D-03, deferred idea in CONTEXT.md).

Purpose: A business owner evaluating AI-found logo candidates can actually see a white or transparent-background logo instead of an apparently empty button.
Output: New `app/components/ContrastSafeLogo.tsx` primitive; `AnalysoiSivusto.tsx` picker wired to it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-CONTEXT.md
@.planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-UI-SPEC.md
@.planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-PATTERNS.md
@app/components/DiagonaalKortti.tsx
@app/business/onboarding/AnalysoiSivusto.tsx
</context>

## Artifacts this phase produces

New symbols created by this plan (downstream drift checks should treat these as in-phase, not pre-existing):

- `app/components/ContrastSafeLogo.tsx` — new file
- `ContrastSafeLogo` — new default-exported React component; props `{ src: string; alt?: string; className?: string }`

<tasks>

<task type="auto">
  <name>Task 1: Create the ContrastSafeLogo presentational primitive</name>
  <files>app/components/ContrastSafeLogo.tsx</files>
  <read_first>
    - .planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-UI-SPEC.md (Component Inventory item 1 — exact markup contract)
    - .planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-PATTERNS.md (`ContrastSafeLogo.tsx` section — target markup, why object-contain not object-cover, why no fallback icon)
    - app/components/DiagonaalKortti.tsx lines 94-107 — the existing `rgba(0,0,0,0.06)` logo-box backdrop pattern this primitive mirrors (read for tint + rounded-lg precedent; do NOT modify this file)
  </read_first>
  <action>
    Create a new file `app/components/ContrastSafeLogo.tsx`. Export a default React function component named `ContrastSafeLogo` with the prop signature `{ src: string; alt?: string; className?: string }` (per D-03/UI-SPEC: single-purpose, presentational, NOT branding-aware — it takes a plain URL string and has no knowledge of `BrandingResult`, `logo_candidates[].type`, or selection state).

    Render: an outer `<div>` with classes `w-full h-12 rounded-lg bg-[rgba(0,0,0,0.06)] flex items-center justify-center overflow-hidden` (append the optional `className` prop after a space if provided), containing an `<img>` with `src={src}`, `alt={alt ?? ''}`, and classes `h-full w-auto object-contain`. The `h-12` (48px) preserves the exact footprint of the `<img>` it replaces; `w-full` makes the backdrop read as a deliberate frame; `object-contain` (NOT `object-cover`) because this is a candidate being evaluated, not a confirmed brand logo cropped to a tile.

    Backdrop tint is exactly `rgba(0,0,0,0.06)` per D-04 — copied from `DiagonaalKortti`'s precedent for visual consistency. Do NOT invent a new tint, a checkerboard, or any transparency-aware pattern.

    Do NOT add a `Building2`/empty fallback (unlike DiagonaalKortti): this primitive is only ever called with a real candidate URL string. Do NOT add `'use client'` (purely presentational, no hooks/state). Do NOT add any animation, hover, or transition — the parent `<button>` owns all interactive state styling.

    Add an eslint-disable comment for `@next/next/no-img-element` directly above the `<img>` (the project uses raw `<img>` for these logos, mirroring the comment currently present at AnalysoiSivusto.tsx line 610).
  </action>
  <verify>
    <automated>cd "C:/ClaudeCodeTestit/liikuntahakemisto" && grep -q "rgba(0,0,0,0.06)" app/components/ContrastSafeLogo.tsx && grep -q "object-contain" app/components/ContrastSafeLogo.tsx && grep -q "export default function ContrastSafeLogo" app/components/ContrastSafeLogo.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `app/components/ContrastSafeLogo.tsx` exists and default-exports `ContrastSafeLogo`.
    - Source contains the literal backdrop class `bg-[rgba(0,0,0,0.06)]` and the literal `h-12` height.
    - The `<img>` uses `object-contain` (not `object-cover`) and `h-full w-auto`.
    - No `'use client'` directive, no `Building2`/fallback-icon import, no animation/transition classes.
    - `npx tsc --noEmit` reports no new type errors.
  </acceptance_criteria>
  <done>`ContrastSafeLogo` component file exists with the exact UI-SPEC markup contract and compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ContrastSafeLogo into the AnalysoiSivusto logo-candidate picker</name>
  <files>app/business/onboarding/AnalysoiSivusto.tsx</files>
  <read_first>
    - app/business/onboarding/AnalysoiSivusto.tsx lines 584-624 — the logo-candidate picker block; the bare `<img>` bug site is at lines 610-615, inside the `.map()` button
    - .planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-PATTERNS.md (`AnalysoiSivusto.tsx` section — exact before/after swap, import to add, comment to remove)
    - .planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-UI-SPEC.md (Component Inventory item 1 "Call site" — the only call site this phase)
    - app/components/ContrastSafeLogo.tsx — the component created in Task 1 (its prop signature)
  </read_first>
  <action>
    In `app/business/onboarding/AnalysoiSivusto.tsx`:

    1. Add the import `import ContrastSafeLogo from '@/app/components/ContrastSafeLogo'` alongside the existing component imports at the top of the file.

    2. At the logo-candidate picker bug site (currently lines 610-615, inside the `brandingResult.logo_candidates.map(candidate => ...)` button), replace the bare `<img src={candidate.url} alt="" className="h-12 w-auto object-contain rounded" />` element with `<ContrastSafeLogo src={candidate.url} />`. Also remove the now-unnecessary `{/* eslint-disable-next-line @next/next/no-img-element */}` comment that sits directly above that `<img>` (line 610) — the raw `<img>` now lives inside `ContrastSafeLogo`, so the disable comment belongs there, not here.

    Touch NOTHING else in this block: keep the `<button>` wrapper, its `key`, `type`, `onClick={() => selectLogo(candidate.url)}`, the `isSelected` ring/border conditional classes (`border-[#111111] ring-2 ring-[#111111]` vs `border-[rgba(0,0,0,0.12)]`), the `<span>` showing `candidate.type`, and the entire empty-state `( ... )` branch below. `ContrastSafeLogo` nests inside the existing button — it does not replace the button (D-03).
  </action>
  <verify>
    <automated>cd "C:/ClaudeCodeTestit/liikuntahakemisto" && grep -q "import ContrastSafeLogo from '@/app/components/ContrastSafeLogo'" app/business/onboarding/AnalysoiSivusto.tsx && grep -q "<ContrastSafeLogo src={candidate.url} />" app/business/onboarding/AnalysoiSivusto.tsx && ! grep -q 'className="h-12 w-auto object-contain rounded"' app/business/onboarding/AnalysoiSivusto.tsx && npx tsc --noEmit && npx next lint --file app/business/onboarding/AnalysoiSivusto.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `AnalysoiSivusto.tsx` imports `ContrastSafeLogo` from `@/app/components/ContrastSafeLogo`.
    - The picker renders `<ContrastSafeLogo src={candidate.url} />` inside the candidate `.map()` button.
    - The old bare `<img>` with `className="h-12 w-auto object-contain rounded"` no longer exists in this file.
    - The `selectLogo`/`isSelected`/`candidate.type` button logic and the empty-state branch are unchanged.
    - `npx tsc --noEmit` passes and lint reports no new errors for this file.
  </acceptance_criteria>
  <done>The logo-candidate picker renders each candidate through `ContrastSafeLogo`; selection behavior and empty-state are untouched; the file compiles and lints clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify contrast fix in the logo-candidate picker</name>
  <action>Manual verification only — no code change. Run the dev server, open AnalysoiSivusto's logo-candidate picker in the 'preview' phase, and confirm white/transparent-background logos are now framed against the rgba(0,0,0,0.06) backdrop and visible, while selection behavior, the type label, and the empty-state branch are unchanged. See how-to-verify below for steps.</action>
  <what-built>The logo-candidate picker in AnalysoiSivusto's 'preview' phase now wraps every candidate logo in a fixed mid-gray (rgba(0,0,0,0.06)) backdrop box via the new shared `ContrastSafeLogo` primitive, so white/transparent-background logos are visibly framed instead of disappearing into the white card.</what-built>
  <how-to-verify>
    1. Run the dev server: `npm run dev` (from `C:/ClaudeCodeTestit/liikuntahakemisto`).
    2. Sign in as a business account and open the onboarding wizard's website-analysis step (`AnalysoiSivusto`), or trigger the `'preview'` phase so the logo-candidate picker renders. If you do not have a live analysis result on hand, point the analyzer at a site whose logo is a white/transparent PNG (or temporarily inspect a candidate with a transparent logo).
    3. Confirm: each logo candidate now sits inside a light gray rounded box; a white or transparent-background logo is clearly visible against that gray, not invisible.
    4. Confirm the picker still works as before: clicking a candidate still shows the dark selection ring, the small type label under each logo is still there, and the "no logo candidates" empty-state copy (if you can trigger it) is unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" if white/transparent logos are now visible and selection still works, or describe what looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI-supplied `candidate.url` → `<img src>` | Logo candidate URLs originate from the scraper/analyzer pipeline (external website content) and are rendered as image sources in the browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Information Disclosure | `ContrastSafeLogo` `<img src={candidate.url}>` | accept | URL is rendered only as an `<img>` source (no script context); candidate URLs are already validated/stored by the Phase 47 SSRF-guarded pipeline before reaching this picker. This plan changes presentation only — it does not introduce a new data source or sink. |
| T-49-02 | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — `ContrastSafeLogo` uses only React + existing project imports; no new dependency added. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for the whole project.
- `grep` confirms `ContrastSafeLogo` exists, is imported into `AnalysoiSivusto.tsx`, and the old bare logo `<img>` (`className="h-12 w-auto object-contain rounded"`) is gone from that file.
- `DiagonaalKortti.tsx` and `PaikkaSheet.tsx` are byte-for-byte unchanged (out of scope, D-03): `git diff --name-only` must NOT list either file.
- Human checkpoint confirms white/transparent logos are visible and selection still works.
</verification>

<success_criteria>
- PREV-03 met: the logo-candidate picker uses a shared contrast-safe logo display primitive, so a white/transparent logo is visibly distinguishable against the picker's white card.
- The primitive (`ContrastSafeLogo`) is the only new component, used at exactly one call site (AnalysoiSivusto's picker).
- `DiagonaalKortti.tsx` / `PaikkaSheet.tsx` unchanged.
</success_criteria>

<output>
Create `.planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-01-SUMMARY.md` when done.
</output>
