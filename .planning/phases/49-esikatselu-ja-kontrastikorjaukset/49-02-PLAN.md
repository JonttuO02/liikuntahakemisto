---
phase: 49-esikatselu-ja-kontrastikorjaukset
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/business/onboarding/StepEsikatselu.tsx
  - messages/fi.json
  - messages/en.json
autonomous: false
requirements: [PREV-02]
must_haves:
  truths:
    - "Step 6 of the onboarding wizard shows the same CalloutCard venues see live on the map, not the unused PaikkaKortti"
    - "A draft venue with null coordinates still renders the Step 6 CalloutCard without breaking (dummy-coordinate type shim, no crash)"
    - "The swapped section's caption reads the new 'KARTTAKORTTI' / 'MAP CALLOUT' label, while the DiagonaalKortti and PaikkaSheet preview sections below are unchanged"
  artifacts:
    - path: "app/business/onboarding/StepEsikatselu.tsx"
      provides: "Step 6 preview first slot rendering CalloutCard instead of PaikkaKortti"
      contains: "CalloutCard"
    - path: "messages/fi.json"
      provides: "New previewLabelCallout FI caption"
      contains: "previewLabelCallout"
    - path: "messages/en.json"
      provides: "New previewLabelCallout EN caption"
      contains: "previewLabelCallout"
  key_links:
    - from: "app/business/onboarding/StepEsikatselu.tsx"
      to: "app/components/CalloutCard.tsx"
      via: "import + render with coordinate type shim"
      pattern: "CalloutCard p=\\{\\{ \\.\\.\\.draftAsPaikka"
    - from: "app/business/onboarding/StepEsikatselu.tsx"
      to: "messages/fi.json / messages/en.json"
      via: "t('previewLabelCallout')"
      pattern: "previewLabelCallout"
---

<objective>
Fix PREV-02: Step 6 of the onboarding wizard (`StepEsikatselu.tsx`) currently renders `PaikkaKortti` in its first preview slot — a component used nowhere in the live app — giving the business owner a preview that does not match what venues actually see. Swap it for `CalloutCard`, the component actually rendered on the live map (`Etusivu.tsx`, `app/business/map/page.tsx`). Relabel that section's caption and add a new i18n key for it.

Per D-01: swap ONLY the first preview slot; leave the `DiagonaalKortti` and `PaikkaSheet` sections below untouched.
Per D-02: `CalloutCard` requires non-null `latitude`/`longitude`, but `draftAsPaikka` (from `PaikkaBase`) can have `null` coordinates. The user confirmed this card is a purely static visual mockup in Step 6 — never placed on a real map — and `CalloutCard`'s render body never reads `p.latitude`/`p.longitude` (it only uses `nimi`, `laji`, `hinta_min/max`, `hinta_kuvaus`). So the fix is a pure type-satisfaction shim: pass real coordinates when present, otherwise `?? 0`. Do NOT build a "missing coordinates" placeholder/empty-state component (deferred idea in CONTEXT.md).

Per D-01: do NOT repoint the existing shared `previewLabelCard` key (still used by out-of-scope `PreviewModal.tsx`). Add a NEW `previewLabelCallout` key instead.

Purpose: What the business owner sees in the Step 6 preview matches what will actually be published on the map.
Output: Modified `StepEsikatselu.tsx`; new `previewLabelCallout` keys in `messages/fi.json` and `messages/en.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-CONTEXT.md
@.planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-UI-SPEC.md
@.planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-PATTERNS.md
@app/business/onboarding/StepEsikatselu.tsx
@app/components/CalloutCard.tsx
</context>

## Artifacts this phase produces

New symbols created by this plan (downstream drift checks should treat these as in-phase, not pre-existing):

- `previewLabelCallout` — new i18n key in the Business namespace of `messages/fi.json` (`"KARTTAKORTTI"`) and `messages/en.json` (`"MAP CALLOUT"`)

No new components or files — this plan swaps an existing import and adds two translation keys.

<tasks>

<task type="auto">
  <name>Task 1: Add previewLabelCallout i18n key to both message files</name>
  <files>messages/fi.json, messages/en.json</files>
  <read_first>
    - messages/fi.json line 181 — existing `"previewLabelCard": "LISTAKORTTI"` and its sibling keys `previewLabelDiag`/`previewLabelSheet` (lines 181-183) in the Business namespace
    - messages/en.json line 181 — existing `"previewLabelCard": "LIST CARD"` and siblings
    - .planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-PATTERNS.md (messages section — exact key name and copy)
    - .planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-UI-SPEC.md (Copywriting Contract — FI "KARTTAKORTTI", EN "MAP CALLOUT")
  </read_first>
  <action>
    Add a NEW translation key `previewLabelCallout` to both message files, inside the SAME object that already contains `previewLabelCard`/`previewLabelDiag`/`previewLabelSheet` (the Business namespace, around line 181-183). Do NOT create a new namespace, do NOT duplicate the object, and do NOT modify or repoint the existing `previewLabelCard` key (it stays — out-of-scope `PreviewModal.tsx` still uses it, D-01).

    - In `messages/fi.json`: add `"previewLabelCallout": "KARTTAKORTTI"` (all-caps, matching siblings `LISTAKORTTI`/`DIAGONAALIKORTTI`/`PROFIILISIVU`).
    - In `messages/en.json`: add `"previewLabelCallout": "MAP CALLOUT"`.

    Place the new key adjacent to the three existing `previewLabel*` keys. Ensure valid JSON (trailing-comma correctness) in both files.
  </action>
  <verify>
    <automated>cd "C:/ClaudeCodeTestit/liikuntahakemisto" && node -e "const fi=require('./messages/fi.json');const en=require('./messages/en.json');const f=JSON.stringify(fi);const e=JSON.stringify(en);if(!f.includes('\"previewLabelCallout\":\"KARTTAKORTTI\"'.replace(/\s/g,''))&&!/previewLabelCallout/.test(f))throw new Error('fi missing key');if(!/previewLabelCallout/.test(e))throw new Error('en missing key');if(!/previewLabelCard/.test(f)||!/previewLabelCard/.test(e))throw new Error('previewLabelCard was removed');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `messages/fi.json` parses as valid JSON and contains key `previewLabelCallout` with value `KARTTAKORTTI`.
    - `messages/en.json` parses as valid JSON and contains key `previewLabelCallout` with value `MAP CALLOUT`.
    - The pre-existing `previewLabelCard` key still exists in both files (not removed or repointed).
    - The new key sits in the same namespace/object as `previewLabelCard`/`previewLabelDiag`/`previewLabelSheet`.
  </acceptance_criteria>
  <done>Both message files have a valid new `previewLabelCallout` key (FI: KARTTAKORTTI, EN: MAP CALLOUT) and the existing `previewLabelCard` key is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Swap PaikkaKortti for CalloutCard in StepEsikatselu Step 6 first preview slot</name>
  <files>app/business/onboarding/StepEsikatselu.tsx</files>
  <read_first>
    - app/business/onboarding/StepEsikatselu.tsx — full file; line 10 is the `PaikkaKortti` import, lines 37-42 build `draftAsPaikka`, lines 118-124 are the first preview section (caption + `PaikkaKortti`), lines 126-140 are the untouched DiagonaalKortti/PaikkaSheet sections
    - app/components/CalloutCard.tsx lines 1-40 — confirms the prop signature `{ p: Liikuntapaikka & { latitude: number; longitude: number } }` and that the render body never reads `p.latitude`/`p.longitude`
    - .planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-PATTERNS.md (`StepEsikatselu.tsx` section — exact import swap, exact replacement markup, sibling-section confirmation)
    - .planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-UI-SPEC.md (Component Inventory item 2 — CalloutCard swap, caption relabel, static-only behavior)
  </read_first>
  <action>
    In `app/business/onboarding/StepEsikatselu.tsx`:

    1. Import swap: remove `import PaikkaKortti from '@/app/components/PaikkaKortti'` (line 10) and add `import CalloutCard from '@/app/components/CalloutCard'`. Before removing, confirm `PaikkaKortti` has no other usage in this file — grep already verified the only two references are the import (line 10) and the single call site (line 123); there are no others.

    2. First preview section (currently lines 118-124): replace `<PaikkaKortti paikka={draftAsPaikka} />` with `<CalloutCard p={{ ...draftAsPaikka, latitude: draftAsPaikka.latitude ?? 0, longitude: draftAsPaikka.longitude ?? 0 }} />`. The `?? 0` is a pure type-satisfaction shim per D-02 (CalloutCard never reads coordinates in render) — do NOT add any "missing coordinates" UI variant or placeholder component. Inline `?? 0` at the call site matches existing code conventions (the file has no constants section); per CONTEXT.md's discretion note inline is acceptable.

    3. Caption relabel: change that section's caption from `t('previewLabelCard')` to `t('previewLabelCallout')` (the key added in Task 1). Keep the caption `<span>`'s class string identical (`text-[10px] font-bold uppercase tracking-widest text-[rgba(17,17,17,0.45)]`) — only the translation key changes. Optionally update the section's leading comment from `{/* LISTAKORTTI */}` to `{/* KARTTAKORTTI */}` for accuracy.

    Touch NOTHING else: the `DiagonaalKortti` section (`<DiagonaalKortti paikka={draftAsPaikka} brandColor={brandColor} />`, ~line 131) and the `PaikkaSheet` section (`<PaikkaSheet paikka={draftAsPaikka} preview={true} ... />`, ~line 139) below stay exactly as they are (D-01). Do NOT wire any `onClick`, map state, or position to the CalloutCard — it is a static visual mockup here (D-02, specifics in CONTEXT.md).
  </action>
  <verify>
    <automated>cd "C:/ClaudeCodeTestit/liikuntahakemisto" && grep -q "import CalloutCard from '@/app/components/CalloutCard'" app/business/onboarding/StepEsikatselu.tsx && grep -q "previewLabelCallout" app/business/onboarding/StepEsikatselu.tsx && grep -q "latitude: draftAsPaikka.latitude ?? 0" app/business/onboarding/StepEsikatselu.tsx && ! grep -q "PaikkaKortti" app/business/onboarding/StepEsikatselu.tsx && grep -q "DiagonaalKortti" app/business/onboarding/StepEsikatselu.tsx && grep -q "PaikkaSheet" app/business/onboarding/StepEsikatselu.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `StepEsikatselu.tsx` imports `CalloutCard` and no longer imports or references `PaikkaKortti` anywhere.
    - The first preview slot renders `<CalloutCard p={{ ...draftAsPaikka, latitude: draftAsPaikka.latitude ?? 0, longitude: draftAsPaikka.longitude ?? 0 }} />`.
    - That section's caption uses `t('previewLabelCallout')`.
    - The `DiagonaalKortti` and `PaikkaSheet` preview sections are still present and unchanged (both still rendered with `draftAsPaikka`).
    - `npx tsc --noEmit` passes — the coordinate shim satisfies `CalloutCard`'s `latitude: number; longitude: number` requirement with no type error.
  </acceptance_criteria>
  <done>Step 6's first preview slot renders CalloutCard via the coordinate shim with the new KARTTAKORTTI/MAP CALLOUT caption; PaikkaKortti is fully removed; the two sibling preview sections are untouched; the project type-checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Step 6 CalloutCard swap</name>
  <action>Manual verification only — no code change. Run the dev server, step the onboarding wizard to Step 6, and confirm the first preview slot now renders the map-style CalloutCard with the KARTTAKORTTI/MAP CALLOUT caption, renders without crashing for a venue lacking coordinates, and leaves the DiagonaalKortti and PaikkaSheet sections unchanged. See how-to-verify below for steps.</action>
  <what-built>Step 6 of the onboarding wizard now shows the real `CalloutCard` (the card venues actually see on the live map) in its first preview slot instead of the unused `PaikkaKortti`, captioned "KARTTAKORTTI"/"MAP CALLOUT". A draft with no coordinates still renders it via a dummy-coordinate type shim (CalloutCard never reads coordinates in render). The DiagonaalKortti and PaikkaSheet preview sections below are unchanged.</what-built>
  <how-to-verify>
    1. Run the dev server: `npm run dev` (from `C:/ClaudeCodeTestit/liikuntahakemisto`).
    2. Sign in as a business account and step the onboarding wizard through to Step 6 (Esikatselu / preview).
    3. Confirm the first preview card is now the map-style CalloutCard (diagonal-clipped speech-bubble style with the sport icon and animated name/sport toggle), NOT the old flat list card — and its caption reads "KARTTAKORTTI" (FI) / "MAP CALLOUT" (EN, switch language to check).
    4. Confirm the card renders even for a venue without coordinates (e.g. a freshly created draft not yet geocoded) — it should NOT crash or show an empty/broken state.
    5. Confirm the two sections below it (DiagonaalKortti card and the PaikkaSheet venue-page preview) are unchanged and still render correctly.
  </how-to-verify>
  <resume-signal>Type "approved" if Step 6 shows the CalloutCard with the correct caption, renders without coordinates, and the sibling previews are unchanged — or describe what looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft/branding data → `CalloutCard` props in Step 6 | Draft venue fields (name, sport, price) sourced from the business owner's own onboarding draft are rendered into a static preview card. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-03 | Information Disclosure | Step 6 `CalloutCard` preview | accept | Renders only the business owner's own draft fields back to themselves; no cross-tenant data, no new data source. Presentation-only swap of an already-rendered preview. |
| T-49-04 | Denial of Service | `CalloutCard` null-coordinate render | mitigate | The `?? 0` shim guarantees non-null `latitude`/`longitude` are always passed, so the card cannot fail its type contract or break on an un-geocoded draft. |
| T-49-05 | Tampering | npm/pip/cargo installs | accept | No package installs — this plan swaps an existing import and adds two i18n strings; no new dependency. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for the whole project (the coordinate shim resolves the `number | null` vs `number` mismatch).
- `grep` confirms `CalloutCard` is imported and `PaikkaKortti` is fully gone from `StepEsikatselu.tsx`.
- `grep` confirms `previewLabelCallout` exists in both message files and `previewLabelCard` was NOT removed.
- `git diff --name-only` lists only `StepEsikatselu.tsx`, `messages/fi.json`, `messages/en.json` — `PreviewModal.tsx`, `DiagonaalKortti.tsx`, `PaikkaSheet.tsx`, `PaikkaKortti.tsx` are NOT modified.
- Human checkpoint confirms the CalloutCard renders in Step 6 (incl. no-coordinates case) with the new caption and the sibling previews are unchanged.
</verification>

<success_criteria>
- PREV-02 met: Step 6 renders `CalloutCard` (not `PaikkaKortti`), with a working fallback (dummy-coordinate shim) for venues missing coordinates.
- Only the first preview slot changed; DiagonaalKortti/PaikkaSheet sections untouched.
- New `previewLabelCallout` caption added without disturbing the shared `previewLabelCard` key.
</success_criteria>

<output>
Create `.planning/phases/49-esikatselu-ja-kontrastikorjaukset/49-02-SUMMARY.md` when done.
</output>
