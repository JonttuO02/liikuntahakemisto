---
phase: 50-flow-uudelleenj-rjestys
plan: 02
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - app/business/onboarding/page.tsx
  - app/business/WizardInner.tsx
  - app/business/onboarding/StepMediat.tsx
  - app/business/onboarding/StepHinnasto.tsx
  - app/business/onboarding/StepAukioloajat.tsx
  - app/business/onboarding/StepYhteystiedot.tsx
  - app/business/onboarding/AnalysoiSivusto.tsx
  - app/business/onboarding/ProgressBar.tsx
autonomous: false
requirements: [FLOW-01, FLOW-04]
user_setup: []
must_haves:
  truths:
    - "A new business owner sees StepPaikka (venue confirmation) BEFORE the AnalysoiSivusto website-analysis step"
    - "The wizard's numbered steps are StepMediat=1, StepHinnasto=2, StepAukioloajat=3, StepYhteystiedot=4, StepEsikatselu=5"
    - "The progress bar shows 5 steps (Media, Pricing, Hours, Contact, Preview) — no Place-name entry"
    - "Pre-phases (StepPaikka, AnalysoiSivusto) render with no progress bar"
    - "WizardInner EditMode (existing-venue editing) step numbering is unchanged"
    - "Skipping URL analysis lands the user on the wizard's new Step 1 (StepMediat)"
  artifacts:
    - path: "app/business/onboarding/page.tsx"
      provides: "3-phase page-level router: StepPaikka pre-phase -> AnalysoiSivusto pre-phase -> wizard (D-01)"
      contains: "StepPaikka"
    - path: "app/business/WizardInner.tsx"
      provides: "5-step OnboardingMode wizard (StepMediat=1 ... StepEsikatselu=5), StepPaikka removed (D-02)"
    - path: "app/business/onboarding/ProgressBar.tsx"
      provides: "5-entry stepLabels array (D-08)"
  key_links:
    - from: "app/business/onboarding/page.tsx"
      to: "StepPaikka"
      via: "new pre-phase render branch resolving paikka_id like PrePhase"
      pattern: "StepPaikka"
    - from: "app/business/WizardInner.tsx"
      to: "/api/business/onboarding/save-step"
      via: "saveAndAdvance with renumbered step arguments 1-4"
      pattern: "saveAndAdvance"
---

<objective>
Reorder the onboarding flow so StepPaikka (venue confirmation) renders as a page-level pre-phase
BEFORE the AnalysoiSivusto website-analysis step, and renumber the wizard from 6 positions down to
5 numbered steps (StepMediat=1 ... StepEsikatselu=5). This is the app-code half of Phase 50.

Purpose: Implement FLOW-01 (StepPaikka before URL analysis) and the app-side of FLOW-04 (every
hardcoded step literal and the progress bar shift to match the migrated current_step values from 50-01).
Output: page.tsx becomes a 3-phase router (paikka -> analyze -> wizard); WizardInner OnboardingMode
drops StepPaikka and renumbers steps 2-6 down to 1-5; all five callers' step literals and the progress
bar are updated; EditMode is untouched.

Depends on 50-01: the live DB now holds renumbered current_step values and save-step accepts input
step 0-5, so the renumbered literals this plan writes resolve to the correct resume steps.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-flow-uudelleenj-rjestys/50-CONTEXT.md
@.planning/phases/50-flow-uudelleenj-rjestys/50-PATTERNS.md
@.planning/phases/50-flow-uudelleenj-rjestys/50-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New symbols introduced by this plan (exclude from drift / source-grounding checks):

- New component `StepPaikkaPrePhase` (or equivalently-named wrapper) inside `app/business/onboarding/page.tsx` — a Suspense-boundary descendant that resolves `paikka_id` (URL param → `business_paikka_links` lookup, mirroring the existing `PrePhase` function) and renders `<StepPaikka>`. Exact name and whether it reuses an extracted paikka_id/paikkaInfo helper is Claude's Discretion per D-01.
- Extended `PagePhase` union type in `page.tsx`: from `'pre' | 'wizard'` to a 3-value union (e.g. `'paikka' | 'analyze' | 'wizard'`) per D-01.
- A new page-level transition from the paikka phase to the analyze phase (driven by StepPaikka's onNext).

All other changes are in-place numeric/positional edits to existing files (no new exported symbols).
StepPaikka.tsx itself is NOT modified — it is only relocated from being rendered inside WizardInner
to being rendered inside page.tsx.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Move StepPaikka into page.tsx as a pre-phase; extend PagePhase to 3 phases (D-01, D-03, D-04)</name>
  <read_first>
    - app/business/onboarding/page.tsx (FULL — `PagePhase` type line 10, `PrePhase` function lines 24-81 with paikka_id resolution, `OnboardingWizardPage` lines 83-162 with `handleConfirm` lines 88-129 including the `step: 1` literal at line 117, `handleSkip` lines 131-134, render branches lines 136-161)
    - app/business/onboarding/StepPaikka.tsx (FULL — read-only confirmation component; props are `paikkaInfo: {nimi, osoite, kaupunki} | null`, `paikkaId: number | null`, `onNext: () => void`; no side effects)
    - app/business/WizardInner.tsx (lines 63-144 — `loadDraft()`, specifically the `liikuntapaikat` select at lines 129-136 that fetches `nimi, osoite, kaupunki` for paikkaInfo; this is the fetch the new pre-phase needs to replicate or extract per D-01's Claude's Discretion sub-note)
    - .planning/phases/50-flow-uudelleenj-rjestys/50-CONTEXT.md (D-01 reorder architecture, D-03 onSkip unchanged, D-04 handleConfirm step:0)
    - .planning/phases/50-flow-uudelleenj-rjestys/50-PATTERNS.md (page.tsx section lines 27-108 — PrePhase resolution template, render gating pattern, D-04 handleConfirm exact change)
  </read_first>
  <action>
    Restructure `app/business/onboarding/page.tsx` into a 3-phase page-level sequence per D-01:
    StepPaikka (pre) → AnalysoiSivusto (pre) → wizard.

    1. Extend the `PagePhase` type (line 10) from `'pre' | 'wizard'` to a 3-value union. Use
       `'paikka' | 'analyze' | 'wizard'`. Initial state becomes `'paikka'` (not `'pre'`).

    2. Build a new `StepPaikkaPrePhase` wrapper component that mirrors the EXISTING `PrePhase`
       function's paikka_id resolution block EXACTLY (lines 39-74: read URL param `paikka_id` first,
       parse to int, fall back to a `business_paikka_links` lookup keyed on the authenticated
       `user.id` via `createBusinessBrowserClient`). Reuse this logic — do not re-derive it. It must
       call `useSearchParams()` (so it must live INSIDE a `<Suspense>` boundary, exactly like the
       existing `PrePhase`; `OnboardingWizardPage` itself must never call `useSearchParams()`).
       In addition to paikka_id, this wrapper needs `paikkaInfo` (nimi/osoite/kaupunki) for StepPaikka's
       display — fetch it from the `liikuntapaikat` table the same way `WizardInner.loadDraft()` does
       (select `nimi, osoite, kaupunki` where `id` = resolvedPaikkaId). Whether you extract a shared
       helper for this fetch or duplicate the small select is Claude's Discretion (D-01). It renders
       `<StepPaikka paikkaInfo={...} paikkaId={...} onNext={...} />`; `onNext` advances the page phase
       from `'paikka'` to `'analyze'`. It must also surface the resolved paikka_id up to
       `OnboardingWizardPage`'s `paikkaId` state (reuse the existing `onPaikkaIdResolved={setPaikkaId}`
       callback pattern) so `handleConfirm`'s save-step write has the id.

    3. Rename/repoint the existing `'pre'` render branch (lines 139-147) to `pagePhase === 'analyze'`
       — it keeps rendering `<PrePhase>` (the AnalysoiSivusto wrapper) UNCHANGED. Add a new
       `pagePhase === 'paikka' && (...)` branch ABOVE it that renders the new `StepPaikkaPrePhase`
       inside a `<Suspense fallback={<PreVaiheSpinner />}>`, following the same Suspense-wrapping
       convention. The `pagePhase === 'wizard'` branch (lines 148-158) is unchanged.

    4. D-03: `AnalysoiSivusto`'s `onSkip` behavior is unchanged — `handleSkip` (lines 131-134) still
       does `setBrandingData(null); setPagePhase('wizard')`, landing the user on the wizard's new
       Step 1 (StepMediat). There is NO "go back to StepPaikka" path. Do not add one.

    5. D-04: in `handleConfirm` (lines 112-120), change the `step: 1` literal (line 117) to `step: 0`
       (so save-step sets `current_step = 0 + 1 = 1`, landing the resumed wizard on new Step 1/StepMediat).
       Update the inline comment block (lines 114-116) to reference "Step 1 (StepMediat)" and the new
       numbering instead of the old "Step 2". (50-01 already widened the save-step lower bound to accept
       step 0, so this write will succeed rather than silently 400.)
  </action>
  <verify>
    <automated>cd "C:/ClaudeCodeTestit/liikuntahakemisto" && npx tsc --noEmit 2>&1 | grep -c "page.tsx" | grep -qx 0 && grep -c "StepPaikka" app/business/onboarding/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` imports and renders `StepPaikka` (grep for `StepPaikka` returns >= 1)
    - `PagePhase` type has 3 string members; initial `pagePhase` state value is the paikka phase
    - A render branch for the paikka phase appears ABOVE the analyze (former 'pre') branch
    - `handleConfirm` sends `step: 0` (no `step: 1` literal remains in the handleConfirm fetch body)
    - `handleSkip` still calls `setPagePhase('wizard')` with no StepPaikka return path (D-03)
    - `npx tsc --noEmit` reports zero errors in page.tsx
  </acceptance_criteria>
  <done>page.tsx renders StepPaikka as the first pre-phase, then AnalysoiSivusto, then the wizard; handleConfirm uses step:0; types pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove StepPaikka from WizardInner and renumber the 5 wizard steps (D-02, D-05)</name>
  <read_first>
    - app/business/WizardInner.tsx (FULL — `OnboardingMode` lines 35-295: step bounds `rawStep`/`step` lines 49-50 with `rawStep > 6`; draft resume guard line 121 `savedStep > 1 && step === 1`; forward-skip guard lines 170-175; preview re-fetch trigger line 179 `if (step !== 6)`; per-step render block lines 239-289 covering `step === 1` StepPaikka through `step === 6` StepEsikatselu. EditMode lines 297-438 — DO NOT MODIFY, includes tabs `[1,2,3,4,5]` line 358 and `router.push('/business/' + paikkaId + '?step=N')` calls)
    - app/business/onboarding/StepMediat.tsx (confirm whether its `onPrev` prop is optional before removing it for the new first step)
    - .planning/phases/50-flow-uudelleenj-rjestys/50-CONTEXT.md (D-02 — 5 numbered steps, all literals shift down by 1; D-05 note — EditMode untouched)
    - .planning/phases/50-flow-uudelleenj-rjestys/50-PATTERNS.md (WizardInner section lines 112-168 — exact before/after of bounds, render block, resume guard, preview trigger)
  </read_first>
  <action>
    Renumber `OnboardingMode` in `app/business/WizardInner.tsx` so the wizard runs 5 steps with
    StepPaikka removed (D-02). EditMode (lines 297-438) is explicitly OUT OF SCOPE per D-05 — do not
    touch its tabs, step numbers, or router.push calls.

    1. Step upper bound (line 50): change `rawStep > 6` to `rawStep > 5`.

    2. Per-step render block (lines 239-289):
       - DELETE the entire `{step === 1 && (<StepPaikka .../>)}` block (lines 239-245). StepPaikka is
         no longer rendered inside the wizard — it now lives in page.tsx as a pre-phase.
       - Shift every remaining block down by 1 and update its `saveAndAdvance(N)` argument and
         `goToStep(N)` (onPrev) target accordingly:
         - StepMediat: `step === 2` → `step === 1`; `onNext={() => saveAndAdvance(1)}`; this is now the
           FIRST wizard step so the previous step no longer exists inside the wizard. Read StepMediat.tsx
           to confirm `onPrev` is optional; if optional, remove the `onPrev` prop; if required by its
           type, pass a handler that does not navigate backward into a non-existent step (do NOT
           `goToStep(0)`). Do not invent a back-to-pre-phase navigation — D-02/D-03 define no such path.
         - StepHinnasto: `step === 3` → `step === 2`; `onNext={() => saveAndAdvance(2)}`; `onPrev={() => goToStep(1)}`
         - StepAukioloajat: `step === 4` → `step === 3`; `onNext={() => saveAndAdvance(3)}`; `onPrev={() => goToStep(2)}`
         - StepYhteystiedot: `step === 5` → `step === 4`; `onNext={() => saveAndAdvance(4)}`; `onPrev={() => goToStep(3)}`
         - StepEsikatselu: `step === 6` → `step === 5`; `onPrev={() => goToStep(4)}`

    3. Preview re-fetch trigger (line 179): change `if (step !== 6) return` to `if (step !== 5) return`
       (StepEsikatselu is now step 5).

    4. Draft resume guard (line 121): the comparison `savedStep > 1 && step === 1` STAYS as-is. Under
       the new numbering, step 1 is StepMediat and `savedStep` values are migrated (50-01) so the
       semantics "if real progress exists, resume there instead of bouncing to step 1" remain correct.
       Add a brief inline comment noting step 1 now means StepMediat (not StepPaikka) post-reorder.
       The `completedSteps` calc (lines 59-61) and forward-skip guard (lines 170-175) need no literal
       change — they operate over the new 1-5 range automatically.

    5. Remove the now-unused `import StepPaikka` (line 13) if OnboardingMode no longer references it
       (EditMode does not import or render StepPaikka, so the import becomes dead after the step===1
       block is deleted).
  </action>
  <verify>
    <automated>cd "C:/ClaudeCodeTestit/liikuntahakemisto" && npx tsc --noEmit 2>&1 | grep -c "WizardInner" | grep -qx 0 && grep -c "step === 6" app/business/WizardInner.tsx | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - No `step === 6` literal remains in WizardInner.tsx (grep returns 0)
    - No `<StepPaikka` render call remains in OnboardingMode
    - StepEsikatselu renders under `step === 5`; StepMediat renders under `step === 1`
    - The step bounds line uses `rawStep > 5` (not `> 6`)
    - The preview re-fetch guard uses `if (step !== 5)`
    - EditMode's `[1, 2, 3, 4, 5].map` (line ~358) and its `router.push` step targets are unchanged (the diff touches only OnboardingMode, lines 35-295)
    - `npx tsc --noEmit` reports zero errors in WizardInner.tsx
  </acceptance_criteria>
  <done>OnboardingMode runs StepMediat=1 through StepEsikatselu=5 with StepPaikka removed; EditMode untouched; types pass.</done>
</task>

<task type="auto">
  <name>Task 3: Shift step literals in the 4 Step components + AnalysoiSivusto; drop ProgressBar's first label (D-02, D-05, D-08)</name>
  <read_first>
    - app/business/onboarding/StepMediat.tsx (line 191 — `step: 2` in the save-step fetch body)
    - app/business/onboarding/StepHinnasto.tsx (line 196 — `step: 3`)
    - app/business/onboarding/StepAukioloajat.tsx (line 182 — `step: 4`)
    - app/business/onboarding/StepYhteystiedot.tsx (line 124 — `step: 5`)
    - app/business/onboarding/AnalysoiSivusto.tsx (line 305 — `step: 6` in handleQuickAccept's save-step body; onSkip/onConfirm props lines 16-21 are unchanged)
    - app/business/onboarding/ProgressBar.tsx (lines 16-23 — `stepLabels` array starting with `t('stepPlaceName')`)
    - .planning/phases/50-flow-uudelleenj-rjestys/50-CONTEXT.md (D-02 step literals, D-05 AnalysoiSivusto step:6→5, D-08 ProgressBar label drop — `stepPlaceName` i18n key NOT deleted)
    - .planning/phases/50-flow-uudelleenj-rjestys/50-PATTERNS.md (per-file sections lines 187-279 and ProgressBar lines 230-254)
  </read_first>
  <action>
    Apply the single-literal shifts to each caller per D-02/D-05, and drop the ProgressBar's first
    label per D-08. In every Step*/AnalysoiSivusto file, ONLY the numeric `step:` literal in the
    save-step fetch body changes — the surrounding fetch shape, headers, error handling, and all other
    logic are identical everywhere and must NOT be touched.

    1. StepMediat.tsx line 191: `step: 2` → `step: 1`
    2. StepHinnasto.tsx line 196: `step: 3` → `step: 2`
    3. StepAukioloajat.tsx line 182: `step: 4` → `step: 3`
    4. StepYhteystiedot.tsx line 124: `step: 5` → `step: 4`
    5. AnalysoiSivusto.tsx line 305 (handleQuickAccept): `step: 6` → `step: 5` (D-05). This value is
       short-lived (the `submit` route deletes the onboarding_draft row on success) but `5` is the
       correct in-range value to leave the row at if `submit` fails partway. Do NOT change onSkip/onConfirm
       prop signatures (lines 16-21) — they are unchanged per D-03.
    6. ProgressBar.tsx `stepLabels` array (lines 16-23): drop the first entry `t('stepPlaceName')`, so
       the array becomes the 5 entries `[t('stepMedia'), t('stepPricing'), t('stepHours'), t('stepContact'), t('stepPreview')]`.
       Do NOT delete the `stepPlaceName` i18n key from the message files — StepPaikka.tsx's own `<h2>`
       (line 22) still uses it (D-08). The rest of ProgressBar (the index-driven map render) needs no
       change — `stepNum = index + 1` now naturally numbers Media=1 ... Preview=5.
  </action>
  <verify>
    <automated>cd "C:/ClaudeCodeTestit/liikuntahakemisto" && npx tsc --noEmit 2>&1 | grep -Ec "StepMediat|StepHinnasto|StepAukioloajat|StepYhteystiedot|AnalysoiSivusto|ProgressBar" | grep -qx 0 && grep -c "stepPlaceName" app/business/onboarding/ProgressBar.tsx | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - StepMediat.tsx save-step body sends `step: 1`; StepHinnasto.tsx `step: 2`; StepAukioloajat.tsx `step: 3`; StepYhteystiedot.tsx `step: 4`
    - AnalysoiSivusto.tsx handleQuickAccept sends `step: 5` (no `step: 6` literal remains); onSkip/onConfirm prop signatures unchanged
    - ProgressBar.tsx `stepLabels` has exactly 5 entries and no longer contains `t('stepPlaceName')` (grep for `stepPlaceName` in ProgressBar.tsx returns 0)
    - The `stepPlaceName` key still exists in the i18n message files (NOT deleted) and StepPaikka.tsx line 22 still references it
    - `npx tsc --noEmit` reports zero errors across these 6 files
  </acceptance_criteria>
  <done>All five callers send renumbered step literals (1,2,3,4,5) and ProgressBar shows the 5-step labels; the stepPlaceName i18n key is preserved; types pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify the reordered onboarding flow end-to-end</name>
  <what-built>
    StepPaikka now renders as the first pre-phase (before AnalysoiSivusto's website analysis), the
    wizard runs 5 numbered steps (StepMediat=1 ... StepEsikatselu=5), the progress bar shows 5 steps
    with no Place-name entry, and in-flight drafts (migrated by 50-01) resume on the correct step.
  </what-built>
  <how-to-verify>
    1. Run the dev server: `npm run dev`.
    2. As a logged-in business user with a claimed/created venue, visit `/business/onboarding` (or
       `/business/onboarding?paikka_id=<id>`). Expected: the FIRST screen is StepPaikka — the venue
       name/address confirmation with a "Seuraava" (next) button, and NO progress bar.
    3. Click next. Expected: the AnalysoiSivusto website-analysis screen appears (still no progress bar).
    4. Click "Ohita" (skip URL analysis). Expected: you land on the wizard's Step 1 = StepMediat
       (media/logo/gallery), and the progress bar now shows 5 steps: Media, Pricing, Hours, Contact, Preview.
    5. Step through to the end. Expected: the final step (5) is StepEsikatselu (preview/CalloutCard).
    6. Resume test (FLOW-04): with a draft mid-wizard, reload `/business/onboarding`. Expected: it
       resumes on the correct step (e.g. a draft saved at StepHinnasto resumes on step 2, not a stale number).
    7. Confirm EditMode is unaffected: open an existing venue's edit view at `/business/<id>` and verify
       its tabs and step navigation still work as before (5 tabs, StepPaikka not present — unchanged from prod).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what rendered incorrectly (wrong step order, missing/extra progress-bar entry, resume landing on a stale step, or EditMode regression).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → save-step route | Wizard step components POST field + renumbered step integer (validated by 50-01's bounds check) |
| URL `step` param → OnboardingMode | Untrusted `step` query param drives which step renders; forward-skip guard prevents jumping past unfinished steps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-04 | Tampering | URL `step` param manipulation | mitigate | OnboardingMode clamps `rawStep` to 1-5 (Task 2) and the existing forward-skip guard (lines 170-175) redirects any `step > maxReachedStep + 1` back to the next reachable step — unchanged by the renumber |
| T-50-05 | Information Disclosure | StepPaikkaPrePhase paikka_id resolution | mitigate | Reuses the existing PrePhase resolution that keys the `business_paikka_links` lookup on the authenticated `user.id` from `createBusinessBrowserClient().auth.getUser()` — a user cannot resolve another business's paikka_id without supplying it in the URL, and the downstream save-step ownership check (50-01, unchanged) rejects writes to venues they do not own |
| T-50-06 | Tampering | EditMode accidental modification | mitigate | Task 2 scopes edits to OnboardingMode (lines 35-295) only; acceptance criteria assert EditMode's tab array and router.push targets are unchanged |
</threat_model>

<verification>
- `npx tsc --noEmit` reports zero errors across all 8 modified files
- No `step === 6` literal in WizardInner.tsx; no `step: 6` literal in AnalysoiSivusto.tsx
- ProgressBar.tsx no longer references `stepPlaceName`; the i18n key still exists in message files
- Human checkpoint confirms StepPaikka renders first, wizard runs 5 steps, drafts resume correctly, EditMode unaffected
</verification>

<success_criteria>
- FLOW-01: a new business owner sees StepPaikka before being asked for a website URL to analyze
- FLOW-04 (app side): every renumbered step literal matches the 50-01-migrated current_step values, so in-flight drafts resume on the correct step
- The wizard is a 5-step flow (StepMediat=1 ... StepEsikatselu=5) with a 5-entry progress bar
- EditMode step numbering is untouched
</success_criteria>

<output>
Create `.planning/phases/50-flow-uudelleenj-rjestys/50-02-SUMMARY.md` when done
</output>
