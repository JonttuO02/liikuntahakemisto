---
phase: 39-auth-separaatio
plan: 04
type: execute
wave: 3
depends_on:
  - 39-01
  - 39-02
  - 39-03
files_modified:
  - app/business/[id]/layout.tsx
  - app/business/onboarding/layout.tsx
  - app/business/rekisteroidy/page.tsx
  - app/business/page.tsx
  - app/business/onboarding/OnboardingWizardInner.tsx
  - app/business/onboarding/StepMediat.tsx
  - app/business/onboarding/StepYhteystiedot.tsx
  - app/business/onboarding/StepAukioloajat.tsx
  - app/business/onboarding/StepHinnasto.tsx
  - app/business/onboarding/StepEsikatselu.tsx
autonomous: true
requirements:
  - AUTHSEP-02
  - AUTHSEP-03
  - AUTHSEP-04

must_haves:
  truths:
    - "All /business/* RSC layouts call createBusinessServerClient(cookies()) instead of createServerSupabase(cookies())"
    - "All /business/* client components call createBusinessBrowserClient() instead of createBrowserSupabase()"
    - "app/business/rekisteroidy/page.tsx uses createBusinessBrowserClient() and links to /business/kirjaudu instead of opening AuthModal"
    - "app/business/rekisteroidy/page.tsx does not import AuthModal"
    - "Consumer pages (/, /profiili, /suosikit) are untouched — they still use createBrowserSupabase() and createServerSupabase()"
    - "No /business/* file imports createBrowserSupabase or createServerSupabase after migration"
    - "TypeScript compiles cleanly for all modified files"
    - "All /api/business/* and /api/admin/* route handlers use supabaseAdmin.auth.getUser(token) — no session cookie is read; AUTHSEP-02 (api side), AUTHSEP-03, and D-05 are satisfied by the existing JWT Bearer pattern"
  artifacts:
    - path: "app/business/[id]/layout.tsx"
      provides: "RSC auth guard for /business/[id]/* routes"
      contains: "createBusinessServerClient"
    - path: "app/business/onboarding/layout.tsx"
      provides: "RSC auth guard for /business/onboarding/* routes"
      contains: "createBusinessServerClient"
    - path: "app/business/rekisteroidy/page.tsx"
      provides: "Business registration page with business client"
      contains: "createBusinessBrowserClient"
  key_links:
    - from: "app/business/[id]/layout.tsx"
      to: "lib/supabase-business.ts"
      via: "import createBusinessServerClient"
      pattern: "createBusinessServerClient"
    - from: "app/business/rekisteroidy/page.tsx"
      to: "lib/supabase-business.ts"
      via: "import createBusinessBrowserClient"
      pattern: "createBusinessBrowserClient"
    - from: "app/business/rekisteroidy/page.tsx"
      to: "/business/kirjaudu"
      via: "Next.js Link (replaces AuthModal button)"
      pattern: "business/kirjaudu"
---

<objective>
Migrate all /business/* pages and layouts to use the business Supabase client. RSC layouts switch from createServerSupabase to createBusinessServerClient. Client components switch from createBrowserSupabase to createBusinessBrowserClient. rekisteroidy gets the AuthModal replacement. After this plan, no /business/* file touches the consumer sb-* cookie namespace.

Purpose: Implements AUTHSEP-02, AUTHSEP-03, AUTHSEP-04. Without this migration, business pages would silently use the consumer session, defeating the entire auth separation.
Output: 10 modified files — all /business/* RSC and client code using the business client exclusively.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-auth-separaatio/39-CONTEXT.md
@.planning/phases/39-auth-separaatio/39-01-SUMMARY.md

<interfaces>
<!-- What to swap: -->

SERVER COMPONENTS (RSC layouts):
- Old import: import { createServerSupabase } from '@/lib/supabaseSSR'
- New import: import { createBusinessServerClient } from '@/lib/supabase-business'
- Old call: const supabase = createServerSupabase(cookies())
- New call: const supabase = createBusinessServerClient(cookies())
- Redirect target also changes in both layouts: redirect('/') → redirect('/business/kirjaudu')

CLIENT COMPONENTS (all other /business/* files):
- Old import: import { createBrowserSupabase } from '@/lib/supabaseSSR'
- New import: import { createBusinessBrowserClient } from '@/lib/supabase-business'
- Old call: createBrowserSupabase()  (any usage)
- New call: createBusinessBrowserClient()

These are purely mechanical search-and-replace changes. No logic changes needed in any client component except rekisteroidy.

<!-- rekisteroidy ADDITIONAL CHANGES (per D-13, D-14): -->

rekisteroidy uses createBrowserSupabase in two places (detectRecovery and handleSubmit) — both become createBusinessBrowserClient.

The "sign in" link currently renders:
```tsx
<button type="button" className="font-bold text-[#111111] hover:underline" onClick={() => setAuthModalOpen(true)}>
  {t('signInLink')}
</button>
```
and at the bottom: <AuthModal open={authModalOpen} onClose={() => setAuthModalOpen(false)} />

Replace with a Next.js Link:
```tsx
import Link from 'next/link'
// ...
<Link href="/business/kirjaudu" className="font-bold text-[#111111] hover:underline">
  {t('signInLink')}
</Link>
```

Remove: useState for authModalOpen, import AuthModal, <AuthModal .../> JSX. Remove the authModalOpen state variable entirely (it is only used with AuthModal).

<!-- Files with createBrowserSupabase calls (exact paths): -->
- app/business/page.tsx — 2 calls
- app/business/rekisteroidy/page.tsx — 2 calls (+ AuthModal replacement)
- app/business/onboarding/OnboardingWizardInner.tsx — 3 calls
- app/business/onboarding/StepMediat.tsx — 3 calls
- app/business/onboarding/StepYhteystiedot.tsx — 2 calls
- app/business/onboarding/StepAukioloajat.tsx — 2 calls
- app/business/onboarding/StepHinnasto.tsx — 2 calls
- app/business/onboarding/StepEsikatselu.tsx — 1 call

<!-- Files with createServerSupabase calls: -->
- app/business/[id]/layout.tsx — 1 call
- app/business/onboarding/layout.tsx — 1 call

<!-- Files NOT in scope (do not touch): -->
- app/business/[id]/page.tsx — uses supabaseAdmin only (no consumer/business client)
- app/business/layout.tsx — is a passthrough (<>{children}</>) with no auth calls
- Any file under app/ NOT in app/business/ — consumer pages stay on createBrowserSupabase/createServerSupabase
- All /api/business/* and /api/admin/* route handlers — use JWT Bearer verification (supabaseAdmin.auth.getUser(token)); these are cookie-namespace-agnostic by construction — do NOT touch them in this plan
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate RSC layouts — createServerSupabase → createBusinessServerClient</name>
  <files>app/business/[id]/layout.tsx, app/business/onboarding/layout.tsx</files>
  <read_first>
    - app/business/[id]/layout.tsx — read the full file before editing
    - app/business/onboarding/layout.tsx — read the full file before editing
    - lib/supabase-business.ts — confirm createBusinessServerClient is exported
  </read_first>
  <action>
In app/business/[id]/layout.tsx:
1. Replace: import { createServerSupabase } from '@/lib/supabaseSSR'
   With:     import { createBusinessServerClient } from '@/lib/supabase-business'
2. Replace: const supabase = createServerSupabase(cookies())
   With:     const supabase = createBusinessServerClient(cookies())
3. Replace: redirect('/')
   With:     redirect('/business/kirjaudu')

In app/business/onboarding/layout.tsx: Apply the identical three substitutions (same pattern — import, call, redirect target).

No other changes. Both layouts remain RSC async functions, same structure, same import of { redirect } from 'next/navigation' and { cookies } from 'next/headers'.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "business.*layout" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - app/business/[id]/layout.tsx: grep "createBusinessServerClient" returns 1+ matches
    - app/business/[id]/layout.tsx: grep "createServerSupabase" returns 0 matches
    - app/business/[id]/layout.tsx: grep "business/kirjaudu" returns 1 match (redirect target)
    - app/business/onboarding/layout.tsx: grep "createBusinessServerClient" returns 1+ matches
    - app/business/onboarding/layout.tsx: grep "createServerSupabase" returns 0 matches
    - app/business/onboarding/layout.tsx: grep "business/kirjaudu" returns 1 match
    - `npx tsc --noEmit 2>&1 | grep "business.*layout"` returns empty
  </acceptance_criteria>
  <done>Both RSC layouts use createBusinessServerClient(cookies()) and redirect to /business/kirjaudu on unauthenticated access.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 7 client component files — pure createBrowserSupabase → createBusinessBrowserClient search-and-replace</name>
  <files>
    app/business/page.tsx,
    app/business/onboarding/OnboardingWizardInner.tsx,
    app/business/onboarding/StepMediat.tsx,
    app/business/onboarding/StepYhteystiedot.tsx,
    app/business/onboarding/StepAukioloajat.tsx,
    app/business/onboarding/StepHinnasto.tsx,
    app/business/onboarding/StepEsikatselu.tsx
  </files>
  <read_first>
    - app/business/page.tsx — read to locate the 2 createBrowserSupabase call sites
    - app/business/onboarding/OnboardingWizardInner.tsx — read to locate the 3 call sites
    - lib/supabase-business.ts — confirm createBusinessBrowserClient is exported
  </read_first>
  <action>
For ALL 7 files listed in &lt;files&gt;: apply exactly two mechanical search-and-replace substitutions per file, nothing else:

1. Replace the import line:
   import { createBrowserSupabase } from '@/lib/supabaseSSR'
   → import { createBusinessBrowserClient } from '@/lib/supabase-business'

2. Replace every call site:
   createBrowserSupabase()
   → createBusinessBrowserClient()

No logic changes, no restructuring, no other modifications. These are pure renames. Do not touch app/business/rekisteroidy/page.tsx in this task — that file has additional surgery handled in Task 3.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "app/business" | grep -v rekisteroidy | head -20</automated>
  </verify>
  <acceptance_criteria>
    - For EACH of the 7 files: grep "createBrowserSupabase" returns 0 matches
    - For EACH of the 7 files: grep "createBusinessBrowserClient" returns 1+ matches
    - Consumer files (lib/supabaseSSR.ts) are unchanged: grep "createBrowserSupabase" in lib/supabaseSSR.ts still returns 1 match (the export definition)
    - `npx tsc --noEmit 2>&1 | grep "app/business" | grep -v rekisteroidy` returns empty (no TS errors in these 7 files)
  </acceptance_criteria>
  <done>7 client component files use createBusinessBrowserClient(). No logic changes made. TypeScript compiles cleanly for these files.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate rekisteroidy — swap client + remove AuthModal + add /business/kirjaudu link</name>
  <files>app/business/rekisteroidy/page.tsx</files>
  <read_first>
    - app/business/rekisteroidy/page.tsx — read the FULL file before editing; understand the AuthModal usage pattern and authModalOpen state
  </read_first>
  <action>
In app/business/rekisteroidy/page.tsx, make the following changes in order:

STEP 1 — Swap client (same as Task 2 for the other files):
- Replace: import { createBrowserSupabase } from '@/lib/supabaseSSR'
  With:     import { createBusinessBrowserClient } from '@/lib/supabase-business'
- Replace every: createBrowserSupabase()
  With:           createBusinessBrowserClient()
  (There are 2 call sites: one in detectRecovery and one in handleSubmit)

STEP 2 — Remove AuthModal import:
- Remove the import line: import AuthModal from '@/app/components/AuthModal'
  (or whatever the exact import path is — remove the entire line)

STEP 3 — Add Link import (if not already present):
- Add: import Link from 'next/link'
  (Next.js Link is almost certainly already imported; if so, skip this step)

STEP 4 — Remove authModalOpen state:
- Remove the line: const [authModalOpen, setAuthModalOpen] = useState(false)
  (The authModalOpen variable is used only by the AuthModal button and modal — removing AuthModal makes this state dead code)

STEP 5 — Replace the sign-in button with a Link:
- Find the button element that calls setAuthModalOpen(true). It looks like:
  &lt;button type="button" className="font-bold text-[#111111] hover:underline" onClick={() => setAuthModalOpen(true)}&gt;
    {t('signInLink')}
  &lt;/button&gt;
- Replace it with:
  &lt;Link href="/business/kirjaudu" className="font-bold text-[#111111] hover:underline"&gt;
    {t('signInLink')}
  &lt;/Link&gt;
- Keep the surrounding paragraph and {t('alreadyHaveAccount')} text unchanged — only the button element is replaced

STEP 6 — Remove AuthModal JSX:
- Remove the &lt;AuthModal open={authModalOpen} onClose={() => setAuthModalOpen(false)} /&gt; element
  (typically at the bottom of the JSX return, just before the closing wrapper)

Do not change any other logic, form handling, translation keys, or layout structure.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "rekisteroidy" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - app/business/rekisteroidy/page.tsx: grep "AuthModal" returns 0 matches
    - app/business/rekisteroidy/page.tsx: grep "authModalOpen" returns 0 matches
    - app/business/rekisteroidy/page.tsx: grep "business/kirjaudu" returns 1 match (the Link href)
    - app/business/rekisteroidy/page.tsx: grep "createBusinessBrowserClient" returns 2 matches (detectRecovery + handleSubmit)
    - app/business/rekisteroidy/page.tsx: grep "createBrowserSupabase" returns 0 matches
    - `npx tsc --noEmit 2>&1 | grep "rekisteroidy"` returns empty (no TS errors in this file)
  </acceptance_criteria>
  <done>rekisteroidy uses createBusinessBrowserClient(), does not import or render AuthModal, and links to /business/kirjaudu for sign-in. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| business page→Supabase | All business client calls now go through sb-biz-* cookies |
| consumer page→Supabase | Consumer pages remain on sb-* cookies — no business client imported there |
| API route→Supabase | /api/business/* and /api/admin/* use JWT Bearer (supabaseAdmin.auth.getUser(token)) — cookie-namespace-agnostic by construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-09 | Spoofing | Business RSC reading consumer session | mitigate | Both RSC layouts now call createBusinessServerClient — reads only sb-biz-* cookies |
| T-39-10 | Information Disclosure | Consumer auth state visible to business pages | mitigate | Business client singleton is separate from consumer singleton — different module variable, different cookie name |
| T-39-11 | Tampering | AuthModal removed from rekisteroidy without replacement | mitigate | Replaced with explicit Link to /business/kirjaudu — existing users have a navigation path |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After all tasks complete:
- `grep -r "createBrowserSupabase\|createServerSupabase" app/business/ 2>/dev/null` returns 0 lines (all business files use business client)
- `grep -r "createBusinessBrowserClient\|createBusinessServerClient" app/business/ 2>/dev/null` returns 10+ lines (all client and server files)
- `grep "createBrowserSupabase" lib/supabaseSSR.ts` still returns the function definition line (consumer client untouched)
- `grep -rn "createServerSupabase\|createBrowserSupabase\|createBusinessServerClient" app/api/business/ app/api/admin/ 2>/dev/null` returns no matches (API routes use supabaseAdmin JWT Bearer — no session-cookie client present; confirms AUTHSEP-02 api side, AUTHSEP-03, and D-05)
- `npx tsc --noEmit 2>&1 | grep "app/business"` returns empty
- `npx tsc --noEmit 2>&1 | head -20` shows no errors (or only pre-existing errors unrelated to this phase)
</verification>

<success_criteria>
Every /business/* page and layout uses the business Supabase client exclusively. No /business/* file imports createBrowserSupabase or createServerSupabase. Consumer pages are untouched. rekisteroidy uses the business client and links to /business/kirjaudu instead of opening AuthModal. API routes in /api/business/* and /api/admin/* use JWT Bearer via supabaseAdmin — confirmed by grep returning no session-cookie client imports. TypeScript compiles cleanly for all modified files.
</success_criteria>

<output>
Create .planning/phases/39-auth-separaatio/39-04-SUMMARY.md when done
</output>
