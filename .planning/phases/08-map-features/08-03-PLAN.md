---
phase: 08-map-features
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - app/paikat/[id]/page.tsx
  - app/components/Etusivu.tsx
autonomous: true
requirements:
  - MAP-07

must_haves:
  truths:
    - "Näytä kartalla on the profile page links to /?id=<paikka.id> (not https://maps.google.com/...)"
    - "Navigating to /?id=<paikka_id> auto-opens the fullscreen map"
    - "The fullscreen map pans and zooms to zoom 16 centered on the target venue when id param is present"
    - "The bottom sheet does NOT auto-open on /?id= arrival — only mini-card becomes visible"
  artifacts:
    - path: "app/paikat/[id]/page.tsx"
      provides: "Internal Link to /?id=paikka.id instead of external Google Maps anchor"
      contains: "/?id="
    - path: "app/components/Etusivu.tsx"
      provides: "useSearchParams reading ?id= param, useEffect to open map and focus venue"
      contains: "useSearchParams"
  key_links:
    - from: "app/paikat/[id]/page.tsx"
      to: "Etusivu via /?id=paikka.id"
      via: "Next.js Link component"
      pattern: "href.*\\?id="
    - from: "app/components/Etusivu.tsx"
      to: "fullscreen map + MapAutoZoom"
      via: "useSearchParams id param → setKartaAuki(true) + setAutoZoomTarget"
      pattern: "focusId.*setKartaAuki.*setAutoZoomTarget"
---

<objective>
Change "Näytä kartalla" on the venue profile page to navigate in-app to `/?id=<paikka_id>`, and implement Etusivu's handler that reads this param, opens the fullscreen map, and zooms to the target venue.

Purpose: MAP-07 — users tapping "Näytä kartalla" stay in the app's own map view instead of being sent to Google Maps. The map auto-centers on the venue at close zoom.

Output: (1) profile page link changed from external anchor to internal `<Link>`, (2) Etusivu reads `?id=` via `useSearchParams` and triggers `setKartaAuki(true)` + `setAutoZoomTarget` for the matched venue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-map-features/08-CONTEXT.md
@.planning/phases/08-map-features/08-01-SUMMARY.md

<interfaces>
<!-- Extracted from current codebase -->

From app/paikat/[id]/page.tsx — current "Näytä kartalla" anchor (lines 70–79):
```tsx
{paikka.latitude != null && paikka.longitude != null && (
  <Row icon={<MapPin className="w-5 h-5 text-[rgba(17,17,17,0.5)]" />} label="Sijainti">
    <a
      href={`https://maps.google.com/?q=${paikka.latitude},${paikka.longitude}`}
      target="_blank"
      rel="noopener noreferrer"
      className="text-[#111111] hover:text-[rgba(17,17,17,0.6)] text-sm font-bold underline underline-offset-2 [transition:color_150ms_var(--ease-out)]"
    >
      Näytä kartalla →
    </a>
  </Row>
)}
```

The profile page is a server component — `Link` from next/link is already imported (line 5).

From app/components/Etusivu.tsx — existing state declarations (~line 74):
```tsx
const [kartaAuki, setKartaAuki] = useState(false)
// ... other state
```

The component already imports: useState, useEffect, useMemo from 'react'.
`useSearchParams` from 'next/navigation' is NOT currently imported — must be added.

From Plan 08-02, Etusivu.tsx will have:
```tsx
const [autoZoomTarget, setAutoZoomTarget] = useState<{ lat: number; lng: number } | null>(null)
```
This state and `MapAutoZoom` (the component that fires pan+zoom) are created in Plan 08-02.
Plan 08-03 DEPENDS on Plan 08-01 only in the wave — but functionally uses the autoZoomTarget state created in Plan 08-02. Since 08-02 and 08-03 are in the same wave (wave 2), the executor must run 08-02 before 08-03, or the executor for 08-03 must add autoZoomTarget state if 08-02 hasn't run yet.

IMPORTANT: Plans 08-02 and 08-03 both modify Etusivu.tsx. They MUST be executed sequentially (08-02 first, then 08-03). The wave-2 parallel assignment is only valid if different files are touched — since both touch Etusivu.tsx, 08-03 effectively depends on 08-02.

Etusivu already uses useSearchParams indirectly (via the `searchParams` page prop in page.tsx). In Etusivu.tsx itself, `useSearchParams` from next/navigation has NOT been used yet — add the import.

CLAUDE.md constraint: `?nakyma=kartta` is a dead parameter. URL must be `/?id=<paikka_id>` with no nakyma param (D-11).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Change profile page "Näytä kartalla" to internal Link</name>
  <files>app/paikat/[id]/page.tsx</files>
  <read_first>
    app/paikat/[id]/page.tsx — read the full file to see the current "Näytä kartalla" anchor (lines 69–79), existing Link import (line 5), and Row component pattern
    .planning/phases/08-map-features/08-CONTEXT.md — D-10 link target, D-11 URL format
    CLAUDE.md — URL routing constraint: ?nakyma=kartta is dead, never generate it
  </read_first>
  <action>
    In `app/paikat/[id]/page.tsx`, replace the external Google Maps anchor inside the "Sijainti" Row with an internal Next.js Link.

    Current code to replace:
    ```
    <a
      href={`https://maps.google.com/?q=${paikka.latitude},${paikka.longitude}`}
      target="_blank"
      rel="noopener noreferrer"
      className="text-[#111111] hover:text-[rgba(17,17,17,0.6)] text-sm font-bold underline underline-offset-2 [transition:color_150ms_var(--ease-out)]"
    >
      Näytä kartalla →
    </a>
    ```

    Replace with:
    ```
    <Link
      href={`/?id=${paikka.id}`}
      className="text-[#111111] hover:text-[rgba(17,17,17,0.6)] text-sm font-bold underline underline-offset-2 [transition:color_150ms_var(--ease-out)]"
    >
      Näytä kartalla →
    </Link>
    ```

    Notes:
    - Remove `target="_blank"` and `rel="noopener noreferrer"` — this is now in-app navigation
    - `Link` is already imported from 'next/link' on line 5
    - URL is `/?id=${paikka.id}` — NO `?nakyma=kartta` (dead param per CLAUDE.md)
    - The Row structure, icon, label, and className string stay identical
    - The condition `paikka.latitude != null && paikka.longitude != null` stays as-is (still relevant — we only want to show the link if the venue has coordinates)
  </action>
  <verify>
    <automated>cd /c/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - app/paikat/[id]/page.tsx uses `<Link href={`/?id=${paikka.id}`}>` for Näytä kartalla
    - No `<a href="https://maps.google.com/...">` for the Näytä kartalla link remains
    - No `?nakyma=kartta` in the href
    - `target="_blank"` and `rel="noopener noreferrer"` removed from this link
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Etusivu reads ?id= param and opens map focused on venue</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    app/components/Etusivu.tsx — read the full file after Plans 08-01 and 08-02 have been applied. Focus on: (1) all imports at the top, (2) state declarations block (~lines 74–81), (3) the useEffect blocks, (4) the `autoZoomTarget` state added by Plan 08-02, (5) the `setKartaAuki` usage
    .planning/phases/08-map-features/08-CONTEXT.md — D-12 param reading, D-13 no auto-open sheet, D-14 fullscreen map auto-opens
  </read_first>
  <action>
    Make the following changes to Etusivu.tsx (after Plans 08-01 and 08-02 have been applied):

    **1. Add useSearchParams import**:
    In the import block at the top, add `useSearchParams` to the existing next/navigation import line. If there is no next/navigation import line yet, add:
    `import { useSearchParams } from 'next/navigation'`

    **2. Add focusId reading** (in the component body, after the existing state declarations):
    `const searchParams = useSearchParams()`
    `const focusId = searchParams.get('id')`

    **3. Add focus useEffect** (after the existing useEffect blocks, before the suodatettu useMemo):
    ```
    useEffect(() => {
      if (!focusId) return
      const id = Number(focusId)
      const target = paikat.find(p => p.id === id)
      if (!target || target.latitude == null || target.longitude == null) return
      setKartaAuki(true)
      setAutoZoomTarget({ lat: target.latitude, lng: target.longitude })
      // Do NOT call setValittu — D-13: big sheet does not auto-open
    }, [focusId, paikat]) // eslint-disable-line react-hooks/exhaustive-deps
    ```

    The `setKartaAuki(true)` call opens the fullscreen map (D-14). The `setAutoZoomTarget` call triggers the `MapAutoZoom` component (added in Plan 08-02) to pan+zoom to the venue. `setValittu` is deliberately NOT called (D-13 — only mini-card becomes visible, user taps to open sheet).

    **Dependency note**: `autoZoomTarget` state and `setAutoZoomTarget` are created in Plan 08-02. This task assumes Plan 08-02 has already been applied to Etusivu.tsx. Read the file first to confirm `autoZoomTarget` state exists before writing this effect.

    **useSearchParams wrapping**: `useSearchParams()` requires the component to be wrapped in `<Suspense>` at the call site. In `app/page.tsx`, Etusivu is already wrapped in `<Suspense fallback={<div className="min-h-screen bg-white" />}>` — this satisfies the requirement. No changes needed to page.tsx.
  </action>
  <verify>
    <automated>cd /c/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `useSearchParams` is imported from 'next/navigation' in Etusivu.tsx
    - `const focusId = searchParams.get('id')` exists in the component body
    - A useEffect reads `focusId`, finds the matching paikka, calls `setKartaAuki(true)` and `setAutoZoomTarget`
    - The effect does NOT call `setValittu`
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL ?id= param → paikat lookup | User-supplied id param parsed as Number; matched against loaded paikat array |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | focusId URL param | mitigate | `Number(focusId)` coerces to NaN for non-numeric input; `paikat.find(p => p.id === id)` returns undefined for NaN — safe no-op. No Supabase query fired from this param. |
| T-08-05 | Spoofing | in-app navigation from profile page | accept | Link navigates to /?id=N which only reads from already-loaded paikat prop — no privilege escalation possible |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `grep "href.*/?id=" app/paikat/[id]/page.tsx` matches the Link href
3. `grep "maps.google.com" app/paikat/[id]/page.tsx` returns no results (old anchor removed)
4. `grep "useSearchParams" app/components/Etusivu.tsx` returns ≥ 1 result (import + usage)
5. `grep "focusId" app/components/Etusivu.tsx` returns ≥ 2 results (declaration + effect)
6. `grep "setKartaAuki(true)" app/components/Etusivu.tsx` — focus effect contains this call
</verification>

<success_criteria>
- Profile page "Näytä kartalla →" is a Next.js Link to `/?id=<paikka.id>` with no external href
- Navigating to `/?id=123` opens fullscreen map automatically
- Map pans and zooms to zoom 16 on the target venue
- Bottom sheet does NOT auto-open (valittu remains null on arrival)
- Non-numeric or unrecognized id param is a safe no-op (no error thrown, no redirect)
- TypeScript build passes: `npx tsc --noEmit` exits 0
</success_criteria>

<output>
Create `.planning/phases/08-map-features/08-03-SUMMARY.md` when done
</output>
