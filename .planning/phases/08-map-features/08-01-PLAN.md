---
phase: 08-map-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Etusivu.tsx
autonomous: true
requirements:
  - MAP-05
  - MAP-06

must_haves:
  truths:
    - "User location marker on the fullscreen map has a white ripple ring pulsing outward"
    - "User location marker on the preview map also has the white ripple ring"
    - "The fullscreen map tracks zoom level reactively via onCameraChanged"
    - "zoomLevel state is available as a number in Etusivu for downstream use by Plan 08-02"
  artifacts:
    - path: "app/components/Etusivu.tsx"
      provides: "GPS accuracy ring + zoomLevel state + onCameraChanged wiring"
      contains: "zoomLevel"
  key_links:
    - from: "app/components/Etusivu.tsx"
      to: "fullscreen <Map>"
      via: "onCameraChanged prop"
      pattern: "onCameraChanged.*setZoomLevel"
    - from: "app/components/Etusivu.tsx"
      to: "user location AdvancedMarker (both maps)"
      via: "motion.div ring inside marker div"
      pattern: "animate.*scale.*opacity.*Infinity"
---

<objective>
Add the GPS accuracy ring to the user location AdvancedMarker and wire up reactive zoom tracking on the fullscreen map.

Purpose: MAP-05 delivers the white ripple ring. The zoomLevel state wired here is the infrastructure MAP-06 (Plan 08-02) depends on to drive the pin→mini-card transformation.

Output: Etusivu.tsx modified with (1) a `motion.div` ripple ring inside both user-location AdvancedMarker elements, and (2) `zoomLevel` state updated via `onCameraChanged` on the fullscreen Map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-map-features/08-CONTEXT.md

<interfaces>
<!-- Extracted from app/components/Etusivu.tsx — current user location marker structure (lines 280-286 and 345-351) -->

Both map instances contain this identical user location AdvancedMarker:
```tsx
{coords && (
  <AdvancedMarker position={coords} zIndex={20}>
    <div style={{ width: 24, height: 24, position: 'relative' }}>
      <div style={{ position: 'absolute', inset: 0, borderRadius: '50%', background: 'rgba(66,133,244,0.18)' }} />
      <div style={{ position: 'absolute', inset: 3, borderRadius: '50%', background: '#4285F4', border: '2.5px solid white' }} />
    </div>
  </AdvancedMarker>
)}
```

Current fullscreen Map component (line 325):
```tsx
<Map
  defaultCenter={TAMPERE}
  defaultZoom={14}
  mapId={MAP_ID} colorScheme={isDark ? 'DARK' : 'LIGHT'}
  style={{ width: '100%', height: '100%' }}
  disableDefaultUI
  gestureHandling="greedy"
  clickableIcons={false}
  keyboardShortcuts={false}
  onClick={() => setValittu(null)}
>
```

Framer Motion animate with repeat:Infinity pattern (already used in Phase 5 AI widget):
```tsx
animate={{ scale: [0.5, 2], opacity: [0.6, 0] }}
transition={{ duration: 1.8, repeat: Infinity, ease: 'easeOut' }}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add white ripple ring to user location AdvancedMarker (both map instances)</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    app/components/Etusivu.tsx — read the full file to see current user location marker structure (look for both occurrences: preview map ~line 280 and fullscreen map ~line 345)
    .planning/phases/08-map-features/08-CONTEXT.md — D-02 animation spec, D-03 placement spec
    CLAUDE.md — animation rules (duration, ease values)
  </read_first>
  <action>
    In Etusivu.tsx, locate both user-location AdvancedMarker elements (one in the preview map, one in the fullscreen map). They are structurally identical: a `div` with `width: 24, height: 24, position: 'relative'` containing a translucent blue ring div and a solid blue circle div.

    Inside each of these `div` wrappers (as the FIRST child, before the two existing inner divs), add a `motion.div` with these exact props:
    - `style={{ position: 'absolute', inset: -8, borderRadius: '50%', border: '2px solid rgba(255,255,255,0.7)', pointerEvents: 'none' }}`
    - `animate={{ scale: [0.5, 2], opacity: [0.6, 0] }}`
    - `transition={{ duration: 1.8, repeat: Infinity, ease: 'easeOut' }}`

    The outer container div must have `overflow: 'visible'` added (or confirm it already has it) so the ring (which extends 8px outside via `inset: -8`) is not clipped. The current style is `{ width: 24, height: 24, position: 'relative' }` — add `overflow: 'visible'`.

    `motion` is already imported from framer-motion in Etusivu.tsx (verify on the import line). No new imports needed.

    Do NOT add the ring to the preview map's markers if doing so would break the 3D perspective — the ring is cosmetic and small; add it to both instances. Both user-location markers are structurally identical, so the change is identical in both places.
  </action>
  <verify>
    <automated>cd /c/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Both user-location AdvancedMarker divs in Etusivu.tsx contain a motion.div ring child
    - Ring has `animate={{ scale: [0.5, 2], opacity: [0.6, 0] }}` and `transition={{ duration: 1.8, repeat: Infinity, ease: 'easeOut' }}`
    - Ring has `inset: -8` and `border: '2px solid rgba(255,255,255,0.7)'`
    - Outer container div has `overflow: 'visible'`
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add zoomLevel state and onCameraChanged to fullscreen Map</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    app/components/Etusivu.tsx — read current state declarations (lines 74–81) and fullscreen Map component (around line 325) to see exact current props
    .planning/phases/08-map-features/08-CONTEXT.md — D-04 zoom threshold, zoom reading pattern in `<specifics>`
  </read_first>
  <action>
    In Etusivu.tsx, make these two changes:

    1. Add a new state variable with the other state declarations (after line 81, alongside kartaAuki/fullH/isDark):
       `const [zoomLevel, setZoomLevel] = useState(14)`
       (Initial value 14 matches the fullscreen map's `defaultZoom={14}`)

    2. Add the `onCameraChanged` prop to the fullscreen `<Map>` component (the one with `gestureHandling="greedy"`):
       `onCameraChanged={(ev) => setZoomLevel(ev.detail.zoom)}`

    Do NOT add `onCameraChanged` to the preview map (the one with `gestureHandling="none"` — zoom logic only applies to the fullscreen interactive map).

    `useState` is already imported. The `ev.detail.zoom` type comes from `@vis.gl/react-google-maps` — no type import needed, TypeScript infers it from the Map prop type.
  </action>
  <verify>
    <automated>cd /c/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `const [zoomLevel, setZoomLevel] = useState(14)` exists in Etusivu.tsx state declarations
    - The fullscreen Map (gestureHandling="greedy") has `onCameraChanged={(ev) => setZoomLevel(ev.detail.zoom)}`
    - The preview Map (gestureHandling="none") does NOT have onCameraChanged
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Maps API → client | Map zoom/camera events come from Maps JS API — trusted first-party |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service | onCameraChanged callback | accept | Zoom events are throttled by Maps API; setting state on each event is standard pattern — no extra throttle needed |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Etusivu.tsx contains `zoomLevel` state and `onCameraChanged` on fullscreen Map
3. Both user-location AdvancedMarker elements contain the `motion.div` ring with `scale: [0.5, 2]` and `opacity: [0.6, 0]`
4. No lint/type errors on the motion.div ring (framer-motion types accept array values for animate)
</verification>

<success_criteria>
- `grep -c "animate.*scale.*0\.5.*2.*opacity.*0\.6.*0\|scale.*\[0\.5.*2\]" app/components/Etusivu.tsx` returns ≥ 2 (one per marker instance)
- `grep -c "onCameraChanged" app/components/Etusivu.tsx` returns 1 (fullscreen map only)
- `grep -c "zoomLevel" app/components/Etusivu.tsx` returns ≥ 2 (declaration + usage)
- TypeScript build passes: `npx tsc --noEmit` exits 0
</success_criteria>

<output>
Create `.planning/phases/08-map-features/08-01-SUMMARY.md` when done
</output>
