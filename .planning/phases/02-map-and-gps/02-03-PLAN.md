---
phase: 02-map-and-gps
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - app/components/Etusivu.tsx
  - app/components/PaikkaKortti.tsx
  - app/components/LiikuntapaikatLista.tsx
  - package.json
autonomous: true
requirements:
  - MAP-02
  - MAP-03

must_haves:
  truths:
    - "Etusivu.tsx preview and fullscreen maps use Map + AdvancedMarker — no GoogleMap/Marker/useJsApiLoader"
    - "PaikkaKortti accepts an optional distanceStr prop and renders it when present"
    - "LiikuntapaikatLista computes distances with useMemo and passes them to PaikkaKortti"
    - "Distance string '1,2 km' appears on venue cards in list view when GPS is active in Kartta"
    - "@react-google-maps/api is removed from package.json"
  artifacts:
    - path: "app/components/Etusivu.tsx"
      provides: "Preview and fullscreen maps migrated to vis.gl"
      contains: "AdvancedMarker"
    - path: "app/components/PaikkaKortti.tsx"
      provides: "Card component with optional distance display"
      contains: "distanceStr"
    - path: "app/components/LiikuntapaikatLista.tsx"
      provides: "List view with GPS distance memoization"
      contains: "distancesMap"
  key_links:
    - from: "app/components/LiikuntapaikatLista.tsx"
      to: "lib/geo.ts"
      via: "haversineKm + formatDistance imports"
      pattern: "haversineKm"
    - from: "app/components/LiikuntapaikatLista.tsx"
      to: "hooks/useGPS.ts"
      via: "useGPS() call for list distance context"
      pattern: "useGPS"
    - from: "app/components/PaikkaKortti.tsx"
      to: "distanceStr prop"
      via: "optional string prop from LiikuntapaikatLista"
      pattern: "distanceStr"
---

<objective>
Migrate Etusivu.tsx's two Google Maps instances to @vis.gl/react-google-maps, add distance strings to list-view venue cards via PaikkaKortti + LiikuntapaikatLista, and remove the old @react-google-maps/api package. After this plan, the full double-load flash is eliminated and MAP-02 (distance on every card) is delivered.

Purpose: Complete the library migration and deliver the distance string feature. This is the final plan of Phase 2.

Output: Etusivu.tsx fully migrated, PaikkaKortti has distanceStr prop, LiikuntapaikatLista has GPS + distance memoization, @react-google-maps/api removed.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\ClaudeCodeTestit\liikuntahakemisto\.planning\phases\02-map-and-gps\02-02-SUMMARY.md

<!-- Key interfaces the executor needs -->
<interfaces>
From @vis.gl/react-google-maps (now installed):
  import { Map, AdvancedMarker, useMap } from '@vis.gl/react-google-maps'
  // APIProvider is already in layout.tsx — do NOT add it again in Etusivu.tsx

From lib/geo.ts (created in Plan 01):
  export function haversineKm(lat1: number, lng1: number, lat2: number, lng2: number): number
  export function formatDistance(km: number): string

From hooks/useGPS.ts (created in Plan 01):
  export function useGPS(): { status: GPSStatus; coords: { lat: number; lng: number } | null; requestLocation: () => void }

From app/components/Etusivu.tsx (current — 526 lines):
  Contains TWO GoogleMap instances:
    1. Preview map (lines 266-295): inside AnimatePresence !kartaAuki branch, 3D-tilted, zoom=12
    2. Fullscreen map (lines 337-359): inside AnimatePresence kartaAuki branch, zoom=14, with Marker pins
  Both use isLoaded guard from useJsApiLoader.

  Etusivu migration plan:
  - Remove useJsApiLoader call (line 60) and isLoaded state entirely
  - Remove mapInstanceRef, previewMapRef, onFullscreenMapLoad, onPreviewMapLoad (lines 57-81)
  - Remove getMarkerIcon() function (lines 147-164) — no longer needed with AdvancedMarker

  Preview map (replace GoogleMap/Marker with Map/AdvancedMarker):
  - mapId="DEMO_MAP_ID", defaultCenter={TAMPERE}, defaultZoom={12}
  - style={{ width: '100%', height: '100%' }}
  - disableDefaultUI, gestureHandling="none", clickableIcons=false, keyboardShortcuts=false
  - Markers: use SimplePin component (lightweight inline SVG, no Framer Motion animations):
      function SimplePin({ laji }: { laji: string }) {
        const color = lajiKonfig[laji]?.color ?? '#6b7280'
        return (
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 28 38" width={28} height={38}>
            <path d="M14 0C6.268 0 0 6.268 0 14c0 5.25 2.875 9.83 7.125 12.3L14 38l6.875-11.7C25.125 23.83 28 19.25 28 14 28 6.268 21.732 0 14 0Z" fill={color} />
          </svg>
        )
      }
  - Day/night styles: add MapStyleController as child of preview Map (same helper as in Kartta.tsx)
  - Remove isLoaded guard — no longer needed. If Map is not ready, it renders an empty div.
  - The `isLoaded` conditional (line 265: `{isLoaded ? <GoogleMap>...</GoogleMap> : <div>}`)
    becomes just the <Map> element directly.

  Fullscreen map (replace GoogleMap/Marker with Map/AdvancedMarker):
  - mapId="DEMO_MAP_ID", defaultCenter={TAMPERE}, defaultZoom={14}
  - style={{ width: '100%', height: '100%' }}
  - disableDefaultUI, gestureHandling="greedy"
  - onClick={() => setValittu(null)}
  - Markers: use AdvancedMarker + SimplePin (NOT the SportPin from Kartta.tsx —
    Etusivu does not need animated pins; SimplePin is correct for the decorative fullscreen map)
  - Remove: `window.google.maps.event.trigger(mapInstanceRef.current, 'resize')` in
    onAnimationComplete (lines 331-334) — @vis.gl handles resize automatically. Remove the
    entire onAnimationComplete callback. The Framer Motion transition on the outer div continues
    to work with no resize trigger needed.
  - Day/night styles: add MapStyleController as child of fullscreen Map
  - Remove the `isLoaded &&` guard around the GoogleMap

  Keep everything else in Etusivu.tsx unchanged:
  - Night background overlays, stars
  - AI widget / typewriter animation
  - Weather widget
  - Karuselli
  - kartaAuki state and 3D perspective animation
  - Bottom sheet for valittu
  - Filter pills in fullscreen map
  - Day/night toggle buttons

  The ICON_SVG record (lines 21-29) and SPORT_ICONS record (lines 31-34) are used
  only in the now-deleted getMarkerIcon(). After getMarkerIcon() is removed, check
  whether ICON_SVG and SPORT_ICONS are used anywhere else in Etusivu.tsx. If they
  are only in getMarkerIcon(), remove them too. SPORT_ICONS is also used in the
  bottom sheet (line 467 SPORT_ICONS[valittu.laji]) — keep it for that usage.
  ICON_SVG is only used in getMarkerIcon() — remove it.

From app/components/PaikkaKortti.tsx (current — 111 lines):
  Props: { paikka: Liikuntapaikka }
  Add optional prop: distanceStr?: string
  Render location: in the bottom row, after the price/hinta span, render:
    {distanceStr && (
      <span className="text-xs text-[rgba(17,17,17,0.4)] shrink-0 tabular-nums flex items-center gap-1">
        <MapPin className="w-3 h-3 shrink-0" />
        {distanceStr}
      </span>
    )}
  MapPin is already imported in PaikkaKortti.tsx (line 5).
  Position: In the bottom row div (line 76 "mt-auto flex items-center justify-between"),
  the distanceStr goes BETWEEN the CTA button (left) and the hinta span (right) —
  or if hinta is absent, between the CTA and the "Lisätään pian" placeholder.
  Actually: put distanceStr to the RIGHT side next to hinta (both are right-side metadata).
  Change the right side from a single span to a flex column or just render them both:
    <div className="flex flex-col items-end gap-0.5 shrink-0">
      {hinta ? <span className="text-sm font-semibold...">{hinta}</span>
             : <span className="text-xs text-[rgba...]">Lisätään pian</span>}
      {distanceStr && (
        <span className="text-xs text-[rgba(17,17,17,0.4)] tabular-nums flex items-center gap-0.5">
          <MapPin className="w-3 h-3" />{distanceStr}
        </span>
      )}
    </div>

From app/components/LiikuntapaikatLista.tsx (current — ~120 lines):
  Currently renders: PaikkaKortti for each filtered paikka, no distance.
  No GPS state currently.

  Add GPS to LiikuntapaikatLista:
  - Import useGPS from '@/hooks/useGPS'
  - Import haversineKm, formatDistance from '@/lib/geo'
  - Add const { coords } = useGPS() at top of component
    (Do NOT destructure requestLocation — lista does not have a GPS button;
     coords will only be non-null if the user used GPS in Kartta and then navigated to lista —
     actually, useGPS state is local to each component mount. Since lista mounts fresh,
     coords will always be null. This is acceptable for MAP-02 — the distance shows in
     Kartta bottom sheet (Plan 02) and in lista when the user has GPS. For lista specifically,
     add a small "Käytä sijaintiani" button in the hero/filter bar so users can enable GPS
     while in list view.)
  - Add a GPS button to the filter row in LiikuntapaikatLista:
      <button
        onClick={requestLocation}
        disabled={status === 'requesting'}
        className="shrink-0 flex items-center gap-1.5 px-3.5 py-2 rounded-full text-sm font-semibold
          border border-[rgba(0,0,0,0.1)] text-[rgba(17,17,17,0.6)] hover:text-[#111111]
          hover:border-[rgba(0,0,0,0.2)] [transition:color_150ms_var(--ease-out),border-color_150ms_var(--ease-out)]
          disabled:opacity-50"
      >
        <MapPin className="w-3.5 h-3.5" />
        {status === 'requesting' ? 'Haetaan...' : status === 'granted' ? 'Sijainti käytössä' : 'Etäisyydet'}
      </button>
    Import MapPin from lucide-react (check if already imported — it's not in current lista).
    Add to imports: import { Search, MapPin } from 'lucide-react'

  - Compute distancesMap:
      const distancesMap = useMemo<Record<string, number>>(() => {
        if (!coords) return {}
        return Object.fromEntries(
          paikat
            .filter((p): p is typeof p & { latitude: number; longitude: number } =>
              p.latitude != null && p.longitude != null)
            .map(p => [p.id, haversineKm(coords.lat, coords.lng, p.latitude, p.longitude)])
        )
      }, [coords, paikat])

  - Pass distanceStr to PaikkaKortti:
      <PaikkaKortti
        key={p.id}
        paikka={p}
        distanceStr={distancesMap[p.id] != null ? formatDistance(distancesMap[p.id]) : undefined}
      />

  - Import useCallback from react if needed (for GPS button). requestLocation from useGPS
    is already a stable useCallback — no need to wrap again.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Etusivu.tsx to @vis.gl and remove @react-google-maps/api</name>
  <files>
    app/components/Etusivu.tsx
    package.json
  </files>
  <action>
    In app/components/Etusivu.tsx:

    1. Remove import line 6: `import { GoogleMap, Marker, useJsApiLoader } from '@react-google-maps/api'`
    2. Add import: `import { Map, AdvancedMarker, useMap } from '@vis.gl/react-google-maps'`
    3. Remove the useJsApiLoader call (line 60-62) and the isLoaded variable entirely.
    4. Remove mapInstanceRef, previewMapRef (lines 57-58), onFullscreenMapLoad,
       onPreviewMapLoad callbacks (lines 75-82).
    5. Remove getMarkerIcon() function (lines 147-164).
    6. Remove ICON_SVG constant (lines 21-29) — used only in getMarkerIcon().
    7. Keep SPORT_ICONS (lines 31-34) — used in the bottom sheet for valittu icon.

    Add MapStyleController helper function (before the Etusivu component):
      function MapStyleController({ isDark }: { isDark: boolean }) {
        const map = useMap()
        useEffect(() => {
          if (!map) return
          map.setOptions({ styles: (isDark ? NIGHT_MAP_STYLES : DAY_MAP_STYLES) as google.maps.MapTypeStyle[] })
        }, [map, isDark])
        return null
      }

    Add SimplePin helper function (before the Etusivu component):
      function SimplePin({ laji }: { laji: string }) {
        const color = lajiKonfig[laji]?.color ?? '#6b7280'
        return (
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 28 38" width={28} height={38} style={{ display: 'block' }}>
            <path
              d="M14 0C6.268 0 0 6.268 0 14c0 5.25 2.875 9.83 7.125 12.3L14 38l6.875-11.7C25.125 23.83 28 19.25 28 14 28 6.268 21.732 0 14 0Z"
              fill={color}
            />
          </svg>
        )
      }

    Replace the old isDark useEffect that called mapInstanceRef/previewMapRef.setOptions
    (lines 70-73) with nothing — MapStyleController handles styles reactively as a child
    of each Map component. Remove those useEffect lines.

    Preview map section (inside AnimatePresence !kartaAuki branch):
    Replace:
      {isLoaded ? (
        <GoogleMap
          mapContainerStyle={{ width: '100%', height: '100%' }}
          center={TAMPERE}
          zoom={12}
          options={{ streetViewControl: false, mapTypeControl: false, fullscreenControl: false,
            zoomControl: false, disableDefaultUI: true, keyboardShortcuts: false,
            clickableIcons: false, gestureHandling: 'none',
            styles: (isDark ? NIGHT_MAP_STYLES : DAY_MAP_STYLES) as google.maps.MapTypeStyle[] }}
          onLoad={onPreviewMapLoad}
        >
          {paikatKartalla.map(p => (
            <Marker key={p.id} position={...} icon={getMarkerIcon(p, false)} />
          ))}
        </GoogleMap>
      ) : (
        <div className="w-full h-full bg-[#f0f0f0]" />
      )}
    With:
      <Map
        mapId="DEMO_MAP_ID"
        defaultCenter={TAMPERE}
        defaultZoom={12}
        style={{ width: '100%', height: '100%' }}
        disableDefaultUI
        gestureHandling="none"
        clickableIcons={false}
        keyboardShortcuts={false}
      >
        {paikatKartalla.map(p => (
          <AdvancedMarker
            key={p.id}
            position={{ lat: p.latitude, lng: p.longitude }}
          >
            <SimplePin laji={p.laji} />
          </AdvancedMarker>
        ))}
        <MapStyleController isDark={isDark} />
      </Map>

    Fullscreen map section (inside AnimatePresence kartaAuki branch):
    Replace the `{isLoaded && (<GoogleMap ...>...</GoogleMap>)}` block with:
      <Map
        mapId="DEMO_MAP_ID"
        defaultCenter={TAMPERE}
        defaultZoom={14}
        style={{ width: '100%', height: '100%' }}
        disableDefaultUI
        gestureHandling="greedy"
        clickableIcons={false}
        keyboardShortcuts={false}
        onClick={() => setValittu(null)}
      >
        {paikatKartalla.map(p => (
          <AdvancedMarker
            key={p.id}
            position={{ lat: p.latitude, lng: p.longitude }}
            zIndex={valittu?.id === p.id ? 10 : 1}
            onClick={() => setValittu(p)}
          >
            <SimplePin laji={p.laji} />
          </AdvancedMarker>
        ))}
        <MapStyleController isDark={isDark} />
      </Map>
    Remove the entire onAnimationComplete callback from the inner motion.div transition
    (the `window.google.maps.event.trigger` call) — @vis.gl handles resize automatically.
    Keep the Framer Motion transition config (duration: 0.45, ease: EASE_MAP) unchanged.

    After Etusivu.tsx is updated, run: npm uninstall @react-google-maps/api
    This removes the package from node_modules and package.json.

    Verify no file in the project still imports from @react-google-maps/api after uninstall.
  </action>
  <verify>
    <automated>cd C:\ClaudeCodeTestit\liikuntahakemisto && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <done>
    - Etusivu.tsx contains no import from '@react-google-maps/api'
    - Etusivu.tsx contains AdvancedMarker
    - @react-google-maps/api is absent from package.json
    - grep -r "@react-google-maps" app/ -- returns 0 results
    - TypeScript build passes
    - npm run dev: / route renders correctly, preview map shows, fullscreen map expansion works
  </done>
</task>

<task type="auto">
  <name>Task 2: Add distance string to PaikkaKortti and wire GPS into LiikuntapaikatLista</name>
  <files>
    app/components/PaikkaKortti.tsx
    app/components/LiikuntapaikatLista.tsx
  </files>
  <action>
    In app/components/PaikkaKortti.tsx:
    Add optional prop distanceStr?: string to the component props:
      export default function PaikkaKortti({ paikka, distanceStr }: { paikka: Liikuntapaikka; distanceStr?: string })

    In the bottom row div (the mt-auto flex row), wrap the right side (hinta and Lisätään pian)
    in a flex column that also shows distanceStr below it:
      <div className="flex flex-col items-end gap-0.5 shrink-0">
        {hinta ? (
          <span className="text-sm font-semibold text-[#111111] tabular-nums">{hinta}</span>
        ) : (
          <span className="text-xs text-[rgba(17,17,17,0.35)]">Lisätään pian</span>
        )}
        {distanceStr && (
          <span className="text-xs text-[rgba(17,17,17,0.4)] tabular-nums flex items-center gap-0.5">
            <MapPin className="w-3 h-3 shrink-0" />
            {distanceStr}
          </span>
        )}
      </div>
    MapPin is already imported in PaikkaKortti.tsx (line 5 of current file).
    When distanceStr is undefined (no GPS), the layout looks exactly as before.

    In app/components/LiikuntapaikatLista.tsx:
    1. Add imports:
       - import { useGPS } from '@/hooks/useGPS'
       - import { haversineKm, formatDistance } from '@/lib/geo'
       - Add MapPin to existing lucide-react import (Search is already imported)
       - Add useCallback to existing react import (useState, useMemo are already imported)

    2. Inside the component function, after the filter state:
       const { status, coords, requestLocation } = useGPS()

    3. Add distancesMap useMemo:
       const distancesMap = useMemo<Record<string, number>>(() => {
         if (!coords) return {}
         return Object.fromEntries(
           paikat
             .filter((p): p is typeof p & { latitude: number; longitude: number } =>
               p.latitude != null && p.longitude != null)
             .map(p => [p.id, haversineKm(coords.lat, coords.lng, p.latitude, p.longitude)])
         )
       }, [coords, paikat])

    4. Add GPS button to the filter area. In the current file, there is a search input row
       and sport filter pills row. Add the GPS button to the end of the sport filter pills
       row as a sibling pill. Position it after the last LAJIT_FILTTERI pill.
       The button should use the same pill visual style as the filter pills but with a
       MapPin icon and text:
         <motion.button
           onClick={requestLocation}
           disabled={status === 'requesting'}
           whileTap={{ scale: 0.95, transition: { duration: 0.1 } }}
           className={`shrink-0 flex items-center gap-1.5 px-3.5 py-2 rounded-full text-sm font-semibold
             [transition:background-color_150ms_var(--ease-out),color_150ms_var(--ease-out)]
             disabled:opacity-50
             ${status === 'granted'
               ? 'bg-[#111111] text-white'
               : 'border border-[rgba(0,0,0,0.1)] text-[rgba(17,17,17,0.6)] hover:text-[#111111] hover:border-[rgba(0,0,0,0.2)]'
             }`}
         >
           <MapPin className="w-3.5 h-3.5" />
           {status === 'requesting' ? 'Haetaan...' : status === 'granted' ? 'Sijainti päällä' : 'Etäisyydet'}
         </motion.button>
       Import motion from framer-motion (already imported in LiikuntapaikatLista via korttiVariants).
       Add motion to the existing framer-motion import.

    5. Update PaikkaKortti rendering to pass distanceStr:
       Change:
         <PaikkaKortti key={p.id} paikka={p} />
       To:
         <PaikkaKortti
           key={p.id}
           paikka={p}
           distanceStr={distancesMap[p.id] != null ? formatDistance(distancesMap[p.id]) : undefined}
         />

    Also add distanceStr to the Kartta.tsx bottom sheet (MAP-02 says "every venue card
    in list and map view"):
    In Kartta.tsx, the bottom sheet shows valittu. After Plan 02, coords is available
    in Kartta.tsx state. Add a distance line to the bottom sheet:
      {coords && valittu?.latitude != null && valittu?.longitude != null && (
        <p className="mt-1 flex items-center gap-1.5 text-xs text-[rgba(17,17,17,0.45)]">
          <MapPin className="w-3 h-3 shrink-0" />
          {formatDistance(haversineKm(coords.lat, coords.lng, valittu.latitude, valittu.longitude))}
        </p>
      )}
    Add this after the address line in the Kartta.tsx bottom sheet.
    Add imports to Kartta.tsx: import { haversineKm, formatDistance } from '@/lib/geo'
    (This touches Kartta.tsx again — same plan, acceptable since it completes MAP-02.)
  </action>
  <verify>
    <automated>cd C:\ClaudeCodeTestit\liikuntahakemisto && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <done>
    - PaikkaKortti accepts and renders distanceStr prop
    - LiikuntapaikatLista has useGPS, haversineKm, distancesMap
    - LiikuntapaikatLista GPS button visible in filter row
    - Kartta.tsx bottom sheet shows distance when GPS is active
    - TypeScript build passes
    - npm run dev: in /?nakyma=lista, tap "Etäisyydet" button → browser prompts → grant →
      distance strings appear on all venue cards with coordinates
    - In /?nakyma=kartta, tap a pin → bottom sheet shows distance when GPS is active
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User device → LiikuntapaikatLista GPS button | Same GPS trigger pattern as Kartta — must be onClick only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Information Disclosure | LiikuntapaikatLista — coords in state | mitigate | coords stays in useState only; passed to haversineKm() for distance computation, never serialized or transmitted |
| T-02-09 | Elevation of Privilege | GPS auto-request in lista | mitigate | requestLocation only in button onClick — same constraint as MAP-01; verified by grep for useEffect(.*requestLocation) |
| T-02-10 | Tampering | @react-google-maps/api removal | accept | Removal eliminates the dependency; no security impact, reduces attack surface |
| T-02-SC | Tampering | No new npm installs in this plan | accept | npm uninstall only; no new packages |
</threat_model>

<verification>
After both tasks complete:

1. npx tsc --noEmit exits 0
2. npm run lint exits 0
3. grep -r "@react-google-maps" app/ lib/ hooks/ -- returns 0 results
4. grep -r "useJsApiLoader" app/ -- returns 0 results
5. grep "distanceStr" app/components/PaikkaKortti.tsx -- returns match
6. grep "distancesMap" app/components/LiikuntapaikatLista.tsx -- returns match
7. grep "haversineKm" app/components/Kartta.tsx -- returns match
8. Manual: / (Etusivu) renders, preview map shows colored pins, fullscreen map opens
9. Manual: /?nakyma=lista shows all venue cards; tap "Etäisyydet" button → GPS prompt →
   after grant, each card shows distance e.g. "1,2 km"
10. Manual: /?nakyma=kartta → tap a pin → bottom sheet shows distance when GPS active
11. Manual: preview map on Etusivu opens and closes correctly (3D animation unchanged)
12. Manual: No "Google Maps JavaScript API loaded multiple times" warning in browser console
</verification>

<success_criteria>
- @react-google-maps/api is fully removed from the project
- No useJsApiLoader call exists anywhere in the codebase
- Distance strings appear on venue cards in list view after GPS grant
- Distance appears in Kartta bottom sheet after GPS grant
- Etusivu preview and fullscreen maps render with colored pins
- Fullscreen map 3D animation still works (no onAnimationComplete regression)
- TypeScript and lint both pass
- No double-load flash: pins render in a single cycle with APIProvider
</success_criteria>

<output>
Create .planning/phases/02-map-and-gps/02-03-SUMMARY.md when done
</output>
