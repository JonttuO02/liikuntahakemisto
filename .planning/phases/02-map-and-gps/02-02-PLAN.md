---
phase: 02-map-and-gps
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - app/components/Kartta.tsx
  - app/page.tsx
  - app/components/BottomNav.tsx
autonomous: true
requirements:
  - MAP-01
  - MAP-03

must_haves:
  truths:
    - "Navigating to /?nakyma=kartta renders Kartta.tsx (not Etusivu)"
    - "Kartta.tsx renders venue pins using AdvancedMarker — no OverlayView, no useJsApiLoader"
    - "GPS button is visible on the map and triggers browser location prompt on tap"
    - "Map centers on user position after GPS grant; silently stays on Tampere if denied"
    - "Day/night toggle works — map styles change without page reload"
    - "BottomNav middle tab links to /?nakyma=kartta and is active on that route"
  artifacts:
    - path: "app/components/Kartta.tsx"
      provides: "Full-screen map view with GPS and AdvancedMarker pins"
      contains: "useGPS"
    - path: "app/page.tsx"
      provides: "Route wiring for ?nakyma=kartta"
      contains: "nakyma === 'kartta'"
    - path: "app/components/BottomNav.tsx"
      provides: "Kartta tab replacing Lista tab"
      contains: "nakyma=kartta"
  key_links:
    - from: "app/page.tsx"
      to: "app/components/Kartta.tsx"
      via: "lazy import + searchParams.nakyma === 'kartta'"
      pattern: "nakyma.*kartta"
    - from: "app/components/Kartta.tsx"
      to: "hooks/useGPS.ts"
      via: "useGPS() call"
      pattern: "useGPS"
    - from: "app/components/Kartta.tsx"
      to: "@vis.gl/react-google-maps"
      via: "Map + AdvancedMarker imports"
      pattern: "AdvancedMarker"
---

<objective>
Rewrite Kartta.tsx to use @vis.gl/react-google-maps with AdvancedMarker pins and the useGPS hook. Wire /?nakyma=kartta in page.tsx to render Kartta. Update BottomNav to replace "Lista" with "Kartta". After this plan the full-screen map view is live at /?nakyma=kartta with working GPS.

Purpose: This is the primary user-facing delivery of Phase 2 — the GPS map experience. Etusivu.tsx is left for Plan 03.

Output: Kartta.tsx rewritten, page.tsx routing updated, BottomNav updated.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\ClaudeCodeTestit\liikuntahakemisto\.planning\phases\02-map-and-gps\02-01-SUMMARY.md

<!-- Key interfaces the executor needs -->
<interfaces>
From hooks/useGPS.ts (created in Plan 01):
  type GPSStatus = 'idle' | 'requesting' | 'granted' | 'denied' | 'unavailable' | 'timeout'
  interface GPSState { status: GPSStatus; coords: { lat: number; lng: number } | null; requestLocation: () => void }
  export function useGPS(): GPSState

From lib/constants.ts (created in Plan 01):
  export const TAMPERE = { lat: 61.4978, lng: 23.761 }

From @vis.gl/react-google-maps (installed in Plan 01):
  import { Map, AdvancedMarker, useMap } from '@vis.gl/react-google-maps'
  // APIProvider is in layout.tsx — do NOT import it in Kartta.tsx

  Map props used in Kartta:
    mapId="DEMO_MAP_ID"       -- required for AdvancedMarker to render
    defaultCenter={TAMPERE}   -- initial center (uncontrolled)
    defaultZoom={14}
    style={{ width: '100%', height: '100%' }}
    disableDefaultUI
    gestureHandling="greedy"
    onClick={() => setValittu(null)}
  
  center (controlled) is used ONLY after GPS resolves to pan programmatically.
  Use map.panTo(coords) via useMap() hook — do not set center as a prop on re-render.
  Using center as a controlled prop would re-pan on every filter interaction.

  AdvancedMarker props:
    key={p.id}
    position={{ lat: p.latitude, lng: p.longitude }}
    zIndex={valittu?.id === p.id ? 999 : index + 1}
    onClick={() => handlePinClick(p)}
  Children: the SportPin component body (motion.div — see below)

From app/components/Kartta.tsx (current state — to be rewritten):
  - SportPin component (lines 22-122): the inner motion.div block is kept AS-IS
    except the OverlayView wrapper is removed. The motion.div becomes the direct
    child of AdvancedMarker.
  - Remove: OverlayView import, PIN_OFFSET constant, the OverlayView JSX wrapper
  - Keep: SportPin function itself (lines 22-122 content), bottom sheet, day/night toggle

  SportPin signature (keep unchanged):
    function SportPin({
      paikka, index, isActive, isDark, onClick,
    }: {
      paikka: Liikuntapaikka & { latitude: number; longitude: number }
      index: number
      isActive: boolean
      isDark: boolean
      onClick: () => void
    })
  SportPin no longer renders its own OverlayView — it renders only the motion.div
  and its children. The AdvancedMarker in the parent handles positioning.

  MapStyleController (new helper component inside Kartta.tsx):
    function MapStyleController({ isDark }: { isDark: boolean }) {
      const map = useMap()
      useEffect(() => {
        if (!map) return
        map.setOptions({ styles: (isDark ? NIGHT_MAP_STYLES : DAY_MAP_STYLES) as google.maps.MapTypeStyle[] })
      }, [map, isDark])
      return null
    }
  Render <MapStyleController isDark={isDark} /> as a child of <Map>.

From app/lib/mapStyles.ts (existing, unchanged):
  export { DAY_MAP_STYLES, NIGHT_MAP_STYLES, isNightHour }

From app/lib/lajit.ts (existing, unchanged):
  export { lajiKonfig, LAJIT_FILTTERI }

From app/page.tsx (current state):
  Line 3-4: imports Etusivu and LiikuntapaikatLista (no Kartta import)
  Line 26-38: if nakyma === 'lista' → LiikuntapaikatLista, else → Etusivu

  Update needed:
  Add lazy Kartta import:
    import { lazy, Suspense } from 'react'
    const Kartta = lazy(() => import('./components/Kartta'))
  Add route branch BEFORE the else-Etusivu:
    if (searchParams.nakyma === 'kartta') {
      return (
        <Suspense fallback={<div className="w-full h-screen bg-white" />}>
          <Kartta paikat={data} />
        </Suspense>
      )
    }
  Note: page.tsx is a Server Component; lazy() from React is fine here since Next.js
  supports it. Alternatively use next/dynamic — either works. Prefer React.lazy +
  Suspense per CLAUDE.md constraint.

From app/components/BottomNav.tsx (current state):
  Tab 2 (middle): href="/?nakyma=lista", isLista = nakyma === 'lista', icon List, label "Lista"
  
  Change to:
    href="/?nakyma=kartta"
    isKartta = nakyma === 'kartta'
    icon: Map from lucide-react (import { Home, Map, Heart })
    label: "Kartta"
    strokeWidth: isKartta ? 2.2 : 1.8
  Remove isLista variable entirely.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Kartta.tsx with @vis.gl Map, AdvancedMarker, and useGPS</name>
  <files>app/components/Kartta.tsx</files>
  <action>
    Rewrite Kartta.tsx. Keep SportPin's inner body (the motion.div from line 47 to line 119
    of the original) but remove the OverlayView wrapper. Remove GoogleMap, OverlayView,
    and useJsApiLoader imports entirely.

    New imports needed:
      import { Map, AdvancedMarker, useMap } from '@vis.gl/react-google-maps'
      import { useGPS } from '@/hooks/useGPS'
      import { TAMPERE } from '@/lib/constants'
      // Remove: import { GoogleMap, OverlayView, useJsApiLoader } from '@react-google-maps/api'
      // Keep all other existing imports (useState, useEffect, useRef removed if unused,
      //   useCallback, motion, AnimatePresence, Activity, Building2, Dumbbell, Leaf,
      //   MapPin, Moon, Sun, Target, Waves, X, Zap, LucideIcon, lajiKonfig, hintateksti,
      //   Liikuntapaikka, DAY_MAP_STYLES, NIGHT_MAP_STYLES, isNightHour)
      // Add: MapPin from lucide-react for GPS button

    SportPin component:
    - Remove the OverlayView JSX wrapper (lines 37-41 and 120-122 in original)
    - Remove PIN_OFFSET constant
    - Remove OverlayView from imports
    - The component now returns the motion.div directly (no outer wrapper)
    - All props stay the same

    MapStyleController (new, inside the file):
    function MapStyleController({ isDark }: { isDark: boolean }) {
      const map = useMap()
      useEffect(() => {
        if (!map) return
        map.setOptions({ styles: (isDark ? NIGHT_MAP_STYLES : DAY_MAP_STYLES) as google.maps.MapTypeStyle[] })
      }, [map, isDark])
      return null
    }

    Kartta component changes:
    - Remove: useJsApiLoader call, isLoaded state/guard, mapRef, onMapLoad callback
    - Add: const { status, coords, requestLocation } = useGPS()
    - The map uses defaultCenter and defaultZoom (uncontrolled initial position)
    - After GPS resolves (status === 'granted'), pan the map programmatically.
      Use a useRef<google.maps.Map | null> to hold the map instance for panTo.
      Acquire via a MapPanController child component using useMap():
        function MapPanController({ coords }: { coords: { lat: number; lng: number } | null }) {
          const map = useMap()
          useEffect(() => {
            if (!map || !coords) return
            map.panTo(coords)
          }, [map, coords])
          return null
        }
      Render <MapPanController coords={coords} /> as a child of <Map>.
      This avoids setting center as a controlled prop (which would re-pan on every re-render).

    GPS button (inside the map container, absolute positioned):
    - Position: absolute top-3 right-14 (leave room for day/night toggle)
    - Show "Käytä sijaintiani" when status is 'idle', 'denied', 'unavailable', 'timeout'
    - Show a loading indicator (spinner or text "Haetaan...") when status is 'requesting'
    - When status is 'granted', show a filled location icon (MapPin with fill) to indicate
      GPS is active — button remains tappable to re-request
    - onClick={requestLocation}
    - NEVER call requestLocation in a useEffect
    - Apply glass-btn styling consistent with the day/night toggle button
    - GPS button is ALWAYS visible (not conditional on valittu state)

    Map JSX:
    <div className="relative w-full h-[calc(100svh-56px)] mt-0">
      <Map
        mapId="DEMO_MAP_ID"
        defaultCenter={TAMPERE}
        defaultZoom={14}
        style={{ width: '100%', height: '100%' }}
        disableDefaultUI
        gestureHandling="greedy"
        onClick={() => setValittu(null)}
      >
        {paikatKartalla.map((p, i) => (
          <AdvancedMarker
            key={p.id}
            position={{ lat: p.latitude, lng: p.longitude }}
            zIndex={valittu?.id === p.id ? 999 : i + 1}
            onClick={() => setValittu(valittu?.id === p.id ? null : p)}
          >
            <SportPin
              paikka={p}
              index={i}
              isActive={valittu?.id === p.id}
              isDark={isDark}
              onClick={() => setValittu(valittu?.id === p.id ? null : p)}
            />
          </AdvancedMarker>
        ))}
        <MapStyleController isDark={isDark} />
        <MapPanController coords={coords} />
      </Map>

      {/* Day/Night toggle — keep from original, style unchanged */}
      {/* GPS button — new, top-3 right-14 */}
      {/* Bottom sheet for valittu — keep from original */}
    </div>

    The loading fallback (when isLoaded was false in the old code) is removed entirely.
    APIProvider in layout.tsx ensures the Maps API is ready before Map mounts.
    If the API is not yet loaded, Map renders an empty div — this is acceptable.

    paikatKartalla filter stays the same (filter by latitude/longitude not null).

    Day/night toggle and bottom sheet: keep these exactly from the original Kartta.tsx
    (lines 191-282). No changes to those sections.

    Height: Use h-[calc(100svh-56px)] to fill the screen below the 56px NavBar.
    This replaces the original h-[520px] fixed height.
  </action>
  <verify>
    <automated>cd C:\ClaudeCodeTestit\liikuntahakemisto && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <done>
    - Kartta.tsx contains no import from '@react-google-maps/api'
    - Kartta.tsx contains AdvancedMarker import from '@vis.gl/react-google-maps'
    - Kartta.tsx contains useGPS import from '@/hooks/useGPS'
    - TypeScript build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire /?nakyma=kartta route and update BottomNav</name>
  <files>
    app/page.tsx
    app/components/BottomNav.tsx
  </files>
  <action>
    In app/page.tsx:
    Add lazy import for Kartta. page.tsx is a Server Component so use next/dynamic
    (more natural in Next.js App Router Server Components than React.lazy):
      import dynamic from 'next/dynamic'
      const Kartta = dynamic(() => import('./components/Kartta'), {
        ssr: false,
        loading: () => <div className="w-full h-screen bg-white" />,
      })
    Remove import of the static Kartta if one was added. Use only dynamic import.

    Add the kartta route branch after the lista branch and before the final return:
      if (searchParams.nakyma === 'kartta') {
        return <Kartta paikat={data} />
      }

    The final return (Etusivu) remains unchanged.

    In app/components/BottomNav.tsx:
    - Remove: import { Home, List, Heart } from 'lucide-react'
    - Add:    import { Home, Map, Heart } from 'lucide-react'
    - Remove: isLista variable
    - Add:    const isKartta = pathname === '/' && nakyma === 'kartta'
    - Change middle tab:
        href="/?nakyma=kartta"
        active condition: isKartta
        icon: <Map className="w-5 h-5" strokeWidth={isKartta ? 2.2 : 1.8} />
        label: "Kartta"
    - The active/inactive color constants (ACTIVE, INACTIVE) remain the same.
    - Suosikit tab is unchanged.
    - Koti tab is unchanged.
  </action>
  <verify>
    <automated>cd C:\ClaudeCodeTestit\liikuntahakemisto && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <done>
    - app/page.tsx has a branch for nakyma === 'kartta' rendering Kartta
    - app/components/BottomNav.tsx middle tab links to /?nakyma=kartta with Map icon and "Kartta" label
    - No reference to /?nakyma=lista remains in BottomNav
    - TypeScript build passes
    - npm run dev: navigating to /?nakyma=kartta shows the Kartta map component
    - GPS button visible on the map
    - Tapping GPS button triggers browser location prompt (on HTTPS/localhost)
    - After grant: map pans to user position
    - After deny: map stays on Tampere, no error message shown
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User tap → GPS request | User gesture is required — no auto-request |
| GPS coords → React state | Coords stay client-side only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | Kartta.tsx — GPS coords in state | mitigate | coords in useState only; never passed to any API route, never in URL, never in console.log |
| T-02-06 | Elevation of Privilege | GPS auto-request bypass | mitigate | requestLocation is only called from button onClick — no useEffect([]) call; CLAUDE.md constraint enforced by code review in verify step |
| T-02-07 | Tampering | varauslinkki hrefs in bottom sheet | accept | Existing rel="noopener noreferrer" on all external links; inherited from original Kartta.tsx bottom sheet |
| T-02-SC | Tampering | No new npm installs in this plan | accept | No new packages; @vis.gl already installed in Plan 01 |
</threat_model>

<verification>
After both tasks complete:

1. npx tsc --noEmit exits 0
2. npm run lint exits 0
3. grep -r "OverlayView" app/components/Kartta.tsx -- returns 0 results
4. grep -r "useJsApiLoader" app/components/Kartta.tsx -- returns 0 results
5. grep "AdvancedMarker" app/components/Kartta.tsx -- returns matches
6. grep "useGPS" app/components/Kartta.tsx -- returns matches
7. grep "nakyma=kartta" app/page.tsx -- returns match
8. grep "nakyma=kartta" app/components/BottomNav.tsx -- returns match
9. grep "nakyma=lista" app/components/BottomNav.tsx -- returns 0 results (removed)
10. Manual: navigate to /?nakyma=kartta — map renders with pins
11. Manual: BottomNav middle tab shows map icon and "Kartta" label
12. Manual: GPS button visible, triggers prompt on tap
</verification>

<success_criteria>
- /?nakyma=kartta renders Kartta.tsx full-screen map
- All venue pins appear using AdvancedMarker (not Marker/OverlayView)
- GPS button triggers location prompt on user tap, never on mount
- Tampere fallback is silent — no error state shown when GPS denied
- Day/night toggle applies map styles
- BottomNav Kartta tab navigates to /?nakyma=kartta
- TypeScript and lint both pass
</success_criteria>

<output>
Create .planning/phases/02-map-and-gps/02-02-SUMMARY.md when done
</output>
