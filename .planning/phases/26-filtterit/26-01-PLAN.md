---
phase: 26-filtterit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Etusivu.tsx
autonomous: true
requirements:
  - FILTER-02

must_haves:
  truths:
    - "Filtering by sport uses string[] comparison — searchLaji.length === 0 means 'all'"
    - "sessionStorage saves with _v: 2; any stored state without _v: 2 is discarded on restore"
    - "searchKertakaynti and searchAukinyt state variables no longer exist in Etusivu.tsx"
    - "getOpenStatus and isMembershipOnly imports are removed from Etusivu.tsx"
    - "Reset button sets searchLaji to [] and searchKaupunki to 'Kaikki'"
  artifacts:
    - path: "app/components/Etusivu.tsx"
      provides: "Refactored filter state and sessionStorage logic"
      contains: "searchLaji: string[]"
  key_links:
    - from: "searchLaji state"
      to: "searchSuodatettu useMemo"
      via: "searchLaji.length === 0 || searchLaji.includes(p.laji.toLowerCase())"
      pattern: "searchLaji\\.length === 0"
    - from: "handleCardClick"
      to: "sessionStorage"
      via: "_v: 2 field in saved state object"
      pattern: "_v: 2"
    - from: "sessionStorage restore useEffect"
      to: "setSearchLaji"
      via: "Array.isArray(s.searchLaji) guard"
      pattern: "Array\\.isArray\\(s\\.searchLaji\\)"
---

<objective>
Refactor Etusivu.tsx filter state: remove dead searchKertakaynti and searchAukinyt state, change searchLaji from string to string[], update all six call sites atomically, and migrate sessionStorage to version _v: 2. This is the data model foundation that Plan 26-02 (carousel UI) builds on.

Purpose: FILTER-02 compliance — simplify filter surface to paikkakunta + laji only; prevent dead filter states from leaking through sessionStorage across sessions.
Output: Etusivu.tsx with clean two-filter state model and _v: 2 session versioning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-filtterit/26-CONTEXT.md
@.planning/phases/26-filtterit/26-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead filter state and update searchLaji type</name>
  <files>app/components/Etusivu.tsx</files>

  <read_first>
    app/components/Etusivu.tsx — read lines 1–30 (imports), 248–270 (state declarations), 670–700 (searchSuodatettu + isFilterActive), 1380–1400 (reset button onClick) before making any changes.
  </read_first>

  <action>
    Make six atomic changes in app/components/Etusivu.tsx. Do them in order:

    1. IMPORTS (lines 14–15): Remove the two import lines for getOpenStatus and isMembershipOnly — they have no callers outside the filter logic being deleted.
       Remove: `import { getOpenStatus } from '@/lib/aukiolo'`
       Remove: `import { isMembershipOnly } from '@/lib/priceUtils'`

    2. STATE DECLARATIONS (lines 253–256): Replace the four filter state lines with two:
       Remove: `const [searchLaji, setSearchLaji] = useState('Kaikki')`
       Remove: `const [searchAukinyt, setSearchAukinyt] = useState(false)`
       Remove: `const [searchKertakaynti, setSearchKertakaynti] = useState(false)`
       Add: `const [searchLaji, setSearchLaji] = useState<string[]>([])`
       Keep: `const [searchKaupunki, setSearchKaupunki] = useState('Kaikki')` (unchanged)

    3. searchSuodatettu useMemo (lines ~675–684): Replace the filter predicate and dependency array per D-06. The new predicate removes matchesKertakaynti and matchesAuki entirely and uses:
       `const matchesLaji = searchLaji.length === 0 || searchLaji.includes(p.laji.toLowerCase())`
       Dependency array becomes: `[paikat, searchLaji, searchHaku, searchKaupunki]`

    4. isFilterActive (line ~686): Replace with D-07 expression:
       `const isFilterActive = searchLaji.length > 0 || searchKaupunki !== 'Kaikki'`

    5. "Tyhjennä haku" reset button onClick (lines ~1382–1388): Replace the five-setter block with three setters per D-08/D-09:
       `setSearchHaku('')`
       `setSearchLaji([])`
       `setSearchKaupunki('Kaikki')`
       Remove: `setSearchKertakaynti(false)` and `setSearchAukinyt(false)`

    Do NOT touch the filter row JSX (the `<select>` elements and buttons at lines ~1318–1357) — that is Plan 26-02's job.

    Note: After removing searchAukinyt and searchKertakaynti state, TypeScript will report errors if their names appear anywhere else in the file. Search for any remaining references to `searchAukinyt`, `searchKertakaynti`, `setSearchAukinyt`, `setSearchKertakaynti` and remove them. The only expected sites are the four state declarations, the two useMemo lines, and the reset button — all addressed above.
  </action>

  <verify>
    <automated>cd /c/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - Etusivu.tsx contains `useState&lt;string[]&gt;([])` for searchLaji (not `useState('Kaikki')`)
    - Etusivu.tsx does NOT contain any of: `searchKertakaynti`, `searchAukinyt`, `setSearchKertakaynti`, `setSearchAukinyt`
    - Etusivu.tsx does NOT import `getOpenStatus` or `isMembershipOnly`
    - Etusivu.tsx contains `searchLaji.length === 0 || searchLaji.includes(p.laji.toLowerCase())` in searchSuodatettu
    - Etusivu.tsx contains `const isFilterActive = searchLaji.length > 0 || searchKaupunki !== 'Kaikki'`
    - Reset button onClick contains `setSearchLaji([])` (not `setSearchLaji('Kaikki')`)
    - `npx tsc --noEmit` exits with 0 errors related to the changed code (filter state removal must not introduce new TS errors)
  </acceptance_criteria>

  <done>searchLaji is typed string[], dead filter state is fully removed, TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate sessionStorage to _v: 2</name>
  <files>app/components/Etusivu.tsx</files>

  <read_first>
    app/components/Etusivu.tsx — read lines 337–360 (handleCardClick / sessionStorage save) and lines 444–480 (sessionStorage restore useEffect) before making changes.
  </read_first>

  <action>
    Two targeted replacements in app/components/Etusivu.tsx:

    1. handleCardClick sessionStorage save (lines ~340–349): Add `_v: 2` as first field in the state object and remove searchKertakaynti and searchAukinyt fields. The new state object is:
       `{ _v: 2, scrollTop, searchHaku, searchLaji, searchKaupunki, searchOpen: true }`
       searchLaji is now string[] — no type coercion needed, JSON.stringify handles arrays correctly.

    2. sessionStorage restore useEffect (lines ~444–480): After parsing and validating that `s` is a non-null object, add a version guard as the first check:
       `if (s._v !== 2) { sessionStorage.removeItem('etusivu-scroll-state'); return; }`
       This line goes immediately after the `if (typeof s !== 'object' || s === null) return` guard.
       Then remove the two lines that restore searchKertakaynti and searchAukinyt:
         Remove: `if (typeof s.searchKertakaynti === 'boolean') setSearchKertakaynti(s.searchKertakaynti)`
         Remove: `if (typeof s.searchAukinyt === 'boolean') setSearchAukinyt(s.searchAukinyt)`
       Change the searchLaji restore line from the string type-guard to an Array.isArray guard:
         Remove: `if (typeof s.searchLaji === 'string') setSearchLaji(s.searchLaji)`
         Add: `if (Array.isArray(s.searchLaji)) setSearchLaji(s.searchLaji)`
       All other restore lines (searchHaku, searchKaupunki, scrollTop, searchOpen, sheetVisible, suppressAutoOpenRef) remain unchanged.
  </action>

  <verify>
    <automated>cd /c/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - handleCardClick state object contains `_v: 2` as a field (grep: `_v: 2`)
    - handleCardClick state object does NOT contain `searchKertakaynti` or `searchAukinyt` as fields
    - sessionStorage restore useEffect contains `if (s._v !== 2)` version guard
    - sessionStorage restore contains `Array.isArray(s.searchLaji)` (not `typeof s.searchLaji === 'string'`)
    - sessionStorage restore does NOT contain lines referencing `searchKertakaynti` or `searchAukinyt`
    - `npx tsc --noEmit` exits with 0 errors
  </acceptance_criteria>

  <done>sessionStorage saves _v: 2 and rejects any state without it; searchLaji restores as string[].</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessionStorage → React state | Parsed JSON from sessionStorage is untrusted; type guards prevent invalid state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | sessionStorage restore | mitigate | _v: 2 version guard + Array.isArray type check; non-conforming data is discarded entirely |
| T-26-02 | Information Disclosure | Dead filter state | mitigate | searchKertakaynti/searchAukinyt removed from all sessionStorage save/restore paths — no dead state can leak |
</threat_model>

<verification>
After both tasks complete:
- `npx tsc --noEmit` returns 0 errors
- `grep -c "searchKertakaynti\|searchAukinyt" app/components/Etusivu.tsx` returns 0
- `grep -c "isMembershipOnly\|getOpenStatus" app/components/Etusivu.tsx` returns 0
- `grep "searchLaji" app/components/Etusivu.tsx | grep -v "string\[\]"` shows only uses with .length or .includes (not string comparison)
- `grep "_v: 2" app/components/Etusivu.tsx` returns at least 2 matches (save + restore guard)
</verification>

<success_criteria>
- FILTER-02: Dead filter UI (Kertakäynti OK, Auki nyt) state is fully removed from Etusivu.tsx
- FILTER-02: searchLaji is string[] throughout — state, save, restore, filter logic, reset
- FILTER-02: sessionStorage _v: 2 migration complete; old session data cannot restore dead filter state
- TypeScript compiles without errors introduced by this plan
</success_criteria>

<output>
Create `.planning/phases/26-filtterit/26-01-SUMMARY.md` when done
</output>
