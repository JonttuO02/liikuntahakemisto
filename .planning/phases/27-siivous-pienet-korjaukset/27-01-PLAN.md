---
phase: 27-siivous-pienet-korjaukset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/NavBar.tsx
  - app/components/NavPill.tsx
  - app/suosikit/page.tsx
  - app/suosikit/SuosikitClient.tsx
autonomous: true
requirements:
  - NAV-06
  - NAV-07

must_haves:
  truths:
    - "Visiting /suosikit returns a 404 (route does not exist)"
    - "NavBar expanded menu no longer shows a TO DO link"
    - "NavPill expanded menu no longer shows a TO DO link"
    - "Supabase suosikit queries in Etusivu.tsx are NOT touched — the TODO overlay still works"
  artifacts:
    - path: "app/suosikit/page.tsx"
      provides: "DELETED — must not exist"
    - path: "app/suosikit/SuosikitClient.tsx"
      provides: "DELETED — must not exist"
    - path: "app/components/NavBar.tsx"
      provides: "NavBar without /suosikit link"
      contains: "no href=\"/suosikit\""
    - path: "app/components/NavPill.tsx"
      provides: "NavPill without /suosikit link"
      contains: "no href=\"/suosikit\""
  key_links:
    - from: "NavBar.tsx expanded menu"
      to: "removed"
      via: "delete Link block with href=/suosikit (lines ~89-98)"
      pattern: "no href.*suosikit in NavBar.tsx"
    - from: "NavPill.tsx expanded menu"
      to: "removed"
      via: "delete Link block with href=/suosikit (lines ~70-73)"
      pattern: "no href.*suosikit in NavPill.tsx"
---

<objective>
Remove the /suosikit route entirely and scrub every navigation link that points to it.

Purpose: The /suosikit page was the standalone TO DO list page. It has been superseded by the inline TODO overlay on the main screen. The route and its nav links are dead and should not exist.

Output: app/suosikit/ directory deleted, NavBar.tsx and NavPill.tsx stripped of the /suosikit Link blocks.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/PROJECT.md
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/ROADMAP.md
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/phases/27-siivous-pienet-korjaukset/27-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete /suosikit route files</name>
  <files>app/suosikit/page.tsx, app/suosikit/SuosikitClient.tsx</files>
  <read_first>
    - app/suosikit/page.tsx — read before deleting so the deletion is intentional
    - app/suosikit/SuosikitClient.tsx — same
    - .planning/phases/27-siivous-pienet-korjaukset/27-CONTEXT.md — D-01: delete both files, D-03: Supabase suosikit queries in Etusivu.tsx are NOT touched
  </read_first>
  <action>
    Delete both files using the filesystem:
    - Delete `app/suosikit/page.tsx`
    - Delete `app/suosikit/SuosikitClient.tsx`

    Do NOT remove or touch anything in `app/components/Etusivu.tsx` — the Supabase queries that read from the `suosikit` table in Etusivu.tsx power the TODO overlay and must remain intact (per D-03).

    After deletion the `app/suosikit/` directory should be empty (Next.js does not require an explicit directory deletion).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const missing=['app/suosikit/page.tsx','app/suosikit/SuosikitClient.tsx']; missing.forEach(f=>{if(fs.existsSync(f))throw new Error(f+' still exists')}); console.log('OK: both files deleted')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/suosikit/page.tsx` does not exist on disk
    - `app/suosikit/SuosikitClient.tsx` does not exist on disk
    - `app/components/Etusivu.tsx` is not modified (no diff on this file)
  </acceptance_criteria>
  <done>Both suosikit route files are absent from the repository.</done>
</task>

<task type="auto">
  <name>Task 2: Remove /suosikit nav links from NavBar.tsx and NavPill.tsx</name>
  <files>app/components/NavBar.tsx, app/components/NavPill.tsx</files>
  <read_first>
    - app/components/NavBar.tsx — the /suosikit link is the `clientEmail && (...)` block at lines ~89-98; it wraps a `<Link href="/suosikit">` with a Bookmark icon and "TO DO" label
    - app/components/NavPill.tsx — the /suosikit link is the `<Link href="/suosikit">` at lines ~70-73 inside the `user && (...)` block; "Profiili" link above it must remain
    - .planning/phases/27-siivous-pienet-korjaukset/27-CONTEXT.md — D-02, D-04: remove both links; D-05: the fixed TodoButton in Etusivu.tsx STAYS (this plan does not touch Etusivu.tsx)
  </read_first>
  <action>
    NavBar.tsx — lines ~89-98:
    Remove the entire conditional block:
    ```
    {clientEmail && (
      <Link
        href="/suosikit"
        onClick={() => setOpen(false)}
        className="..."
      >
        <Bookmark className="w-3.5 h-3.5" />
        TO DO
      </Link>
    )}
    ```
    After removal, if the `Bookmark` icon from `lucide-react` is no longer imported anywhere in NavBar.tsx, remove it from the import line too (check for other usages first).

    NavPill.tsx — lines ~70-73 (inside the `user && (...)` block):
    Remove the Link block:
    ```
    <Link href="/suosikit" onClick={() => setOpen(false)} className={BTN}>
      <Bookmark className="w-3.5 h-3.5" />
      TO DO
    </Link>
    ```
    The `<Link href="/profiili">` block immediately above it must remain untouched.
    After removal, if `Bookmark` is no longer used anywhere in NavPill.tsx, remove it from the import line.

    Do NOT remove the comment on line 63 of NavPill.tsx that mentions `/suosikit` — that comment is a note about guest guards; removing it would lose context. Actually: check if the comment now refers to a deleted file and is therefore misleading — if so, remove or update it to only mention ProfiiliClient.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const nb=fs.readFileSync('app/components/NavBar.tsx','utf8'); const np=fs.readFileSync('app/components/NavPill.tsx','utf8'); if(nb.includes('href=\"/suosikit\"'))throw new Error('NavBar still has /suosikit'); if(np.includes('href=\"/suosikit\"'))throw new Error('NavPill still has /suosikit'); console.log('OK: both nav links removed')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/NavBar.tsx` does not contain `href="/suosikit"`
    - `app/components/NavPill.tsx` does not contain `href="/suosikit"`
    - `app/components/NavPill.tsx` still contains `href="/profiili"` (Profiili link untouched)
    - Unused `Bookmark` imports removed from each file if no other usage remains
    - TypeScript compiles without errors: `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Neither NavBar nor NavPill contains a link to /suosikit. The Profiili link in NavPill is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route existence | Removing a file-based route removes the HTTP endpoint — no additional access control needed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Tampering | app/suosikit/ deletion | accept | Deleting files is an idempotent operation; no data loss (suosikit table in Supabase unaffected) |
| T-27-01-02 | Information Disclosure | Dead route still accessible | mitigate | Deletion of page.tsx causes Next.js to return 404 for /suosikit automatically |
| T-27-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Run after both tasks:
1. `npx tsc --noEmit` — must pass with zero errors
2. `node -e "const fs=require('fs'); ['app/suosikit/page.tsx','app/suosikit/SuosikitClient.tsx'].forEach(f=>{if(fs.existsSync(f))throw new Error(f+' exists')}); console.log('files deleted OK')"` — must print "files deleted OK"
3. `node -e "const nb=require('fs').readFileSync('app/components/NavBar.tsx','utf8'); const np=require('fs').readFileSync('app/components/NavPill.tsx','utf8'); if(nb.includes('/suosikit')||np.includes('/suosikit'))throw new Error('link found'); console.log('links removed OK')"` — must print "links removed OK"
</verification>

<success_criteria>
- /suosikit route files are deleted from disk
- NavBar.tsx and NavPill.tsx contain no href="/suosikit" links
- TypeScript compiles cleanly
- The TODO overlay in Etusivu.tsx (fixed bookmark button + Supabase suosikit queries) is untouched
</success_criteria>

<output>
Create `.planning/phases/27-siivous-pienet-korjaukset/27-01-SUMMARY.md` when done.
</output>
