---
phase: 27-siivous-pienet-korjaukset
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/PaikkaSheet.tsx
autonomous: true
requirements:
  - SHEET-04

must_haves:
  truths:
    - "The sheet no longer has an 'Avaa paikkasivu selaimessa' link at the bottom"
    - "All other sheet content (booking link, phone, hours, price, reviews) is unchanged"
  artifacts:
    - path: "app/components/PaikkaSheet.tsx"
      provides: "PaikkaSheet without browser link"
      contains: "no 'Avaa paikkasivu selaimessa'"
  key_links:
    - from: "PaikkaSheet.tsx bottom of scroll container"
      to: "removed"
      via: "delete Link block with href=/paikat/${paikka.id} (lines ~192-199)"
      pattern: "no Avaa paikkasivu selaimessa in PaikkaSheet.tsx"
---

<objective>
Remove the "Avaa paikkasivu selaimessa →" link from the bottom of PaikkaSheet.tsx.

Purpose: The full venue page (/paikat/[id]) is no longer considered a user-facing destination in the v1.6 redesign. The link clutters the sheet footer with a muted dead-end link. Per D-15 there is no replacement.

Output: PaikkaSheet.tsx with the Link element at lines ~192-199 deleted.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/PROJECT.md
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/phases/27-siivous-pienet-korjaukset/27-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the "Avaa paikkasivu selaimessa" link from PaikkaSheet</name>
  <files>app/components/PaikkaSheet.tsx</files>
  <read_first>
    - app/components/PaikkaSheet.tsx — the link to remove is the `<Link href={`/paikat/${paikka.id}`} ...>Avaa paikkasivu selaimessa →</Link>` block at lines ~192-199, sitting just before the closing `</div>` of the px-4 pb-8 flex flex-col gap-4 content wrapper
    - .planning/phases/27-siivous-pienet-korjaukset/27-CONTEXT.md — D-15: remove with no replacement
  </read_first>
  <action>
    In `app/components/PaikkaSheet.tsx`, delete the entire Link block:

    ```
    {/* Link to full venue page */}
    <Link
      href={`/paikat/${paikka.id}`}
      className="text-center text-xs text-[rgba(17,17,17,0.35)] hover:text-[rgba(17,17,17,0.6)] [transition:color_150ms_var(--ease-out)] underline underline-offset-2 mt-2"
    >
      Avaa paikkasivu selaimessa →
    </Link>
    ```

    This includes the comment `{/* Link to full venue page */}` immediately above it.

    After deletion, check whether `Link` from `next/link` is still used elsewhere in PaikkaSheet.tsx. It is NOT used anywhere else in this file, so remove the `import Link from 'next/link'` import line as well to keep the file clean.

    Do not touch any other part of the file — specifically: the booking link button (`isSafeUrl(paikka.varauslinkki)` block), the SheetRow components, ReviewSection, and drag behavior all remain unchanged.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('app/components/PaikkaSheet.tsx','utf8'); if(c.includes('Avaa paikkasivu selaimessa'))throw new Error('link still present'); if(c.includes(\"import Link\"))throw new Error('unused Link import still present'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/PaikkaSheet.tsx` does not contain the text `Avaa paikkasivu selaimessa`
    - `app/components/PaikkaSheet.tsx` does not contain `import Link from 'next/link'` (no longer needed)
    - The `isSafeUrl(paikka.varauslinkki)` booking button block is still present
    - `ReviewSection` is still rendered
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>PaikkaSheet.tsx has no "Avaa paikkasivu selaimessa" link and no unused Link import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sheet → venue page | Link removal eliminates one navigation path; /paikat/[id] route itself is unaffected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Information Disclosure | /paikat/[id] route still accessible directly | accept | Route exists, removing UI link only; no security impact |
| T-27-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Run after task:
1. `npx tsc --noEmit` — must pass
2. `node -e "const c=require('fs').readFileSync('app/components/PaikkaSheet.tsx','utf8'); if(c.includes('Avaa paikkasivu'))throw new Error('FAIL'); console.log('OK')"` — must print OK
</verification>

<success_criteria>
- PaikkaSheet.tsx does not contain the browser link element
- Unused Link import is removed
- TypeScript compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/27-siivous-pienet-korjaukset/27-02-SUMMARY.md` when done.
</output>
