---
phase: 27-siivous-pienet-korjaukset
plan: 03
type: execute
wave: 2
depends_on:
  - 27-01
  - 27-02
files_modified:
  - app/components/Etusivu.tsx
autonomous: true
requirements:
  - FILTER-04
  - FILTER-05
  - SEARCH-01

must_haves:
  truths:
    - "The CombinedFilterPill container has a visible rgba(0,0,0,0.04) background tint"
    - "Tapping the map while the filter pill is visible but search is not focused does not get intercepted by the collapsed search input row"
    - "Searching with no results shows nothing — no 'Ei tuloksia' text and no 'Tyhjennä haku' button"
  artifacts:
    - path: "app/components/Etusivu.tsx"
      provides: "Etusivu with FILTER-04, FILTER-05, SEARCH-01 applied"
      contains: "rgba(0,0,0,0.04)"
  key_links:
    - from: "CombinedFilterPill outer motion.div (line ~334)"
      to: "rgba(0,0,0,0.04) background"
      via: "added to style prop"
      pattern: "rgba\\(0,0,0,0\\.04\\)"
    - from: "search-expand motion.div (line ~417)"
      to: "pointer-events: none when !listOpen"
      via: "conditional pointerEvents in style prop"
      pattern: "pointerEvents.*listOpen"
    - from: "Ei tuloksia / Tyhjennä haku block (lines ~1618-1633)"
      to: "deleted"
      via: "remove the else branch of the search results ternary"
      pattern: "no Ei tuloksia in Etusivu.tsx"
---

<objective>
Apply three targeted fixes to Etusivu.tsx: add a subtle background tint to the filter pill
container (FILTER-04), prevent the collapsed search input row from intercepting map touches
(FILTER-05), and remove the empty-state "Ei tuloksia" / "Tyhjennä haku" UI from search results
(SEARCH-01).

Purpose: FILTER-04 and FILTER-05 address visual and interaction regressions introduced when
the `.glass` class was removed from the filter pill. SEARCH-01 removes UI that was deemed
unnecessary — the user wants zero results to simply show nothing.

Output: Etusivu.tsx with three surgical edits; all other code untouched.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/PROJECT.md
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/phases/27-siivous-pienet-korjaukset/27-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FILTER-04 — add rgba(0,0,0,0.04) background to CombinedFilterPill container</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    - app/components/Etusivu.tsx lines 332–440 — the CombinedFilterPill return block; the outermost element is `motion.div` with `ref={pillRef}` and `style={{ position: 'relative', overflow: 'hidden', padding: 2 }}`
    - .planning/phases/27-siivous-pienet-korjaukset/27-CONTEXT.md — D-06: add `rgba(0,0,0,0.04)` as background on the pill container; the .glass class is already removed; no other background changes
  </read_first>
  <action>
    In `app/components/Etusivu.tsx`, locate the `CombinedFilterPill` function's return statement.
    The outermost `motion.div` has: `style={{ position: 'relative', overflow: 'hidden', padding: 2 }}`

    Add `background: 'rgba(0,0,0,0.04)'` to that same style object so it becomes:
    `style={{ position: 'relative', overflow: 'hidden', padding: 2, background: 'rgba(0,0,0,0.04)' }}`

    Do not change the inner `motion.div` (which has `style={{ background: '#ffffff', overflow: 'hidden' }}`).
    Do not add any class names. This is a style-prop-only change.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('app/components/Etusivu.tsx','utf8'); if(!c.includes(\"rgba(0,0,0,0.04)\"))throw new Error('background not added'); console.log('OK: FILTER-04')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/Etusivu.tsx` contains the string `rgba(0,0,0,0.04)`
    - The inner `motion.div` still has `background: '#ffffff'` (unchanged)
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>CombinedFilterPill outer container has the rgba(0,0,0,0.04) background tint per D-06.</done>
</task>

<task type="auto">
  <name>Task 2: FILTER-05 + SEARCH-01 — pointer-events fix on collapsed search row; remove empty-state UI</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    - app/components/Etusivu.tsx lines 415–437 — the `AnimatePresence` block containing `motion.div key="search-expand"` which collapses to `height: 0` when `!listOpen`; the inner div contains the Search icon and `input` element; `overflow: hidden` is already set on the motion.div style
    - app/components/Etusivu.tsx lines 1597–1636 — the search results `div.px-4.pb-4` block; the ternary at line 1600 renders cards when `searchSuodatettu.length > 0` else renders the "Ei tuloksia" + "Tyhjennä haku" block (lines 1618–1633)
    - .planning/phases/27-siivous-pienet-korjaukset/27-CONTEXT.md — D-07/D-08: add pointer-events:none to the hidden search input wrapper when search is not active; element stays in DOM; D-09: remove "Ei tuloksia" and "Tyhjennä haku" elements entirely, no replacement
  </read_first>
  <action>
    ### FILTER-05 — pointer-events fix

    Locate the `motion.div key="search-expand"` inside `CombinedFilterPill` (approximately lines 417–435).
    Its current style: `style={{ overflow: 'hidden' }}`

    Add a conditional `pointerEvents` property so the collapsed row cannot intercept touches:
    Change `style={{ overflow: 'hidden' }}` to `style={{ overflow: 'hidden', pointerEvents: listOpen ? undefined : 'none' }}`

    The `listOpen` prop is already in scope at that location (it is a prop of `CombinedFilterPill`).
    When `listOpen` is `false` the height animates to 0 AND pointer events are disabled.
    When `listOpen` is `true` pointer events are the default (undefined = inherited).

    ### SEARCH-01 — remove Ei tuloksia + Tyhjennä haku

    Locate the ternary inside the search results container (approximately lines 1600–1633):
    ```
    {!searchFocused && (searchSuodatettu.length > 0 ? (
      <div className="flex flex-col gap-3">
        {/* DiagonaalKortti list */}
      </div>
    ) : (
      <div className="flex flex-col items-center py-12">
        <p className="glass rounded-2xl px-6 py-4 text-[rgba(17,17,17,0.5)] text-sm">Ei tuloksia</p>
        <motion.button ...>Tyhjennä haku</motion.button>
      </div>
    ))}
    ```

    Remove the ternary's else branch entirely. Replace the whole ternary with a simple conditional render:
    ```
    {!searchFocused && searchSuodatettu.length > 0 && (
      <div className="flex flex-col gap-3">
        {searchSuodatettu.map(p => (
          <DiagonaalKortti ... />
        ))}
      </div>
    )}
    ```

    The "Ei tuloksia" `<p>` and the "Tyhjennä haku" `<motion.button>` must not exist anywhere in the file after this edit. The `setSearchHaku('')` / `setSearchLaji([])` / `setSearchKaupunki('Kaikki')` calls inside the deleted button are removed with it — do not move them elsewhere.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('app/components/Etusivu.tsx','utf8'); if(c.includes('Ei tuloksia'))throw new Error('Ei tuloksia still present'); if(c.includes('Tyhjennä haku'))throw new Error('Tyhjennä haku still present'); if(!c.includes('pointerEvents: listOpen'))throw new Error('pointer-events fix missing'); console.log('OK: FILTER-05 + SEARCH-01')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/Etusivu.tsx` does not contain the text `Ei tuloksia`
    - `app/components/Etusivu.tsx` does not contain the text `Tyhjennä haku`
    - `app/components/Etusivu.tsx` contains `pointerEvents: listOpen` (the conditional expression) on the search-expand motion.div style
    - The `DiagonaalKortti` card list inside the search results section still renders when `searchSuodatettu.length > 0`
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>
    The collapsed search input row has pointer-events:none when listOpen is false (FILTER-05).
    The empty-state block ("Ei tuloksia" + "Tyhjennä haku") is fully removed (SEARCH-01).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI only | All changes are presentational/interaction fixes with no data flow changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Tampering | Filter pill touch area | accept | pointer-events:none reduces attack surface (touch interception) — this is a security improvement |
| T-27-03-02 | Information Disclosure | Removed "Ei tuloksia" state | accept | No sensitive data involved; removing UI text has no security implications |
| T-27-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Run after both tasks:
1. `npx tsc --noEmit` — must pass with zero errors
2. `node -e "const c=require('fs').readFileSync('app/components/Etusivu.tsx','utf8'); const checks=[['rgba(0,0,0,0.04)','FILTER-04'],['pointerEvents: listOpen','FILTER-05'],]; checks.forEach(([s,n])=>{if(!c.includes(s))throw new Error(n+' missing: '+s)}); ['Ei tuloksia','Tyhjennä haku'].forEach(s=>{if(c.includes(s))throw new Error('SEARCH-01 FAIL: '+s+' still present')}); console.log('OK: all checks pass')"` — must print "OK: all checks pass"
</verification>

<success_criteria>
- CombinedFilterPill outer container has rgba(0,0,0,0.04) background (FILTER-04)
- Collapsed search input row has pointer-events:none when listOpen=false (FILTER-05)
- "Ei tuloksia" and "Tyhjennä haku" elements are fully removed from Etusivu.tsx (SEARCH-01)
- TypeScript compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/27-siivous-pienet-korjaukset/27-03-SUMMARY.md` when done.
</output>
