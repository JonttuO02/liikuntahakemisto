---
phase: "14"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/NavPill.tsx
  - app/components/Etusivu.tsx
autonomous: true
requirements:
  - AUTH-04

must_haves:
  truths:
    - "NavPill expanded menu shows Profiili link above Suosikit link"
    - "Etusivu inline expanding pill shows Profiili link above Suosikit link"
    - "Profiili link navigates to /profiili"
    - "Link is visible to all users regardless of auth state (per D-09)"
  artifacts:
    - path: "app/components/NavPill.tsx"
      provides: "Profiili link in NavPill expanded menu"
      contains: "href=\"/profiili\""
    - path: "app/components/Etusivu.tsx"
      provides: "Profiili link in inline pill expanded menu"
      contains: "href=\"/profiili\""
  key_links:
    - from: "app/components/NavPill.tsx"
      to: "/profiili"
      via: "Link href"
      pattern: "href=\"/profiili\""
    - from: "app/components/Etusivu.tsx"
      to: "/profiili"
      via: "Link href"
      pattern: "href=\"/profiili\""
---

<objective>
Add a "Profiili" navigation link to both active nav components: NavPill.tsx (used on all secondary pages) and the inline expanding pill in Etusivu.tsx (used on the homepage).

Purpose: Satisfies D-09 and D-10 — Profiili link visible to all users, positioned above Suosikit in the menu. This is independent of the database and profile page work, so it runs in Wave 1.

Output: Both nav components modified with a Profiili link using the User icon, placed before the Suosikit link.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-CONTEXT.md
@.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-PATTERNS.md

<interfaces>
<!-- From app/components/NavPill.tsx — current expanded content structure -->
From app/components/NavPill.tsx (PATTERNS.md NavPill section):
```tsx
// Line 10: const BTN = 'flex items-center gap-1.5 px-3 h-8 rounded-full glass-btn text-sm font-bold text-[rgba(17,17,17,0.7)] hover:text-[#111111] [transition:color_150ms_ease]'
// Line 6: import { Search, Heart, User, LogOut, MoreHorizontal, X } from 'lucide-react'
// User icon already imported — no new import needed

// Current expanded content order (lines 56–76):
// Haku link
// Suosikit link   <-- insert Profiili BEFORE this
// Kirjaudu ulos / Kirjaudu button
```

<!-- From app/components/Etusivu.tsx — inline pill expanded content -->
From app/components/Etusivu.tsx (PATTERNS.md Modification 1, lines 577–608):
```tsx
// User is already imported (line 7) — no new import needed
// Inline BTN class (line 580):
// "flex items-center gap-1.5 px-3 h-8 rounded-full glass-btn text-sm font-bold text-[rgba(17,17,17,0.7)] hover:text-[#111111] [transition:color_150ms_ease]"

// Current inline pill expanded content order:
// Haku button
// Suosikit Link   <-- insert Profiili BEFORE this
// Kirjaudu ulos / Kirjaudu button
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Profiili link to NavPill.tsx</name>
  <files>app/components/NavPill.tsx</files>
  <read_first>
    - app/components/NavPill.tsx (read entire file — identify exact lines for Suosikit link insertion point)
    - .planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-PATTERNS.md (NavPill.tsx pattern section)
  </read_first>
  <action>
    Read the full file to locate the Suosikit link (`href="/suosikit"`) in the expanded pill content. Insert a new `<Link>` element immediately before it (per D-10: Profiili above Suosikit).

    The new link uses the same `BTN` className constant already defined in the file. Use the `User` icon which is already imported at line 6. Close the pill with `onClick={() => setOpen(false)}` (same pattern as the Suosikit link):

    New link to insert: `href="/profiili"`, icon `<User className="w-3.5 h-3.5" />`, label `Profiili`, `onClick={() => setOpen(false)}`, `className={BTN}`.

    Do NOT modify: the import line (User already imported), the BTN constant, any other links or buttons, the auth-conditional section at the bottom.

    Result menu order after change: Haku → Profiili → Suosikit → Kirjaudu ulos / Kirjaudu
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/NavPill.tsx` contains `href="/profiili"`
    - The Profiili link appears before (higher line number order in JSX renders before) the Suosikit link `href="/suosikit"` in the file
    - The link uses the `BTN` className (not an inline class string)
    - The link uses `<User className="w-3.5 h-3.5" />` as the icon
    - The link has `onClick={() => setOpen(false)}`
    - `npx tsc --noEmit` produces no new TypeScript errors for NavPill.tsx
  </acceptance_criteria>
  <done>NavPill expanded menu contains Profiili link above Suosikit link, using User icon, onClick closes pill.</done>
</task>

<task type="auto">
  <name>Task 2: Add Profiili link to Etusivu.tsx inline pill</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    - app/components/Etusivu.tsx (read lines 560–620 to locate the inline pill expanded content section)
    - .planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-PATTERNS.md (Modification 1 section — Etusivu.tsx inline pill)
  </read_first>
  <action>
    Read lines 560–620 of Etusivu.tsx to locate the Suosikit link (`href="/suosikit"`) inside the inline expanding pill. Insert a new `<Link>` element immediately before it (per D-10: Profiili above Suosikit).

    The new link uses the same inline className as the other pill links at that location (identical to the BTN class string used in NavPill: `"flex items-center gap-1.5 px-3 h-8 rounded-full glass-btn text-sm font-bold text-[rgba(17,17,17,0.7)] hover:text-[#111111] [transition:color_150ms_ease]"`). Use `<User className="w-3.5 h-3.5" />` — already imported at line 7. Use `onClick={closeOverlays}` matching the Suosikit link's onClick pattern.

    New link: `href="/profiili"`, icon `<User className="w-3.5 h-3.5" />`, label `Profiili`, `onClick={closeOverlays}`.

    THIS TASK ONLY modifies the inline pill nav section (lines ~577–608). Do NOT touch: state declarations, auth callback, AI fetch useEffect, or any other section of Etusivu.tsx.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/Etusivu.tsx` contains `href="/profiili"` in the inline pill section (around lines 560–620)
    - The Profiili link appears before the Suosikit link `href="/suosikit"` in JSX order within the inline pill
    - The link uses `<User className="w-3.5 h-3.5" />` icon
    - The link has `onClick={closeOverlays}`
    - Only the inline pill section was modified — no other sections of Etusivu.tsx were changed
    - `npx tsc --noEmit` produces no new TypeScript errors
  </acceptance_criteria>
  <done>Etusivu.tsx inline pill expanded menu contains Profiili link above Suosikit, using User icon, onClick=closeOverlays.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser navigation | Link destinations are hard-coded; no user-controlled URL construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Information Disclosure | /profiili link visibility | accept | D-09 intentionally shows link to all users; unauthenticated users see CTA on the profile page — consistent with /suosikit pattern |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks:
- `grep -c "href=\"/profiili\"" app/components/NavPill.tsx` returns at least 1
- `grep -c "href=\"/profiili\"" app/components/Etusivu.tsx` returns at least 1
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- NavPill.tsx has Profiili link (href="/profiili") above Suosikit link
- Etusivu.tsx inline pill has Profiili link (href="/profiili") above Suosikit link
- No TypeScript errors introduced
</success_criteria>

<output>
Create `.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-02-SUMMARY.md` when done
</output>
