---
phase: "14"
plan: "04"
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - app/components/Etusivu.tsx
  - app/api/saasuositus/route.ts
autonomous: true
requirements:
  - AI-05

must_haves:
  truths:
    - "Authenticated user always triggers POST /api/saasuositus (not GET), even with zero favorites"
    - "POST body includes kotikaupunki field when user has set a home city (per D-07)"
    - "Etusivu loads kotikaupunki from profiles table in the auth callback alongside suosikit"
    - "route.ts POST handler parses kotikaupunki from body and sanitizes it"
    - "When kotikaupunki differs from kaupunki (case-insensitive), reissussa context is appended to AI prompt"
    - "When kotikaupunki matches kaupunki or is absent, no reissussa context is added"
    - "Cache key does NOT include kotikaupunki (per D-11)"
  artifacts:
    - path: "app/components/Etusivu.tsx"
      provides: "kotikaupunki state + profiles fetch in auth callback + POST trigger change"
      contains: "kotikaupunki"
    - path: "app/api/saasuositus/route.ts"
      provides: "kotikaupunki parsing + reissussa prompt context"
      contains: "buildReissuKonteksti"
  key_links:
    - from: "app/components/Etusivu.tsx"
      to: "/api/saasuositus"
      via: "POST fetch with body.kotikaupunki"
      pattern: "kotikaupunki.*saasuositus"
    - from: "app/api/saasuositus/route.ts"
      to: "lib/buildReissuKonteksti.ts"
      via: "import"
      pattern: "from '@/lib/buildReissuKonteksti'"
---

<objective>
Extend `Etusivu.tsx` to load `kotikaupunki` from the profiles table on login and include it in the AI fetch POST body. Extend `route.ts` to parse `kotikaupunki`, import `buildReissuKonteksti`, and conditionally append the reissussa context to the AI prompt.

Purpose: Delivers AI-05 — authenticated users with a home city set get a contextually personalized AI recommendation when visiting a different city. These two files are modified together because they form one data flow: Etusivu sends → route.ts receives.

Output: Two modified files. Authenticated users always send POST. POST body gains kotikaupunki when available. Route handler appends reissussa context to prompt only when cities differ.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-CONTEXT.md
@.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-RESEARCH.md
@.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-PATTERNS.md
@.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-01-SUMMARY.md

<interfaces>
<!-- From app/components/Etusivu.tsx — targeted modification areas (from PATTERNS.md) -->
From app/components/Etusivu.tsx (PATTERNS.md Modification 2 and 3 sections):

Modification 2 targets:
- Line ~112: `const [suosikitIds, setSuosikitIds] = useState<Set<number>>(new Set())`  → add kotikaupunki state after
- Lines ~220–231: subscribeToAuthUser callback → add profiles fetch after suosikit fetch; add `setKotikaupunki('')` in else branch

Modification 3 targets:
- Lines ~252–281: AI fetch useEffect → change trigger condition `suosikkiNimet.length > 0` to `supabaseUser !== null`; extend POST body with `kotikaupunki`
- Cache key construction (line ~253–255): must NOT include kotikaupunki (D-11)
- Dependency array `[suosikitSizeAndIds, weatherKaupunki]` stays unchanged (D-11)

<!-- From app/api/saasuositus/route.ts — current POST handler structure (PATTERNS.md route.ts section) -->
From app/api/saasuositus/route.ts (lines 80–115, PATTERNS.md):

Current POST body parsing:
```typescript
let suosikit: string[] = []
let kaupunki = 'Tampere'
try {
  const body = await request.json()
  suosikit = Array.isArray(body.suosikit) ? body.suosikit.slice(0,10)... : []
  if (typeof body.kaupunki === 'string') kaupunki = body.kaupunki
} catch {}
```

Current prompt construction:
```typescript
const suosikkiLista = suosikit.length ? `\nKäyttäjän suosikit: ${suosikit.join(', ')}.` : ''
const prompt = `Tänään on ${day} ${kaupunki}ssa. Lämpötila on ${temp}°C ja sää on ${weatherDesc}. Kirjoita YKSI lyhyt suomenkielinen lause... Älä käytä emojeja.${suosikkiLista}`
```

<!-- From lib/buildReissuKonteksti.ts — created in Plan 01 -->
From lib/buildReissuKonteksti.ts:
```typescript
export function buildReissuKonteksti(kotikaupunki: string | undefined, kaupunki: string): string
// Returns '' if kotikaupunki falsy/blank or matches kaupunki (case-insensitive, trimmed)
// Returns ` Käyttäjä vierailee ${kaupunki}ssa — hänen kotikaupunkinsa on ${kotikaupunki}.` otherwise
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Etusivu.tsx — kotikaupunki state, profiles fetch, POST trigger</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    - app/components/Etusivu.tsx (read lines 100–290 to see all three targeted areas: state block ~112, auth callback ~220–231, AI fetch ~252–281)
    - .planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-PATTERNS.md (Modification 2 and 3 sections — full code diffs)
    - .planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-RESEARCH.md (Pattern 6, anti-patterns, Pitfall 4)
  </read_first>
  <action>
    Make exactly three targeted edits to Etusivu.tsx:

    EDIT 1 — Add kotikaupunki state (per D-05). After the line `const [suosikitIds, setSuosikitIds] = useState<Set<number>>(new Set())` (around line 112), add:
    `const [kotikaupunki, setKotikaupunki] = useState<string>('')`

    EDIT 2 — Add profiles fetch to auth callback (per D-05). In the `subscribeToAuthUser` async callback (around lines 220–231), after the block that fetches suosikit and calls `setSuosikitIds(...)`, add profiles fetch:
    `const { data: profileData } = await supabase.from('profiles').select('kotikaupunki').eq('user_id', user.id).single()`
    `setKotikaupunki(profileData?.kotikaupunki ?? '')`
    In the `else` branch (user is null), add `setKotikaupunki('')` alongside the existing `setSuosikitIds(new Set())`.

    EDIT 3 — Change AI fetch trigger and extend POST body (per D-06, D-07). In the AI fetch `useEffect` (around lines 252–281), change the ternary condition from `suosikkiNimet.length > 0` to `supabaseUser !== null`. Extend the POST body `JSON.stringify({...})` to spread `kotikaupunki` conditionally: `...(kotikaupunki ? { kotikaupunki } : {})`.

    Cache key: DO NOT modify the cache key string (D-11). It stays as `'saasuositus-' + date + '-' + weatherKaupunki + (suosikitIds.size > 0 ? '-' + suosikitIds.size : '')`.
    Dependency array: DO NOT modify `[suosikitSizeAndIds, weatherKaupunki]` (D-11).

    These are surgical edits — do not restructure, reformat, or touch any other lines.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/Etusivu.tsx` contains `const [kotikaupunki, setKotikaupunki] = useState<string>('')`
    - File contains `.from('profiles').select('kotikaupunki').eq('user_id', user.id).single()`
    - File contains `setKotikaupunki(profileData?.kotikaupunki ?? '')`
    - The AI fetch condition uses `supabaseUser !== null` (not `suosikkiNimet.length > 0`)
    - POST body includes spread `...(kotikaupunki ? { kotikaupunki } : {})`
    - Cache key string does NOT contain `kotikaupunki`
    - Dependency array of AI fetch useEffect remains `[suosikitSizeAndIds, weatherKaupunki]` (no kotikaupunki added)
    - `npx tsc --noEmit` produces no new TypeScript errors
  </acceptance_criteria>
  <done>Etusivu.tsx has kotikaupunki state, profiles fetch in auth callback, POST triggered for all authenticated users with kotikaupunki in body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend route.ts POST handler with kotikaupunki parsing and reissussa prompt</name>
  <files>app/api/saasuositus/route.ts</files>
  <read_first>
    - app/api/saasuositus/route.ts (read full file — identify POST handler body parsing block and prompt construction block)
    - .planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-PATTERNS.md (route.ts modification section — full code diffs)
    - .planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-RESEARCH.md (Pattern 4, Pitfall 5, D-08, D-12)
    - lib/buildReissuKonteksti.ts (function signature — created in Plan 01)
  </read_first>
  <behavior>
    - POST with `{ kaupunki: 'Helsinki', kotikaupunki: 'Tampere' }` → prompt ends with ` Käyttäjä vierailee Helsingissä — hänen kotikaupunkinsa on Tampere.` (or equivalent from buildReissuKonteksti)
    - POST with `{ kaupunki: 'Tampere', kotikaupunki: 'tampere' }` → prompt does NOT contain reissussa context
    - POST with `{ kaupunki: 'Helsinki' }` (no kotikaupunki) → prompt does NOT contain reissussa context
    - POST with `{ kaupunki: 'Helsinki', kotikaupunki: '  ' }` (whitespace) → prompt does NOT contain reissussa context
    - kotikaupunki in POST body is sanitized: `replace(/[^\w\sÄäÖöÅå\-,.'()&]/g, '').slice(0, 80).trim()`
  </behavior>
  <action>
    Make two targeted edits to `app/api/saasuositus/route.ts`:

    EDIT 1 — Add import at top of file:
    `import { buildReissuKonteksti } from '@/lib/buildReissuKonteksti'`

    EDIT 2 — Extend POST body parsing. In the POST handler (around lines 80–92), after `if (typeof body.kaupunki === 'string') kaupunki = body.kaupunki`, add kotikaupunki parsing (per D-08):
    Declare `let kotikaupunki: string | undefined` before the try block.
    Inside the try block, after the kaupunki parse, add:
    ```
    if (typeof body.kotikaupunki === 'string' && body.kotikaupunki.trim()) {
      kotikaupunki = body.kotikaupunki
        .replace(/[^\w\sÄäÖöÅå\-,.'()&]/g, '')
        .slice(0, 80)
        .trim()
    }
    ```

    EDIT 3 — Extend prompt construction. After the `suosikkiLista` constant, add:
    `const reissuKonteksti = buildReissuKonteksti(kotikaupunki, kaupunki)`
    Then append `${reissuKonteksti}` at the end of the existing prompt template literal (after `${suosikkiLista}`).

    These are surgical edits — do not restructure or reformat the handler.
  </action>
  <verify>
    <automated>npx vitest run lib/saasuositus.test.ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/saasuositus/route.ts` imports `buildReissuKonteksti` from `'@/lib/buildReissuKonteksti'`
    - POST handler declares `let kotikaupunki: string | undefined`
    - File contains `body.kotikaupunki` parsing with `.replace(/[^\w\sÄäÖöÅå\-,.'()&]/g, '').slice(0, 80).trim()`
    - File contains `buildReissuKonteksti(kotikaupunki, kaupunki)` call
    - Prompt template literal ends with `${suosikkiLista}${reissuKonteksti}` (or equivalent — reissuKonteksti is last)
    - `npx vitest run` exits 0 (all tests pass including saasuositus.test.ts)
    - `npx tsc --noEmit` produces no TypeScript errors
  </acceptance_criteria>
  <done>route.ts parses kotikaupunki from POST body (sanitized), calls buildReissuKonteksti, appends result to prompt. All unit tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /api/saasuositus | kotikaupunki is user-controlled input; must be sanitized before inserting into AI prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering | kotikaupunki prompt injection | mitigate | Sanitize in route.ts: `replace(/[^\w\sÄäÖöÅå\-,.'()&]/g, '').slice(0, 80)` — same pattern as suosikit names |
| T-14-09 | Information Disclosure | Etusivu reads profiles table | accept | RLS SELECT policy enforces auth.uid() = user_id; browser client uses anon key with RLS |
| T-14-10 | Denial of Service | POST /api/saasuositus for all auth users | accept | Route already has Anthropic API error handling + time-based fallback; no new attack surface |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
After both tasks:
- `grep -c "buildReissuKonteksti" app/api/saasuositus/route.ts` returns at least 2 (import + call)
- `grep -c "kotikaupunki" app/components/Etusivu.tsx` returns at least 4 (state, fetch, POST body, else reset)
- `npx vitest run` exits 0
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Etusivu.tsx: kotikaupunki state + profiles fetch in auth callback + POST for all auth users
- route.ts: kotikaupunki parsed + sanitized + buildReissuKonteksti integrated into prompt
- All vitest tests pass (including lib/saasuositus.test.ts)
- No TypeScript compile errors
</success_criteria>

<output>
Create `.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-04-SUMMARY.md` when done
</output>
