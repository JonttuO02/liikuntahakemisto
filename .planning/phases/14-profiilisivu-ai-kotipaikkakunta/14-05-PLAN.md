---
phase: "14"
plan: "05"
type: execute
wave: 3
depends_on:
  - "14-02"
  - "14-03"
  - "14-04"
files_modified: []
autonomous: false
requirements:
  - AUTH-04
  - AI-05

must_haves:
  truths:
    - "Profiili link is visible in NavPill on /suosikit, /paikat/[id], /tietosuoja, and /profiili pages"
    - "Profiili link is visible in Etusivu inline pill on the homepage"
    - "Unauthenticated user visiting /profiili sees CTA (not form)"
    - "Authenticated user visiting /profiili sees their email and kotikaupunki input"
    - "Saving kotikaupunki persists across page reload"
    - "Supabase migration applied — profiles table exists"
    - "AI widget produces reissussa context when home city differs from current city"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: confirm the complete Phase 14 feature works end-to-end in the running application.

Purpose: Functional behaviours cannot be fully verified by TypeScript compilation or unit tests. This checkpoint covers: navigation link visibility, profile page auth flows, kotikaupunki persistence, and AI reissussa context in practice.

Output: User confirms all checklist items pass, or reports issues for gap closure.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-01-SUMMARY.md
@.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-02-SUMMARY.md
@.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-03-SUMMARY.md
@.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply database migration and run full test suite</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260528083110_profiles.sql (verify it exists and has correct DDL)
    - .planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-01-SUMMARY.md
  </read_first>
  <action>
    Run the following commands in sequence to ensure the system is ready for verification:

    1. Apply migration: `npx supabase db push` (or `npx supabase migration up` — use whichever is consistent with the project's Supabase workflow)
    2. Run full test suite: `npx vitest run`
    3. Check TypeScript compilation: `npx tsc --noEmit`
    4. Start dev server if not running: `npm run dev`

    If `supabase db push` fails due to an already-applied migration or connection error, note the error in the summary and proceed — the migration may have been applied in a prior step.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 with all tests passing
    - `npx tsc --noEmit` exits 0 with no errors
    - Dev server is running on localhost:3000
  </acceptance_criteria>
  <done>All automated checks pass; dev server running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 14 features</name>
  <what-built>
    - Profiili navigation link added to NavPill.tsx and Etusivu.tsx inline pill (Plan 02)
    - /profiili page with auth-gated profile form: email display, kotikaupunki text input, save with inline feedback (Plan 03)
    - Supabase profiles table with RLS (Plan 01)
    - Etusivu kotikaupunki loading in auth callback + POST for all authenticated users (Plan 04)
    - AI route handler uses kotikaupunki for reissussa context when cities differ (Plan 04)
  </what-built>
  <how-to-verify>
    Open http://localhost:3000 in your browser. Verify the following in order:

    **Navigation (Plan 02):**
    1. Click the NavPill (top-right expanding pill) on the homepage — confirm "Profiili" link appears above "Suosikit" in the menu
    2. Navigate to /suosikit — confirm NavPill menu also shows Profiili above Suosikit
    3. Navigate to /paikat/[any venue id] — confirm same menu order

    **Profiili page — unauthenticated (Plan 03):**
    4. While logged OUT, visit http://localhost:3000/profiili
    5. Confirm you see a CTA screen with "Profiili vaatii kirjautumisen" and a "Kirjaudu sisään" button — NOT a form
    6. Confirm the "Takaisin hakemistoon" link goes to / (not /?nakyma=lista)

    **Profiili page — authenticated (Plans 01, 03):**
    7. Click "Kirjaudu sisään" and log in
    8. Navigate to /profiili — confirm you see your email address
    9. Confirm you see the "Kotipaikkakunta" text input (empty on first visit)
    10. Type a city name (e.g. "Tampere") and click "Tallenna"
    11. Confirm "Kotikaupunki tallennettu" appears briefly (about 2.5 seconds) then disappears
    12. Reload the page — confirm "Tampere" is still in the text field (persisted to Supabase)

    **AI reissussa context (Plan 04) — optional but recommended:**
    13. On /profiili, set kotikaupunki to a city DIFFERENT from where AI widget shows (e.g. your current AI city is "Helsinki" — set kotikaupunki to "Tampere")
    14. Return to homepage — the AI widget text may still show cached content from today
    15. (Optional) Clear sessionStorage and refresh — if the AI widget fetches fresh, the new text should reflect your home city context in some way (you will not see "kotona/reissussa" explicitly — Claude handles the wording)

    Report any visual issues, errors, or unexpected behaviour.
  </how-to-verify>
  <resume-signal>Type "approved" if all checklist items pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual verification | Human-only; no new attack surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
<human-check>
All items in Task 2 how-to-verify checklist confirmed by user.
</human-check>
</verification>

<success_criteria>
- All automated checks pass (vitest, tsc)
- User confirms: Profiili link visible in nav, /profiili shows CTA for anon / form for auth, kotikaupunki persists after reload
- Phase 14 complete, ready for /gsd:verify-work 14
</success_criteria>

<output>
Create `.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-05-SUMMARY.md` when done
</output>
