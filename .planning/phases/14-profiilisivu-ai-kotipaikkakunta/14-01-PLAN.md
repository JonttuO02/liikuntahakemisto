---
phase: "14"
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260528083110_profiles.sql
  - lib/buildReissuKonteksti.ts
  - lib/saasuositus.test.ts
autonomous: true
requirements:
  - AUTH-04
  - AI-05

must_haves:
  truths:
    - "profiles table exists in Supabase with user_id PK, kotikaupunki text, updated_at timestamptz"
    - "RLS SELECT/INSERT/UPDATE policies enforce auth.uid() = user_id"
    - "buildReissuKonteksti('Tampere','Helsinki') returns a string containing 'kotikaupunkinsa on Tampere'"
    - "buildReissuKonteksti('tampere','Tampere') returns empty string (case-insensitive match)"
    - "buildReissuKonteksti(undefined,'Tampere') returns empty string"
    - "npx vitest run exits 0 with all saasuositus.test.ts cases passing"
  artifacts:
    - path: "supabase/migrations/20260528083110_profiles.sql"
      provides: "profiles table DDL + RLS policies"
      contains: "CREATE TABLE IF NOT EXISTS profiles"
    - path: "lib/buildReissuKonteksti.ts"
      provides: "Pure helper for reissussa AI context"
      exports: ["buildReissuKonteksti"]
    - path: "lib/saasuositus.test.ts"
      provides: "Unit tests for buildReissuKonteksti"
      contains: "describe('buildReissuKonteksti'"
  key_links:
    - from: "lib/saasuositus.test.ts"
      to: "lib/buildReissuKonteksti.ts"
      via: "import"
      pattern: "from './buildReissuKonteksti'"
    - from: "app/api/saasuositus/route.ts"
      to: "lib/buildReissuKonteksti.ts"
      via: "import (in Plan 04)"
      pattern: "from '@/lib/buildReissuKonteksti'"
---

<objective>
Create the Supabase `profiles` table migration and extract the reissussa AI context logic into a tested pure helper function.

Purpose: Establishes the database foundation (profiles table + RLS) that ProfiiliClient and Etusivu depend on, and creates the unit-testable `buildReissuKonteksti` helper that route.ts will import in Plan 04.

Output: Migration SQL file ready for `supabase db push`, `lib/buildReissuKonteksti.ts` with exported function, `lib/saasuositus.test.ts` with 4 passing unit tests.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-CONTEXT.md
@.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-RESEARCH.md
@.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-PATTERNS.md

<interfaces>
<!-- From supabase/migrations/20260523_suosikit.sql — RLS structural reference -->
From supabase/migrations/20260523_suosikit.sql:
```sql
CREATE TABLE IF NOT EXISTS suosikit (
  id          bigserial PRIMARY KEY,
  user_id     uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  paikka_id   int  NOT NULL REFERENCES liikuntapaikat(id) ON DELETE CASCADE,
  UNIQUE (user_id, paikka_id)
);
ALTER TABLE suosikit ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users can read own suosikit"  ON suosikit FOR SELECT  USING (auth.uid() = user_id);
CREATE POLICY "Users can insert own suosikit" ON suosikit FOR INSERT  WITH CHECK (auth.uid() = user_id);
CREATE POLICY "Users can delete own suosikit" ON suosikit FOR DELETE  USING (auth.uid() = user_id);
```
<!-- Note: profiles needs user_id as PRIMARY KEY (no bigserial), and needs UPDATE policy (no DELETE). -->

<!-- From lib/aukiolo.test.ts — vitest test file structure -->
```typescript
import { describe, it, expect } from 'vitest'
import { getOpenStatus } from './aukiolo'
describe('getOpenStatus', () => {
  it('...', () => { expect(...).toEqual(...) })
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create profiles table migration</name>
  <files>supabase/migrations/20260528083110_profiles.sql</files>
  <read_first>
    - supabase/migrations/20260523_suosikit.sql (RLS policy structure to follow)
    - .planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-CONTEXT.md (D-01, D-02, D-03)
    - .planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-PATTERNS.md (migration pattern section)
  </read_first>
  <action>
    Create `supabase/migrations/20260528083110_profiles.sql` with the following schema (per D-02):
    - Table name: `profiles`
    - Columns: `user_id uuid PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE`, `kotikaupunki text`, `updated_at timestamptz DEFAULT now()`
    - `ALTER TABLE profiles ENABLE ROW LEVEL SECURITY`
    - Policy "Users can read own profile" FOR SELECT USING (auth.uid() = user_id)
    - Policy "Users can insert own profile" FOR INSERT WITH CHECK (auth.uid() = user_id)
    - Policy "Users can update own profile" FOR UPDATE USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id)

    Critical differences from suosikit: `user_id` is PRIMARY KEY (not a separate `bigserial id`), no `paikka_id` foreign key, no UNIQUE constraint needed (PK already enforces uniqueness), no DELETE policy (CASCADE from auth.users handles cleanup), but UPDATE policy IS required (second save hits UPDATE path).
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS profiles" supabase/migrations/20260528083110_profiles.sql && grep -c "FOR UPDATE" supabase/migrations/20260528083110_profiles.sql && grep -c "FOR INSERT" supabase/migrations/20260528083110_profiles.sql && grep -c "FOR SELECT" supabase/migrations/20260528083110_profiles.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260528083110_profiles.sql` exists
    - File contains `CREATE TABLE IF NOT EXISTS profiles`
    - File contains `user_id uuid PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE`
    - File contains `kotikaupunki text`
    - File contains `updated_at timestamptz DEFAULT now()`
    - File contains `ALTER TABLE profiles ENABLE ROW LEVEL SECURITY`
    - File contains all three policies: FOR SELECT, FOR INSERT WITH CHECK, FOR UPDATE USING ... WITH CHECK
    - File does NOT contain `FOR DELETE` policy (not needed)
    - File does NOT contain `bigserial` (user_id is PK, no separate id column)
  </acceptance_criteria>
  <done>Migration file exists with correct schema and all three RLS policies (SELECT + INSERT + UPDATE). No DELETE policy, no bigserial column.</done>
</task>

<task type="tdd">
  <name>Task 2: Extract and test buildReissuKonteksti helper</name>
  <files>lib/buildReissuKonteksti.ts, lib/saasuositus.test.ts</files>
  <read_first>
    - .planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-PATTERNS.md (Pattern 4 — AI Prompt Extension, lib/saasuositus.test.ts section)
    - .planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-RESEARCH.md (Pattern 4 — prompt template, D-12)
    - lib/aukiolo.test.ts (vitest describe/it/expect pattern)
    - vitest.config.ts (confirm include: ['lib/**/*.test.ts'])
  </read_first>
  <behavior>
    - buildReissuKonteksti('Tampere', 'Helsinki') returns a string containing "Käyttäjä vierailee Helsingissä" or similar Finnish phrasing with both city names present and kotikaupunki = Tampere
    - buildReissuKonteksti('tampere', 'Tampere') returns '' (case-insensitive same-city comparison)
    - buildReissuKonteksti(undefined, 'Tampere') returns ''
    - buildReissuKonteksti('   ', 'Tampere') returns '' (whitespace-only after trim)
    - buildReissuKonteksti('Tampere', 'tampere') returns '' (reverse case-insensitive)
  </behavior>
  <action>
    RED phase: Create `lib/saasuositus.test.ts` importing `{ buildReissuKonteksti }` from `'./buildReissuKonteksti'`. Write the 4 test cases listed in the behavior block. Run `npx vitest run lib/saasuositus.test.ts` — it MUST fail (module not found or function not exported).

    GREEN phase: Create `lib/buildReissuKonteksti.ts` that exports a single function `buildReissuKonteksti(kotikaupunki: string | undefined, kaupunki: string): string`. Implementation logic (per D-12):
    - Return '' if kotikaupunki is falsy or `kotikaupunki.trim()` is empty
    - Return '' if `kotikaupunki.trim().toLowerCase() === kaupunki.trim().toLowerCase()`
    - Otherwise return ` Käyttäjä vierailee ${kaupunki}ssa — hänen kotikaupunkinsa on ${kotikaupunki}.` (note: leading space — this string is appended to the prompt)

    Run `npx vitest run lib/saasuositus.test.ts` — it MUST pass.

    No REFACTOR needed for a function this simple unless duplication is obvious.
  </action>
  <verify>
    <automated>npx vitest run lib/saasuositus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/buildReissuKonteksti.ts` exists and exports `buildReissuKonteksti`
    - `lib/saasuositus.test.ts` exists with at least 4 test cases covering: cities differ (returns context string), same city case-insensitive (returns ''), undefined kotikaupunki (returns ''), whitespace-only kotikaupunki (returns '')
    - `npx vitest run lib/saasuositus.test.ts` exits 0 with all tests passing
    - `npx vitest run` (full suite) exits 0 with no regressions
  </acceptance_criteria>
  <done>buildReissuKonteksti exported from lib/buildReissuKonteksti.ts; all unit tests pass; full vitest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Supabase DB | kotikaupunki write; RLS must enforce user_id isolation |
| Browser → POST /api/saasuositus | kotikaupunki sent as user input; sanitized before AI prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | profiles RLS — cross-user write | mitigate | RLS INSERT+UPDATE policies enforce `auth.uid() = user_id` — implemented in this migration |
| T-14-02 | Information Disclosure | profiles RLS — cross-user read | mitigate | RLS SELECT policy enforces `auth.uid() = user_id` |
| T-14-03 | Tampering | buildReissuKonteksti — prompt injection via kotikaupunki | mitigate | Sanitization in route.ts (Plan 04): `replace(/[^\w\sÄäÖöÅå\-,.'()&]/g, '').slice(0, 80)` |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 14 — verified from package.json |
</threat_model>

<verification>
After both tasks complete:
- `grep -c "FOR UPDATE" supabase/migrations/20260528083110_profiles.sql` returns 1
- `grep -c "FOR INSERT" supabase/migrations/20260528083110_profiles.sql` returns 1
- `grep -c "FOR SELECT" supabase/migrations/20260528083110_profiles.sql` returns 1
- `npx vitest run` exits 0
</verification>

<success_criteria>
- profiles migration file exists with correct DDL and 3 RLS policies (SELECT, INSERT, UPDATE)
- buildReissuKonteksti helper exported from lib/buildReissuKonteksti.ts
- All 4+ unit tests in lib/saasuositus.test.ts pass
- Full vitest suite remains green
</success_criteria>

<output>
Create `.planning/phases/14-profiilisivu-ai-kotipaikkakunta/14-01-SUMMARY.md` when done
</output>
