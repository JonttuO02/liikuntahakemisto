---
phase: 60-hallintaoikeuspyynn-t-backend-s-hk-posti
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260626000000_business_access_requests.sql
autonomous: false
requirements: [ACCESS-03, ACCESS-06]
must_haves:
  truths:
    - "A business_access_requests table exists with status pending/approved/rejected and a partial UNIQUE on (requester_id, paikka_id) WHERE status='pending'"
    - "A requester can read only their own access requests via RLS; an owner can read requests targeting venues their company owns; no authenticated UPDATE path exists"
    - "business_accounts.company_id is nullable so an invite-link signup can exist with no company until approval (D-09a)"
    - "The schema is pushed to the live Supabase instance"
  artifacts:
    - path: "supabase/migrations/20260626000000_business_access_requests.sql"
      provides: "business_access_requests table + RLS + partial UNIQUE index + company_id nullability relaxation"
      contains: "CREATE TABLE IF NOT EXISTS business_access_requests"
  key_links:
    - from: "business_access_requests.requester_id"
      to: "business_accounts.user_id"
      via: "FK REFERENCES business_accounts(user_id) ON DELETE CASCADE"
      pattern: "REFERENCES business_accounts"
    - from: "Owner reads requests for owned venues policy"
      to: "current_company_id()"
      via: "RLS SELECT policy composes with the Phase 59 SECURITY DEFINER helper"
      pattern: "current_company_id\\(\\)"
---

<objective>
Create the `business_access_requests` table with RLS, the D-08 partial UNIQUE idempotency index, and relax `business_accounts.company_id` to nullable so the D-09a no-auto-company invite-link signup path is possible. This is the schema foundation gating all of Phase 60's Route Handlers.

Purpose: ACCESS-03 (request row lands as pending) and ACCESS-06 (RLS blocks management access pre-approval) are enforced structurally by this table's shape and policies. Every downstream Route Handler depends on this table existing in the live DB.
Output: One new migration file, pushed to the live Supabase instance.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-CONTEXT.md
@.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-RESEARCH.md
@.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-PATTERNS.md
@supabase/migrations/20260625000000_companies_role_rls.sql
@supabase/migrations/20260625000001_fix_column_privilege_escalation.sql
@supabase/migrations/20260625000002_tighten_business_accounts_grant.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create business_access_requests migration with table, RLS, partial UNIQUE, and company_id nullability relaxation</name>
  <files>supabase/migrations/20260626000000_business_access_requests.sql</files>
  <read_first>
    - supabase/migrations/20260625000000_companies_role_rls.sql (4-part header comment convention, current_company_id() helper, RLS policy shape, REVOKE pattern, the SET NOT NULL on company_id at lines 122-124 that THIS migration reverses)
    - supabase/migrations/20260625000001_fix_column_privilege_escalation.sql (table-wide REVOKE before column allow-list GRANT pattern — Pitfall 2)
    - supabase/migrations/20260625000002_tighten_business_accounts_grant.sql (grant-tightening follow-up)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-RESEARCH.md (Schema Design section — proposed DDL is the authoritative template; Pitfall 1 and Open Question 1 on D-09a)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-CONTEXT.md (D-08, D-09, D-09a, D-10)
  </read_first>
  <action>
    Create `supabase/migrations/20260626000000_business_access_requests.sql` wrapped in a single `BEGIN; ... COMMIT;` transaction. Open with the codebase's 4-part header comment block (Analog sources, Decision log mapping D-08/D-09/D-09a/D-10, NOT-included deferrals, existing-RLS-policy enumeration) matching the style of `20260625000000_companies_role_rls.sql`.

    Step 1 — Relax company_id (D-09a, per ACCESS-03): `ALTER TABLE business_accounts ALTER COLUMN company_id DROP NOT NULL;`. Comment explaining this reverses Phase 59's SET NOT NULL specifically so invite-link signups can exist with `company_id = NULL` until access-request approval sets it. Note `current_company_id()` already returns NULL gracefully for such rows (Phase 59 D-12), so no helper change is needed.

    Step 2 — Create the table `business_access_requests` with columns exactly: `id BIGSERIAL PRIMARY KEY`, `requester_id UUID NOT NULL REFERENCES business_accounts(user_id) ON DELETE CASCADE`, `paikka_id BIGINT NOT NULL REFERENCES liikuntapaikat(id) ON DELETE CASCADE`, `status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','approved','rejected'))`, `rejection_reason TEXT`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`. Use `CREATE TABLE IF NOT EXISTS`.

    Step 3 — D-08 partial UNIQUE index: `CREATE UNIQUE INDEX business_access_requests_pending_unique ON business_access_requests (requester_id, paikka_id) WHERE (status = 'pending');`. Comment that this is genuinely new to the codebase (no prior partial UNIQUE index) and is the DB-layer enforcement for idempotent duplicate submission.

    Step 4 — `ALTER TABLE business_access_requests ENABLE ROW LEVEL SECURITY;`.

    Step 5 — Three policies, NO UPDATE policy: (a) SELECT "Requester reads own access requests" USING `auth.uid() = requester_id`; (b) SELECT "Owner reads requests for owned venues" USING an EXISTS subquery joining `business_paikka_links bpl` to `business_accounts ba` on `ba.user_id = bpl.business_account_id` WHERE `bpl.paikka_id = business_access_requests.paikka_id AND bpl.claim_status = 'approved' AND ba.company_id = current_company_id() AND ba.role = 'owner'`; (c) INSERT "Requester inserts own access request" WITH CHECK `auth.uid() = requester_id`.

    Per Pitfall 2: add NO UPDATE RLS policy and NO `authenticated` UPDATE grant on this table — status/rejection_reason transitions happen ONLY via supabaseAdmin in the approve/reject Route Handlers. Include a comment explaining this is the deliberate defensive choice (avoids the column-only-REVOKE-doesn't-narrow-table-grant pitfall by construction). Do NOT add a column-level REVOKE.
  </action>
  <verify>
    <automated>grep -q "CREATE TABLE IF NOT EXISTS business_access_requests" supabase/migrations/20260626000000_business_access_requests.sql && grep -q "business_access_requests_pending_unique" supabase/migrations/20260626000000_business_access_requests.sql && grep -q "ALTER COLUMN company_id DROP NOT NULL" supabase/migrations/20260626000000_business_access_requests.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File contains `CREATE TABLE IF NOT EXISTS business_access_requests` with all 7 columns and the status CHECK constraint
    - File contains the partial UNIQUE index `business_access_requests_pending_unique ... WHERE (status = 'pending')`
    - File contains `ALTER COLUMN company_id DROP NOT NULL` on business_accounts
    - File contains exactly two SELECT policies and one INSERT policy, and NO `CREATE POLICY` with `FOR UPDATE`, and NO `GRANT UPDATE` / column-level `REVOKE UPDATE` on business_access_requests
    - The owner-read SELECT policy references `current_company_id()` and filters `ba.role = 'owner'` and `bpl.claim_status = 'approved'`
    - Whole migration is wrapped in BEGIN; / COMMIT;
  </acceptance_criteria>
  <done>The migration file exists, defines the table/index/policies/nullability change exactly per the RESEARCH.md DDL, and grep checks pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: [BLOCKING] Push schema to live Supabase instance</name>
  <files>supabase/migrations/20260626000000_business_access_requests.sql</files>
  <what-built>The `business_access_requests` migration SQL file (table, RLS, partial UNIQUE index, company_id nullability relaxation).</what-built>
  <action>Push the migration to the live Supabase instance with `supabase db push` (set `SUPABASE_ACCESS_TOKEN` for non-TTY), then confirm the table, its RLS policies, and the `business_accounts.company_id` nullability change in the Supabase dashboard. This is mandatory — verification cannot pass without the push since types come from config, not the live DB.</action>
  <how-to-verify>
    [BLOCKING] Schema push to the live Supabase instance. This is mandatory — the phase CANNOT pass verification without it. Build and type checks pass without the push (types come from config, not the live DB), creating a false-positive state.

    1. Ensure `SUPABASE_ACCESS_TOKEN` is set in the environment (non-TTY workaround for `supabase db push`).
    2. Run `supabase db push` from the project root.
    3. If the push prompts interactively and cannot be suppressed, run it manually and paste the result.
    4. Confirm in the Supabase dashboard (Database -> Tables) that `business_access_requests` exists, and (Authentication -> Policies) that it has exactly the two SELECT policies and one INSERT policy with RLS enabled and NO UPDATE policy.
    5. Confirm `business_accounts.company_id` is now nullable (Database -> Tables -> business_accounts -> company_id column shows nullable).
  </how-to-verify>
  <resume-signal>Type "approved" once `supabase db push` succeeded and the table/policies/nullability are confirmed in the dashboard, or describe the error.</resume-signal>
</task>

</tasks>

<artifacts_produced>
- New file: `supabase/migrations/20260626000000_business_access_requests.sql`
- New DB table: `business_access_requests` (columns: id, requester_id, paikka_id, status, rejection_reason, created_at, updated_at)
- New DB index: `business_access_requests_pending_unique` (partial UNIQUE)
- New RLS policies: "Requester reads own access requests", "Owner reads requests for owned venues", "Requester inserts own access request"
- Schema change: `business_accounts.company_id` becomes nullable
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client -> business_access_requests | Untrusted client SELECT/INSERT crosses RLS here |
| supabaseAdmin (service role) -> business_access_requests | Trusted server-only writes (status transitions) bypass RLS |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Elevation of Privilege | business_access_requests RLS | mitigate | ENABLE ROW LEVEL SECURITY + explicit SELECT/INSERT policies only; no default-allow gap |
| T-60-02 | Elevation of Privilege | status column self-approval | mitigate | No UPDATE RLS policy and no authenticated UPDATE grant — status changes only via supabaseAdmin |
| T-60-04 | Information Disclosure | cross-requester request reads | mitigate | SELECT policy self-scoped to auth.uid() = requester_id; owner read scoped via current_company_id() + role='owner' |
| T-60-SC | Tampering | supabase db push (no new npm packages) | accept | Phase adds zero new packages; push is operator-gated via blocking human checkpoint |
</threat_model>

<verification>
- Migration file passes the grep checks in Task 1
- `supabase db push` succeeds and the table + policies + nullability change are visible in the Supabase dashboard
- No UPDATE policy or authenticated UPDATE grant on business_access_requests exists
</verification>

<success_criteria>
- `business_access_requests` table exists in the live DB with correct columns, CHECK, partial UNIQUE index, RLS enabled, and exactly 3 policies (2 SELECT, 1 INSERT)
- `business_accounts.company_id` is nullable in the live DB
</success_criteria>

<output>
Create `.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-01-SUMMARY.md` when done
</output>
