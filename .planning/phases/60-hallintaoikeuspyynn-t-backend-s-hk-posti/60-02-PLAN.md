---
phase: 60-hallintaoikeuspyynn-t-backend-s-hk-posti
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/email.ts
autonomous: true
requirements: [ACCESS-05]
must_haves:
  truths:
    - "An exported sendAccessRequestNotificationEmail sends an owner-notification email when a request arrives"
    - "An exported sendAccessRequestDecisionEmail sends an approved-or-rejected decision email to the requester, with an optional reason line"
    - "Every interpolated subject string passes through sub() and every HTML-body string through esc()"
  artifacts:
    - path: "lib/email.ts"
      provides: "Two new exported Resend send functions for access-request notification + decision"
      exports: ["sendAccessRequestNotificationEmail", "sendAccessRequestDecisionEmail"]
  key_links:
    - from: "sendAccessRequestNotificationEmail / sendAccessRequestDecisionEmail"
      to: "sub() and esc() helpers"
      via: "header-injection and XSS safety on every interpolation"
      pattern: "sub\\(|esc\\("
---

<objective>
Add two new exported email-sending functions to `lib/email.ts` following the file's exact existing conventions (Resend client, FROM constant, sub()/esc() safety helpers). One notifies the venue owner that a new access request arrived; the other notifies the requester of the approve/reject decision.

Purpose: ACCESS-05 — owner receives a Resend email on submission, requester receives one on decision (rejection reason optional).
Output: Two new exports in `lib/email.ts`. No new file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-RESEARCH.md
@.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-UI-SPEC.md
@lib/email.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sendAccessRequestNotificationEmail and sendAccessRequestDecisionEmail to lib/email.ts</name>
  <files>lib/email.ts</files>
  <read_first>
    - lib/email.ts (full file — the resend client, FROM/ADMIN_EMAIL constants, sub()/esc() helpers, and the sendApprovalEmail/sendRejectionEmail function shapes to copy)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-RESEARCH.md (Code Examples section — authoritative drafts for both functions)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-UI-SPEC.md (Copywriting Contract — confirmed final Finnish subject/body copy)
  </read_first>
  <action>
    Append two new exported async functions to `lib/email.ts` (do NOT create a new module, do NOT modify existing functions or the sub()/esc() helpers).

    Function 1 — `sendAccessRequestNotificationEmail(to: string, params: { requesterName: string; venueName: string; requestId: number })`: subject `[Aktiivi] Uusi hallintaoikeuspyyntö — ${sub(params.venueName)}`; HTML body with an `<h2>Uusi hallintaoikeuspyyntö</h2>`, a `Pyytäjä:` line using `esc(params.requesterName)`, a `Paikka:` line using `esc(params.venueName)`, and a link to `${process.env.NEXT_PUBLIC_APP_URL ?? 'http://localhost:3000'}/business`. Send via `resend.emails.send({ from: FROM, to, subject, html })`.

    Function 2 — `sendAccessRequestDecisionEmail(to: string, params: { venueName: string; approved: boolean; reason?: string })`: subject is `[Aktiivi] Hallintaoikeuspyyntösi on hyväksytty — ${sub(params.venueName)}` when approved, else `[Aktiivi] Hallintaoikeuspyyntösi on hylätty — ${sub(params.venueName)}`. Approved HTML body: `<h2>Pyyntösi on hyväksytty!</h2>` + a paragraph naming `esc(params.venueName)` + a link to `/business`. Rejected HTML body: `<h2>Pyyntösi on hylätty</h2>` + a paragraph naming `esc(params.venueName)` + a reason line `<p><strong>Syy:</strong> ${esc(params.reason)}</p>` ONLY when `params.reason` is truthy (optional per ACCESS-05). Send via `resend.emails.send({ from: FROM, to, subject, html })`.

    Both functions MUST call `sub()` on every interpolated subject string and `esc()` on every interpolated HTML-body string — the project's only header-injection/XSS defense for email. Do not interpolate raw user data anywhere.
  </action>
  <verify>
    <automated>grep -q "export async function sendAccessRequestNotificationEmail" lib/email.ts && grep -q "export async function sendAccessRequestDecisionEmail" lib/email.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `lib/email.ts` exports both `sendAccessRequestNotificationEmail` and `sendAccessRequestDecisionEmail`
    - Every subject-line interpolation in both functions is wrapped in `sub(...)`
    - Every HTML-body interpolation of params (venueName, requesterName, reason) is wrapped in `esc(...)`
    - The rejected-decision body renders the `Syy:` reason line only when `params.reason` is truthy
    - `npx tsc --noEmit` passes (no type errors introduced)
    - Existing functions (sendAdminNotificationEmail, sendApprovalEmail, sendRejectionEmail) and the sub()/esc() helpers are unchanged
  </acceptance_criteria>
  <done>Both functions exist, type-check passes, and all subject/body interpolations are routed through sub()/esc().</done>
</task>

</tasks>

<artifacts_produced>
- New export: `sendAccessRequestNotificationEmail(to, { requesterName, venueName, requestId })` in `lib/email.ts`
- New export: `sendAccessRequestDecisionEmail(to, { venueName, approved, reason? })` in `lib/email.ts`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied data -> email subject/body | venueName / requesterName / reason are interpolated into outbound email |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-05 | Tampering | email subject header | mitigate | sub() strips CR/LF from every interpolated subject string (header-injection defense) |
| T-60-06 | Tampering | email HTML body | mitigate | esc() HTML-escapes every interpolated body string (XSS defense) |
| T-60-SC | Tampering | npm installs | accept | Zero new packages; reuses existing resend dependency |
</threat_model>

<verification>
- Both functions exported and `npx tsc --noEmit` clean
- All interpolations routed through sub()/esc()
</verification>

<success_criteria>
- `lib/email.ts` compiles and exports the two new functions following the existing send-function conventions exactly
</success_criteria>

<output>
Create `.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-02-SUMMARY.md` when done
</output>
