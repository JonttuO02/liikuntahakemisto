---
phase: 60-hallintaoikeuspyynn-t-backend-s-hk-posti
plan: 05
type: execute
wave: 3
depends_on: ["60-03"]
files_modified:
  - app/business/liity/page.tsx
  - app/business/page.tsx
  - messages/fi.json
  - messages/en.json
requirements: [ACCESS-03, ACCESS-06]
autonomous: true
must_haves:
  truths:
    - "An approved owner sees a 'Kopioi kutsulinkki' button on each approved venue row that copies /business/liity?paikka_id=X to the clipboard"
    - "A requester with a pending business_access_requests row sees a 'Pyyntosi odottaa hyvaksyntaa' banner on /business"
    - "Opening /business/liity?paikka_id=X drives the signup-or-submit flow and surfaces D-08/D-09/D-10 states (pending banner / friendly errors)"
    - "All new copy is in the next-intl Business namespace (fi + en), no hardcoded strings"
  artifacts:
    - path: "app/business/liity/page.tsx"
      provides: "Deep-link landing page that reads ?paikka_id and routes to signup-or-submit"
      min_lines: 40
    - path: "app/business/page.tsx"
      provides: "Copy invite link button on approved venue rows + pending-request banner"
    - path: "messages/fi.json"
      provides: "Finnish copy for invite/pending/error states under Business namespace"
      contains: "accessRequestPendingTitle"
  key_links:
    - from: "app/business/liity/page.tsx"
      to: "POST /api/business/access-request/submit"
      via: "fetch with Authorization Bearer token on Laheta pyynto"
      pattern: "access-request/submit"
    - from: "app/business/page.tsx pending banner"
      to: "business_access_requests"
      via: "client-side createBusinessBrowserClient query for the requester's pending row"
      pattern: "business_access_requests"
---

<objective>
Add the three minimal user-facing surfaces this backend phase needs to be testable end-to-end: the owner's "Kopioi kutsulinkki" button, the requester's pending-request banner on `/business`, and the `/business/liity` deep-link landing page. All copy goes through next-intl. No new design tokens — reuse the existing StatusCard/VenueRow glassmorphism patterns per the UI-SPEC.

Purpose: makes the link-generate -> request -> pending-status loop usable and testable (ACCESS-03), and surfaces the pre-approval "odottaa hyvaksyntaa" state (ACCESS-06 requester-facing half). Depends on Plan 03 (submit endpoint + invite signup path).
Output: one new page, modifications to `app/business/page.tsx`, and new i18n keys in fi/en.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-CONTEXT.md
@.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-UI-SPEC.md
@.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-PATTERNS.md
@app/business/page.tsx
@lib/supabase-business.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add invite/pending/error i18n keys to the Business namespace (fi + en)</name>
  <files>messages/fi.json, messages/en.json</files>
  <read_first>
    - messages/fi.json (Business namespace — existing dashboardStatusPendingTitle/Body, reapplyCta keys to sit alongside)
    - messages/en.json (Business namespace — mirror the same keys)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-UI-SPEC.md (Copywriting Contract — exact Finnish copy for every new key)
  </read_first>
  <action>
    Add these keys under the existing `Business` namespace in BOTH `messages/fi.json` and `messages/en.json` (Finnish copy is locked by the UI-SPEC; provide natural English equivalents for en.json):

    copyInviteLinkCta = "Kopioi kutsulinkki"; inviteLinkCopied = "Linkki kopioitu"; accessRequestPendingTitle = "Pyyntosi odottaa hyvaksyntaa"; accessRequestPendingBody = "Paikan paahallitsija kasittelee pyyntosi pian. Saat sahkopostin, kun paatos on tehty."; liityHeading = "Liity yrityksen tiimiin"; liityBody = "Pyyda hallintaoikeutta tahan paikkaan. Paahallitsija hyvaksyy pyyntosi."; liitySubmitCta = "Laheta pyynto"; liityErrorInvalidLink = "Tama kutsulinkki ei ole voimassa. Pyyda uutta linkkia paikan paahallitsijalta."; liityErrorAlreadyInCompany = "Tilisi on jo liitetty toiseen yritykseen. Ota yhteytta paahallitsijaan, jos tarvitset paasyn tahan paikkaan."

    Use the proper Finnish characters (a-umlaut/o-umlaut) verbatim from the UI-SPEC Copywriting Contract table — the simplified spellings above are only to keep this prose ASCII. Keep keys grouped consistently with the file's existing convention. Do not remove or rename existing keys. Ensure both JSON files remain valid (no trailing commas).
  </action>
  <verify>
    <automated>node -e "const fi=require('./messages/fi.json'); const en=require('./messages/en.json'); const need=['copyInviteLinkCta','inviteLinkCopied','accessRequestPendingTitle','accessRequestPendingBody','liityHeading','liityBody','liitySubmitCta','liityErrorInvalidLink','liityErrorAlreadyInCompany']; const miss=need.filter(k=>!fi.Business||!fi.Business[k]||!en.Business||!en.Business[k]); if(miss.length){console.error('MISSING',miss);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 9 new keys exist under `Business` in both `messages/fi.json` and `messages/en.json`
    - Both JSON files parse without error (the node verify command exits 0)
    - Existing Business keys are unchanged
    - Finnish copy matches the UI-SPEC Copywriting Contract exactly (including diacritics)
  </acceptance_criteria>
  <done>The 9 keys exist in both locale files, JSON is valid, and the verify command prints OK.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Copy invite link button and pending-request banner to app/business/page.tsx</name>
  <files>app/business/page.tsx</files>
  <read_first>
    - app/business/page.tsx (full file — StatusCard component, VenueRow component, the createBusinessBrowserClient client-fetch pattern, the per-venue action row where CTAs live)
    - lib/supabase-business.ts (createBusinessBrowserClient — the client to query business_access_requests)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-UI-SPEC.md (Interaction States + Color + Copywriting — button placement, amber-400 pending banner reuse, clipboard text-swap)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-PATTERNS.md (app/business/page.tsx section — exact .glass rounded-2xl p-4 ... border-l-4 banner shape to reuse)
  </read_first>
  <action>
    Modify `app/business/page.tsx` (client component). Two additions, reusing existing patterns — introduce no new color/typography/components.

    (a) Copy-invite-link button: in the per-venue action row of `VenueRow` (alongside the existing preview/edit CTAs), render a button labelled `t('copyInviteLinkCta')` ONLY for venues where `link.claim_status === 'approved'` (only an approved owner can invite). On click, call `navigator.clipboard.writeText(\`${window.location.origin}/business/liity?paikka_id=${link.paikka_id}\`)` — pure client-side, no API call. On success, swap the button label to `t('inviteLinkCopied')` via a `useState` for ~2 seconds, then revert. Style it consistently with the existing sibling CTAs (reuse `buttonVariants()` or the same Tailwind classes the neighboring CTAs use); if rendered icon-only, ensure a 44px touch target per the UI-SPEC spacing note.

    (b) Pending-request banner: add a client-side fetch (in the existing `useEffect`/data-load path that already uses `createBusinessBrowserClient()`) querying `business_access_requests` for the current user's pending row: `.from('business_access_requests').select('id, paikka_id, status').eq('status', 'pending')` (RLS already scopes to `requester_id = auth.uid()`). Hold the result in state. When a pending row exists, render a new banner using the SAME card shape as StatusCard's pending variant: `<div className="glass rounded-2xl p-4 flex flex-col gap-1 border-l-4 border-amber-400">` with title `t('accessRequestPendingTitle')` (`text-sm font-bold text-[#111111]`) and body `t('accessRequestPendingBody')` (`text-xs text-[rgba(17,17,17,0.45)]`). Place it in the same banner region StatusCard renders into. Do NOT introduce a new color or a new card style.

    All strings via `useTranslations('Business')` — no hardcoded text (CLAUDE.md i18n constraint).
  </action>
  <verify>
    <automated>grep -q "copyInviteLinkCta" app/business/page.tsx && grep -q "business_access_requests" app/business/page.tsx && grep -q "accessRequestPendingTitle" app/business/page.tsx && grep -q "/business/liity?paikka_id=" app/business/page.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - A "Kopioi kutsulinkki" button renders only on approved venue rows and copies `${origin}/business/liity?paikka_id=${paikka_id}` via `navigator.clipboard.writeText`, with a ~2s "Linkki kopioitu" confirmation swap
    - A pending-request banner reads from a client-side `business_access_requests` pending query and renders with the existing `.glass rounded-2xl p-4 ... border-l-4 border-amber-400` shape when a pending row exists
    - No hardcoded UI strings — all via `t('...')` in the Business namespace
    - No new color tokens, typography sizes, or shadcn components introduced
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>The approved-venue rows show the copy-link button with confirmation feedback, the pending banner appears for requesters with a pending row, and type-check passes.</done>
</task>

<task type="auto">
  <name>Task 3: Create the /business/liity deep-link landing page</name>
  <files>app/business/liity/page.tsx</files>
  <read_first>
    - app/business/page.tsx (createBusinessBrowserClient client-fetch idiom + auth-session handling to mirror)
    - lib/supabase-business.ts (createBusinessBrowserClient + how to get the session access token for the Authorization header)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-UI-SPEC.md (Interaction States table for /business/liity — every state's behavior and copy)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-RESEARCH.md (Deep-Link Invite Flow — link consumption steps; A1 route name, A2 reuse existing signup)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-CONTEXT.md (D-03 signup-via-link, D-08/D-09/D-10 states)
  </read_first>
  <action>
    Create `app/business/liity/page.tsx` as a `'use client'` component using `createBusinessBrowserClient()` and `useTranslations('Business')`. Read `paikka_id` from the URL search params (`useSearchParams`). This is intentionally bare per D-03 (no design polish, no marketing copy).

    States (per UI-SPEC Interaction States table):
    - Heading `t('liityHeading')` + body `t('liityBody')` as the page shell.
    - Not authenticated as a business account / no business_accounts row: route the user to the existing signup flow (reuse `/business/rekisteroidy` styling/form unchanged per A2; the invite path should ultimately register via the Plan 03 `invite: true` no-auto-company path). Do not build a new signup screen.
    - Authenticated business account, no pending request yet: show the venue context + a `t('liitySubmitCta')` ("Laheta pyynto") button. On click, POST to `/api/business/access-request/submit` with `{ paikka_id }` and an `Authorization: Bearer <session access token>` header (obtain the token from the business browser client session, same way other authenticated client fetches in this codebase do). Disable the button while in-flight.
    - On submit success (new OR idempotent duplicate, D-08): show the same pending state copy (`accessRequestPendingTitle`/`accessRequestPendingBody`) — no error.
    - On a 400 corresponding to D-10 (invalid/no-owner link): show `t('liityErrorInvalidLink')` in `text-red-600`, no submit button.
    - On a 400 corresponding to D-09 (already in a company): show `t('liityErrorAlreadyInCompany')` in `text-red-600`, no submit button.
    - Malformed/missing `paikka_id` in the URL: treat as the invalid-link error state.

    Reuse existing glassmorphism classes (`.glass rounded-2xl p-4`, the `text-sm`/`text-xs`/`text-xl font-bold` scale) — no new design tokens. Largest heading is `text-xl font-bold text-[#111111]` (no hero on this minimal page, per UI-SPEC Typography).
  </action>
  <verify>
    <automated>grep -q "useSearchParams" app/business/liity/page.tsx && grep -q "access-request/submit" app/business/liity/page.tsx && grep -q "liitySubmitCta" app/business/liity/page.tsx && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/business/liity/page.tsx` is a `'use client'` page reading `paikka_id` via `useSearchParams`
    - Authenticated-with-no-pending state renders the `liitySubmitCta` button that POSTs `{ paikka_id }` to `/api/business/access-request/submit` with an Authorization Bearer token, disabled while in-flight
    - Submit success (including idempotent duplicate) renders the pending banner copy, not an error
    - D-10 and D-09 error responses render `liityErrorInvalidLink` / `liityErrorAlreadyInCompany` respectively in `text-red-600` with no submit button; malformed paikka_id is treated as invalid-link
    - Unauthenticated visitors are routed to the existing signup flow (no new signup screen built)
    - All strings via `t('...')`; no new design tokens; `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>The landing page reads the paikka_id, drives signup-or-submit, and renders pending/error states using existing patterns; type-check passes.</done>
</task>

</tasks>

<artifacts_produced>
- New file: `app/business/liity/page.tsx` (deep-link landing page)
- Modified: `app/business/page.tsx` — Copy invite link button on approved venue rows + pending-request banner + business_access_requests client query
- Modified: `messages/fi.json` and `messages/en.json` — 9 new Business-namespace keys (copyInviteLinkCta, inviteLinkCopied, accessRequestPendingTitle, accessRequestPendingBody, liityHeading, liityBody, liitySubmitCta, liityErrorInvalidLink, liityErrorAlreadyInCompany)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> POST /access-request/submit | The liity page sends paikka_id + bearer token; server re-validates everything |
| client -> business_access_requests SELECT | RLS self-scopes the pending-banner query to the requester |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-13 | Spoofing | client-side guard bypass | mitigate | Client D-09/D-10 checks are UX-only; the submit endpoint (Plan 03) re-runs all guards server-side — never trusted client-side |
| T-60-14 | Information Disclosure | pending banner query | mitigate | business_access_requests SELECT RLS self-scopes to auth.uid() = requester_id (Plan 01) |
| T-60-15 | Tampering | error message rendering | accept | Error copy is static i18n strings, not server-echoed user input — no injection surface |
| T-60-SC | Tampering | npm installs | accept | Zero new packages; reuses existing next-intl / supabase-business client |
</threat_model>

<verification>
- `npx tsc --noEmit` clean after all tasks; both JSON locale files valid
- Manual (per VALIDATION.md): owner copies link from an approved venue row; opening the link as a fresh session drives signup (no auto-company) then submit -> pending banner; second open -> still pending (idempotent); malformed/no-owner link -> invalid-link error; account already in a company -> already-in-company error
</verification>

<success_criteria>
- The owner can copy an invite link, a requester can submit via that link and see a clear pending state, and D-08/D-09/D-10 states surface with friendly Finnish copy — all using existing design patterns and i18n
</success_criteria>

<output>
Create `.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-05-SUMMARY.md` when done
</output>
