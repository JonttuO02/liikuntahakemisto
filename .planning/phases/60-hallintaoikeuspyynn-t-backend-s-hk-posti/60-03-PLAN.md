---
phase: 60-hallintaoikeuspyynn-t-backend-s-hk-posti
plan: 03
type: execute
wave: 2
depends_on: ["60-01", "60-02"]
files_modified:
  - app/api/business/register/route.ts
  - app/api/business/access-request/submit/route.ts
requirements: [ACCESS-03, ACCESS-05]
autonomous: true
must_haves:
  truths:
    - "An invite-link signup creates a business_accounts row with company_id = NULL (no auto-created company) — D-09a"
    - "POST /api/business/access-request/submit verifies JWT, blocks if the requester already belongs to a company (D-09), rejects if the venue has no approved owner (D-10), and is idempotent on duplicate pending submission (D-08)"
    - "On a successful new submission the venue's approved owner receives a Resend notification email (non-critical try/catch)"
    - "A submitted request lands in business_access_requests as status='pending'"
  artifacts:
    - path: "app/api/business/access-request/submit/route.ts"
      provides: "Access-request submission endpoint with D-08/D-09/D-10 guards and owner notification email"
      exports: ["POST"]
    - path: "app/api/business/register/route.ts"
      provides: "Existing signup endpoint extended with a no-auto-company invite-link path (D-09a)"
      exports: ["POST"]
  key_links:
    - from: "app/api/business/access-request/submit/route.ts"
      to: "business_access_requests table"
      via: "supabaseAdmin insert with 23505 idempotency catch"
      pattern: "business_access_requests"
    - from: "app/api/business/access-request/submit/route.ts"
      to: "sendAccessRequestNotificationEmail"
      via: "non-critical owner notification after insert"
      pattern: "sendAccessRequestNotificationEmail"
---

<objective>
Build the access-request submission endpoint and the no-auto-company invite-link signup path. The submit endpoint runs the D-09/D-10 server-side guards, performs the D-08 idempotent insert, and fires the owner-notification email. The signup path lets a brand-new invite-link employee register without an auto-created company so D-09's guard works as intended.

Purpose: ACCESS-03 (submission lands as pending via invite link) and the submission half of ACCESS-05 (owner notified). Depends on Plan 01 (table + nullable company_id) and Plan 02 (email functions).
Output: One new Route Handler + an extended register endpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-CONTEXT.md
@.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-RESEARCH.md
@.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-PATTERNS.md
@app/api/business/reapply/route.ts
@app/api/business/register/route.ts
@app/api/admin/approve/route.ts
@lib/email.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend register endpoint with a no-auto-company invite-link signup path (D-09a)</name>
  <files>app/api/business/register/route.ts</files>
  <read_first>
    - app/api/business/register/route.ts (full file — the JWT verify, body parse, companies-insert-then-business_accounts-insert-with-rollback shape this task must conditionally skip)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-CONTEXT.md (D-09, D-09a — the locked outcome: invite-link signups don't get an auto-created company)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-RESEARCH.md (Pitfall 1, Open Question 1, Assumption A2 — why a parameterized path is needed)
  </read_first>
  <action>
    Modify `app/api/business/register/route.ts` to accept an optional `invite: boolean` flag in the request body (default false). Parse it alongside the existing `company_name`/`role_in_company` parse, treating it as `body.invite === true`.

    When `invite === true` (invite-link signup, per D-09a): SKIP the `companies` INSERT entirely and INSERT the `business_accounts` row using the verified `user.id` with `company_id: null`, `role: 'member'`, and `role_in_company` as before. In this branch `company_name` is NOT required (an invite-link employee is joining an existing company, not naming a new one) — relax the missing-`company_name` 400 so it only fires in the non-invite branch. Do NOT create a company; `company_id` stays NULL until the access-request approval sets it (Plan 04).

    When `invite` is falsy (existing default flow): keep the current behavior unchanged — INSERT companies, then business_accounts with `company_id: newCompanyId, role: 'owner'`, including the existing rollback-on-failure delete of the orphan companies row.

    Use the verified `user.id` for the business_accounts INSERT in BOTH branches — never `body.user_id`. Keep the security comment about not trusting body-supplied identity.
  </action>
  <verify>
    <automated>grep -q "invite" app/api/business/register/route.ts && grep -q "company_id: null" app/api/business/register/route.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The register route reads an `invite` flag from the body and branches on it
    - The invite branch inserts business_accounts with `company_id: null` and `role: 'member'`, and does NOT insert a companies row
    - The invite branch does not 400 on a missing `company_name`
    - The non-invite branch is behaviorally unchanged (companies insert + rollback + role:'owner')
    - Both branches use the JWT-verified `user.id`, never `body.user_id`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Register endpoint supports the no-auto-company invite path with company_id NULL, the default path is unchanged, and type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create the access-request submit Route Handler with D-08/D-09/D-10 guards and owner-notification email</name>
  <files>app/api/business/access-request/submit/route.ts</files>
  <read_first>
    - app/api/business/reapply/route.ts (full file — JWT verify, body parse with parseInt+isNaN, lookup-then-branch, non-critical email shape)
    - app/api/admin/approve/route.ts (JWT verify + supabaseAdmin patterns)
    - lib/email.ts (sendAccessRequestNotificationEmail signature added in Plan 02)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-RESEARCH.md (Pattern 3 idempotent insert; Submit endpoint validation order; Pitfall 5 venue-scoped owner lookup; the Architecture diagram's submit branch)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-CONTEXT.md (D-08, D-09, D-10)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-UI-SPEC.md (error copy for D-09/D-10 — friendly Finnish messages)
  </read_first>
  <action>
    Create `app/api/business/access-request/submit/route.ts` exporting an async `POST(request: Request)`. Follow the reapply route's structural shape. Validation order:

    1. JWT verify: read `Authorization: Bearer` token, `supabaseAdmin.auth.getUser(token)`, return 401 if `authError || !user`. Never trust `body.requester_id`.
    2. Parse `paikka_id` from body with `parseInt(body.paikka_id, 10)` + `isNaN` guard -> 400 on missing/invalid.
    3. D-09 guard: SELECT `company_id` from `business_accounts` WHERE `user_id = user.id` (maybeSingle). If no row -> 400 (must sign up first). If `row.company_id` is not null -> 400 with a friendly Finnish error indicating the account already belongs to a company (matches UI-SPEC D-09 copy).
    4. D-10 guard: SELECT `1`/`id` from `business_paikka_links` WHERE `paikka_id = paikkaId AND claim_status = 'approved'` (maybeSingle). If no row -> 400 with the friendly Finnish "invite link not valid / no current owner" error (matches UI-SPEC D-10 copy). No orphan request row is created.
    5. D-08 idempotent insert: `supabaseAdmin.from('business_access_requests').insert({ requester_id: user.id, paikka_id: paikkaId }).select().single()`. If `insertError?.code === '23505'`, re-fetch the existing pending row (`.eq('requester_id', user.id).eq('paikka_id', paikkaId).eq('status','pending').single()`) and return `{ ok: true, request: existing }`. If another insert error -> 500. Otherwise return `{ ok: true, request: inserted }`.
    6. Non-critical owner-notification email (try/catch around the whole block, console.error with `[access-request/submit]` prefix on failure, never block the 200): resolve the venue's approved owner by joining `business_paikka_links` (paikka_id = paikkaId, claim_status='approved') to `business_accounts` filtered by `role = 'owner'` to get the owner's `user_id`, then `supabaseAdmin.auth.admin.getUserById(ownerUserId)` for the owner email; fetch the venue `nimi` from `liikuntapaikat`; resolve the requester's display identity (email via `supabaseAdmin.auth.admin.getUserById(user.id)` or the account's name field — requesterName param). Call `sendAccessRequestNotificationEmail(ownerEmail, { requesterName, venueName, requestId })` only when owner email + venue resolved. Scope the owner lookup by `paikka_id`, never company-wide (Pitfall 5).

    Place the email block AFTER the insert/idempotency resolution so it fires for new submissions; for the idempotent-duplicate path returning early, the email is not re-sent (avoid duplicate notifications).
  </action>
  <verify>
    <automated>grep -q "export async function POST" app/api/business/access-request/submit/route.ts && grep -q "23505" app/api/business/access-request/submit/route.ts && grep -q "sendAccessRequestNotificationEmail" app/api/business/access-request/submit/route.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exports `POST`; JWT verified via `supabaseAdmin.auth.getUser(token)`; body `paikka_id` parsed with isNaN guard
    - D-09 guard rejects with 400 when the requester's `company_id` is not null (and when no business_accounts row exists)
    - D-10 guard rejects with 400 when no approved `business_paikka_links` row exists for `paikka_id`; no request row is inserted in that case
    - D-08 idempotency: a unique-violation (`23505`) re-fetches and returns the existing pending row with `{ ok: true }` (status 200), not an error
    - Owner-notification email is sent via `sendAccessRequestNotificationEmail` inside a try/catch and never blocks the response; the owner is resolved venue-scoped (paikka_id + role='owner'), not company-wide
    - `requester_id` written from the JWT-verified `user.id`, never `body.requester_id`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>The submit endpoint enforces all three guards, is idempotent on duplicate pending submission, sends the owner email non-critically, and type-checks.</done>
</task>

</tasks>

<artifacts_produced>
- New file: `app/api/business/access-request/submit/route.ts` exporting `POST`
- Modified: `app/api/business/register/route.ts` — `invite` flag branch (no-auto-company, company_id NULL, role 'member')
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> POST /access-request/submit | Untrusted body (paikka_id, invite flag) crosses here |
| supabaseAdmin -> business_access_requests / business_accounts | Trusted service-role writes bypass RLS |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-07 | Spoofing / Elevation of Privilege | requester identity | mitigate | requester_id taken from JWT-verified user.id, never body.requester_id (V2/V4) |
| T-60-08 | Tampering | paikka_id input | mitigate | parseInt + isNaN guard before any DB read (V5 input validation) |
| T-60-09 | Elevation of Privilege | access to venue without owner | mitigate | D-10 guard requires an approved business_paikka_links owner row before insert |
| T-60-10 | Denial of Service | duplicate pending request flooding | mitigate | D-08 partial UNIQUE + idempotent 23505 catch returns existing row |
| T-60-05 | Tampering | owner email subject/body | mitigate | sendAccessRequestNotificationEmail uses sub()/esc() (from Plan 02) |
| T-60-SC | Tampering | npm installs | accept | Zero new packages |
</threat_model>

<verification>
- `npx tsc --noEmit` clean after both tasks
- Manual (per VALIDATION.md): curl the submit endpoint as a fresh invite-signup session (company_id NULL) -> request lands pending; repeat -> idempotent same row; malformed/no-owner paikka_id -> friendly 400, no orphan row; account with non-null company_id -> 400 D-09 error
</verification>

<success_criteria>
- Invite-link signup yields a business_accounts row with NULL company_id
- Submit endpoint enforces D-08/D-09/D-10 and notifies the owner on new submissions
</success_criteria>

<output>
Create `.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-03-SUMMARY.md` when done
</output>
