---
phase: 60-hallintaoikeuspyynn-t-backend-s-hk-posti
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - middleware.ts
requirements: [ACCESS-03, ACCESS-05, ACCESS-06]
autonomous: true
must_haves:
  truths:
    - "An unauthenticated visitor opening /business/liity?paikka_id=X reaches the liity client page instead of being bounced to /business/kirjaudu by middleware"
    - "The liity page's own useEffect redirect logic (to /business/rekisteroidy?paikka_id=X for unauthenticated users) is allowed to run"
    - "The existing business auth guard still protects every other /business route (dashboard, paikka edit, etc.) for unauthenticated users"
  artifacts:
    - path: "middleware.ts"
      provides: "Business auth guard whose isPublicBusinessPath excludes /business/liity alongside /business/rekisteroidy and /business/kirjaudu"
      contains: "/business/liity"
  key_links:
    - from: "middleware.ts isPublicBusinessPath"
      to: "app/business/liity/page.tsx useEffect redirect"
      via: "middleware no longer intercepts /business/liity, so the client page's own redirect logic runs"
      pattern: "/business/liity"
---

<objective>
Fix the UAT Test 2 gap: unauthenticated visitors to `/business/liity` are redirected to `/business/kirjaudu` by the business auth guard in `middleware.ts` BEFORE the client page's own `useEffect` redirect (to `/business/rekisteroidy?paikka_id=X`) can run. `/business/liity` is a public deep-link landing path (invite consumption) and must be excluded from the auth guard, exactly like `/business/rekisteroidy` and `/business/kirjaudu`.

Purpose: completes the deep-link invite flow (ACCESS-03) and unblocks the requester-facing access-request states (ACCESS-05/ACCESS-06) so an invited user can actually reach the liity page. UAT Tests 3 and 4 are blocked by this same one-line bug and become runnable once it lands.
Output: a single-line addition to the `isPublicBusinessPath` predicate in `middleware.ts`, with the documenting comment updated to list the new public path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-UAT.md
@middleware.ts
@app/business/liity/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /business/liity to isPublicBusinessPath in middleware.ts</name>
  <files>middleware.ts</files>
  <read_first>
    - middleware.ts (full file — the business branch, the `isPublicBusinessPath` predicate on lines 35-37, and the documenting comment on lines 33-34)
    - app/business/liity/page.tsx (confirm it is a `'use client'` page whose unauthenticated redirect lives in a useEffect that never runs while middleware intercepts)
    - .planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-UAT.md (Test 2 failure — root cause and intended behavior)
  </read_first>
  <action>
    In `middleware.ts`, extend the `isPublicBusinessPath` predicate (currently `pathname.startsWith('/business/rekisteroidy') || pathname.startsWith('/business/kirjaudu')`) to also include `pathname.startsWith('/business/liity')`. Add it as a third `||` clause.

    Also update the preceding documenting comment (currently: "Excluded paths: /business/rekisteroidy and /business/kirjaudu (public entry points)") to also name `/business/liity` so the comment continues to accurately list every public business path. Do NOT remove or restructure the comment — only extend it to mention the new path (e.g. note that `/business/liity` is the public deep-link invite landing page whose client-side useEffect handles its own redirect).

    Change nothing else: the consumer branch, the cookie/session refresh logic, the `!isPublicBusinessPath && !user` redirect to `/business/kirjaudu`, and the matcher config must remain byte-for-byte identical.
  </action>
  <verify>
    <automated>grep -q "pathname.startsWith('/business/liity')" middleware.ts && grep -q "/business/liity" middleware.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `middleware.ts` `isPublicBusinessPath` evaluates true for any path under `/business/liity` (new `pathname.startsWith('/business/liity')` clause present)
    - The original two clauses (`/business/rekisteroidy`, `/business/kirjaudu`) are unchanged and still present
    - The documenting comment above the predicate names `/business/liity` as a public path (the comment is preserved, not deleted)
    - The auth-guard redirect (`!isPublicBusinessPath && !user` -> `/business/kirjaudu`) and all session/cookie logic are otherwise unchanged
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>An unauthenticated request to `/business/liity?paikka_id=X` is no longer redirected by middleware to `/business/kirjaudu`; the request reaches the liity client page whose own useEffect handles the unauthenticated redirect. Type-check passes.</done>
</task>

</tasks>

<artifacts_produced>
- Modified: `middleware.ts` — `isPublicBusinessPath` now excludes `/business/liity` from the business auth guard (third clause), with the documenting comment extended to list the new public path
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client -> /business/liity | Public deep-link landing page; no authenticated data is served before the page routes the visitor to signup/submit |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-16 | Elevation of Privilege | middleware public-path exclusion | medium | mitigate | `/business/liity` serves no privileged data — it is a thin client landing page that only reads `paikka_id` and, for authenticated users, POSTs to `/api/business/access-request/submit` which re-runs all guards server-side (Plan 03). Excluding it from the auth guard exposes no protected resource. |
| T-60-17 | Spoofing | over-broad startsWith match | low | accept | `pathname.startsWith('/business/liity')` matches only the intended public invite landing path; no other `/business` route shares that prefix. |
| T-60-SC | Tampering | npm installs | low | accept | Zero new packages; edits one existing predicate. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean after the edit
- Manual (re-run UAT Test 2): in a fresh/unauthenticated session, open `/business/liity?paikka_id=X`; the browser must land on the liity page (then the page's own useEffect redirects to `/business/rekisteroidy?paikka_id=X`), NOT on `/business/kirjaudu`
- Regression: opening any other `/business` route (e.g. `/business`) while unauthenticated still redirects to `/business/kirjaudu`
- Tests 3 and 4 (previously blocked by this bug) become runnable
</verification>

<success_criteria>
- Unauthenticated visitors reach `/business/liity` and its client-side redirect runs, while all other `/business` routes remain guarded — completing ACCESS-03 / ACCESS-05 / ACCESS-06 and unblocking UAT Tests 2, 3, and 4
</success_criteria>

<output>
Create `.planning/phases/60-hallintaoikeuspyynn-t-backend-s-hk-posti/60-06-SUMMARY.md` when done
</output>
