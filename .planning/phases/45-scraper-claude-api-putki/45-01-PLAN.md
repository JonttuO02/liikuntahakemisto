---
phase: 45-scraper-claude-api-putki
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - supabase/migrations/20260616000001_business_media_bucket.sql
  - lib/branding/scraper.test.ts
  - lib/branding/analyzer.test.ts
autonomous: true
requirements:
  - SCRAP-01
  - SCRAP-02
  - SCRAP-03
  - SCRAP-04
  - SCRAP-05

must_haves:
  truths:
    - "npm install succeeds with sharp and @vercel/functions as production dependencies"
    - "supabase db push creates the business-media Storage bucket without errors"
    - "npx vitest run lib/branding runs (stubs fail with MISSING_IMPL placeholder, not crash)"
  artifacts:
    - path: "package.json"
      provides: "sharp and @vercel/functions in dependencies block (not devDependencies)"
      contains: "sharp"
    - path: "supabase/migrations/20260616000001_business_media_bucket.sql"
      provides: "idempotent Storage bucket INSERT"
      contains: "ON CONFLICT (id) DO NOTHING"
    - path: "lib/branding/scraper.test.ts"
      provides: "failing test stubs for SCRAP-01, SCRAP-02, SCRAP-03, SCRAP-05"
    - path: "lib/branding/analyzer.test.ts"
      provides: "failing test stubs for SCRAP-04 (Claude mocked)"
  key_links:
    - from: "lib/branding/scraper.test.ts"
      to: "lib/branding/scraper.ts"
      via: "import"
      pattern: "from './scraper'"
    - from: "lib/branding/analyzer.test.ts"
      to: "lib/branding/analyzer.ts"
      via: "import"
      pattern: "from './analyzer'"
---

<objective>
Foundation prerequisites for the Phase 45 scraper pipeline: install two missing production
dependencies (sharp, @vercel/functions), create the Supabase Storage bucket migration, push
the schema, and create test stub files that Wave 1+ tasks will fill with assertions.

Purpose: Every downstream plan depends on these artifacts existing. The bucket must exist before
any logo upload can succeed. Test stubs must exist before implementation tasks can verify
GREEN state via Vitest.

Output: Updated package.json, migration SQL, pushed Storage bucket, two test stub files.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/45-scraper-claude-api-putki/45-CONTEXT.md
@.planning/phases/45-scraper-claude-api-putki/45-RESEARCH.md
@.planning/phases/45-scraper-claude-api-putki/45-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install production dependencies and create Storage bucket migration</name>
  <read_first>
    - package.json (current state — verify sharp and @vercel/functions are absent from both dependencies and devDependencies before adding)
    - supabase/migrations/20260615000001_business_branding.sql (comment header style to replicate)
    - .planning/phases/45-scraper-claude-api-putki/45-RESEARCH.md §"Package Legitimacy Audit" (all three packages are [OK])
    - .planning/phases/45-scraper-claude-api-putki/45-CONTEXT.md §D-17 (bucket SQL exact form)
  </read_first>
  <action>
    Run `npm install sharp @vercel/functions` to add both as production dependencies (not --save-dev).
    @anthropic-ai/sdk is already installed at ^0.97.1 — do not reinstall or change its version.

    After install, verify package.json has "sharp": "^0.35.1" and "@vercel/functions": "^3.7.1" in
    the "dependencies" block (not "devDependencies").

    Create supabase/migrations/20260616000001_business_media_bucket.sql with:
    - Comment header block matching the style in 20260615000001_business_branding.sql: phase name,
      purpose, path pattern, idempotency note
    - Single SQL statement per D-17:
      INSERT INTO storage.buckets (id, name, public)
      VALUES ('business-media', 'business-media', true)
      ON CONFLICT (id) DO NOTHING;
    - No RLS policies — the bucket is public (logos are public images, per D-02)

    File name must be exactly: 20260616000001_business_media_bucket.sql (timestamp > 20260615*).
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); const d = p.dependencies; if (!d.sharp) throw new Error('sharp missing'); if (!d['@vercel/functions']) throw new Error('@vercel/functions missing'); console.log('OK sharp=' + d.sharp + ' vercel=' + d['@vercel/functions'])"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies block contains "sharp" (not in devDependencies)
    - package.json dependencies block contains "@vercel/functions" (not in devDependencies)
    - supabase/migrations/20260616000001_business_media_bucket.sql exists
    - Migration file contains exactly: INSERT INTO storage.buckets (id, name, public) VALUES ('business-media', 'business-media', true) ON CONFLICT (id) DO NOTHING;
    - node_modules/sharp directory exists after npm install
    - node_modules/@vercel/functions directory exists after npm install
  </acceptance_criteria>
  <done>Both packages installed as production deps; migration file created with idempotent bucket INSERT.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push Supabase schema to create business-media bucket</name>
  <read_first>
    - supabase/migrations/20260616000001_business_media_bucket.sql (the file created in Task 1 — verify it exists before pushing)
    - .planning/phases/45-scraper-claude-api-putki/45-RESEARCH.md §"Pitfall 6: storage.buckets INSERT migraatio ja squash" (do not run migration squash after this push)
  </read_first>
  <action>
    Run: supabase db push

    This applies 20260616000001_business_media_bucket.sql to the remote Supabase project, creating
    the business-media bucket via SQL INSERT.

    If supabase db push prompts interactively (e.g. "Are you sure?"), answer yes/y. If it requires
    authentication and no supabase session exists, mark this task for manual intervention: the user
    must run `supabase login` then `supabase db push` manually.

    After push: verify the bucket exists by running:
    supabase db remote-get --schema storage 2>/dev/null | grep -i business-media
    OR by checking the Supabase dashboard Storage section.

    Do NOT run `supabase migration squash` at any point during Phase 45 (Pitfall 6: known CLI bug
    that deletes bucket INSERTs from migration history).
  </action>
  <verify>
    <automated>supabase db push 2>&1 | grep -E "Success|success|Applied|applied|business_media_bucket|already applied"</automated>
  </verify>
  <acceptance_criteria>
    - supabase db push exits without error (exit code 0)
    - Command output contains "Success" or "Applied" or "already applied"
    - The business-media bucket row is present in storage.buckets on the remote Supabase project (verifiable via Supabase dashboard → Storage → Buckets)
  </acceptance_criteria>
  <done>business-media Storage bucket exists on remote Supabase. Phase 45 logo uploads will succeed.</done>
</task>

<task type="auto">
  <name>Task 3: Create Vitest test stub files for lib/branding/</name>
  <read_first>
    - .planning/phases/45-scraper-claude-api-putki/45-VALIDATION.md (per-task verification map — which requirements each test file covers)
    - .planning/phases/45-scraper-claude-api-putki/45-PATTERNS.md §"Vitest test file pattern" (import style from lib/onboardingUtils.test.ts)
    - vitest.config.ts (confirm lib/**/*.test.ts is covered — no config change needed)
  </read_first>
  <action>
    Create lib/branding/scraper.test.ts with stubs for SCRAP-01, SCRAP-02, SCRAP-03, and SCRAP-05.
    Each stub must call the real function import and use expect().toBeDefined() or a
    throw new Error('MISSING_IMPL') sentinel so the file runs (does not crash the test runner)
    but all tests fail until the implementation exists.

    Stub structure for scraper.test.ts:
    - Import: import { describe, it, expect } from 'vitest' and import { scrapeWebsite } from './scraper'
    - describe('scrapeWebsite', ...) containing:
      - it('SCRAP-01: fetches HTML with Mozilla User-Agent') — stub
      - it('SCRAP-02: extracts hex colors from theme-color and :root CSS vars') — stub
      - it('SCRAP-03: collects logo candidates in priority order (favicon, og:image, img[*=logo])') — stub
      - it('SCRAP-05: converts logo candidates to PNG base64 via sharp (max 512px)') — stub

    Create lib/branding/analyzer.test.ts with stubs for SCRAP-04.
    Stub structure:
    - Import: import { describe, it, expect, vi } from 'vitest' and import { analyzeWithClaude } from './analyzer'
    - At top of file, mock '@anthropic-ai/sdk' with vi.mock so tests do not make live API calls
    - describe('analyzeWithClaude', ...) containing:
      - it('SCRAP-04: returns structured JSON with logo_index, logo_type, colors') — stub
      - it('SCRAP-04: throws on out-of-bounds logo_index') — stub

    Both files live in lib/branding/ (create directory if it does not exist yet).
    The scraper.ts and analyzer.ts source files do NOT need to exist yet — stubs may use
    try/catch around the import or use vi.mock to avoid import errors.

    Use the import/describe/it pattern from 45-PATTERNS.md §"Vitest test file pattern".
  </action>
  <verify>
    <automated>npx vitest run lib/branding 2>&1 | grep -E "FAIL|PASS|test files|Tests|Error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/branding/scraper.test.ts exists and contains describe('scrapeWebsite', ...)
    - lib/branding/analyzer.test.ts exists and contains describe('analyzeWithClaude', ...)
    - npx vitest run lib/branding runs without crashing the test runner (exit code may be 1 due to failing stubs, but the runner itself completes)
    - Test output shows the stub test names (SCRAP-01, SCRAP-02, SCRAP-03, SCRAP-04, SCRAP-05) in the failure list
    - No "Cannot find module" or "SyntaxError" in vitest output (stubs must be syntactically valid)
  </acceptance_criteria>
  <done>Both test stub files exist in lib/branding/; vitest runs them (failing stubs, not crashes); Wave 1 tasks can fill in real assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → package.json | Third-party packages installed from npm registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-00-01 | Tampering | npm package install (sharp, @vercel/functions) | mitigate | All three packages verified [OK] via slopcheck in RESEARCH.md Package Legitimacy Audit; no postinstall scripts detected |
| T-45-00-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck audit completed — all packages [OK]; no [ASSUMED] or [SUS] packages present |
</threat_model>

<verification>
Run after all three tasks complete:

1. node -e "const p=require('./package.json'); ['sharp','@vercel/functions'].forEach(k=>{if(!p.dependencies[k])throw new Error(k+' missing from dependencies')}); console.log('packages OK')"
2. ls supabase/migrations/20260616000001_business_media_bucket.sql
3. npx vitest run lib/branding 2>&1 | grep -E "test files|Tests"

The test runner must complete (non-crash) even though stubs fail.
</verification>

<success_criteria>
- package.json: "sharp" and "@vercel/functions" present in dependencies (not devDependencies)
- node_modules/sharp and node_modules/@vercel/functions exist
- supabase/migrations/20260616000001_business_media_bucket.sql exists with ON CONFLICT (id) DO NOTHING
- supabase db push succeeds; business-media bucket exists in remote Supabase Storage
- lib/branding/scraper.test.ts and lib/branding/analyzer.test.ts exist with valid syntax
- npx vitest run lib/branding completes without test runner crash
</success_criteria>

<output>
Create .planning/phases/45-scraper-claude-api-putki/45-01-SUMMARY.md when done
</output>
