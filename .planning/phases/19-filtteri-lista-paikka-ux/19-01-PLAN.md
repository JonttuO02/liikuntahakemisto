---
phase: 19-filtteri-lista-paikka-ux
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types.ts
  - supabase/migrations/20260530000000_add_image_url_to_paikat.sql
autonomous: true
requirements:
  - DATA-08

must_haves:
  truths:
    - "Liikuntapaikka type in lib/types.ts has an image_url field typed as `string | null | undefined` (optional)"
    - "A Supabase migration file exists at supabase/migrations/20260530000000_add_image_url_to_paikat.sql"
    - "The migration uses ALTER TABLE paikat ADD COLUMN IF NOT EXISTS image_url TEXT"
    - "No RLS policy changes are made — image_url is a read field like all other paikat columns"
    - "TypeScript compiles without errors after adding the field"
  artifacts:
    - path: "lib/types.ts"
      provides: "Liikuntapaikka type with image_url field"
      contains: "image_url"
    - path: "supabase/migrations/20260530000000_add_image_url_to_paikat.sql"
      provides: "DDL to add image_url TEXT column to paikat table"
      contains: "ADD COLUMN IF NOT EXISTS image_url TEXT"
---

<objective>
Add the image_url field to the Liikuntapaikka TypeScript type and create the Supabase migration that adds the column to the paikat table.

Purpose: DATA-08 — admins need to manually populate a venue photo URL in Supabase; all downstream components (DiagonaalKortti in plan 19-02) depend on this type field being present.

Output: Updated lib/types.ts with image_url added to Liikuntapaikka + new migration file.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/PROJECT.md
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/ROADMAP.md
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/STATE.md
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/phases/19-filtteri-lista-paikka-ux/19-CONTEXT.md

<interfaces>
Current lib/types.ts Liikuntapaikka type (read full file before editing):
```typescript
export type Liikuntapaikka = {
  id: number
  nimi: string
  laji: string
  // ... other fields ...
  featured?: boolean | null
}
```

After this plan, image_url is appended as an optional field at the end of the Liikuntapaikka type:
```typescript
  image_url?: string | null   // Phase 19 / DATA-08 — manual URL populated in Supabase admin
```

Migration file naming convention from existing migrations (e.g. 20260519000000_add_phase1_columns.sql, 20260528083110_profiles.sql):
- Timestamp prefix + descriptive slug + .sql
- New file: 20260530000000_add_image_url_to_paikat.sql
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add image_url to Liikuntapaikka type</name>
  <files>lib/types.ts</files>

  <read_first>
    lib/types.ts — read the full file to see current type structure and last field before appending
  </read_first>

  <action>
    In lib/types.ts, append one field to the Liikuntapaikka type as the last property, before the closing brace.

    The field to add (exactly):
      image_url?: string | null

    Add it after the `featured?: boolean | null` line. No comment needed — keep it concise.

    Do not modify any other types (Suosikki remains unchanged).
  </action>

  <verify>
    <automated>cd /c/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - lib/types.ts CONTAINS the string `image_url?: string | null`
    - lib/types.ts does NOT contain any other new lines — only the single field was added
    - `npx tsc --noEmit` exits with code 0 (no type errors)
  </acceptance_criteria>

  <done>image_url field added to Liikuntapaikka type — TypeScript compiles clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Supabase migration for image_url column</name>
  <files>supabase/migrations/20260530000000_add_image_url_to_paikat.sql</files>

  <read_first>
    supabase/migrations/ — list existing migration files to confirm naming convention and that this timestamp does not conflict
  </read_first>

  <action>
    Create a new file at supabase/migrations/20260530000000_add_image_url_to_paikat.sql with exactly this content:

    ALTER TABLE paikat ADD COLUMN IF NOT EXISTS image_url TEXT;

    A single SQL statement. No RLS policy changes — image_url is a plain read field like all other paikat columns. No NOT NULL constraint — existing rows will have NULL, which is the expected default.
  </action>

  <acceptance_criteria>
    - File supabase/migrations/20260530000000_add_image_url_to_paikat.sql EXISTS
    - File content CONTAINS `ALTER TABLE paikat ADD COLUMN IF NOT EXISTS image_url TEXT`
    - File content does NOT contain any CREATE POLICY or GRANT statements
    - No other migration files were modified
  </acceptance_criteria>

  <done>Migration file created at supabase/migrations/20260530000000_add_image_url_to_paikat.sql with ADD COLUMN IF NOT EXISTS image_url TEXT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase DB column | image_url TEXT — stores admin-provided URL strings; no user input accepted; no RLS changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Information Disclosure | image_url column | accept | Column is read-only via anon key (existing RLS); no new exposure |
| T-19-01-SC | Tampering | npm installs | accept | No new packages — migration and type change only |
</threat_model>

<verification>
After both tasks complete, verify:

1. **Type field present:** `grep "image_url" lib/types.ts` — returns one match containing `image_url?: string | null`

2. **Migration exists:** `ls supabase/migrations/ | grep image_url` — returns `20260530000000_add_image_url_to_paikat.sql`

3. **Migration content:** `cat supabase/migrations/20260530000000_add_image_url_to_paikat.sql` — shows `ALTER TABLE paikat ADD COLUMN IF NOT EXISTS image_url TEXT`

4. **TypeScript clean:** `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- lib/types.ts Liikuntapaikka type has `image_url?: string | null` as the last field
- supabase/migrations/20260530000000_add_image_url_to_paikat.sql exists with the ADD COLUMN IF NOT EXISTS statement
- npx tsc --noEmit passes with exit code 0
</success_criteria>

<output>
Create `.planning/phases/19-filtteri-lista-paikka-ux/19-01-SUMMARY.md` when done
</output>
