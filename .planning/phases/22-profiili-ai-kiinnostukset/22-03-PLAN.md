---
phase: 22-profiili-ai-kiinnostukset
plan: 03
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - app/components/Etusivu.tsx
  - app/api/saasuositus/route.ts
autonomous: true
requirements:
  - PROFILE-02

must_haves:
  truths:
    - "Etusivu.tsx loads kiinnostukset from profiles in subscribeToAuthUser alongside kotikaupunki"
    - "The POST body to /api/saasuositus includes kiinnostukset: string[] when the user is logged in"
    - "The route handler sanitizes body.kiinnostukset and appends buildKiinnostuksetKonteksti output to the prompt"
    - "A user with no interests set receives the same prompt as before (empty array → '' from buildKiinnostuksetKonteksti)"
  artifacts:
    - path: "app/components/Etusivu.tsx"
      provides: "kiinnostukset state + profiles query extension + POST body field"
      contains: "kiinnostukset"
    - path: "app/api/saasuositus/route.ts"
      provides: "kiinnostukset sanitization + prompt extension"
      contains: "buildKiinnostuksetKonteksti"
  key_links:
    - from: "app/components/Etusivu.tsx"
      to: "app/api/saasuositus/route.ts"
      via: "POST body.kiinnostukset"
      pattern: "kiinnostukset.*todoNimet"
    - from: "app/api/saasuositus/route.ts"
      to: "lib/buildKiinnostuksetKonteksti.ts"
      via: "named import"
      pattern: "buildKiinnostuksetKonteksti"
---

<objective>
Wire the kiinnostukset data flow end-to-end: Etusivu.tsx loads interests from profiles and includes them in the POST request; route.ts sanitizes the new field and appends the AI context string to the prompt via buildKiinnostuksetKonteksti.

Purpose: Delivers PROFILE-02 — the AI recommendation reflects the user's declared sport interests.
Output: Updated Etusivu.tsx (state + profiles query + POST body) and updated route.ts (sanitization + prompt).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-profiili-ai-kiinnostukset/22-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Etusivu.tsx — existing relevant sections (read before modifying) -->

State declarations (line ~172-173):
  const [kotikaupunki, setKotikaupunki] = useState&lt;string&gt;('')
  // New state to add:
  //   const [kiinnostukset, setKiinnostukset] = useState&lt;string[]&gt;([])

subscribeToAuthUser callback (lines ~348-363):
  return subscribeToAuthUser(async (user) =&gt; {
    setSupabaseUser(user)
    if (user) {
      const { data } = await supabase.from('suosikit').select('paikka_id').eq('user_id', user.id)
      if (data) setTodoIds(new Set(data.map((s: { paikka_id: number }) =&gt; s.paikka_id)))
      // Load kotikaupunki from profiles (PGRST116 = no row yet for new users, safe to ignore)
      const { data: profileData } = await supabase.from('profiles').select('kotikaupunki').eq('user_id', user.id).single()
      setKotikaupunki(profileData?.kotikaupunki ?? '')
    } else {
      setTodoIds(new Set())
      setKotikaupunki('')
    }
  })

AI fetch useEffect (lines ~384-413):
  const fetchPromise = supabaseUser !== null
    ? fetch('/api/saasuositus', { method: 'POST', headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ suosikit: todoNimet, kaupunki: weatherKaupunki, ...(kotikaupunki ? { kotikaupunki } : {}) }) })
    : fetch('/api/saasuositus?kaupunki=' + encodeURIComponent(weatherKaupunki))
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [suosikitSizeAndIds, weatherKaupunki, kotikaupunki])
  // NOTE: kiinnostukset must NOT be added to the deps array — per D-13, cache key is day-based only

<!-- route.ts — existing POST handler structure (line ~82-127) -->

Existing POST body parsing (line ~82-101):
  let suosikit: string[] = []
  let kaupunki = 'Tampere'
  let kotikaupunki: string | undefined
  // ... body.suosikit parsed with .slice(0,10).filter(string).map(replace regex)
  // ... body.kotikaupunki sanitized with replace regex + .slice(0,80).trim()

Prompt construction (line ~107-113):
  const suosikkiLista = suosikit.length ? `\nKäyttäjän suosikit: ${suosikit.join(', ')}.` : ''
  const reissuKonteksti = buildReissuKonteksti(kotikaupunki, kaupunki)
  const prompt = `...${weatherDesc}...${suosikkiLista}${reissuKonteksti}`
  // New structure (D-12): `...${suosikkiLista}${reissuKonteksti}${kiinnostuksetKonteksti}`

Existing import:
  import { buildReissuKonteksti } from '@/lib/buildReissuKonteksti'
  // New import to add:
  // import { buildKiinnostuksetKonteksti } from '@/lib/buildKiinnostuksetKonteksti'

Sport key sanitization regex (same allowlist as suosikit, per D-11):
  .slice(0, 10).filter((s: unknown): s is string =&gt; typeof s === 'string')
  .map((s: string) =&gt; s.replace(/[^\w\sÄäÖöÅå\-,.'()&amp;]/g, '').slice(0, 80))
  // For kiinnostukset: same pattern as suosikit array sanitization above
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Etusivu.tsx — kiinnostukset state, profiles query, POST body</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    - app/components/Etusivu.tsx — read entire file before editing (all state declarations, subscribeToAuthUser callback, AI fetch useEffect with deps array, existing kotikaupunki handling)
  </read_first>
  <action>
    Make three targeted changes in Etusivu.tsx (per D-10):

    1. Add kiinnostukset state: After the existing `const [kotikaupunki, setKotikaupunki] = useState&lt;string&gt;('')` line, add:
       const [kiinnostukset, setKiinnostukset] = useState&lt;string[]&gt;([])

    2. Extend subscribeToAuthUser callback: Change the profiles select from .select('kotikaupunki') to .select('kotikaupunki, kiinnostukset'). After setKotikaupunki(profileData?.kotikaupunki ?? ''), add: setKiinnostukset(profileData?.kiinnostukset ?? []). In the else branch (user logged out), add: setKiinnostukset([])

    3. Extend AI fetch POST body: In the fetchPromise ternary, the JSON.stringify object currently spreads kotikaupunki conditionally. Add kiinnostukset to the same object — unconditionally as kiinnostukset: kiinnostukset (always send the array; empty array is harmless and handled in route.ts). The full body object becomes: { suosikit: todoNimet, kaupunki: weatherKaupunki, ...(kotikaupunki ? { kotikaupunki } : {}), kiinnostukset }

    IMPORTANT: Do NOT add kiinnostukset to the useEffect dependency array — per D-13 the cache key is day-based only, not interest-based.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - useState&lt;string[]&gt;([]) for kiinnostukset declared
    - profiles query uses .select('kotikaupunki, kiinnostukset')
    - setKiinnostukset(profileData?.kiinnostukset ?? []) called after setKotikaupunki
    - setKiinnostukset([]) called in logged-out else branch
    - POST body JSON includes kiinnostukset field
    - The AI fetch useEffect deps array still reads: [suosikitSizeAndIds, weatherKaupunki, kotikaupunki] — kiinnostukset NOT added
    - npx tsc --noEmit exits with 0
  </acceptance_criteria>
  <done>Etusivu.tsx loads and sends kiinnostukset; deps array unchanged per D-13; compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend route.ts — kiinnostukset sanitization and prompt extension</name>
  <files>app/api/saasuositus/route.ts</files>
  <read_first>
    - app/api/saasuositus/route.ts — read entire file before editing (POST handler, existing suosikit + kotikaupunki sanitization patterns, prompt construction line)
    - lib/buildKiinnostuksetKonteksti.ts — confirm exported function name and signature before importing
  </read_first>
  <action>
    Make three targeted changes in route.ts (per D-11, D-12):

    1. Add import at top of file after existing buildReissuKonteksti import:
       import { buildKiinnostuksetKonteksti } from '@/lib/buildKiinnostuksetKonteksti'

    2. In the POST handler body parsing try block, after the kotikaupunki parsing block, add kiinnostukset variable initialization before the try block: let kiinnostukset: string[] = []. Inside the try block, add kiinnostukset parsing after the kotikaupunki block:
       kiinnostukset = Array.isArray(body.kiinnostukset)
         ? body.kiinnostukset
             .slice(0, 10)
             .filter((s: unknown): s is string =&gt; typeof s === 'string')
             .map((s: string) =&gt; s.replace(/[^\w\sÄäÖöÅå\-,.'()&amp;]/g, '').slice(0, 80))
         : []

    3. After the reissuKonteksti line, add:
       const kiinnostuksetKonteksti = buildKiinnostuksetKonteksti(kiinnostukset)

       Update the prompt string to append kiinnostuksetKonteksti after reissuKonteksti (per D-12):
       Change `...${suosikkiLista}${reissuKonteksti}` to `...${suosikkiLista}${reissuKonteksti}${kiinnostuksetKonteksti}`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - import { buildKiinnostuksetKonteksti } from '@/lib/buildKiinnostuksetKonteksti' present at top of file
    - let kiinnostukset: string[] = [] initialized before the try block in POST handler
    - Array.isArray(body.kiinnostukset) guard used for sanitization
    - .slice(0, 10) applied to kiinnostukset array (same limit as suosikit)
    - .replace(/[^\w\sÄäÖöÅå\-,.'()&amp;]/g, '').slice(0, 80) applied per element
    - const kiinnostuksetKonteksti = buildKiinnostuksetKonteksti(kiinnostukset) declared
    - prompt string ends with ...${reissuKonteksti}${kiinnostuksetKonteksti}
    - npx tsc --noEmit exits with 0
    - GET handler unchanged (no kiinnostukset in GET path)
  </acceptance_criteria>
  <done>Route.ts sanitizes kiinnostukset and appends context to prompt; GET path unchanged; compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST body → route.ts | Untrusted kiinnostukset array from client injected into AI prompt |
| route.ts → Anthropic API | Constructed prompt must not allow prompt injection via interest keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Tampering | POST body.kiinnostukset | mitigate | Same allowlist regex as suosikit: [^\w\sÄäÖöÅå\-,.'()&] strips special chars; .slice(0,10) limits array length; .slice(0,80) limits string length per element |
| T-22-04 | Information Disclosure | AI prompt | accept | Interests are user-provided sport keys; no PII injected into prompt |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. |
</threat_model>

<verification>
- npx tsc --noEmit passes with no errors
- curl -X POST /api/saasuositus with body {"suosikit":[],"kaupunki":"Tampere","kiinnostukset":["padel","tennis"]} returns 200 with non-empty text
- curl -X POST /api/saasuositus with body {"suosikit":[],"kaupunki":"Tampere","kiinnostukset":[]} returns 200 (same behavior as before — no regression)
- GET /api/saasuositus?kaupunki=Tampere still returns 200 (GET path unmodified)
- Etusivu.tsx deps array for AI fetch still contains only [suosikitSizeAndIds, weatherKaupunki, kotikaupunki]
</verification>

<success_criteria>
The full data flow works: logged-in user's kiinnostukset flow from profiles → Etusivu state → POST body → route.ts sanitization → buildKiinnostuksetKonteksti → AI prompt. Empty interests produce no change to prompt.
</success_criteria>

<output>
Create .planning/phases/22-profiili-ai-kiinnostukset/22-03-SUMMARY.md when done.
</output>
