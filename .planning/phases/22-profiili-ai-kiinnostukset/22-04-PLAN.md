---
phase: 22-profiili-ai-kiinnostukset
plan: 04
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-02
  - 22-03
files_modified: []
autonomous: false
requirements:
  - PROFILE-01
  - PROFILE-02

must_haves:
  truths:
    - "The kiinnostukset text[] column exists in the live Supabase profiles table"
    - "supabase db push exits 0 with no errors"
    - "A logged-in user can save interests on /profiili and they persist across browser reload"
    - "The AI widget recommendation reflects declared interests"
    - "A user with no interests sees the same recommendation quality as before"
  artifacts:
    - path: "supabase/migrations/20260531000000_profiles_add_kiinnostukset.sql"
      provides: "Applied migration — column exists in live DB"
      contains: "ADD COLUMN IF NOT EXISTS kiinnostukset"
  key_links:
    - from: "supabase/migrations/20260531000000_profiles_add_kiinnostukset.sql"
      to: "Supabase live DB profiles table"
      via: "supabase db push"
      pattern: "supabase db push"
---

<objective>
Push the Phase 22 migration to the live Supabase database (BLOCKING — phase cannot verify without the column existing), then verify end-to-end that all three phase success criteria are met.

Purpose: The kiinnostukset column must exist in the live database before any profile saves or reads can work. This plan is the deployment gate.
Output: Live database updated; end-to-end human verification completed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-profiili-ai-kiinnostukset/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push Supabase migration</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260531000000_profiles_add_kiinnostukset.sql — confirm migration file exists and contains correct DDL before pushing
  </read_first>
  <action>
    Run supabase db push to apply the kiinnostukset migration to the live database. The migration file supabase/migrations/20260531000000_profiles_add_kiinnostukset.sql must exist (created in Plan 01) before this step. Command: supabase db push. If the command requires authentication, ensure SUPABASE_ACCESS_TOKEN is set in the environment or run supabase login first.

    After the push completes, verify the column exists by running: supabase db execute --sql "SELECT column_name, data_type FROM information_schema.columns WHERE table_name='profiles' AND column_name='kiinnostukset';" and confirming at least one row is returned showing column_name=kiinnostukset and data_type=ARRAY.
  </action>
  <verify>
    <automated>supabase db push 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - supabase db push exits with code 0
    - No error output referencing the profiles table or kiinnostukset column
    - Column verification query returns a row with column_name=kiinnostukset
  </acceptance_criteria>
  <done>kiinnostukset text[] column exists in live Supabase profiles table.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification — interests persistence and AI integration</name>
  <what-built>
    Phase 22 is complete when this checkpoint passes. The full implementation includes:
    - /profiili: new "Kiinnostuksen kohteet" card with 9 sport pills, Tallenna button, inline feedback
    - Interests saved to Supabase profiles.kiinnostukset and restored on reload
    - Interests sent in POST body to /api/saasuositus and included in AI prompt
    - Empty interests → no change to AI recommendation (no regression)
  </what-built>
  <how-to-verify>
    Verify all three phase success criteria:

    **SC-1: Interests persist across sessions**
    1. Go to /profiili (logged in)
    2. Confirm "Kiinnostuksen kohteet" card appears below kotikaupunki card
    3. Select 2–3 sport pills (they should turn black/filled)
    4. Click "Tallenna" — confirm "Kiinnostukset tallennettu" text appears briefly
    5. Hard-reload the page (Ctrl+Shift+R)
    6. Confirm the same pills are still selected

    **SC-2: AI includes interests**
    7. With interests selected and saved, navigate to the homepage (/)
    8. Wait for the AI weather widget to load (the recommendation text at the top of the bottom sheet)
    9. Clear sessionStorage key starting with "saasuositus-" in DevTools (Application → Session Storage) to force a fresh AI call
    10. Reload the page — confirm the AI text loads (non-empty recommendation appears)
    11. Optional: open Network tab, filter by "saasuositus", inspect POST request body — confirm kiinnostukset array is present

    **SC-3: No regression for users without interests**
    12. Log out (or use a different account with no interests set)
    13. Navigate to / — confirm AI widget loads normally with a recommendation
    14. Confirm no JavaScript errors in console related to kiinnostukset
  </how-to-verify>
  <resume-signal>
    Type "approved" if all three success criteria pass.
    If any criterion fails, describe the issue (e.g., "pills not persisting" or "AI not loading") and the plan will be revised.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CLI → Supabase DB | supabase db push requires valid credentials; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Tampering | supabase db push | accept | IF NOT EXISTS guard prevents accidental data loss on re-run; migration is additive only |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. |
</threat_model>

<verification>
- supabase db push exits 0
- Column kiinnostukset of type ARRAY exists in profiles table
- /profiili interests card works end-to-end (SC-1)
- AI widget includes interests in recommendation (SC-2)
- No regression for users without interests (SC-3)
</verification>

<success_criteria>
All three phase success criteria verified by human tester. Phase 22 complete.
</success_criteria>

<output>
Create .planning/phases/22-profiili-ai-kiinnostukset/22-04-SUMMARY.md when done.
</output>
