---
phase: 22-profiili-ai-kiinnostukset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260531000000_profiles_add_kiinnostukset.sql
  - lib/buildKiinnostuksetKonteksti.ts
autonomous: true
requirements:
  - PROFILE-01
  - PROFILE-02

must_haves:
  truths:
    - "The profiles table accepts a kiinnostukset text[] column"
    - "buildKiinnostuksetKonteksti returns '' for empty/null input"
    - "buildKiinnostuksetKonteksti returns a Finnish sentence listing the interests when given a non-empty array"
  artifacts:
    - path: "supabase/migrations/20260531000000_profiles_add_kiinnostukset.sql"
      provides: "ALTER TABLE profiles ADD COLUMN IF NOT EXISTS kiinnostukset text[] DEFAULT '{}'"
      contains: "ADD COLUMN IF NOT EXISTS kiinnostukset"
    - path: "lib/buildKiinnostuksetKonteksti.ts"
      provides: "AI prompt context builder for sport interests"
      exports: ["buildKiinnostuksetKonteksti"]
  key_links:
    - from: "lib/buildKiinnostuksetKonteksti.ts"
      to: "app/api/saasuositus/route.ts"
      via: "named import"
      pattern: "buildKiinnostuksetKonteksti"
---

<objective>
Create the Supabase migration file that adds the kiinnostukset column to the profiles table, and create the new lib/buildKiinnostuksetKonteksti.ts helper that converts a string[] of sport keys into an AI prompt context sentence.

Purpose: These two new files are the foundation for the whole phase — the migration defines the persistence layer and the lib function is the contract that route.ts will import. Both are pure new-file additions with no existing code modified.
Output: One SQL migration file, one TypeScript lib module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-profiili-ai-kiinnostukset/22-CONTEXT.md

<interfaces>
<!-- Reference pattern — 1:1 model for buildKiinnostuksetKonteksti -->
From lib/buildReissuKonteksti.ts:
```typescript
export function buildReissuKonteksti(
  kotikaupunki: string | undefined,
  kaupunki: string
): string {
  if (!kotikaupunki || kotikaupunki.trim() === '') return ''
  if (kotikaupunki.trim().toLowerCase() === kaupunki.trim().toLowerCase()) return ''
  return ` Käyttäjä vierailee ${kaupunki}ssa — hänen kotikaupunkinsa on ${kotikaupunki}.`
}
```

From supabase/migrations/20260528083110_profiles.sql (existing schema — DO NOT modify):
```sql
CREATE TABLE IF NOT EXISTS profiles (
  user_id     uuid PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  kotikaupunki text,
  updated_at  timestamptz DEFAULT now()
);
-- RLS policies exist: SELECT, INSERT, UPDATE all using auth.uid() = user_id
-- UPDATE policy covers ALL columns — no new policies needed for kiinnostukset
```

From lib/lajit.ts (sport keys):
export const lajiKonfig: Record<string, LajiKonfig> = {
  padel, tennis, jooga, kuntosali, uinti, kiipeily, jääkiekko, liikuntahalli, liikunta
}
// Object.keys(lajiKonfig) gives all 9 sport keys
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Supabase migration — ADD COLUMN kiinnostukset</name>
  <files>supabase/migrations/20260531000000_profiles_add_kiinnostukset.sql</files>
  <read_first>
    - supabase/migrations/20260528083110_profiles.sql — understand existing table shape and naming convention
  </read_first>
  <behavior>
    - Migration is idempotent: IF NOT EXISTS guard prevents duplicate column error on re-run
    - Column type is text[] with DEFAULT '{}'
    - No new RLS policies needed — existing UPDATE policy already covers all columns
  </behavior>
  <action>
    Create a new migration file at supabase/migrations/20260531000000_profiles_add_kiinnostukset.sql containing a single DDL statement: ALTER TABLE profiles ADD COLUMN IF NOT EXISTS kiinnostukset text[] DEFAULT '{}'. Add a brief comment above it explaining the purpose (per D-07). Do not add any RLS changes.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS kiinnostukset" supabase/migrations/20260531000000_profiles_add_kiinnostukset.sql</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260531000000_profiles_add_kiinnostukset.sql exists
    - Contains exactly: ALTER TABLE profiles ADD COLUMN IF NOT EXISTS kiinnostukset text[] DEFAULT '{}'
    - Contains no CREATE POLICY statements (RLS unchanged per D-07)
    - grep count returns 1
  </acceptance_criteria>
  <done>Migration file exists with correct DDL, no RLS modifications.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create lib/buildKiinnostuksetKonteksti.ts</name>
  <files>lib/buildKiinnostuksetKonteksti.ts</files>
  <read_first>
    - lib/buildReissuKonteksti.ts — 1:1 reference pattern (structure, JSDoc style, leading-space convention)
    - lib/lajit.ts — lajiKonfig to understand the sport key/label mapping (not used inside the function itself, but informs the JSDoc)
  </read_first>
  <behavior>
    - buildKiinnostuksetKonteksti(kiinnostukset: string[] | undefined | null): string
    - Returns '' when kiinnostukset is undefined, null, or an empty array
    - Returns a Finnish sentence with a leading space: " Käyttäjä on kiinnostunut lajeista: padel, tennis." when given ['padel', 'tennis']
    - Sport keys are used as-is (no label lookup — the AI understands Finnish sport names directly)
    - The leading space matches buildReissuKonteksti convention so the caller can append directly to the prompt
  </behavior>
  <action>
    Create lib/buildKiinnostuksetKonteksti.ts. Export a single named function buildKiinnostuksetKonteksti(kiinnostukset: string[] | undefined | null): string. Add JSDoc matching the style of buildReissuKonteksti.ts: describe the leading-space convention and the empty-array return. The function body: if array is falsy or length is 0 return ''; else return ` Käyttäjä on kiinnostunut lajeista: ${kiinnostukset.join(', ')}.` (leading space, comma-separated keys, period at end). Per D-09.
  </action>
  <verify>
    <automated>npx tsc --noEmit --strict lib/buildKiinnostuksetKonteksti.ts 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File lib/buildKiinnostuksetKonteksti.ts exists
    - Exports function named buildKiinnostuksetKonteksti with signature (kiinnostukset: string[] | undefined | null): string
    - Returns '' for undefined, null, and []
    - Returns " Käyttäjä on kiinnostunut lajeista: padel, tennis." for ['padel', 'tennis'] (leading space, period)
    - No TypeScript compilation errors (tsc --noEmit returns exit 0)
  </acceptance_criteria>
  <done>buildKiinnostuksetKonteksti exported and type-checks cleanly; behavior matches buildReissuKonteksti pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL migration → Supabase | DDL runs against live DB; idempotent guard prevents data loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | kiinnostukset column | accept | text[] column; values sanitized at route.ts ingress (Plan 03). No user-controlled SQL path. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. |
</threat_model>

<verification>
- supabase/migrations/20260531000000_profiles_add_kiinnostukset.sql exists and contains ADD COLUMN IF NOT EXISTS kiinnostukset text[] DEFAULT '{}'
- lib/buildKiinnostuksetKonteksti.ts exports buildKiinnostuksetKonteksti
- npx tsc --noEmit on the new lib file passes
- Neither file modifies any existing file
</verification>

<success_criteria>
Migration file and lib function exist; both type-check; the lib returns '' for empty input and a leading-space Finnish sentence for a populated array.
</success_criteria>

<output>
Create .planning/phases/22-profiili-ai-kiinnostukset/22-01-SUMMARY.md when done.
</output>
