---
phase: 22-profiili-ai-kiinnostukset
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - app/profiili/ProfiiliClient.tsx
autonomous: true
requirements:
  - PROFILE-01

must_haves:
  truths:
    - "A logged-in user sees a 'Kiinnostuksen kohteet' card on /profiili below the kotikaupunki card"
    - "All 9 sport pills from lib/lajit.ts are shown in flex-wrap layout"
    - "Tapping a pill toggles its selected state (black filled vs. outlined)"
    - "Tapping 'Tallenna' saves the current selection to Supabase profiles and shows inline feedback"
    - "On next load, the previously saved interests are pre-selected"
  artifacts:
    - path: "app/profiili/ProfiiliClient.tsx"
      provides: "Kiinnostukset card UI, handleSaveKiinnostukset, loadProfile extension"
      contains: "handleSaveKiinnostukset"
  key_links:
    - from: "app/profiili/ProfiiliClient.tsx"
      to: "Supabase profiles table"
      via: "upsert with onConflict: user_id"
      pattern: "upsert.*kiinnostukset"
---

<objective>
Add the sport interests card to ProfiiliClient.tsx — a new `.glass rounded-2xl` section below the kotikaupunki card that shows all 9 sport pills, a separate Tallenna button, and inline success feedback. Extend loadProfile to fetch kiinnostukset and prepopulate state.

Purpose: Delivers PROFILE-01 — the user-facing persistence of sport interests.
Output: Updated ProfiiliClient.tsx with kiinnostukset UI, state, and save handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-profiili-ai-kiinnostukset/22-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Current ProfiiliClient.tsx state (read before modifying) -->
From app/profiili/ProfiiliClient.tsx — existing state and patterns:

State declarations:
  const [kotikaupunki, setKotikaupunki] = useState<string>('')
  const [userId, setUserId] = useState<string | null>(null)
  const [saved, setSaved] = useState(false)
  // New state to add:
  //   const [kiinnostukset, setKiinnostukset] = useState<string[]>([])
  //   const [savedKiinnostukset, setSavedKiinnostukset] = useState(false)

loadProfile (existing):
  const { data } = await supabase.from('profiles').select('kotikaupunki').eq('user_id', uid).single()
  setKotikaupunki(data?.kotikaupunki ?? '')
  // Extend to: .select('kotikaupunki, kiinnostukset')
  // Add: setKiinnostukset(data?.kiinnostukset ?? [])

handleSave (existing kotikaupunki save — copy pattern):
  await supabase.from('profiles').upsert(
    { user_id: userId, kotikaupunki: trimmed, updated_at: new Date().toISOString() },
    { onConflict: 'user_id' }
  )
  if (!error) { setSaved(true); setTimeout(() => setSaved(false), 2500) }

From lib/lajit.ts — sport keys and labels:
  lajiKonfig keys: padel, tennis, jooga, kuntosali, uinti, kiipeily, jääkiekko, liikuntahalli, liikunta
  lajiKonfig[key].label: 'Padel', 'Tennis', 'Jooga', 'Kuntosali', 'Uinti', 'Kiipeily', 'Jääkiekko', 'Liikuntahalli', 'Liikunta'
  Access via: Object.entries(lajiKonfig)

Design system from CLAUDE.md:
  Selected pill:   bg-[#111111] text-white font-bold text-[10px] rounded-full px-3 py-1.5
  Unselected pill: border border-[rgba(0,0,0,0.12)] text-[#111111] font-bold text-[10px] rounded-full px-3 py-1.5
  Label caps:      text-[10px] font-bold text-[#111111] uppercase tracking-widest
  Tallenna button: bg-[#111111] hover:bg-[#333333] text-white font-bold text-sm px-5 py-2 rounded-full self-start
  Success text:    text-sm text-green-700
  Card:            glass rounded-2xl p-4 flex flex-col gap-3
  Pill wrapper:    flex flex-wrap gap-2
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend loadProfile and add kiinnostukset state</name>
  <files>app/profiili/ProfiiliClient.tsx</files>
  <read_first>
    - app/profiili/ProfiiliClient.tsx — read entire file before any edits (understand current state, imports, loadProfile, handleSave, JSX)
    - lib/lajit.ts — confirm import path and exported names (lajiKonfig)
  </read_first>
  <action>
    In ProfiiliClient.tsx make the following changes (per D-08, D-04, D-05):

    1. Add import: import { lajiKonfig } from '@/lib/lajit'

    2. Add two new state declarations after the existing useState declarations:
       - const [kiinnostukset, setKiinnostukset] = useState&lt;string[]&gt;([])
       - const [savedKiinnostukset, setSavedKiinnostukset] = useState(false)

    3. Extend loadProfile query: change .select('kotikaupunki') to .select('kotikaupunki, kiinnostukset'). After setKotikaupunki line, add: setKiinnostukset(data?.kiinnostukset ?? [])

    4. In the else branch of subscribeToAuthUser (user is null/logged out), add: setKiinnostukset([])

    5. Add a toggleKiinnostus helper function (after handleSave):
       function toggleKiinnostus(key: string) {
         setKiinnostukset(prev => prev.includes(key) ? prev.filter(k => k !== key) : [...prev, key])
       }

    6. Add handleSaveKiinnostukset function (after toggleKiinnostus, per D-06):
       async function handleSaveKiinnostukset() {
         if (!userId) return
         const supabase = createBrowserSupabase()
         const { error } = await supabase
           .from('profiles')
           .upsert(
             { user_id: userId, kiinnostukset, updated_at: new Date().toISOString() },
             { onConflict: 'user_id' }
           )
         if (!error) {
           setSavedKiinnostukset(true)
           setTimeout(() => setSavedKiinnostukset(false), 2500)
         }
       }
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - import { lajiKonfig } from '@/lib/lajit' present in file
    - useState&lt;string[]&gt;([]) for kiinnostukset declared
    - useState(false) for savedKiinnostukset declared
    - loadProfile query uses .select('kotikaupunki, kiinnostukset')
    - setKiinnostukset(data?.kiinnostukset ?? []) called after setKotikaupunki
    - setKiinnostukset([]) called in logged-out else branch
    - toggleKiinnostus function defined
    - handleSaveKiinnostukset function defined with upsert({ user_id: userId, kiinnostukset, ... })
    - npx tsc --noEmit exits with 0 (no type errors)
  </acceptance_criteria>
  <done>State, toggle helper, and save handler added; file compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add kiinnostukset card to authenticated JSX</name>
  <files>app/profiili/ProfiiliClient.tsx</files>
  <read_first>
    - app/profiili/ProfiiliClient.tsx — current state after Task 1 (state and handlers already added)
  </read_first>
  <action>
    In the authenticated return JSX of ProfiiliClient.tsx, insert a second card immediately after the closing tag of the kotikaupunki card (the existing .glass rounded-2xl div), and before the Takaisin link. Per D-02, D-03, D-01, D-05:

    The new card structure:
    - Outer div: className="glass rounded-2xl p-4 flex flex-col gap-3 mt-4"
    - Label: &lt;label className="text-[10px] font-bold text-[#111111] uppercase tracking-widest"&gt;Kiinnostuksen kohteet&lt;/label&gt;
    - Pill container: &lt;div className="flex flex-wrap gap-2"&gt; containing one &lt;button&gt; per laji from Object.entries(lajiKonfig)
    - Each pill button: onClick={() => toggleKiinnostus(key)}, type="button"
      - When kiinnostukset.includes(key): className="bg-[#111111] text-white font-bold text-[10px] rounded-full px-3 py-1.5 [transition:background-color_150ms_var(--ease-out)]"
      - When not included: className="border border-[rgba(0,0,0,0.12)] text-[#111111] font-bold text-[10px] rounded-full px-3 py-1.5 bg-white [transition:background-color_150ms_var(--ease-out)]"
      - Pill label text: {konfig.label}
    - Tallenna button: onClick={handleSaveKiinnostukset}, same style as existing kotikaupunki Tallenna button (bg-[#111111] hover:bg-[#333333] text-white font-bold text-sm px-5 py-2 rounded-full self-start)
    - Inline feedback: {savedKiinnostukset && &lt;p className="text-sm text-green-700"&gt;Kiinnostukset tallennettu&lt;/p&gt;}
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains text "Kiinnostuksen kohteet" as a label in the JSX
    - Object.entries(lajiKonfig) used to render pill buttons
    - toggleKiinnostus called in each pill's onClick
    - handleSaveKiinnostukset called in Tallenna button's onClick
    - "Kiinnostukset tallennettu" success text present in JSX
    - savedKiinnostukset state gates the success text
    - npx tsc --noEmit exits with 0
    - No sport colors from lajiKonfig used on pills — only bg-[#111111]/border style (D-01)
  </acceptance_criteria>
  <done>Kiinnostukset card rendered in authenticated view; 9 pills toggle correctly; save feedback works; compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProfiiliClient → Supabase profiles | Browser client upserts kiinnostukset string[] — anon key protected by RLS (user_id = auth.uid()) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02 | Tampering | profiles upsert | accept | RLS UPDATE policy `auth.uid() = user_id` prevents cross-user writes. Array values are sport keys from a closed UI list — no freeform input. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. |
</threat_model>

<verification>
- /profiili page shows kiinnostukset card below kotikaupunki card for authenticated users
- All 9 sport pills render with Finnish labels from lajiKonfig
- Selecting/deselecting pills toggles visual state (black filled vs outlined)
- Tapping Tallenna calls upsert with { user_id, kiinnostukset, updated_at } (onConflict: user_id)
- Inline "Kiinnostukset tallennettu" text appears and disappears after 2.5s
- Reloading page restores previously saved selections (loadProfile fetches kiinnostukset)
- npx tsc --noEmit passes
</verification>

<success_criteria>
Authenticated user can select sport interests on /profiili, save them, and see them restored on next page load. No changes to kotikaupunki behavior.
</success_criteria>

<output>
Create .planning/phases/22-profiili-ai-kiinnostukset/22-02-SUMMARY.md when done.
</output>
