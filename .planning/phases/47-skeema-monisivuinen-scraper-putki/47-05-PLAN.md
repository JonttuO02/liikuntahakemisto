---
phase: 47-skeema-monisivuinen-scraper-putki
plan: 05
type: execute
wave: 3
depends_on: ["47-01", "47-02", "47-03", "47-04"]
files_modified:
  - app/api/business/analyze-website/route.ts
  - lib/branding/storage.ts
autonomous: false
requirements: [BRDDB-05, SCRAP-06, SCRAP-07, SCRAP-08, SCRAP-09]

must_haves:
  truths:
    - "POST /api/business/analyze-website accepts a paikka_id and verifies the caller owns that venue (approved link) before analyzing"
    - "All business_branding UPSERTs/queries — including all onConflict targets — are scoped by (business_account_id, paikka_id) once the composite UNIQUE exists (D-16)"
    - "The background pipeline crawls subpages, optionally captures a homepage screenshot, sends labeled multi-page input to Claude, and stores logo_candidates + image_urls"
    - "Analyzing venue B does not overwrite venue A's branding row for the same business account"
    - "The entry-URL SSRF check uses the shared isUrlSafe validator (no inline duplicate)"
  artifacts:
    - path: "app/api/business/analyze-website/route.ts"
      provides: "paikka_id-scoped, ownership-checked, multi-page analysis route"
      contains: "business_account_id,paikka_id"
    - path: "lib/branding/storage.ts"
      provides: "Gallery + multi-logo-candidate upload helpers"
      exports: ["uploadLogo"]
  key_links:
    - from: "app/api/business/analyze-website/route.ts"
      to: "lib/branding/ssrfGuard.ts"
      via: "import isUrlSafe"
      pattern: "import.*isUrlSafe"
    - from: "app/api/business/analyze-website/route.ts"
      to: "business_paikka_links"
      via: "ownership verification before analysis"
      pattern: "business_paikka_links"
    - from: "app/api/business/analyze-website/route.ts"
      to: "business_branding"
      via: "UPSERT onConflict (business_account_id,paikka_id)"
      pattern: "onConflict.*business_account_id,paikka_id"
---

<objective>
Wire the route handler to the new schema and pipeline: accept and ownership-verify `paikka_id` (IDOR mitigation), replace the inline SSRF block with the shared `isUrlSafe` validator, scope all three `business_branding` UPSERTs and the GET query by `(business_account_id, paikka_id)` (D-16 / BRDDB-05), and thread the new scraper/analyzer shapes through `runAnalysis` (labeled multi-page input, optional homepage screenshot, `logo_candidates` + `image_urls` storage). Add gallery/multi-logo upload helpers to `storage.ts`. This is the integration plan that makes Plans 47-01..04 deliver end-to-end behavior.

Purpose: Closes BRDDB-05 (no cross-venue overwrite) and completes SCRAP-06/07/08/09 by connecting the crawler, screenshot, analyzer, and per-venue schema into the live route.
Output: An updated route handler scoped per venue with ownership checks, plus extended storage helpers; ends with a human-verify checkpoint confirming Vercel plan status and an end-to-end smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/47-skeema-monisivuinen-scraper-putki/47-CONTEXT.md
@.planning/phases/47-skeema-monisivuinen-scraper-putki/47-PATTERNS.md
@.planning/phases/47-skeema-monisivuinen-scraper-putki/47-RESEARCH.md

@app/api/business/analyze-website/route.ts
@lib/branding/storage.ts

<interfaces>
<!-- Produced by Wave 1-2 plans. Wire against these; do not re-implement. -->
From lib/branding/ssrfGuard.ts (Plan 47-02): `isUrlSafe(url: string): boolean`
From lib/branding/scraper.ts (Plan 47-03): `scrapeWebsite(url)` now returns
  `{ logoUrls, logoBuffers, colors, htmlSnippet, labeledPages: {label,html}[], imageUrls: string[] }`
From lib/branding/screenshot.ts (Plan 47-04): `captureHomepageScreenshot(url): Promise<Buffer|null>`
From lib/branding/analyzer.ts (Plan 47-04): `analyzeWithClaude(logoBuffers, labeledPages, screenshot?)` returns
  array-based `{ logos:{index,type}[], colors:{hex,role}[], prices:{...,source_page}[], opening_hours:{...,source_page}[], website_url, logo_index, raw_analysis }`

<!-- Schema (Plan 47-01, live after db push): business_branding has paikka_id (NOT NULL FK),
     logo_candidates JSONB, image_urls JSONB, selected_background_color/accent_color TEXT,
     and UNIQUE(business_account_id, paikka_id). The 3 UPSERT onConflict targets must become
     'business_account_id,paikka_id' (RESEARCH.md Pitfall 4 — there are exactly 3 sites). -->

<!-- Ownership / param conventions (PATTERNS.md route.ts section): -->
- paikka_id read from the POST JSON body (`body?.paikka_id`) — lowest-friction match since `url` is already read there (Claude's Discretion confirmed body is acceptable). GET reads it from `new URL(request.url).searchParams.get('paikka_id')`.
- Ownership check (model on the CR-04 business-account check, lines 136-143): query `business_paikka_links` for `business_account_id = user.id AND paikka_id = <paikkaId> AND claim_status = 'approved'` via `.select(...).eq(...).eq(...).eq(...).maybeSingle()`; 403 if null.
- runAnalysis signature becomes `runAnalysis(url, businessAccountId, paikkaId)`; thread paikkaId into all 3 UPSERT payloads (`paikka_id: paikkaId`) and onConflict targets.

From lib/branding/storage.ts (Plan 47-04 untouched; this plan extends): existing `uploadLogo(businessAccountId, pngBuffer)` uploads to `branding/{id}/logo.png`. New helpers must follow the same upload+getPublicUrl+`?t=Date.now()` shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gallery + multi-logo-candidate upload helpers to storage.ts</name>
  <read_first>
    - lib/branding/storage.ts (full file: uploadLogo pattern to mirror)
    - .planning/phases/47-skeema-monisivuinen-scraper-putki/47-PATTERNS.md (storage.ts section: path-pattern guidance)
  </read_first>
  <files>lib/branding/storage.ts</files>
  <action>
    Add two exported helpers mirroring `uploadLogo`'s exact shape (upload with `upsert: true`, `console.error('[branding/storage] ...')` on error then throw, `getPublicUrl`, return `publicUrl + '?t=' + Date.now()`):

    1. `uploadLogoCandidate(businessAccountId: string, pngBuffer: Buffer, index: number): Promise<string>` — path `branding/{businessAccountId}/logo-{index}.png`. Used to persist each distinct logo candidate (D-12 `logo_candidates`).
    2. `uploadGalleryImage(businessAccountId: string, pngBuffer: Buffer, index: number): Promise<string>` — path `branding/{businessAccountId}/gallery/{index}.png`. Used to persist scraped gallery images (D-11 `image_urls`).

    Keep the existing `uploadLogo` unchanged (the primary `logo_url` still uses it for backward compat). Each new helper is parameterized only by businessAccountId + index (paikka scoping is handled by the DB row, not the storage path, to keep the existing bucket layout stable).
  </action>
  <verify>
    <automated>grep -q "uploadLogoCandidate" lib/branding/storage.ts && grep -q "uploadGalleryImage" lib/branding/storage.ts && npx tsc --noEmit 2>&1 | grep -i "storage.ts" || echo STORAGE_TS_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `storage.ts` exports `uploadLogoCandidate` and `uploadGalleryImage`, both following the upload→getPublicUrl→`?t=` pattern with the `[branding/storage]` error log.
    - `uploadLogo` is unchanged.
    - `npx tsc --noEmit` reports no error in `storage.ts`.
  </acceptance_criteria>
  <done>Gallery and per-candidate logo upload helpers added.</done>
</task>

<task type="auto">
  <name>Task 2: Scope the route by (business_account_id, paikka_id), add ownership check, use shared SSRF validator, thread the new pipeline</name>
  <read_first>
    - app/api/business/analyze-website/route.ts (full file: inline SSRF block 106-133, CR-04 check 136-143, 3 UPSERT sites with onConflict, GET .eq filter line 180, runAnalysis signature line 20)
    - lib/branding/ssrfGuard.ts (isUrlSafe — Plan 47-02)
    - lib/branding/scraper.ts (new ScrapeResult shape — Plan 47-03 SUMMARY)
    - lib/branding/analyzer.ts (new analyzeWithClaude signature + result shape — Plan 47-04 SUMMARY)
    - lib/branding/screenshot.ts (captureHomepageScreenshot — Plan 47-04)
    - lib/branding/storage.ts (uploadLogo + new helpers from Task 1)
  </read_first>
  <files>app/api/business/analyze-website/route.ts</files>
  <action>
    POST handler:
    1. Replace the inline SSRF try/catch block (lines 106-133) with `import { isUrlSafe } from '@/lib/branding/ssrfGuard'` at the top and a single guard: `if (!isUrlSafe(url)) return NextResponse.json({ error: 'Invalid or private URL' }, { status: 400 })`.
    2. Read `paikka_id` from the POST body alongside `url` (`const paikkaId = body?.paikka_id`). Validate it is a positive integer; 400 if missing/invalid.
    3. After the existing CR-04 business-account check, add an ownership check (IDOR mitigation, ASVS V5/V4): query `business_paikka_links` for `.eq('business_account_id', user.id).eq('paikka_id', paikkaId).eq('claim_status', 'approved').maybeSingle()`; if null, return 403 `{ error: 'You do not own this venue' }`. This prevents a business account from triggering analysis (and a write) against a venue it does not own.
    4. Change the status='analyzing' UPSERT to include `paikka_id: paikkaId` in the payload and `{ onConflict: 'business_account_id,paikka_id' }`.
    5. Pass `paikkaId` to `waitUntil(runAnalysis(url, user.id, paikkaId))`.

    runAnalysis (signature → `runAnalysis(url, businessAccountId, paikkaId: number)`):
    6. Call `scrapeWebsite(url)` and destructure `{ logoBuffers, labeledPages, imageUrls }` (plus colors as needed).
    7. Capture screenshot: `const screenshot = await captureHomepageScreenshot(url)` (null on failure — non-fatal). Wrap so a screenshot failure never aborts the pipeline.
    8. Call `analyzeWithClaude(logoBuffers, labeledPages, screenshot)`.
    9. Upload logos: keep the primary `logo_url` via `uploadLogo` for the chosen `result.logo_index` (backward compat). Additionally upload each distinct logo candidate via `uploadLogoCandidate` and build a `logo_candidates` array (JSON of `{url, type}` per logo). Upload gallery images: fetch+convert each `imageUrls` entry through the existing image pipeline (or store the raw scraped URLs if conversion is out of budget — prefer storing converted Supabase Storage URLs via `uploadGalleryImage` to satisfy SEC-46-02's same-origin storage rule; if fetching all is too costly, cap and document). Build the `image_urls` array.
    10. Final UPSERT: add `paikka_id: paikkaId`, `logo_candidates`, `image_urls` to the payload (keep existing `colors` populated from the flat hex projection for backward compat), and change onConflict to `'business_account_id,paikka_id'`.
    11. Failure-path UPSERT (catch block): add `paikka_id: paikkaId` and `{ onConflict: 'business_account_id,paikka_id' }`.

    GET handler:
    12. Read `paikka_id` from `new URL(request.url).searchParams.get('paikka_id')`; 400 if missing/invalid.
    13. Add `.eq('paikka_id', paikkaId)` to the existing `.eq('business_account_id', user.id)` query, and add `logo_candidates, image_urls, selected_background_color, selected_accent_color` to the `.select(...)` list (additive — keep `logo_url, colors, logo_type, raw_analysis` so the Phase-46 consumer keeps working, RESEARCH.md Pitfall 5).

    Grep for EVERY `onConflict:` and `.eq('business_account_id'` occurrence in this file before considering complete — there are exactly 3 onConflict sites and 1 GET .eq filter (RESEARCH.md Pitfall 4). The status='analyzing' POST UPSERT is the 3rd onConflict site.
  </action>
  <verify>
    <automated>grep -c "onConflict: 'business_account_id,paikka_id'" app/api/business/analyze-website/route.ts | grep -q "3" && grep -q "business_paikka_links" app/api/business/analyze-website/route.ts && grep -q "isUrlSafe" app/api/business/analyze-website/route.ts && npx tsc --noEmit 2>&1 | grep -i "route.ts" || echo ROUTE_TS_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - The inline SSRF block is gone; `isUrlSafe` is imported and used as the entry-URL guard.
    - `paikka_id` is read in both POST (body) and GET (query param) and validated as a positive integer.
    - An ownership check against `business_paikka_links` (approved claim) returns 403 when the caller does not own the venue.
    - All 3 `onConflict` targets are `'business_account_id,paikka_id'` (grep count == 3) and the GET query filters by `paikka_id`.
    - `runAnalysis` is `(url, businessAccountId, paikkaId)` and passes `labeledPages` + screenshot to `analyzeWithClaude`.
    - The final UPSERT writes `logo_candidates` and `image_urls`; `colors`/`logo_url` remain populated additively.
    - Screenshot capture failure does not abort the pipeline.
    - `npx tsc --noEmit` reports no error in `route.ts`.
  </acceptance_criteria>
  <done>Route fully scoped per venue with ownership check, shared SSRF validator, and the new multi-page pipeline wired.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Vercel plan status + end-to-end multi-page pipeline smoke test</name>
  <action>Pause for human verification. Confirm Vercel plan status, run the full Vitest suite and build, and perform the end-to-end / multi-venue / redirect smoke checks described in <how-to-verify>. Do not auto-advance — this checkpoint also gates the D-04 Vercel Pro deployment prerequisite.</action>
  <what-built>
    The full multi-page analysis pipeline is wired end-to-end: per-venue scoping (BRDDB-05), ownership-checked `paikka_id`, shared SSRF validator with redirect re-validation (SCRAP-07), keyword subpage crawl (SCRAP-06), labeled multi-page Claude prompt with optional homepage screenshot (SCRAP-08), and gallery image extraction/storage (SCRAP-09). Build + full Vitest suite pass.
  </what-built>
  <how-to-verify>
    1. Confirm Vercel plan status (RESEARCH.md Open Question 3 / D-04): the homepage screenshot path requires Vercel Pro. State whether the project is already on Pro. If still on Hobby, the crawl/SSRF/schema work still functions, but screenshot capture will return null (degraded, non-fatal) — that is acceptable to ship, but note it.
    2. Run `npx vitest run` — full suite must be green (scraper, analyzer, ssrfGuard tests included).
    3. Run `npm run build` — must succeed (validates the Next 14.2 externalization config compiles).
    4. End-to-end smoke (against a deployed preview if Pro is active, otherwise local): POST `/api/business/analyze-website` with `{ url: '<a real business site with a /hinnasto subpage>', paikka_id: <a venue you own with an approved link> }`. Then GET `/api/business/analyze-website?paikka_id=<same>` and confirm the row has `status: 'analyzed'`, a non-empty `logo_candidates`, an `image_urls` array, and that `prices`/`opening_hours` in `raw_analysis` carry `source_page` labels reflecting subpage data.
    5. Multi-venue check (BRDDB-05): analyze venue A, then analyze venue B under the same business account, then GET venue A again — confirm A's branding row is intact (not overwritten by B).
    6. Smoke-test the Node/undici manual-redirect assumption (Assumption A1, carried from Plan 47-02): point the analyzer at a URL that 301-redirects to a same-origin page and confirm the redirect was followed (not treated as a failed fetch). If it silently fails, the `redirect: 'manual'` Location-readability assumption is wrong on Vercel's runtime and needs a follow-up.
  </how-to-verify>
  <resume-signal>Type "approved" (note Vercel plan status + whether screenshot capture was exercisable), or describe issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST route | `url` and `paikka_id` are attacker-controllable; a business account could submit a paikka_id it doesn't own |
| route → business_branding write | a write scoped to the wrong (account, paikka) pair would overwrite another venue's branding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-11 | Elevation of Privilege / Tampering | IDOR via unowned paikka_id in request body | mitigate | Ownership check: `business_paikka_links` must contain `(user.id, paikka_id, claim_status='approved')` before any analysis/write; 403 otherwise |
| T-47-12 | Tampering | cross-venue branding overwrite | mitigate | All UPSERTs scoped by composite `(business_account_id, paikka_id)` onConflict; verified by the multi-venue smoke check |
| T-47-13 | Information Disclosure | SSRF on entry URL | mitigate | Shared `isUrlSafe` guard at POST entry (replaces inline duplicate); subpage/redirect hops guarded by Plan 47-02/03 |
| T-47-14 | Information Disclosure | GET surfacing a non-Storage logo_url | mitigate | Existing SEC-46-02 same-origin storage check retained; gallery/logo-candidate URLs prefer Supabase Storage URLs |
| T-47-SC | Tampering | npm installs | mitigate | No installs in this plan (handled + audited in Plan 47-04) |
</threat_model>

<verification>
- `npx vitest run` full suite green.
- `npm run build` succeeds.
- `grep -c "onConflict: 'business_account_id,paikka_id'"` == 3 in route.ts.
- Ownership check present (`business_paikka_links` + `claim_status='approved'`).
- End-to-end + multi-venue + redirect smoke checks pass at the checkpoint.
</verification>

<success_criteria>
- BRDDB-05: analyzing venue B never overwrites venue A's branding (composite-scoped writes, verified).
- SCRAP-06/07/08/09 are observable end-to-end: subpages crawled and SSRF-re-validated, labeled multi-page prompt (+ optional screenshot) sent to Claude, source_page-attributed prices/hours and logo_candidates/image_urls stored.
- The entry-URL SSRF check uses the shared validator; the inline duplicate is removed.
- Vercel plan status confirmed; screenshot path degrades gracefully on Hobby.
</success_criteria>

<output>
Create `.planning/phases/47-skeema-monisivuinen-scraper-putki/47-05-SUMMARY.md` when done. Record: Vercel plan status at verification time, whether screenshot capture was exercisable, the Assumption A1 redirect smoke-test result, and the confirmed GET response shape (so Phase 48 consumes it correctly).
</output>
