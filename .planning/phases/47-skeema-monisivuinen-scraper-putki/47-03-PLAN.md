---
phase: 47-skeema-monisivuinen-scraper-putki
plan: 03
type: execute
wave: 2
depends_on: ["47-02"]
files_modified:
  - lib/branding/scraper.ts
  - lib/branding/scraper.test.ts
autonomous: true
requirements: [SCRAP-06, SCRAP-09]

must_haves:
  truths:
    - "scrapeWebsite follows same-origin links from the homepage to pricing/hours/contact subpages, capped at 3-5 total pages"
    - "Subpages are selected via Finnish+English keyword matching, falling back to first-N same-origin links if no keyword matches found (D-06)"
    - "Extracted prices/hours can reflect data found only on a subpage, not just the homepage"
    - "scrapeWebsite returns general gallery image URLs distinct from logo candidates, noise-filtered and capped at a reasonable gallery size (D-11)"
    - "Every outbound fetch in scraper.ts — homepage, subpages, CSS files, and logo-candidate images — goes through fetchWithSsrfGuard (re-validated, redirect:'manual' per D-10), not a bare fetch"
  artifacts:
    - path: "lib/branding/scraper.ts"
      provides: "Multi-page crawl, keyword subpage discovery, gallery image extraction, labeled-section output, all fetches SSRF-guarded"
      contains: "fetchWithSsrfGuard"
  key_links:
    - from: "lib/branding/scraper.ts"
      to: "lib/branding/fetchSafe.ts"
      via: "import fetchWithSsrfGuard"
      pattern: "import.*fetchWithSsrfGuard"
    - from: "lib/branding/scraper.ts"
      to: "lib/branding/ssrfGuard.ts"
      via: "import isUrlSafe for per-link pre-validation"
      pattern: "import.*isUrlSafe"
---

<objective>
Extend `scraper.ts` from a single-page scrape into a multi-page crawler (SCRAP-06): discover same-origin pricing/hours/contact subpages via keyword matching (D-06), fetch up to 4 of them (3-5 total pages, D-07) through the re-validated `fetchWithSsrfGuard` wrapper, and return labeled per-page HTML sections instead of one flat 8000-char homepage slice. Also extract general gallery image URLs (SCRAP-09, D-11) distinct from logo candidates. Route ALL outbound fetches in this file — homepage, subpages, CSS files, and logo-candidate images — through the SSRF-guarded wrapper from Plan 47-02, satisfying D-10's "logo/page/CSS fetches" redirect:'manual' re-validation requirement.

Purpose: SCRAP-06/SCRAP-09 — pricing and opening hours commonly live on dedicated subpages, not the homepage; and the Mediat step (Phase 48) needs a pool of real page images to prefill from.
Output: An extended `scraper.ts` producing a labeled multi-page `ScrapeResult` plus gallery image URLs, with unit coverage, and with every outbound fetch routed through the redirect-revalidating wrapper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/47-skeema-monisivuinen-scraper-putki/47-CONTEXT.md
@.planning/phases/47-skeema-monisivuinen-scraper-putki/47-PATTERNS.md
@.planning/phases/47-skeema-monisivuinen-scraper-putki/47-RESEARCH.md

@lib/branding/scraper.ts
@lib/branding/scraper.test.ts

<interfaces>
<!-- Plan 47-02 produced these (Wave 1). Import them; do not re-implement. -->
From lib/branding/ssrfGuard.ts: `export function isUrlSafe(url: string): boolean`
From lib/branding/fetchSafe.ts: `export async function fetchWithSsrfGuard(url: string, init?: RequestInit): Promise<Response>`

<!-- Current ScrapeResult (lib/branding/scraper.ts) — the shape that must change to labeled multi-page. -->
Current: `{ logoUrls: string[]; logoBuffers: Buffer[]; colors: string[]; htmlSnippet: string }`
`htmlSnippet` is `strippedHtml.slice(0, 8000)` of the homepage only.

<!-- New ScrapeResult shape this plan must produce (consumed by analyzer in Plan 47-04 and route in 47-05): -->
New (additive — keep logoUrls/logoBuffers/colors):
- `labeledPages: Array<{ label: string; html: string }>` — e.g. `[{label:'homepage',html},{label:'pricing',html},...]`, each stripped (comments/script/style) and per-page truncated.
- `imageUrls: string[]` — gallery candidates (SCRAP-09), deduped against logo candidate URLs, capped at 15.
- Keep `htmlSnippet` populated from the homepage labeled page for backward compatibility (so the GET response and any Phase-46 consumer do not break before Phase 48 — RESEARCH.md Pitfall 5: prefer additive over breaking).

<!-- Existing bare-fetch call sites in scraper.ts that D-10 requires routing through the wrapper: -->
- CSS fetch: line 80, `const r = await fetch(cssUrl, { signal: AbortSignal.timeout(5000) })` inside the parallel CSS-fetch `Promise.all` map (lines 77-86).
- Logo-candidate image fetch: line 178, `const imgRes = await fetch(candidateUrl, { signal: AbortSignal.timeout(5000) })` inside the logo loop (lines 176-187).

<!-- Existing idioms to copy (PATTERNS.md): -->
- Logo `<img>` detection loop (scraper.ts lines 139-167) — template for both subpage-link discovery and gallery extraction.
- CR-05: re-create the `/g` regex INSIDE each loop (shared /g regex retains lastIndex across reuse).
- WR-01 size guard: `if (html.length > 5 * 1024 * 1024) throw` per fetched page.
- CR-03 strip: `.replace(/<!--[\s\S]*?-->/g,'').replace(/<script...>/gi,'').replace(/<style...>/gi,'')` per page before labeling.
- Mock-fetch test convention from scraper.test.ts: `makeHtmlResponse(html)`, `mockFetch.mockResolvedValueOnce(...)`, `mockFetch.mockReset()` in beforeEach.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add same-origin subpage discovery + gallery image extraction helpers</name>
  <read_first>
    - lib/branding/scraper.ts (existing regex idioms: img loop lines 139-167, CR-05 comment lines 89-90, WR-01 line 52, CR-03 strip lines 200-203)
    - lib/branding/fetchSafe.ts (fetchWithSsrfGuard signature from Plan 47-02)
    - lib/branding/ssrfGuard.ts (isUrlSafe signature)
    - .planning/phases/47-skeema-monisivuinen-scraper-putki/47-RESEARCH.md (Pattern 3 discoverSubpages + gallery noise-filter code examples)
    - lib/branding/scraper.test.ts (mock-fetch helpers and convention)
  </read_first>
  <files>lib/branding/scraper.ts, lib/branding/scraper.test.ts</files>
  <behavior>
    - discoverSubpages: given homepage HTML with `<a href="/hinnasto">Hinnasto</a>`, `<a href="/yhteystiedot">Yhteystiedot</a>`, and an external `<a href="https://facebook.com/x">`, returns `{ pricing: '<abs>/hinnasto', contact: '<abs>/yhteystiedot' }` and excludes the external link (same-origin only).
    - discoverSubpages keyword categories: pricing matches hinnasto|hinnat|pricing|prices; hours matches aukioloajat|aukiolo|hours|opening; contact matches yhteystiedot|yhteys|contact. Match against href + link text.
    - discoverSubpages fallback (D-06): when ZERO keyword matches found, returns the first N same-origin links (N = remaining page budget) so the crawl degrades to "homepage + first few links" rather than failing.
    - extractGalleryImages: skips `data:` URIs; skips images with width/height attr below 100px when present; skips src/alt/class matching /icon|sprite|pixel|spinner|loader|favicon|tracking|1x1/i; excludes URLs already in the logo-candidate set; dedupes; caps at 15.
  </behavior>
  <action>
    In `scraper.ts`, add two non-exported helper functions, both following the existing regex idioms and CR-05 discipline (fresh `/g` regex literal inside each loop):

    1. `discoverSubpages(html, baseUrl): Record<string,string>` — copy RESEARCH.md Pattern 3 almost verbatim. Use `const anchorRegex = /<a\s+[^>]*href=["']([^"']+)["'][^>]*>([\s\S]*?)<\/a>/gi`. Resolve each href to absolute via `new URL(href, baseUrl)`, skip if hostname !== baseHost. Score `href + ' ' + linkText` against a `KEYWORDS` map (pricing/hours/contact regexes above), recording the first same-origin match per category. After the loop, if the result object is empty, apply the fallback: collect the first `budget` same-origin absolute URLs (budget passed as a param, default 4) and return them keyed `link0`, `link1`, ... (these become labeled `[PAGE: link0]` sections — acceptable per D-06's "homepage-only is a valid fallback").

    2. `extractGalleryImages(html, baseUrl, excludeUrls: Set<string>): string[]` — copy RESEARCH.md's gallery noise-filter example almost verbatim. `const imgTagRegex = /<img[^>]+>/gi`; per tag read src/width/height/alt/class with fresh per-tag regexes; apply the skip rules from the behavior block; resolve to absolute; skip if in `excludeUrls`; dedupe; `return found.slice(0, 15)` (D-11 cap of 15 — Claude's Discretion, 15 chosen as a reasonable gallery size; MIN_DIMENSION=100px).

    Add tests in `scraper.test.ts` under `describe('discoverSubpages / SCRAP-06', ...)` and `describe('extractGalleryImages / SCRAP-09', ...)` covering every behavior-block case, including the same-origin exclusion and the no-keyword fallback. Export the two helpers (or test them via the public scrapeWebsite in Task 2 — prefer exporting them for direct unit testing, marked with a comment that they are exported for testing).
  </action>
  <verify>
    <automated>npx vitest run lib/branding/scraper.test.ts -t "SCRAP-06" && npx vitest run lib/branding/scraper.test.ts -t "SCRAP-09"</automated>
  </verify>
  <acceptance_criteria>
    - `discoverSubpages` returns only same-origin URLs; external-host links are excluded.
    - Finnish AND English keywords both match (hinnasto and pricing both map to `pricing`).
    - With zero keyword matches, the fallback returns the first N same-origin links (non-empty when same-origin links exist).
    - `extractGalleryImages` excludes data URIs, sub-100px-dimensioned images, noise-pattern images, and logo-candidate URLs; result length ≤ 15.
    - Each helper re-creates its `/g` regex inside the loop (CR-05 discipline preserved).
    - Tests pass for both `-t "SCRAP-06"` and `-t "SCRAP-09"`.
  </acceptance_criteria>
  <done>Subpage discovery and gallery extraction helpers implemented and unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire scrapeWebsite to crawl subpages and route every outbound fetch (page, CSS, logo) through the SSRF-guarded wrapper</name>
  <read_first>
    - lib/branding/scraper.ts (full current scrapeWebsite, especially the ScrapeResult interface and the homepage fetch/strip/return; ALSO the two existing bare fetches that D-10 requires routing through the wrapper: the CSS fetch at line 80 `await fetch(cssUrl, { signal: AbortSignal.timeout(5000) })` inside the parallel CSS-fetch map (lines 77-86), and the logo-candidate image fetch at line 178 `await fetch(candidateUrl, { signal: AbortSignal.timeout(5000) })` inside the logo loop (lines 176-187))
    - lib/branding/fetchSafe.ts (fetchWithSsrfGuard)
    - lib/branding/scraper.test.ts (existing SCRAP-01 test that asserts the four-field ScrapeResult — must stay green or be updated additively)
    - .planning/phases/47-skeema-monisivuinen-scraper-putki/47-RESEARCH.md (System Architecture Diagram steps 1-3; per-page truncation budget guidance for SCRAP-08)
  </read_first>
  <files>lib/branding/scraper.ts, lib/branding/scraper.test.ts</files>
  <behavior>
    - scrapeWebsite fetches the homepage via fetchWithSsrfGuard (not bare fetch), then discovers subpages, then fetches up to 4 subpages in parallel via fetchWithSsrfGuard, skipping any that time out / 404 / are non-HTML (existing fail-soft idiom).
    - The CSS-file fetches (currently bare at line 80) and the logo-candidate image fetches (currently bare at line 178) ALSO go through fetchWithSsrfGuard — every outbound fetch in scraper.ts is wrapped (D-10: "logo/page/CSS fetches").
    - Total labeled pages returned is between 1 (homepage only, all subpages failed) and 5 (homepage + 4).
    - Each labeledPages entry is stripped (comments/script/style) and per-page truncated to a budget (e.g. homepage 6000 chars, each subpage 4000 chars — total stays within Claude's input budget).
    - imageUrls aggregates gallery images discovered across the homepage and subpages, deduped, excluding logo candidates, capped at 15.
    - htmlSnippet remains populated (from the homepage labeled section) so existing consumers do not break.
    - A subpage link, CSS URL, or logo-candidate URL that resolves to a private IP is never fetched (fetchWithSsrfGuard throws and the fetch is treated as a soft failure — empty CSS text / skipped logo candidate / skipped subpage).
  </behavior>
  <action>
    Update the `ScrapeResult` interface to add `labeledPages: Array<{ label: string; html: string }>` and `imageUrls: string[]` (keep `logoUrls`, `logoBuffers`, `colors`, `htmlSnippet`).

    Rewrite `scrapeWebsite(url)`:
    1. Fetch the homepage via `fetchWithSsrfGuard(url)` (replace the bare `fetch` at lines 44-47). Keep the WR-01 5MB guard. Keep theme-color + CSS :root color extraction and logo-candidate detection as-is (those still run on the homepage HTML).
    2. Call `discoverSubpages(homepageHtml, url, 4)` to get the subpage URL map.
    3. For each discovered subpage URL: pre-validate with `isUrlSafe` (cheap reject before fetch), then fetch via `fetchWithSsrfGuard` inside a `try/catch` that returns null on any failure (timeout/404/non-HTML/SSRF-throw) — reuse the existing `Promise.all` + fail-soft idiom (scraper.ts lines 77-86). Apply the WR-01 size guard per page.
    4. Build `labeledPages`: start with `{ label: 'homepage', html: strip(homepageHtml).slice(0, 6000) }`, then push `{ label: <category>, html: strip(subHtml).slice(0, 4000) }` for each successfully-fetched subpage, using the discovery map's category as the label. Apply the CR-03 strip per page.
    5. Build `imageUrls` by running `extractGalleryImages` over the homepage AND each fetched subpage HTML, passing a `Set` of the logo-candidate absolute URLs as `excludeUrls`, then dedupe and cap at 15.
    6. Return the extended ScrapeResult; set `htmlSnippet` to the homepage labeled page's html (backward compat).

    Then, per D-10 ("logo/page/CSS fetches"), route the two remaining bare fetches through the same wrapper — this is closing the literal D-10 requirement, not new scope:
    7. CSS fetch (line 80): replace `await fetch(cssUrl, { signal: AbortSignal.timeout(5000) })` with `await fetchWithSsrfGuard(cssUrl, { signal: AbortSignal.timeout(5000) })`. Keep the surrounding `try/catch` that returns `''` on failure — when the wrapper throws (SSRF reject, redirect-hop-cap exceeded, or network error) the CSS text degrades to empty, which the existing color-extraction already tolerates.
    8. Logo-candidate image fetch (line 178): replace `await fetch(candidateUrl, { signal: AbortSignal.timeout(5000) })` with `await fetchWithSsrfGuard(candidateUrl, { signal: AbortSignal.timeout(5000) })`. Keep the surrounding `try/catch`/`continue` — when the wrapper throws, the candidate is skipped (same as the existing `!imgRes.ok` continue path). Do NOT pre-call `isUrlSafe` here separately; the wrapper performs the validation and redirect re-checks internally.

    Update `scraper.test.ts`: keep/adjust the SCRAP-01 test additively (it should still find the four original fields plus the two new ones). Add a `describe('scrapeWebsite multi-page / SCRAP-06', ...)` test that mocks a homepage with a `/hinnasto` link and a pricing subpage, asserts `labeledPages` contains both a `homepage` and a `pricing` entry, and asserts a subpage link to `http://127.0.0.1/x` is never fetched (inspect `mockFetch.mock.calls`). Add a `describe('scrapeWebsite SSRF coverage / D-10', ...)` assertion that no bare `fetch(` is invoked for CSS or logo-candidate loads — assert (via mock-call inspection or a source-level grep gate in CI) that the CSS URL and a logo-candidate URL are fetched through the wrapper, and that a CSS URL or logo URL pointing at `http://127.0.0.1/x` is rejected (wrapper throws → empty CSS text / skipped candidate). Mock `fetchWithSsrfGuard` indirectly by mocking global fetch (the wrapper uses `redirect: 'manual'`; for non-redirect 200 responses include a `headers.get` stub returning null for 'location' so the wrapper returns the response).
  </action>
  <verify>
    <automated>npx vitest run lib/branding/scraper.test.ts && grep -vn '^[[:space:]]*//' lib/branding/scraper.ts | grep -n 'await fetch(' | grep -v 'fetchWithSsrfGuard' | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - `ScrapeResult` includes `labeledPages` and `imageUrls`; original four fields retained.
    - Every outbound fetch in scrapeWebsite is routed through `fetchWithSsrfGuard`: the homepage fetch, every subpage fetch, the CSS-file fetch (was line 80), and the logo-candidate image fetch (was line 178). No bare `await fetch(` remains anywhere in scraper.ts (grep gate in `<verify>` confirms zero non-commented `await fetch(` calls that are not `fetchWithSsrfGuard`).
    - `labeledPages` length is between 1 and 5.
    - A subpage link, CSS URL, or logo-candidate URL resolving to a private IP is never fetched (the wrapper throws and the load soft-fails: empty CSS text / skipped logo candidate / skipped subpage). Asserted via mock call inspection.
    - `imageUrls` excludes logo-candidate URLs and is capped at 15.
    - `htmlSnippet` is still populated from the homepage.
    - `npx vitest run lib/branding/scraper.test.ts` is fully green.
  </acceptance_criteria>
  <done>scrapeWebsite crawls subpages through the SSRF-guarded wrapper and returns labeled multi-page sections plus gallery images; every outbound fetch in scraper.ts (page, CSS, logo) is routed through fetchWithSsrfGuard per D-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped homepage links → subpage fetches | hrefs discovered in untrusted HTML are fetched; each must be SSRF-re-validated |
| scraped CSS-link / logo-`<img>` URLs → asset fetches | stylesheet hrefs and image srcs come from untrusted HTML; each fetch must be SSRF-re-validated (D-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-06 | Information Disclosure | SSRF via a homepage link to a private IP | mitigate | Every subpage URL pre-validated with `isUrlSafe` and fetched via `fetchWithSsrfGuard` (redirect-revalidating); private targets are skipped |
| T-47-07 | Denial of Service | crawling unbounded pages / huge responses | mitigate | 4-subpage cap (3-5 total) + per-page WR-01 5MB guard + AbortSignal timeout via wrapper |
| T-47-08 | Tampering | prompt injection via scraped subpage content reaching Claude | mitigate | CR-03 strip (comments/script/style) applied per labeled page before output; further mitigated by the D-01 "only use content belonging to this company" prompt instruction in Plan 47-04 (partial, accepted risk posture) |
| T-47-09 | Information Disclosure | SSRF via a CSS-link href or logo-`<img>` src pointing at a private IP (the two previously-bare fetches) | mitigate | CSS fetch (was line 80) and logo-candidate image fetch (was line 178) routed through `fetchWithSsrfGuard` per D-10; private/redirect-laundered targets throw and soft-fail (empty CSS / skipped candidate) |
</threat_model>

<verification>
- `npx vitest run lib/branding/scraper.test.ts` green (SCRAP-01 retained additively, SCRAP-06 + SCRAP-09 new, D-10 SSRF coverage new).
- scrapeWebsite imports `fetchWithSsrfGuard` and `isUrlSafe`; no bare `fetch` anywhere in scraper.ts (page, subpage, CSS, and logo-image fetches all wrapped).
- labeledPages + imageUrls present on the returned ScrapeResult.
</verification>

<success_criteria>
- SCRAP-06: homepage + up to 4 keyword-matched same-origin subpages are crawled (3-5 total), each fetch SSRF-re-validated.
- SCRAP-09: gallery image URLs extracted and noise-filtered, distinct from logo candidates, capped at 15.
- D-10: logo, page, and CSS fetches all route through the redirect-revalidating `fetchWithSsrfGuard` wrapper — no bare `fetch` remains in scraper.ts.
- Output is labeled per-page (foundation for SCRAP-08 in Plan 47-04) and backward-compatible (`htmlSnippet` retained).
</success_criteria>

<output>
Create `.planning/phases/47-skeema-monisivuinen-scraper-putki/47-03-SUMMARY.md` when done. Document the exact `ScrapeResult` shape (so Plans 47-04 and 47-05 consume it correctly), the per-page truncation budgets chosen, and confirm all four fetch call sites (homepage, subpage, CSS, logo-image) now route through `fetchWithSsrfGuard`.
</output>
