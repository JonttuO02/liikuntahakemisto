---
phase: 47-skeema-monisivuinen-scraper-putki
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.mjs
  - lib/branding/prompt.ts
  - lib/branding/screenshot.ts
  - lib/branding/analyzer.ts
  - lib/branding/analyzer.test.ts
autonomous: true
requirements: [SCRAP-08]
user_setup:
  - service: vercel
    why: "Same Vercel Pro dependency as Plan 47-01: the homepage screenshot path (D-03) needs Pro's maxDuration/waitUntil budget. The screenshot module is built to degrade gracefully (returns null) if Chromium is unavailable, so the rest of the pipeline works on Hobby — but screenshot capture itself only functions once Pro is active."

must_haves:
  truths:
    - "BRANDING_ANALYSIS_PROMPT is the verbatim user-authored multi-page prompt (logos array, colors {hex,role}[], source_page fields)"
    - "analyzeWithClaude accepts labeled multi-page sections and an optional homepage screenshot and returns array-based logos/colors (D-02: screenshot improves color extraction beyond CSS/meta-tag regex)"
    - "captureHomepageScreenshot returns a Buffer on success and null on any failure (non-fatal) — homepage ONLY, not the other crawled subpages (D-03/D-05)"
    - "Invalid logo type/color role/source_page values from Claude are defensively defaulted, never crash the pipeline"
    - "Vercel Pro upgrade is flagged as an out-of-band deployment prerequisite (D-04), not worked around in code — screenshot path degrades to null on Hobby"
  artifacts:
    - path: "lib/branding/prompt.ts"
      provides: "Replacement BRANDING_ANALYSIS_PROMPT (verbatim per D-01)"
      contains: "only use content that belongs to THIS company"
    - path: "lib/branding/screenshot.ts"
      provides: "Playwright + @sparticuz/chromium homepage screenshot, fail-soft"
      exports: ["captureHomepageScreenshot"]
    - path: "lib/branding/analyzer.ts"
      provides: "Array-based logos/colors analysis with screenshot content block"
      exports: ["analyzeWithClaude"]
  key_links:
    - from: "lib/branding/analyzer.ts"
      to: "lib/branding/prompt.ts"
      via: "import BRANDING_ANALYSIS_PROMPT"
      pattern: "import.*BRANDING_ANALYSIS_PROMPT"
---

<objective>
Replace the branding prompt with the verbatim user-authored multi-page version (D-01), reshape `analyzeWithClaude` to accept labeled multi-page HTML sections plus an optional homepage screenshot and to return array-based `logos`/`colors` with `source_page` provenance (SCRAP-08, D-02), and add a fail-soft homepage screenshot module using `playwright-core` + `@sparticuz/chromium` (D-03) with the Next.js 14.2 config needed to ship Chromium on Vercel. Install the two new production packages.

Purpose: SCRAP-08 — Claude currently receives one flat 8000-char homepage slice and returns a single `logo_index`/flat color list. The new shape gives Claude labeled per-page content (so it can attribute prices/hours to a `source_page`) and returns every distinct logo + role-tagged colors, which Phase 48's picker UI consumes.
Output: Replaced prompt, reshaped analyzer with runtime validation for the new array fields, a new screenshot module, installed packages, and updated `next.config.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/47-skeema-monisivuinen-scraper-putki/47-CONTEXT.md
@.planning/phases/47-skeema-monisivuinen-scraper-putki/47-PATTERNS.md
@.planning/phases/47-skeema-monisivuinen-scraper-putki/47-RESEARCH.md

@lib/branding/analyzer.ts
@lib/branding/prompt.ts
@lib/branding/analyzer.test.ts
@next.config.mjs

<interfaces>
<!-- The verbatim replacement prompt text is in 47-CONTEXT.md <specifics> — copy it EXACTLY, including the HUOM comment block. -->

<!-- New analyzer input shape (consumed from Plan 47-03's ScrapeResult, wired in Plan 47-05): -->
analyzeWithClaude must accept:
- `logoCandidatesBuffers: Buffer[]` (unchanged — numbered logo candidate images, index 0-based)
- `labeledPages: Array<{ label: string; html: string }>` (NEW — replaces the single htmlSnippet string)
- `screenshot?: Buffer | null` (NEW — optional homepage PNG; when present, prepend as an image content block labeled [SCREENSHOT: homepage])

<!-- New BrandingAnalysisResult shape returned (array-based per D-01): -->
- `logos: Array<{ index: number; type: 'wordmark'|'icon'|'combination'|'unknown' }>`  (replaces logo_index)
- `colors: Array<{ hex: string; role: 'background'|'primary'|'secondary'|'accent'|'text'|'unknown' }>`  (replaces string[])
- `prices: Array<{ label: string; price: string; source_page: string }>`
- `opening_hours: Array<{ day: string; open: string; close: string; source_page: string }>`
- `website_url: string`
- `raw_analysis: unknown`
KEEP `logo_index` and a flat `colors` projection populated additively (logos[0]?.index ?? -1; colors.map(c=>c.hex)) so Plan 47-05's existing upload/UPSERT and the Phase-46 GET consumer keep working until Phase 48 (RESEARCH.md Pitfall 5: additive, not breaking).

<!-- Existing runtime-validation idioms to replicate (analyzer.ts lines 87-100): VALID_LOGO_TYPES include-check with 'unknown' fallback; website_url typeof guard; WR-04 hex-regex color filter. -->
<!-- Screenshot code example is in 47-RESEARCH.md "Code Examples" and 47-PATTERNS.md screenshot.ts section — copy verbatim, add the `// Server-only.` header. -->
<!-- Mock-Anthropic test convention: analyzer.test.ts lines 7-35 (_mockCreate spy on MockAnthropic). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install playwright-core + @sparticuz/chromium and configure Next.js 14.2 externalization</name>
  <read_first>
    - package.json (current dependencies; note `playwright` ^1.60.0 is already a devDependency — leave it)
    - next.config.mjs (current config wrapped in withNextIntl(withSerwist(...)))
    - .planning/phases/47-skeema-monisivuinen-scraper-putki/47-RESEARCH.md (Standard Stack note on option (b); Pitfalls 1 & 2: Next 14 config keys + outputFileTracingIncludes)
  </read_first>
  <files>package.json, next.config.mjs</files>
  <action>
    Install the two production packages at the RESEARCH-verified versions (both passed slopcheck `[OK]`, Approved in the Package Legitimacy Audit — no [ASSUMED]/[SUS]/[SLOP] flags, so no blocking legitimacy checkpoint is required): run `npm install playwright-core@1.61.0 @sparticuz/chromium@149.0.0`. Do NOT import the full `playwright` package into production code — only `playwright-core` (option (b) per RESEARCH.md). Leave the existing `playwright` devDependency untouched.

    Re-verify versions against the live registry immediately before installing (RESEARCH.md "Valid until" note flags `@sparticuz/chromium`/`playwright-core` pairing as the one fast-moving risk): run `npm view @sparticuz/chromium version` and `npm view playwright-core version`; if a newer compatible pair is published, prefer the latest matching pair and record the chosen versions in the SUMMARY.

    Edit `next.config.mjs` to add an `experimental` block to the object passed to `withSerwist(...)` (Next.js 14.2 uses `experimental.serverComponentsExternalPackages`, NOT the Next-15 `serverExternalPackages` — Pitfall 1):
    - `serverComponentsExternalPackages: ['playwright-core', '@sparticuz/chromium']`
    - `outputFileTracingIncludes: { 'app/api/business/analyze-website/route': ['./node_modules/@sparticuz/chromium/bin/**'] }` (Pitfall 2 — ensures the Chromium binary ships with the deployed function).
    Preserve the existing `redirects()` config in the same object.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies['playwright-core']||!p.dependencies['@sparticuz/chromium']) process.exit(1); console.log('PKGS_OK')" && grep -q "serverComponentsExternalPackages" next.config.mjs && echo CONFIG_OK</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include `playwright-core` and `@sparticuz/chromium` (production deps, not dev).
    - `playwright` remains a devDependency, unchanged.
    - `next.config.mjs` contains `experimental.serverComponentsExternalPackages` listing both packages (NOT `serverExternalPackages`).
    - `next.config.mjs` contains `outputFileTracingIncludes` mapping the analyze-website route to the chromium bin dir.
    - The existing `redirects()` config is preserved.
    - Chosen package versions recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Packages installed (production), Next 14.2 externalization + file-tracing configured.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the prompt and build the fail-soft screenshot module</name>
  <read_first>
    - lib/branding/prompt.ts (current prompt to fully replace; keep header-comment style)
    - .planning/phases/47-skeema-monisivuinen-scraper-putki/47-CONTEXT.md (<specifics>: verbatim replacement prompt + HUOM comment block)
    - lib/branding/scraper.ts (toPngBase64 lines 17-28: the fail-soft `[branding/X]` error-log convention to mirror)
    - .planning/phases/47-skeema-monisivuinen-scraper-putki/47-RESEARCH.md (Code Examples: captureHomepageScreenshot)
  </read_first>
  <files>lib/branding/prompt.ts, lib/branding/screenshot.ts</files>
  <action>
    1. `prompt.ts`: replace the entire `BRANDING_ANALYSIS_PROMPT` template-literal body with the verbatim text from 47-CONTEXT.md `<specifics>`. Keep the existing two-line header comment, and prepend the HUOM comment block from CONTEXT.md above the export. Do not paraphrase or trim — the prompt is a locked decision (D-01).

    2. Create `lib/branding/screenshot.ts` opening with `// Server-only. Never import in client components.`. Copy the `captureHomepageScreenshot(url: string): Promise<Buffer | null>` implementation verbatim from RESEARCH.md Code Examples: import `chromium` from `@sparticuz/chromium` and `{ chromium as playwrightChromium }` from `playwright-core`; launch with `args: chromium.args, executablePath: await chromium.executablePath(), headless: true`; `newPage({ viewport: { width: 1280, height: 800 } })`; `page.goto(url, { waitUntil: 'networkidle', timeout: 15000 })`; `page.screenshot({ type: 'png', fullPage: false })`; return the buffer. Wrap in try/catch returning `null` and logging `console.error('[branding/screenshot] capture error:', err)`; `finally { await browser?.close() }`. The null return is intentional and non-fatal (D-02: screenshot is "in addition to", not required).
  </action>
  <verify>
    <automated>grep -q "only use content that belongs to THIS company" lib/branding/prompt.ts && grep -q "captureHomepageScreenshot" lib/branding/screenshot.ts && grep -q "Server-only" lib/branding/screenshot.ts && npx tsc --noEmit 2>&1 | grep -v "^$" | grep -i "screenshot\|prompt" || echo TS_CLEAN_FOR_FILES</automated>
  </verify>
  <acceptance_criteria>
    - `prompt.ts` contains the verbatim replacement prompt (greppable markers: "only use content that belongs to THIS company", the `logos` array shape, `source_page` fields) and the HUOM comment block.
    - `screenshot.ts` exports `captureHomepageScreenshot`, opens with the `// Server-only.` header, returns `Buffer | null`, and never throws (errors caught, null returned, `[branding/screenshot]` log prefix used).
    - `screenshot.ts` imports `playwright-core` (not the full `playwright` package).
  </acceptance_criteria>
  <done>Prompt replaced verbatim; fail-soft screenshot module built.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reshape analyzeWithClaude for labeled multi-page input + array-based logos/colors</name>
  <read_first>
    - lib/branding/analyzer.ts (full current file: content-array assembly lines 38-53, runtime-validation idioms lines 87-100, error handling)
    - lib/branding/analyzer.test.ts (mock-Anthropic _mockCreate convention, makeOkResponse helper)
    - lib/branding/prompt.ts (the new prompt shape the parsing must match — built in Task 2)
    - .planning/phases/47-skeema-monisivuinen-scraper-putki/47-CONTEXT.md (<specifics> closing paragraph: which fields to validate at runtime)
  </read_first>
  <files>lib/branding/analyzer.ts, lib/branding/analyzer.test.ts</files>
  <behavior>
    - Given mocked Claude JSON with `logos:[{index:0,type:"icon"},{index:1,type:"wordmark"}]`, the result `logos` array has both entries with valid types.
    - A logo entry with an out-of-bounds `index` (>= logoCandidatesBuffers.length) is dropped (not crashed).
    - A logo entry with an invalid `type` value is defaulted to `'unknown'`.
    - Given `colors:[{hex:"#112233",role:"background"},{hex:"notahex",role:"x"}]`, only the valid-hex entry survives, and an unrecognized role is defaulted to `'unknown'`.
    - `prices`/`opening_hours` entries missing `source_page` get `source_page: ''` (typeof-string guard), never undefined.
    - When a `screenshot` Buffer is provided, the content array sent to Claude includes an image block before the text block (image-before-text best practice).
    - Backward-compat: result also exposes `logo_index` (= logos[0]?.index ?? -1) and a flat `colors`-hex projection is available to the caller for the existing upload/UPSERT path.
    - labeledPages are concatenated into the text content as `[PAGE: <label>]\n<html>` sections (SCRAP-08), replacing the single "HTML snippet:" block.
  </behavior>
  <action>
    Change the `analyzeWithClaude` signature to `analyzeWithClaude(logoCandidatesBuffers: Buffer[], labeledPages: Array<{ label: string; html: string }>, screenshot?: Buffer | null)`.

    Update `BrandingAnalysisResult` to the array-based shape (see <interfaces>): `logos: Array<{index:number;type:LogoType}>`, `colors: Array<{hex:string;role:ColorRole}>`, `prices`/`opening_hours` with `source_page`, `website_url`, `raw_analysis`. Define `LogoType` (the existing 4-value union) and `ColorRole` ('background'|'primary'|'secondary'|'accent'|'text'|'unknown'). ADD additive backward-compat fields: `logo_index: number` and `colorsFlat: string[]` (or document that the caller derives them) so Plan 47-05's existing `uploadLogo(...logoBuffers[result.logo_index])` and the GET consumer keep working pre-Phase-48.

    Content assembly: build `imageItems` from the logo buffers as today; if `screenshot` is non-null, prepend a screenshot image block (`type:'image', source:{type:'base64',media_type:'image/png',data:screenshot.toString('base64')}`) BEFORE the logo images. Build the text block as `BRANDING_ANALYSIS_PROMPT + '\n\n' + labeledPages.map(p => '[PAGE: ' + p.label + ']\n' + p.html).join('\n\n')`. Keep `model: 'claude-haiku-4-5-20251001'`, raise `max_tokens` to 4096 (larger output: multiple logos/colors/prices with source_page). Keep `parseClaudeJson` fence-stripping.

    Runtime validation (replicate the existing defensive idioms): 
    - `logos`: filter to entries where `index` is an integer in `[0, logoCandidatesBuffers.length)`, mapping `type` through the `VALID_LOGO_TYPES` include-check with `'unknown'` fallback. Drop malformed entries.
    - `colors`: filter to entries whose `hex` matches `/^#[0-9a-fA-F]{3,6}$/` (WR-04 style), mapping `role` through a `VALID_ROLES` include-check with `'unknown'` fallback. Max 6 (per prompt).
    - `prices`/`opening_hours`: coerce each `source_page` to a string (default `''`) with the same typeof guard used for `website_url`.
    - `website_url`: keep the existing typeof-string guard.
    - Set `logo_index = result.logos[0]?.index ?? -1` for backward compat.
    Keep the `catch` → `console.error('[branding/analyzer] error:', err); throw err`.

    Update `analyzer.test.ts`: change `makeOkResponse`/`makeBuffers` usage to the new signature (pass `labeledPages` instead of a string), emit the new array-based JSON in the mock, and add `describe('analyzeWithClaude multi-page / SCRAP-08', ...)` covering every behavior-block case (out-of-bounds index dropped, invalid type defaulted, invalid hex filtered, invalid role defaulted, missing source_page → '', screenshot block present). Assert the text content includes `[PAGE: homepage]`.
  </action>
  <verify>
    <automated>npx vitest run lib/branding/analyzer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `analyzeWithClaude` signature is `(logoCandidatesBuffers, labeledPages, screenshot?)`.
    - Returned `logos` is an array of `{index,type}` with out-of-bounds indexes dropped and invalid types defaulted to `'unknown'`.
    - Returned `colors` is an array of `{hex,role}` with non-hex entries filtered and invalid roles defaulted to `'unknown'`, max 6.
    - `prices`/`opening_hours` entries always have a string `source_page`.
    - Screenshot, when provided, is sent as an image block before the text block.
    - Text content includes `[PAGE: <label>]` sections (asserted in test).
    - Backward-compat `logo_index` is derivable/present so the existing upload path still works.
    - `npx vitest run lib/branding/analyzer.test.ts` is fully green.
  </acceptance_criteria>
  <done>Analyzer accepts labeled multi-page input + optional screenshot and returns validated array-based logos/colors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped content → Claude prompt | Untrusted page HTML reaches the LLM; malicious content may attempt prompt injection |
| Claude JSON → application | LLM output is untrusted; malformed array fields must not crash the pipeline |
| npm install | new dependency supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-09 | Tampering | prompt injection via scraped content | mitigate | CR-03 strip (Plan 47-03) + D-01 "only use content belonging to THIS company" instruction; partial mitigation, accepted posture |
| T-47-10 | Tampering | malformed Claude JSON (bad index/type/role/source_page) | mitigate | Runtime validation: out-of-bounds indexes dropped, invalid enums defaulted to 'unknown', non-hex colors filtered, source_page coerced to string |
| T-47-SC | Tampering | npm installs (playwright-core, @sparticuz/chromium) | mitigate | Both passed slopcheck `[OK]`, Approved in RESEARCH.md Package Legitimacy Audit, verifiable against official GitHub repos (Microsoft Playwright, Sparticuz); no postinstall scripts; versions re-verified before install. Not [ASSUMED]/[SUS]/[SLOP] → no blocking human checkpoint required |
</threat_model>

<verification>
- `npx vitest run lib/branding/analyzer.test.ts` green (new array shape + screenshot block).
- `prompt.ts` contains the verbatim replacement prompt.
- `screenshot.ts` exports a fail-soft `captureHomepageScreenshot`.
- `package.json` + `next.config.mjs` configured per Next 14.2 (serverComponentsExternalPackages, outputFileTracingIncludes).
</verification>

<success_criteria>
- SCRAP-08: Claude receives labeled per-page sections (`[PAGE: ...]`) plus an optional homepage screenshot, and returns role-tagged colors and per-page-attributed prices/hours.
- The new array-based `logos`/`colors` shape is produced with defensive runtime validation, while additive backward-compat fields keep the existing upload/GET path working until Phase 48.
- Chromium ships correctly on Vercel via the Next 14.2 externalization + file-tracing config.
</success_criteria>

<output>
Create `.planning/phases/47-skeema-monisivuinen-scraper-putki/47-04-SUMMARY.md` when done. Record the exact new `analyzeWithClaude` signature and `BrandingAnalysisResult` shape (Plan 47-05 wires against it), the chosen package versions, and the Vercel-Pro/Assumption-A2 (outputFileTracingIncludes) caveat to smoke-test in a preview deployment.
</output>
