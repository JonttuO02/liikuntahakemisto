---
phase: 64-hallintaoikeuspyynn-t-dashboard-ui
plan: 04
type: execute
wave: 2
depends_on: ["64-01", "64-02", "64-03"]
files_modified:
  - app/components/TeamManagementPopup.tsx
  - app/components/DiagonaalKortti.tsx
  - app/business/page.tsx
  - messages/fi.json
  - messages/en.json
autonomous: false
requirements: [ACCESS-04, ACCESS-07]
must_haves:
  truths:
    - "One team-management icon button (Users glyph, D-01) on the DiagonaalKortti dashboardActions panel opens a single combined popup — the ONE entry point for both requests and team (D-01, D-08)"
    - "The icon renders only when the venue has >=1 pending request OR >=1 team member beyond the owner (D-02)"
    - "The popup has two sections: Pending requests (rows with requester name + inline Approve/Reject) and Current team (rows with member name/role + remove control) (D-03)"
    - "Approve calls the existing POST /api/business/access-request/approve with request_id; Reject calls the existing POST .../reject with request_id and NO reason field (one-click reject, D-13)"
    - "The owner's own row is shown in Current team labeled '(Sinä) Omistaja' with its remove control disabled/grayed (opacity-40 pointer-events-none) — never omitted (D-14)"
    - "Removing a sub-manager requires an inline two-state confirm before the DELETE to /api/business/access-request/remove fires (D-10)"
    - "The popup fetches its data from GET /api/business/access-request/list on open using the session bearer token; all cross-account identity comes from that service-role endpoint, never the anon client (D-07, D-09)"
  prohibitions:
    - statement: "MUST NOT build a nav-level unread counter or notification badge/bell for pending requests — showing/hiding the icon is the only signal (D-04)"
      status: resolved
      verification: "No badge/dot/count element is rendered on the icon or in the nav; visibility toggle only (source review)"
  artifacts:
    - "app/components/TeamManagementPopup.tsx — new client popup component"
    - "app/components/DiagonaalKortti.tsx — new onManageTeam dashboardActions prop + Users icon button"
    - "app/business/page.tsx — popup state, D-02 visibility gate, popup instance"
  key_links:
    - "DiagonaalKortti Users button → business/page.tsx opens TeamManagementPopup(paikka_id) → GET list → Approve/Reject (existing) + Remove (new) → refetch/optimistic update"
---

<objective>
Build the ACCESS-04/ACCESS-07 dashboard UI: a single "team management" popup opened from a new icon button on `DiagonaalKortti`'s `dashboardActions` panel. The popup fetches pending requests + current team from the Plan 64-01 list endpoint, wires Approve/Reject to the existing Phase 60 endpoints, and wires sub-manager removal (with an inline confirm) to the Plan 64-02 removal endpoint. The owner's own row is shown but its remove control is disabled (D-14).

Purpose: The in-dashboard surface ACCESS-04 requires (view + approve/reject) and ACCESS-07 requires (remove sub-manager, cannot remove self), following the exact glass/backdrop/Escape popup and dashboard-icon conventions from Phase 63.
Output: One new component + two modified files + new i18n keys.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-hallintaoikeuspyynn-t-dashboard-ui/64-UI-SPEC.md
@.planning/phases/64-hallintaoikeuspyynn-t-dashboard-ui/64-PATTERNS.md
@.planning/phases/64-hallintaoikeuspyynn-t-dashboard-ui/64-RESEARCH.md
@app/components/RejectionReasonPopup.tsx
@app/business/page.tsx
</context>

## Artifacts this phase produces (Plan 64-04)

New symbols created by this plan (exclude from source-drift acknowledgement):
- File `app/components/TeamManagementPopup.tsx` (default export `TeamManagementPopup`)
- New optional prop `onManageTeam?: () => void` on `DiagonaalKortti`'s `dashboardActions`
- New client state in `app/business/page.tsx`: `teamPopupPaikkaId: number | null`
- New i18n keys in `Business` namespace: `manageTeamCta`, `teamManagementTitle`, `sectionPendingRequests`, `sectionCurrentTeam`, `pendingEmptyBody`, `approveCta`, `rejectCta`, `ownerSelfLabel`, `memberRoleBadge`, `removeAccessAria`, `ownerCannotRemoveAria`, `removeConfirmBody`, `removeConfirmCta`, `removeCancelCta`, `teamActionError`, `teamAlreadyProcessed`

<tasks>

<task type="auto">
  <name>Task 1: Build TeamManagementPopup component + i18n</name>
  <read_first>
    - app/components/RejectionReasonPopup.tsx (full file — copy backdrop/panel/Escape/close scaffolding verbatim, durations 0.2 backdrop / 0.25 panel ease [0.25,0.1,0.25,1])
    - app/business/liity/page.tsx (createBusinessBrowserClient().auth.getSession() → Authorization: 'Bearer ' + token fetch pattern, lines 90-102)
    - app/api/business/access-request/approve/route.ts (request_id body contract for Approve)
    - app/api/business/access-request/reject/route.ts (request_id body contract; reason omitted per D-13)
    - .planning/phases/64-hallintaoikeuspyynn-t-dashboard-ui/64-UI-SPEC.md (Component & Interaction Notes: popup layout, row layouts, colors, copy table)
    - .planning/phases/64-hallintaoikeuspyynn-t-dashboard-ui/64-PATTERNS.md (TeamManagementPopup section — list rendering + inline confirm guidance)
    - messages/fi.json / messages/en.json (Business namespace — insert new keys)
  </read_first>
  <action>
    Create app/components/TeamManagementPopup.tsx ('use client'). Copy the scaffolding from RejectionReasonPopup.tsx exactly: `fixed inset-0 z-[80] flex items-end sm:items-center justify-center`, `aria-modal="true" role="dialog"`, backdrop `motion.div` (`bg-[rgba(0,0,0,0.40)]`, duration 0.2, click-to-close), Escape `useEffect` listener, and the top-right `X` close button. Widen the panel to `glass rounded-2xl p-6 w-full max-w-md mx-4` and cap body height with `max-h-[60vh] overflow-y-auto` to avoid unbounded growth (UI-SPEC).
    Props: `{ open: boolean; paikkaId: number | null; onClose: () => void; onChanged?: () => void }`.
    Data: a `useEffect` gated on `open && paikkaId != null` fetches `GET /api/business/access-request/list?paikka_id=<paikkaId>` with `Authorization: 'Bearer ' + token` from `createBusinessBrowserClient().auth.getSession()`. Store `{ pendingRequests, teamMembers }` in local state with a loading + error state.
    Layout top-to-bottom: `h2` title (`teamManagementTitle`) → section label `sectionPendingRequests` (`text-[10px] font-bold uppercase tracking-widest`) + rows OR `pendingEmptyBody` empty-line → hairline `border-t border-[rgba(0,0,0,0.07)]` → section label `sectionCurrentTeam` + team rows.
    Pending-request row: requester name bold (`text-sm font-bold text-[#111111]`, fall back to email when name is null) on the left; Approve + Reject buttons on the right (`flex gap-2`, `h-9 px-4 rounded-full text-sm font-bold`) — Approve filled `bg-[#111111] hover:bg-[#333333] text-white` calling POST `/api/business/access-request/approve` with `{ request_id: row.id }`; Reject outline `text-red-600 border border-red-200 hover:border-red-400` calling POST `/api/business/access-request/reject` with `{ request_id: row.id }` and NO reason (D-13). Buttons show `disabled:opacity-60` while in flight; on success remove the row from local state and call `onChanged?.()`.
    Current-team row: name bold + role badge (`text-[10px] font-bold uppercase tracking-widest`, `ownerSelfLabel` "(Sinä) Omistaja" for the `isSelf` owner row, else `memberRoleBadge` "Jäsen"). Remove control on the right: a `w-7 h-7 rounded-full` `Trash2` button `text-red-600 hover:text-red-700`. For the `isSelf` row render the button `opacity-40 pointer-events-none text-[rgba(17,17,17,0.35)]` with aria-label `ownerCannotRemoveAria` — never omit the row (D-14). For other rows, clicking swaps the row's right side to an inline two-state confirm (`useState<string|null>` confirm-target keyed by userId, no separate modal): `removeConfirmBody` text with `removeConfirmCta` filled `bg-red-600 hover:bg-red-700 text-white` (calls DELETE-intent POST `/api/business/access-request/remove` with `{ paikka_id: paikkaId, target_user_id: userId }`) + `removeCancelCta` plain text. On success remove the row and call `onChanged?.()`. On a 409 show `teamAlreadyProcessed`, on any other failure show `teamActionError`.
    Render display_name and email as escaped JSX text nodes only — never `dangerouslySetInnerHTML` (XSS mitigation, matches RejectionReasonPopup convention).
    Add all new i18n keys (listed in the Artifacts section above) to messages/fi.json and messages/en.json under `Business`, using the exact fi/en copy from the UI-SPEC Copywriting Contract table. Do not reorder or remove existing keys.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "JSON.parse(require('fs').readFileSync('messages/fi.json','utf8')); JSON.parse(require('fs').readFileSync('messages/en.json','utf8')); console.log('ok')"</automated>
    <human-check>Open /business as an owner of a venue with a pending request and a sub-manager. Confirm: the popup opens with both sections; Approve/Reject act on a pending row; the owner's own row shows a disabled remove control labeled "(Sinä) Omistaja"; clicking remove on a sub-manager shows the inline confirm before the DELETE fires.</human-check>
  </verify>
  <acceptance_criteria>
    - app/components/TeamManagementPopup.tsx exists, is a client component, and default-exports `TeamManagementPopup`
    - Fetches the list endpoint on open with a bearer token (source: `Authorization` header + `access-request/list`)
    - Reject fetch body contains `request_id` and no `reason` key (D-13) — source assertion
    - Remove fetch body contains `paikka_id` and `target_user_id` (source assertion)
    - The isSelf/owner team row renders `opacity-40 pointer-events-none` on its remove control and is present in the list (D-14)
    - No `dangerouslySetInnerHTML` in the file (`grep -c dangerouslySetInnerHTML app/components/TeamManagementPopup.tsx` returns 0)
    - `npx tsc --noEmit` passes; both message files parse as valid JSON and contain `teamManagementTitle`
  </acceptance_criteria>
  <done>TeamManagementPopup renders both sections, wires approve/reject/remove with correct contracts and the owner self-protection UI, with all copy in i18n.</done>
</task>

<task type="auto">
  <name>Task 2: Add the onManageTeam icon button to DiagonaalKortti</name>
  <read_first>
    - app/components/DiagonaalKortti.tsx (dashboardActions interface lines 43-50; existing onCopyInviteLink icon-button block lines 264-274; lucide import line 5)
    - .planning/phases/64-hallintaoikeuspyynn-t-dashboard-ui/64-UI-SPEC.md (Entry-point icon section — Users glyph, always last, panelShade-conditional styling, Pitfall 6 no-wrap)
    - .planning/phases/64-hallintaoikeuspyynn-t-dashboard-ui/64-PATTERNS.md (DiagonaalKortti section — exact button shape to copy)
  </read_first>
  <action>
    In app/components/DiagonaalKortti.tsx: add `onManageTeam?: () => void` to the `dashboardActions` interface (after `onShowRejectionInfo?`). Add `Users` to the lucide-react import on line 5. In the dashboardActions controls panel, add a new icon button as the LAST icon in the existing `flex items-center gap-2` row, copying the onCopyInviteLink button block (lines 264-274) exactly and swapping: handler → `dashboardActions.onManageTeam`, glyph → `<Users className="w-3.5 h-3.5" />`, aria-label → `tBusiness('manageTeamCta')`. Guard render with `{dashboardActions.onManageTeam && (...)}`. Keep the `w-7 h-7 rounded-full flex items-center justify-center [transition:color_150ms_ease]` class and the `panelShade ? { backgroundColor: panelChipBg, color: panelShadeContrastText } : 'glass-btn ...'` conditional identical to the sibling buttons. Preserve the `e.stopPropagation(); e.preventDefault()` click guards. Do NOT add any numeric badge/dot to the icon (D-04).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>On /business, render a DiagonaalKortti for an approved venue that also has an invite-link-copy affordance and confirm all icons fit on one row (no wrap) at both mobile (w-full) and desktop (sm:w-[396px]) widths (Pitfall 6).</human-check>
  </verify>
  <acceptance_criteria>
    - `dashboardActions` interface includes `onManageTeam?: () => void`
    - `Users` is imported from lucide-react (`grep -c "Users" app/components/DiagonaalKortti.tsx` >= 1)
    - The new button reuses the exact `w-7 h-7 rounded-full` shape and panelShade conditional as the onCopyInviteLink button, with `stopPropagation`/`preventDefault` guards
    - No badge/dot/count element on the icon (D-04) — source review
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>DiagonaalKortti exposes a Users team-management icon (D-01) following the established icon-button pattern, with no notification badge.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the popup + D-02 visibility gate into the business dashboard</name>
  <read_first>
    - app/business/page.tsx (DashboardVenueCard lines 87-134; dashboardActions object lines 122-131; page state lines 143-151; checkState fetch block lines 153-218; venueLinks.map render loop lines 275-287; RejectionReasonPopup instance lines 342-347)
    - app/api/business/access-request/list/route.ts (Plan 64-01 GET endpoint — response shape `{ pendingRequests: [...], teamMembers: [{ userId, name, email, role, isSelf }] }`; the venue-scoped read reused here for the D-02 gate)
    - app/business/liity/page.tsx (createBusinessBrowserClient().auth.getSession() → Authorization: 'Bearer ' + token fetch pattern, lines 90-102 — same bearer-token pattern used for the render-time list fetch)
    - app/components/TeamManagementPopup.tsx (the component being wired — props contract)
    - .planning/phases/64-hallintaoikeuspyynn-t-dashboard-ui/64-PATTERNS.md (business/page.tsx section — visibility gate + state + wiring guidance)
  </read_first>
  <action>
    In app/business/page.tsx: add page-level state `const [teamPopupPaikkaId, setTeamPopupPaikkaId] = useState<number | null>(null)` alongside `rejectionPopupLink`, plus `const [teamSummaryByPaikkaId, setTeamSummaryByPaikkaId] = useState<Record<number, { pendingCount: number; memberBeyondOwnerCount: number }>>({})`.

    D-02 visibility gate (implement EXACTLY as the locked decision states — the icon shows ONLY when the venue has pending requests AND/OR team members beyond the owner; do NOT relax this to "all approved venues"): the gate needs, per venue, the pending-request count for that paikka_id and the count of team members beyond the owner. Neither is available from the existing `pendingAccessRequests` fetch (it is requester-scoped to the current user, so it does NOT reflect other users' pending requests to an owned venue). Source the gate data from the SAME service-role read the popup uses — the Plan 64-01 endpoint `GET /api/business/access-request/list?paikka_id=<id>` — computed ONCE per venue at dashboard-render time. Do NOT add a separate/second fetch path (no direct anon-client count query, no new count endpoint): reuse this list endpoint.
    In `checkState()` (after `setVenueLinks(...)`), for each linked venue whose derived status is `approved` and not kesken, fetch `GET /api/business/access-request/list?paikka_id=<paikka_id>` with `Authorization: 'Bearer ' + token` (token from `createBusinessBrowserClient().auth.getSession()`, same pattern as app/business/liity/page.tsx). Run these per-venue reads with `Promise.all` over the approved-venue subset. From each response derive `{ pendingCount: pendingRequests.length, memberBeyondOwnerCount: teamMembers.filter(m => !m.isSelf).length }` and accumulate into a map keyed by paikka_id; call `setTeamSummaryByPaikkaId(map)`. `memberBeyondOwnerCount` counts every team row that is NOT the caller's own owner row (`isSelf === false`) — i.e. sub-managers. On a per-venue fetch failure, default that venue's summary to `{ pendingCount: 0, memberBeyondOwnerCount: 0 }` (fail closed → icon hidden) so a transient error never surfaces a broken control. Only approved-and-not-kesken venues are read, so this is authorized for every fetch (non-owners never reach this branch).
    Thread `onManageTeam` into the `DashboardVenueCard` dashboardActions object using the conditional-undefined idiom, gated on BOTH approval AND the D-02 state test: `onManageTeam: link.claim_status === 'approved' && !isKesken && ((teamSummaryByPaikkaId[link.paikka_id]?.pendingCount ?? 0) >= 1 || (teamSummaryByPaikkaId[link.paikka_id]?.memberBeyondOwnerCount ?? 0) >= 1) ? () => onManageTeam(link.paikka_id) : undefined`. Plumb a new `onManageTeam: (paikkaId: number) => void` prop and the per-venue summary (or the derived boolean) through DashboardVenueCard from the page; the page's `onManageTeam` handler calls `setTeamPopupPaikkaId`. Because the gate depends on `teamSummaryByPaikkaId`, the icon appears once the render-time summaries resolve — an approved-but-empty venue (owner only, zero pending) correctly shows NO icon per D-02.
    Render one page-level `<TeamManagementPopup open={teamPopupPaikkaId !== null} paikkaId={teamPopupPaikkaId} onClose={() => setTeamPopupPaikkaId(null)} onChanged={/* re-run checkState so the D-02 summaries + gate refresh after an approve/reject/remove */} />` next to the existing RejectionReasonPopup instance. Import TeamManagementPopup at the top. The popup's own on-open fetch (Task 1) hits the same list endpoint for live interaction data; this is intentional (fresh state at open time), not a new gate fetch — the render-time reads exist solely to drive the D-02 icon gate.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
    <human-check>Open /business as an owner. (1) A venue with >=1 pending request OR >=1 sub-manager beyond the owner shows the team icon; clicking it opens the popup scoped to that venue; approving/rejecting/removing updates the list. (2) An approved-but-empty venue (owner only, zero pending requests) shows NO team icon (D-02). (3) The icon does not appear on kesken/pending/rejected cards. (4) After the last pending request is handled and the last sub-manager removed, the icon disappears on the next dashboard load (checkState re-run via onChanged).</human-check>
  </verify>
  <acceptance_criteria>
    - `teamPopupPaikkaId` state exists and drives a single page-level TeamManagementPopup instance
    - `teamSummaryByPaikkaId` is populated at render time from per-venue `GET /api/business/access-request/list` reads (source assertion: no separate/second gate fetch path — the same list endpoint is reused)
    - `onManageTeam` is passed to DiagonaalKortti ONLY for `claim_status === 'approved' && !isKesken` venues AND only when that venue's summary has `pendingCount >= 1 || memberBeyondOwnerCount >= 1` (D-02 gate — source assertion; an approved owner-only, request-less venue passes `undefined` and shows no icon)
    - `memberBeyondOwnerCount` is derived by excluding the caller's own owner row (`isSelf === false`) so the owner alone never trips the gate
    - TeamManagementPopup is imported and rendered in app/business/page.tsx
    - `npx tsc --noEmit` passes and `npx vitest run` is GREEN (no regressions)
  </acceptance_criteria>
  <done>The dashboard shows the team-management icon only for venues with pending requests and/or sub-managers beyond the owner (D-02), opens a venue-scoped popup from the DiagonaalKortti icon, and the approve/reject/remove flow works end-to-end against the new + existing endpoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Popup client → list/approve/reject/remove endpoints | Bearer-token-authenticated fetches; all authorization is server-side (Plans 64-01/64-02 + existing Phase 60) |
| display_name/email → DOM | User-supplied identity strings rendered into the popup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-64-14 | Information Disclosure / XSS | popup identity rendering | medium | mitigate | display_name/email rendered as escaped JSX text only; no `dangerouslySetInnerHTML` (Task 1 acceptance criterion) |
| T-64-15 | Tampering | client self-removal disable | high | mitigate | The disabled owner remove control is UI-only defense-in-depth; the actual block is the Plan 64-02 server hard-block (T-64-05) — this plan relies on it, does not replace it |
| T-64-16 | Elevation of Privilege | approve/reject/remove authorization | high | accept (delegated) | The popup calls endpoints whose venue-scoped owner authorization is enforced server-side (Plans 64-01/64-02, Phase 60); the client performs no authorization decision |
</threat_model>

<verification>
- `npx tsc --noEmit` passes across the three modified/new files
- `npx vitest run` — full suite GREEN (no regressions)
- Manual UAT (VALIDATION.md Manual-Only): icon visibility, popup two sections, owner disabled row, remove confirm step
</verification>

<success_criteria>
ACCESS-04: owner views + approves/rejects pending requests in the dashboard popup; sub-managers cannot (server-enforced). ACCESS-07: owner removes sub-managers via inline-confirm; owner's own row is visibly protected and cannot be removed. No notification badge/bell added (D-04).
</success_criteria>

<output>
Create `.planning/phases/64-hallintaoikeuspyynn-t-dashboard-ui/64-04-SUMMARY.md` when done
</output>
