---
phase: 04-service-information-ui
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/components/HoursTable.tsx
  - app/paikat/[id]/page.tsx
autonomous: true
requirements:
  - UI-04

must_haves:
  truths:
    - "The venue profile page shows a full weekly hours schedule with grouped Finnish day ranges"
    - "Today's hours group is displayed in bold (font-bold text-[#111111]); other groups are muted"
    - "The hours section renders as a Row with a Clock icon and 'Aukioloajat' label"
    - "When aukioloajat is null, no hours Row is rendered (silently omitted)"
    - "The price Row shows hinta_kuvaus as plain prose text when present"
    - "When hinta_kuvaus is null, the price Row shows hintateksti() result in serif bold"
    - "When both hinta_kuvaus and hintateksti() are null/empty, no price Row is rendered"
    - "TypeScript compiles with no errors after changes"
  artifacts:
    - path: "app/components/HoursTable.tsx"
      provides: "Client island for today-highlighting of grouped hours"
      contains: "use client"
    - path: "app/paikat/[id]/page.tsx"
      provides: "Hours Row + updated price Row using formatGroupedHours and hinta_kuvaus"
      contains: "formatGroupedHours"
  key_links:
    - from: "app/paikat/[id]/page.tsx"
      to: "lib/aukiolo.ts"
      via: "import { formatGroupedHours } from '@/lib/aukiolo'"
      pattern: "formatGroupedHours"
    - from: "app/paikat/[id]/page.tsx"
      to: "app/components/HoursTable.tsx"
      via: "import HoursTable from '@/app/components/HoursTable'"
      pattern: "HoursTable"
    - from: "app/components/HoursTable.tsx"
      to: "lib/aukiolo.ts"
      via: "import type { HourGroup } from '@/lib/aukiolo'"
      pattern: "HourGroup"
---

<objective>
Create HoursTable.tsx (client island for today-highlighting) and update the venue profile page to show grouped weekly opening hours and the hinta_kuvaus price description. Implements D-06 (profile), D-09, and D-10.

Purpose: The profile page is the one place where a user gets comprehensive service information — full weekly hours at a glance, with today highlighted, plus the full pricing description that the card can only partially show.

Output: New app/components/HoursTable.tsx + modified app/paikat/[id]/page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-service-information-ui/04-CONTEXT.md
@.planning/phases/04-service-information-ui/04-UI-SPEC.md
@.planning/phases/04-service-information-ui/04-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs. -->

From lib/aukiolo.ts (created in 04-01):
```typescript
export interface HourGroup {
  key: string        // e.g. "monday-tuesday-wednesday-thursday-friday"
  label: string      // e.g. "Ma–Pe" or "La"
  hours: string      // e.g. "06:00–22:00" or "suljettu"
  dayKeys: string[]  // e.g. ['monday','tuesday','wednesday','thursday','friday']
}

export function formatGroupedHours(
  aukioloajat: Record<string, { open: string; close: string }> | null | undefined
): HourGroup[]
// Returns [] for null/undefined input — no today-detection, pure data grouping
```

From lib/utils.ts (existing):
```typescript
export function hintateksti(min: number | null, max: number | null): string
// Returns '' when both null — check !== '' before using
```

Existing profile page Row component (lines 121-133 of app/paikat/[id]/page.tsx):
```typescript
function Row({ icon, label, children }: { icon: React.ReactNode; label: string; children: React.ReactNode }) {
  return (
    <div className="flex items-start gap-4">
      <div className="w-10 h-10 rounded-xl glass flex items-center justify-center shrink-0">
        {icon}
      </div>
      <div>
        <p className="text-[10px] font-bold text-[rgba(17,17,17,0.4)] uppercase tracking-widest mb-0.5">{label}</p>
        {children}
      </div>
    </div>
  )
}
```

Current profile page hinta block (lines 87-91):
```typescript
{hinta && (
  <Row icon={<CircleDollarSign className="w-5 h-5 text-[rgba(17,17,17,0.5)]" />} label="Hinta">
    <span className="font-serif text-xl font-bold text-[#111111]">{hinta}</span>
  </Row>
)}
```

Existing Row order in profile page content card (lines 65-97):
1. Sijainti Row (lat/lng present)
2. Puhelin Row (puhelin present)
3. Hinta Row (hinta truthy)   ← update + add Aukioloajat Row after/before
4. Kuvaus Row (kuvaus present)

Target Row order after this plan:
1. Sijainti Row
2. Aukioloajat Row (new) — if aukioloajat non-null and formatGroupedHours returns non-empty
3. Puhelin Row
4. Hinta Row (updated to use hinta_kuvaus fallback)
5. Kuvaus Row
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create HoursTable.tsx client island</name>
  <files>app/components/HoursTable.tsx</files>

  <read_first>
    - .planning/phases/04-service-information-ui/04-RESEARCH.md (Pattern 3 section — HoursTable client island)
    - .planning/phases/04-service-information-ui/04-UI-SPEC.md (section "Weekly Hours Row", today highlight classes)
    - lib/aukiolo.ts (confirm HourGroup interface shape and dayKeys field)
  </read_first>

  <action>
    Create app/components/HoursTable.tsx as a 'use client' component:

    - First line: 'use client'
    - Import: import type { HourGroup } from '@/lib/aukiolo'
    - DAY_KEYS constant (for today detection): ['sunday','monday','tuesday','wednesday','thursday','friday','saturday']
    - Export default function HoursTable({ groups }: { groups: HourGroup[] })
    - Inside: const todayKey = DAY_KEYS[new Date().getDay()]
    - Render: <div className="flex flex-col gap-1">
    - Map over groups: for each group, check isToday = group.dayKeys.includes(todayKey)
    - Apply className per UI-SPEC:
        - today: 'text-sm font-bold text-[#111111]'
        - other: 'text-sm text-[rgba(17,17,17,0.65)]'
    - Each row: <p key={group.key} className={...}>{group.label} {group.hours}</p>
    - Note: group.hours is already 'suljettu' for closed days (from formatGroupedHours) — no special handling needed here

    The component receives pre-grouped data from the server (pure data, no Date required for grouping). It only needs browser Date for today-highlighting. This is why it must be a client component — server UTC time would be wrong for Finnish users at midnight.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - app/components/HoursTable.tsx exists
    - First line of file is exactly: 'use client'
    - File contains: import type { HourGroup } from '@/lib/aukiolo'
    - File contains: DAY_KEYS = ['sunday','monday','tuesday','wednesday','thursday','friday','saturday']
    - File contains: new Date().getDay() (client-side today detection)
    - File contains: group.dayKeys.includes(todayKey) (today check)
    - File contains: 'text-sm font-bold text-[#111111]' (today highlight class)
    - File contains: 'text-sm text-[rgba(17,17,17,0.65)]' (other days class)
    - File contains: flex flex-col gap-1 (container class)
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>

  <done>HoursTable.tsx is a valid 'use client' component that highlights today's group using browser local time.</done>
</task>

<task type="auto">
  <name>Task 2: Update profile page — add hours Row, update price Row</name>
  <files>app/paikat/[id]/page.tsx</files>

  <read_first>
    - app/paikat/[id]/page.tsx (read the full current file before touching anything — Row order, existing imports, hinta block)
    - app/components/HoursTable.tsx (confirm component API before importing)
    - lib/aukiolo.ts (confirm formatGroupedHours signature)
    - lib/utils.ts (confirm hintateksti returns '' not null for both-null case)
    - .planning/phases/04-service-information-ui/04-UI-SPEC.md (section "Price Display" profile row, section "Weekly Hours Row")
  </read_first>

  <action>
    Change 1 — Add new imports:
    In the existing import block at top of app/paikat/[id]/page.tsx:
    - Add Clock to the lucide-react import: { Phone, MapPin, CircleDollarSign, Info, ChevronLeft, Clock }
    - Add: import { formatGroupedHours } from '@/lib/aukiolo'
    - Add: import HoursTable from '@/app/components/HoursTable'

    Change 2 — Compute grouped hours and updated price in the function body:
    After existing const declarations (laji, hinta, osoiteRivi), add:
      const hoursGroups  = formatGroupedHours(paikka.aukioloajat ?? null)
      const hintaTeksti  = hintateksti(paikka.hinta_min, paikka.hinta_max)
      const priceToShow  = paikka.hinta_kuvaus || (hintaTeksti !== '' ? hintaTeksti : null)

    NOTE: The existing "const hinta = hintateksti(...)" line should be replaced with hintaTeksti above. Update all references from hinta to hintaTeksti/priceToShow in this file.

    Change 3 — Insert Aukioloajat Row after Sijainti Row, before Puhelin Row (D-10):
    The new Row goes right after the closing bracket of the Sijainti Row conditional block.

      {hoursGroups.length > 0 && (
        <Row icon={<Clock className="w-5 h-5 text-[rgba(17,17,17,0.5)]" />} label="Aukioloajat">
          <HoursTable groups={hoursGroups} />
        </Row>
      )}

    When aukioloajat is null: formatGroupedHours returns [] → hoursGroups.length === 0 → Row not rendered. This satisfies D-10 ("omit the Row entirely when null").

    Change 4 — Update price Row to use hinta_kuvaus fallback chain (D-06 profile):
    Replace the existing hinta Row block:
      {hinta && (
        <Row icon={<CircleDollarSign ...>} label="Hinta">
          <span className="font-serif text-xl font-bold text-[#111111]">{hinta}</span>
        </Row>
      )}
    With:
      {priceToShow && (
        <Row icon={<CircleDollarSign className="w-5 h-5 text-[rgba(17,17,17,0.5)]" />} label="Hinta">
          {paikka.hinta_kuvaus ? (
            <p className="text-sm text-[rgba(17,17,17,0.65)] leading-relaxed">{paikka.hinta_kuvaus}</p>
          ) : (
            <span className="font-serif text-xl font-bold text-[#111111]">{priceToShow}</span>
          )}
        </Row>
      )}

    Rendering distinction per UI-SPEC (section "Price Display"):
    - hinta_kuvaus present → plain prose text: text-sm text-[rgba(17,17,17,0.65)] leading-relaxed
    - hintateksti() result (numeric range) → serif bold: font-serif text-xl font-bold text-[#111111]
    - both null → priceToShow is null → Row not rendered (omitted entirely)
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run lib/aukiolo.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - app/paikat/[id]/page.tsx imports Clock from 'lucide-react'
    - app/paikat/[id]/page.tsx imports formatGroupedHours from '@/lib/aukiolo'
    - app/paikat/[id]/page.tsx imports HoursTable from '@/app/components/HoursTable' (or '@/components/HoursTable' — whichever matches the project path alias)
    - app/paikat/[id]/page.tsx contains "hoursGroups.length > 0" (guards the hours Row)
    - app/paikat/[id]/page.tsx contains "label=\"Aukioloajat\"" (hours Row label)
    - app/paikat/[id]/page.tsx contains "<HoursTable groups={hoursGroups}" (client island render)
    - app/paikat/[id]/page.tsx contains "paikka.hinta_kuvaus" (price fallback chain)
    - app/paikat/[id]/page.tsx contains "leading-relaxed" (hinta_kuvaus prose styling)
    - app/paikat/[id]/page.tsx contains "font-serif text-xl font-bold" (hintateksti fallback styling)
    - The old "const hinta = hintateksti" line is replaced (no dangling reference to old hinta variable)
    - npx tsc --noEmit exits with code 0
    - npx vitest run lib/aukiolo.test.ts exits with code 0
  </acceptance_criteria>

  <done>Profile page shows Aukioloajat Row with grouped weekly hours (today highlighted via HoursTable client island) and updated Hinta Row with hinta_kuvaus prose fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase DB → paikka server fetch | Profile page fetches venue by ID; ID validated as integer before query; no user-controlled SQL |
| hinta_kuvaus / aukioloajat → profile render | Operator-entered data rendered as plain text; no dangerouslySetInnerHTML used |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | hinta_kuvaus on profile page | accept | Pricing prose is intended public information; operator-entered in seed data; no PII |
| T-04-05 | Spoofing | Profile page ID param | accept | Existing `Number.isInteger(id) || id < 1` guard already in place (Phase 1); not changed in this plan |
</threat_model>

<verification>
npx tsc --noEmit — zero TypeScript errors across all modified and new files
npx vitest run lib/aukiolo.test.ts — utility tests remain green
Visual: visit /paikat/1 in dev server; confirm Aukioloajat Row appears with grouped Finnish day ranges; confirm today's group is bold; confirm Hinta Row shows prose text when hinta_kuvaus is populated
</verification>

<success_criteria>
HoursTable.tsx client island created and correctly highlights today using browser local time (D-09 today highlight). Profile page shows Aukioloajat Row with grouped weekly hours (D-09, D-10) and uses hinta_kuvaus fallback chain for the Hinta Row (D-06). When aukioloajat is null the hours Row is omitted. When both price fields are null the Hinta Row is omitted. TypeScript clean.
</success_criteria>

<output>
Create `.planning/phases/04-service-information-ui/04-04-SUMMARY.md` when done.
</output>
