---
phase: 04-service-information-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/components/PaikkaKortti.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "A card with an open venue shows a green dot + 'Auki nyt · 09:00–21:00' below the venue name"
    - "A card with a closed venue shows 'Suljettu' in muted gray below the venue name"
    - "A card with null aukioloajat (aukinyt prop false or absent) shows 'Aukioloajat lisätään pian' in the same position (D-03)"
    - "A card with null aukioloajat when aukinyt prop is true shows 'Aukioloajat tuntematon' instead (D-08)"
    - "A card for a venue with 'kertakäynti' in hinta_kuvaus shows a 'Kertakäynti OK' pill badge next to the sport badge"
    - "The booking CTA link reads 'Varaa aika →' (not 'Varaa →')"
    - "Price shows hinta_kuvaus when present, falls back to hintateksti(), falls back to 'Lisätään pian'"
    - "TypeScript compiles with no errors after changes"
  artifacts:
    - path: "app/components/PaikkaKortti.tsx"
      provides: "Card with open status badge, drop-in badge, hinta_kuvaus price, corrected CTA"
      contains: "getOpenStatus"
  key_links:
    - from: "app/components/PaikkaKortti.tsx"
      to: "lib/aukiolo.ts"
      via: "import { getOpenStatus } from '@/lib/aukiolo'"
      pattern: "getOpenStatus"
    - from: "app/components/PaikkaKortti.tsx"
      to: "lib/utils.ts"
      via: "import { hintateksti } from '@/lib/utils'"
      pattern: "hintateksti"
---

<objective>
Update PaikkaKortti.tsx to display open status, drop-in badge, correct price, and fixed CTA text. This plan implements D-01, D-02, D-03, D-04, D-05, D-06, D-08 (card) from CONTEXT.md and the UI-01, UI-02, UI-03 requirements.

Purpose: Every venue card surfaces the most important service info — "is it open?" and "can I drop in?" — without any user interaction required.

Output: Modified PaikkaKortti.tsx with six targeted changes to the existing component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-service-information-ui/04-CONTEXT.md
@.planning/phases/04-service-information-ui/04-UI-SPEC.md
@.planning/phases/04-service-information-ui/04-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from source files. -->

From lib/aukiolo.ts (created in 04-01):
```typescript
export type OpenStatus =
  | { status: 'open';   hours: string }
  | { status: 'closed'; hours: string | null }
  | { status: 'no-data' }

export function getOpenStatus(
  aukioloajat: Record<string, { open: string; close: string }> | null | undefined,
  now?: Date
): OpenStatus
```

From lib/types.ts (existing):
```typescript
export type Liikuntapaikka = {
  // ... existing fields ...
  hinta_kuvaus?: string | null
  aukioloajat?: Record<string, { open: string; close: string }> | null
}
```

From lib/utils.ts (existing):
```typescript
export function hintateksti(min: number | null, max: number | null): string
// Returns '' (empty string, NOT null) when both min and max are null
```

PaikkaKortti props interface (add aukinyt to the existing props type):
```typescript
// Before (existing):
interface PaikkaKorttiProps {
  paikka: Liikuntapaikka
}

// After (add optional prop):
interface PaikkaKorttiProps {
  paikka: Liikuntapaikka
  aukinyt?: boolean   // passed by LiikuntapaikatLista when the "Auki nyt" toggle is active (D-08)
}
```

Current PaikkaKortti.tsx structure (line references from the read file):
- Line 44: outer p-4 flex flex-col gap-2.5 flex-1 div
- Lines 47-53: sport badge <span> (self-start, single badge)
- Lines 56-60: venue name <Link><h3>
- Lines 63-68: address row (MapPin + osoite)  ← hours row inserts between name and here (D-02)
- Lines 85: "Varaa →" text  ← change to "Varaa aika →"
- Lines 99-103: price/fallback block  ← update with hinta_kuvaus fallback chain
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add open status row, drop-in badge, hinta_kuvaus price, and CTA fix</name>
  <files>app/components/PaikkaKortti.tsx</files>

  <behavior>
    - Open (has data, currently within hours): green dot + "Auki nyt · HH:MM–HH:MM" below name
    - Closed (has data, outside hours): "Suljettu" in muted gray below name, no dot
    - No data (aukioloajat null/undefined or no-data), aukinyt prop false or absent: "Aukioloajat lisätään pian" in muted placeholder below name (D-03)
    - No data (aukioloajat null/undefined or no-data), aukinyt prop true: "Aukioloajat tuntematon" in muted placeholder below name (D-08)
    - Drop-in badge: "Kertakäynti OK" pill rendered inline after sport badge when hinta_kuvaus contains 'kertakäynti' (case-insensitive)
    - Price: hinta_kuvaus first, then hintateksti() if non-empty, then "Lisätään pian" placeholder
    - CTA: booking link text changed from "Varaa →" to "Varaa aika →"
  </behavior>

  <read_first>
    - app/components/PaikkaKortti.tsx (read the full current file before touching anything)
    - lib/aukiolo.ts (confirm exported names and OpenStatus type shape)
    - lib/utils.ts (confirm hintateksti returns '' not null for both-null case)
    - .planning/phases/04-service-information-ui/04-UI-SPEC.md (exact Tailwind classes, copywriting contract)
  </read_first>

  <action>
    Change 1 — Import getOpenStatus:
    Add to existing imports at top of file: import { getOpenStatus } from '@/lib/aukiolo'

    Change 2 — Add aukinyt to props and compute open status + drop-in flag in component body:
    Update the props interface to add the optional aukinyt prop (see <interfaces> block above for the before/after).
    Destructure it in the function signature: function PaikkaKortti({ paikka, aukinyt = false }: PaikkaKorttiProps)

    Inside PaikkaKortti function body, after the existing const declarations (hinta, osoite, Icon):
      const openStatus  = getOpenStatus(paikka.aukioloajat)
      const hasDropIn   = paikka.hinta_kuvaus?.toLowerCase().includes('kertakäynti') ?? false
      const hintaTeksti = hintateksti(paikka.hinta_min, paikka.hinta_max)
      const priceToShow = paikka.hinta_kuvaus || (hintaTeksti !== '' ? hintaTeksti : null)

    NOTE: Remove the existing "const hinta = hintateksti(...)" line and replace with hintaTeksti above. Update all downstream references from hinta to hintaTeksti/priceToShow.

    Change 3 — Update sport badge row to support drop-in badge (per D-05, UI-SPEC section "Drop-in Badge"):
    Change the sport badge container from:
      <span className="self-start inline-flex items-center gap-1.5 ...">
    To a wrapping flex row:
      <div className="flex items-center gap-2 flex-wrap">
        <span className="inline-flex items-center gap-1.5 text-[11px] font-semibold px-2.5 py-1 rounded-full text-white" style={{ backgroundColor: laji.color }}>
          <Icon className="w-3 h-3" />
          {laji.label}
        </span>
        {hasDropIn && (
          <span className="inline-flex items-center gap-1 text-xs font-bold px-2 py-1 rounded-full bg-[rgba(17,17,17,0.06)] text-[rgba(17,17,17,0.55)]">
            Kertakäynti OK
          </span>
        )}
      </div>

    Change 4 — Insert open status row after the venue name Link block, before the address block (D-02 position).
    The no-data branch uses the aukinyt prop to choose between two strings per D-03 and D-08:
      {openStatus.status === 'open' && (
        <div className="inline-flex items-center gap-2">
          <span className="w-1.5 h-1.5 rounded-full bg-green-500 shrink-0" />
          <span className="text-xs font-bold text-green-700">
            Auki nyt · {openStatus.hours}
          </span>
        </div>
      )}
      {openStatus.status === 'closed' && (
        <div className="inline-flex items-center gap-2">
          <span className="text-xs text-[rgba(17,17,17,0.45)]">Suljettu</span>
        </div>
      )}
      {openStatus.status === 'no-data' && (
        <span className="text-xs text-[rgba(17,17,17,0.35)]">
          {aukinyt ? 'Aukioloajat tuntematon' : 'Aukioloajat lisätään pian'}
        </span>
      )}

    Change 5 — Update price display in bottom row (D-06):
    Replace the existing hinta conditional block (currently checks `if hinta`) with:
      {priceToShow ? (
        <span className="text-sm font-bold text-[#111111] tabular-nums">{priceToShow}</span>
      ) : (
        <span className="text-xs text-[rgba(17,17,17,0.35)]">Lisätään pian</span>
      )}

    Change 6 — Fix CTA text:
    Change "Varaa →" to "Varaa aika →" on the motion.a booking link.

    IMPORTANT: The separator between "Auki nyt" and the time range is a middle dot U+00B7 surrounded by spaces: " · " (not a regular hyphen or dash). The time range uses an en-dash U+2013: "09:00–21:00".
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx vitest run lib/aukiolo.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - app/components/PaikkaKortti.tsx imports getOpenStatus from '@/lib/aukiolo'
    - app/components/PaikkaKortti.tsx props interface includes "aukinyt?: boolean"
    - app/components/PaikkaKortti.tsx function signature destructures "aukinyt = false"
    - app/components/PaikkaKortti.tsx contains the string "Auki nyt ·" (middle dot separator)
    - app/components/PaikkaKortti.tsx contains "Suljettu"
    - app/components/PaikkaKortti.tsx contains "Aukioloajat lisätään pian" (default no-data string, D-03)
    - app/components/PaikkaKortti.tsx contains "Aukioloajat tuntematon" (aukinyt-active no-data string, D-08)
    - app/components/PaikkaKortti.tsx no-data branch uses the ternary: aukinyt ? 'Aukioloajat tuntematon' : 'Aukioloajat lisätään pian'
    - app/components/PaikkaKortti.tsx contains "Kertakäynti OK"
    - app/components/PaikkaKortti.tsx contains "Varaa aika →" (not "Varaa →")
    - app/components/PaikkaKortti.tsx contains "bg-green-500" (green dot for open state)
    - app/components/PaikkaKortti.tsx contains "text-green-700" (open badge text)
    - app/components/PaikkaKortti.tsx contains "hinta_kuvaus" (price fallback chain implemented)
    - app/components/PaikkaKortti.tsx contains "flex-wrap" (drop-in badge overflow handled)
    - npx tsc --noEmit exits with code 0 (no TypeScript errors)
    - The word "Varaa →" no longer appears in the file (replaced by "Varaa aika →")
  </acceptance_criteria>

  <done>PaikkaKortti renders open status, drop-in badge, correct price and CTA. No-data row shows "Aukioloajat tuntematon" when aukinyt is true, "Aukioloajat lisätään pian" otherwise. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase DB → paikka prop | aukioloajat and hinta_kuvaus come from the DB; treated as trusted, but utility handles malformed input gracefully |
| hinta_kuvaus text → drop-in detection | String matching is read-only; no user-controlled data involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | hinta_kuvaus display | accept | hinta_kuvaus is operator-entered pricing prose from seed data; no PII; rendered as plain text (no dangerouslySetInnerHTML) |
</threat_model>

<verification>
npx tsc --noEmit — zero TypeScript errors
npx vitest run lib/aukiolo.test.ts — utility tests remain green
Visual: venue card shows green "Auki nyt · 09:00–21:00" for an open venue (manual check after dev server starts)
Visual: when aukinyt filter is active, a card with null aukioloajat shows "Aukioloajat tuntematon" not "Aukioloajat lisätään pian"
</verification>

<success_criteria>
PaikkaKortti.tsx displays open status badge below venue name (D-01, D-02), correct placeholder when no data depending on aukinyt prop (D-03, D-08), drop-in badge in top row when applicable (D-04, D-05), hinta_kuvaus price with hintateksti fallback (D-06), and corrected "Varaa aika →" CTA text. TypeScript clean.
</success_criteria>

<output>
Create `.planning/phases/04-service-information-ui/04-02-SUMMARY.md` when done.
</output>
