---
phase: 46-pre-vaihe-ui-velhointegraatio
plan: 03
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - app/business/WizardInner.tsx
  - app/business/onboarding/StepHinnasto.tsx
  - app/business/onboarding/StepAukioloajat.tsx
  - app/business/onboarding/StepYhteystiedot.tsx
autonomous: true
requirements:
  - ONBOARD-11
  - ONBOARD-12
  - ONBOARD-10

must_haves:
  truths:
    - "WizardInner's OnboardingMode accepts optional brandingData prop and passes price rows to StepHinnasto when brandingData.raw_analysis.prices is non-empty"
    - "WizardInner passes brandingData's opening_hours to StepAukioloajat when available"
    - "WizardInner passes brandingData's website_url to StepYhteystiedot when available"
    - "StepHinnasto uses branding prices as initial rows only when no draft hinnasto exists (draft takes priority)"
    - "StepAukioloajat uses branding hours as initialDraftAukioloajat only when no draft aukioloajat exists"
    - "StepYhteystiedot uses branding website_url to pre-fill website field only when no draft website exists"
    - "WizardInner passes brandingData to StepEsikatselu as the brandingData prop"
  artifacts:
    - path: "app/business/WizardInner.tsx"
      provides: "brandingData prop on OnboardingMode + pass-through to step components"
      contains: "brandingData"
    - path: "app/business/onboarding/StepHinnasto.tsx"
      provides: "initialBrandingHinnasto prop for branding-sourced price pre-fill"
      contains: "initialBrandingHinnasto"
    - path: "app/business/onboarding/StepAukioloajat.tsx"
      provides: "initialBrandingAukioloajat prop for branding-sourced hours pre-fill"
      contains: "initialBrandingAukioloajat"
    - path: "app/business/onboarding/StepYhteystiedot.tsx"
      provides: "initialBrandingWebsite prop for branding-sourced website pre-fill"
      contains: "initialBrandingWebsite"
  key_links:
    - from: "app/business/WizardInner.tsx"
      to: "app/business/onboarding/StepHinnasto.tsx"
      via: "initialBrandingHinnasto prop"
      pattern: "initialBrandingHinnasto"
    - from: "app/business/WizardInner.tsx"
      to: "app/business/onboarding/StepEsikatselu.tsx"
      via: "brandingData prop"
      pattern: "brandingData"
---

<objective>
Wire brandingData through WizardInner's OnboardingMode into steps 3, 4, 5, and 6 so that wizard fields are pre-filled from analysis results.

Purpose: Delivers ONBOARD-11 (hinnasto pre-fill), ONBOARD-12 (aukioloajat pre-fill), ONBOARD-10 (website pre-fill), and completes the data flow from AnalysoiSivusto through to StepEsikatselu. Step components get new props for branding-sourced defaults; draft data always takes priority over branding data.

Output: Modified WizardInner.tsx, StepHinnasto.tsx, StepAukioloajat.tsx, StepYhteystiedot.tsx.

Can run in parallel with 46-02 and 46-04 (no file overlap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-pre-vaihe-ui-velhointegraatio/46-01-SUMMARY.md

<interfaces>
From lib/branding/brandingResult.ts (created in 46-01):

export type BrandingResult = {
  status: 'pending' | 'analyzing' | 'analyzed' | 'failed'
  logo_url: string | null
  logo_type: string | null
  colors: string[] | null
  raw_analysis: {
    prices: Array<{ label: string; price: string }>
    opening_hours: Array<{ day: string; open: string; close: string }>
    website_url: string
  } | null
  error_message: string | null
}

Current WizardInner.tsx OnboardingMode step rendering:
- step 3: <StepHinnasto paikkaId={paikkaId} initialHinnasto={draft?.hinnasto} onNext/onPrev />
- step 4: <StepAukioloajat paikkaId={paikkaId} existingAukioloajat={paikkaInfo?.aukioloajat} initialDraftAukioloajat={draft?.aukioloajat} onNext/onPrev />
- step 5: <StepYhteystiedot paikkaId={paikkaId} initialYhteystiedot={draft?.yhteystiedot} onNext/onPrev />
- step 6: <StepEsikatselu draft={draft} paikkaInfo={paikkaInfo} onPrev />

Current StepHinnasto Props:
  paikkaId: number
  onNext: () => void
  onPrev: () => void
  initialHinnasto?: Array<{ kategoria: string; hinta: string; lisatieto?: string }> | null
  initialPaikkaHinnasto?: Array<{ kategoria: string; hinta: string; lisatieto?: string }> | null
  editMode?: boolean
  onSaveSuccess?: () => void
  onSaveComplete?: (rows) => void

StepHinnasto init logic:
  const source = editMode ? (initialPaikkaHinnasto ?? initialHinnasto) : initialHinnasto
  If source && source.length > 0 → populate rows from source
  Otherwise → 4 fixed placeholder rows

Current StepAukioloajat Props:
  paikkaId: number
  existingAukioloajat: Record<string, { open: string; close: string }> | null | undefined
  initialDraftAukioloajat?: Record<string, { open: string; close: string }> | null
  onNext/onPrev/editMode/onSaveSuccess/onSaveComplete

StepAukioloajat init logic (useEffect):
  const source = initialDraftAukioloajat ?? existingAukioloajat
  If source has keys → fill hours from source

Current StepYhteystiedot Props:
  paikkaId: number
  onNext/onPrev
  initialYhteystiedot?: { puhelin?, email?, website?, kuvaus? } | null
  editMode?/onSaveSuccess?/onSaveComplete?

StepYhteystiedot init:
  useState(initialYhteystiedot?.website ?? '')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new pre-fill props to StepHinnasto, StepAukioloajat, StepYhteystiedot</name>
  <files>
    app/business/onboarding/StepHinnasto.tsx
    app/business/onboarding/StepAukioloajat.tsx
    app/business/onboarding/StepYhteystiedot.tsx
  </files>
  <action>
Add one new optional prop to each of the three step components. These props carry branding-sourced defaults that are used ONLY when the draft does not already have data for that field.

StepHinnasto.tsx:
- Add optional prop initialBrandingHinnasto?: Array<{ label: string; price: string }> | null to Props type.
- Destructure it in the function signature.
- In the rows useState initializer, modify the source resolution (currently: `const source = editMode ? (initialPaikkaHinnasto ?? initialHinnasto) : initialHinnasto`):
  In onboarding mode (editMode=false), the priority chain is: initialHinnasto (draft) → initialBrandingHinnasto (branding) → 4 fixed rows.
  Change the onboarding branch to:
    const draftSource = initialHinnasto   // draft data
    const brandSource = initialBrandingHinnasto   // branding data
    const source = draftSource?.length ? draftSource
      : brandSource?.length
        ? brandSource.map(b => ({ kategoria: b.label, hinta: b.price, lisatieto: '' }))
        : null
  Then the existing `if (source && source.length > 0)` block maps source rows as before.
  In edit mode (editMode=true), brandingHinnasto is NOT used — edit mode keeps its existing logic unchanged.

StepAukioloajat.tsx:
- Add optional prop initialBrandingAukioloajat?: Record<string, { open: string; close: string }> | null to Props type.
- Destructure it.
- In the useEffect that pre-fills hours, the current priority is `initialDraftAukioloajat ?? existingAukioloajat`.
  Change to: `initialDraftAukioloajat ?? initialBrandingAukioloajat ?? existingAukioloajat`.
  The effect dependency array must include initialBrandingAukioloajat.
  Logic after that is identical — the filled state comes from source using ORDERED_DAYS iteration.
  In edit mode, this prop is simply not passed by the edit-mode call site (WizardInner EditMode), so it stays undefined and the priority chain is the same as before.

StepYhteystiedot.tsx:
- Add optional prop initialBrandingWebsite?: string | null to Props interface.
- Destructure it.
- Change the website useState initializer from:
    useState(initialYhteystiedot?.website ?? '')
  to:
    useState(initialYhteystiedot?.website ?? initialBrandingWebsite ?? '')
  This means: draft website wins, then branding website, then empty string.
  No other state or logic changes.
  </action>
  <verify>
    <automated>cd /c/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | grep -E "StepHinnasto|StepAukioloajat|StepYhteystiedot" || echo "No step type errors"</automated>
  </verify>
  <done>
    All three step components have their new optional branding props. TypeScript reports no errors. Existing edit-mode call sites in WizardInner (EditMode) are unaffected — they do not pass the new props, which default to undefined, leaving behavior unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add brandingData prop to WizardInner's OnboardingMode and pass through to steps 3–6</name>
  <files>app/business/WizardInner.tsx</files>
  <action>
Modify WizardInner.tsx to accept and thread brandingData through OnboardingMode.

1. Add to imports:
   import { type BrandingResult } from '@/lib/branding/brandingResult'

2. Modify WizardInnerProps union type. The 'onboarding' mode variant must accept an optional brandingData prop:
   type WizardInnerProps =
     | { mode: 'onboarding'; brandingData?: BrandingResult | null }
     | { mode: 'edit'; paikka: Liikuntapaikka; paikkaId: number }

3. In the OnboardingMode function signature, add brandingData?: BrandingResult | null as a parameter (destructured from props). The OnboardingMode function is currently declared as `function OnboardingMode()` with no params and reads from the WizardInnerProps via a parent closure — but actually looking at the code structure: OnboardingMode is a local function, not receiving props directly. WizardInner at the bottom calls `<OnboardingMode />`. Therefore:
   - Change OnboardingMode to accept props: `function OnboardingMode({ brandingData }: { brandingData?: BrandingResult | null })`.
   - In WizardInner's export, pass it down: `if (props.mode === 'onboarding') return <OnboardingMode brandingData={props.brandingData} />`.

4. Inside OnboardingMode, build the branding-derived pre-fill values using brandingData (only when brandingData?.status === 'analyzed'):
   const brandingPrices = brandingData?.status === 'analyzed' ? (brandingData.raw_analysis?.prices ?? null) : null
   const brandingHours = brandingData?.status === 'analyzed' ? (() => {
     const hrs = brandingData.raw_analysis?.opening_hours
     if (!hrs?.length) return null
     const result: Record<string, { open: string; close: string }> = {}
     for (const h of hrs) { result[h.day] = { open: h.open, close: h.close } }
     return result
   })() : null
   const brandingWebsite = brandingData?.status === 'analyzed' ? (brandingData.raw_analysis?.website_url || null) : null

5. Pass them to the step renders in OnboardingMode JSX:
   - step 3 (StepHinnasto): add initialBrandingHinnasto={brandingPrices}
   - step 4 (StepAukioloajat): add initialBrandingAukioloajat={brandingHours}
   - step 5 (StepYhteystiedot): add initialBrandingWebsite={brandingWebsite}
   - step 6 (StepEsikatselu): add brandingData={brandingData}

6. EditMode is unchanged. The exported WizardInner function now passes brandingData from props.onboarding branch only.

IMPORTANT: The WizardInner component is currently used from onboarding/page.tsx as `<WizardInner mode="onboarding" />` without brandingData. After this change that still compiles because brandingData is optional. The 46-05 plan will update page.tsx to pass brandingData.
  </action>
  <verify>
    <automated>cd /c/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | grep WizardInner || echo "No WizardInner type errors"</automated>
  </verify>
  <done>
    WizardInner compiles cleanly with the new brandingData prop. OnboardingMode receives brandingData and passes initialBrandingHinnasto to step 3, initialBrandingAukioloajat to step 4, initialBrandingWebsite to step 5, and brandingData to step 6. Existing call site in page.tsx (no brandingData prop) still compiles without change.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brandingData props → form field initial values | Branding API response values pre-fill wizard form fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03-01 | Tampering | Branding pre-fill overwriting user edits | mitigate | Draft data always takes priority over branding data in every priority chain; user can edit all pre-filled fields |
| T-46-03-02 | Information Disclosure | website_url from branding in StepYhteystiedot | accept | URL is the business's own website as entered by the user and confirmed by analysis; no PII risk |
</threat_model>

<verification>
1. WizardInner.tsx imports BrandingResult from '@/lib/branding/brandingResult'
2. WizardInner OnboardingMode passes brandingData to StepEsikatselu
3. WizardInner OnboardingMode passes initialBrandingHinnasto to StepHinnasto
4. WizardInner OnboardingMode passes initialBrandingAukioloajat to StepAukioloajat
5. WizardInner OnboardingMode passes initialBrandingWebsite to StepYhteystiedot
6. npx tsc --noEmit reports no errors across all four modified files
</verification>

<success_criteria>
All four files compile cleanly. No existing call sites are broken. When brandingData is provided with status='analyzed' and non-null raw_analysis, wizard steps 3, 4, and 5 will receive branding-sourced initial values that they use when no draft data is present. Step 6 receives brandingData for the preview.
</success_criteria>

<output>
Create .planning/phases/46-pre-vaihe-ui-velhointegraatio/46-03-SUMMARY.md when done
</output>
