---
phase: 46-pre-vaihe-ui-velhointegraatio
plan: 02
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - app/components/DiagonaalKortti.tsx
  - app/business/onboarding/StepEsikatselu.tsx
autonomous: true
requirements:
  - PREV-01
  - ONBOARD-13

must_haves:
  truths:
    - "DiagonaalKortti accepts optional brandColor prop; when provided the left panel uses that hex as backgroundColor and text color is derived via getContrastColor"
    - "DiagonaalKortti renders with no visual change when brandColor is undefined (fallback to existing laji.color behavior)"
    - "StepEsikatselu accepts optional brandingData prop of type BrandingResult | null"
    - "StepEsikatselu calls buildBrandingPreview when brandingData is non-null and passes the result to PaikkaKortti, DiagonaalKortti, and PaikkaSheet"
    - "StepEsikatselu passes brandColor=brandingData.colors[0] to DiagonaalKortti when available"
    - "StepEsikatselu falls back to buildDraftAsPaikka when brandingData is null (existing behavior unchanged)"
  artifacts:
    - path: "app/components/DiagonaalKortti.tsx"
      provides: "brandColor prop support on left panel"
      contains: "brandColor"
    - path: "app/business/onboarding/StepEsikatselu.tsx"
      provides: "brandingData prop + buildBrandingPreview integration"
      contains: "buildBrandingPreview"
  key_links:
    - from: "app/business/onboarding/StepEsikatselu.tsx"
      to: "lib/branding/brandingResult.ts"
      via: "import buildBrandingPreview, BrandingResult"
      pattern: "buildBrandingPreview"
    - from: "app/components/DiagonaalKortti.tsx"
      to: "lib/branding/brandingResult.ts"
      via: "import getContrastColor"
      pattern: "getContrastColor"
---

<objective>
Add brandColor support to DiagonaalKortti and wire brandingData into StepEsikatselu so the step 6 preview renders with brand colors and logo.

Purpose: Delivers PREV-01 (brand preview in step 6) and ONBOARD-13 (brändivärit step 6). DiagonaalKortti's left panel backgroundColor becomes the brand primary color; text color adapts via YIQ. StepEsikatselu uses buildBrandingPreview when brandingData is available.

Output: Modified DiagonaalKortti.tsx and StepEsikatselu.tsx.

Can run in parallel with 46-03 and 46-04 (no file overlap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-pre-vaihe-ui-velhointegraatio/46-01-SUMMARY.md

<interfaces>
From lib/branding/brandingResult.ts (created in 46-01):

export type BrandingResult = {
  status: 'pending' | 'analyzing' | 'analyzed' | 'failed'
  logo_url: string | null
  logo_type: string | null
  colors: string[] | null
  raw_analysis: {
    prices: Array<{ label: string; price: string }>
    opening_hours: Array<{ day: string; open: string; close: string }>
    website_url: string
  } | null
  error_message: string | null
}

export function getContrastColor(hex: string): '#000000' | '#ffffff'
export function buildBrandingPreview(paikkaBase: PaikkaBase, brandingResult: BrandingResult, draftPaikkaId: number): Liikuntapaikka

From lib/onboardingUtils.ts:
export function buildDraftAsPaikka(draft: OnboardingDraft, paikka: PaikkaBase): Liikuntapaikka
export type PaikkaBase = { nimi, laji, osoite, kaupunki, latitude, longitude, aukioloajat }

Current DiagonaalKortti.tsx interface:
interface DiagonaalKorttiProps {
  paikka: Liikuntapaikka
  distanceStr?: string
  isSaved?: boolean
  onShowMap?: (paikka: Liikuntapaikka) => void
  onCardClick?: () => void
  onToggleTodo?: (id: number) => void
}

Current StepEsikatselu interface:
interface StepEsikatseluProps {
  draft: OnboardingDraft | null
  paikkaInfo: PaikkaBase | null
  onPrev: () => void
}

Left panel in DiagonaalKortti.tsx:
- div with className="absolute inset-0 z-10 flex flex-col p-3 overflow-hidden"
  style={{ clipPath: 'polygon(0 0, 62% 0, 57% 100%, 0 100%)' }}
- Currently has no backgroundColor style
- Logo slot shows paikka.logo_url or Building2 icon
- Sport pill: style={{ backgroundColor: laji.color }}
- Name text: className "font-bold text-[#111111] text-sm"
- Price items: className "text-xs font-bold text-[#111111] tabular-nums bg-[rgba(0,0,0,0.05)]"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add brandColor prop to DiagonaalKortti left panel</name>
  <files>app/components/DiagonaalKortti.tsx</files>
  <action>
Modify DiagonaalKortti.tsx to accept an optional brandColor?: string prop.

1. Add brandColor?: string to DiagonaalKorttiProps interface.
2. Destructure brandColor from the props in the function signature.
3. Import getContrastColor from '@/lib/branding/brandingResult'.
4. Derive a contrastText variable: when brandColor is a non-empty string, call getContrastColor(brandColor); otherwise undefined.
5. Apply to the left panel div (the one with clipPath polygon and ref={leftPanelRef}):
   - Add style prop that merges the existing clipPath with backgroundColor: when brandColor is truthy, use brandColor; otherwise leave backgroundColor unset (undefined). Use spread or a computed style object.
6. Apply contrastText to the venue name paragraph (currently "font-bold text-[#111111] text-sm leading-snug line-clamp-1 overflow-hidden") — when contrastText is set, apply it as inline color style.
7. Apply contrastText to price item spans (the "text-xs font-bold text-[#111111] tabular-nums" ones inside the marquee and the flex-wrap fallback) — when contrastText is set, apply as inline color style. The bg-[rgba(0,0,0,0.05)] background still applies via className; only the text color changes.
8. The sport pill (style={{ backgroundColor: laji.color }}) stays unchanged — it always uses laji.color. The pill text is always text-white, which is correct.
9. The open/closed status text and distance text in the bottom row do NOT change — they stay at their existing muted opacity colors.
10. When brandColor is undefined (all existing call sites), behavior is 100% identical to before — no regression. The left panel has no backgroundColor, contrastText is undefined, inline color styles are not applied.

Do NOT change the right panel (venue photo / Camera fallback). Do NOT change the card-level glass class. Do NOT change the sport pill backgroundColor.
  </action>
  <verify>
    <automated>cd /c/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | grep DiagonaalKortti || echo "No DiagonaalKortti type errors"</automated>
  </verify>
  <done>
    DiagonaalKortti accepts brandColor prop. When passed a dark hex (e.g. '#1a1a2e'), left panel has that backgroundColor and name/price text is white. When brandColor is undefined, component renders exactly as before. TypeScript reports no errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add brandingData prop to StepEsikatselu and wire buildBrandingPreview</name>
  <files>app/business/onboarding/StepEsikatselu.tsx</files>
  <action>
Modify StepEsikatselu.tsx to accept an optional brandingData?: BrandingResult | null prop and use it when building the preview object.

1. Add to imports:
   - import { type BrandingResult, buildBrandingPreview } from '@/lib/branding/brandingResult'
   Keep existing: import { buildDraftAsPaikka, type OnboardingDraft, type PaikkaBase } from '@/lib/onboardingUtils'

2. Add brandingData?: BrandingResult | null to StepEsikatseluProps interface.

3. Destructure brandingData from props.

4. Replace the existing draftAsPaikka derivation:
   Current:
     const draftAsPaikka = draft && paikkaInfo ? buildDraftAsPaikka(draft, paikkaInfo) : null

   New logic (per D-ES-01, D-ES-02):
   - If brandingData is non-null AND paikkaInfo is non-null AND draft?.paikka_id is a number:
       use buildBrandingPreview(paikkaInfo, brandingData, draft.paikka_id)
   - Otherwise (brandingData is null/undefined, or paikkaInfo is null, or no paikka_id):
       fall back to buildDraftAsPaikka(draft, paikkaInfo) when both are non-null, else null
   This is an either/or: when brandingData is truthy, always use buildBrandingPreview. The draft is still needed for paikka_id — if draft is null, draftAsPaikka is null in both branches.

5. Extract brandColor for the DiagonaalKortti call:
   const brandColor = brandingData?.colors?.[0] ?? undefined

6. In the JSX where DiagonaalKortti is rendered, pass brandColor={brandColor} as a prop (per D-BR-02).

7. PaikkaKortti and PaikkaSheet continue to receive draftAsPaikka as paikka — they do not get brandColor (they use logo_url from the paikka object which buildBrandingPreview already sets).

No other changes to the component logic — the loading states, submit handler, and footer are unchanged.
  </action>
  <verify>
    <automated>cd /c/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | grep StepEsikatselu || echo "No StepEsikatselu type errors"</automated>
  </verify>
  <done>
    StepEsikatselu accepts brandingData prop. When brandingData is non-null, buildBrandingPreview is used and DiagonaalKortti receives brandColor={brandingData.colors[0]}. When brandingData is null/undefined, existing buildDraftAsPaikka behavior is unchanged. TypeScript reports no errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brandColor prop → CSS backgroundColor | Hex string from API response is used as inline style value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02-01 | Tampering | brandColor CSS injection | accept | Value is used only as inline style backgroundColor — CSS injection via a hex value has no XSS vector; React's style prop serializes values safely |
| T-46-02-02 | Information Disclosure | logo_url in preview | accept | Logo is the business's own uploaded asset from Supabase Storage; no third-party PII |
</threat_model>

<verification>
1. DiagonaalKortti.tsx has brandColor in its Props interface
2. DiagonaalKortti.tsx imports getContrastColor from '@/lib/branding/brandingResult'
3. StepEsikatselu.tsx has brandingData in its Props interface
4. StepEsikatselu.tsx imports buildBrandingPreview from '@/lib/branding/brandingResult'
5. StepEsikatselu.tsx passes brandColor={brandColor} to DiagonaalKortti
6. npx tsc --noEmit reports no errors
</verification>

<success_criteria>
Both components compile cleanly. DiagonaalKortti renders brand-colored left panel when brandColor is supplied and is visually identical to before when it is not. StepEsikatselu uses buildBrandingPreview path when brandingData is provided and the legacy buildDraftAsPaikka path otherwise.
</success_criteria>

<output>
Create .planning/phases/46-pre-vaihe-ui-velhointegraatio/46-02-SUMMARY.md when done
</output>
