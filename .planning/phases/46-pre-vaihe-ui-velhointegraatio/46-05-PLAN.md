---
phase: 46-pre-vaihe-ui-velhointegraatio
plan: 05
type: execute
wave: 3
depends_on:
  - 46-02
  - 46-03
  - 46-04
files_modified:
  - app/business/onboarding/page.tsx
  - app/business/WizardInner.tsx
autonomous: true
requirements:
  - ONBOARD-08
  - ONBOARD-09
  - ONBOARD-13
  - PREV-01

must_haves:
  truths:
    - "app/business/onboarding/page.tsx is a 'use client' component"
    - "Page renders AnalysoiSivusto when phase state is 'pre'"
    - "Page renders WizardInner with brandingData prop when phase state is 'wizard'"
    - "Clicking Ohita in AnalysoiSivusto transitions to 'wizard' with brandingData=null"
    - "Clicking Jatka velhoon in AnalysoiSivusto transitions to 'wizard' with the confirmed BrandingResult"
    - "URL stays /business/onboarding throughout (no navigation, only state change)"
    - "Page wraps WizardInner in Suspense with the same fallback spinner as before"
  artifacts:
    - path: "app/business/onboarding/page.tsx"
      provides: "Phase-state controller for pre-vaihe + wizard"
      contains: "AnalysoiSivusto"
  key_links:
    - from: "app/business/onboarding/page.tsx"
      to: "app/business/onboarding/AnalysoiSivusto.tsx"
      via: "import and render AnalysoiSivusto"
      pattern: "AnalysoiSivusto"
    - from: "app/business/onboarding/page.tsx"
      to: "app/business/WizardInner.tsx"
      via: "render WizardInner with brandingData prop"
      pattern: "brandingData"
---

<objective>
Refactor onboarding/page.tsx from a server component thin wrapper into a 'use client' controller that manages the pre-vaihe / wizard phase transition and owns the brandingData state.

Purpose: This is the final wiring plan. After 46-02, 46-03, and 46-04 deliver their pieces, this plan connects them: page.tsx renders AnalysoiSivusto first, captures the confirmed BrandingResult, then renders WizardInner with that data. The URL stays /business/onboarding throughout (per D-PA-01).

Output: Rewritten app/business/onboarding/page.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-pre-vaihe-ui-velhointegraatio/46-02-SUMMARY.md
@.planning/phases/46-pre-vaihe-ui-velhointegraatio/46-03-SUMMARY.md
@.planning/phases/46-pre-vaihe-ui-velhointegraatio/46-04-SUMMARY.md

<interfaces>
Current app/business/onboarding/page.tsx (thin server wrapper):
  import { Suspense } from 'react'
  import WizardInner from '../WizardInner'

  export default function OnboardingWizardPage() {
    return (
      <Suspense fallback={<spinner />}>
        <WizardInner mode="onboarding" />
      </Suspense>
    )
  }

After 46-03, WizardInner accepts:
  { mode: 'onboarding'; brandingData?: BrandingResult | null }

After 46-04, AnalysoiSivusto props:
  onConfirm: (brandingData: BrandingResult) => void
  onSkip: () => void

From lib/branding/brandingResult.ts (46-01):
  export type BrandingResult = { status, logo_url, logo_type, colors, raw_analysis, error_message }

Design system (spinner fallback — keep identical to current):
  <div className="min-h-screen bg-white flex items-center justify-center">
    <div className="w-6 h-6 rounded-full border-2 border-[rgba(17,17,17,0.12)] border-t-[#111111] animate-spin" />
  </div>

Note on layout.tsx: The /business/onboarding route is protected by layout.tsx (RSC guard). That file should NOT be modified — it handles auth. page.tsx only needs to manage UI state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite onboarding/page.tsx as a 'use client' phase controller</name>
  <files>app/business/onboarding/page.tsx</files>
  <action>
Completely rewrite app/business/onboarding/page.tsx. The new file replaces the server component with a client component that owns the pre-vaihe / wizard phase state.

Structure:

1. Add 'use client' at the top.

2. Imports:
   import { useState, Suspense } from 'react'
   import WizardInner from '../WizardInner'
   import AnalysoiSivusto from './AnalysoiSivusto'
   import { type BrandingResult } from '@/lib/branding/brandingResult'

3. State:
   type PagePhase = 'pre' | 'wizard'
   const [pagePhase, setPagePhase] = useState<PagePhase>('pre')
   const [brandingData, setBrandingData] = useState<BrandingResult | null>(null)

4. Callbacks:
   function handleConfirm(result: BrandingResult) {
     setBrandingData(result)
     setPagePhase('wizard')
   }
   function handleSkip() {
     setBrandingData(null)
     setPagePhase('wizard')
   }

5. Rendering:
   return (
     <main className="min-h-screen bg-white flex flex-col items-center px-4 py-12">
       <div className="w-full max-w-xl">
         {pagePhase === 'pre' && (
           <AnalysoiSivusto onConfirm={handleConfirm} onSkip={handleSkip} />
         )}
         {pagePhase === 'wizard' && (
           <Suspense fallback={
             <div className="min-h-screen bg-white flex items-center justify-center">
               <div className="w-6 h-6 rounded-full border-2 border-[rgba(17,17,17,0.12)] border-t-[#111111] animate-spin" />
             </div>
           }>
             <WizardInner mode="onboarding" brandingData={brandingData} />
           </Suspense>
         )}
       </div>
     </main>
   )

6. Export the function as default with the name OnboardingWizardPage (keeps the same export name as before so Next.js routing is unaffected).

Notes:
- The outer main + max-w-xl wrapper was previously inside OnboardingMode's return. Now page.tsx owns the layout wrapper. To avoid double-wrapping, OnboardingMode in WizardInner.tsx currently returns its own `<main className="min-h-screen bg-white ...">` wrapper. After this change, OnboardingMode's outer main wrapper will result in a nested main. To fix this: in WizardInner.tsx OnboardingMode, change the outer element from `<main className="min-h-screen bg-white flex flex-col items-center px-4 py-12">` to a React Fragment `<>` — OnboardingMode's content div `<div className="w-full max-w-xl">` becomes the new outer element. This keeps the DOM semantics clean (only one main element). Make this change in WizardInner.tsx as part of this task.

IMPORTANT: The `<Suspense>` wrapper is needed for WizardInner because it uses `useSearchParams()` which requires Suspense in Next.js App Router. Do not remove it.
  </action>
  <verify>
    <automated>cd /c/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | grep -E "page|WizardInner|AnalysoiSivusto" | head -20 || echo "No type errors in page/wizard/analysis"</automated>
  </verify>
  <done>
    app/business/onboarding/page.tsx is a 'use client' component that renders AnalysoiSivusto in 'pre' phase and WizardInner (with brandingData) in 'wizard' phase. TypeScript compilation reports no errors. The file no longer has a server-only import pattern. WizardInner's OnboardingMode no longer has a double-wrapping main element.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 46 integration is complete. page.tsx is a client component that shows AnalysoiSivusto first, then transitions to WizardInner with brandingData when user confirms or skips. DiagonaalKortti renders brand colors. Steps 3-5 pre-fill from analysis. Step 6 preview uses branding data.
  </what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Navigate to http://localhost:3000/business/onboarding (log in as a business user first if needed)
    3. Verify you see the "Analysoi sivustosi" screen before any wizard steps
    4. Test the Ohita button — it should skip directly to step 1 of the wizard
    5. Test URL submission with a real URL (e.g. https://www.example.com) — you should see "Analysoidaan sivustoasi..." with a spinner and the Ohita button
    6. After analysis completes (or fails), verify appropriate state is shown (preview or error message)
    7. If analysis succeeds: click "Jatka velhoon →" and verify wizard step 3 has pre-filled prices if any were found
    8. Navigate to step 6 (Esikatselu) and verify DiagonaalKortti shows brand colors if analysis extracted them
    9. Run: npm run build — verify no build errors
  </how-to-verify>
  <resume-signal>Type "approved" if all flows work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 'pre' → 'wizard' state transition | brandingData flows from AnalysoiSivusto result into WizardInner |
| page.tsx → WizardInner | brandingData state passed as prop crosses component boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-05-01 | Elevation of Privilege | page.tsx as client component | accept | Auth guard is in layout.tsx (RSC); converting page.tsx to 'use client' does not bypass the RSC guard which runs first in the Next.js rendering pipeline |
| T-46-05-02 | Tampering | brandingData prop passed to WizardInner | accept | brandingData is read-only in WizardInner; it only pre-fills form fields that the user can override; no server mutations happen from this data directly |
</threat_model>

<verification>
1. app/business/onboarding/page.tsx starts with 'use client'
2. File imports AnalysoiSivusto from './AnalysoiSivusto'
3. File imports WizardInner from '../WizardInner'
4. File imports BrandingResult from '@/lib/branding/brandingResult'
5. useState for pagePhase ('pre' | 'wizard') is present
6. useState for brandingData (BrandingResult | null) is present
7. WizardInner receives brandingData={brandingData} prop
8. AnalysoiSivusto receives onConfirm={handleConfirm} onSkip={handleSkip}
9. npx tsc --noEmit reports no errors
10. npm run build completes without errors (verified in human checkpoint)
</verification>

<success_criteria>
The full Phase 46 feature is live: a business user visiting /business/onboarding sees the "Analysoi sivustosi" screen first, can submit a URL or skip, and lands in the wizard with branding data pre-filled. The entire flow is a single URL with no navigation. Build succeeds. All five plans' artifacts work together end-to-end.
</success_criteria>

<output>
Create .planning/phases/46-pre-vaihe-ui-velhointegraatio/46-05-SUMMARY.md when done
</output>
