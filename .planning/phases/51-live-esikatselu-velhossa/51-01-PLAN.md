---
phase: 51-live-esikatselu-velhossa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/livePreview/LivePreviewContext.tsx
  - lib/livePreview/useDebouncedPreviewField.ts
autonomous: true
requirements: [LIVEPREV-01, LIVEPREV-04]
must_haves:
  truths:
    - "A shared live-preview context exposes the current in-progress draft as a Liikuntapaikka-shaped object plus brandColor/accentColor"
    - "Dispatching a partial field update (hinnasto, aukioloajat, yhteystiedot, media_urls) into the context updates the derived preview object without a Supabase round-trip"
    - "A debounce helper exists that delays a value with cleanup-on-unmount and re-fires on dependency change"
  artifacts:
    - path: "lib/livePreview/LivePreviewContext.tsx"
      provides: "LivePreviewProvider, useLivePreview hook, reducer, derived livePreviewPaikka/brandColor/accentColor"
      exports: ["LivePreviewProvider", "useLivePreview"]
      min_lines: 90
    - path: "lib/livePreview/useDebouncedPreviewField.ts"
      provides: "useDebouncedValue hook with setTimeout + cleanup"
      exports: ["useDebouncedValue"]
      min_lines: 15
  key_links:
    - from: "lib/livePreview/LivePreviewContext.tsx"
      to: "lib/onboardingUtils.ts buildDraftAsPaikka"
      via: "import + call on every state change"
      pattern: "buildDraftAsPaikka"
    - from: "lib/livePreview/LivePreviewContext.tsx"
      to: "lib/branding/brandingResult.ts buildBrandingPreview"
      via: "import + call when brandingData present"
      pattern: "buildBrandingPreview"
---

<objective>
Create the shared live-preview state mechanism: a React Context + reducer (the first in `app/`/`lib/`) that accumulates in-progress wizard field values and derives a `Liikuntapaikka`-shaped preview object plus `brandColor`/`accentColor`, by calling the existing `buildDraftAsPaikka`/`buildBrandingPreview` builders unchanged. Also create the optional debounce helper used by free-text fields.

Purpose: LIVEPREV-01 (each step updates a shared live-preview state on field change) and the data half of LIVEPREV-04 (preview renders from current unsaved values). This is the contract Plans 02/03/04 consume.
Output: `lib/livePreview/LivePreviewContext.tsx`, `lib/livePreview/useDebouncedPreviewField.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-live-esikatselu-velhossa/51-CONTEXT.md
@.planning/phases/51-live-esikatselu-velhossa/51-PATTERNS.md
@lib/onboardingUtils.ts
@lib/branding/brandingResult.ts
@app/business/onboarding/StepEsikatselu.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create LivePreviewProvider + useLivePreview context with reducer and derived preview object</name>
  <files>lib/livePreview/LivePreviewContext.tsx</files>
  <read_first>
    - lib/onboardingUtils.ts (OnboardingDraft, PaikkaBase types; buildDraftAsPaikka field mapping — the reducer state shape MUST mirror buildDraftAsPaikka's `draft` input: media_urls, hinnasto, aukioloajat, yhteystiedot)
    - lib/branding/brandingResult.ts (BrandingResult type; buildBrandingPreview signature `(paikkaBase, brandingResult, draftPaikkaId, selectedLogoUrl?)`; the brandColor/accentColor derivation is NOT here — it lives in StepEsikatselu)
    - app/business/onboarding/StepEsikatselu.tsx (lines 37-61 — the exact draftAsPaikka selection logic and brandColor/accentColor derivation to LIFT into the provider verbatim)
    - .planning/phases/51-live-esikatselu-velhossa/51-PATTERNS.md (provider section — composition guidance)
  </read_first>
  <action>
    Create `'use client'` module `lib/livePreview/LivePreviewContext.tsx`. Export `LivePreviewProvider` (React component) and `useLivePreview` (hook).

    Provider props: `paikkaInfo: PaikkaBase | null`, `paikkaId: number | null`, `brandingData?: BrandingResult | null`, `initialDraft?: Partial<OnboardingDraft> | null`, and `children`. (EditMode seeds via `initialDraft` built from its `local*` state in Plan 03; onboarding passes `initialDraft` from the loaded `draft`.)

    Reducer state shape MUST mirror `buildDraftAsPaikka`'s `draft` input: a `PreviewDraft` object with optional `media_urls: { logo: string | null; photos: string[] } | null`, `hinnasto`, `aukioloajat`, `yhteystiedot` (same field types as `OnboardingDraft`), and `paikka_id: number`. Initialize from `initialDraft` ?? derived from `paikkaInfo`.

    Reducer actions (discriminated union, `type` field): `'SET_HINNASTO'` (payload rows), `'SET_AUKIOLOAJAT'` (payload Record), `'SET_YHTEYSTIEDOT'` (payload partial yhteystiedot — merge with existing), `'SET_MEDIA'` (payload `{ logo?: string | null; photos?: string[] }` — merge), `'RESET'` (payload full PreviewDraft). Each action returns a new state object (immutable spread).

    Derive `livePreviewPaikka` with `useMemo` keyed on `[state, paikkaInfo, brandingData]`: replicate StepEsikatselu lines 37-46 EXACTLY — when `brandingData && paikkaInfo && typeof state.paikka_id === 'number'` call `buildBrandingPreview(paikkaInfo, brandingData, state.paikka_id, state.media_urls?.logo)`; else when `paikkaInfo` present call `buildDraftAsPaikka(state as OnboardingDraft, paikkaInfo)`; else `null`. (Cast/assemble `state` to satisfy `OnboardingDraft` — it has `paikka_id` plus the optional draft fields.)

    Derive `brandColor` and `accentColor` with the SAME logic as StepEsikatselu lines 51-61: `brandColor = brandingData?.selected_background_color ?? brandingData?.colors?.find(c => c.role === 'background')?.hex ?? undefined`; `accentColor = brandingData?.selected_accent_color ?? brandingData?.colors?.find(c => c.role === 'accent')?.hex ?? undefined`.

    Context value type: `{ livePreviewPaikka: Liikuntapaikka | null; brandColor: string | undefined; accentColor: string | undefined; dispatch: Dispatch<LivePreviewAction> }`. `useLivePreview()` reads the context via `useContext` and throws `'useLivePreview must be used within LivePreviewProvider'` if the context is null/undefined (mirrors standard provider-guard pattern).

    Do NOT add any Supabase calls, network calls, or `useEffect` data-fetching — this provider derives synchronously from props + dispatched state only (per LIVEPREV-04: current unsaved values, no DB round-trip). Implements LIVEPREV-01 and the data half of LIVEPREV-04.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -c "LivePreviewContext" | grep -q '^0$' && echo "no type errors in LivePreviewContext"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `lib/livePreview/LivePreviewContext.tsx` begins with `'use client'`
    - Source assertion: file imports `buildDraftAsPaikka` from `@/lib/onboardingUtils` and `buildBrandingPreview` from `@/lib/branding/brandingResult`
    - Source assertion: file exports both `LivePreviewProvider` and `useLivePreview`
    - Source assertion: brandColor derivation string `selected_background_color` and accentColor string `selected_accent_color` both appear
    - Behavior: `npx tsc --noEmit` reports zero errors originating in `LivePreviewContext.tsx`
  </acceptance_criteria>
  <done>The provider compiles, exposes `useLivePreview`, and produces `livePreviewPaikka`/`brandColor`/`accentColor` derived from dispatched state via the existing builder functions with no network calls.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create useDebouncedValue hook for free-text field debounce</name>
  <files>lib/livePreview/useDebouncedPreviewField.ts</files>
  <read_first>
    - app/business/onboarding/StepMediat.tsx (lines 58-65 — the useEffect-with-cleanup pattern to mirror: set in effect, clean up in returned function, re-run on dependency change)
    - lib/livePreview/LivePreviewContext.tsx (the file created in Task 1 — keep naming/style consistent)
  </read_first>
  <action>
    Create module `lib/livePreview/useDebouncedPreviewField.ts` exporting `useDebouncedValue<T>(value: T, delayMs: number = 280): T`. Internally hold a `useState` for the debounced value initialized to `value`; in a `useEffect` keyed on `[value, delayMs]` set a `setTimeout(() => setDebounced(value), delayMs)` and return a cleanup function that calls `clearTimeout` — exactly the set-in-effect / clear-in-cleanup shape used by StepMediat's `stagedPreviewUrls` effect. Default delay `280` (within D-04's 250-300ms window). No `'use client'` directive needed for a pure hook module, but it must only be imported by client components (it uses React hooks). Implements the debounce half of D-04.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -c "useDebouncedPreviewField" | grep -q '^0$' && echo "no type errors in useDebouncedPreviewField"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file exports `useDebouncedValue`
    - Source assertion: file contains both `setTimeout` and `clearTimeout`
    - Source assertion: default delay literal `280` present in the signature
    - Behavior: `npx tsc --noEmit` reports zero errors originating in `useDebouncedPreviewField.ts`
  </acceptance_criteria>
  <done>`useDebouncedValue` compiles, debounces by ~280ms default, and cleans up its timer on unmount/dependency change.</done>
</task>

</tasks>

<artifacts_produced>
This phase plan produces these new symbols:
- `lib/livePreview/LivePreviewContext.tsx` — new file
  - `LivePreviewProvider` — React component (named export)
  - `useLivePreview` — hook (named export) returning `{ livePreviewPaikka, brandColor, accentColor, dispatch }`
  - `LivePreviewAction` — reducer action union type (named export)
  - `PreviewDraft` — reducer state type (named export)
- `lib/livePreview/useDebouncedPreviewField.ts` — new file
  - `useDebouncedValue<T>(value, delayMs?)` — hook (named export)
</artifacts_produced>

<threat_model>
This phase is pure client-side UI state. The provider derives synchronously from props and locally-dispatched state — no new API route, no new DB write, no new user input crossing a trust boundary. All persistence still flows through the existing, already-validated `save-step` / `update-paikka` routes (unchanged). No new trust boundary is introduced.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | LivePreviewContext derived state | accept | Preview-only, never persisted directly; the unmodified save routes remain the sole validated write path. No mitigation required. |
| T-51-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this phase (Context/reducer/setTimeout are React/stdlib). If any install is proposed, treat as [ASSUMED] and gate. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for the two new files.
- Grep confirms both builder functions are imported and called in the provider.
- No `fetch(`, `supabase`, or `createBusinessBrowserClient` appears in `LivePreviewContext.tsx`.
</verification>

<success_criteria>
- `useLivePreview()` returns a derived `livePreviewPaikka` that reflects the latest dispatched field values.
- `brandColor`/`accentColor` match StepEsikatselu's derivation byte-for-byte in logic.
- `useDebouncedValue` is ready for the step components in Plan 04.
</success_criteria>

<output>
Create `.planning/phases/51-live-esikatselu-velhossa/51-01-SUMMARY.md` when done.
</output>
