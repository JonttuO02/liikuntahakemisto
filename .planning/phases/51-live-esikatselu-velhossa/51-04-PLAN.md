---
phase: 51-live-esikatselu-velhossa
plan: 04
type: execute
wave: 3
depends_on: ["51-01", "51-02", "51-03"]
files_modified:
  - app/business/WizardInner.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LIVEPREV-01, LIVEPREV-02, LIVEPREV-03, LIVEPREV-04]
must_haves:
  truths:
    - "A single LivePreviewProvider wraps both OnboardingMode and EditMode so each renders the live preview from in-progress field values"
    - "On lg+ viewports both modes show a fixed 360px sticky preview column beside the form; below lg they show the Muokkaa/Esikatselu toggle with full content swap"
    - "EditMode's previewOpen + PreviewModal button-and-modal flow is removed and replaced by the live toggle/pane"
    - "The view toggle resets to 'edit' whenever the step/tab changes"
    - "PreviewModal.tsx is NOT deleted — the /business dashboard still imports it"
  artifacts:
    - path: "app/business/WizardInner.tsx"
      provides: "Provider mount, desktop split + mobile toggle layout, EditMode modal removal"
      contains: "LivePreviewProvider"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected Out-of-Scope wording per D-03"
      contains: "LIVEPREV"
  key_links:
    - from: "app/business/WizardInner.tsx"
      to: "lib/livePreview/LivePreviewContext.tsx LivePreviewProvider"
      via: "wraps default export return"
      pattern: "LivePreviewProvider"
    - from: "app/business/WizardInner.tsx"
      to: "app/business/onboarding/LivePreviewPane.tsx + LivePreviewToggle.tsx"
      via: "render in both modes"
      pattern: "LivePreviewPane|LivePreviewToggle"
---

<objective>
Integrate the live preview into `WizardInner.tsx`: mount a single `LivePreviewProvider` above both `OnboardingMode` and `EditMode`, add the desktop split-view (`lg:` 360px sticky column) + mobile segmented-toggle layout to both modes, seed the provider from each mode's existing state (onboarding `draft`, EditMode `local*`), reset the toggle to `'edit'` on step/tab change (D-08), and remove EditMode's `previewOpen`/`PreviewModal`/`previewCta` flow (D-02). Also apply the D-03 REQUIREMENTS.md Out-of-Scope correction.

Purpose: closes LIVEPREV-02 (desktop side-by-side), LIVEPREV-03 (mobile toggle), and ties LIVEPREV-01/04 together end-to-end in both modes. Per D-01/D-02/D-06/D-07/D-08.
Output: modified `WizardInner.tsx`, corrected `REQUIREMENTS.md`.

CRITICAL CORRECTION TO D-02: `PreviewModal.tsx` MUST NOT be deleted. CONTEXT.md D-02 assumed EditMode is its only call site, but `app/business/page.tsx` (the /business dashboard) imports and renders `PreviewModal` at line ~331 independently. Remove ONLY the EditMode usage in WizardInner; leave `PreviewModal.tsx` and the dashboard usage intact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-live-esikatselu-velhossa/51-CONTEXT.md
@.planning/phases/51-live-esikatselu-velhossa/51-PATTERNS.md
@.planning/phases/51-live-esikatselu-velhossa/51-UI-SPEC.md
@app/business/WizardInner.tsx
@lib/livePreview/LivePreviewContext.tsx
@app/business/onboarding/LivePreviewPane.tsx
@app/business/onboarding/LivePreviewToggle.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mount LivePreviewProvider and add split/toggle layout in OnboardingMode</name>
  <files>app/business/WizardInner.tsx</files>
  <read_first>
    - app/business/WizardInner.tsx (OnboardingMode lines 34-309: the `draft`/`paikkaInfo`/`paikkaId`/`brandingData` state; the `<div className="w-full max-w-xl">` form column lines 242-306; the AnimatePresence crossfade lines 249-256; the default export lines 455-460)
    - lib/livePreview/LivePreviewContext.tsx (LivePreviewProvider props: paikkaInfo, paikkaId, brandingData, initialDraft, children; useLivePreview)
    - app/business/onboarding/LivePreviewPane.tsx (default export, no props)
    - app/business/onboarding/LivePreviewToggle.tsx (props activeView, onChange)
    - .planning/phases/51-live-esikatselu-velhossa/51-UI-SPEC.md (lines 100-149 — desktop split-view wrapper `flex gap-6 items-start justify-center`, preview column `hidden lg:flex flex-col gap-4 w-[360px] flex-shrink-0 sticky top-6`; mobile toggle above content with AnimatePresence full content swap; reset-on-step-change)
  </read_first>
  <action>
    In `OnboardingMode`'s render: wrap the existing `<div className="w-full max-w-xl">` form column in the UI-SPEC desktop split-view wrapper `<div className="flex gap-6 items-start justify-center">`. After the form column, add the desktop preview column: `<div className="hidden lg:flex flex-col gap-4 w-[360px] flex-shrink-0 sticky top-6"><LivePreviewPane /></div>` (LivePreviewPane already renders the two captioned cards; do NOT re-add captions here).

    Add `const [activeView, setActiveView] = useState<'edit' | 'preview'>('edit')`. Above the ProgressBar (mobile only), render `<div className="lg:hidden"><LivePreviewToggle activeView={activeView} onChange={setActiveView} /></div>`. Below `lg:`, when `activeView === 'preview'`, the step content area is replaced (full content swap, not overlay) by `<LivePreviewPane />` using the existing `AnimatePresence mode="wait"` opacity-only crossfade (duration 0.2) — keyed appropriately so edit↔preview swaps cleanly. The desktop `lg:` column and the form remain mounted regardless; only the sub-`lg` content area swaps.

    Add a `useEffect` keyed on `[step]` that calls `setActiveView('edit')` (D-08 reset-on-step-change), modeled on the existing step-keyed useEffect at lines 188-193.

    Provider mount: in the DEFAULT-EXPORTED `WizardInner` (lines 455-460), do NOT wrap there directly because the provider needs `paikkaInfo`/`paikkaId`/`draft` which live inside `OnboardingMode`/`EditMode`. Instead mount `LivePreviewProvider` INSIDE `OnboardingMode`'s return wrapping the split-view, passing `paikkaInfo={paikkaInfo}` `paikkaId={paikkaId}` `brandingData={brandingData}` `initialDraft={draft}`. (Task 2 mounts a second provider instance inside EditMode with EditMode's seed — two instances is acceptable per CONTEXT.md Claude's Discretion since the two modes never co-render.)

    Implements LIVEPREV-02/03 for onboarding and ties LIVEPREV-01/04 to the rendered pane.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -c "WizardInner" | grep -q '^0$' && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: WizardInner imports `LivePreviewProvider` from `@/lib/livePreview/LivePreviewContext`, and `LivePreviewPane`/`LivePreviewToggle` from `./onboarding/...`
    - Source assertion: contains the desktop column class `w-[360px] flex-shrink-0 sticky top-6`
    - Source assertion: contains `setActiveView('edit')` inside a useEffect (reset-on-step-change)
    - Source assertion: `LivePreviewProvider` wraps OnboardingMode's split-view with `initialDraft={draft}`
    - Behavior: `npx tsc --noEmit` reports zero errors in WizardInner.tsx
  </acceptance_criteria>
  <done>OnboardingMode shows a sticky 360px preview column on lg+ and a Muokkaa/Esikatselu toggle with full content swap below lg, fed by a mounted LivePreviewProvider; the toggle resets to edit on step change.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace EditMode PreviewModal with live toggle/pane; apply D-03 REQUIREMENTS correction; keep PreviewModal.tsx alive</name>
  <files>app/business/WizardInner.tsx, .planning/REQUIREMENTS.md</files>
  <read_first>
    - app/business/WizardInner.tsx (EditMode lines 312-452: previewOpen state line 316; the PreviewModal AnimatePresence block lines 348-356; the previewCta button lines 386-392; the local* seed state lines 320-331; the tab bar lines 371-385; the `<div className="flex flex-col gap-6">` form wrapper line 358)
    - .planning/REQUIREMENTS.md (the Out of Scope table; the PreviewModal/CalloutCard exclusion row to correct per D-03 — same correction pattern Phase 49 used for PREV-03)
    - app/business/page.tsx (CONFIRM line ~10 import + line ~331 render of PreviewModal — this is WHY PreviewModal.tsx must survive)
    - lib/livePreview/LivePreviewContext.tsx (provider props; the local* → initialDraft seed mapping)
  </read_first>
  <action>
    EditMode removal (D-02, scoped): delete the `previewOpen` state (line 316), the entire `<AnimatePresence>{previewOpen && <PreviewModal .../>}</AnimatePresence>` block (lines 348-356), and the `previewCta` button (lines 386-392). Remove the now-unused `PreviewModal` import from WizardInner ONLY if no other reference remains in WizardInner.tsx. DO NOT delete `app/components/PreviewModal.tsx` and DO NOT touch `app/business/page.tsx` — the dashboard still uses PreviewModal independently (verified: import at line ~10, render at line ~331).

    EditMode live preview: mount a `LivePreviewProvider` inside EditMode wrapping its content, seeded from the `local*` state — `initialDraft={{ paikka_id: paikkaId, hinnasto: localHinnasto ?? undefined, aukioloajat: localAukioloajat ?? undefined, yhteystiedot: localYhteystiedot, media_urls: { logo: localLogoUrl, photos: localPhotoUrls } }}` and `paikkaInfo={{ nimi: paikka.nimi, laji: paikka.laji, osoite: paikka.osoite, kaupunki: paikka.kaupunki, latitude: paikka.latitude, longitude: paikka.longitude, aukioloajat: paikka.aukioloajat }}`, `paikkaId={paikkaId}`, `brandingData={null}` (EditMode has no branding analysis context — colors fall back to undefined, matching today's PreviewModal which passed none). Wrap the existing `<div className="flex flex-col gap-6">` in the same `flex gap-6 items-start justify-center` split-view wrapper used in Task 1, and add the `hidden lg:flex ... w-[360px] sticky top-6` `<LivePreviewPane />` column beside it.

    Add `const [activeView, setActiveView] = useState<'edit'|'preview'>('edit')`, render `<div className="lg:hidden"><LivePreviewToggle .../></div>` above the tab bar, swap content below lg via the same AnimatePresence pattern, and add a `useEffect` keyed on `[currentStep]` resetting `setActiveView('edit')` (D-08).

    REQUIREMENTS.md (D-03): locate the Out of Scope row that excludes the `PreviewModal.tsx` → `CalloutCard` swap and reword/remove it to reflect that EditMode's live preview now renders `CalloutCard`/`DiagonaalKortti` (not PaikkaKortti), mirroring how Phase 49 corrected PREV-03's wording. If no such literal row exists in the current REQUIREMENTS.md table, add a clarifying note under the Live Preview section that EditMode preview is in-scope and renders CalloutCard/DiagonaalKortti. Do not alter unrelated rows.

    Implements LIVEPREV-02/03/04 for EditMode (criterion 5: EditMode tabs get the same live preview, replacing PreviewModal) and D-03.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -c "WizardInner" | grep -q '^0$' && test -f app/components/PreviewModal.tsx && grep -q "PreviewModal" app/business/page.tsx && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `previewOpen` no longer appears in WizardInner.tsx (grep returns 0)
    - Source assertion: WizardInner.tsx no longer renders `<PreviewModal` (grep for `<PreviewModal` returns 0)
    - Source assertion: `app/components/PreviewModal.tsx` still EXISTS on disk and `app/business/page.tsx` still contains `PreviewModal` (dashboard usage preserved)
    - Source assertion: EditMode mounts `LivePreviewProvider` with `initialDraft` seeded from `localHinnasto`/`localAukioloajat`/`localYhteystiedot`/`localLogoUrl`/`localPhotoUrls`
    - Source assertion: REQUIREMENTS.md Out-of-Scope no longer excludes the PreviewModal→CalloutCard swap (the exclusion row is removed/reworded)
    - Behavior: `npx tsc --noEmit` reports zero errors in WizardInner.tsx
  </acceptance_criteria>
  <done>EditMode shows the same live split/toggle preview as onboarding, the click-to-open PreviewModal flow is gone from EditMode, PreviewModal.tsx survives for the dashboard, and REQUIREMENTS.md reflects the corrected scope.</done>
</task>

</tasks>

<artifacts_produced>
This phase plan produces no new files. It modifies:
- `app/business/WizardInner.tsx` — mounts `LivePreviewProvider` (one instance per mode), adds desktop split-view 360px sticky `LivePreviewPane` column + mobile `LivePreviewToggle` with `activeView` state and reset-on-step/tab-change in both OnboardingMode and EditMode; removes EditMode `previewOpen`/`PreviewModal`/`previewCta`
- `.planning/REQUIREMENTS.md` — corrected Out-of-Scope wording per D-03

Explicitly NOT changed: `app/components/PreviewModal.tsx` (kept — dashboard dependency), `app/business/page.tsx` (kept).
</artifacts_produced>

<threat_model>
Client-side UI composition. No new API route or DB write. EditMode's removal of PreviewModal does not remove any validation — the preview was always read-only. The provider derives from already-loaded `paikka`/`local*` state. No new trust boundary.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-05 | Tampering | EditMode provider seed | accept | Seeds from server-fetched `paikka` (ownership already verified in `app/business/[id]/page.tsx` RSC) and local edit state; no new write path. |
| T-51-06 | Information Disclosure | dead-code deletion error | mitigate | Explicit acceptance criterion verifies `PreviewModal.tsx` and the dashboard usage survive — prevents breaking the /business dashboard preview by over-eagerly deleting per D-02's stale assumption. |
| T-51-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase. Any proposed install → [ASSUMED] + gate. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `app/components/PreviewModal.tsx` exists; `app/business/page.tsx` still references it.
- WizardInner contains no `previewOpen` and no `<PreviewModal` render.
- Both modes render `LivePreviewProvider`, `LivePreviewPane`, and `LivePreviewToggle`.
- REQUIREMENTS.md Out-of-Scope no longer excludes the EditMode PreviewModal→CalloutCard swap.
</verification>

<success_criteria>
- LIVEPREV-02: desktop shows preview beside the active step/tab in both modes.
- LIVEPREV-03: mobile toggles between form and preview in both modes.
- LIVEPREV-04 + criterion 5: EditMode renders CalloutCard/DiagonaalKortti live, replacing the click-to-open modal.
- Dashboard PreviewModal usage unbroken.
</success_criteria>

<output>
Create `.planning/phases/51-live-esikatselu-velhossa/51-04-SUMMARY.md` when done.
</output>
