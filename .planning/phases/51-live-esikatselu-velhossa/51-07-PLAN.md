---
phase: 51-live-esikatselu-velhossa
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/livePreview/LivePreviewContext.tsx
autonomous: true
gap_closure: true
requirements: [LIVEPREV-01, LIVEPREV-02, LIVEPREV-03, LIVEPREV-04]
must_haves:
  truths:
    - "When brandingData is present (AI-website-analysis onboarding path) AND the user has edited pricing on StepHinnasto, the live preview's hinta_kuvaus reflects the live state.hinnasto (serialized via hinnastaToHintaKuvaus), NOT the frozen AI-scraped raw_analysis.prices — satisfying LIVEPREV-01/04 / criterion #1/#4 for the pricing step on the branding path"
    - "When brandingData is present AND the user has edited opening hours on StepAukioloajat, the live preview's aukioloajat reflects the live state.aukioloajat, NOT the frozen AI-scraped raw_analysis.opening_hours"
    - "When brandingData is present AND the user has edited contact info on StepYhteystiedot, the live preview's puhelin/kuvaus/varauslinkki reflect state.yhteystiedot (puhelin/kuvaus/website) instead of the hardcoded nulls (puhelin/kuvaus) and AI-scraped website_url (varauslinkki)"
    - "When brandingData is present but the user has NOT yet touched a given step (state.hinnasto / state.aukioloajat / state.yhteystiedot still null), that field falls back to exactly the existing buildBrandingPreview-derived AI-scraped value (or hardcoded null), so a fresh AI-onboarding session still shows scraped data before the user edits anything — no field goes blank"
    - "buildBrandingPreview's exported signature in lib/branding/brandingResult.ts is unchanged (no new parameters) — the shared builder used by StepEsikatselu/pre-phase esikatseluruutu is untouched; the overlay happens entirely in the caller (LivePreviewContext.tsx)"
    - "The non-branding branch (buildDraftAsPaikka) and EditMode (brandingData={null}) paths are unchanged — they already thread full live state correctly"
  artifacts:
    - path: "lib/livePreview/LivePreviewContext.tsx"
      provides: "The brandingData branch of the livePreviewPaikka useMemo overlays state.hinnasto/state.aukioloajat/state.yhteystiedot onto buildBrandingPreview's returned base object before returning, with per-field fallback to the branding-derived base value when the live state field is null/empty"
      contains: "hinnastaToHintaKuvaus"
      min_lines: 180
  key_links:
    - from: "lib/livePreview/LivePreviewContext.tsx (livePreviewPaikka useMemo, branding branch)"
      to: "lib/livePreview/LivePreviewContext.tsx (state.hinnasto / state.aukioloajat / state.yhteystiedot)"
      via: "spread of buildBrandingPreview(...) base object then per-field overlay reading state.hinnasto (serialized via hinnastaToHintaKuvaus), state.aukioloajat, state.yhteystiedot with `?? base.<field>` fallback"
      pattern: "\\.\\.\\.base"
    - from: "lib/livePreview/LivePreviewContext.tsx"
      to: "lib/onboardingUtils.ts hinnastaToHintaKuvaus"
      via: "import of hinnastaToHintaKuvaus to serialize state.hinnasto into hinta_kuvaus, mirroring buildDraftAsPaikka's serialization"
      pattern: "hinnastaToHintaKuvaus"
---

<objective>
Close the third-round verification gap (CR-01 / criteria #1 and #4 partial) for Phase 51. `lib/livePreview/LivePreviewContext.tsx`'s `livePreviewPaikka` useMemo (lines 138-146) has two branches. The non-branding branch (lines 142-144) calls `buildDraftAsPaikka(state as OnboardingDraft, paikkaInfo)`, which correctly threads the full live reducer state — verified correct. The branding branch (lines 139-141) calls `buildBrandingPreview(paikkaInfo, brandingData, state.paikka_id, state.media_urls?.logo)` and returns its result directly. `buildBrandingPreview` (lib/branding/brandingResult.ts lines 117-169) derives `hinta_kuvaus`/`aukioloajat` solely from `brandingResult.raw_analysis` (the frozen AI-scraped snapshot) and hardcodes `puhelin`/`kuvaus` to `null` — it has no parameters for live `hinnasto`/`aukioloajat`/`yhteystiedot`. Result: for any venue onboarded via the AI-website-analysis flow (`WizardInner.tsx` line 255 passes the real `brandingData` to the onboarding LivePreviewProvider), editing pricing (StepHinnasto), hours (StepAukioloajat), or contact info (StepYhteystiedot) dispatches into `state` correctly but the rendered preview ignores it, staying frozen on AI-scraped/null data for the whole onboarding session. EditMode is unaffected (line 399 passes `brandingData={null}`, taking the buildDraftAsPaikka branch).

Fix: in the branding branch ONLY, capture `buildBrandingPreview(...)`'s result as a `base` object, then return a new object spreading `base` and overlaying the live state per field — `hinta_kuvaus` from `state.hinnasto` (when non-empty) via `hinnastaToHintaKuvaus`, `aukioloajat` from `state.aukioloajat` (when present), `puhelin`/`kuvaus`/`varauslinkki` from `state.yhteystiedot` (when present) — each falling back to the corresponding `base` value when the live state field is null/empty. This replicates the exact merge-with-fallback semantics `buildDraftAsPaikka` already uses for the non-branding branch (lib/onboardingUtils.ts lines 105-109), confined to the caller so `buildBrandingPreview`'s shared signature is never touched.

Purpose: completes LIVEPREV-01 and LIVEPREV-04 for the pricing/hours/contact field paths on the AI-website-analysis onboarding path — the last remaining failing path (media already works via the `selectedLogoUrl` 4th arg; EditMode and non-branding onboarding already pass).
Output: modified `lib/livePreview/LivePreviewContext.tsx` (no new files, no new exported symbols, no change to buildBrandingPreview's signature).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-live-esikatselu-velhossa/51-CONTEXT.md
@.planning/phases/51-live-esikatselu-velhossa/51-VERIFICATION.md
@.planning/phases/51-live-esikatselu-velhossa/51-REVIEW.md
@lib/livePreview/LivePreviewContext.tsx
@lib/branding/brandingResult.ts
@lib/onboardingUtils.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Overlay live hinnasto/aukioloajat/yhteystiedot onto buildBrandingPreview's output in the branding branch of livePreviewPaikka so AI-onboarded venues see live pricing/hours/contact edits</name>
  <files>lib/livePreview/LivePreviewContext.tsx</files>
  <read_first>
    - lib/livePreview/LivePreviewContext.tsx (the WHOLE file — specifically: the import block at lines 16-26, where `buildDraftAsPaikka, type OnboardingDraft, type PaikkaBase` is imported from `@/lib/onboardingUtils` at line 24 — `hinnastaToHintaKuvaus` is ALSO exported from `@/lib/onboardingUtils` and must be added to THIS existing import statement (not a new import line); the PreviewDraft state shape at lines 35-46 confirming `state.hinnasto` is `Array<{kategoria,hinta,lisatieto?}> | null`, `state.aukioloajat` is `Record<string,{open,close}> | null`, `state.yhteystiedot` is `{puhelin?,email?,website?,kuvaus?} | null`; the livePreviewPaikka useMemo at lines 138-146 — this is the ONLY block to modify; its dependency array `[state, paikkaInfo, brandingData]` at line 146 already includes the full `state` so the memo already recomputes on every dispatch — DO NOT change the deps array)
    - lib/branding/brandingResult.ts (buildBrandingPreview lines 117-169 — confirm its return object field names exactly: `hinta_kuvaus` (string, '' when no scraped prices), `aukioloajat` (Record | null), `puhelin` (null), `kuvaus` (null), `varauslinkki` (raw_analysis.website_url ?? null); note line 15 already imports `hinnastaToHintaKuvaus` here, confirming it lives in @/lib/onboardingUtils; DO NOT modify this file — its signature and body stay exactly as-is)
    - lib/onboardingUtils.ts (buildDraftAsPaikka lines 94-115 — the REFERENCE pattern to replicate: line 105 `varauslinkki: draft.yhteystiedot?.website ?? null`, line 106 `kuvaus: draft.yhteystiedot?.kuvaus ?? null`, line 107 `puhelin: draft.yhteystiedot?.puhelin ?? null`, line 108 `hinta_kuvaus: hinnastaToHintaKuvaus(draft.hinnasto ?? [])`, line 109 `aukioloajat: draft.aukioloajat ?? paikka.aukioloajat ?? null`; and hinnastaToHintaKuvaus lines 65-75 — it filters rows whose `hinta` is empty and returns '' when none remain, so an all-empty live hinnasto serializes to '')
    - .planning/phases/51-live-esikatselu-velhossa/51-REVIEW.md (CR-01 finding lines 45-88 — the exact overlay snippet to implement, including the `?? base.<field>` fallback semantics)
    - .planning/phases/51-live-esikatselu-velhossa/51-VERIFICATION.md (gaps block lines 16-27 and the "Independent CR-01 Investigation" trace lines 64-119 — confirms the branding branch drops live state and that the fix belongs in the caller, not buildBrandingPreview)
  </read_first>
  <action>
    Make TWO changes to `lib/livePreview/LivePreviewContext.tsx`, both confined to the import block (line 24) and the `livePreviewPaikka` useMemo (lines 138-146). Do NOT touch the reducer, buildInitialState, the Provider's brandColor/accentColor derivation, the context value memo, the hook, or any type declaration. Do NOT modify lib/branding/brandingResult.ts or lib/onboardingUtils.ts at all.

    1. Add `hinnastaToHintaKuvaus` to the EXISTING import from `@/lib/onboardingUtils` at line 24. The line currently reads `import { buildDraftAsPaikka, type OnboardingDraft, type PaikkaBase } from '@/lib/onboardingUtils'`. Add `hinnastaToHintaKuvaus` as a named value import in that same statement (e.g. `import { buildDraftAsPaikka, hinnastaToHintaKuvaus, type OnboardingDraft, type PaikkaBase } from '@/lib/onboardingUtils'`). Do NOT add a separate import line. Do NOT import anything new from brandingResult.ts.

    2. Modify ONLY the branding branch (the `if (brandingData && paikkaInfo && typeof state.paikka_id === 'number')` block, currently lines 139-141) of the `livePreviewPaikka` useMemo. Currently it is a single-line `return buildBrandingPreview(...)`. Change it to: first capture the result in a `const base = buildBrandingPreview(paikkaInfo, brandingData, state.paikka_id, state.media_urls?.logo)` (keep the EXACT same four arguments — paikkaInfo, brandingData, state.paikka_id, state.media_urls?.logo — do NOT add a fifth argument), then `return` a new object spreading `base` and overlaying these five fields with the live state, each with a fallback to the `base` value:
       - `hinta_kuvaus: state.hinnasto?.length ? hinnastaToHintaKuvaus(state.hinnasto) : base.hinta_kuvaus` — when the user has added at least one pricing row, serialize the live rows the same way buildDraftAsPaikka does; otherwise keep the AI-scraped hinta_kuvaus from base. (Using `state.hinnasto?.length` as the guard matches the reducer: until SET_HINNASTO fires, state.hinnasto is null; after it fires it is the live rows array.)
       - `aukioloajat: state.aukioloajat ?? base.aukioloajat` — live hours when the user has edited them (SET_AUKIOLOAJAT dispatched), else the AI-scraped hours from base.
       - `puhelin: state.yhteystiedot?.puhelin ?? base.puhelin` — live phone when entered, else base (which is null today).
       - `kuvaus: state.yhteystiedot?.kuvaus ?? base.kuvaus` — live description when entered, else base (null today).
       - `varauslinkki: state.yhteystiedot?.website ?? base.varauslinkki` — live website when entered, else the AI-scraped website_url from base.

       Leave the non-branding branch (`if (paikkaInfo) { return buildDraftAsPaikka(state as OnboardingDraft, paikkaInfo) }`) and the final `return null` exactly as they are. Leave the useMemo dependency array `[state, paikkaInfo, brandingData]` exactly as it is — it already triggers recompute on every dispatch.

    Why this is correct and minimal: `buildBrandingPreview` is a shared builder also called by StepEsikatselu and the pre-phase esikatseluruutu; changing its signature would ripple to those out-of-scope screens. By overlaying in the caller, those screens keep their exact current behavior while the live-preview pane gets the live values. The `?? base.<field>` (and `state.hinnasto?.length ? ... : base.hinta_kuvaus`) fallbacks guarantee that before the user touches a given step, the field shows exactly what buildBrandingPreview produced (AI-scraped or null) — so a fresh AI-onboarding session is visually identical to today until the user starts editing. This is the same merge-with-fallback shape buildDraftAsPaikka already uses for the non-branding branch, so both branches now behave consistently. Note `state.yhteystiedot?.website` maps to `varauslinkki` exactly as buildDraftAsPaikka does (lib/onboardingUtils.ts line 105) — there is no separate `varauslinkki`/`website` rename to worry about.

    Do NOT introduce a `RESET` dispatch (the dead RESET action stays out of scope). Do NOT add `state.yhteystiedot?.email` anywhere — email is not a Liikuntapaikka field and buildDraftAsPaikka does not map it either. Do NOT touch StepHinnasto.tsx, StepAukioloajat.tsx, StepYhteystiedot.tsx, StepMediat.tsx, or WizardInner.tsx — their dispatches are verified correct; this is purely the consumption side.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `lib/livePreview/LivePreviewContext.tsx` imports `hinnastaToHintaKuvaus` from `@/lib/onboardingUtils` (added to the existing line-24 import, not a new import line). `grep -c "from '@/lib/onboardingUtils'" lib/livePreview/LivePreviewContext.tsx` returns exactly 1.
    - The branding branch of the `livePreviewPaikka` useMemo declares `const base = buildBrandingPreview(paikkaInfo, brandingData, state.paikka_id, state.media_urls?.logo)` with EXACTLY those four arguments (no fifth argument added), then returns an object literal spreading `base`.
    - The returned overlay object sets `hinta_kuvaus: state.hinnasto?.length ? hinnastaToHintaKuvaus(state.hinnasto) : base.hinta_kuvaus`, `aukioloajat: state.aukioloajat ?? base.aukioloajat`, `puhelin: state.yhteystiedot?.puhelin ?? base.puhelin`, `kuvaus: state.yhteystiedot?.kuvaus ?? base.kuvaus`, and `varauslinkki: state.yhteystiedot?.website ?? base.varauslinkki`.
    - `grep -n "\.\.\.base" lib/livePreview/LivePreviewContext.tsx` shows the spread inside the branding branch.
    - The non-branding branch still reads `return buildDraftAsPaikka(state as OnboardingDraft, paikkaInfo)` and the final `return null` is unchanged.
    - The useMemo dependency array is still exactly `[state, paikkaInfo, brandingData]`.
    - `lib/branding/brandingResult.ts` is NOT modified — `buildBrandingPreview`'s signature still has exactly four parameters (paikkaBase, brandingResult, draftPaikkaId, selectedLogoUrl?). `lib/onboardingUtils.ts` is NOT modified.
    - No `RESET` dispatch is introduced anywhere in the file.
    - `npx tsc --noEmit -p tsconfig.json` produces zero errors.
    - Behavior (human spot-check, see Human Verification below): start onboarding via the website-analysis flow for a venue with scraped prices/hours; on the pricing step change a price → the live preview updates instantly (after the ~280ms debounce); repeat for hours and contact info; contact puhelin/kuvaus now show typed values instead of permanently blank.
  </acceptance_criteria>
  <done>The branding branch of livePreviewPaikka overlays live state.hinnasto/state.aukioloajat/state.yhteystiedot onto buildBrandingPreview's output with per-field fallback to the AI-scraped/null base values, so AI-website-analysis-onboarded venues see live pricing/hours/contact edits in the preview while untouched steps still show scraped data — satisfying LIVEPREV-01/04 / criteria #1/#4 for the branding onboarding path, without touching buildBrandingPreview's shared signature.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.json` is clean (no new type errors introduced).
- `grep -n "\.\.\.base" lib/livePreview/LivePreviewContext.tsx` shows the spread of the branding-derived base object in the branding branch.
- `grep -n "hinnastaToHintaKuvaus" lib/livePreview/LivePreviewContext.tsx` shows the import addition and the `hinta_kuvaus` overlay usage.
- `grep -c "from '@/lib/onboardingUtils'" lib/livePreview/LivePreviewContext.tsx` returns exactly 1 (single import statement, hinnastaToHintaKuvaus added to it).
- `git diff --stat lib/branding/brandingResult.ts lib/onboardingUtils.ts` shows ZERO changes to either file (buildBrandingPreview's shared signature and onboardingUtils are untouched).
- The non-branding branch and the final `return null` are byte-identical to before; only the branding branch changed.
</verification>

<success_criteria>
LIVEPREV-01 and LIVEPREV-04 (criteria #1 and #4) are fully met for the pricing/hours/contact field paths on every onboarding path: (1) AI-website-analysis-onboarded venues now see live pricing/hours/contact edits in the preview, overlaid on top of the AI-scraped base; (2) before the user edits a given step, that field still shows the AI-scraped (or null) value, so fresh sessions are visually unchanged; (3) the non-branding onboarding path and EditMode remain unaffected (they use buildDraftAsPaikka); (4) buildBrandingPreview's shared signature, and therefore StepEsikatselu / the pre-phase esikatseluruutu, are untouched. After this fix, all 5 phase truths are verifiable and REQUIREMENTS.md's LIVEPREV-01..04 can be checked.
</success_criteria>

<human_verification>
After the fix lands, a human should spot-check the exact CR-01 scenario once (static reasoning establishes correctness; this confirms it end-to-end):
1. Start onboarding via the AI-website-analysis flow for a venue whose scrape produced prices and opening hours (`brandingData.status === 'analyzed'` with non-empty `raw_analysis.prices`/`opening_hours`).
2. On the pricing step (StepHinnasto), change a price → after ~280ms the live preview's CalloutCard/DiagonaalKortti shows the new price, not the scraped one.
3. On the opening-hours step (StepAukioloajat), edit a day's hours → the live preview reflects the edited hours.
4. On the contact step (StepYhteystiedot), type a phone number and a description → the live preview's puhelin/kuvaus now show the typed values (previously permanently blank), and changing the website updates varauslinkki.
5. Fallback check: at the start of step 2/3 before editing anything, confirm the preview still shows the AI-scraped prices/hours (not blank) — i.e. untouched steps fall back to scraped data.
6. Regression check: EditMode (brandingData null) still reflects live edits exactly as before — unchanged.
</human_verification>

<output>
Create `.planning/phases/51-live-esikatselu-velhossa/51-07-SUMMARY.md` when done.

Artifacts this plan produces: none new (no new files, no new exported symbols). This is a bugfix adding one named import (`hinnastaToHintaKuvaus`) and converting the branding branch of the `livePreviewPaikka` useMemo from a direct `return buildBrandingPreview(...)` into a `const base = buildBrandingPreview(...)` followed by a `return { ...base, <5 overlaid fields> }`, all within `lib/livePreview/LivePreviewContext.tsx`. `lib/branding/brandingResult.ts` and `lib/onboardingUtils.ts` are NOT modified.
</output>
