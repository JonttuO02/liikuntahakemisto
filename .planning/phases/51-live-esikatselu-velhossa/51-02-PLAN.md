---
phase: 51-live-esikatselu-velhossa
plan: 02
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - app/business/onboarding/LivePreviewPane.tsx
  - app/business/onboarding/LivePreviewToggle.tsx
  - messages/fi.json
  - messages/en.json
autonomous: true
requirements: [LIVEPREV-02, LIVEPREV-03, LIVEPREV-04]
must_haves:
  truths:
    - "A LivePreviewPane renders CalloutCard stacked above DiagonaalKortti from the shared context's livePreviewPaikka, with the StepEsikatselu spinner as its empty state"
    - "A LivePreviewToggle renders a two-segment Muokkaa/Esikatselu glass pill matching the EditMode tab-bar active/inactive tokens"
    - "New i18n keys previewToggleEdit/previewToggleLive exist in fi.json and en.json under the Business namespace"
  artifacts:
    - path: "app/business/onboarding/LivePreviewPane.tsx"
      provides: "Stacked CalloutCard/DiagonaalKortti preview column reading useLivePreview()"
      min_lines: 30
    - path: "app/business/onboarding/LivePreviewToggle.tsx"
      provides: "Muokkaa/Esikatselu segmented control"
      min_lines: 20
  key_links:
    - from: "app/business/onboarding/LivePreviewPane.tsx"
      to: "lib/livePreview/LivePreviewContext.tsx useLivePreview"
      via: "hook call for livePreviewPaikka/brandColor/accentColor"
      pattern: "useLivePreview"
    - from: "app/business/onboarding/LivePreviewPane.tsx"
      to: "app/components/CalloutCard.tsx + app/components/DiagonaalKortti.tsx"
      via: "render"
      pattern: "CalloutCard|DiagonaalKortti"
---

<objective>
Build the two presentation components that render the live preview: `LivePreviewPane` (the stacked CalloutCard/DiagonaalKortti column, used both as the desktop split-view sidebar and the mobile preview-side content) and `LivePreviewToggle` (the mobile Muokkaa/Esikatselu segmented control). Add the two new i18n keys. Both components consume the shared context from Plan 01 — they hold no data state of their own.

Purpose: LIVEPREV-02 (desktop side-by-side preview), LIVEPREV-03 (mobile toggle), and the render half of LIVEPREV-04 (renders via CalloutCard/DiagonaalKortti). Per D-06/D-07/D-08 and the 51-UI-SPEC.md contract.
Output: `LivePreviewPane.tsx`, `LivePreviewToggle.tsx`, updated `messages/fi.json` + `messages/en.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-live-esikatselu-velhossa/51-CONTEXT.md
@.planning/phases/51-live-esikatselu-velhossa/51-PATTERNS.md
@.planning/phases/51-live-esikatselu-velhossa/51-UI-SPEC.md
@app/business/onboarding/StepEsikatselu.tsx
@lib/livePreview/LivePreviewContext.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create LivePreviewPane reading useLivePreview, with StepEsikatselu spinner empty state</name>
  <files>app/business/onboarding/LivePreviewPane.tsx</files>
  <read_first>
    - app/business/onboarding/StepEsikatselu.tsx (lines 119-151 — the spinner empty-state block and the two-card stacked JSX with previewLabelCallout/previewLabelDiag captions; COPY the CalloutCard latitude/longitude `?? 0` shim verbatim; DROP the third PaikkaSheet section per LIVEPREV-04)
    - lib/livePreview/LivePreviewContext.tsx (useLivePreview return shape: livePreviewPaikka, brandColor, accentColor)
    - .planning/phases/51-live-esikatselu-velhossa/51-UI-SPEC.md (lines 100-125 — the desktop column wrapper is owned by Plan 03/WizardInner; this component renders ONLY the inner two-card stack + captions + empty state, NOT the outer `w-[360px] sticky` wrapper)
    - app/components/CalloutCard.tsx (prop shape: `p`, `brandColor`, `accentColor`)
    - app/components/DiagonaalKortti.tsx (prop shape: `paikka`, `brandColor`, `accentColor`)
  </read_first>
  <action>
    Create `'use client'` component `LivePreviewPane` (default export, no props). Call `const { livePreviewPaikka, brandColor, accentColor } = useLivePreview()` and `const t = useTranslations('Business')`.

    When `livePreviewPaikka` is null, render the EXACT spinner from StepEsikatselu lines 123-126: `<div className="flex justify-center py-8"><div className="w-6 h-6 rounded-full border-2 border-[rgba(17,17,17,0.12)] border-t-[#111111] animate-spin" /></div>`. (No 8-second timeout / error branch — live preview has placeholder data and no network call, per UI-SPEC Error-state row.)

    When present, render the two-card stack copied from StepEsikatselu lines 128-143: a `flex flex-col gap-4` container (16px gap per UI-SPEC, NOT the gap-6 of StepEsikatselu). First child: caption `<span className="text-[10px] font-bold uppercase tracking-widest text-[rgba(17,17,17,0.45)]">{t('previewLabelCallout')}</span>` then `<CalloutCard p={{ ...livePreviewPaikka, latitude: livePreviewPaikka.latitude ?? 0, longitude: livePreviewPaikka.longitude ?? 0 }} brandColor={brandColor} accentColor={accentColor} />`. Second child: caption `{t('previewLabelDiag')}` then `<DiagonaalKortti paikka={livePreviewPaikka} brandColor={brandColor} accentColor={accentColor} />`.

    Do NOT wrap the cards in an extra `.glass` div (UI-SPEC explicitly forbids double-glass; the cards carry their own glass surface). Do NOT render PaikkaSheet. Reuse existing i18n keys `previewLabelCallout`/`previewLabelDiag` (already in messages). Implements LIVEPREV-04 render half + supplies the content for LIVEPREV-02/03.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -c "LivePreviewPane" | grep -q '^0$' && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file begins with `'use client'` and imports `useLivePreview` from `@/lib/livePreview/LivePreviewContext`
    - Source assertion: renders both `CalloutCard` and `DiagonaalKortti`; does NOT contain the string `PaikkaSheet`
    - Source assertion: contains the spinner class `border-t-[#111111] animate-spin`
    - Source assertion: contains `latitude: livePreviewPaikka.latitude ?? 0` (the null-coordinate shim)
    - Behavior: `npx tsc --noEmit` reports zero errors originating in `LivePreviewPane.tsx`
  </acceptance_criteria>
  <done>LivePreviewPane renders the two-card stack from context, shows the shared spinner when no preview data exists, and omits PaikkaSheet.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create LivePreviewToggle segmented control + add previewToggle i18n keys</name>
  <files>app/business/onboarding/LivePreviewToggle.tsx, messages/fi.json, messages/en.json</files>
  <read_first>
    - .planning/phases/51-live-esikatselu-velhossa/51-UI-SPEC.md (lines 127-149 — the EXACT segmented-control JSX contract: `.glass rounded-full p-1 flex w-full max-w-xl mx-auto mb-4` track, two `flex-1 h-9 rounded-full` buttons, Pencil/Eye icons w-3.5 h-3.5, whileTap scale 0.95, active `bg-[#111111] text-white` / inactive `text-[rgba(17,17,17,0.45)]`)
    - app/business/WizardInner.tsx (lines 377-381 — the active/inactive token to match exactly)
    - app/business/onboarding/ProgressBar.tsx (whileTap scale-only feedback precedent)
    - messages/fi.json (lines ~182-183 — the previewLabelCallout/previewLabelDiag key precedent in the Business namespace; add the new keys adjacent)
    - messages/en.json (same location)
  </read_first>
  <action>
    Create `'use client'` component `LivePreviewToggle` (default export) with props `{ activeView: 'edit' | 'preview'; onChange: (view: 'edit' | 'preview') => void }`. Use `motion.button` from framer-motion for `whileTap={{ scale: 0.95 }}`. Render the track and two buttons EXACTLY per UI-SPEC lines 129-140: track `className="glass rounded-full p-1 flex w-full max-w-xl mx-auto mb-4"`; each button `flex-1 h-9 rounded-full text-sm font-bold flex items-center justify-center gap-1.5 [transition:background-color_150ms_var(--ease-out),color_150ms_var(--ease-out)]` with the active class `bg-[#111111] text-white` when its view is active else `text-[rgba(17,17,17,0.45)]`. Left button: `<Pencil className="w-3.5 h-3.5" />` + `{t('previewToggleEdit')}`, `onClick={() => onChange('edit')}`. Right button: `<Eye className="w-3.5 h-3.5" />` + `{t('previewToggleLive')}`, `onClick={() => onChange('preview')}`. Import `Pencil`, `Eye` from `lucide-react`; `useTranslations('Business')`.

    Add to `messages/fi.json` under the `Business` namespace (adjacent to `previewLabelCallout`): `"previewToggleEdit": "Muokkaa"`, `"previewToggleLive": "Esikatselu"`. Add to `messages/en.json` same keys: `"previewToggleEdit": "Edit"`, `"previewToggleLive": "Preview"`. Preserve existing JSON structure and trailing commas validity. Implements D-07; supplies the control for LIVEPREV-03.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('messages/fi.json','utf8')).Business.previewToggleEdit && JSON.parse(require('fs').readFileSync('messages/en.json','utf8')).Business.previewToggleLive && console.log('keys ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `LivePreviewToggle.tsx` begins with `'use client'`, imports `Pencil` and `Eye` from `lucide-react`, and uses `whileTap={{ scale: 0.95 }}`
    - Source assertion: contains active token `bg-[#111111] text-white` and track class `glass rounded-full p-1`
    - CLI: `node -e "..."` prints `keys ok` (both JSON files parse and contain the new keys)
    - Behavior: `messages/fi.json.Business.previewToggleEdit === "Muokkaa"` and `messages/en.json.Business.previewToggleEdit === "Edit"`
    - Behavior: `npx tsc --noEmit` reports zero errors originating in `LivePreviewToggle.tsx`
  </acceptance_criteria>
  <done>The two-segment control renders with the correct tokens/icons/copy and both message files validate with the new keys.</done>
</task>

</tasks>

<artifacts_produced>
This phase plan produces these new symbols:
- `app/business/onboarding/LivePreviewPane.tsx` — new file; default export `LivePreviewPane` (no props)
- `app/business/onboarding/LivePreviewToggle.tsx` — new file; default export `LivePreviewToggle` with props `{ activeView, onChange }`
- New i18n keys in `messages/fi.json` and `messages/en.json` under `Business`: `previewToggleEdit`, `previewToggleLive`
</artifacts_produced>

<threat_model>
Pure client-side presentation. No new API route, DB write, or trust boundary. Components read derived context state (Plan 01) and render existing card components unchanged.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-02 | Information Disclosure | LivePreviewPane render | accept | Renders only the business owner's own in-progress draft data already loaded client-side; no cross-tenant data path introduced. |
| T-51-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (lucide-react, framer-motion, next-intl already in deps). Any proposed install → [ASSUMED] + gate. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Both message JSON files parse and contain `previewToggleEdit`/`previewToggleLive`.
- `LivePreviewPane.tsx` contains no `PaikkaSheet` reference and no extra `.glass` wrapper around the card stack.
</verification>

<success_criteria>
- LivePreviewPane visually matches StepEsikatselu's two-card stack (minus PaikkaSheet) and shows the shared spinner empty state.
- LivePreviewToggle matches the UI-SPEC segmented-control contract exactly.
- i18n keys present in both locales.
</success_criteria>

<output>
Create `.planning/phases/51-live-esikatselu-velhossa/51-02-SUMMARY.md` when done.
</output>
