---
phase: 51-live-esikatselu-velhossa
plan: 03
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - app/business/onboarding/StepHinnasto.tsx
  - app/business/onboarding/StepMediat.tsx
  - app/business/onboarding/StepAukioloajat.tsx
  - app/business/onboarding/StepYhteystiedot.tsx
autonomous: true
requirements: [LIVEPREV-01, LIVEPREV-04]
must_haves:
  truths:
    - "Typing in a StepHinnasto pricing cell dispatches a debounced SET_HINNASTO into the live-preview context"
    - "Selecting a logo/photo in StepMediat dispatches SET_MEDIA with the local blob URL immediately (no debounce)"
    - "Editing StepAukioloajat hours and StepYhteystiedot contact fields dispatches into the context (debounced for free-text)"
    - "Existing save-on-submit (onNext/onSaveComplete) behavior is unchanged in all four steps"
  artifacts:
    - path: "app/business/onboarding/StepHinnasto.tsx"
      provides: "Debounced SET_HINNASTO dispatch alongside updateRow"
      contains: "useLivePreview"
    - path: "app/business/onboarding/StepMediat.tsx"
      provides: "Instant SET_MEDIA dispatch on file selection"
      contains: "useLivePreview"
  key_links:
    - from: "app/business/onboarding/StepHinnasto.tsx"
      to: "lib/livePreview/LivePreviewContext.tsx dispatch"
      via: "useLivePreview().dispatch with debounced rows"
      pattern: "dispatch\\(\\{ *type: 'SET_HINNASTO'"
    - from: "app/business/onboarding/StepMediat.tsx"
      to: "lib/livePreview/LivePreviewContext.tsx dispatch"
      via: "SET_MEDIA on blob URL"
      pattern: "SET_MEDIA"
---

<objective>
Wire the four numbered wizard step components to push their in-progress field values into the shared live-preview context as the user edits — debounced (~280ms) for free-text/numeric fields (D-04), instant for discrete selections like logo/photo blob URLs (D-05). The existing save-on-submit flow (onNext/onSaveComplete/save routes) is left completely untouched; this adds a parallel outward channel only.

Purpose: LIVEPREV-01 (each step updates shared preview state on field change) and the live-data half of LIVEPREV-04 (current unsaved values). Per D-04/D-05.
Output: modified `StepHinnasto.tsx`, `StepMediat.tsx`, `StepAukioloajat.tsx`, `StepYhteystiedot.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-live-esikatselu-velhossa/51-CONTEXT.md
@.planning/phases/51-live-esikatselu-velhossa/51-PATTERNS.md
@lib/livePreview/LivePreviewContext.tsx
@lib/livePreview/useDebouncedPreviewField.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Dispatch debounced SET_HINNASTO from StepHinnasto and SET_YHTEYSTIEDOT/SET_AUKIOLOAJAT from contact/hours steps</name>
  <files>app/business/onboarding/StepHinnasto.tsx, app/business/onboarding/StepAukioloajat.tsx, app/business/onboarding/StepYhteystiedot.tsx</files>
  <read_first>
    - app/business/onboarding/StepHinnasto.tsx (updateRow line 114; the `rows` state shape PricingRow; the existing handleNext/handleSave — do NOT change them)
    - app/business/onboarding/StepYhteystiedot.tsx (puhelin/email/website/kuvaus useState fields lines 39-42; onChange handlers; existing handleNext/handleSave unchanged)
    - app/business/onboarding/StepAukioloajat.tsx (hours HoursState lines 58, ORDERED_DAYS; the day open/close inputs; existing save flow unchanged)
    - lib/livePreview/LivePreviewContext.tsx (useLivePreview, LivePreviewAction shapes: SET_HINNASTO, SET_YHTEYSTIEDOT, SET_AUKIOLOAJAT)
    - lib/livePreview/useDebouncedPreviewField.ts (useDebouncedValue signature)
  </read_first>
  <action>
    In all three files, call `const { dispatch } = useLivePreview()` at the top of the component.

    StepHinnasto: derive the preview payload from `rows` — map the non-fixed-vs-fixed rows to `{ kategoria, hinta, lisatieto }[]` filtered to non-empty hinta, the same projection used in handleNext line 198-200. Wrap `rows` in `useDebouncedValue(rows, 280)` and in a `useEffect` keyed on the debounced value dispatch `{ type: 'SET_HINNASTO', payload: <projected rows> }`. Do NOT modify `updateRow`, `handleNext`, or `handleSave` themselves — the debounce observes `rows` state, it does not intercept the mutation. Pricing cells are free-text/numeric → debounced per D-04.

    StepYhteystiedot: combine `{ puhelin, email, website, kuvaus }` into a value, wrap with `useDebouncedValue(..., 280)`, and dispatch `{ type: 'SET_YHTEYSTIEDOT', payload: { puhelin, email, website, kuvaus } }` in a `useEffect` keyed on the debounced value. All four are free-text → debounced per D-04. Leave the onChange setters and save flow unchanged.

    StepAukioloajat: build the `Record<string, { open: string; close: string }>` for days marked `isOpen` (mirror how handleNext serializes hours), wrap with `useDebouncedValue(..., 280)`, dispatch `{ type: 'SET_AUKIOLOAJAT', payload: <record> }` in a `useEffect` keyed on the debounced value. The time inputs are typed free-text → debounce per D-04 (confirm: these are `<input>` text/number fields, not native time-pickers — debounce applies).

    All dispatches are additive: they MUST NOT alter `onNext`, `onPrev`, `onSaveComplete`, `onSaveSuccess`, `handleNext`, or `handleSave`. Implements LIVEPREV-01 + LIVEPREV-04 for these three steps.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -cE "StepHinnasto|StepYhteystiedot|StepAukioloajat" | grep -q '^0$' && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: each of the three files contains `useLivePreview` and `useDebouncedValue`
    - Source assertion: StepHinnasto contains `SET_HINNASTO`, StepYhteystiedot contains `SET_YHTEYSTIEDOT`, StepAukioloajat contains `SET_AUKIOLOAJAT`
    - Source assertion: the literal strings `handleNext`, `handleSave`, `onSaveComplete` still appear unchanged (existing save flow preserved) — grep that each still has its original `fetch('/api/business/onboarding/save-step'` or `fetch('/api/business/update-paikka'` call
    - Behavior: `npx tsc --noEmit` reports zero errors in these three files
  </acceptance_criteria>
  <done>Editing any pricing/hours/contact field dispatches a debounced preview update; the original save-on-submit code paths are byte-identical except for the added dispatch wiring.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Dispatch instant SET_MEDIA from StepMediat on logo/photo file selection</name>
  <files>app/business/onboarding/StepMediat.tsx</files>
  <read_first>
    - app/business/onboarding/StepMediat.tsx (stagedPreviewUrls useMemo lines 59-65; handleLogoFilesSelected lines 67-70; handlePhotoFilesSelected lines 72-78; existingLogoUrl/existingPhotoUrls state; existing handleNext/handleSave upload flow — do NOT change the upload/save logic)
    - lib/livePreview/LivePreviewContext.tsx (SET_MEDIA action: payload `{ logo?: string | null; photos?: string[] }`)
  </read_first>
  <action>
    Call `const { dispatch } = useLivePreview()`. Add a `logoPreviewUrl` derived blob URL for the selected logo file (mirror the `stagedPreviewUrls` useMemo + revoke-on-cleanup useEffect pattern at lines 59-65, but for `logoFiles[0]`). In a `useEffect` keyed on `[logoFiles, photoFiles, existingLogoUrl, existingPhotoUrls]`, dispatch `{ type: 'SET_MEDIA', payload: { logo: <logoPreviewUrl ?? existingLogoUrl ?? null>, photos: [...existingPhotoUrls, ...stagedPreviewUrls] } }` — INSTANT, no debounce (D-05: discrete file selection reuses the local `URL.createObjectURL` blob preview as the live image source so nothing waits on the Supabase upload).

    Do NOT modify `handleNext`, `handleSave`, the upload-to-Storage logic, or the save-step write — those still run on submit and persist the real (uploaded) URLs. The blob URLs are preview-only. Reuse the existing `URL.revokeObjectURL` cleanup pattern so no blob URL leaks. Implements D-05 + LIVEPREV-01/04 for the media step.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -c "StepMediat" | grep -q '^0$' && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: StepMediat contains `useLivePreview` and `SET_MEDIA`
    - Source assertion: StepMediat does NOT wrap the SET_MEDIA dispatch in `useDebouncedValue` (instant per D-05) — grep that `useDebouncedValue` is absent from this file
    - Source assertion: existing `supabase.storage.from('business-media').upload` call still present (upload flow untouched)
    - Source assertion: `URL.revokeObjectURL` appears at least twice (existing photo cleanup + new logo cleanup)
    - Behavior: `npx tsc --noEmit` reports zero errors in StepMediat.tsx
  </acceptance_criteria>
  <done>Selecting a logo or photo immediately pushes its blob URL into the live preview without waiting for upload; the existing upload/save flow is unchanged.</done>
</task>

</tasks>

<artifacts_produced>
This phase plan produces no new files. It adds to existing components:
- `StepHinnasto.tsx` — `useLivePreview` + debounced `SET_HINNASTO` dispatch
- `StepYhteystiedot.tsx` — `useLivePreview` + debounced `SET_YHTEYSTIEDOT` dispatch
- `StepAukioloajat.tsx` — `useLivePreview` + debounced `SET_AUKIOLOAJAT` dispatch
- `StepMediat.tsx` — `useLivePreview` + instant `SET_MEDIA` dispatch with logo blob URL
</artifacts_produced>

<threat_model>
Pure client-side. The dispatches feed preview-only derived state; all persistence still flows through the existing validated save-step/update-paikka routes (unchanged). No new trust boundary. Blob URLs are local object URLs, never sent to any server.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-03 | Tampering | Step dispatches | accept | Dispatches only update local preview state; the unmodified save routes remain the sole validated write path. |
| T-51-04 | Denial of Service | blob URL leak | mitigate | Reuse existing `URL.revokeObjectURL` cleanup-on-unmount/dependency-change pattern for the new logo blob URL to prevent memory growth. |
| T-51-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase. Any proposed install → [ASSUMED] + gate. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for all four step files.
- Grep confirms each step's original save call (`save-step` or `update-paikka`) is still present.
- StepMediat retains its Supabase upload call and adds no debounce.
</verification>

<success_criteria>
- Each step pushes current field values into the shared context as the user edits (debounced text, instant media).
- No regression to the existing save-on-submit behavior in any step.
</success_criteria>

<output>
Create `.planning/phases/51-live-esikatselu-velhossa/51-03-SUMMARY.md` when done.
</output>
