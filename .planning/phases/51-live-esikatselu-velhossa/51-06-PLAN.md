---
phase: 51-live-esikatselu-velhossa
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/business/onboarding/StepMediat.tsx
autonomous: true
gap_closure: true
requirements: [LIVEPREV-04]
must_haves:
  truths:
    - "In EditMode, after the user saves new media on the Mediat tab (handleSave updates existingLogoUrl/existingPhotoUrls) and then switches tabs WITHOUT an intervening remount of StepMediat, the unmount fallback dispatches the POST-save media into LivePreviewContext — never the stale pre-save values captured at mount (per D-05/LIVEPREV-04: not stale data from the last save)"
    - "The unmount cleanup reads the latest persisted existingLogoUrl/existingPhotoUrls through a ref that is refreshed on every change, not through a closure frozen at the mount-time render"
    - "The original CR-01 behavior is preserved: when a user stages a blob: URL and leaves without saving, the unmount cleanup still dispatches a non-blob persisted (or no-media) fallback, so no revoked blob: URL ever lingers in the shared preview"
  artifacts:
    - path: "app/business/onboarding/StepMediat.tsx"
      provides: "A latestMediaRef (useRef) holding the current existingLogoUrl/existingPhotoUrls, refreshed by a dependency-tracking useEffect keyed on [existingLogoUrl, existingPhotoUrls], read by the unmount-only cleanup's SET_MEDIA dispatch instead of closing over the state directly"
      contains: "latestMediaRef"
      min_lines: 600
  key_links:
    - from: "app/business/onboarding/StepMediat.tsx (unmount cleanup)"
      to: "app/business/onboarding/StepMediat.tsx (latestMediaRef.current)"
      via: "unmount-time SET_MEDIA dispatch reads latestMediaRef.current.logo/.photos rather than closing over existingLogoUrl/existingPhotoUrls"
      pattern: "latestMediaRef\\.current"
    - from: "app/business/onboarding/StepMediat.tsx"
      to: "lib/livePreview/LivePreviewContext.tsx SET_MEDIA reducer action"
      via: "unmount-time dispatch of SET_MEDIA with non-blob fallback URLs read from the ref"
      pattern: "SET_MEDIA"
---

<objective>
Close the re-opened verification gap (WR-01 / criterion #4 partial) for Phase 51. Plan 51-05 closed the original CR-01 staged-blob staleness by adding an unmount-only `useEffect` (`[]` deps) in `app/business/onboarding/StepMediat.tsx` (lines 99-110) whose cleanup dispatches `SET_MEDIA` with `existingLogoUrl`/`existingPhotoUrls`. That cleanup closure is created exactly once, at mount, so it permanently captures the mount-time values. In EditMode, when the user saves new media (`handleSave` calls `setExistingLogoUrl`/`setExistingPhotoUrls` at lines 344-345) and then switches tabs without an intervening remount, the unmount cleanup re-broadcasts the stale pre-save values, overwriting the correct post-save preview — re-opening criterion #4 ("not stale data from the last save") on the save-then-navigate path.

Fix: introduce a `latestMediaRef` (`useRef`) holding the current `existingLogoUrl`/`existingPhotoUrls`, refresh it via a small `useEffect` keyed on `[existingLogoUrl, existingPhotoUrls]`, and have the existing unmount-only cleanup dispatch read `latestMediaRef.current.logo`/`latestMediaRef.current.photos` instead of closing over the state variables directly. The ref always holds the latest non-blob persisted value, so this fixes WR-01 without reintroducing CR-01.

Purpose: completes LIVEPREV-04 for the media field path on the save-then-navigate path (the only remaining failing path; pricing/hours/contact and the staged-but-unsaved blob path already pass).
Output: modified `app/business/onboarding/StepMediat.tsx` (no new files, no new exported symbols).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-live-esikatselu-velhossa/51-CONTEXT.md
@.planning/phases/51-live-esikatselu-velhossa/51-VERIFICATION.md
@.planning/phases/51-live-esikatselu-velhossa/51-REVIEW.md
@app/business/onboarding/StepMediat.tsx
@lib/livePreview/LivePreviewContext.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Read the unmount fallback's stale-mount-time values through a ref so EditMode save-then-navigate dispatches post-save media, not pre-save</name>
  <files>app/business/onboarding/StepMediat.tsx</files>
  <read_first>
    - app/business/onboarding/StepMediat.tsx (the WHOLE file — specifically: the imports at line 3, where `useRef` is ALREADY imported alongside `useState, useMemo, useEffect`, so NO new import is needed; `const { dispatch } = useLivePreview()` at line 33; the `existingLogoUrl`/`existingPhotoUrls` useState declarations at lines 37-46; the existing `photoInputRef` useRef at line 55 as the established `useRef` usage pattern in this file; the instant SET_MEDIA effect at lines 82-90; the unmount-only fallback effect at lines 99-110 — this is the effect to modify; and handleSave's lines 344-345 `setExistingLogoUrl(finalLogoUrl)` / `setExistingPhotoUrls(finalPhotoUrls)` which are the post-save state updates the current cleanup closure cannot observe)
    - lib/livePreview/LivePreviewContext.tsx (the SET_MEDIA action shape at line 54 — `payload: { logo?: string | null; photos?: string[] }` — and the reducer's SET_MEDIA branch at lines 68-81: passing `logo` and `photos` explicitly REPLACES the prior values; the unmount dispatch must continue to supply both keys with non-blob values)
    - .planning/phases/51-live-esikatselu-velhossa/51-REVIEW.md (WR-01 finding at lines 43-78 — the suggested ref-based fix; the plan's implementation must match the intent: ref refreshed on every relevant render, cleanup reads from the ref)
    - .planning/phases/51-live-esikatselu-velhossa/51-VERIFICATION.md (gaps block and the "Independent WR-01 Investigation" trace at lines 49-75 — confirms the mount-time closure capture is the root cause and the ref fix resolves it without reintroducing CR-01)
  </read_first>
  <action>
    Make THREE changes to `app/business/onboarding/StepMediat.tsx`, all confined to the media live-preview wiring region (lines ~82-110). Do NOT touch any render markup, handleNext, handleSave's upload/POST logic, the two blob-revocation effects (lines 65-67, 74-76), or the instant SET_MEDIA effect (lines 82-90).

    1. Declare a new ref named exactly `latestMediaRef`, initialized with the current state values. Place it immediately before the existing unmount-only fallback effect (i.e. just before the comment block at lines 92-98). Use the `useRef` already imported at line 3 — initialize it as `useRef({ logo: existingLogoUrl, photos: existingPhotoUrls })`. The ref's `.current` shape is `{ logo: string | null, photos: string[] }`, mirroring the SET_MEDIA payload keys. This matches the existing `photoInputRef` useRef pattern already in the file at line 55.

    2. Add a small dependency-tracking effect that keeps `latestMediaRef.current` in sync on every render where the persisted media changes. Place it immediately after the `latestMediaRef` declaration and before the unmount-only fallback effect. Its body assigns `latestMediaRef.current = { logo: existingLogoUrl, photos: existingPhotoUrls }`. Its dependency array MUST be exactly `[existingLogoUrl, existingPhotoUrls]` (NOT `[]`, NOT omitted) — this is the dependency array that makes the ref observe the post-save `setExistingLogoUrl`/`setExistingPhotoUrls` updates from handleSave lines 344-345. This effect needs NO eslint-disable directive because its dependency array is exhaustive.

    3. Modify the EXISTING unmount-only fallback effect (lines 99-110): change its cleanup dispatch payload to read from the ref instead of closing over the state variables. The payload becomes `logo: latestMediaRef.current.logo ?? null` and `photos: latestMediaRef.current.photos`. Keep the effect's empty dependency array `[]` and keep the existing `// eslint-disable-next-line react-hooks/exhaustive-deps` directive — the cleanup intentionally runs only at actual unmount, and reading through the ref (a stable object identity whose `.current` is updated by change #2) means the cleanup always sees the latest persisted values without the effect itself needing to re-run. Do NOT add `existingLogoUrl`/`existingPhotoUrls` to this effect's dependency array (that would change it from unmount-only back to a per-change effect and reintroduce a different bug).

    Why this is correct: the unmount cleanup closure now closes over `latestMediaRef` (a stable ref object whose identity never changes across renders), not over the snapshot values. At actual unmount time it dereferences `latestMediaRef.current`, which change #2's effect has kept current through every post-save re-render. So: (a) WR-01 is fixed — after a save, the ref holds the post-save URLs, so the unmount dispatch broadcasts post-save media; (b) CR-01 stays fixed — the ref never holds a `blob:` URL (it only ever mirrors the persisted `existingLogoUrl`/`existingPhotoUrls`, never `logoPreviewUrl`/`stagedPreviewUrls`), so leaving with an unsaved staged blob still falls back to the persisted/no-media state.

    Do NOT introduce a `RESET` dispatch (the dead RESET action remains out of scope; SET_MEDIA replacement is the chosen mechanism). Do NOT change the instant SET_MEDIA effect (lines 82-90) — it must continue dispatching `logoPreviewUrl ?? existingLogoUrl` and `[...existingPhotoUrls, ...stagedPreviewUrls]` so the live preview still updates instantly on file selection. Do NOT touch StepHinnasto.tsx, StepAukioloajat.tsx, StepYhteystiedot.tsx, or WizardInner.tsx — they have no blob URLs and are verified clean.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `app/business/onboarding/StepMediat.tsx` declares a `useRef` named exactly `latestMediaRef`, initialized to `{ logo: existingLogoUrl, photos: existingPhotoUrls }`.
    - A dependency-tracking `useEffect` assigns `latestMediaRef.current = { logo: existingLogoUrl, photos: existingPhotoUrls }` with dependency array exactly `[existingLogoUrl, existingPhotoUrls]` (this effect carries NO eslint-disable directive).
    - The existing unmount-only fallback effect still has an empty dependency array `[]` with its `// eslint-disable-next-line react-hooks/exhaustive-deps` directive intact, and its cleanup dispatch payload now reads `logo: latestMediaRef.current.logo ?? null` and `photos: latestMediaRef.current.photos` — it no longer references `existingLogoUrl`/`existingPhotoUrls` directly inside the cleanup.
    - `grep -n "latestMediaRef" app/business/onboarding/StepMediat.tsx` shows at least three hits: the `useRef` declaration, the sync-effect assignment, and the unmount cleanup read.
    - The instant SET_MEDIA effect (lines ~82-90), the two blob-revocation effects (lines ~65-67, ~74-76), `handleNext`, and `handleSave`'s upload/POST logic are unchanged in behavior.
    - No `RESET` dispatch is introduced anywhere in the file.
    - `npx tsc --noEmit -p tsconfig.json` produces zero errors.
    - Behavior (human spot-check, see Human Verification below): in EditMode, upload and save a new logo on the Mediat tab, then switch to the Hinnasto tab without revisiting Mediat — the live preview shows the just-saved logo, NOT the previous one.
  </acceptance_criteria>
  <done>EditMode's save-then-navigate-without-remount sequence no longer re-broadcasts stale pre-save media into LivePreviewContext; the unmount cleanup reads the latest persisted media through latestMediaRef, satisfying LIVEPREV-04 / criterion #4 for the media field path while preserving the CR-01 fix.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.json` is clean (no new type errors introduced).
- `grep -n "latestMediaRef" app/business/onboarding/StepMediat.tsx` shows the ref declaration, the `[existingLogoUrl, existingPhotoUrls]`-keyed sync effect, and the unmount cleanup read.
- `grep -n "SET_MEDIA" app/business/onboarding/StepMediat.tsx` still shows TWO dispatch sites: the instant effect (logoPreviewUrl/stagedPreviewUrls, unchanged) and the unmount fallback effect (now reading from latestMediaRef.current).
- `grep -n "RESET" app/business/onboarding/StepMediat.tsx` shows ZERO matches.
- The unmount fallback effect retains its `[]` dependency array; only its payload source changed (from direct state to `latestMediaRef.current`).
</verification>

<success_criteria>
LIVEPREV-04 / criterion #4 is fully met for the media field path on every path: (1) staged-but-unsaved blob URLs still fall back to persisted/no-media on leave (CR-01, preserved); (2) saved-then-navigated-away media now broadcasts the post-save value, not the stale pre-save value (WR-01, fixed). Pricing, hours, and contact field paths remain unaffected. After this fix, all 5 phase truths are verifiable and REQUIREMENTS.md's LIVEPREV-04 can be checked.
</success_criteria>

<human_verification>
After the fix lands, a human should spot-check the exact WR-01 regression sequence once (static closure-semantics reasoning establishes correctness; this confirms it end-to-end):
1. Open EditMode for an existing venue and go to the Mediat tab.
2. Upload a new logo and click Tallenna (save) — wait for the save-success banner.
3. Switch to the Hinnasto tab WITHOUT revisiting the Mediat tab.
4. Confirm the live preview sidebar (desktop) or the Esikatselu toggle (mobile) shows the JUST-SAVED logo, NOT the previously-persisted one and NOT a broken image.
5. Regression check (CR-01 still fixed): return to Mediat, stage a logo but do NOT save, switch tabs — the preview shows the persisted logo or no-logo state, never a broken blob image.
</human_verification>

<output>
Create `.planning/phases/51-live-esikatselu-velhossa/51-06-SUMMARY.md` when done.

Artifacts this plan produces: none new (no new files, no new exported symbols). This is a bugfix adding one `useRef` (`latestMediaRef`) and one dependency-tracking `useEffect`, and modifying the existing unmount-only `useEffect`'s cleanup payload source, all within `app/business/onboarding/StepMediat.tsx`.
</output>
