---
phase: 51-live-esikatselu-velhossa
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/business/onboarding/StepMediat.tsx
autonomous: true
gap_closure: true
requirements: [LIVEPREV-04]
must_haves:
  truths:
    - "When a logo/photo is staged on the Mediat step and the user navigates to another wizard step before saving, the live preview no longer shows a broken blob: image — it falls back to the last-known persisted (non-blob) media or the no-media state (per D-05/LIVEPREV-04: not stale data)"
    - "On unmount, StepMediat clears the staged blob: URLs from LivePreviewContext BEFORE those exact blob URLs are revoked by the existing cleanup effects"
  artifacts:
    - path: "app/business/onboarding/StepMediat.tsx"
      provides: "Unmount-time SET_MEDIA dispatch that replaces staged blob: URLs in LivePreviewContext with the last-known persisted existingLogoUrl/existingPhotoUrls before revocation"
      contains: "dispatch"
      min_lines: 590
  key_links:
    - from: "app/business/onboarding/StepMediat.tsx"
      to: "lib/livePreview/LivePreviewContext.tsx SET_MEDIA reducer action"
      via: "unmount-time dispatch of SET_MEDIA with non-blob fallback URLs"
      pattern: "SET_MEDIA"
---

<objective>
Close the single verification gap (CR-01 / criterion #4 partial) for Phase 51: staged `blob:` media URLs become permanently stale in the shared live preview once the user leaves the Mediat step before saving. Add an unmount-time dispatch in `StepMediat.tsx` that replaces the staged `blob:` URLs in `LivePreviewContext` with the last-known persisted (non-blob) `existingLogoUrl`/`existingPhotoUrls` BEFORE the existing cleanup effects call `URL.revokeObjectURL` on those blobs.

Purpose: completes LIVEPREV-04 ("renders via CalloutCard/DiagonaalKortti using current in-progress (unsaved) field values, not stale data from the last save") for the media field path. Pricing/hours/contact fields already pass — this is the only remaining gap.
Output: modified `app/business/onboarding/StepMediat.tsx` (no new files, no new exported symbols).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-live-esikatselu-velhossa/51-CONTEXT.md
@.planning/phases/51-live-esikatselu-velhossa/51-VERIFICATION.md
@.planning/phases/51-live-esikatselu-velhossa/51-REVIEW.md
@app/business/onboarding/StepMediat.tsx
@lib/livePreview/LivePreviewContext.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Dispatch a non-blob fallback SET_MEDIA on StepMediat unmount so live preview never holds a revoked blob URL</name>
  <files>app/business/onboarding/StepMediat.tsx</files>
  <read_first>
    - app/business/onboarding/StepMediat.tsx (the whole file — specifically: `const { dispatch } = useLivePreview()` at line 33; `existingLogoUrl`/`existingPhotoUrls` state at lines 37-46; the two blob-revocation cleanup effects at lines 65-67 (stagedPreviewUrls) and 74-76 (logoPreviewUrl); the instant live-preview SET_MEDIA effect at lines 82-90 that currently dispatches `logoPreviewUrl ?? existingLogoUrl` and `[...existingPhotoUrls, ...stagedPreviewUrls]`)
    - lib/livePreview/LivePreviewContext.tsx (the SET_MEDIA action shape at line 54 — `payload: { logo?: string | null; photos?: string[] }` — and the reducer's SET_MEDIA branch at lines 68-81: passing `logo` and `photos` explicitly REPLACES the prior values; both keys must be supplied to fully drop the blob URLs)
  </read_first>
  <action>
    Add ONE new cleanup `useEffect` to `StepMediat` that runs ONLY on unmount (empty dependency array `[]`) and dispatches a `SET_MEDIA` action whose payload contains ONLY the persisted, non-blob URLs — i.e. `logo: existingLogoUrl ?? null` and `photos: existingPhotoUrls` — so the shared `LivePreviewContext` no longer references any `blob:` URL once the component unmounts.

    Placement and ordering requirements:
    - Place this new unmount effect AFTER the existing instant SET_MEDIA effect (lines 82-90) and AFTER the two blob-revocation cleanup effects (lines 65-67 and 74-76). React runs cleanup functions in declaration order on unmount, so declaring the dispatch effect last ensures its cleanup (the dispatch) runs only at unmount alongside the others; the dispatch updating context state before the page re-renders is what prevents the revoked-blob `<img>` from ever painting.
    - The effect body returns a cleanup function (the dispatch lives inside the returned cleanup, not in the effect body itself), matching the pattern of the two existing revocation effects.
    - Use the React `useEffect` already imported at line 3 (no new imports needed). `dispatch` is already destructured at line 33; `existingLogoUrl`/`existingPhotoUrls` are already in scope.
    - Because the effect intentionally reads `existingLogoUrl`/`existingPhotoUrls` but must NOT re-run when they change (it is an unmount-only reset), suppress the exhaustive-deps lint with the same `// eslint-disable-next-line react-hooks/exhaustive-deps` directive already used elsewhere in this codebase, placed on the line immediately before the closing `}, [])`.

    Do NOT alter the existing instant SET_MEDIA effect (lines 82-90), the two revocation effects (lines 65-67, 74-76), `handleNext`, `handleSave`, or any render markup — this is purely additive. Do NOT introduce a `RESET` dispatch (the dead RESET action / WR-01 is explicitly out of scope; SET_MEDIA replacement is the chosen mechanism). Do NOT touch hinnasto/aukioloajat/yhteystiedot — they are unaffected (no blob URLs).

    Note the edge case this correctly handles: if the user staged a logo and the Mediat step's last persisted value is `existingLogoUrl = null`, the unmount dispatch sets `logo: null`, so the preview falls back to the no-logo state rather than a broken blob image. If a previously-saved logo existed (`existingLogoUrl` is a real https URL), the preview falls back to that persisted logo. Both are the correct "not stale data" outcomes per criterion #4.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `app/business/onboarding/StepMediat.tsx` contains a NEW `useEffect` with an empty dependency array `[]` whose returned cleanup function calls `dispatch({ type: 'SET_MEDIA', payload: { logo: ..., photos: ... } })` using `existingLogoUrl` (fallback `?? null`) and `existingPhotoUrls` — NOT `logoPreviewUrl` and NOT `stagedPreviewUrls` (the payload must contain zero blob: URLs).
    - The new unmount effect is declared AFTER the existing blob-revocation effects (lines ~65-67, ~74-76) and after the instant SET_MEDIA effect (lines ~82-90).
    - An `// eslint-disable-next-line react-hooks/exhaustive-deps` directive precedes the new effect's `[]` deps line.
    - The two existing revocation effects and the instant SET_MEDIA effect are UNCHANGED.
    - No `RESET` dispatch is introduced anywhere in the file.
    - `npx tsc --noEmit -p tsconfig.json` produces zero errors.
    - Behavior (human spot-check, see Human Verification below): select a logo on the Mediat step, navigate to the Hinnasto step before saving — the live preview sidebar (desktop) / toggle (mobile) shows the persisted logo or the no-logo state, NOT a broken `<img src="blob:...">`.
  </acceptance_criteria>
  <done>Leaving the Mediat step with an unsaved staged logo/photo no longer leaves a revoked blob: URL in LivePreviewContext; the live preview shows the last-known persisted media (or no-media), satisfying LIVEPREV-04 / criterion #4 for the media field path.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.json` is clean (no new type errors introduced).
- `grep -n "SET_MEDIA" app/business/onboarding/StepMediat.tsx` shows TWO dispatch sites: the original instant effect (logoPreviewUrl/stagedPreviewUrls) and the new unmount fallback effect (existingLogoUrl/existingPhotoUrls).
- `grep -n "RESET" app/business/onboarding/StepMediat.tsx` shows ZERO matches (RESET is out of scope).
- The instant SET_MEDIA effect, both blob-revocation cleanup effects, `handleNext`, and `handleSave` are byte-for-byte unchanged.
</verification>

<success_criteria>
LIVEPREV-04 / criterion #4 is fully met for the media field path: a business owner who stages a logo/photo on the Mediat step and navigates away before saving sees the persisted media (or no-media) in the live preview — never a broken blob image — for the remainder of the wizard session. Pricing, hours, and contact field paths remain unaffected and correct.
</success_criteria>

<human_verification>
After the fix lands, a human should spot-check once (this is the regression scenario both the code review and the verification flagged; it does not need an automated test):
1. Start the business onboarding wizard (or EditMode) and go to the Mediat step.
2. Select a logo file (do NOT save / do NOT click Seuraava).
3. Navigate to the Hinnasto step.
4. Confirm the live preview sidebar (desktop) or the Esikatselu toggle (mobile) shows the previously-persisted logo or the no-logo state — NOT a broken image placeholder.
</human_verification>

<output>
Create `.planning/phases/51-live-esikatselu-velhossa/51-05-SUMMARY.md` when done.

Artifacts this plan produces: none new (no new files, no new exported symbols). This is a bugfix adding one unmount-time `useEffect` to the existing `app/business/onboarding/StepMediat.tsx`.
</output>
