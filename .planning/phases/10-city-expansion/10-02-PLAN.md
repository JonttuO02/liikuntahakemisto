---
phase: 10-city-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - app/api/admin/sync-paikat/route.ts
autonomous: true
requirements:
  - DATA-05
  - DATA-06

must_haves:
  truths:
    - "GET /api/admin/sync-paikat?kaupunki=Helsinki fetches Helsinki venues and writes kaupunki='Helsinki' to DB"
    - "GET /api/admin/sync-paikat?kaupunki=Turku fetches Turku venues and writes kaupunki='Turku' to DB"
    - "GET /api/admin/sync-paikat (no param) falls back to Tampere — existing behavior unchanged"
    - "Helsinki/Turku sync does not overwrite Tampere rows (onConflict: place_id is preserved)"
    - "SPORT_LAJIT replaces SPORT_QUERIES — queries are built dynamically as laji + kaupunki"
    - "parseOsoite filters the kaupunki name dynamically, not the hardcoded /tampere/i pattern"
    - "fetchSportQuery uses city coordinates from SUOMI_KAUPUNGIT, not the hardcoded TAMPERE constant"
  artifacts:
    - path: "app/api/admin/sync-paikat/route.ts"
      provides: "Parameterized multi-city sync route"
      contains: "SPORT_LAJIT"
  key_links:
    - from: "app/api/admin/sync-paikat/route.ts"
      to: "lib/constants.ts"
      via: "import SUOMI_KAUPUNGIT"
      pattern: "SUOMI_KAUPUNGIT"
---

<objective>
Extend the sync-paikat admin route to accept a `?kaupunki=` query parameter, making it capable of syncing Helsinki and Turku venues without touching Tampere data.

Purpose: DATA-05 and DATA-06 require Helsinki and Turku venues in the database. The route currently hardcodes Tampere in three places: the search queries, the Google Places location coordinates, and the DB column value. This plan removes all three hardcodings.

Output: app/api/admin/sync-paikat/route.ts — parameterized, city-agnostic sync route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-city-expansion/10-CONTEXT.md
@.planning/phases/10-city-expansion/10-01-SUMMARY.md

<interfaces>
From lib/constants.ts (after Plan 01):
```typescript
export const TAMPERE = { lat: 61.4978, lng: 23.761 }

export const SUOMI_KAUPUNGIT: { nimi: string; lat: number; lng: number }[] = [
  { nimi: 'Helsinki', lat: 60.1699, lng: 24.9384 },
  { nimi: 'Tampere',  lat: 61.4978, lng: 23.7610 },
  { nimi: 'Turku',    lat: 60.4518, lng: 22.2666 },
  // ... 22 more cities
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize sync-paikat route for multi-city support</name>
  <files>app/api/admin/sync-paikat/route.ts</files>
  <read_first>
    app/api/admin/sync-paikat/route.ts — read full current implementation before any edits; understand all hardcoded Tampere references
    lib/constants.ts — verify SUOMI_KAUPUNGIT export exists (created in Plan 01)
  </read_first>
  <action>
    Make the following changes to app/api/admin/sync-paikat/route.ts:

    1. IMPORTS: Replace `import { TAMPERE } from '@/lib/constants'` with `import { SUOMI_KAUPUNGIT } from '@/lib/constants'`. Remove the SportQuery interface and SPORT_QUERIES array entirely.

    2. SPORT_LAJIT: Add a new string array `const SPORT_LAJIT: string[] = ['padel', 'uimahalli', 'jooga', 'kiipeily bouldering', 'jääkiekko halli', 'kuntosali fitness', 'tennis', 'liikuntahalli']` (laji strings only — no city; per D-05). The `laji` stored in DB for each entry uses only the first word of each string: 'padel', 'uinti', 'jooga', 'kiipeily', 'jääkiekko', 'kuntosali', 'tennis', 'liikuntahalli'. Preserve the DB laji mapping by building a lookup: the laji in the upsert row uses only the first word of each SPORT_LAJIT entry (e.g. 'kiipeily bouldering' → 'kiipeily', 'kuntosali fitness' → 'kuntosali', 'jääkiekko halli' → 'jääkiekko', 'uimahalli' → 'uinti'). Actually, keep the existing DB laji values — define a separate mapping object or keep the hakutermi/laji pair approach but build the hakutermi dynamically. The cleanest approach: define `const SPORT_LAJIT: { hakutermi: string; laji: string }[]` where `hakutermi` is the laji-only part (no city) and `laji` is the DB value — this preserves the existing laji→hakutermi mapping pattern while removing the city from the array. Then in fetchSportQuery, build the full query as `${q.hakutermi} ${kaupunki}`.

    3. fetchSportQuery SIGNATURE: Change signature to `async function fetchSportQuery(hakutermi: string, laji: string, apiKey: string, coords: { lat: number; lng: number })`. Replace `${TAMPERE.lat},${TAMPERE.lng}` in the `location` param with `${coords.lat},${coords.lng}`.

    4. parseOsoite SIGNATURE: Change to `function parseOsoite(name: string, formattedAddress: string, kaupunki: string): string | null`. Replace the hardcoded `!/tampere/i.test(p)` filter with `p.toLowerCase() !== kaupunki.toLowerCase()` (per D-03). Keep the `Finland` and `Suomi` filters.

    5. GET HANDLER: Read `?kaupunki` query param from the URL: `const kaupunki = new URL(req.url).searchParams.get('kaupunki') ?? 'Tampere'`. Look up city coordinates: `const cityCoords = SUOMI_KAUPUNGIT.find(c => c.nimi === kaupunki) ?? { lat: 61.4978, lng: 23.7610 }`. Pass `cityCoords` to each `fetchSportQuery` call. Update the SPORT_QUERIES.map call to build hakutermi as `${q.hakutermi} ${kaupunki}`.

    6. rivit MAPPING: Replace `kaupunki: 'Tampere'` with `kaupunki: kaupunki` (the resolved param value). Update `parseOsoite` call to `parseOsoite(p.name, p.formatted_address, kaupunki)`.

    7. UPSERT: The existing `onConflict: 'place_id'` stays unchanged — this is what prevents Helsinki/Turku data from overwriting Tampere rows (per D-04).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v "^//" app/api/admin/sync-paikat/route.ts | grep -c "Tampere"` returns 1 (only the fallback default value in the GET handler, e.g. `?? 'Tampere'`)
    - `grep "SPORT_LAJIT" app/api/admin/sync-paikat/route.ts` matches at least one line (array definition)
    - `grep "SPORT_QUERIES" app/api/admin/sync-paikat/route.ts` returns empty (old array removed)
    - `grep "onConflict.*place_id" app/api/admin/sync-paikat/route.ts` matches (upsert conflict key preserved)
    - `grep "kaupunki.toLowerCase()" app/api/admin/sync-paikat/route.ts` matches in parseOsoite (dynamic filter per D-03)
    - `grep "SUOMI_KAUPUNGIT" app/api/admin/sync-paikat/route.ts` matches (import and usage)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>
    sync-paikat route accepts ?kaupunki param, looks up coordinates from SUOMI_KAUPUNGIT, builds queries dynamically, writes the correct kaupunki value to DB, and filters city name from addresses dynamically. Tampere hardcodings are gone except the fallback default.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Sync route parameterization: the route now accepts ?kaupunki=Helsinki, ?kaupunki=Turku, and defaults to Tampere. SPORT_LAJIT replaces SPORT_QUERIES.
  </what-built>
  <how-to-verify>
    Run a test sync for Helsinki (use your ADMIN_SECRET):

    ```
    curl -H "Authorization: Bearer $ADMIN_SECRET" "http://localhost:3000/api/admin/sync-paikat?kaupunki=Helsinki"
    ```

    Expected response: JSON with `loydetty`, `tallennettu`, `website_loydetty` fields and no error key.

    Then check Supabase: query `liikuntapaikat` table and confirm rows with `kaupunki = 'Helsinki'` exist and have Helsinki-area addresses (not Tampere addresses).

    Also verify Tampere rows are still present: `SELECT count(*) FROM liikuntapaikat WHERE kaupunki = 'Tampere'` should still return the original count.
  </how-to-verify>
  <resume-signal>Type "approved" if Helsinki venues appear in DB alongside intact Tampere rows, or describe what failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API caller → route | Authorization: Bearer header checked; untrusted kaupunki param |
| Route → Google Places API | Server-to-server, API key in env var |
| Route → Supabase | Service role key; upsert with place_id conflict resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | ?kaupunki param in sync route | mitigate | Param is used only in search query string and DB column — no SQL injection (Supabase SDK parameterizes). City lookup via find() on SUOMI_KAUPUNGIT falls back to Tampere coords if unknown city supplied; unknown city name simply writes that string to kaupunki column (acceptable for admin-only route) |
| T-10-02-02 | Information Disclosure | GOOGLE_PLACES_API_KEY in route | accept | Server-only env var, never exposed to client; existing pattern |
| T-10-02-SC | Tampering | No new npm installs | accept | No package manager invocations in this plan |
</threat_model>

<verification>
- TypeScript: `npx tsc --noEmit` — zero errors
- Source assertions: `grep "SPORT_QUERIES" app/api/admin/sync-paikat/route.ts` — empty (old array gone)
- Source assertions: `grep "onConflict.*place_id" app/api/admin/sync-paikat/route.ts` — present
- Human checkpoint: Helsinki sync returns valid JSON and DB rows appear with kaupunki='Helsinki'
</verification>

<success_criteria>
- /api/admin/sync-paikat?kaupunki=Helsinki successfully syncs Helsinki venues to DB
- /api/admin/sync-paikat?kaupunki=Turku successfully syncs Turku venues to DB
- /api/admin/sync-paikat (no param) still syncs Tampere as before
- Existing Tampere rows are not overwritten (upsert deduplicates by place_id)
- No hardcoded city name except the fallback default 'Tampere' in the GET handler
</success_criteria>

<output>
Create `.planning/phases/10-city-expansion/10-02-SUMMARY.md` when done
</output>
