---
phase: 53-google-places-datan-ja-synkkauksen-poisto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/admin/sync-paikat/route.ts
  - app/api/admin/__tests__/sync-paikat-filter.test.ts
autonomous: true
requirements: [DATA-11]
tags: [sync-removal, decommission, admin-route, google-places]

must_haves:
  truths:
    - "Calling /api/admin/sync-paikat returns 404 (route file no longer exists)"
    - "No source file under app/ or lib/ imports from or references the sync-paikat route"
    - "The full test suite passes after the route and its filter test are removed"
  artifacts:
    - path: "app/api/admin/sync-paikat/route.ts"
      provides: "MUST NOT EXIST after this plan — route fully deleted"
      contains: "DELETED"
    - path: "app/api/admin/__tests__/sync-paikat-filter.test.ts"
      provides: "MUST NOT EXIST after this plan — obsolete filter test deleted"
      contains: "DELETED"
  key_links:
    - from: "app/ (entire tree)"
      to: "app/api/admin/sync-paikat/route.ts"
      via: "no remaining import or string reference to sync-paikat after deletion"
      pattern: "sync-paikat"
---

<objective>
Decommission the Google Places sync mechanism by deleting the `/api/admin/sync-paikat` route file and its now-obsolete unit test. This satisfies DATA-11 ("`/api/admin/sync-paikat`-reitti ja ajastus poistettu kokonaan koodista").

Per CONTEXT.md D-04/D-05: there is no Vercel account link, no `vercel.json`, and no external scheduler (confirmed `ls vercel.json` → not found, and `grep -rn "sync-paikat"` finds only the test file's own comment). Decommissioning therefore reduces to deleting the route file — no separate "disable the cron" step exists because there is nothing external to race against. This is the safe, reversible code-only step that must land before the irreversible data-deletion migration runs (ARCHITECTURE.md "Migration Order" step 2).

Purpose: Stop the Google Places API call path entirely; remove the only consumer of `GOOGLE_PLACES_API_KEY` (server-only) so it can be retired in a later cleanup.
Output: Two deleted files; route returns 404; suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-google-places-datan-ja-synkkauksen-poisto/53-CONTEXT.md
@.planning/phases/53-google-places-datan-ja-synkkauksen-poisto/53-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- **DELETED:** `app/api/admin/sync-paikat/route.ts` — the admin GET route (was gated by `ADMIN_SECRET` bearer auth, no JWT) that performed Google Places Text Search + Place Details and upserted `liikuntapaikat`.
- **DELETED:** `app/api/admin/__tests__/sync-paikat-filter.test.ts` — the inline pure-function unit test for the route's `business_managed`-based pre-filter (the filter logic D-02 explicitly identifies as the wrong provenance signal).
- No new symbols, routes, CLI flags, functions, or env vars are introduced. This plan is purely subtractive.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Confirm sync-paikat has no remaining code consumers, then delete the route and its test</name>
  <read_first>
    - app/api/admin/sync-paikat/route.ts (the route being deleted — confirm it is the `ADMIN_SECRET` bearer-auth GET route, and the only consumer of `GOOGLE_PLACES_API_KEY`)
    - app/api/admin/__tests__/sync-paikat-filter.test.ts (the test being deleted — confirm it imports nothing from the route file; per PATTERNS.md it duplicates `buildSyncResults` inline so deletion breaks no imports)
    - app/api/admin/approve/route.ts (reference only — confirms retained admin routes use JWT + `profiles.is_admin`, NOT the `ADMIN_SECRET` bearer pattern, so deleting sync-paikat orphans no shared auth helper)
    - .planning/phases/53-google-places-datan-ja-synkkauksen-poisto/53-CONTEXT.md (D-04, D-05 — no external scheduler exists; route deletion is the whole decommission)
  </read_first>
  <action>
    First run a reference scan to confirm no other source references the route before deleting: search `app/` and `lib/` (`*.ts` and `*.tsx`) for the literal `sync-paikat`. The only expected hit is the comment inside `app/api/admin/__tests__/sync-paikat-filter.test.ts` itself. If any OTHER file (a route, lib module, or component) references `sync-paikat`, STOP and report the unexpected consumer rather than deleting — DATA-11 assumes the route is reference-isolated per D-05.

    After the scan confirms isolation, delete both files in full: `app/api/admin/sync-paikat/route.ts` and `app/api/admin/__tests__/sync-paikat-filter.test.ts`. Per CONTEXT.md D-02 and PATTERNS.md, the test validates the `business_managed`-based filter that this milestone is explicitly moving away from (the correct predicate is provenance via `business_paikka_links`, authored in Plan 02), so the test is removed rather than adapted — it tests behavior that no longer exists once the route is gone.

    Do NOT touch `GOOGLE_PLACES_API_KEY` env var, `business_managed` column, or any `business_managed` reference in components/routes (per D-06 those are out of scope for this phase). Do NOT modify `app/api/admin/approve/route.ts` or any other admin route.
  </action>
  <verify>
    <automated>test ! -f app/api/admin/sync-paikat/route.ts && test ! -f app/api/admin/__tests__/sync-paikat-filter.test.ts && echo DELETED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/admin/sync-paikat/route.ts` does not exist (`test ! -f` returns success).
    - `app/api/admin/__tests__/sync-paikat-filter.test.ts` does not exist (`test ! -f` returns success).
    - A recursive search for `sync-paikat` across `app/` and `lib/` (`*.ts`, `*.tsx`) returns zero matches after deletion: `grep -rn "sync-paikat" app/ lib/ --include="*.ts" --include="*.tsx"` produces no output.
    - No file other than the two deleted files was modified (git status shows only deletions for this task).
  </acceptance_criteria>
  <done>Both files deleted; zero remaining `sync-paikat` references in app/ and lib/; no other files changed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the build and test suite are green after removal, and the route resolves to 404</name>
  <read_first>
    - package.json (confirm the test runner script — Vitest — and the build/typecheck script names before invoking them)
  </read_first>
  <action>
    Run the project's typecheck/build and the full Vitest suite to prove the deletions did not break any remaining code or leave a dangling import. The removed test (`sync-paikat-filter.test.ts`) self-contained its `buildSyncResults` helper, so no other suite should reference it; confirm the suite still passes with the test gone.

    The route returning 404 is structural: in Next.js App Router, deleting `app/api/admin/sync-paikat/route.ts` means the path `/api/admin/sync-paikat` no longer matches any route segment and Next.js serves its default 404 for it. There is no runtime route table to edit — verify by confirming the route directory is empty/absent (the build output will not list the route). A live HTTP 404 check against a running dev server is acceptable as supplementary manual evidence but is not required for the automated gate.

    Use the actual script names from package.json (do not assume); typically `npm run build` (or `npm run typecheck`) and `npm test` / `npx vitest run`.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The full Vitest suite passes with zero failures after both files are deleted (no suite references the removed test or route).
    - Typecheck/build completes with no errors referencing `sync-paikat` or a missing route module.
    - The directory `app/api/admin/sync-paikat/` contains no `route.ts` (route is structurally unreachable → 404), confirmed by `test ! -f app/api/admin/sync-paikat/route.ts`.
  </acceptance_criteria>
  <done>Vitest suite green; build/typecheck clean; route path structurally resolves to 404 (no route file present).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external caller → /api/admin/sync-paikat | Untrusted HTTP request previously authenticated by a static `ADMIN_SECRET` bearer token (no JWT/session). This boundary is being removed entirely. |
| code reference graph → deleted route | Any retained module that imported or invoked the route would break at build time if the route were a shared dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | sync-paikat route deletion orphaning a shared auth helper | mitigate | Task 1 runs a `grep -rn "sync-paikat" app/ lib/` reference scan BEFORE deletion and confirms (via PATTERNS.md) the `ADMIN_SECRET` bearer pattern is unique to this file — retained admin routes (`approve`, `reject`, `applications`) use JWT + `profiles.is_admin`, so no shared helper is orphaned. STOP-and-report if any unexpected consumer is found. |
| T-53-02 | Denial of Service | external scheduler still calling a now-404 route | accept | Per D-04 there is no `vercel.json`, no Vercel project link, and no external scheduler — confirmed by `ls vercel.json` (not found). A 404 on a dead route is a loud, harmless failure (research-preferred over silent success), and there is no live caller to fail. |
| T-53-03 | Information Disclosure | `GOOGLE_PLACES_API_KEY` left in env after its only consumer is gone | accept | Out of scope for DATA-11 (per CONTEXT.md Deferred Ideas — key removal is a noted follow-up, not scheduled). Leaving the unused server-only var carries no active exposure since no code path reads it after deletion; it is not logged or returned to any client. |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages — purely deletes two files and runs existing scripts. No package-legitimacy gate applies. |
</threat_model>

<verification>
- `grep -rn "sync-paikat" app/ lib/ --include="*.ts" --include="*.tsx"` returns nothing.
- `test ! -f app/api/admin/sync-paikat/route.ts` and `test ! -f app/api/admin/__tests__/sync-paikat-filter.test.ts` both succeed.
- `npx vitest run` exits 0 with all tests passing.
- Build/typecheck clean.
</verification>

<success_criteria>
DATA-11 satisfied: the sync-paikat route and (non-existent) schedule are removed entirely from code; the route path resolves to 404; no other code references it; suite and build remain green.
</success_criteria>

<output>
Create `.planning/phases/53-google-places-datan-ja-synkkauksen-poisto/53-01-SUMMARY.md` when done.
</output>
