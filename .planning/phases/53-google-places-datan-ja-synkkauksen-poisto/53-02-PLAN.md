---
phase: 53-google-places-datan-ja-synkkauksen-poisto
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260622120000_remove_google_places_data.sql
  - supabase/migrations/_audit/53-row-count-audit.sql
autonomous: true
requirements: [DATA-12]
tags: [data-deletion, migration, provenance, cascade, google-places, audit]

must_haves:
  truths:
    - "A new migration file exists whose DELETE predicate keeps every liikuntapaikat row that has a business_paikka_links row and deletes only rows with none"
    - "The deletion predicate uses business_paikka_links presence (NOT EXISTS), never business_managed"
    - "A standalone before/after row-count audit script exists covering liikuntapaikat, reviews, suosikit, business_paikka_links"
    - "The migration documents the full 5-table cascade chain and is idempotent (re-runnable as a no-op)"
  artifacts:
    - path: "supabase/migrations/20260622120000_remove_google_places_data.sql"
      provides: "Provenance-aware one-time deletion migration for pure-Google liikuntapaikat rows"
      contains: "NOT EXISTS"
      min_lines: 25
    - path: "supabase/migrations/_audit/53-row-count-audit.sql"
      provides: "Standalone before/after row-count audit queries (run manually, not by the migration runner)"
      contains: "business_paikka_links"
      min_lines: 15
  key_links:
    - from: "supabase/migrations/20260622120000_remove_google_places_data.sql"
      to: "business_paikka_links"
      via: "DELETE predicate references business_paikka_links presence, never business_managed"
      pattern: "business_paikka_links"
    - from: "supabase/migrations/20260622120000_remove_google_places_data.sql"
      to: "liikuntapaikat"
      via: "DELETE FROM liikuntapaikat gated by NOT EXISTS subquery"
      pattern: "DELETE FROM liikuntapaikat"
---

<objective>
Author (but do NOT execute) the provenance-aware deletion migration and a standalone row-count audit script. This is the file-authoring half of DATA-12 ("Google Places -peräiset liikuntapaikka-rivit poistettu tietokannasta provenance-tarkistuksella `business_paikka_links.link_type`, ei vain `business_managed`"). The actual DB execution and audit review is gated in Plan 03.

Per CONTEXT.md D-01/D-02/D-03 and PITFALLS.md Pitfall 1: pure-Google rows are rows with NO `business_paikka_links` row at all. These are hard-deleted. The deletion predicate is provenance-based via `business_paikka_links` presence — it does NOT look at `business_managed` (a `link_type='claim'` row carries Google-origin base data but must be KEPT because it is claimed). All `reviews`/`suosikit` cascade losses on deleted rows are confirmed test-only and acceptable (D-03).

This plan only WRITES SQL files. No database is touched here — that is the irreversible, human-gated step in Plan 03.

Purpose: Encode the safe deletion contract as reviewable SQL before anything irreversible runs.
Output: One migration file + one audit script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-google-places-datan-ja-synkkauksen-poisto/53-CONTEXT.md
@.planning/phases/53-google-places-datan-ja-synkkauksen-poisto/53-PATTERNS.md
@.planning/research/PITFALLS.md
@supabase/migrations/20260612000000_cleanup_test_accounts.sql
@supabase/migrations/20260605000000_business_accounts.sql
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- **NEW migration:** `supabase/migrations/20260622120000_remove_google_places_data.sql` — one-time, irreversible, narratively-commented data-deletion migration. Deletes `liikuntapaikat` rows with NO matching `business_paikka_links` row (pure-Google provenance per D-02), triggering documented `ON DELETE CASCADE` through `reviews`, `suosikit`, `business_paikka_links`, `onboarding_draft`, `business_branding.paikka_id`. Idempotent (re-run = no-op).
- **NEW audit script:** `supabase/migrations/_audit/53-row-count-audit.sql` — standalone before/after row-count `SELECT`s for `liikuntapaikat`, `reviews`, `suosikit`, `business_paikka_links`, plus a per-category breakdown listing how many rows are pure-Google (deletion target) vs. linked (kept). NOT picked up by the Supabase migration runner (lives under `_audit/`, an underscore-prefixed non-migration directory) — run manually before/after the push in Plan 03.
- No new app code, routes, functions, CLI flags, or env vars.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Write the provenance-aware deletion migration following the codebase's narrative-comment convention</name>
  <read_first>
    - supabase/migrations/20260612000000_cleanup_test_accounts.sql (THE structural analog — copy its header/cascade-chain/idempotency comment shape exactly: phase+requirement IDs, why-safe, exact cascade chain, idempotency guarantee, then the DELETE)
    - supabase/migrations/20260605000000_business_accounts.sql (lines 55-65 — confirms `business_paikka_links.paikka_id BIGINT NOT NULL REFERENCES liikuntapaikat(id) ON DELETE CASCADE`, `UNIQUE(paikka_id)`, `link_type CHECK IN ('claim','created')` — the schema the predicate joins against)
    - .planning/research/PITFALLS.md (Pitfall 1 — the SELECT-first dry-run pattern and the 5-table cascade chain; the `business_managed`-is-not-provenance reasoning)
    - .planning/phases/53-google-places-datan-ja-synkkauksen-poisto/53-CONTEXT.md (D-01 hard-delete, D-02 exact predicate, D-03 cascade-loss-acceptable)
    - .planning/phases/53-google-places-datan-ja-synkkauksen-poisto/53-PATTERNS.md (the migration analog section with the verbatim cascade-chain comment block to adapt)
  </read_first>
  <action>
    Create `supabase/migrations/20260622120000_remove_google_places_data.sql`. Use the timestamp prefix `20260622120000` (date 2026-06-22, ordered after the latest existing migration `20260617000000_renumber_onboarding_steps.sql`). Follow the narrative-comment convention from `20260612000000_cleanup_test_accounts.sql` exactly — every non-trivial migration in this repo opens with a comment block.

    Header comment block must state: (1) `Phase 53 DATA-12: delete pure-Google liikuntapaikat rows.` and reference DATA-11/DATA-12; (2) why it is safe — the predicate keeps every row that has any `business_paikka_links` row regardless of `link_type` ('claim' or 'created') or `business_managed` value, so claimed/created venues, their reviews, and favorites survive; (3) the exact cascade chain (see below); (4) idempotency — a `NOT EXISTS` predicate is naturally a no-op once pure-Google rows are gone; (5) an explicit IRREVERSIBLE warning that this is a one-time mass deletion and must only run after the Plan 03 row-count audit baseline is captured and reviewed.

    Document the cascade chain in the comment block as the 5 `ON DELETE CASCADE` FKs that fire when a `liikuntapaikat` row is deleted (each verified against its source migration):
      liikuntapaikat to reviews (paikka_id) [20260528000000_reviews.sql]
      liikuntapaikat to suosikit (paikka_id) [20260523_suosikit.sql]
      liikuntapaikat to business_paikka_links (paikka_id) [20260605000000_business_accounts.sql]
      liikuntapaikat to onboarding_draft (paikka_id) [20260606000000_onboarding.sql]
      liikuntapaikat to business_branding (paikka_id) [20260616100000_business_branding_plural_and_paikka_scoping.sql]
    Note in the comment that rows with a `business_paikka_links` row are excluded from deletion by construction, so no claimed/created venue's dependents cascade.

    The single deletion statement must be `DELETE FROM liikuntapaikat WHERE NOT EXISTS (SELECT 1 FROM business_paikka_links bpl WHERE bpl.paikka_id = liikuntapaikat.id);`. Use the correlated `NOT EXISTS` form (not `NOT IN`) — `NOT IN` against a subquery silently returns zero rows if any subquery value is NULL, and although `business_paikka_links.paikka_id` is `NOT NULL` today, `NOT EXISTS` is NULL-safe by construction and is the more robust idempotent form. The predicate must NOT reference `business_managed` anywhere (D-02 / PITFALLS Pitfall 1) and must NOT inspect `link_type` value (presence of any row, 'claim' or 'created', means keep).

    Do NOT add any `place_id IS NOT NULL` clause or `business_managed = false` clause — the sole provenance signal is `business_paikka_links` row presence. Do NOT include the audit SELECT statements inside this migration file (they live in the separate audit script from Task 2, so the migration runner only ever executes the one DELETE). Do NOT drop or alter `business_managed`, `place_id`, or any column (D-06).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260622120000_remove_google_places_data.sql && grep -q "NOT EXISTS" supabase/migrations/20260622120000_remove_google_places_data.sql && grep -q "DELETE FROM liikuntapaikat" supabase/migrations/20260622120000_remove_google_places_data.sql && ! grep -v '^[[:space:]]*--' supabase/migrations/20260622120000_remove_google_places_data.sql | grep -q "business_managed" && echo MIGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260622120000_remove_google_places_data.sql` exists with a timestamp prefix lexicographically greater than `20260617000000`.
    - The file contains exactly one `DELETE FROM liikuntapaikat` statement and it uses a `NOT EXISTS (SELECT 1 FROM business_paikka_links ...)` predicate.
    - Excluding comment lines (lines whose first non-whitespace is `--`), the word `business_managed` does NOT appear in any executable SQL: `grep -v '^[[:space:]]*--' <file> | grep -c business_managed` returns 0.
    - No `place_id` filter and no `business_managed = false` filter appears in the WHERE clause.
    - The header comment block documents all 5 cascade-chain FKs and states the IRREVERSIBLE warning and the idempotency guarantee.
    - The file contains no `SELECT count` audit queries (those live only in the audit script).
  </acceptance_criteria>
  <done>Migration file created with NULL-safe provenance-based `NOT EXISTS` deletion predicate, full narrative comment block, 5-FK cascade documentation, and no `business_managed` in executable SQL.</done>
</task>

<task type="auto">
  <name>Task 2: Write the standalone before/after row-count audit script (D-07)</name>
  <read_first>
    - .planning/phases/53-google-places-datan-ja-synkkauksen-poisto/53-CONTEXT.md (D-07 — row-count SELECT before and after, comparing liikuntapaikat/reviews/suosikit/business_paikka_links; no pg_dump required)
    - .planning/research/PITFALLS.md (Pitfall 1 — the "categorize before deleting anything" SELECT-first dry-run pattern showing the per-category breakdown by link_type presence)
    - supabase/migrations/20260605000000_business_accounts.sql (confirm column/table names used in the audit joins: business_paikka_links.paikka_id, link_type)
  </read_first>
  <action>
    Create `supabase/migrations/_audit/53-row-count-audit.sql`. The `_audit/` subdirectory with the leading underscore keeps this file OUT of the Supabase migration runner's pickup (it only executes top-level timestamped `.sql` files), so this script is run manually by the operator in Plan 03, never auto-applied.

    The script contains a header comment explaining it is the DATA-12 / D-07 row-count audit, run manually BEFORE and AFTER applying `20260622120000_remove_google_places_data.sql`, comparing the two outputs to confirm only intended pure-Google rows (and their cascade dependents) dropped.

    Include these query blocks:
      1. Baseline totals: a single `SELECT` returning the row counts of `liikuntapaikat`, `reviews`, `suosikit`, and `business_paikka_links` (use scalar subqueries or UNION ALL so it is one readable result set with a label column per table).
      2. Provenance breakdown (the dry-run categorization): a `SELECT` counting `liikuntapaikat` rows split into "pure-Google (deletion target)" = rows where `NOT EXISTS (SELECT 1 FROM business_paikka_links bpl WHERE bpl.paikka_id = liikuntapaikat.id)`, vs. "linked-kept" = rows that DO have a `business_paikka_links` row. Optionally further break the linked group by `link_type` ('claim' vs 'created') for visibility.
      3. Expected-delta documentation: a comment stating that after the migration runs, the "pure-Google" count must be 0, the "linked-kept" `liikuntapaikat` count must be unchanged, and `business_paikka_links` count must be unchanged (no claimed/created link cascaded). `reviews`/`suosikit` may drop only by the cascade tied to deleted pure-Google rows (test-only data per D-03).

    The predicate in the audit MUST match the migration's predicate exactly (`NOT EXISTS` against `business_paikka_links`, never `business_managed`) so the before-count of the deletion target equals the number of rows the migration will remove. Do NOT include any DELETE or mutating statement in this file — it is read-only.
  </action>
  <verify>
    <automated>test -f supabase/migrations/_audit/53-row-count-audit.sql && grep -q "business_paikka_links" supabase/migrations/_audit/53-row-count-audit.sql && grep -qi "suosikit" supabase/migrations/_audit/53-row-count-audit.sql && grep -qi "reviews" supabase/migrations/_audit/53-row-count-audit.sql && ! grep -iq "DELETE FROM" supabase/migrations/_audit/53-row-count-audit.sql && echo AUDIT_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/_audit/53-row-count-audit.sql` exists under the underscore-prefixed `_audit/` directory (not a runner-pickup path).
    - The script references all four audited tables: `liikuntapaikat`, `reviews`, `suosikit`, `business_paikka_links`.
    - The provenance breakdown query uses the same `NOT EXISTS (... business_paikka_links ...)` predicate as the migration, and does NOT reference `business_managed` in any executable query.
    - The file contains zero mutating statements: `grep -i "DELETE FROM\|UPDATE \|INSERT INTO\|DROP \|TRUNCATE" <file>` returns no executable (non-comment) match.
    - A comment documents the expected post-migration deltas (pure-Google count to 0; linked-kept and business_paikka_links unchanged).
  </acceptance_criteria>
  <done>Read-only audit script created covering all four tables with a provenance breakdown matching the migration predicate and documented expected deltas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → liikuntapaikat (+ 5 cascade dependents) | The DELETE predicate is the only thing standing between "purge pure-Google rows" and "accidentally cascade-destroy claimed venues, reviews, favorites, drafts, branding." A wrong predicate is an irreversible data-loss boundary. |
| audit script → production read | Read-only SELECTs; the boundary is that the audit must accurately reflect what the migration will delete, or the operator's go/no-go review in Plan 03 is based on wrong numbers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04 | Tampering | deletion predicate accidentally matching claimed/business rows | mitigate | The predicate is `NOT EXISTS (SELECT 1 FROM business_paikka_links bpl WHERE bpl.paikka_id = liikuntapaikat.id)` — link-type-agnostic, so ANY `business_paikka_links` row (claim OR created) excludes the venue from deletion. `business_paikka_links.paikka_id` is `NOT NULL UNIQUE` (verified in 20260605000000), so the join is well-defined. Task 1 acceptance criteria assert `business_managed` is absent from executable SQL, preventing the PITFALLS Pitfall 1 mis-filter. |
| T-53-05 | Tampering | `NOT IN` NULL-collapse silently deleting nothing (or wrong set) | mitigate | Migration uses correlated `NOT EXISTS`, not `NOT IN` — NULL-safe by construction; acceptance criteria grep-asserts `NOT EXISTS` is present. |
| T-53-06 | Repudiation / Information | audit baseline not matching actual deletion scope | mitigate | The audit script's deletion-target predicate is required (acceptance criteria) to be the identical `NOT EXISTS` form, so the pre-migration "pure-Google" count equals the rows the migration removes — giving the Plan 03 operator a verifiable before/after delta. |
| T-53-07 | Elevation / Tampering | audit script accidentally mutating data | mitigate | Acceptance criteria assert zero `DELETE/UPDATE/INSERT/DROP/TRUNCATE` executable statements in the audit file; it is read-only by gate. |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages — it writes two SQL files only. No package-legitimacy gate applies. |
</threat_model>

<verification>
- Migration file exists with timestamp > `20260617000000`, contains one `DELETE FROM liikuntapaikat` with `NOT EXISTS` predicate, no `business_managed` in executable SQL, full cascade + idempotency + irreversible-warning comment block.
- Audit script exists under `_audit/`, references all four tables, uses matching `NOT EXISTS` predicate, contains no mutating statements.
- Neither file is executed against any database in this plan (DB push is Plan 03).
</verification>

<success_criteria>
File-authoring half of DATA-12 complete: a reviewable, provenance-safe, idempotent deletion migration and a matching read-only row-count audit script exist on disk, ready for the human-gated push in Plan 03. No database touched.
</success_criteria>

<output>
Create `.planning/phases/53-google-places-datan-ja-synkkauksen-poisto/53-02-SUMMARY.md` when done.
</output>
