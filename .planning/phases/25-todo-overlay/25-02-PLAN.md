---
phase: 25-todo-overlay
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - app/components/Etusivu.tsx
  - app/components/DiagonaalKortti.tsx
autonomous: true
requirements:
  - TODO-07

must_haves:
  truths:
    - "Deleting a place from the TO DO overlay shows an inline 'Kävikö paikassa?' prompt in the same card slot"
    - "The prompt only appears for logged-in users (supabaseUser !== null); non-authenticated users get silent deletion"
    - "Tapping 'Kyllä' replaces the prompt with an inline star-rating + comment form"
    - "Tapping 'Ei' dismisses the slot without showing a form"
    - "Submitting the review calls the reviews Supabase upsert and removes the slot on success"
    - "Tapping 'Ohita' dismisses the form slot without submitting"
  artifacts:
    - path: "app/components/Etusivu.tsx"
      provides: "KavikoPaikassaPrompt, InlineReviewExpanded, inline review flow"
      contains: "pendingReviewPaikkaId"
    - path: "app/components/DiagonaalKortti.tsx"
      provides: "onToggleTodo optional callback prop"
      contains: "onToggleTodo"
  key_links:
    - from: "DiagonaalKortti delete trigger (onToggleTodo prop)"
      to: "handleOverlayDelete in Etusivu.tsx"
      via: "overlay passes handleOverlayDelete as onToggleTodo; elsewhere passes toggleTodo"
      pattern: "onToggleTodo"
    - from: "handleOverlayDelete"
      to: "pendingReviewPaikkaId state"
      via: "sets pendingReviewPaikkaId when todoOpen && supabaseUser !== null"
      pattern: "setPendingReviewPaikkaId"
    - from: "KavikoPaikassaPrompt 'Kyllä' button"
      to: "reviewPaikkaId state"
      via: "onClick sets reviewPaikkaId to show InlineReviewExpanded"
      pattern: "setReviewPaikkaId"
    - from: "InlineReviewExpanded submit"
      to: "supabase.from('reviews').upsert"
      via: "handleInlineReviewSubmit"
      pattern: "reviews.*upsert"
---

<objective>
Add KavikoPaikassaPrompt and InlineReviewExpanded to the TodoOverlay in Etusivu.tsx. When a logged-in user removes a place from their TO DO list via the overlay, the card slot transforms inline to a "Kävikö paikassa?" prompt. Confirming expands a simplified star + comment review form. Non-authenticated users get silent deletion.

Purpose: Captures review intent at the moment of intent signal (removing a place from the TO DO list), without interrupting the overlay experience.

Output: Modified DiagonaalKortti.tsx with optional `onToggleTodo` prop; modified Etusivu.tsx with handleOverlayDelete, pendingReviewPaikkaId and reviewPaikkaId state, KavikoPaikassaPrompt motion.div, InlineReviewExpanded motion.div, and Supabase reviews upsert logic — all inline in the overlay card list.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-todo-overlay/25-CONTEXT.md
@.planning/phases/25-todo-overlay/25-UI-SPEC.md
@.planning/phases/25-todo-overlay/25-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts from Plan 01 output and existing code. -->

From app/components/Etusivu.tsx (after Plan 01):
```typescript
// NEW state from Plan 01:
const [todoOpen, setTodoOpen] = useState(false)

// EXISTING state to use:
const [supabaseUser, setSupabaseUser] = useState<{ id: string; email?: string } | null>(null)
const [todoIds, setTodoIds] = useState<Set<number>>(new Set())
// toggleTodo(id: number) — existing function handles Supabase delete/insert for suosikit table

// closeOverlays (from Plan 01):
function closeOverlays() { setRightOpen(false); setTodoOpen(false) }

// Supabase client:
import { createBrowserSupabase } from '@/lib/supabaseSSR'
// Usage: const supabase = createBrowserSupabase()
// reviews upsert: supabase.from('reviews').upsert({ user_id, paikka_id, rating, teksti, ... }, { onConflict: 'user_id,paikka_id' })
```

From app/components/DiagonaalKortti.tsx (existing props — MUST ADD onToggleTodo):
```typescript
// Current props interface (read the file to find exact interface name):
interface DiagonaalKorttiProps {
  paikka: Liikuntapaikka
  distanceStr?: string
  onShowMap?: (paikka: Liikuntapaikka) => void
  onCardClick?: () => void
  // ADD: onToggleTodo?: (id: number) => void
}
// The component internally calls toggleTodo from context or prop — read the file to confirm current pattern
```

From app/components/ReviewForm.tsx (pattern reference — do NOT import ReviewForm):
```typescript
// The full form fields: rating (StarPicker), teksti, visitDate, crowdRating, isAnonymous
// InlineReviewExpanded uses ONLY: rating + teksti
// upsert payload minimum: { user_id: string, paikka_id: number, rating: number, teksti: string,
//   is_anonymous: false, reviewer_name: string|null, visit_date: null, crowd_rating: null }
```

From app/components/StarPicker.tsx (already importable):
```typescript
// Props: value: number, onChange: (n: number) => void
// Renders 5 star buttons with amber fill
import StarPicker from './StarPicker'
```

From Plan 01 — TodoOverlay JSX structure (Plan 01 created):
```tsx
// The overlay card list currently renders:
{todoPaikat.map(p => (
  <DiagonaalKortti key={p.id} paikka={p} onShowMap={...} />
))}
// This changes in Plan 02 to:
{todoPaikat.map(p => (
  <DiagonaalKortti key={p.id} paikka={p} onShowMap={...} onToggleTodo={handleOverlayDelete} />
))}
// Plus sibling KavikoPaikassaPrompt and InlineReviewExpanded elements (see Task 1)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add onToggleTodo to DiagonaalKortti, add delete state, handleOverlayDelete, and KavikoPaikassaPrompt</name>
  <files>app/components/DiagonaalKortti.tsx, app/components/Etusivu.tsx</files>
  <read_first>
    - app/components/DiagonaalKortti.tsx (full file — read the props interface and where the bookmark/toggle action is called internally; identify the exact prop interface name to extend)
    - app/components/Etusivu.tsx (read lines 240-280 for existing state declarations; read the toggleTodo function ~lines 339-375; read the overlay card list section created in Plan 01 — search for "todoPaikat"; read closeOverlays function)
    - .planning/phases/25-todo-overlay/25-UI-SPEC.md (section 1.5 KavikoPaikassaPrompt, section 5.3 enter animation, section 6 Finnish strings, section 8 Tailwind class references)
  </read_first>
  <action>
    Make these changes across two files:

    --- DiagonaalKortti.tsx ---

    1. ADD `onToggleTodo?: (id: number) => void` to the props interface of DiagonaalKortti.
       This is an optional callback. When provided, it is called instead of (or in addition to) the component's internal bookmark toggle action when the user taps the bookmark button.
       Read the file to find where the internal bookmark toggle fires (look for toggleTodo call or similar) and route it through the prop: if onToggleTodo is provided, call onToggleTodo(paikka.id); otherwise call the existing internal toggleTodo. This keeps backward compatibility — all existing DiagonaalKortti usages without onToggleTodo continue to work unchanged.

    --- Etusivu.tsx ---

    2. ADD two new state variables after the existing `todoOpen` state:
       const [pendingReviewPaikkaId, setPendingReviewPaikkaId] = useState<number | null>(null)
       const [reviewPaikkaId, setReviewPaikkaId] = useState<number | null>(null)

    3. EXTEND closeOverlays() to also reset pending review state:
       function closeOverlays() { setRightOpen(false); setTodoOpen(false); setPendingReviewPaikkaId(null); setReviewPaikkaId(null) }
       This prevents stale review state from persisting across overlay open/close cycles.

    4. ADD a handleOverlayDelete function after the existing toggleTodo function:
       async function handleOverlayDelete(id: number) {
         await toggleTodo(id)  // removes from suosikit table and todoIds
         if (supabaseUser !== null) {
           setPendingReviewPaikkaId(id)
         }
         // non-authenticated: deletion happens silently (no prompt)
       }
       This function is ONLY passed as onToggleTodo to DiagonaalKortti cards rendered inside the overlay.
       DiagonaalKortti cards rendered elsewhere in Etusivu.tsx continue to use toggleTodo directly (no onToggleTodo prop).

    5. UPDATE the overlay card list (in the todoPaikat.map() from Plan 01) to pass handleOverlayDelete:
       Change: <DiagonaalKortti key={p.id} paikka={p} onShowMap={...} />
       To:     <DiagonaalKortti key={p.id} paikka={p} onShowMap={...} onToggleTodo={handleOverlayDelete} />

    6. ADD the KavikoPaikassaPrompt. After the closing tag of the stagger motion.div (the todoPaikat.map container), add an AnimatePresence wrapping a conditional:
       If pendingReviewPaikkaId !== null AND todoOpen is true, render a motion.div (KavikoPaikassaPrompt) with:
       - key: "kavikoPrompt"
       - initial: { opacity: 0, y: 14 }
       - animate: { opacity: 1, y: 0 }
       - exit: { opacity: 0, y: -8 }
       - transition: { duration: 0.22, ease: [0.23, 1, 0.32, 1] }
       - className: "glass rounded-2xl h-32 flex items-center justify-between px-4"
       - Content:
         a. p element with className "text-sm font-bold text-[#111111]" and text "Kävikö paikassa?"
         b. div with className "flex gap-2":
            - motion.button (Kyllä): whileTap={{ scale: 0.95 }}, onClick={() => { setReviewPaikkaId(pendingReviewPaikkaId); setPendingReviewPaikkaId(null) }}, className "bg-[#111111] hover:bg-[#333333] text-white font-bold text-sm px-4 py-2 rounded-full [transition:background-color_150ms_ease]", text "Kyllä"
            - motion.button (Ei): whileTap={{ scale: 0.95 }}, onClick={() => setPendingReviewPaikkaId(null)}, className "border border-[rgba(0,0,0,0.12)] text-[#111111] font-bold text-sm px-4 py-2 rounded-full", text "Ei"
  </action>
  <verify>
    <automated>grep -n "pendingReviewPaikkaId\|handleOverlayDelete\|Kävikö paikassa\|onToggleTodo" C:/ClaudeCodeTestit/liikuntahakemisto/app/components/Etusivu.tsx</automated>
    <automated>grep -n "onToggleTodo" C:/ClaudeCodeTestit/liikuntahakemisto/app/components/DiagonaalKortti.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DiagonaalKortti.tsx props interface contains: onToggleTodo?: (id: number) => void
    - DiagonaalKortti.tsx routes bookmark toggle through onToggleTodo when provided; falls back to internal toggleTodo when not provided
    - Etusivu.tsx contains: const [pendingReviewPaikkaId, setPendingReviewPaikkaId] = useState&lt;number | null&gt;(null)
    - Etusivu.tsx contains: const [reviewPaikkaId, setReviewPaikkaId] = useState&lt;number | null&gt;(null)
    - closeOverlays in Etusivu.tsx calls: setTodoOpen(false), setPendingReviewPaikkaId(null), AND setReviewPaikkaId(null) — all three reset together
    - handleOverlayDelete exists and only sets pendingReviewPaikkaId when BOTH todoOpen is true AND supabaseUser !== null
    - The overlay's todoPaikat.map() passes onToggleTodo={handleOverlayDelete} to DiagonaalKortti
    - Other DiagonaalKortti usages in Etusivu.tsx do NOT receive onToggleTodo (backward compatible)
    - KavikoPaikassaPrompt renders with className containing "h-32 flex items-center justify-between"
    - "Kävikö paikassa?" text is present in the file
    - "Kyllä" button onClick sets reviewPaikkaId and clears pendingReviewPaikkaId
    - "Ei" button onClick clears pendingReviewPaikkaId
    - AnimatePresence wraps the KavikoPaikassaPrompt motion.div
    - TypeScript compiles: npx tsc --noEmit passes
  </acceptance_criteria>
  <done>Logged-in user removing a TO DO place (via bookmark tap in overlay) triggers handleOverlayDelete → "Kävikö paikassa?" prompt inline; non-authenticated user experiences silent deletion; Kyllä/Ei buttons work correctly; closeOverlays resets all review state</done>
</task>

<task type="auto">
  <name>Task 2: Add InlineReviewExpanded with Supabase reviews upsert</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    - app/components/Etusivu.tsx (current state after Task 1; search for "reviewPaikkaId" to find the new state; look at existing ReviewForm.tsx submit pattern at lines 101-133 for the upsert payload shape)
    - app/components/ReviewForm.tsx (lines 101-133: handleSubmit function — copy the upsert payload pattern; lines 146-170: star rating field layout; do NOT import ReviewForm itself)
    - app/components/StarPicker.tsx (confirm props: value: number, onChange: (n: number) => void)
    - .planning/phases/25-todo-overlay/25-UI-SPEC.md (section 1.6 InlineReviewExpanded, section 5.4 enter animation, section 6 Finnish strings for submit/cancel/success/error, section 8 Tailwind class references)
  </read_first>
  <action>
    Add InlineReviewExpanded as a sibling of the KavikoPaikassaPrompt in the overlay JSX. This renders when reviewPaikkaId !== null.

    1. ADD local state for the inline review form. Because these are transient per-overlay-open, add them alongside the other state declarations:
       const [inlineRating, setInlineRating] = useState(0)
       const [inlineTeksti, setInlineTeksti] = useState('')
       const [inlineSubmitting, setInlineSubmitting] = useState(false)
       const [inlineSubmitError, setInlineSubmitError] = useState<string | null>(null)
       const [inlineSubmitted, setInlineSubmitted] = useState(false)

    2. ADD a handleInlineReviewSubmit async function (inside the Etusivu component function body, after handleOverlayDelete):
       - Guard: if !supabaseUser || inlineRating === 0 || !reviewPaikkaId return
       - Set inlineSubmitting = true, inlineSubmitError = null
       - const supabase = createBrowserSupabase()
       - upsert payload: { user_id: supabaseUser.id, paikka_id: reviewPaikkaId, rating: inlineRating, teksti: inlineTeksti.trim(), is_anonymous: false, reviewer_name: supabaseUser.email?.split('@')[0] ?? null, visit_date: null, crowd_rating: null }
       - Call: const { error } = await supabase.from('reviews').upsert(payload, { onConflict: 'user_id,paikka_id' })
       - On error: setInlineSubmitting(false); setInlineSubmitError('Tallennus epäonnistui. Yritä uudelleen.')
       - On success: setInlineSubmitting(false); setInlineSubmitted(true); after 1500ms timeout: setReviewPaikkaId(null); setInlineRating(0); setInlineTeksti(''); setInlineSubmitted(false)

    3. ADD a reset helper that clears all inline review state (call this when reviewPaikkaId is reset to null by "Ohita"):
       function resetInlineReview() { setReviewPaikkaId(null); setInlineRating(0); setInlineTeksti(''); setInlineSubmitError(null); setInlineSubmitted(false) }

    4. ADD InlineReviewExpanded to the JSX, inside the same AnimatePresence that wraps KavikoPaikassaPrompt, as another condition. When reviewPaikkaId !== null, render a motion.div with:
       - key: "inlineReview"
       - initial: { opacity: 0, y: 14 }
       - animate: { opacity: 1, y: 0 }
       - exit: { opacity: 0, y: -8 }
       - transition: { duration: 0.22, ease: [0.23, 1, 0.32, 1] }
       - className: "glass rounded-2xl flex flex-col gap-3 p-4"

       Inside the motion.div:
       a. Success state (inlineSubmitted): p element with className "text-sm font-bold text-[#111111]" and text "Arvostelu tallennettu"

       b. Form state (!inlineSubmitted):
          - Star rating section:
            p label: className "text-[10px] font-bold text-[#111111] uppercase tracking-widest", text "TÄHTIARVOSANA"
            StarPicker component: value={inlineRating} onChange={setInlineRating}
          - Comment section:
            p label: className "text-[10px] font-bold text-[#111111] uppercase tracking-widest", text "KOMMENTTI"
            textarea: className "w-full text-sm text-[#111111] bg-transparent border border-[rgba(0,0,0,0.12)] rounded-xl px-3 py-2 resize-none focus:outline-none focus:border-[rgba(0,0,0,0.25)]", rows={3}, placeholder="Vapaaehtoinen kommentti", value={inlineTeksti}, onChange={e => setInlineTeksti(e.target.value)}
          - Error message (inlineSubmitError): p with className "text-sm text-red-600", text {inlineSubmitError}
          - Button row: div with className "flex items-center gap-3":
            - Submit button: disabled={inlineRating === 0 || inlineSubmitting}, onClick={handleInlineReviewSubmit}, className "bg-[#111111] hover:bg-[#333333] text-white font-bold text-sm px-4 py-2 rounded-full [transition:background-color_150ms_ease] disabled:opacity-40", text: inlineSubmitting ? 'Tallennetaan…' : 'Jätä arvostelu'
            - Cancel link: button element with onClick={resetInlineReview}, className "text-sm text-[rgba(17,17,17,0.45)] underline", text "Ohita"

    5. ENSURE the import for StarPicker is at the top of the file. Check if it already exists; if not, add:
       import StarPicker from './StarPicker'

    IMPORTANT: The `reviewPaikkaId` state is of type `number | null`. The guard `if (!reviewPaikkaId) return` in handleInlineReviewSubmit provides TypeScript narrowing — this is sufficient because the function is only callable when reviewPaikkaId !== null (the form only renders in that condition).
  </action>
  <verify>
    <automated>grep -n "reviewPaikkaId\|inlineRating\|handleInlineReviewSubmit\|Jätä arvostelu\|Ohita\|StarPicker" C:/ClaudeCodeTestit/liikuntahakemisto/app/components/Etusivu.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Etusivu.tsx imports StarPicker from './StarPicker'
    - Etusivu.tsx contains: const [inlineRating, setInlineRating] = useState(0)
    - handleInlineReviewSubmit function exists and contains: supabase.from('reviews').upsert
    - The upsert payload includes user_id, paikka_id, rating, teksti, is_anonymous: false, visit_date: null, crowd_rating: null
    - InlineReviewExpanded motion.div has key="inlineReview"
    - StarPicker component is rendered inside InlineReviewExpanded with value={inlineRating}
    - "Jätä arvostelu" and "Ohita" strings present in file
    - "Arvostelu tallennettu" success string present in file
    - "Tallennus epäonnistui. Yritä uudelleen." error string present in file
    - resetInlineReview function clears reviewPaikkaId to null
    - handleOverlayDelete (from Task 1) only sets pendingReviewPaikkaId when BOTH todoOpen is true AND supabaseUser !== null
    - TypeScript compiles: `npx tsc --noEmit` in project root passes with zero errors in Etusivu.tsx
    - Functional: logged-in user deletes place → "Kävikö?" prompt → "Kyllä" → star form → submit → "Arvostelu tallennettu" → slot disappears
    - Functional: "Ohita" button dismisses the form without submitting
  </acceptance_criteria>
  <done>InlineReviewExpanded renders star rating + comment textarea, submits review to Supabase reviews table via upsert, shows success state, and dismisses cleanly on "Ohita"</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Supabase API | InlineReviewExpanded submits reviews — user_id comes from supabaseUser (Supabase-authenticated session) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Spoofing | handleInlineReviewSubmit — user_id from supabaseUser.id | mitigate | supabaseUser is set by subscribeToAuthUser (Supabase JWT-validated); never accept user_id from user input |
| T-25-04 | Tampering | reviews upsert — paikka_id from reviewPaikkaId state | accept | paikka_id originates from paikat prop (server-fetched); not from URL params or user input; onConflict upsert enforces 1 review per user per place at DB level |
| T-25-05 | Information Disclosure | inlineTeksti textarea | accept | Review text submitted to reviews table; same as existing ReviewForm behavior; RLS gates reads |
| T-25-06 | Denial of Service | repeated rapid reviews submits | accept | Supabase upsert is idempotent; inlineSubmitting guard prevents double-submit |
| T-25-SC | Tampering | npm/pip installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- Logged-in user: deletes TO DO place (taps bookmark in overlay) → "Kävikö paikassa?" prompt appears in overlay (no navigation)
- "Ei" button: dismisses the prompt, no review form shown
- "Kyllä" button: "Kävikö paikassa?" fades out, InlineReviewExpanded fades in with star picker + textarea
- Star picker: tapping stars fills amber stars; submit button is disabled until at least 1 star selected
- "Jätä arvostelu": disabled when rating === 0; shows "Tallennetaan…" while submitting
- On success: "Arvostelu tallennettu" shown, slot auto-dismisses after 1.5s
- On error: "Tallennus epäonnistui. Yritä uudelleen." shown, form stays open
- "Ohita": dismisses InlineReviewExpanded without submitting; clears all inline state
- Non-authenticated user: deletes place → no prompt, silent removal (pendingReviewPaikkaId never set)
- Closing and reopening the overlay: no stale prompt or form visible (closeOverlays resets pendingReviewPaikkaId and reviewPaikkaId)
- TypeScript: `npx tsc --noEmit` passes
</verification>

<success_criteria>
Kun käyttäjä poistaa paikan TO DO -listalta, aukeaa pop-up joka kysyy "Kävikö paikassa?" ja ehdottaa arvostelun jättämistä (TODO-07). The full Phase 25 success criteria:
1. TO DO overlay opens on map (TODO-03) ✓ (Plan 01)
2. Button toggles Bookmark/X (TODO-04) ✓ (Plan 01)
3. Scale animation open/close + card stagger (TODO-05) ✓ (Plan 01)
4. "TO DO" header visually distinct from search (TODO-06) ✓ (Plan 01)
5. Delete → "Kävikö?" → review form (TODO-07) ✓ (this plan)
</success_criteria>

<output>
Create `.planning/phases/25-todo-overlay/25-02-SUMMARY.md` when done
</output>
