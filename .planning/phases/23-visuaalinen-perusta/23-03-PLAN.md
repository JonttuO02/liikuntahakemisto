---
phase: 23-visuaalinen-perusta
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - app/components/SportPin.tsx
  - app/globals.css
autonomous: true
requirements:
  - MAP-11
  - MAP-12
must_haves:
  truths:
    - "SportPin component renders a blue gradient pin (top #38bdf8, tip #0284c7) with a white circle"
    - "Correct sport icon (SVG path) appears inside the white circle for each of the 7 sport types"
    - "An orbiting glint dot animates continuously on all pins using CSS transform only"
    - "Phase 24 can recolor the sport icon via CSS color property (currentColor used)"
    - "Pin body uses inline HTML elements (not an img data-URL)"
  artifacts:
    - path: "app/components/SportPin.tsx"
      provides: "Inline HTML pin component with blue gradient, white circle, sport icon, orbit glint"
      exports: ["default SportPin"]
      contains: "spinOrbit"
    - path: "app/globals.css"
      provides: "@keyframes spinOrbit CSS animation for glint orbit"
      contains: "spinOrbit"
  key_links:
    - from: "app/components/SportPin.tsx"
      to: "app/globals.css"
      via: "CSS class .pin-glint referencing animation spinOrbit"
      pattern: "spinOrbit"
    - from: "app/components/SportPin.tsx"
      to: "lib/sportPins.ts"
      via: "SVG path strings migrated (not imported — embedded as constants)"
      pattern: "currentColor"
---

<objective>
Create SportPin.tsx — a new 'use client' component that renders a map pin as inline HTML elements inside an AdvancedMarker. Migrate sport icon SVG paths from lib/sportPins.ts into a SportPin-local constant. Add @keyframes spinOrbit to globals.css for the orbiting glint animation.

Purpose: Per D-07–D-12 (MAP-11, MAP-12) — inline HTML pins replace data-URL img tags, enabling CSS animation (orbit glint) and Phase 24 sport-color theming via currentColor.
Output: app/components/SportPin.tsx (new file), app/globals.css (new @keyframes + .pin-glint class).
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
From lib/sportPins.ts (source for SVG paths — read file, then embed paths as a local const in SportPin.tsx):

SPORT_ICONS_SVG keys: padel, kuntosali, jooga, uinti, tennis, liikuntahalli, liikunta
Each value is a `<g stroke="#374151" ...>` string.

IMPORTANT: In SportPin.tsx, change `stroke="#374151"` → `stroke="currentColor"` in every icon path so Phase 24 can override via CSS `color` property (per D-10).

From app/globals.css:
- @keyframes pinBounce at line 74 — pattern for adding new @keyframes
- .gmap-pin class at line 80 — pattern for pin CSS class
- File uses @layer components / @layer utilities structure; new @keyframes go in the Google Maps section (after line 73, outside any @layer block)

From app/components/Etusivu.tsx AdvancedMarker context (lines 578–612):
  The existing single-pin wrapper div is:
  `<div style={{ position: 'relative', width: 0, height: 0 }}>`
  Inside it, a motion.div with:
  `style={{ position: 'absolute', bottom: 0, left: 0, transform: 'translateX(-50%)' }}`
  SportPin renders as the child of that motion.div, replacing the current img tag.
  Pin dimensions must match current: width 28px, height 38px (to preserve anchor geometry).

Orbit glint implementation guide (per D-04, D-06):
- A small div (~4×4px, border-radius 50%) positioned absolutely relative to the pin body
- Placed at the outer edge of the pin circle (the white circle has cx=14 cy=14 r=10, so outer edge ~r=13px from pin center)
- animation: spinOrbit 4s linear infinite (4s chosen as mid-range of D-04's 3–5s, elegant and smooth)
- transform-origin positioned so the glint orbits the circle center (transform-origin: 0px 13px or equivalent offset)
- To stagger pins so they don't all sync: use animation-delay derived from a deterministic offset — SportPin accepts an optional `animDelay?: number` prop (0–1 value, default 0); CSS animation-delay = `${animDelay * 4}s`; Etusivu will pass a per-pin offset (plan 04)
- Glint color: white with slight blue tint — `rgba(255,255,255,0.9)` with a subtle `box-shadow: 0 0 3px rgba(56,189,248,0.8)` — box-shadow is on a non-animating property (it's static, not animated), which is allowed per D-06 (only ANIMATED properties must be transform/opacity)

@keyframes spinOrbit:
  from { transform: rotate(0deg) translateY(-13px); }
  to   { transform: rotate(360deg) translateY(-13px); }
  (The translateY(-13px) keeps the glint at the circle's outer edge while rotate() makes it orbit)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add @keyframes spinOrbit and .pin-glint to globals.css</name>
  <files>app/globals.css</files>

  <read_first>
    - app/globals.css lines 73–95 — see pinBounce pattern and .gmap-pin section to know where to insert
  </read_first>

  <action>
    Append to the "Google Maps custom markers" section of app/globals.css (after the existing .gmap-pin[data-active="true"] and .pin-label rules, before the "Night page theme" comment at line 114).

    Add two new blocks in the Google Maps section:

    Block 1 — @keyframes spinOrbit:
      @keyframes spinOrbit {
        from { transform: rotate(0deg) translateY(-13px); }
        to   { transform: rotate(360deg) translateY(-13px); }
      }

    Block 2 — .pin-glint class:
      .pin-glint {
        position: absolute;
        width: 4px;
        height: 4px;
        border-radius: 50%;
        background: rgba(255,255,255,0.9);
        box-shadow: 0 0 3px rgba(56,189,248,0.8);
        top: 50%;
        left: 50%;
        margin-top: -2px;
        margin-left: -2px;
        transform-origin: 0px 13px;
        animation: spinOrbit 4s linear infinite;
        pointer-events: none;
      }

    These additions go OUTSIDE any @layer block (same as the existing .gmap-pin rule). Do not modify any existing rules.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('app/globals.css','utf8');if(!c.includes('spinOrbit'))throw new Error('spinOrbit missing');if(!c.includes('.pin-glint'))throw new Error('.pin-glint missing');if(!c.includes('translateY(-13px)'))throw new Error('translateY missing');console.log('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - app/globals.css contains @keyframes spinOrbit with `from { transform: rotate(0deg) translateY(-13px); }` and `to { transform: rotate(360deg) translateY(-13px); }`
    - app/globals.css contains `.pin-glint` class with `animation: spinOrbit 4s linear infinite`
    - Existing @keyframes pinBounce and .gmap-pin rules are unchanged
  </acceptance_criteria>

  <done>globals.css has spinOrbit keyframes and .pin-glint class.</done>
</task>

<task type="auto">
  <name>Task 2: Create SportPin.tsx — inline HTML pin component</name>
  <files>app/components/SportPin.tsx</files>

  <read_first>
    - lib/sportPins.ts — read full file to copy the 7 SVG path strings (padel, kuntosali, jooga, uinti, tennis, liikuntahalli, liikunta); note the g() helper pattern but do NOT import from sportPins.ts — embed paths directly
    - app/globals.css — confirm .pin-glint and spinOrbit were added in Task 1 before proceeding
  </read_first>

  <action>
    Create app/components/SportPin.tsx as a new 'use client' component (per D-07–D-10, MAP-11, MAP-12).

    PROPS INTERFACE:
      interface SportPinProps {
        laji: string
        animDelay?: number  // 0–1, used as animation-delay multiplier; default 0
      }

    SPORT ICONS CONSTANT (embed in the file, do NOT import from sportPins.ts):
    - Copy the 7 icon path strings from lib/sportPins.ts SPORT_ICONS_SVG.
    - Change all occurrences of `stroke="#374151"` → `stroke="currentColor"` in every path string (per D-10, Phase 24 compatibility).
    - Keep `stroke-width="2.5"`, `fill="none"`, `stroke-linecap="round"`, `stroke-linejoin="round"` as SVG attributes on the <g> element or individual paths — these are fine to keep inline since they're structural, not color.
    - The constant should be typed Record<string, string> and named SPORT_ICONS.
    - Fallback for unknown sport: a simple circle path `<g stroke="currentColor" stroke-width="2.5" fill="none"><circle cx="12" cy="12" r="4"/></g>`.

    PIN STRUCTURE (inline HTML — no SVG for the outer pin body):
    The component returns a single outer div. Dimensions: width 28px, height 38px. Position relative. This matches the current img dimensions so the AdvancedMarker anchor geometry is unchanged.

    Outer div: `style={{ position: 'relative', width: 28, height: 38, cursor: 'pointer' }}`

    Inside the outer div:

    A. Pin body div (the teardrop shape via CSS):
       - `style={{ position: 'absolute', inset: 0, borderRadius: '50% 50% 50% 50% / 60% 60% 40% 40%', background: 'linear-gradient(to bottom, #38bdf8 0%, #0284c7 100%)' }}`
       - This creates the classic map-pin teardrop with a CSS gradient (per D-01).

    B. White circle div (per D-02):
       - `style={{ position: 'absolute', top: 4, left: 4, width: 20, height: 20, borderRadius: '50%', background: 'white' }}`
       - Centers at approximately cx=14 cy=14 in a 28px-wide pin.

    C. Sport icon SVG (per D-10):
       - Wrap in a div: `style={{ position: 'absolute', top: 5, left: 5, width: 18, height: 18, color: '#374151' }}`
       - The `color` property sets the default currentColor value for Phase 24 override compatibility.
       - Inside: an SVG element `viewBox="0 0 24 24" width="18" height="18"`
       - Render the icon path via `dangerouslySetInnerHTML={{ __html: SPORT_ICONS[laji.toLowerCase()] ?? SPORT_ICONS['fallback'] }}`
       - Note: dangerouslySetInnerHTML is safe here — SPORT_ICONS is a compile-time constant, not user input.

    D. Glint div (orbit animation per D-04, D-05, D-06):
       - `className="pin-glint"`
       - `style={{ animationDelay: `${(animDelay ?? 0) * 4}s` }}`
       - The .pin-glint class (added in Task 1) handles all positioning and animation.

    EXPORTS:
      export default function SportPin({ laji, animDelay }: SportPinProps)

    CLASSNAME HYGIENE: No Tailwind classes on SportPin elements — use inline style only to avoid Tailwind purge issues with dynamically-generated content inside AdvancedMarker. The .pin-glint CSS class is the only class used (it's defined globally).
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('app/components/SportPin.tsx','utf8');if(!c.includes('use client'))throw new Error('use client missing');if(!c.includes('currentColor'))throw new Error('currentColor missing');if(!c.includes('SPORT_ICONS'))throw new Error('SPORT_ICONS missing');if(!c.includes('pin-glint'))throw new Error('pin-glint class missing');if(!c.includes('animDelay'))throw new Error('animDelay prop missing');if(!c.includes('#38bdf8'))throw new Error('gradient top color missing');if(!c.includes('#0284c7'))throw new Error('gradient tip color missing');if(c.includes('#374151')&&!c.includes('color:'))throw new Error('stroke #374151 not replaced with currentColor');if(c.includes('from.*sportPins'))throw new Error('should not import from sportPins');console.log('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - app/components/SportPin.tsx exists with 'use client' directive
    - File contains `export default function SportPin`
    - SPORT_ICONS constant uses `stroke="currentColor"` (not `stroke="#374151"`) in all icon path strings
    - Pin body has `background: 'linear-gradient(to bottom, #38bdf8 0%, #0284c7 100%)'`
    - White circle div is present with `borderRadius: '50%'` and `background: 'white'`
    - Glint div uses className="pin-glint" and inline animationDelay from animDelay prop
    - No import from lib/sportPins or @/lib/sportPins
    - Component accepts `laji: string` and `animDelay?: number` props
  </acceptance_criteria>

  <done>SportPin.tsx renders blue gradient pin with white circle, sport icon (currentColor), and CSS-orbit glint. Phase 24 can recolor icons via CSS color property.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| laji prop → dangerouslySetInnerHTML | SPORT_ICONS is a compile-time constant; laji is used as a lookup key only — HTML output is never user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | Tampering | SportPin dangerouslySetInnerHTML | accept | SPORT_ICONS is a compile-time Record<string,string>; laji is only a lookup key; user input never reaches innerHTML |
| T-23-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — only new files created |
</threat_model>

<verification>
Both verify commands pass. SportPin.tsx and globals.css match acceptance criteria above.
</verification>

<success_criteria>
SportPin.tsx: blue gradient pin, white circle, sport icon with currentColor, orbiting glint via CSS. globals.css: @keyframes spinOrbit and .pin-glint added. No new npm dependencies.
</success_criteria>

<output>
Create `.planning/phases/23-visuaalinen-perusta/23-03-SUMMARY.md` when done.
</output>
