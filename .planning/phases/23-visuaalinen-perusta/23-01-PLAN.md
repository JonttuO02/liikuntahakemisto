---
phase: 23-visuaalinen-perusta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
autonomous: true
requirements:
  - UI-22
must_haves:
  truths:
    - "Application loads with Outfit font rendered for all body/UI text"
    - "Playfair Display still renders for elements using font-serif"
    - "No downstream component files need modification (--font-sans CSS var unchanged)"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Outfit font loaded via next/font/google, bound to --font-sans"
      contains: "Outfit"
  key_links:
    - from: "app/layout.tsx"
      to: "html element className"
      via: "outfit.variable CSS variable"
      pattern: "outfit\\.variable"
---

<objective>
Replace the Inter font import with Outfit in app/layout.tsx. The --font-sans CSS variable binding is preserved so zero downstream components need changes.

Purpose: Per D-11 (UI-22) — Outfit gives the app a sportier, more contemporary feel while the --font-sans abstraction keeps the swap transparent to all consumers.
Output: Modified app/layout.tsx with Outfit imported and wired as --font-sans.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
Current app/layout.tsx (30 lines, full file already small):

Line 2:  import { Inter, Playfair_Display } from 'next/font/google'
Line 7:  const inter    = Inter({ subsets: ['latin'], variable: '--font-sans' })
Line 8:  const playfair = Playfair_Display({ subsets: ['latin'], variable: '--font-serif' })
Line 22: <html lang="fi" className={cn('font-sans', inter.variable, playfair.variable)}>

Target state after this plan:
Line 2:  import { Outfit, Playfair_Display } from 'next/font/google'
Line 7:  const outfit   = Outfit({ subsets: ['latin'], variable: '--font-sans' })
Line 8:  (unchanged)
Line 22: <html lang="fi" className={cn('font-sans', outfit.variable, playfair.variable)}>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace Inter with Outfit in app/layout.tsx</name>
  <files>app/layout.tsx</files>

  <read_first>
    - app/layout.tsx — read the full file before editing (it is 30 lines); extract the exact import line, font instantiation line, and className line
  </read_first>

  <action>
    Make three targeted edits to app/layout.tsx (per D-11, UI-22):

    1. Line 2 — change the named import: replace `Inter` with `Outfit` in the import from 'next/font/google'. `Playfair_Display` stays.

    2. Line 7 — rename the font instance and swap the constructor:
       - Old: `const inter    = Inter({ subsets: ['latin'], variable: '--font-sans' })`
       - New: `const outfit   = Outfit({ subsets: ['latin'], variable: '--font-sans' })`
       The variable value '--font-sans' is unchanged.

    3. Line 22 — update the html className to reference `outfit.variable` instead of `inter.variable`:
       - Old: `className={cn('font-sans', inter.variable, playfair.variable)}`
       - New: `className={cn('font-sans', outfit.variable, playfair.variable)}`

    No other lines change. Playfair_Display instantiation (line 8) and the `playfair.variable` reference are untouched.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('app/layout.tsx','utf8');if(c.includes('Inter'))throw new Error('Inter still present');if(!c.includes('Outfit'))throw new Error('Outfit missing');if(!c.includes(\"outfit.variable\"))throw new Error('outfit.variable missing');if(!c.includes('Playfair_Display'))throw new Error('Playfair removed');if(!c.includes(\"playfair.variable\"))throw new Error('playfair.variable missing');console.log('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - app/layout.tsx line 2 contains `Outfit` in the import (no `Inter` anywhere in the file)
    - app/layout.tsx contains `const outfit` (not `const inter`)
    - app/layout.tsx html className contains `outfit.variable`
    - app/layout.tsx still contains `Playfair_Display` and `playfair.variable` unchanged
    - `next dev` starts without type errors (Outfit is a valid next/font/google export)
  </acceptance_criteria>

  <done>app/layout.tsx imports Outfit instead of Inter; --font-sans variable bound to outfit.variable; Playfair_Display untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → browser | Google Fonts served via next/font (inlined at build, no runtime CDN request) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-SC | Tampering | npm/next/font/google | accept | next/font is part of the Next.js core package — already audited in prior phases; no new package install required |
</threat_model>

<verification>
Run `node -e "const fs=require('fs');const c=fs.readFileSync('app/layout.tsx','utf8');if(c.includes('Inter'))throw new Error('Inter still present');console.log('OK')"` — must print OK.
</verification>

<success_criteria>
app/layout.tsx uses Outfit (not Inter) for --font-sans. Playfair_Display unchanged. Zero other files modified.
</success_criteria>

<output>
Create `.planning/phases/23-visuaalinen-perusta/23-01-SUMMARY.md` when done.
</output>
