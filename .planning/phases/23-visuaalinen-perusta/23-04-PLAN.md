---
phase: 23-visuaalinen-perusta
plan: 04
type: execute
wave: 3
depends_on:
  - 23-03
files_modified:
  - app/components/Etusivu.tsx
  - lib/sportPins.ts
autonomous: true
requirements:
  - MAP-11
  - MAP-12
  - MAP-13
must_haves:
  truths:
    - "All single-venue map pins render blue gradient inline HTML (not red img data-URLs)"
    - "Cluster pins render blue gradient inline HTML with count label (not red img data-URLs)"
    - "Each pin's glint dot orbits at a different phase (staggered animation-delay)"
    - "lib/sportPins.ts no longer exports pinUrl or clusterPinUrl (or file is emptied/deleted)"
    - "No TypeScript or build errors from removing pinUrl/clusterPinUrl imports"
  artifacts:
    - path: "app/components/Etusivu.tsx"
      provides: "AdvancedMarker renders SportPin for single pins; inline cluster HTML element"
      contains: "SportPin"
    - path: "lib/sportPins.ts"
      provides: "Legacy pin functions removed or file cleared"
  key_links:
    - from: "app/components/Etusivu.tsx"
      to: "app/components/SportPin.tsx"
      via: "import SportPin + JSX usage"
      pattern: "import SportPin"
    - from: "Etusivu.tsx cluster AdvancedMarker"
      to: "inline div cluster element"
      via: "blue gradient div replacing img src={clusterPinUrl(...)}"
      pattern: "cluster-pin-body"
---

<objective>
Wire SportPin into Etusivu.tsx: replace img data-URL tags with SportPin for single pins, replace cluster img with inline HTML cluster element using the same blue theme. Remove the now-unused pinUrl/clusterPinUrl import and clean up lib/sportPins.ts.

Purpose: Per D-07–D-09 (MAP-11, MAP-12, MAP-13) — this completes the inline HTML pin migration. All map pins now render blue gradient elements with orbit glint animation.
Output: Modified Etusivu.tsx (SportPin wired, cluster inline HTML), modified lib/sportPins.ts (exports removed or file cleared).
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-visuaalinen-perusta/23-03-SUMMARY.md
</context>

<interfaces>
From app/components/Etusivu.tsx (read before editing):

Line 20: `import { pinUrl, clusterPinUrl } from '@/lib/sportPins'`
  → Replace with: `import SportPin from './SportPin'`

Single pin rendering (lines 583–593):
  Current:
    `<img src={pinUrl(p.laji)} width={28} height={38} alt="" className="gmap-pin" />`
  Target:
    `<SportPin laji={p.laji} animDelay={pinAnimDelay(p.id)} />`
  The motion.div wrapper remains — only the img child changes.

Cluster pin rendering (lines 623–632):
  Current:
    `<img src={clusterPinUrl(item.items.length)} width={28} height={38} alt="" style={{ cursor: 'pointer', display: 'block' }} onClick={...} />`
  Target: Replace with inline HTML div cluster element (see Task 2 action for structure).
  The motion.div wrapper and onClick remain — only the img child changes.

SportPin props (from 23-03 SUMMARY):
  interface SportPinProps { laji: string; animDelay?: number }
  export default function SportPin({ laji, animDelay }: SportPinProps)

pinAnimDelay helper: a small deterministic function to derive a 0–1 stagger value from a pin's id (string). Use: `(id.charCodeAt(0) % 10) / 10` — gives a stable per-pin offset without random flicker on re-render. Define this as a `const` above the JSX return in Etusivu, NOT as a React state or effect.

lib/sportPins.ts — this file's pinUrl and clusterPinUrl are used ONLY in Etusivu.tsx (confirmed: grep shows only 2 files: Etusivu.tsx and sportPins.ts itself). After removing the import from Etusivu.tsx, clear the file to a minimal comment + empty export to avoid "no exports" TypeScript errors. Do NOT delete the file (other phases may reference it in docs).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace single-pin img with SportPin in Etusivu.tsx</name>
  <files>app/components/Etusivu.tsx</files>

  <read_first>
    - app/components/Etusivu.tsx lines 18–22 — see current imports section to find pinUrl/clusterPinUrl import
    - app/components/Etusivu.tsx lines 580–615 — see exact single-pin img tag and its motion.div wrapper
    - app/components/SportPin.tsx — confirm the SportPin default export and its props interface before wiring
  </read_first>

  <action>
    Make two edits to app/components/Etusivu.tsx for the single-pin replacement (per D-07, D-08, MAP-11, MAP-12):

    EDIT A — Update import on line 20:
      Remove: `import { pinUrl, clusterPinUrl } from '@/lib/sportPins'`
      Add:    `import SportPin from './SportPin'`
    (clusterPinUrl will be handled inline in Task 2; pinUrl is fully replaced by SportPin)

    EDIT B — Add pinAnimDelay helper constant:
    After the existing imports section (before the component function body or early inside it, before the JSX return), add a module-level const:
      `const pinAnimDelay = (id: string): number => (id.charCodeAt(0) % 10) / 10`
    This derives a stable 0–1 stagger value per pin without React state.

    EDIT C — Replace img tag on line 591:
      Remove: `<img src={pinUrl(p.laji)} width={28} height={38} alt="" className="gmap-pin" />`
      Add:    `<SportPin laji={p.laji} animDelay={pinAnimDelay(p.id)} />`
    The surrounding motion.div (key="pin", style with position/bottom/left/transform, exit/transition, onClick) is untouched.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('app/components/Etusivu.tsx','utf8');if(c.includes('pinUrl'))throw new Error('pinUrl still present');if(!c.includes('SportPin'))throw new Error('SportPin import missing');if(!c.includes('pinAnimDelay'))throw new Error('pinAnimDelay missing');if(!c.includes('animDelay={pinAnimDelay'))throw new Error('animDelay not passed to SportPin');console.log('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - Etusivu.tsx does NOT contain `pinUrl` anywhere
    - Etusivu.tsx contains `import SportPin from './SportPin'`
    - Etusivu.tsx contains `const pinAnimDelay = (id: string)`
    - Etusivu.tsx contains `<SportPin laji={p.laji} animDelay={pinAnimDelay(p.id)} />`
    - The motion.div key="pin" wrapper with position/exit/onClick is structurally unchanged
  </acceptance_criteria>

  <done>Single-pin img replaced with SportPin; pinUrl import removed; animDelay stagger wired.</done>
</task>

<task type="auto">
  <name>Task 2: Replace cluster img with inline HTML + clean up sportPins.ts</name>
  <files>app/components/Etusivu.tsx, lib/sportPins.ts</files>

  <read_first>
    - app/components/Etusivu.tsx lines 616–665 — see cluster AdvancedMarker structure; the img is inside a motion.div whileTap; the AnimatePresence cluster-popup follows
    - lib/sportPins.ts — read full file to understand what to clear
  </read_first>

  <action>
    TWO EDITS:

    EDIT A — Replace cluster img in Etusivu.tsx (per D-09, MAP-13):

    Locate the cluster img tag (approximately line 624):
      `<img src={clusterPinUrl(item.items.length)} width={28} height={38} alt="" style={{ cursor: 'pointer', display: 'block' }} onClick={...} />`

    Replace it with an inline div cluster pin. The outer motion.div (whileTap={{ scale: 0.95 }}) stays.
    The new inline cluster element must:
    - Have width: 28, height: 38 (same dimensions as before)
    - Show the blue gradient teardrop shape: `borderRadius: '50% 50% 50% 50% / 60% 60% 40% 40%'`, `background: 'linear-gradient(to bottom, #38bdf8 0%, #0284c7 100%)'`
    - Contain a white circle: position absolute, top 4, left 4, width 20, height 20, borderRadius '50%', background 'white'
    - Contain a count label inside the white circle: position absolute, top 4, left 0, width 28, textAlign 'center', fontSize 9, fontWeight 'bold', color '#0284c7', lineHeight '20px', the text is `item.items.length > 9 ? '9+' : String(item.items.length)`
    - Include the glint dot: a `<div className="pin-glint" />` child of the outer cluster div
    - The outer cluster div must have: `style={{ position: 'relative', width: 28, height: 38, cursor: 'pointer' }}` and keep the `onClick` handler from the original img tag.
    - Remove the `display: 'block'` style (no longer needed for div).

    Structure of the replacement (prose description — do NOT add fenced code blocks):
    The motion.div wrapper stays. Inside it, a single outer div that replaces the img. The outer div has: position relative, width 28, height 38, cursor pointer, and the onClick from the original img. Inside the outer div: (1) pin body div with the blue gradient teardrop borderRadius, positioned absolute inset 0; (2) white circle div positioned absolute top 4 left 4 width 20 height 20 borderRadius 50% background white; (3) count label div positioned absolute with the item count text in color #0284c7 fontSize 9 fontWeight bold; (4) glint div with className="pin-glint".

    EDIT B — Clear lib/sportPins.ts:
    Replace the entire contents of lib/sportPins.ts with a minimal comment indicating the file has been superseded by SportPin.tsx, and export an empty object to avoid TypeScript "no exports" errors:
      // Sport pin rendering migrated to app/components/SportPin.tsx (Phase 23)
      export {}
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const e=fs.readFileSync('app/components/Etusivu.tsx','utf8');if(e.includes('clusterPinUrl'))throw new Error('clusterPinUrl still in Etusivu');if(!e.includes('pin-glint'))throw new Error('pin-glint missing from cluster');if(!e.includes('38bdf8'))throw new Error('blue gradient missing from cluster');const s=fs.readFileSync('lib/sportPins.ts','utf8');if(s.includes('function pinUrl')||s.includes('function clusterPinUrl'))throw new Error('sportPins.ts still has pin functions');console.log('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - Etusivu.tsx does NOT contain `clusterPinUrl` anywhere
    - Etusivu.tsx cluster section contains `#38bdf8` (blue gradient) in the inline cluster element
    - Etusivu.tsx cluster section contains `className="pin-glint"` on a glint div
    - Etusivu.tsx cluster section's count label shows `item.items.length > 9 ? '9+' : String(item.items.length)` in color `#0284c7`
    - lib/sportPins.ts does NOT contain `function pinUrl` or `function clusterPinUrl`
    - lib/sportPins.ts contains `export {}` to prevent TypeScript errors
    - The cluster AnimatePresence popup (cluster-popup motion.div) is structurally unchanged
  </acceptance_criteria>

  <done>Cluster pin is inline HTML with blue gradient + glint. lib/sportPins.ts cleared. No more data-URL img tags in Etusivu.tsx.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase DB → paikka.id → pinAnimDelay | id is a UUID from Supabase; charCodeAt(0) extracts first character only — no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-01 | Information Disclosure | pinAnimDelay(p.id) | accept | Only uses charCodeAt(0) % 10; reveals no user data; deterministic from public DB id |
| T-23-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Both verify commands from Task 1 and Task 2 pass. Additionally:
- `grep -c "gmap-pin" app/components/Etusivu.tsx` returns 0 (img with gmap-pin class removed)
- `grep -c "pinUrl\|clusterPinUrl" app/components/Etusivu.tsx` returns 0
- `npx tsc --noEmit` passes without errors (or `next build` if preferred)
</verification>

<success_criteria>
All map pins (single + cluster) render blue gradient inline HTML with orbit glint. lib/sportPins.ts is cleared. Zero TypeScript errors. No red img data-URL pins on the map.
</success_criteria>

<output>
Create `.planning/phases/23-visuaalinen-perusta/23-04-SUMMARY.md` when done.
</output>
