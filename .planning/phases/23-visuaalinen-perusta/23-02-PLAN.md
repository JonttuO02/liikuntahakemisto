---
phase: 23-visuaalinen-perusta
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/AktiiviLogo.tsx
autonomous: true
requirements:
  - UI-23
must_haves:
  truths:
    - "Logo renders at ~32px height (not the previous 56px)"
    - "Decorative arcs are always #111111 (black), no color change"
    - "Letters start hidden (opacity 0 / zero clip-rect width)"
    - "A 5-second auto-loop plays: sweep reveals letters (~0.6s), visible (~1s), fade (~0.4s), pause (~3s), then repeats"
    - "Letter text color is sporty blue (#38bdf8 → #0284c7 gradient or solid #0ea5e9)"
    - "The component accepts no external props — self-contained auto-loop"
  artifacts:
    - path: "app/components/AktiiviLogo.tsx"
      provides: "Redesigned logo with blue sweep animation, auto-loop, 32px height"
      contains: "spinLoop"
  key_links:
    - from: "AktiiviLogo.tsx"
      to: "SVG clipPath sweep-clip"
      via: "rectRef animated by Framer Motion"
      pattern: "sweep-clip"
---

<objective>
Redesign AktiiviLogo.tsx: shrink to 32px height, replace the 5-gradient external-prop cycle with a self-contained auto-looping blue sweep animation (D-13 through D-18).

Purpose: Per D-13–D-18 (UI-23) — the logo in the bottom sheet gets a sporty blue reveal animation matching the new pin color palette, runs automatically without any external trigger, and sits smaller so it doesn't dominate the sheet handle.
Output: Modified app/components/AktiiviLogo.tsx — no props required, self-contained loop.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
Current AktiiviLogo.tsx key facts (read before editing):
- 'use client' component, imports: useRef, useEffect, useState from react; animate from framer-motion
- Accepts gradientIndex: number prop — this prop will be REMOVED
- rectRef points to a SVGRectElement inside clipPath id="sweep-clip"
- animate(rect, { width: 1672 }, { duration: 0.55 }) — this is the sweep mechanism to reuse
- SVG viewBox="100 200 1480 580", current style={{ height: 56, width: 'auto' }}
- letterPaths array: 8 path strings for A,K,T,I,I,V,I letters
- Decorative paths use stroke="#111111" — keep as-is

Letter stroke current state: stroke="url(#grad-prev)" / stroke="url(#grad-curr)"
Target state: single blue gradient (#38bdf8 → #0284c7), letters hidden initially (clip rect width=0), auto-reveal loop.

Framer Motion imperative animate API (already imported):
  animate(element, { width: 1672 }, { duration: 0.6, ease: 'easeInOut' })
  returns controls with .then(callback) and .cancel()
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Redesign AktiiviLogo.tsx — auto-loop blue sweep, 32px height</name>
  <files>app/components/AktiiviLogo.tsx</files>

  <read_first>
    - app/components/AktiiviLogo.tsx — read full file before rewriting; note exact rectRef usage, animate call signature, letterPaths array, and SVG structure
  </read_first>

  <action>
    Rewrite app/components/AktiiviLogo.tsx as a self-contained auto-looping component (per D-13–D-18, UI-23). The rewrite removes the gradientIndex prop entirely and replaces the 5-gradient system with a single blue gradient and a useEffect loop.

    STRUCTURAL CHANGES:

    1. Remove: GRADIENTS array, AktiiviLogoProps interface, gradientIndex parameter, prevIndexRef, prevIndex state, currIndex computation. The component takes no props: `export default function AktiiviLogo()`.

    2. Keep: rectRef (SVGRectElement), useEffect, animate import, letterPaths array (8 paths, unchanged), decorative path strings (unchanged, stroke="#111111").

    3. Add: a single blue linearGradient definition id="grad-blue" with x1="155" y1="430" x2="1517" y2="430" (same userSpaceOnUse coords as before), stop 0% stopColor="#38bdf8", stop 100% stopColor="#0284c7".

    4. Remove grad-prev / grad-curr gradient defs. Letter paths in BOTH layers now use stroke="url(#grad-blue)".

    5. Auto-loop useEffect (replaces the prop-triggered useEffect):
       - On mount, start loop immediately.
       - Loop sequence: (a) set rect width to 0 immediately via rect.setAttribute('width','0'); (b) animate rect width 0→1672 over 0.6s easeInOut (sweep reveals letters, D-16 ~0.6s); (c) in .then(): wait 1000ms (letters visible, D-16 ~1s); (d) animate rect width 1672→0 over 0.4s easeInOut (fade/collapse, D-16 ~0.4s); (e) in .then(): wait 3000ms (pause, D-16 ~3s); (f) repeat from step (a).
       - Use a `cancelled` boolean ref guard so cleanup cancels all pending timeouts/animations when component unmounts.
       - Implement the loop as a recursive async function `runLoop()` called once in the effect. Use `setTimeout` wrapped in Promises for the wait steps.
       - Return cleanup: set `cancelled = true`, cancel any in-progress animation controls.
       - The eslint-disable comment for react-hooks/exhaustive-deps can be removed since there are no external deps.

    6. Initial SVG state: the clipPath rect starts with width="0" (letters hidden, D-15). Since the loop starts immediately on mount, the letters will be hidden for only a frame before the first sweep begins. This is correct per D-15 (hidden by default).

    7. Remove the two-layer letter rendering (prev + curr). Replace with a single layer of letter paths, all using stroke="url(#grad-blue)", wrapped in clipPath="url(#sweep-clip)" — only the clipped layer needed now.

    8. SVG style: change `style={{ height: 56, width: 'auto' }}` → `style={{ height: 32, width: 'auto' }}` (D-13).

    CONSTRAINTS (from D-06 / STATE.md):
    - No box-shadow, background, or filter animations — only transform/opacity/width are used. The rect width animation satisfies this.
    - No spring physics.

    IMPORTANT: Do NOT include fenced code blocks in this action. The letterPaths array values are unchanged from the current file — copy them verbatim.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('app/components/AktiiviLogo.tsx','utf8');if(c.includes('GRADIENTS'))throw new Error('GRADIENTS array not removed');if(c.includes('gradientIndex'))throw new Error('gradientIndex prop not removed');if(!c.includes('grad-blue'))throw new Error('grad-blue missing');if(!c.includes('#38bdf8'))throw new Error('#38bdf8 missing');if(!c.includes('#0284c7'))throw new Error('#0284c7 missing');if(!c.includes('height: 32'))throw new Error('height not 32');if(!c.includes('sweep-clip'))throw new Error('sweep-clip missing');if(!c.includes('runLoop'))throw new Error('runLoop function missing');console.log('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - AktiiviLogo.tsx exports `default function AktiiviLogo()` with no parameters (no gradientIndex prop)
    - File contains `grad-blue` linearGradient with stopColor="#38bdf8" and stopColor="#0284c7"
    - File contains `runLoop` function (auto-loop implementation)
    - SVG style has `height: 32` (not 56)
    - Decorative paths still have stroke="#111111"
    - File does NOT contain GRADIENTS, gradientIndex, grad-prev, or grad-curr
    - sweep-clip clipPath and rectRef are still present
    - Letter paths use stroke="url(#grad-blue)"
  </acceptance_criteria>

  <done>AktiiviLogo.tsx is self-contained, auto-loops with blue sweep, renders at 32px height. No external prop required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component mount/unmount | useEffect cleanup must cancel timers to prevent state updates on unmounted component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Denial of Service | AktiiviLogo runLoop | mitigate | cancelled boolean guard + animation.cancel() in useEffect cleanup prevents memory leak / setState-after-unmount |
</threat_model>

<verification>
Run verify command above — must print OK. Additionally confirm file does not contain `gradientIndex` anywhere.
</verification>

<success_criteria>
AktiiviLogo.tsx: height 32px, blue-only gradient (#38bdf8 → #0284c7), auto-looping sweep (no external prop), letters start hidden, decorative arcs black.
</success_criteria>

<output>
Create `.planning/phases/23-visuaalinen-perusta/23-02-SUMMARY.md` when done.
</output>
