---
phase: 51.1-live-preview-on-analysoisivusto-analyze-quick-accept-results
plan: 02
type: execute
wave: 2
depends_on:
  - 51.1-01
files_modified:
  - app/business/onboarding/AnalysoiSivusto.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "On the AnalysoiSivusto preview phase, the user sees a live CalloutCard/DiagonaalKortti preview before clicking Hyväksy ja lähetä or Jatka velhoon"
    - "Picking a logo updates the preview's logo live"
    - "Toggling a gallery image updates the preview's photos live"
    - "Picking background/accent colors updates the preview's colors live"
    - "Desktop shows a sticky w-[360px] preview sidebar; mobile shows a LivePreviewToggle that swaps between the pickers and the preview"
    - "The checking/url-input/analyzing/error/timeout phases render unchanged (single column, no split/toggle)"
  artifacts:
    - path: "app/business/onboarding/AnalysoiSivusto.tsx"
      provides: "preview-phase JSX wrapped in LivePreviewProvider + wizard split-view/toggle; SET_MEDIA dispatch on logo/gallery picks; merged brandingData override for colors; new paikkaInfo prop"
      contains: "LivePreviewProvider"
  key_links:
    - from: "app/business/onboarding/AnalysoiSivusto.tsx (selectLogo / toggleGalleryImage)"
      to: "lib/livePreview/LivePreviewContext.tsx (reducer SET_MEDIA)"
      via: "dispatch({ type: 'SET_MEDIA', payload: { logo / photos } })"
      pattern: "type: 'SET_MEDIA'"
    - from: "app/business/onboarding/AnalysoiSivusto.tsx (LivePreviewProvider brandingData prop)"
      to: "lib/livePreview/LivePreviewContext.tsx (brandColor/accentColor derivation)"
      via: "brandingData override { ...brandingResult, selected_background_color: bgColor, selected_accent_color: accentColor }"
      pattern: "selected_background_color: bgColor"
    - from: "app/business/onboarding/AnalysoiSivusto.tsx (LivePreviewPane)"
      to: "lib/livePreview/LivePreviewContext.tsx (useLivePreview)"
      via: "LivePreviewPane consumes provider context to render the card stack"
      pattern: "<LivePreviewPane"
---

<objective>
Add the wizard's live preview to `AnalysoiSivusto`'s `preview` phase only: wrap that phase's JSX in `LivePreviewProvider` plus the exact desktop split-view / mobile toggle structure used by `WizardInner.tsx`, and keep the preview in sync with the screen's logo/color/gallery picks.

Purpose: Close the scoping gap from Phase 51 (D-02/D-03) — the user must see the actual venue card before quick-submitting ("Hyväksy ja lähetä") or continuing to the wizard ("Jatka velhoon →"). The other phase branches have no previewable data and stay unchanged (D-03).

Output: `app/business/onboarding/AnalysoiSivusto.tsx` modified — new `paikkaInfo` prop, `LivePreviewProvider` wrapping the preview phase, split-view/toggle layout, `SET_MEDIA` dispatches on logo/gallery picks, and a merged `brandingData` override for live colors.

This plan depends on Plan 01, which adds the `paikkaInfo` prop value at the call site in `page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51.1-live-preview-on-analysoisivusto-analyze-quick-accept-results/51.1-CONTEXT.md
@.planning/phases/51-live-esikatselu-velhossa/51-UI-SPEC.md

@app/business/onboarding/AnalysoiSivusto.tsx
@app/business/WizardInner.tsx
@lib/livePreview/LivePreviewContext.tsx
@app/business/onboarding/LivePreviewPane.tsx
@app/business/onboarding/LivePreviewToggle.tsx
@lib/onboardingUtils.ts
@lib/branding/brandingResult.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add paikkaInfo prop, activeView state, and provider/dispatch imports to AnalysoiSivusto</name>
  <files>app/business/onboarding/AnalysoiSivusto.tsx</files>
  <read_first>
    - `app/business/onboarding/AnalysoiSivusto.tsx` lines 1-21 — imports and `AnalysoiSivustoProps`. Current state:
      - Line 3: `import { useState, useEffect, useRef } from 'react'`.
      - Lines 5-8: imports `useRouter`, `Check`, `createBusinessBrowserClient`, `BrandingResult`, `ContrastSafeLogo`. NO live-preview imports yet.
      - Lines 14-21: `AnalysoiSivustoProps = { paikkaId, onConfirm, onSkip }` — NO `paikkaInfo`.
      - Line 96: `export default function AnalysoiSivusto({ paikkaId, onConfirm, onSkip }: AnalysoiSivustoProps)`.
    - `app/business/onboarding/AnalysoiSivusto.tsx` lines 96-123 — local state hooks. Note `selectedLogoUrl` (109), `bgColor` (110), `accentColor` (112), `selectedGallery` (122) already exist.
    - `app/business/WizardInner.tsx` lines 250-341 — the exact split-view/toggle JSX shape to replicate. Note: it uses an `activeView` state of type `'edit' | 'preview'` and `setActiveView`.
    - `app/business/onboarding/LivePreviewToggle.tsx` lines 18-21 — `LivePreviewToggleProps` confirms `activeView: 'edit' | 'preview'`.
    - `lib/livePreview/LivePreviewContext.tsx` lines 55-92 — `LivePreviewAction` (note `SET_MEDIA` payload `{ logo?: string | null; photos?: string[] }`) and reducer; lines 120-182 — `LivePreviewProvider` props (`paikkaInfo: PaikkaBase | null`, `paikkaId`, `brandingData?`, `initialDraft?`) and `useLivePreview`.
    - `lib/onboardingUtils.ts` lines 19-27 — `PaikkaBase` type.
  </read_first>
  <action>
    Update imports at the top of `AnalysoiSivusto.tsx`:
    1. Import the provider and the dispatch hook: `import { LivePreviewProvider, useLivePreview } from '@/lib/livePreview/LivePreviewContext'`.
    2. Import the two presentational components: `import LivePreviewPane from '@/app/business/onboarding/LivePreviewPane'` and `import LivePreviewToggle from '@/app/business/onboarding/LivePreviewToggle'`.
    3. Import the type: `import { type PaikkaBase } from '@/lib/onboardingUtils'`.

    Update `AnalysoiSivustoProps` (lines 14-21): add `paikkaInfo: PaikkaBase | null` to the interface.

    Update the component signature (line 96): destructure the new prop — `export default function AnalysoiSivusto({ paikkaId, paikkaInfo, onConfirm, onSkip }: AnalysoiSivustoProps)`.

    Add an `activeView` state hook near the other `useState` declarations (e.g. after line 122): `const [activeView, setActiveView] = useState<'edit' | 'preview'>('edit')`. Default `'edit'` so the pickers show first (mirrors WizardInner's reset-to-edit default, D-08 from Phase 51).

    Do NOT yet wire the JSX — that is Task 2 and Task 3. This task only establishes the prop, imports, and state.
  </action>
  <verify>
    <automated>cd C:/ClaudeCodeTestit/liikuntahakemisto && grep -q "LivePreviewProvider, useLivePreview" app/business/onboarding/AnalysoiSivusto.tsx && grep -q "import LivePreviewPane from" app/business/onboarding/AnalysoiSivusto.tsx && grep -q "import LivePreviewToggle from" app/business/onboarding/AnalysoiSivusto.tsx && grep -q "paikkaInfo: PaikkaBase | null" app/business/onboarding/AnalysoiSivusto.tsx && grep -q "useState<'edit' | 'preview'>('edit')" app/business/onboarding/AnalysoiSivusto.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "paikkaInfo: PaikkaBase | null" app/business/onboarding/AnalysoiSivusto.tsx` returns `1` (the prop added to `AnalysoiSivustoProps`).
    - The component signature destructures `paikkaInfo` (`grep -c "paikkaId, paikkaInfo, onConfirm, onSkip" app/business/onboarding/AnalysoiSivusto.tsx` returns `1`).
    - `grep -c "useState<'edit' | 'preview'>('edit')" app/business/onboarding/AnalysoiSivusto.tsx` returns `1`.
    - All three new imports present: `LivePreviewProvider, useLivePreview` from the context; `LivePreviewPane`; `LivePreviewToggle`; `type PaikkaBase`.
  </acceptance_criteria>
  <done>AnalysoiSivusto accepts paikkaInfo, imports the live-preview provider/components, and holds an activeView state — no JSX wired yet.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch SET_MEDIA on logo and gallery picks via an inner content component</name>
  <files>app/business/onboarding/AnalysoiSivusto.tsx</files>
  <read_first>
    - `app/business/onboarding/AnalysoiSivusto.tsx` lines 207-256 — the pick handlers:
      - `selectLogo(url)` (207-210): `setSelectedLogoUrl(url); patchBranding({ selected_logo_url: url }, 'logo')`.
      - `toggleGalleryImage(url)` (243-256): updates `setSelectedGallery(prev => ...)` with a 5-item cap and calls `patchBranding({ image_urls: next }, 'gallery')` inside the updater.
    - `app/business/onboarding/AnalysoiSivusto.tsx` lines 129-166 — the one-time init effect that seeds `selectedLogoUrl`, `selectedGallery`, `bgColor`, `accentColor` from `brandingResult`.
    - `lib/livePreview/LivePreviewContext.tsx` lines 73-86 (`SET_MEDIA` reducer — merges `logo`/`photos` individually) and 184-192 (`useLivePreview` throws if called outside a provider).
    - CONTEXT.md D-07 + Claude's Discretion: dispatch `SET_MEDIA` on every logo and gallery pick so `state.media_urls.logo`/`.photos` stay in sync; the provider overlays `state.media_urls?.logo` as `buildBrandingPreview`'s 4th arg.
  </read_first>
  <action>
    The dispatch must come from `useLivePreview()`, which only works inside the provider. Because the provider wraps the JSX (Task 3) but the pick handlers live in the top-level `AnalysoiSivusto` function (outside the provider), do NOT call `useLivePreview()` in the top-level component. Instead, introduce a thin inner content component that renders inside the provider and owns the dispatch + the split/toggle JSX.

    Create a new component in this file, e.g. `function PreviewPhaseContent(props: {...})`, that:
    1. Calls `const { dispatch } = useLivePreview()` at the top.
    2. Receives, via props from `AnalysoiSivusto`, everything the preview-phase JSX currently closes over: `brandingResult`, `selectedLogoUrl`, `selectedGallery`, `bgColor`, `accentColor`, `bgSource`, `accentSource`, `armedSlot`, `setArmedSlot`, `savingSection`, `saveError`, `customHexInput`, `setCustomHexInput`, `customHexError`, `submittingQuick`, `quickError`, `activeView`, `setActiveView`, and the handler functions (`selectLogo`, `toggleGalleryImage`, `handleSwatchClick`, `handleCustomHexSubmit`, `handleQuickAccept`, `onConfirm`, plus the "Analysoi uudelleen" reset closure / a callback to perform it). Pass them down as props rather than re-deriving — keep the existing handler bodies in `AnalysoiSivusto`.
    3. Wraps the existing pick callbacks so a `SET_MEDIA` dispatch fires alongside the existing behavior. Two approaches are acceptable — pick one and apply it consistently:
       - (a) In `AnalysoiSivusto`, change `selectLogo` to also `dispatch`… — NOT possible (dispatch unavailable there). So use approach (b):
       - (b) In `PreviewPhaseContent`, define local wrappers: `onSelectLogo = (url) => { selectLogoFromProps(url); dispatch({ type: 'SET_MEDIA', payload: { logo: url } }) }` and `onToggleGallery = (url) => { const next = toggleGalleryFromProps(url); }` — but `toggleGalleryImage` does not return the next array. To keep gallery in sync WITHOUT changing the existing handler's return contract, dispatch the gallery from a `useEffect` in `PreviewPhaseContent` keyed on `selectedGallery`: `useEffect(() => { dispatch({ type: 'SET_MEDIA', payload: { photos: selectedGallery } }) }, [selectedGallery, dispatch])`, and likewise mirror the logo with `useEffect(() => { dispatch({ type: 'SET_MEDIA', payload: { logo: selectedLogoUrl } }) }, [selectedLogoUrl, dispatch])`. This effect-driven sync covers the initial seed (init effect runs in the parent), every `selectLogo`, and every `toggleGalleryImage` uniformly. Prefer this effect-based approach — it is the simplest correct wiring and needs no changes to the existing handler signatures.
    4. Wire the JSX bindings in Task 3 to use the props passed in (the original handlers), since the effect handles the dispatch.

    Constraint: do NOT add a new reducer action. Colors are handled via the `brandingData` override prop in Task 3, NOT via dispatch (per CONTEXT.md Claude's Discretion — `brandColor`/`accentColor` derive from `brandingData` directly, not from reducer `state`).
  </action>
  <verify>
    <automated>cd C:/ClaudeCodeTestit/liikuntahakemisto && grep -q "function PreviewPhaseContent" app/business/onboarding/AnalysoiSivusto.tsx && grep -q "const { dispatch } = useLivePreview()" app/business/onboarding/AnalysoiSivusto.tsx && grep -c "type: 'SET_MEDIA'" app/business/onboarding/AnalysoiSivusto.tsx | grep -qE '^[23]$' && grep -q "payload: { photos: selectedGallery }" app/business/onboarding/AnalysoiSivusto.tsx && grep -q "payload: { logo: selectedLogoUrl }" app/business/onboarding/AnalysoiSivusto.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - A `PreviewPhaseContent` component exists in the file and calls `useLivePreview()` to obtain `dispatch`.
    - `grep -c "type: 'SET_MEDIA'" app/business/onboarding/AnalysoiSivusto.tsx` returns `2` (one logo effect, one gallery effect).
    - The gallery sync dispatches `{ photos: selectedGallery }` and the logo sync dispatches `{ logo: selectedLogoUrl }`.
    - No new variant is added to `LivePreviewAction` in `lib/livePreview/LivePreviewContext.tsx` (`git diff --stat` shows that file unchanged — colors are NOT dispatched).
  </acceptance_criteria>
  <done>PreviewPhaseContent obtains dispatch from the provider and keeps state.media_urls.logo/.photos in sync with the screen's logo and gallery selections via effects, with no reducer changes.</done>
</task>

<task type="auto">
  <name>Task 3: Wrap the preview-phase JSX in LivePreviewProvider + the WizardInner split-view/toggle pattern</name>
  <files>app/business/onboarding/AnalysoiSivusto.tsx</files>
  <read_first>
    - `app/business/onboarding/AnalysoiSivusto.tsx` lines 496-901 — the full render. Current state:
      - Line 498-499: outer wrapper `<div className="glass rounded-2xl p-6 w-full max-w-xl mx-auto flex flex-col gap-6">`.
      - Lines 502-581: the `checking`/`url-input`/`analyzing`/`error`/`timeout` branches — MUST stay unchanged (D-03).
      - Lines 583-898: the `{phase === 'preview' && brandingResult && ( <> ... </> )}` block — the integration point.
      - Line 880: the existing color merge `{ ...brandingResult, selected_background_color: bgColor, selected_accent_color: accentColor }` passed to `onConfirm` — reuse this exact override shape for the provider's `brandingData` prop.
      - Lines 899-901: closing `</div>` of the outer wrapper + component close.
    - `app/business/WizardInner.tsx` lines 251-341 — the exact JSX skeleton to replicate (outer `<div className="flex gap-6 items-start justify-center">`, inner `<div className="w-full max-w-xl">`, `lg:hidden` toggle, `activeView === 'preview' ? <div className="lg:hidden"><LivePreviewPane /></div> : <>{content}</>`, and the desktop `<div className="hidden lg:flex flex-col gap-4 w-[360px] flex-shrink-0 sticky top-6"><LivePreviewPane /></div>`).
    - `lib/livePreview/LivePreviewContext.tsx` lines 143-159 — confirms `brandingData` + `paikkaInfo` + numeric `paikka_id` triggers the `buildBrandingPreview` branch; line 145 overlays `state.media_urls?.logo`.
    - `.planning/phases/51-live-esikatselu-velhossa/51-UI-SPEC.md` — the visual/interaction contract; match the breakpoint and sticky column dimensions verbatim.
  </read_first>
  <action>
    Restructure ONLY the `preview` branch (lines 583-898). Leave the outer `glass` wrapper, the five non-preview branches (lines 502-581), and the closing tags intact.

    Replace the `{phase === 'preview' && brandingResult && ( <> ...existing preview JSX... </> )}` block with:

    `{phase === 'preview' && brandingResult && (`
      `<LivePreviewProvider`
        `paikkaInfo={paikkaInfo}`
        `paikkaId={paikkaId}`
        `brandingData={{ ...brandingResult, selected_background_color: bgColor, selected_accent_color: accentColor }}`
      `>`
        `<PreviewPhaseContent ...all the props from Task 2... />`
      `</LivePreviewProvider>`
    `)}`

    Notes on the provider props:
    - `brandingData`: pass the SAME merged override used at line 880 — `{ ...brandingResult, selected_background_color: bgColor, selected_accent_color: accentColor }` — so the preview's `brandColor`/`accentColor` reflect the user's live color picks (CONTEXT.md Claude's Discretion). Do NOT pass the raw `brandingResult` (that would freeze colors at the AI defaults).
    - Do NOT pass `initialDraft` — the screen has no draft; logo/photos flow through `SET_MEDIA` (Task 2). The provider's `buildInitialState` defaults are correct.
    - `paikkaId` is a `number` here (the preview branch only renders after analysis), satisfying the provider's numeric-`paikka_id` branch requirement.

    Inside `PreviewPhaseContent`, build the body by moving the existing preview-phase JSX (the logo picker, color picker, gallery picker, footer with "Analysoi uudelleen" / "Jatka velhoon →" / "Hyväksy ja lähetä") into the split/toggle skeleton copied verbatim from `WizardInner.tsx` lines 257-340. Concretely:
    - Outer: `<div className="flex gap-6 items-start justify-center">`.
    - Left column: `<div className="w-full max-w-xl">` containing:
      - `<div className="lg:hidden"><LivePreviewToggle activeView={activeView} onChange={setActiveView} /></div>`.
      - `{activeView === 'preview' ? (<div className="lg:hidden"><LivePreviewPane /></div>) : (<> ...the existing preview-phase pickers + footer... </>)}`.
    - Right column (desktop): `<div className="hidden lg:flex flex-col gap-4 w-[360px] flex-shrink-0 sticky top-6"><LivePreviewPane /></div>`.

    Keep every existing picker/footer element byte-for-byte (same handlers, same classNames, same `onConfirm` call at the footer including its line-880 merge) — only their wrapping changes. The `<h2>Analyysin tulokset</h2>` heading stays as the first child of the edit-view `<>`.

    Because the preview branch is now wrapped in its own flex layout, the outer `glass ... max-w-xl` wrapper at line 499 will double-constrain width. Match WizardInner's structure: the split layout should NOT sit inside the narrow `glass max-w-xl` card. Move the `preview` branch so the `LivePreviewProvider`/split layout renders OUTSIDE the `glass max-w-xl` wrapper (i.e. the `glass` card wraps only the five non-preview single-column branches; the preview branch renders as a sibling). If restructuring the outer wrapper is too invasive, the acceptable alternative is to keep the preview branch where it is but remove `max-w-xl` from the wrapper for the preview phase so the split view has room — choose whichever keeps the five non-preview branches visually unchanged AND gives the desktop split its full width. Document the chosen approach in the SUMMARY.
  </action>
  <verify>
    <automated>cd C:/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -c "AnalysoiSivusto.tsx" | grep -q '^0$' && grep -q "<LivePreviewProvider" app/business/onboarding/AnalysoiSivusto.tsx && grep -q 'selected_background_color: bgColor, selected_accent_color: accentColor' app/business/onboarding/AnalysoiSivusto.tsx && grep -q 'className="hidden lg:flex flex-col gap-4 w-\[360px\] flex-shrink-0 sticky top-6"' app/business/onboarding/AnalysoiSivusto.tsx && grep -q "LivePreviewToggle activeView={activeView} onChange={setActiveView}" app/business/onboarding/AnalysoiSivusto.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports ZERO errors in `AnalysoiSivusto.tsx` (and, combined with Plan 01, zero in `page.tsx` too — the cross-plan `paikkaInfo` error from Plan 01 is now resolved).
    - `grep -c "<LivePreviewProvider" app/business/onboarding/AnalysoiSivusto.tsx` returns `1`, with `brandingData={{ ...brandingResult, selected_background_color: bgColor, selected_accent_color: accentColor }}`.
    - `grep -c 'className="hidden lg:flex flex-col gap-4 w-\[360px\] flex-shrink-0 sticky top-6"' app/business/onboarding/AnalysoiSivusto.tsx` returns `1` (desktop sticky sidebar).
    - `grep -c "LivePreviewToggle activeView={activeView} onChange={setActiveView}" app/business/onboarding/AnalysoiSivusto.tsx` returns `1` (mobile toggle).
    - `grep -c "<LivePreviewPane" app/business/onboarding/AnalysoiSivusto.tsx` returns `2` (mobile preview-view branch + desktop sidebar).
    - The five non-preview branches (`checking`, `url-input`, `analyzing`, `error`, `timeout`) are byte-for-byte unchanged — `git diff` shows no edits within lines 502-581's logic (only possible wrapper relocation, which must not alter their rendered output).
    - The footer's `onConfirm(...)` call still passes the line-880 merge `{ ...brandingResult, selected_background_color: bgColor, selected_accent_color: accentColor }` and `{ logoUrl: selectedLogoUrl, gallery: selectedGallery }` unchanged.
  </acceptance_criteria>
  <done>The preview phase renders the live CalloutCard/DiagonaalKortti via LivePreviewProvider with the wizard's exact desktop split / mobile toggle layout; the five non-preview phases are unchanged; colors/logo/gallery update the preview live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client state → live preview render | Locally-selected logo/colors/gallery feed the visual preview only; not persisted via this code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51.1-03 | Tampering | `SET_MEDIA` payload + `brandingData` override feeding `LivePreviewPane` | accept | The provider performs pure synchronous derivation with no network/DB writes (LIVEPREV-04). Logo/gallery values were already server-validated by the existing `patchBranding` PATCH route (`/api/business/branding`) on selection; the preview reuses those same values for display only. Tampering alters only the local card render, never persisted data. Quick-accept/submit persistence is unchanged and still flows through the existing server-validated save-step/submit routes. |
| T-51.1-04 | Information Disclosure | `LivePreviewPane` rendering venue card | accept | The card shows only already-public venue fields (name, sport, address, price, hours) plus the user's own branding picks — identical to what the public consumer site and the existing wizard preview already display. No new data surfaced. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with zero errors across both `AnalysoiSivusto.tsx` and `page.tsx`.
- Live preview appears on the `preview` phase only; the five other phases render unchanged.
- Picking a logo / toggling a gallery image / picking colors visibly updates the preview (logo + photos via `SET_MEDIA`, colors via the `brandingData` override).
- Desktop: sticky `w-[360px]` sidebar present. Mobile: `LivePreviewToggle` swaps pickers ↔ preview.
- `lib/livePreview/LivePreviewContext.tsx`, `LivePreviewPane.tsx`, and `LivePreviewToggle.tsx` are unmodified (reused as-is, D-04).
</verification>

<success_criteria>
- The user sees the live venue card on the analyze-results screen before clicking "Hyväksy ja lähetä" or "Jatka velhoon →".
- The preview reflects logo, color, and gallery picks made on this screen in real time.
- The split-view/toggle pattern matches `WizardInner.tsx` verbatim and applies only to the `preview` phase.
- No reducer/context/component changes outside `AnalysoiSivusto.tsx` (and `page.tsx` from Plan 01).
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 02)

Modifies `app/business/onboarding/AnalysoiSivusto.tsx`:
- `AnalysoiSivustoProps` — new field `paikkaInfo: PaikkaBase | null`.
- `AnalysoiSivusto` — new destructured `paikkaInfo` prop; new `activeView: 'edit' | 'preview'` state; preview branch now wraps its JSX in `LivePreviewProvider` + `PreviewPhaseContent`.
- `PreviewPhaseContent` — NEW inner component (not exported): consumes `useLivePreview()`, dispatches `SET_MEDIA` for logo/photos via effects, and renders the WizardInner-style split-view/toggle layout around the existing pickers/footer.
- New imports: `LivePreviewProvider`, `useLivePreview` from `@/lib/livePreview/LivePreviewContext`; `LivePreviewPane`; `LivePreviewToggle`; `type PaikkaBase`.

Reused unchanged (no edits): `LivePreviewProvider`/`useLivePreview` (context), `LivePreviewPane`, `LivePreviewToggle`, `buildBrandingPreview`.
</artifacts>

<output>
Create `.planning/phases/51.1-live-preview-on-analysoisivusto-analyze-quick-accept-results/51.1-02-SUMMARY.md` when done.
</output>
