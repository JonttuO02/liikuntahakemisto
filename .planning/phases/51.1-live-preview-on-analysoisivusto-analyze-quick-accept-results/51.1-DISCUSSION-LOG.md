# Phase 51.1: Live preview on AnalysoiSivusto analyze/quick-accept results screen - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-18
**Phase:** 51.1-live-preview-on-analysoisivusto-analyze-quick-accept-results
**Areas discussed:** Placement, Scope

---

## Placement

| Option | Description | Selected |
|--------|-------------|----------|
| Top, above all pickers | Preview appears first, under "Analyysin tulokset" heading | |
| Sticky/pinned while scrolling | Preview stays visible while scrolling through pickers | |
| Bottom, just before action buttons | Final check right before commit | |
| Other (free text) | — | ✓ |

**User's choice:** "Same logic as in the onboarding." — i.e. replicate the wizard's exact desktop split-view (sticky sidebar) + mobile segmented toggle pattern, not a simpler inline placement.
**Notes:** This was a meaningful correction to the framing of the question — the user wants full consistency with `WizardInner.tsx`'s existing pattern, not a simplified one-off placement. Captured as D-02/D-03 in CONTEXT.md.

---

## Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, that's right | Only logo/color/gallery live; price/hours/contact stay static AI-scraped | ✓ |
| No — also want price/hours/contact editable here | Would require new inline editors, bigger change | |

**User's choice:** Confirmed — only logo/color/gallery picks are live; pricing/hours/contact remain the static AI-scraped snapshot, matching what's actually editable on this screen today.
**Notes:** Adding inline editors for price/hours/contact on this screen was explicitly named as a separate, larger phase if ever wanted.

---

## Claude's Discretion

- Exact dispatch wiring for color picks into `LivePreviewProvider`'s `brandingData` prop (state-based vs prop-override) — see CONTEXT.md Claude's Discretion section
- Exact gallery-pick wiring (`SET_MEDIA` dispatch vs inline override)
- No debounce hook needed — all fields on this screen are discrete-selection, not free text
- Whether `paikkaInfo` fetch extension duplicates `StepPaikkaPrePhase`'s query or factors out a shared helper

## Deferred Ideas

- Making pricing/hours/contact info editable on the AnalysoiSivusto screen — explicitly deferred to a future phase if ever wanted, per the Scope discussion above.
