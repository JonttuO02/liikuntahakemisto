---
phase: 51.1-live-preview-on-analysoisivusto-analyze-quick-accept-results
plan: 01
subsystem: ui
tags: [react, typescript, supabase, onboarding-wizard]

# Dependency graph
requires:
  - phase: 51-live-esikatselu-velhossa
    provides: PaikkaBase type, LivePreviewProvider expecting paikkaInfo prop
provides:
  - Full PaikkaBase (nimi, laji, osoite, kaupunki, latitude, longitude) threaded from StepPaikkaPrePhase through OnboardingWizardPage state into PrePhase and AnalysoiSivusto
affects: [51.1-02-live-preview-on-analyze-quick-accept-screen]

# Tech tracking
tech-stack:
  added: []
  patterns: [callback-lift pattern (onXResolved props) for surfacing pre-phase fetch results to parent page state]

key-files:
  created: []
  modified:
    - app/business/onboarding/page.tsx

key-decisions:
  - "Extended StepPaikkaPrePhase's existing partial-shape local paikkaInfo state to PaikkaBase, rather than introducing a second state variable, since PaikkaBase is a structural superset"
  - "No paikkaInfo fetch fallback added inside PrePhase — per plan D-06, paikkaInfo is sourced solely from StepPaikkaPrePhase; null is an accepted state for direct deep-links"

patterns-established:
  - "onPaikkaInfoResolved callback pattern mirrors the existing onPaikkaIdResolved lift-to-parent-state pattern"

requirements-completed: []

# Metrics
duration: 10min
completed: 2026-06-18
status: complete
---

# Phase 51.1 Plan 01: Thread PaikkaBase into AnalysoiSivusto Summary

**Extended StepPaikkaPrePhase's Supabase select to the full PaikkaBase column set and threaded the result through OnboardingWizardPage state into AnalysoiSivusto's new paikkaInfo prop.**

## Performance

- **Duration:** ~10 min
- **Completed:** 2026-06-18T12:29:53Z
- **Tasks:** 2 completed
- **Files modified:** 1

## Accomplishments
- `StepPaikkaPrePhase` now fetches `nimi, laji, osoite, kaupunki, latitude, longitude` (was `nimi, osoite, kaupunki`) and exposes the full row via a new `onPaikkaInfoResolved` callback
- `OnboardingWizardPage` holds `paikkaInfo: PaikkaBase | null` in state, captured from `StepPaikkaPrePhase`, and forwards it through `PrePhase` to `AnalysoiSivusto`'s `paikkaInfo` prop (added by Plan 02)

## Task Commits

Each task was committed atomically:

1. **Task 1 + Task 2: Extend fetch, lift to page state, thread through PrePhase to AnalysoiSivusto** - `9135bb5` (feat)

_Note: Tasks 1 and 2 were committed together — both modify tightly-coupled, sequential sections of the same file (`page.tsx`) where Task 2 directly depends on the callback prop Task 1 introduces. Splitting into two commits would have required an intermediate non-compiling state._

## Files Created/Modified
- `app/business/onboarding/page.tsx` - `StepPaikkaPrePhase` extended select + `onPaikkaInfoResolved` callback; `PrePhase` new `paikkaInfo` prop forwarded to `AnalysoiSivusto`; `OnboardingWizardPage` new `paikkaInfo` state wired to both children

## Decisions Made
- Combined Task 1 and Task 2 into a single commit since they are sequential edits to the same function chain in one file, and Task 2's JSX wiring directly depends on Task 1's new callback signature — no value in an intermediate broken-compile commit.
- Did not modify `StepPaikka.tsx`'s prop type — `PaikkaBase` is a structural superset of the partial shape it already expects (`nimi`, `osoite`, `kaupunki`), so TypeScript's structural typing accepts the wider object with no signature change needed.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The single expected `tsc` error on the `<AnalysoiSivusto paikkaInfo={paikkaInfo} ...>` JSX element (`Property 'paikkaInfo' does not exist on type 'AnalysoiSivustoProps'`) was confirmed present and is explicitly called out as acceptable in the plan's acceptance criteria — it is resolved by Plan 02 (wave 2), which widens `AnalysoiSivustoProps`. No other `tsc` errors were found in `page.tsx`.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 02 can now widen `AnalysoiSivustoProps` to accept `paikkaInfo: PaikkaBase | null` and consume it to feed `LivePreviewProvider`. The single expected `tsc` error on `<AnalysoiSivusto>` will resolve once Plan 02 lands. No blockers.

---
*Phase: 51.1-live-preview-on-analysoisivusto-analyze-quick-accept-results*
*Completed: 2026-06-18*

## Self-Check: PASSED

- FOUND: app/business/onboarding/page.tsx
- FOUND: .planning/phases/51.1-live-preview-on-analysoisivusto-analyze-quick-accept-results/51.1-01-SUMMARY.md
- FOUND: 9135bb5
- FOUND: 0268ba7
