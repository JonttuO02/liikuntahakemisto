---
phase: 28-svg-ikonit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sportIcons.ts
autonomous: true
requirements:
  - ICON-01

must_haves:
  truths:
    - "lib/sportIcons.ts exists and exports SPORT_ICONS as Record<string, string> with inner SVG markup for all 9 app sports plus a fallback key"
    - "All fill color attributes in the extracted path strings are replaced with fill=\"currentColor\" so consumers control color via CSS"
    - "lib/sportIcons.ts exports a SportIcon React component that renders an svg tag using dangerouslySetInnerHTML, falling back to the 'liikunta' icon for unknown sport keys"
    - "No consumer files are changed in this plan — the registry is self-contained"
  artifacts:
    - path: "lib/sportIcons.ts"
      provides: "SPORT_ICONS record and SportIcon component"
      exports: ["SPORT_ICONS", "SportIcon"]
      contains: "padel, kuntosali, jooga, uinti, tennis, liikuntahalli, liikunta, kiipeily, jääkiekko, fallback"
  key_links:
    - from: "lib/sportIcons.ts SportIcon"
      to: "SPORT_ICONS record"
      via: "dangerouslySetInnerHTML"
      pattern: "dangerouslySetInnerHTML.*SPORT_ICONS"
---

<objective>
Extract inner SVG markup from final_sports_svg_exports.zip and write lib/sportIcons.ts containing the SPORT_ICONS registry and SportIcon React component.

Purpose: Establish a single source of truth for SVG sport icons before any consumer is touched. Separating extraction from migration means if extraction produces unexpected markup the executor can fix lib/sportIcons.ts without reverting consumer changes.

Output: lib/sportIcons.ts with SPORT_ICONS record (9 sports + fallback) and SportIcon component; no other file changed.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-svg-ikonit/28-CONTEXT.md

Key facts:
- Source zip: final_sports_svg_exports.zip at project root
- Sports to extract (authoritative list from lajiKonfig keys in lib/lajit.ts):
  padel, kuntosali, jooga, uinti, tennis, liikuntahalli, liikunta, kiipeily, jääkiekko
- SVGs are fill-based (solid shapes). Any hardcoded fill="..." color must become fill="currentColor"
- fallback key: a simple circle — use path data `<circle cx="12" cy="12" r="4" fill="currentColor"/>`
- SportIcon viewBox must be "0 0 24 24" (matches SportPin's transform="translate(5,5) scale(0.75)" for 24px icons)
- dangerouslySetInnerHTML is safe: SPORT_ICONS is a compile-time constant (D-07)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unzip and extract inner SVG markup from final_sports_svg_exports.zip</name>
  <files>final_sports_svg_exports.zip (read), lib/sportIcons.ts (create)</files>
  <read_first>
    - lib/lajit.ts — authoritative list of lajiKonfig keys (9 sports); also shows the LucideIcon-based SPORT_ICONS that will eventually be replaced
    - app/components/SportPin.tsx lines 9–21 — shows the current inner-markup format (stroke-based g() wrapper) and fallback circle; the NEW icons are fill-based so no g() wrapper is needed
    - final_sports_svg_exports.zip — executor must unzip and read each sport's SVG file
  </read_first>
  <action>
    1. Unzip final_sports_svg_exports.zip to a temporary directory (e.g., use PowerShell Expand-Archive or Node's adm-zip / built-in zlib; choose whichever is available in the environment).
    2. For each of the 9 sport keys (padel, kuntosali, jooga, uinti, tennis, liikuntahalli, liikunta, kiipeily, jääkiekko), locate the matching SVG file by name. If the filename uses a different casing or has a suffix (e.g., padel.svg, padel-icon.svg), match case-insensitively to the sport key.
    3. From each SVG file, extract everything BETWEEN the outermost opening `<svg ...>` tag and the closing `</svg>` tag. Do NOT include the outer `<svg>` element itself.
    4. In the extracted inner markup, replace every occurrence of fill="#..." (any hex value) and fill="black" and fill="white" with fill="currentColor". Also replace any stroke="#..." with stroke="currentColor". The goal is that every path/shape inherits its fill color from the CSS `color` property.
    5. Write lib/sportIcons.ts with:
       - A file-level comment referencing the zip source
       - `export const SPORT_ICONS: Record<string, string>` object where each key is the sport slug and the value is the sanitized inner SVG markup string
       - Include a `fallback` key with value `<circle cx="12" cy="12" r="4" fill="currentColor"/>`
       - After SPORT_ICONS, export the SportIcon component as a named function component (NOT default export): `export function SportIcon({ laji, size = 16, className }: { laji: string; size?: number; className?: string })` that returns `<svg viewBox="0 0 24 24" width={size} height={size} className={className} dangerouslySetInnerHTML={{ __html: SPORT_ICONS[laji] ?? SPORT_ICONS['liikunta'] }} />`
       - The file must start with `'use client'` because SportIcon is a React component using JSX
       - Import React at the top if needed for JSX: `import React from 'react'` (only if the project does not have automatic JSX transform — check tsconfig.json; if jsx is "react-jsx" or "preserve" with automatic, no import needed)
  </action>
  <verify>
    <automated>cd C:\ClaudeCodeTestit\liikuntahakemisto && node -e "const m = require('./lib/sportIcons.ts'); console.log('ok')" 2>&1 || npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/sportIcons.ts exists
    - File exports `SPORT_ICONS` as `Record<string, string>` (TypeScript type annotation present)
    - SPORT_ICONS contains keys: padel, kuntosali, jooga, uinti, tennis, liikuntahalli, liikunta, kiipeily, jääkiekko, fallback (10 keys minimum)
    - No value in SPORT_ICONS contains `fill="#` or `fill="black"` or `fill="white"` — all fill colors are `fill="currentColor"`
    - File exports `SportIcon` as a named export (not default)
    - SportIcon renders `<svg viewBox="0 0 24 24" ...>` with `dangerouslySetInnerHTML`
    - SportIcon prop signature: `{ laji: string; size?: number; className?: string }`
    - SportIcon fallback: uses `SPORT_ICONS['liikunta']` when `SPORT_ICONS[laji]` is undefined
    - `npx tsc --noEmit` (run from project root, targeting lib/sportIcons.ts) produces zero TypeScript errors for this file
  </acceptance_criteria>
  <done>lib/sportIcons.ts written with SPORT_ICONS record (all 9 sports + fallback) and SportIcon component; no hardcoded fill colors; TypeScript compiles without errors for this file; no consumer files modified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| zip file → source code | SVG paths extracted from final_sports_svg_exports.zip are embedded as compile-time string constants in lib/sportIcons.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | dangerouslySetInnerHTML in SportIcon | accept | SPORT_ICONS is a compile-time constant — content never originates from user input or network; no runtime injection possible |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no new packages; zip extraction uses built-in OS or already-installed tools only |
</threat_model>

<verification>
After Task 1 completes:
- `lib/sportIcons.ts` exists in the project root lib/ directory
- Running `grep -c "fill=\"currentColor\"" lib/sportIcons.ts` returns a count >= 9 (one per sport path set minimum)
- Running `grep "fill=\"#"` on lib/sportIcons.ts returns zero matches
- File contains the string `export const SPORT_ICONS` and `export function SportIcon`
</verification>

<success_criteria>
lib/sportIcons.ts is the complete, standalone SVG icon registry:
1. SPORT_ICONS has 10 keys (9 sports + fallback) with inner SVG markup strings
2. All fill colors are currentColor — no hardcoded hex values
3. SportIcon component renders svg with dangerouslySetInnerHTML, size prop, className prop, liikunta fallback
4. tsc --noEmit passes for lib/sportIcons.ts with zero errors
5. No consumer file (SportPin, PaikkaKortti, DiagonaalKortti, Etusivu, lib/lajit.ts) has been modified
</success_criteria>

<output>
Create `.planning/phases/28-svg-ikonit/28-01-SUMMARY.md` when done
</output>
