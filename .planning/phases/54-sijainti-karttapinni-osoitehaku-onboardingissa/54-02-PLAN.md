---
phase: 54-sijainti-karttapinni-osoitehaku-onboardingissa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/MapProvider.tsx
  - app/components/SijaintiPicker.tsx
  - app/components/PlaceAutocompleteInput.tsx
autonomous: false
requirements: [SIJAINTI-01, SIJAINTI-02]
must_haves:
  truths:
    - "The Sijainti map renders inside the existing single APIProvider with no double-load warning"
    - "D-02: Clicking the map places a draggable pin (AdvancedMarker draggable) and reports lat/lng to the parent"
    - "D-04: Selecting a PlaceAutocompleteElement suggestion moves the pin, zooms the map, and reports the formatted address"
    - "Placing or dragging the pin reverse-geocodes to a city string, discarding all other Geocoding fields"
  artifacts:
    - path: "app/components/MapProvider.tsx"
      provides: "APIProvider with places library enabled (stable module-level array)"
      contains: "libraries"
    - path: "app/components/SijaintiPicker.tsx"
      provides: "Map + draggable AdvancedMarker + GPS default center + reverse-geocode + autocomplete composition"
      exports: ["default"]
      min_lines: 60
    - path: "app/components/PlaceAutocompleteInput.tsx"
      provides: "PlaceAutocompleteElement web-component wrapper emitting onPlaceSelected"
      exports: ["default"]
  key_links:
    - from: "app/components/SijaintiPicker.tsx"
      to: "app/components/PlaceAutocompleteInput.tsx"
      via: "renders PlaceAutocompleteInput and handles onPlaceSelected"
      pattern: "PlaceAutocompleteInput"
    - from: "app/components/SijaintiPicker.tsx"
      to: "hooks/useGPS.ts"
      via: "useGPS({ autoRequest: true }) for default center"
      pattern: "useGPS"
---

<objective>
Build the standalone, reusable location-picker UI: enable the `places` library on the existing single `APIProvider`, then create `SijaintiPicker.tsx` (Map + draggable AdvancedMarker + GPS default center + client-side reverse-geocode-to-city) and its `PlaceAutocompleteInput.tsx` web-component wrapper subcomponent.

Purpose: Delivers SIJAINTI-01 (click-to-place + drag) and SIJAINTI-02 (autocomplete → pin + zoom). Built as a standalone component per D-01 so Phase 56 reuses it rather than rebuilding. The picker calls the parent back with `{ lat, lng, address, city }`; it does not own form submission (Plan 03 wires it into ClaimSearchForm).
Output: One modified provider and two new components. Includes a blocking human-verify checkpoint because `PlaceAutocompleteElement` is a non-GA web component whose event contract type-checking cannot validate (RESEARCH.md Pitfall 1).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-RESEARCH.md
@.planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-PATTERNS.md
@.planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-UI-SPEC.md
@app/business/map/page.tsx
@hooks/useGPS.ts
</context>

<artifacts_this_phase_produces>
This plan (54-02) creates / modifies the following symbols:
- `LIBRARIES` — module-level `const ['places'] as const` array in `MapProvider.tsx`, passed to `APIProvider libraries={LIBRARIES}`
- `app/components/PlaceAutocompleteInput.tsx` — default-exported React component; props `{ onPlaceSelected: (place: { lat: number; lng: number; formattedAddress: string }) => void }`; listens for the `gmp-select` event and calls `place.fetchFields({ fields: ['formattedAddress', 'location'] })`
- `app/components/SijaintiPicker.tsx` — default-exported React component; props `{ onChange: (value: { lat: number | null; lng: number | null; address: string; city: string }) => void }` (exact prop contract finalized here and consumed by Plan 03); internal `reverseGeocodeCity(lat, lng): Promise<string | null>` helper; reuses `TAMPERE_CENTER = { lat: 61.4978, lng: 23.7610 }`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Enable the places library on the existing APIProvider</name>
  <files>app/components/MapProvider.tsx</files>
  <read_first>
    - app/components/MapProvider.tsx (full file — currently 11 lines, single APIProvider, no libraries prop)
    - .planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-RESEARCH.md (Pitfall 4 — libraries must be a stable module-level array set once, unconditionally)
    - .planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-PATTERNS.md (the MapProvider.tsx section — exact before/after)
  </read_first>
  <action>
    In `app/components/MapProvider.tsx`, declare a module-level constant `const LIBRARIES = ['places'] as const` above the component (NOT an inline literal in JSX — per RESEARCH.md Pitfall 4, an inline array re-created every render can trigger a Maps-JS double-load warning, violating ROADMAP success criterion 4). Add `libraries={LIBRARIES}` to the existing `<APIProvider>`. Do not add a second APIProvider anywhere. Do not change the `apiKey` prop. This is the only Maps provider in the app; it already wraps `/business/onboarding`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `app/components/MapProvider.tsx` contains `const LIBRARIES` at module scope and `libraries={LIBRARIES}` on the APIProvider.
    - Source assertion: the file contains exactly one occurrence of `APIProvider` opening tag (`grep -c '<APIProvider' app/components/MapProvider.tsx` returns 1).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The single APIProvider declares the places library via a stable module-level array; no second provider added.</done>
</task>

<task type="auto">
  <name>Task 2: Create PlaceAutocompleteInput.tsx web-component wrapper</name>
  <files>app/components/PlaceAutocompleteInput.tsx</files>
  <read_first>
    - .planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-PATTERNS.md (the PlaceAutocompleteInput.tsx section — verbatim reference implementation; "No Analog Found" note)
    - .planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-RESEARCH.md (Pattern 1, Pitfall 1 — use gmp-select NOT gmp-placeselect; use placePrediction.toPlace().fetchFields, NOT event.place)
    - .planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-UI-SPEC.md (Autocomplete dropdown styling exception — Google owns shadow-DOM chrome; do not override globally)
  </read_first>
  <action>
    Create `app/components/PlaceAutocompleteInput.tsx`, a `'use client'` default-exported component with props `{ onPlaceSelected: (place: { lat: number; lng: number; formattedAddress: string }) => void }`. Use `useMapsLibrary('places')` from `@vis.gl/react-google-maps`, a `containerRef` div, and an `elementRef`. In a `useEffect` keyed on `[placesLib, onPlaceSelected]`: bail if `!placesLib || !containerRef.current || elementRef.current`; construct `new placesLib.PlaceAutocompleteElement()` (expect to need a scoped `@ts-expect-error` — `@types/google.maps` has typing gaps per Pitfall 1), append it to the container, store in `elementRef`. Add a `gmp-select` listener (NOT `gmp-placeselect`) that calls `event.placePrediction.toPlace()`, `await place.fetchFields({ fields: ['formattedAddress', 'location'] })`, bails if `!place.location`, then calls `onPlaceSelected({ lat: place.location.lat(), lng: place.location.lng(), formattedAddress: place.formattedAddress ?? '' })`. Return a cleanup that removes the listener, calls `autocomplete.remove()`, and nulls `elementRef`. Render `<div ref={containerRef} className="w-full" />`. Wrap the element's container to visually approximate `INPUT_CLASS`'s box (border/height/radius) but do NOT attempt to restyle the Google-owned dropdown shadow DOM (UI-SPEC accepted exception). Request ONLY `['formattedAddress', 'location']` fields — never `place_id` or address components (SIJAINTI-03 client-tier minimization).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file contains `addEventListener('gmp-select'` and does NOT contain the string `gmp-placeselect` (`grep -c gmp-placeselect app/components/PlaceAutocompleteInput.tsx` returns 0).
    - Source assertion: file contains `fetchFields` with a fields array of exactly `formattedAddress` and `location` (no `place_id`, no `addressComponents`).
    - Source assertion: cleanup function calls `autocomplete.remove()` and removes the `gmp-select` listener.
    - `npx tsc --noEmit` exits 0 (scoped `@ts-expect-error` permitted where typings are incomplete).
  </acceptance_criteria>
  <done>A reusable wrapper mounts PlaceAutocompleteElement, fires onPlaceSelected on gmp-select with lat/lng/formattedAddress, requests only the two minimal fields, and cleans up on unmount.</done>
</task>

<task type="auto">
  <name>Task 3: Create SijaintiPicker.tsx composing map, draggable pin, GPS center, reverse-geocode, and autocomplete</name>
  <files>app/components/SijaintiPicker.tsx</files>
  <read_first>
    - app/business/map/page.tsx (Map + AdvancedMarker composition, TAMPERE_CENTER constant line 20, RecenterButton glass-btn pattern lines 40-52, click/drag handlers)
    - hooks/useGPS.ts (full file — useGPS({ autoRequest: true }) returns { status, coords, requestLocation })
    - .planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-PATTERNS.md (the SijaintiPicker.tsx section — Pattern 2 click/drag asymmetry, Pattern 3 reverse-geocode field allowlisting)
    - .planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-RESEARCH.md (Pattern 2, Pattern 3, Pitfall 2 click-vs-drag latLng shape asymmetry, Pitfall 3 Geocoding REQUEST_DENIED graceful fallback)
    - .planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-UI-SPEC.md (layout: map 320px above autocomplete above editable address above editable city; glassmorphism; muted hint copy; gap-3/gap-4)
  </read_first>
  <action>
    Create `app/components/SijaintiPicker.tsx`, a `'use client'` default-exported component with props `{ onChange: (value: { lat: number | null; lng: number | null; address: string; city: string }) => void }` (this exact contract is consumed by Plan 03). Define `const TAMPERE_CENTER = { lat: 61.4978, lng: 23.7610 }` (reuse the exact value/precision from `app/business/map/page.tsx` line 20) and `const MAP_ID = process.env.NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID`. Call `const { coords } = useGPS({ autoRequest: true })` and derive `const defaultCenter = coords ?? TAMPERE_CENTER` (D-03; do not write a new GPS hook). Hold local state: `pin: { lat: number; lng: number } | null`, `address: string`, `city: string`. Render a `<Map mapId={MAP_ID} defaultCenter={defaultCenter} defaultZoom={13} gestureHandling="greedy" style={{ width: '100%', height: '320px' }} onClick={handleMapClick}>` with a child `{pin && <AdvancedMarker position={pin} draggable onDragEnd={handleDragEnd} />}`. Write TWO explicitly-typed handlers (Pitfall 2 — do NOT share one): `handleMapClick` reads `event.detail.latLng` as a plain `{lat,lng}` literal; `handleDragEnd` reads `event.latLng.lat()` / `event.latLng.lng()` (method calls on a google.maps.LatLng). Both funnel into a shared `setPin(coords)` path. On every pin change (click, drag, or autocomplete select), call the internal `async reverseGeocodeCity(lat, lng): Promise<string | null>` (RESEARCH.md Pattern 3 verbatim: `new google.maps.Geocoder().geocode({ location })`, find the `address_components` entry whose `types` includes `'locality'`, return its `long_name`, return null on any failure/REQUEST_DENIED inside a try/catch so geocode failure NEVER blocks submission), then `setCity(result ?? '')`. NEVER store the raw `GeocoderResult` or `Place` object in state — extract primitives immediately (anti-pattern guard, SIJAINTI-03). Render `<PlaceAutocompleteInput onPlaceSelected={...} />` whose handler sets the pin, pans + zooms the map to the location (use `useMap()` + `map.panTo(loc)` + `map.setZoom(16)`), sets `address` to the returned `formattedAddress`, and triggers reverse-geocode. Below the map render an editable address `<input>` reusing the `INPUT_CLASS` constant style (D-05 — value `address`, onChange updates `address`, always user-editable) and an editable city `<input>` (D-06 + RESEARCH.md Open Question 2 — pre-filled by reverse-geocode, value `city`, always editable, never read-only). Above/around the map show a muted hint when `pin === null` (UI-SPEC `sijaintiHint`). Call `props.onChange({ lat, lng, address, city })` whenever any of pin/address/city changes (via a `useEffect` on those values) so the parent always has current values. Use only the existing glassmorphism classes and animation conventions (`whileTap={{ scale: 0.95 }}` only on any recenter button; no spring/scale-in on the pin per UI-SPEC). All user-facing strings must be passed in or routed through next-intl by the parent — for this standalone component, accept hint/placeholder copy via props OR use `useTranslations('Business')` directly (Plan 03 adds the keys; coordinate so keys match). Prefer `useTranslations('Business')` here with the keys named in Plan 03 (`sijaintiHint`, `osoiteHakuPlaceholder`, `kaupunkiAutoPlaceholder`).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `app/components/SijaintiPicker.tsx` imports `useGPS` from `@/hooks/useGPS` and `AdvancedMarker` from `@vis.gl/react-google-maps`.
    - Source assertion: contains a `draggable` AdvancedMarker and an `onClick` on the Map; click and drag are handled by two distinct functions (one uses `event.detail.latLng`, the other uses `.lat()`/`.lng()` method calls).
    - Source assertion: `reverseGeocodeCity` filters by `types.includes('locality')` and is wrapped in try/catch returning `null` on failure.
    - Source assertion: file contains no occurrence of `place_id` and never assigns a `GeocoderResult`/`results` object into React state (`grep -c place_id app/components/SijaintiPicker.tsx` returns 0).
    - Source assertion: reuses `TAMPERE_CENTER = { lat: 61.4978, lng: 23.7610 }` exactly.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>SijaintiPicker renders a GPS-or-Tampere-centered map with a click-placed, draggable pin; selecting an autocomplete suggestion moves+zooms the pin and prefills an editable address; pin changes reverse-geocode to an editable city; only lat/lng/address/city primitives flow to the parent via onChange.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the map picker (map load, GPS, click/drag pin, autocomplete, reverse-geocode)</name>
  <action>Manual browser verification — no code changes. Follow the how-to-verify steps below.</action>
  <what-built>The location picker UI: `MapProvider` now loads the `places` library, and `SijaintiPicker` (with its `PlaceAutocompleteInput` subcomponent) renders a map with a draggable pin, an address autocomplete, and editable address/city fields. Because `PlaceAutocompleteElement` is a non-GA Google web component whose `gmp-select` event contract cannot be validated by type-checking (RESEARCH.md Pitfall 1), and because Google Maps interactions are not unit-testable in jsdom (VALIDATION.md manual-only), these behaviors require a real browser.</what-built>
  <how-to-verify>
    Temporarily render `<SijaintiPicker onChange={(v) => console.log('picker', v)} />` on a dev page under the app (or use the Plan 03 wiring once available), then in a browser with the dev server running:
    1. Open browser dev tools → Network tab on the page. Confirm only ONE Google Maps JS API script load and NO console warning about the API being loaded multiple times / multiple APIProvider instances (ROADMAP success criterion 4).
    2. Confirm the map renders centered on your GPS location if you grant permission, or on Tampere (lat 61.4978, lng 23.7610) if you deny it.
    3. Click anywhere on the map → a pin appears at the clicked point; the console `picker` log shows updated lat/lng (SIJAINTI-01).
    4. Drag the placed pin → lat/lng updates on drag-end in the console log (SIJAINTI-01 drag).
    5. Type a partial address in the autocomplete field, select a suggestion → the pin moves to that location, the map zooms in, and the editable address field pre-fills with the formatted address (SIJAINTI-02). If the suggestion list never appears, the Places API may be disabled in Google Cloud Console (RESEARCH.md Pitfall 3 / Environment Availability) — flag this.
    6. After placing/dragging the pin into a known city, confirm the city field auto-fills with just the locality name (e.g. "Tampere"), and no other address data appears anywhere. If the city stays empty, the Geocoding API may be disabled in Cloud Console (RESEARCH.md Pitfall 3) — flag this; it must degrade gracefully (empty city, manual entry), not crash.
    7. Edit the address and city fields by hand → confirm they remain freely editable (D-05/D-06).
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe which step failed (and any Cloud Console API-enablement findings).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Maps/Places/Geocoding APIs → browser | Third-party responses (place predictions, GeocoderResult) enter the client; only minimal primitives must be extracted, the rest discarded. |
| browser GPS (navigator.geolocation) → component | User location is client-side only, never sent to a URL or server (CLAUDE.md constraint). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-03 | Information Disclosure | `SijaintiPicker.tsx` / `PlaceAutocompleteInput.tsx` — raw `Place`/`GeocoderResult` retained in state could leak `place_id` / address components into a later serialization (DevTools, form spread) | mitigate | Task 2 requests only `['formattedAddress','location']`; Task 3 extracts only `locality.long_name` and never stores the raw result object — only `{lat,lng,address,city}` primitives. (Server-tier enforcement is Plan 01's allowlist — defense in depth.) |
| T-54-04 | Information Disclosure | Maps/Geocoder client-side key exposure | accept | `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` is intentionally public and HTTP-referrer restricted per CLAUDE.md env var table; no new exposure introduced. |
| T-54-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — all map deps pre-existing (RESEARCH.md Package Legitimacy Audit). |
</threat_model>

<verification>
- `npx tsc --noEmit` — clean (scoped `@ts-expect-error` only where `@types/google.maps` lacks PlaceAutocompleteElement typings).
- Manual checkpoint above covers SIJAINTI-01, SIJAINTI-02, D-03 GPS, D-06 reverse-geocode, and ROADMAP criterion 4 (single Maps load) — all of which are manual-only per VALIDATION.md (Google Maps JS SDK is not practically unit-testable in jsdom).
</verification>

<success_criteria>
- Map renders inside the existing single APIProvider with no double-load (criterion 4).
- Pin can be placed by click and adjusted by drag (SIJAINTI-01).
- Autocomplete selection moves the pin, zooms the map, and prefills the editable address (SIJAINTI-02).
- City auto-fills from reverse-geocode (locality only); failure degrades gracefully.
- Only lat/lng/address/city primitives leave the component.
</success_criteria>

<output>
Create `.planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-02-SUMMARY.md` when done.
</output>
