---
phase: 54-sijainti-karttapinni-osoitehaku-onboardingissa
plan: 03
type: execute
wave: 2
depends_on: ["54-01", "54-02"]
files_modified:
  - app/components/ClaimSearchForm.tsx
  - messages/fi.json
  - messages/en.json
autonomous: false
requirements: [SIJAINTI-01, SIJAINTI-02, SIJAINTI-03]
must_haves:
  truths:
    - "The create step shows the SijaintiPicker instead of the plain osoite input and kaupunki select"
    - "Submitting the create form sends latitude and longitude in the POST body"
    - "Submit is blocked until a pin is placed (no NULL-coordinate venues)"
    - "All new strings are localized via next-intl in both fi and en"
  artifacts:
    - path: "app/components/ClaimSearchForm.tsx"
      provides: "create-step wiring of SijaintiPicker + lat/lng in submit body + pin-required guard"
      contains: "SijaintiPicker"
    - path: "messages/fi.json"
      provides: "Finnish source strings for the location step"
      contains: "sijaintiHint"
    - path: "messages/en.json"
      provides: "English translations for the location step"
      contains: "sijaintiHint"
  key_links:
    - from: "app/components/ClaimSearchForm.tsx"
      to: "app/components/SijaintiPicker.tsx"
      via: "renders SijaintiPicker in the create step and stores its onChange values"
      pattern: "SijaintiPicker"
    - from: "app/components/ClaimSearchForm.tsx"
      to: "app/api/business/create-paikka/route.ts"
      via: "POST body now includes latitude/longitude"
      pattern: "create-paikka"
---

<objective>
Wire the `SijaintiPicker` into `ClaimSearchForm.tsx`'s `create` step (per D-01), replacing the plain `osoite` text input and the manual `kaupunki` `<select>`, thread `latitude`/`longitude` into the create POST body, block submission until a pin exists, and add all required next-intl keys in both locales.

Purpose: This is the integration that makes the phase's capability reachable by a real user — the picker built in Plan 02 becomes live in the actual onboarding create flow, and the lat/lng accepted by Plan 01's route is actually sent. Completes SIJAINTI-01/02/03 end-to-end.
Output: A modified `ClaimSearchForm.tsx` create branch and new keys in `messages/fi.json` + `messages/en.json`. Includes an end-to-end human-verify checkpoint (the full create-with-location flow is manual-only per VALIDATION.md).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-CONTEXT.md
@.planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-UI-SPEC.md
@.planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-PATTERNS.md
@.planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-02-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan (54-03) creates / modifies the following symbols:
- `createLat` / `createLng` — new `useState<number | null>(null)` in `ClaimSearchForm.tsx`, fed by `SijaintiPicker`'s `onChange`
- `handleCreate` extension — POST body gains `latitude: createLat, longitude: createLng`; new pre-submit guard blocking when `createLat === null`
- next-intl keys under the `Business` namespace in both `messages/fi.json` and `messages/en.json`: `sijaintiLabel`, `sijaintiHint`, `osoiteHakuPlaceholder`, `kaupunkiAutoPlaceholder`, `sijaintiVirhe`, `sijaintiPakollinen` (FI and EN values per UI-SPEC Copywriting Contract)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add next-intl keys for the location step in fi.json and en.json</name>
  <files>messages/fi.json, messages/en.json</files>
  <read_first>
    - messages/fi.json (the Business namespace — find existing keys createTitle, createNamePlaceholder, createAddressPlaceholder, createCta to place the new keys alongside)
    - messages/en.json (the Business namespace — same keys, ensure parity)
    - .planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-UI-SPEC.md (Copywriting Contract table — exact FI source + EN translation for each key)
  </read_first>
  <action>
    Add these keys under the existing `Business` namespace in BOTH `messages/fi.json` and `messages/en.json` (keys must exist in both files with matching key names — next-intl parity). FI values (fi.json): `sijaintiLabel` = "Sijainti"; `sijaintiHint` = "Klikkaa karttaa tai hae osoitetta asettaaksesi sijainnin"; `osoiteHakuPlaceholder` = "Hae osoitetta…"; `kaupunkiAutoPlaceholder` = "Kaupunki (täytetään automaattisesti)"; `sijaintiVirhe` = "Sijainnin hakeminen epäonnistui. Voit silti kirjoittaa osoitteen ja kaupungin käsin."; `sijaintiPakollinen` = "Aseta paikan sijainti kartalle ennen tallentamista.". EN values (en.json): `sijaintiLabel` = "Location"; `sijaintiHint` = "Click the map or search an address to set the location"; `osoiteHakuPlaceholder` = "Search for an address…"; `kaupunkiAutoPlaceholder` = "City (filled automatically)"; `sijaintiVirhe` = "Couldn't look up the location. You can still type the address and city manually."; `sijaintiPakollinen` = "Set the venue's location on the map before saving.". Keep the existing `createAddressPlaceholder` key (the editable address input still uses it). Do not remove any existing keys.
  </action>
  <verify>
    <automated>node -e "const fi=require('./messages/fi.json').Business, en=require('./messages/en.json').Business; const k=['sijaintiLabel','sijaintiHint','osoiteHakuPlaceholder','kaupunkiAutoPlaceholder','sijaintiVirhe','sijaintiPakollinen']; const miss=k.filter(x=>!fi[x]||!en[x]); if(miss.length){console.error('MISSING',miss);process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - CLI: the node parity check above prints `ok` and exits 0 (all six keys present and non-empty in both files).
    - Source assertion: `messages/fi.json` parses as valid JSON and `messages/en.json` parses as valid JSON (the node require would throw otherwise).
    - Behavior assertion: no existing Business key was removed (createTitle, createNamePlaceholder, createAddressPlaceholder, createCta still present).
  </acceptance_criteria>
  <done>All six location-step keys exist with the specified FI and EN values in both message files; both files remain valid JSON; no existing keys removed.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SijaintiPicker into the ClaimSearchForm create step and send lat/lng on submit</name>
  <files>app/components/ClaimSearchForm.tsx</files>
  <read_first>
    - app/components/ClaimSearchForm.tsx (state declarations lines 49-52; handleCreate lines 129-173; create-step JSX lines 396-474; INPUT_CLASS/SELECT_CLASS/CTA_CLASS lines 25-32)
    - app/components/SijaintiPicker.tsx (the component created in Plan 02 — its onChange prop contract is `{ lat: number|null; lng: number|null; address: string; city: string }`)
    - .planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-PATTERNS.md (the ClaimSearchForm.tsx section — what to replace and what to preserve)
    - .planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-UI-SPEC.md (layout + the sijaintiPakollinen / disabled-CTA belt-and-suspenders requirement)
  </read_first>
  <action>
    In `app/components/ClaimSearchForm.tsx`: (1) Add `import SijaintiPicker from './SijaintiPicker'`. (2) Add state `const [createLat, setCreateLat] = useState<number | null>(null)` and `const [createLng, setCreateLng] = useState<number | null>(null)`. Keep `createOsoite` and `createKaupunki` as controlled state (they stay editable per D-05/D-06) but change `createKaupunki`'s initial value from `'Tampere'` to `''` (it is now auto-filled by reverse-geocode, not a fixed default). (3) In the `create` step JSX (lines ~424-449), REMOVE the plain `osoite` `<input>` (lines 433-439) and the `kaupunki` `<select>` (lines 440-449). Insert `<SijaintiPicker onChange={({ lat, lng, address, city }) => { setCreateLat(lat); setCreateLng(lng); setCreateOsoite(address); setCreateKaupunki(city) }} />` after the name input, inside the same `<form className="flex flex-col gap-3">`. The SijaintiPicker itself renders the editable address and city inputs (per Plan 02), so do not add separate ones here. Add the `t('sijaintiLabel')` heading above the picker using the same `text-sm font-bold text-[#111111]` style as `createTitle`. (4) In `handleCreate` (lines 129-173): add a pre-submit guard after the existing `createOsoite` check — if `createLat === null || createLng === null`, call `setError(t('sijaintiPakollinen'))` and `return` (RESEARCH.md Open Question 1 — hard requirement, no NULL coordinates). Add `latitude: createLat, longitude: createLng` to the `JSON.stringify({...})` POST body (alongside the existing nimi/osoite/kaupunki). (5) On the submit `<button>` add `disabled={loading || createLat === null}` (belt-and-suspenders per UI-SPEC, matching the existing `disabled={loading}` pattern). (6) Preserve the existing `AnimatePresence mode="wait"` / `key="create"` / opacity-only crossfade / `transition={{ duration: 0.2 }}` wrapper and the back button exactly — do not introduce a new animation pattern. Do not touch the `search` or `claim` steps, the search-step `kaupunki` filter dropdown (lines ~199-209 — unrelated), or `handleSearch`/`handleClaim`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `app/components/ClaimSearchForm.tsx` imports and renders `SijaintiPicker`.
    - Source assertion: the create-step JSX no longer contains a `<select` with `value={createKaupunki}` and no longer contains the standalone `osoite` `<input>` with `placeholder={t('createAddressPlaceholder')}` directly in the form (those now live inside SijaintiPicker).
    - Source assertion: `handleCreate`'s fetch body includes `latitude: createLat` and `longitude: createLng`.
    - Source assertion: `handleCreate` returns early with `t('sijaintiPakollinen')` when `createLat === null`.
    - Source assertion: the submit button has `disabled={loading || createLat === null}`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The create step renders SijaintiPicker in place of the old address input and city select; submission sends latitude/longitude, is blocked (guard + disabled button) until a pin exists, and all copy is localized.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the end-to-end create-with-location flow and SIJAINTI-03 persistence</name>
  <action>Manual browser + Supabase verification — no code changes. Follow the how-to-verify steps below.</action>
  <what-built>The full create-from-scratch onboarding flow now uses the map location picker: the `create` step shows the name input, the SijaintiPicker (map + autocomplete + editable address + editable city), and the Create CTA. Submitting sends latitude/longitude to the create-paikka route (Plan 01), which persists them. The end-to-end map flow is manual-only per VALIDATION.md (Google Maps JS SDK + real Supabase write are not unit-testable here).</what-built>
  <how-to-verify>
    With the dev server running, navigate to the business onboarding entry that renders `ClaimSearchForm` and switch to the "create from scratch" step:
    1. Confirm the create step shows: a name input, a "Sijainti"/"Location" heading, the map picker, an editable address field, an editable city field, and the Create button — and that the old standalone city dropdown is gone.
    2. With no pin placed, confirm the Create button is disabled; if you somehow submit, confirm the "Set the venue's location…" error appears (SIJAINTI-01 guard).
    3. Place a pin by clicking the map (or pick an address via autocomplete). Confirm the address field prefills (autocomplete path) and the city field auto-fills with the locality. Edit both fields by hand to confirm they remain editable (D-05/D-06).
    4. Fill the name, ensure a pin is placed, click Create. Confirm the flow redirects to `/business/onboarding?paikka_id=...` (success).
    5. In Supabase (or via the admin/onboarding view), confirm the newly created `liikuntapaikat` row has the latitude/longitude you placed and the address text you typed — and that NO place_id or other Google Places data was stored (SIJAINTI-03). The automated test in Plan 01 already guards this at the route level; this step confirms the real end-to-end write.
  </how-to-verify>
  <resume-signal>Type "approved" if the create-with-location flow works end-to-end and only lat/lng + typed address persisted, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ClaimSearchForm (browser) → `POST /api/business/create-paikka` | The form assembles the POST body; the server (Plan 01) is the authoritative allowlist/validation point — the client guard (pin-required, disabled button) is UX only, not a security control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-05 | Tampering | `ClaimSearchForm.handleCreate` — client could be bypassed (devtools) to submit without a pin or with junk lat/lng | mitigate | Defense in depth: client pin-required guard + disabled CTA here are UX; the authoritative `Number.isFinite` + range + mandatory check is enforced server-side in Plan 01 (T-54-01). This plan does not rely on the client guard for correctness. |
| T-54-06 | Information Disclosure | `handleCreate` POST body could accidentally include extra Places fields via object spread | mitigate | Task 2 builds the body by explicit named keys only (`nimi, osoite, kaupunki, latitude, longitude`); no `...` spread of picker output into the body. SijaintiPicker.onChange only ever yields the four primitives (Plan 02 T-54-03). |
| T-54-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — RESEARCH.md Package Legitimacy Audit. |
</threat_model>

<verification>
- `npx tsc --noEmit` — clean.
- `npx vitest run tests/api/create-paikka.test.ts` and `npm test` — still green (no regressions; this plan does not change route behavior, only the caller).
- next-intl key parity check (Task 1 node command) — `ok`.
- End-to-end create-with-location flow — manual checkpoint (manual-only per VALIDATION.md).
</verification>

<success_criteria>
- The create step uses SijaintiPicker; old osoite input + kaupunki select removed (SIJAINTI-01/02 reachable in the real flow).
- Submission sends latitude/longitude and is blocked until a pin is placed.
- Only lat/lng + user-typed address/city persist end-to-end (SIJAINTI-03 confirmed at the route + DB).
- All new strings localized in fi and en.
</success_criteria>

<output>
Create `.planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-03-SUMMARY.md` when done.
</output>
