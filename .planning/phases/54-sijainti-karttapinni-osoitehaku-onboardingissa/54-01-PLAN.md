---
phase: 54-sijainti-karttapinni-osoitehaku-onboardingissa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/business/create-paikka/route.ts
  - tests/api/create-paikka.test.ts
autonomous: true
requirements: [SIJAINTI-03]
must_haves:
  truths:
    - "Submitting the create form with latitude/longitude persists those exact values to liikuntapaikat"
    - "A request body containing place_id (or other Places/Geocoding fields) never writes place_id to the database"
    - "A request with non-finite or missing latitude/longitude is rejected with 400, not silently coerced"
  artifacts:
    - path: "app/api/business/create-paikka/route.ts"
      provides: "lat/lng allowlist parsing + insert"
      contains: "Number.isFinite"
    - path: "tests/api/create-paikka.test.ts"
      provides: "SIJAINTI-03 data-minimization + lat/lng validation regression tests"
      contains: "does not persist place_id"
  key_links:
    - from: "tests/api/create-paikka.test.ts"
      to: "app/api/business/create-paikka/route.ts"
      via: "imports POST handler and asserts insert payload shape"
      pattern: "create-paikka/route"
---

<objective>
Extend the create-from-scratch write path to accept and persist `latitude`/`longitude` while enforcing SIJAINTI-03's hard data-minimization constraint server-side, and create the missing automated test file that proves both behaviors.

Purpose: This is the only automatable surface in the phase (the map UI is manual-only per VALIDATION.md). It is the SIJAINTI-03 enforcement point — the server must never persist `place_id` or other raw Places/Geocoding fields even if the client sends them. It also fills the RESEARCH.md Wave 0 Gap (`tests/api/create-paikka.test.ts` did not exist).
Output: An extended `create-paikka/route.ts` (lat/lng allowlist + finite/range validation + insert) and a new `tests/api/create-paikka.test.ts` covering data-minimization and lat/lng validation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-RESEARCH.md
@.planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan (54-01) creates / modifies the following symbols:
- `latitude` / `longitude` — new accepted request-body fields on `POST /api/business/create-paikka`, typed `number | null`, validated with `typeof === 'number' && Number.isFinite(...)` and range-checked to lat ∈ [-90, 90] / lng ∈ [-180, 180]
- `tests/api/create-paikka.test.ts` — new Vitest file with the `vi.mock('next/server', ...)` + chainable Supabase-admin builder pattern, exporting no symbols (test suite for `POST /api/business/create-paikka`)
- A mocked `@/lib/email` (`sendAdminNotificationEmail`) in the test to suppress side effects
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend create-paikka route to accept and persist latitude/longitude with server-side allowlist</name>
  <files>app/api/business/create-paikka/route.ts</files>
  <read_first>
    - app/api/business/create-paikka/route.ts (the file being modified — current trim+slice body-parsing block is lines 26-41, insert is lines 46-50)
    - .planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-RESEARCH.md (Code Examples section + Security Domain table + Pitfall 5)
    - .planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-PATTERNS.md (the `create-paikka/route.ts` section — exact lines to extend)
    - lib/types.ts (confirm liikuntapaikat.latitude/longitude columns already exist — this phase populates, does not add columns)
  </read_first>
  <action>
    In `app/api/business/create-paikka/route.ts`, extend ONLY the body-parsing block and the `liikuntapaikat` insert. Declare two new locals `latitude: number | null` and `longitude: number | null`. Inside the existing `try` block (after the `kaupunki` line), parse them with an explicit allowlist: `latitude = typeof body.latitude === 'number' && Number.isFinite(body.latitude) ? body.latitude : null` and the same for `longitude`. Add range validation: latitude must be within [-90, 90] and longitude within [-180, 180] — values outside these bounds become `null`. Make both mandatory: change the existing `if (!nimi || !osoite || !kaupunki)` guard to also fail when `latitude === null || longitude === null`, returning the same `{ error: 'Missing fields' }` 400 (per RESEARCH.md Open Question 1 — pin placement is a hard requirement, no NULL coordinates allowed for newly created venues). Extend the `.insert({ nimi, osoite, kaupunki, laji: 'Muu', published: false })` call to `.insert({ nimi, osoite, kaupunki, latitude, longitude, laji: 'Muu', published: false })`. CRITICAL (SIJAINTI-03): never read `body.place_id`, `body.formatted_address`, or any other Places/Geocoding field, and never spread `...body` into the insert — destructure each field by name only. Do NOT touch JWT verification (lines 9-14), the business-account check (lines 17-24), the link insert (lines 63-79), the `is_claimed` update (lines 84-91), or the admin-email block (lines 94-123).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `app/api/business/create-paikka/route.ts` contains `Number.isFinite(body.latitude)` and `Number.isFinite(body.longitude)`.
    - Source assertion: the `.insert(` call on the `liikuntapaikat` table includes `latitude` and `longitude` keys.
    - Source assertion: the file contains no occurrence of `place_id`, `formatted_address`, or `...body` (negative grep over `app/api/business/create-paikka/route.ts`: `grep -v '^[[:space:]]*//' app/api/business/create-paikka/route.ts | grep -c -e place_id -e formatted_address` returns 0).
    - Behavior assertion: the `Missing fields` 400 guard now also triggers when `latitude` or `longitude` is `null`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Route accepts numeric finite in-range latitude/longitude, persists them in the liikuntapaikat insert, rejects requests missing valid coordinates with 400, and reads no Places/Geocoding fields from the body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/api/create-paikka.test.ts covering SIJAINTI-03 data-minimization and lat/lng validation</name>
  <files>tests/api/create-paikka.test.ts</files>
  <read_first>
    - tests/api/update-paikka.test.ts (the analog — copy its `vi.mock('next/server', ...)` shape and chainable-builder-per-table pattern; note its `makeRequest`/`makeRawRequest`/`VALID_USER`/`setAuthSuccess` helpers)
    - app/api/business/create-paikka/route.ts (the route under test, AFTER Task 1 — note it imports `sendAdminNotificationEmail` from `@/lib/email`, queries `business_accounts` then inserts into `liikuntapaikat` and `business_paikka_links`, then updates `liikuntapaikat`)
    - .planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-PATTERNS.md (the `tests/api/create-paikka.test.ts` section — new test cases to add)
  </read_first>
  <behavior>
    - Test: POST with a valid auth user, a business_accounts row, and body `{ nimi, osoite, kaupunki, latitude: 61.5, longitude: 23.7 }` → the `liikuntapaikat` `.insert(...)` is called with a payload object that includes `latitude: 61.5` and `longitude: 23.7`.
    - Test ("does not persist place_id"): POST with the same valid body PLUS `place_id: 'ChIJxxxx'` and `formatted_address: '...'` → the captured `.insert(...)` payload has no `place_id` and no `formatted_address` key.
    - Test ("rejects invalid lat/lng"): POST with `latitude: NaN` (or omitted) → response status 400 with `{ error: 'Missing fields' }`; the `liikuntapaikat` `.insert(...)` is never called.
    - Test ("rejects out-of-range lat/lng"): POST with `latitude: 999, longitude: 23.7` → status 400, insert not called.
    - Test (happy path): a fully valid request → status 200 with `{ ok: true, paikka_id: <number> }`.
  </behavior>
  <action>
    Create `tests/api/create-paikka.test.ts`. Copy the mock scaffolding shape from `tests/api/update-paikka.test.ts`: `vi.mock('next/server', ...)` returning a `NextResponse.json` stub, and `vi.mock('@/lib/supabaseAdmin.server', ...)` with a `from(table)` switch returning chainable builders per table. Add a `vi.mock('@/lib/email', () => ({ sendAdminNotificationEmail: vi.fn().mockResolvedValue(undefined) }))` so the admin-email side effect is suppressed. Build a chainable mock for `liikuntapaikat` whose `.insert(payload)` captures `payload` into a spy (e.g. `mockLiikuntapaikatInsert`) and returns a builder whose `.select('id').single()` resolves `{ data: { id: 42 }, error: null }`; its `.update(...).eq(...)` resolves `{ error: null }`; its `.select('nimi').eq().single()` resolves a name for the email block. Mock `business_accounts` `.select().eq().maybeSingle()` → `{ data: { user_id: 'user-123' }, error: null }` (the existence check) and `.select('company_name').eq().single()` → a company name. Mock `business_paikka_links` `.insert(...)` → `{ error: null }` and `.select('id').eq().eq().single()` → `{ data: { id: 7 }, error: null }`. Mock `mockGetUser` → `{ data: { user: { id: 'user-123' } }, error: null }`. Import `POST` from `@/app/api/business/create-paikka/route` AFTER the mocks. Implement the five behavior cases above. Use `expect(mockLiikuntapaikatInsert).toHaveBeenCalledWith(expect.objectContaining({ latitude: 61.5, longitude: 23.7 }))` and `expect(mockLiikuntapaikatInsert).toHaveBeenCalledWith(expect.not.objectContaining({ place_id: expect.anything() }))` for the data-minimization guard. Title the data-minimization test so it matches `-t "does not persist place_id"` and the validation test so it matches `-t "rejects invalid lat/lng"` (the exact strings VALIDATION.md's Per-Task Verification Map references).
  </action>
  <verify>
    <automated>npx vitest run tests/api/create-paikka.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/api/create-paikka.test.ts` passes with all five tests green.
    - CLI: `npx vitest run tests/api/create-paikka.test.ts -t "does not persist place_id"` matches and passes exactly one test.
    - CLI: `npx vitest run tests/api/create-paikka.test.ts -t "rejects invalid lat/lng"` matches and passes exactly one test.
    - Behavior assertion: the data-minimization test fails if the route is reverted to spread `...body` or read `body.place_id` (the test is a true regression guard, not a tautology).
    - Source assertion: the test file imports `POST` from `@/app/api/business/create-paikka/route`.
  </acceptance_criteria>
  <done>The new test file is green, proves latitude/longitude are persisted, proves place_id/formatted_address are never persisted even when present in the body, and proves invalid/out-of-range coordinates are rejected with 400.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `POST /api/business/create-paikka` | Untrusted JSON body (nimi, osoite, kaupunki, latitude, longitude, plus anything else the client might send) crosses into the JWT-verified write path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | `create-paikka/route.ts` body parsing — client sends out-of-range / non-finite (`NaN`/`Infinity`) latitude or longitude | mitigate | Task 1: `typeof === 'number' && Number.isFinite(...)` + range check lat ∈ [-90,90] / lng ∈ [-180,180]; reject (400 `Missing fields`) rather than coerce. Asserted by Task 2 "rejects invalid lat/lng" + "rejects out-of-range lat/lng". |
| T-54-02 | Tampering / Information Disclosure | `create-paikka/route.ts` — client smuggles `place_id` / `formatted_address` / raw Places fields into the body hoping the server persists them (data-minimization bypass; undermines the v3.0 Google-Places-decoupling goal) | mitigate | Task 1: explicit per-field allowlist destructuring, never `...body`, never read `body.place_id`. Asserted by Task 2 "does not persist place_id" regression guard. |
| T-54-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase — RESEARCH.md Package Legitimacy Audit confirms all deps pre-existing; gate not applicable. |
</threat_model>

<verification>
- `npx vitest run tests/api/create-paikka.test.ts` — all tests green.
- `npx tsc --noEmit` — no type errors introduced.
- `grep -v '^[[:space:]]*//' app/api/business/create-paikka/route.ts | grep -c -e place_id -e formatted_address` returns 0 (SIJAINTI-03 source-level guard).
</verification>

<success_criteria>
- create-paikka route accepts, validates, and persists latitude/longitude.
- No Places/Geocoding field other than lat/lng can reach the database via this route.
- New automated test file exists and is green, filling the RESEARCH.md Wave 0 Gap.
</success_criteria>

<output>
Create `.planning/phases/54-sijainti-karttapinni-osoitehaku-onboardingissa/54-01-SUMMARY.md` when done.
</output>
