---
phase: 43-business-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260615000000_business_accounts_contact_phone.sql
autonomous: false
requirements:
  - BIZPRO-02

must_haves:
  truths:
    - "contact_phone TEXT column exists in business_accounts on the remote Supabase instance"
    - "The migration file is idempotent (IF NOT EXISTS guard)"
    - "supabase db push exits 0 with no errors"
  artifacts:
    - path: "supabase/migrations/20260615000000_business_accounts_contact_phone.sql"
      provides: "contact_phone column addition"
      contains: "ADD COLUMN IF NOT EXISTS contact_phone TEXT"
  key_links:
    - from: "supabase/migrations/20260615000000_business_accounts_contact_phone.sql"
      to: "business_accounts table (remote)"
      via: "supabase db push"
      pattern: "contact_phone"
---

<objective>
Write the DB migration that adds contact_phone to business_accounts, verify the project is linked, then push it to the remote Supabase instance.

Purpose: The RSC page (Wave 2) runs a SELECT on contact_phone. Without this column existing on the remote DB, the runtime query fails with a column-not-found error even though TypeScript compiles cleanly. This plan is the gate that unblocks Wave 2.
Output: Migration file committed + column live on remote Supabase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing business_accounts columns (from supabase/migrations/20260605000000_business_accounts.sql) -->
<!--
user_id         UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE
company_name    TEXT NOT NULL
approval_status TEXT NOT NULL DEFAULT 'pending' CHECK (...)
created_at      TIMESTAMPTZ NOT NULL DEFAULT now()
-->

<!-- Existing RLS policy that covers the new column automatically -->
<!--
CREATE POLICY "Business updates own account"
  ON business_accounts FOR UPDATE
  USING (auth.uid() = user_id)
  WITH CHECK (auth.uid() = user_id);
-->

<!-- Migration filename convention from this project -->
<!-- Most recent: 20260612000000_cleanup_test_accounts.sql -->
<!-- Phase 43 (today 2026-06-15): 20260615000000_business_accounts_contact_phone.sql -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write contact_phone migration file</name>
  <files>supabase/migrations/20260615000000_business_accounts_contact_phone.sql</files>
  <read_first>
    - supabase/migrations/20260605000000_business_accounts.sql — verify current columns and RLS policy names before writing; confirms "Business updates own account" UPDATE policy covers any column on the row
    - supabase/migrations/ directory listing — confirm 20260615000000 timestamp is the next in sequence and no conflict exists
  </read_first>
  <action>
    Create supabase/migrations/20260615000000_business_accounts_contact_phone.sql with exactly this content (per D-03):
    - Comment block: "Phase 43: Add contact_phone to business_accounts" + "Purpose: Business users can store their personal phone number for admin-to-user contact." + "RLS: existing Business updates own account policy covers UPDATE on this column."
    - SQL statement: ALTER TABLE business_accounts ADD COLUMN IF NOT EXISTS contact_phone TEXT;
    No index, no CHECK constraint, no DEFAULT. The IF NOT EXISTS guard makes the migration safe to re-run. The column is nullable with no default — a business_accounts row may have NULL contact_phone until the user sets one.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS contact_phone TEXT" supabase/migrations/20260615000000_business_accounts_contact_phone.sql</automated>
  </verify>
  <done>File exists at the correct path; grep returns 1 (one matching line); file contains the Phase 43 comment and the ALTER TABLE statement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify project link and push migration to remote Supabase</name>
  <what-built>The migration file supabase/migrations/20260615000000_business_accounts_contact_phone.sql was written in Task 1 and committed.</what-built>
  <how-to-verify>
    Run these commands in order and verify each succeeds:

    1. Check project is linked (confirm output shows a linked project, not an error):
       npx supabase status

    2. If supabase status shows the project is linked, push the migration:
       npx supabase db push

    3. Verify the column now exists on remote (optional but recommended):
       Check that db push output shows "Applying migration 20260615000000_business_accounts_contact_phone.sql" with success, OR that it says "No pending migrations" (meaning it was already applied).

    If supabase status fails with auth/link error: Run `npx supabase login` first, then `npx supabase link --project-ref <YOUR_PROJECT_REF>`, then re-run `npx supabase db push`.
  </how-to-verify>
  <resume-signal>Type "pushed" once db push exits 0. If it fails, describe the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → Remote DB | SQL executed on remote Supabase under the migration role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | business_accounts migration | mitigate | IF NOT EXISTS guard prevents duplicate column addition; migration is idempotent and reviewed before push |
| T-43-02 | Elevation of Privilege | contact_phone column | accept | Existing "Business updates own account" RLS policy (auth.uid() = user_id) automatically covers new column; no new policy needed; verified from migration source |
| T-43-SC | Tampering | npm/npx installs | accept | No new npm packages in this plan; supabase CLI already installed and used in prior phases (CLEAN-01 note in STATE.md) |
</threat_model>

<verification>
- supabase/migrations/20260615000000_business_accounts_contact_phone.sql exists and contains "ADD COLUMN IF NOT EXISTS contact_phone TEXT"
- npx supabase db push exited 0 (confirmed by human checkpoint)
- No existing migrations are broken (no error output from db push)
</verification>

<success_criteria>
The contact_phone TEXT column is live on the remote business_accounts table. The Wave 2 RSC plan can now SELECT contact_phone without a column-not-found runtime error.
</success_criteria>

<output>
Create .planning/phases/43-business-profile/43-01-SUMMARY.md when done
</output>
