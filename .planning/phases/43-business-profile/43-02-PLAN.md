---
phase: 43-business-profile
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/fi.json
  - messages/en.json
autonomous: true
requirements:
  - BIZPRO-01
  - BIZPRO-02
  - BIZPRO-03
  - BIZPRO-04

must_haves:
  truths:
    - "All 11 Business.profile* keys are present in both fi.json and en.json"
    - "Finnish values match D-13 exactly (Yritysprofiili, Yritys, Sähköposti, etc.)"
    - "English values match D-13 exactly (Business Profile, Company, Email, etc.)"
    - "No existing Business namespace keys are removed or renamed"
  artifacts:
    - path: "messages/fi.json"
      provides: "Finnish Business.profile* i18n keys"
      contains: "\"profileTitle\": \"Yritysprofiili\""
    - path: "messages/en.json"
      provides: "English Business.profile* i18n keys"
      contains: "\"profileTitle\": \"Business Profile\""
  key_links:
    - from: "messages/fi.json Business namespace"
      to: "BusinessProfiiliClient.tsx useTranslations('Business')"
      via: "next-intl t('profileTitle'), t('profilePhone'), etc."
      pattern: "profileTitle"
---

<objective>
Add 11 profile-specific i18n keys to the Business namespace in both messages/fi.json and messages/en.json.

Purpose: The BusinessProfiiliClient (Wave 2) uses useTranslations('Business') to render all user-visible strings. Without these keys the client component would throw a "Missing message" error from next-intl at runtime.
Output: Both locale files patched with the 11 profile* keys; no existing keys disturbed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current Business namespace tail in fi.json (lines 218-235 from grep output) -->
<!--
    "navDashboard": "AKTIIVI Business",
    "navMap": "Kartta",
    "navProfile": "Profiili",
    "navSignOut": "Kirjaudu ulos",
    "navOpenMenu": "Avaa valikko",
    "navCloseMenu": "Sulje valikko",
    "dashboardStatusPendingTitle": "Hakemuksesi odottaa hyväksyntää",
    "dashboardStatusPendingBody": "Tarkistamme tiedot ja ilmoitamme sinulle sähköpostitse.",
    "dashboardStatusApprovedTitle": "Tilisi on hyväksytty",
    "dashboardStatusApprovedBody": "Paikkasi näkyvät hakemistossa.",
    "dashboardStatusRejectedTitle": "Hakemuksesi on hylätty",
    "dashboardStatusRejectedBody": "Syy: {reason}",
    "dashboardStatusRejectedBodyNoReason": "Ota yhteyttä ylläpitoon saadaksesi lisätietoja.",
    "dashboardMapCta": "Avaa kartta",
    "dashboardVenuesHeading": "Paikkasi",
    "mapToggleAll": "Kaikki paikat",
    "mapToggleMine": "Omat paikat",
    "mapLoadingVenues": "Ladataan paikkoja..."
  },
-->
<!-- The new keys are inserted before the closing } of the Business namespace object -->

<!-- Keys that must NOT be used instead (per D-13, use dedicated profile keys): -->
<!-- navSignOut, saveCta, saving — do not reuse for profile page labels -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 11 profile keys to fi.json Business namespace</name>
  <files>messages/fi.json</files>
  <read_first>
    - messages/fi.json — read the full file to locate the closing } of the Business namespace; identify the last key before it (currently "mapLoadingVenues") so the new keys are appended in the right position without breaking JSON syntax
  </read_first>
  <action>
    In messages/fi.json, inside the "Business" namespace object, append these 11 key-value pairs after the last existing key ("mapLoadingVenues": "Ladataan paikkoja..."). Add a trailing comma after "mapLoadingVenues" line if not already present, then insert in order:
    "profileTitle": "Yritysprofiili"
    "profileCompanyName": "Yritys"
    "profileEmail": "Sähköposti"
    "profileAccountType": "Yritystili"
    "profilePhone": "Puhelinnumero"
    "profilePhonePlaceholder": "esim. +358 40 123 4567"
    "profileSave": "Tallenna"
    "profileSaved": "Tallennettu"
    "profileSaveError": "Tallennus epäonnistui. Yritä uudelleen."
    "profileLanguage": "Kieli"
    "profileSignOut": "Kirjaudu ulos"
    No trailing comma after profileSignOut (it is the last key in the Business object). Do not modify any other namespace. Do not remove or rename any existing Business key.
  </action>
  <verify>
    <automated>node -e "const f=require('./messages/fi.json'); const keys=['profileTitle','profileCompanyName','profileEmail','profileAccountType','profilePhone','profilePhonePlaceholder','profileSave','profileSaved','profileSaveError','profileLanguage','profileSignOut']; const missing=keys.filter(k=>!f.Business[k]); if(missing.length){process.exitCode=1;console.error('Missing:',missing)}else{console.log('All 11 keys present')}"</automated>
  </verify>
  <done>node command exits 0 and prints "All 11 keys present". fi.json is valid JSON (node can require it). All 11 Business.profile* keys are present with correct Finnish values.</done>
</task>

<task type="auto">
  <name>Task 2: Add 11 profile keys to en.json Business namespace</name>
  <files>messages/en.json</files>
  <read_first>
    - messages/en.json — read the full file to locate the closing } of the Business namespace; identify the last key before it so the new keys are appended in the right position without breaking JSON syntax
  </read_first>
  <action>
    In messages/en.json, inside the "Business" namespace object, append these 11 key-value pairs after the last existing Business key. Add a trailing comma after the last existing key if not already present, then insert in order:
    "profileTitle": "Business Profile"
    "profileCompanyName": "Company"
    "profileEmail": "Email"
    "profileAccountType": "Business Account"
    "profilePhone": "Phone number"
    "profilePhonePlaceholder": "e.g. +358 40 123 4567"
    "profileSave": "Save"
    "profileSaved": "Saved"
    "profileSaveError": "Save failed. Please try again."
    "profileLanguage": "Language"
    "profileSignOut": "Sign out"
    No trailing comma after profileSignOut (it is the last key in the Business object). Do not modify any other namespace. Do not remove or rename any existing Business key.
  </action>
  <verify>
    <automated>node -e "const f=require('./messages/en.json'); const keys=['profileTitle','profileCompanyName','profileEmail','profileAccountType','profilePhone','profilePhonePlaceholder','profileSave','profileSaved','profileSaveError','profileLanguage','profileSignOut']; const missing=keys.filter(k=>!f.Business[k]); if(missing.length){process.exitCode=1;console.error('Missing:',missing)}else{console.log('All 11 keys present')}"</automated>
  </verify>
  <done>node command exits 0 and prints "All 11 keys present". en.json is valid JSON. All 11 Business.profile* keys are present with correct English values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| messages/*.json → next-intl runtime | i18n strings loaded at server render time from locale files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43i-01 | Tampering | messages/fi.json, messages/en.json | accept | i18n files are static assets bundled at build time; no user input reaches them; JSON syntax verified by node require check |
| T-43i-SC | Tampering | npm/npx installs | accept | No new npm packages in this plan; no package manager invocations |
</threat_model>

<verification>
- grep -v "^#" messages/fi.json | grep -c "profileTitle" returns 1
- grep -v "^#" messages/en.json | grep -c "profileTitle" returns 1
- node -e "require('./messages/fi.json')" exits 0 (valid JSON)
- node -e "require('./messages/en.json')" exits 0 (valid JSON)
- All 11 Business.profile* keys present in both files (verified by Task 1 and Task 2 automated commands)
</verification>

<success_criteria>
Both fi.json and en.json contain all 11 Business.profile* keys with the exact Finnish and English values specified in D-13. The BusinessProfiiliClient.tsx (Wave 2) can call t('profileTitle'), t('profilePhone'), etc. without next-intl throwing a "Missing message" error.
</success_criteria>

<output>
Create .planning/phases/43-business-profile/43-02-SUMMARY.md when done
</output>
