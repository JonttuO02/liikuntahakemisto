---
phase: 32-yritysrekisterointi-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/fi.json
  - messages/en.json
  - app/business/page.tsx
autonomous: true
requirements:
  - BIZ-01
  - BIZ-03

must_haves:
  truths:
    - "Both fi.json and en.json contain a 'Business' namespace with all 14 required keys"
    - "GET /business renders a stub page with Finnish or English dashboard heading + coming-soon body copy"
    - "The stub page is a Server Component with no auth gate"
  artifacts:
    - path: "messages/fi.json"
      provides: "Finnish Business i18n namespace (14 keys)"
      contains: '"Business"'
    - path: "messages/en.json"
      provides: "English Business i18n namespace (14 keys)"
      contains: '"Business"'
    - path: "app/business/page.tsx"
      provides: "Stub dashboard page (Server Component)"
      contains: "dashboardTitle"
  key_links:
    - from: "app/business/page.tsx"
      to: "messages/fi.json"
      via: "getTranslations('Business')"
      pattern: "getTranslations.*Business"
---

<objective>
Add the "Business" i18n namespace to both message files and create the /business stub Server Component page.

Purpose: All downstream client components (registration page) use `useTranslations('Business')` — the namespace must exist in both locales before those components are built. The stub page satisfies D-07 and D-08 (no auth gate in Phase 32; Phase 36 replaces it).

Output: messages/fi.json and messages/en.json with 14-key Business namespace; app/business/page.tsx stub page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-yritysrekisterointi-auth/32-CONTEXT.md
@.planning/phases/32-yritysrekisterointi-auth/32-PATTERNS.md
@.planning/phases/32-yritysrekisterointi-auth/32-UI-SPEC.md
</context>

<interfaces>
<!-- Extracted from messages/fi.json and messages/en.json (verified) -->
<!-- Auth namespace structure (insertion model for Business namespace): -->
<!--
"Auth": {
  "signIn": "Kirjaudu sisään",
  "emailPlaceholder": "Sähköpostiosoite",
  ...
},
"Map": { ... }
-->
<!-- Business namespace insertion point: after "Auth" closing brace, before "Map" key -->
<!-- Both files: identical JSON key structure, different string values -->

<!-- Extracted from app/tietosuoja/page.tsx (stub page analog): -->
<!--
import { getTranslations } from 'next-intl/server'
export default async function TietosuojaPage() {
  // Server Component — async, no 'use client'
  const t = await getTranslations('Business')
  return (
    <div className="min-h-screen bg-white">...</div>
  )
}
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add "Business" namespace to fi.json and en.json</name>
  <files>messages/fi.json, messages/en.json</files>
  <read_first>
    messages/fi.json
    messages/en.json
  </read_first>
  <action>
    In both messages/fi.json and messages/en.json, insert the "Business" namespace block immediately after the closing brace of the "Auth" namespace (which ends at line 97) and before the "Map" key. Maintain 2-space JSON indentation. The trailing comma after the closing brace of the "Business" block is required because "Map" follows.

    Keys and values to insert:

    fi.json "Business" block:
    - "registerTitle": "Rekisteröi yrityksesi"
    - "companyNamePlaceholder": "Yrityksen nimi"
    - "emailPlaceholder": "Sähköpostiosoite"
    - "passwordPlaceholder": "Salasana (väh. 6 merkkiä)"
    - "registerCta": "Rekisteröidy"
    - "registering": "Rekisteröidään..."
    - "alreadyHaveAccount": "Onko sinulla jo tili?"
    - "signInLink": "Kirjaudu sisään"
    - "errorEmailInUse": "Sähköpostiosoite on jo käytössä."
    - "errorWeakPassword": "Salasanan on oltava vähintään 6 merkkiä."
    - "errorGeneric": "Jokin meni pieleen. Yritä uudelleen."
    - "errorAccountCreationFailed": "Tilin luonti epäonnistui. Yritä uudelleen."
    - "dashboardTitle": "Tervetuloa hallintapaneeliin"
    - "dashboardComingSoon": "Ominaisuudet lisätään pian."

    en.json "Business" block (same key names, English values):
    - "registerTitle": "Register your business"
    - "companyNamePlaceholder": "Company name"
    - "emailPlaceholder": "Email address"
    - "passwordPlaceholder": "Password (min. 6 characters)"
    - "registerCta": "Register"
    - "registering": "Registering..."
    - "alreadyHaveAccount": "Already have an account?"
    - "signInLink": "Sign in"
    - "errorEmailInUse": "Email address is already in use."
    - "errorWeakPassword": "Password must be at least 6 characters."
    - "errorGeneric": "Something went wrong. Please try again."
    - "errorAccountCreationFailed": "Account creation failed. Please try again."
    - "dashboardTitle": "Welcome to the dashboard"
    - "dashboardComingSoon": "Features coming soon."

    Both files must remain valid JSON after editing (run JSON.parse mentally to verify structure).
  </action>
  <verify>
    <automated>node -e "const fi = require('./messages/fi.json'); const en = require('./messages/en.json'); const keys = ['registerTitle','companyNamePlaceholder','emailPlaceholder','passwordPlaceholder','registerCta','registering','alreadyHaveAccount','signInLink','errorEmailInUse','errorWeakPassword','errorGeneric','errorAccountCreationFailed','dashboardTitle','dashboardComingSoon']; keys.forEach(k => { if (!fi.Business[k]) throw new Error('fi missing: ' + k); if (!en.Business[k]) throw new Error('en missing: ' + k); }); console.log('OK — 14 keys present in both locales')"</automated>
  </verify>
  <acceptance_criteria>
    - messages/fi.json contains "Business": { with all 14 keys listed above
    - messages/en.json contains "Business": { with all 14 keys listed above
    - Both files pass JSON.parse without error
    - fi.json Business.dashboardTitle === "Tervetuloa hallintapaneeliin"
    - en.json Business.dashboardTitle === "Welcome to the dashboard"
    - fi.json Business.registerCta === "Rekisteröidy"
    - en.json Business.registerCta === "Register"
    - The "Business" namespace appears between "Auth" and "Map" in both files
  </acceptance_criteria>
  <done>Both message files contain a valid 14-key Business namespace; node verification command prints "OK — 14 keys present in both locales"</done>
</task>

<task type="auto">
  <name>Task 2: Create /business stub Server Component page</name>
  <files>app/business/page.tsx</files>
  <read_first>
    app/tietosuoja/page.tsx
    app/business/page.tsx (may not exist yet — expected)
  </read_first>
  <action>
    Create app/business/page.tsx as an async Server Component (no 'use client' directive). Per D-07 and D-08: no auth gate, no NavBar, no complex layout — only centered stub text.

    Structure:
    - Import getTranslations from 'next-intl/server'
    - Export default async function BusinessPage()
    - Outer wrapper: div with className "min-h-screen bg-white flex flex-col items-center justify-center px-4"
    - Inner wrapper: div with className "flex flex-col items-center gap-4 text-center"
    - h1: className "text-xl font-bold text-[#111111]", content t('dashboardTitle')
    - p: className "text-sm text-[rgba(17,17,17,0.45)]", content t('dashboardComingSoon')

    Do NOT add NavBar, NavPill, auth checks, or any interactive elements. The page intentionally has no navigation — Phase 36 will replace it entirely with the real dashboard.

    The getTranslations call is: const t = await getTranslations('Business')
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const src = fs.readFileSync('app/business/page.tsx', 'utf8'); if (!src.includes(\"getTranslations\")) throw new Error('missing getTranslations'); if (!src.includes(\"dashboardTitle\")) throw new Error('missing dashboardTitle'); if (!src.includes(\"dashboardComingSoon\")) throw new Error('missing dashboardComingSoon'); if (!src.includes(\"min-h-screen bg-white\")) throw new Error('missing layout class'); if (src.includes(\"'use client'\")) throw new Error('must be Server Component — no use client'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - app/business/page.tsx exists
    - File does NOT contain 'use client'
    - File imports getTranslations from 'next-intl/server'
    - File calls await getTranslations('Business')
    - File renders t('dashboardTitle') inside an h1 element
    - File renders t('dashboardComingSoon') inside a p element
    - Outer div has class "min-h-screen bg-white"
    - No NavBar, no auth imports, no interactive state
  </acceptance_criteria>
  <done>app/business/page.tsx is a valid async Server Component; navigating to /business in the browser shows the Finnish stub heading and coming-soon copy (or English if locale is EN)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→messages/*.json | Static JSON consumed at build time — no runtime trust boundary |
| browser→/business | Server Component rendered on request — no user input accepted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 32 installs zero new packages — no package legitimacy gate required |
| T-32-01-A | Information Disclosure | /business stub page | accept | Page is intentionally public per D-08; stub content is not sensitive; auth gate is deferred to Phase 36 by user decision |
| T-32-01-B | Tampering | messages/fi.json, messages/en.json | accept | Static build-time files; no user input; JSON is schema-validated at build time via next-intl |
</threat_model>

<verification>
After Wave 1 Plan 01 completes:
1. `node -e "const fi = require('./messages/fi.json'); console.log(Object.keys(fi.Business).length)"` must print 14
2. `node -e "const en = require('./messages/en.json'); console.log(Object.keys(en.Business).length)"` must print 14
3. app/business/page.tsx exists on disk
4. app/business/page.tsx does not contain 'use client'
</verification>

<success_criteria>
- messages/fi.json and messages/en.json each contain exactly 14 Business namespace keys
- app/business/page.tsx is an async Server Component that renders dashboardTitle + dashboardComingSoon
- No new packages installed
- Both JSON files remain valid (no parse errors)
</success_criteria>

<output>
Create .planning/phases/32-yritysrekisterointi-auth/32-01-SUMMARY.md when done
</output>
