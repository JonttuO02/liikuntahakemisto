---
phase: 44-brandidata-tietokantaperusta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260615000001_business_branding.sql
autonomous: false
requirements:
  - BRDDB-01
  - BRDDB-02

must_haves:
  truths:
    - "business_branding table exists on the remote Supabase instance with all required columns"
    - "FK business_account_id references business_accounts(user_id) — NOT businesses or business_accounts(id)"
    - "UNIQUE constraint on business_account_id (one row per company)"
    - "status CHECK constraint allows exactly: pending, analyzing, analyzed, failed — no 'approved'"
    - "logo_type CHECK constraint allows exactly: icon, icon_with_text, text_only"
    - "RLS is enabled and three policies exist: SELECT (USING), INSERT (WITH CHECK), UPDATE (USING + WITH CHECK)"
    - "All three RLS policies use auth.uid() = business_account_id as the condition"
    - "Index idx_business_branding_business_account_id exists on business_account_id"
    - "supabase db push exits 0 with no errors"
  artifacts:
    - path: "supabase/migrations/20260615000001_business_branding.sql"
      provides: "business_branding table DDL + RLS + index"
      contains: "create table business_branding"
  key_links:
    - from: "supabase/migrations/20260615000001_business_branding.sql"
      to: "business_branding table (remote Supabase)"
      via: "supabase db push"
      pattern: "business_account_id references business_accounts(user_id)"
---

<objective>
Write the Supabase migration that creates the business_branding table with all columns, constraints, index, and RLS policies, then push it to the remote Supabase instance.

Purpose: Phase 45 (scraper + Claude API) needs this table to store analysis results via UPSERT on business_account_id. Phase 46 (pre-vaihe UI) reads from this table to render the preview. Without this migration applied to remote, both downstream phases fail at runtime despite clean TypeScript compilation.
Output: Migration file committed + business_branding table live on remote Supabase with RLS enforced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- business_accounts PK is user_id (UUID), not id — FK must reference business_accounts(user_id) -->
<!-- Source: supabase/migrations/20260605000000_business_accounts.sql line 23 -->
<!--   user_id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE -->

<!-- RLS triple-pattern (SELECT/INSERT/UPDATE) from business_accounts migration: -->
<!--   SELECT: USING (auth.uid() = user_id) -->
<!--   INSERT: WITH CHECK (auth.uid() = user_id) -->
<!--   UPDATE: USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id) -->
<!-- In business_branding the column is business_account_id, not user_id -->

<!-- Migration filename sequence: -->
<!-- Latest existing: 20260615000000_business_accounts_contact_phone.sql -->
<!-- This plan:       20260615000001_business_branding.sql -->

<!-- Columns from CONTEXT.md D-01 to D-09 (all must be present): -->
<!--   id, business_account_id, website_url, logo_url, logo_type, colors (jsonb), -->
<!--   raw_analysis (jsonb), status, error_message, analyzed_at, created_at, updated_at -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write business_branding migration file</name>
  <files>supabase/migrations/20260615000001_business_branding.sql</files>
  <read_first>
    - supabase/migrations/20260605000000_business_accounts.sql — verify business_accounts PK is user_id (UUID), not id; copy the SELECT/INSERT/UPDATE RLS policy pattern verbatim, replacing user_id with business_account_id
    - supabase/migrations/20260615000000_business_accounts_contact_phone.sql — confirm 20260615000000 is the current last file; new migration must use 20260615000001
    - .planning/phases/44-brandidata-tietokantaperusta/44-CONTEXT.md — read all decisions D-01 through D-14; the exact SQL schema in &lt;specifics&gt; is the authoritative source
  </read_first>
  <action>
    Create supabase/migrations/20260615000001_business_branding.sql with:

    Header comment: "Phase 44: Create business_branding table with RLS for v2.1 AI brand analysis"

    TABLE: business_branding with columns in this order:
      id UUID PRIMARY KEY DEFAULT gen_random_uuid()
      business_account_id UUID NOT NULL REFERENCES business_accounts(user_id) ON DELETE CASCADE
      website_url TEXT NOT NULL
      logo_url TEXT
      logo_type TEXT CHECK (logo_type IN ('icon', 'icon_with_text', 'text_only'))
      colors JSONB
      raw_analysis JSONB
      status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'analyzing', 'analyzed', 'failed'))
      error_message TEXT
      analyzed_at TIMESTAMPTZ
      created_at TIMESTAMPTZ NOT NULL DEFAULT now()
      updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
      CONSTRAINT business_branding_unique_account UNIQUE (business_account_id)

    INDEX: CREATE INDEX idx_business_branding_business_account_id ON business_branding(business_account_id)

    RLS: ALTER TABLE business_branding ENABLE ROW LEVEL SECURITY

    POLICY "Business reads own branding" FOR SELECT USING (auth.uid() = business_account_id)
    POLICY "Business inserts own branding" FOR INSERT WITH CHECK (auth.uid() = business_account_id)
    POLICY "Business updates own branding" FOR UPDATE USING (auth.uid() = business_account_id) WITH CHECK (auth.uid() = business_account_id)

    Critical constraints:
    - FK must be REFERENCES business_accounts(user_id) — not businesses, not business_accounts(id)
    - status CHECK must include exactly: pending, analyzing, analyzed, failed — no 'approved'
    - No DELETE policy (admin operations use service role key per STATE.md)
    - website_url is NOT NULL (every analysis starts with a URL)
  </action>
  <verify>
    <automated>grep -c "references business_accounts(user_id)" supabase/migrations/20260615000001_business_branding.sql</automated>
    <automated>grep -c "business_branding_unique_account" supabase/migrations/20260615000001_business_branding.sql</automated>
    <automated>grep -c "auth.uid() = business_account_id" supabase/migrations/20260615000001_business_branding.sql</automated>
  </verify>
  <done>
    File exists at supabase/migrations/20260615000001_business_branding.sql.
    grep for "references business_accounts(user_id)" returns 1.
    grep for "business_branding_unique_account" returns 1.
    grep for "auth.uid() = business_account_id" returns 3 (one per policy).
    All 12 columns present: id, business_account_id, website_url, logo_url, logo_type, colors, raw_analysis, status, error_message, analyzed_at, created_at, updated_at.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Push migration to remote Supabase</name>
  <what-built>Migration file supabase/migrations/20260615000001_business_branding.sql was written in Task 1.</what-built>
  <how-to-verify>
    Run these commands in order:

    1. Verify the migration file looks correct:
       cat supabase/migrations/20260615000001_business_branding.sql

    2. Check Supabase project link:
       npx supabase status

    3. Push the migration:
       npx supabase db push

    Expected output from db push: "Applying migration 20260615000001_business_branding.sql" with success,
    OR "No pending migrations" if the file was already applied.

    If supabase status fails with auth/link error:
      npx supabase login
      npx supabase link --project-ref &lt;YOUR_PROJECT_REF&gt;
      npx supabase db push
  </how-to-verify>
  <resume-signal>Type "pushed" once db push exits 0. If it fails, describe the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration file → Remote Supabase DB | SQL executed on remote Supabase under the migration role via supabase CLI |
| RLS policies → Row access | auth.uid() compared to business_account_id at query time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Elevation of Privilege | RLS SELECT policy | mitigate | USING (auth.uid() = business_account_id) prevents cross-company branding data reads; verified from policy DDL |
| T-44-02 | Elevation of Privilege | RLS INSERT/UPDATE policies | mitigate | WITH CHECK (auth.uid() = business_account_id) prevents a business from writing to another company's row; same pattern as business_accounts (Phase 31) |
| T-44-03 | Tampering | FK reference | mitigate | FK references business_accounts(user_id) (the actual PK); referencing a non-PK column would be rejected by Postgres; verified by reading business_accounts migration before writing |
| T-44-04 | Tampering | status CHECK constraint | accept | CHECK enforces exactly {pending,analyzing,analyzed,failed}; 'approved' status is deferred per CONTEXT.md; Phase 45 cannot set an invalid status without a Postgres constraint violation |
| T-44-SC | Tampering | npm/npx installs | accept | No new npm packages; supabase CLI already installed and used in prior phases |
</threat_model>

<verification>
- supabase/migrations/20260615000001_business_branding.sql exists with correct FK (business_accounts(user_id)), UNIQUE constraint, index, RLS enabled, and 3 policies
- npx supabase db push exited 0 (confirmed by human checkpoint)
- No existing migrations broken (no error output from db push)
- business_branding table visible on remote with all 12 columns, correct CHECK constraints, and idx_business_branding_business_account_id index
</verification>

<success_criteria>
business_branding table is live on the remote Supabase instance. Phase 45 can UPSERT analysis results keyed on business_account_id. Phase 46 can SELECT branding data for onboarding preview. RLS ensures a business user can only access their own row.
</success_criteria>

<output>
Create .planning/phases/44-brandidata-tietokantaperusta/44-01-SUMMARY.md when done
</output>
