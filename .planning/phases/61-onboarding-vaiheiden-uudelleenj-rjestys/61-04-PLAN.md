---
phase: 61-onboarding-vaiheiden-uudelleenj-rjestys
plan: 04
type: execute
wave: 3
depends_on: ["61-02", "61-03"]
files_modified:
  - app/business/onboarding/page.tsx
  - app/business/onboarding/StepPaikka.tsx
autonomous: true
requirements: [ONBOARD-18, ONBOARD-19, ONBOARD-20, ONBOARD-21]
must_haves:
  truths:
    - "Onboarding starts at the nimi-url step, then sijainti, then (only if a website was given) the AI-results analyze step, else laji-skip"
    - "Entering a website on step 1 fires the background AI analysis and persists the URL to the draft so varauslinkki survives submit"
    - "The old StepPaikka file is deleted and no longer referenced"
  artifacts:
    - "app/business/onboarding/page.tsx (new PagePhase state machine + handlers)"
    - "app/business/onboarding/StepPaikka.tsx DELETED"
  key_links:
    - "page.tsx handleNimiUrlNext -> POST /api/business/analyze-website (background) + POST /api/business/onboarding/save-step (website persistence)"
    - "page.tsx renders StepNimiJaURL + StepSijainti (Plan 02)"
    - "page.tsx handleBackToPrePhase -> WizardInner onBackToAnalyze prop (Plan 03)"
---

<objective>
Rewire the onboarding page-level state machine to the new order: `nimi-url` → `sijainti` → (`analyze` if a website was provided, else `laji-skip`) → `wizard`. Fire the background AI analysis and persist the website URL to the draft when the user supplies one, generalize the wizard's back-to-pre-phase target, fast-forward resuming users past completed pre-phases, and delete the obsolete `StepPaikka`.

Purpose: This is the integration point that connects the new components (Plan 02) and the finalized wizard (Plan 03) into a working flow. It runs last (wave 3) because it imports those artifacts and depends on the WizardInner prop contract being settled.
Output: A rewritten onboarding page state machine and one deletion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-RESEARCH.md
@.planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-PATTERNS.md
@.planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-UI-SPEC.md
@app/business/onboarding/StepNimiJaURL.tsx
@app/business/onboarding/StepSijainti.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite the onboarding page state machine</name>
  <files>app/business/onboarding/page.tsx</files>
  <read_first>
    - app/business/onboarding/page.tsx (current PagePhase type, StepPaikkaPrePhase, the paikkaId/paikkaInfo resolution useEffect, PrePhase/analyze/laji-skip/wizard render blocks, handleBackToAnalyze, handleSkip, the existing AnimatePresence wrapper)
    - app/business/onboarding/StepNimiJaURL.tsx (props it now imports — onNext signature)
    - app/business/onboarding/StepSijainti.tsx (props it now imports)
    - app/business/WizardInner.tsx (the onBackToAnalyze prop it receives)
    - app/api/business/analyze-website/route.ts (confirm POST body { url, paikka_id } — this is the correct route, NOT 'ai-analyze')
    - app/api/business/onboarding/save-step/route.ts (confirm field 'yhteystiedot' and step range)
    - .planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-RESEARCH.md (Pitfall 1, Pitfall 2, Pitfall 8, Pitfall 10, Code Examples Patterns 1 and 2)
    - .planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-PATTERNS.md (page.tsx section)
  </read_first>
  <action>
    Change the `PagePhase` union from `'paikka' | 'analyze' | 'laji-skip' | 'wizard'` to `'nimi-url' | 'sijainti' | 'analyze' | 'laji-skip' | 'wizard'`, with initial phase `'nimi-url'`. Replace the `StepPaikkaPrePhase` Suspense wrapper with `StepNimiJaURLPrePhase` (same paikkaId/paikkaInfo resolution useEffect copied verbatim, same `business_paikka_links` fallback), changing its `onNext` signature to `(websiteUrl: string | null) => void` and rendering `<StepNimiJaURL>` instead of `<StepPaikka>`. Add a fast-forward (Pitfall 10): after the resolution useEffect loads `paikka`, if `paikka.latitude !== null` the sijainti step is already done — call `onNext(null)` to skip ahead rather than re-running pre-phases. Add state `websiteUrl: string | null` (init null) and `aiTriggered: boolean` (init false). Add `handleNimiUrlNext(url: string | null)`: set `websiteUrl`, set phase to `'sijainti'`, and when `url` is non-null and `paikkaId !== null`, retrieve the session token and fire TWO non-awaited fetches — (a) `POST /api/business/analyze-website` with body `{ url, paikka_id: paikkaId }` (CORRECT route name — never 'ai-analyze'), and (b) `POST /api/business/onboarding/save-step` with body `{ paikka_id: paikkaId, step: 0, field: 'yhteystiedot', value: { website: url } }` so the submit route can later read `draft.yhteystiedot.website` into `varauslinkki` (Pitfall 2) — then set `aiTriggered` true. Render `'nimi-url'` (Suspense → StepNimiJaURLPrePhase wired to handleNimiUrlNext) and `'sijainti'` (StepSijainti with `paikkaId`, `onNext={() => websiteUrl ? setPagePhase('analyze') : handleSkip()}`, `onPrev={() => setPagePhase('nimi-url')}`) blocks. Generalize `handleBackToAnalyze` to `handleBackToPrePhase` that sets phase to `'analyze'` when `websiteUrl` is non-null else `'laji-skip'` (Pitfall 8), and pass it to WizardInner's `onBackToAnalyze` prop. Keep the existing analyze/laji-skip/wizard render blocks and the AnimatePresence crossfade wrapper. Remove the `StepPaikka` import.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `PagePhase` includes `'nimi-url'` and `'sijainti'` and no longer includes `'paikka'`; initial phase is `'nimi-url'`.
    - `handleNimiUrlNext` fires a background POST to `/api/business/analyze-website` (exact path, not `ai-analyze`) and a POST to `/api/business/onboarding/save-step` with `field: 'yhteystiedot'` and `value.website` set, only when the URL is non-null.
    - The sijainti `onNext` routes to `'analyze'` when `websiteUrl` is truthy and to the skip/laji-skip path otherwise.
    - `handleBackToPrePhase` targets `'analyze'` vs `'laji-skip'` based on `websiteUrl`, and is wired into WizardInner.
    - The resolution useEffect fast-forwards (`onNext(null)`) when `paikka.latitude !== null`.
    - `StepPaikka` is no longer imported.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The onboarding page drives the new nimi-url → sijainti → analyze/laji-skip → wizard order, triggers AI + persists the URL, and resumes correctly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete the obsolete StepPaikka component</name>
  <files>app/business/onboarding/StepPaikka.tsx</files>
  <read_first>
    - app/business/onboarding/page.tsx (confirm the StepPaikka import was removed in Task 1)
    - .planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-RESEARCH.md (ONBOARD-18; File-by-File Change Summary)
  </read_first>
  <action>
    Delete the file `app/business/onboarding/StepPaikka.tsx`. This must happen only after Task 1 removed its import from page.tsx. Confirm no other file imports it (a repo-wide search should find no remaining references); if any other importer exists, STOP and report it rather than leaving a broken build.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/business/onboarding/StepPaikka.tsx` no longer exists (`test ! -f app/business/onboarding/StepPaikka.tsx`).
    - No source file imports StepPaikka (tsc passes with the file deleted, proving no dangling reference).
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>StepPaikka is removed from the codebase with no dangling references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page.tsx (browser) → /api/business/analyze-website | Authenticated user triggers AI scrape of a self-supplied URL (fire-and-forget) |
| page.tsx (browser) → /api/business/onboarding/save-step | Authenticated user persists the website URL into their draft |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-09 | Tampering / SSRF | website URL passed to analyze-website | medium | transfer | URL fetching/validation is the existing analyze-website route's responsibility (unchanged this phase); page.tsx only forwards a user-typed URL with the user's own JWT. Flag to a future security pass if that route lacks SSRF guards. |
| T-61-10 | Information Disclosure | save-step writes website to JSONB draft | low | mitigate | save-step enforces ALLOWED_FIELDS and JWT/ownership server-side (unchanged); submit route validates http/https before writing varauslinkki |
| T-61-11 | Denial of Service | background AI trigger fired per step-1 advance | low | accept | One fire-and-forget call per onboarding session; rate/cost already bounded by the existing analyze-website route's one-call design |

No package installs in this plan.
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `test ! -f app/business/onboarding/StepPaikka.tsx` succeeds.
- Manual UAT: a fresh onboarding shows name+URL first, then map; entering a URL makes AnalysoiSivusto appear after the map; leaving URL blank routes to laji-skip; submitted venue with a URL has a non-null varauslinkki.
</verification>

<success_criteria>
The onboarding flow runs in the new order, the AI trigger uses the correct route, the website URL persists to the draft, resume fast-forwards past completed steps, and StepPaikka is deleted.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 04)

- Modified `app/business/onboarding/page.tsx` — `PagePhase` now `'nimi-url' | 'sijainti' | 'analyze' | 'laji-skip' | 'wizard'`; new `StepNimiJaURLPrePhase` wrapper; new state `websiteUrl`, `aiTriggered`; new handlers `handleNimiUrlNext`, `handleBackToPrePhase`; renders StepNimiJaURL + StepSijainti.
- DELETED `app/business/onboarding/StepPaikka.tsx`.
</artifacts>

<output>
Create `.planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-04-SUMMARY.md` when done.
</output>
