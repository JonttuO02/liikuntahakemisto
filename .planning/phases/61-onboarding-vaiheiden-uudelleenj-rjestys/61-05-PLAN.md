---
phase: 61-onboarding-vaiheiden-uudelleenj-rjestys
plan: 05
type: execute
wave: 4
depends_on: ["61-04"]
files_modified:
  - app/business/onboarding/page.tsx
autonomous: true
requirements: [ONBOARD-19, ONBOARD-20, ONBOARD-21]
gap_closure: true
must_haves:
  truths:
    - "After completing sijainti, the user goes directly to wizard — the analyze phase is never a user-visible navigation step"
    - "The fast-forward path (alreadyHasLocation=true) also routes to wizard (not analyze) when a websiteUrl is present"
    - "Back navigation from wizard step 1 returns to sijainti (not analyze) when websiteUrl is set"
  artifacts:
    - "app/business/onboarding/page.tsx — three routing references to 'analyze' corrected"
  key_links:
    - "sijainti onNext -> setPagePhase('wizard') when websiteUrl is truthy"
    - "handleNimiUrlNext fast-forward -> setPagePhase('wizard') when url is truthy"
    - "handleBackToPrePhase -> setPagePhase('sijainti') when websiteUrl is truthy"
---

<objective>
Fix three routing expressions in the onboarding page that incorrectly send the user to the 'analyze' phase phase after sijainti, when the AI is already running in the background and the user should go directly to wizard.

Purpose: The analyze phase (PrePhase) was kept as a user-visible routing target in the sijainti onNext and the alreadyHasLocation fast-forward path, even though Phase 61's intent is that AI analysis fires silently in the background and results appear pre-populated in the wizard — no separate AI-review step. The UAT confirmed this as a major regression.
Output: page.tsx with corrected routing — sijainti onNext, fast-forward, and back-navigation all point to the right phases.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-UAT.md
@app/business/onboarding/page.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix analyze-phase routing in the onboarding state machine</name>
  <files>app/business/onboarding/page.tsx</files>
  <action>
    Make three targeted changes to `app/business/onboarding/page.tsx`. Read the file first to confirm exact line context before editing.

    Change 1 — fast-forward path in `handleNimiUrlNext` (the `if (alreadyHasLocation)` branch, currently around line 313):
    `setPagePhase(url ? 'analyze' : 'laji-skip')` → `setPagePhase(url ? 'wizard' : 'laji-skip')`
    Reason: when the user already has a saved location the sijainti step is skipped entirely; the AI call is fired earlier in the same function, so routing to wizard directly is correct.

    Change 2 — sijainti block's `onNext` callback (currently around line 371):
    `onNext={() => (websiteUrl ? setPagePhase('analyze') : handleSkip())}` → `onNext={() => (websiteUrl ? setPagePhase('wizard') : handleSkip())}`
    Reason: AI analysis was already launched by `handleNimiUrlNext`; the flow should go directly into the wizard where AI results will be pre-populated, bypassing the now-unnecessary analyze confirmation step.

    Change 3 — `handleBackToPrePhase` function (currently around lines 351–353):
    `setPagePhase(websiteUrl ? 'analyze' : 'laji-skip')` → `setPagePhase(websiteUrl ? 'sijainti' : 'laji-skip')`
    Reason: 'analyze' is no longer a user-visible phase in the forward flow; back navigation from wizard step 1 must return to sijainti (the immediately preceding step) when a websiteUrl is present, or to laji-skip when no URL was provided.

    Do NOT touch any other logic — the `aiTriggered` guard, the `save-step` persistence, the `skipFastForward` flag, or the analyze/laji-skip/wizard render blocks are all correct and must remain unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - The `if (alreadyHasLocation)` branch in `handleNimiUrlNext` routes to 'wizard' (not 'analyze') when url is truthy.
    - The sijainti block's `onNext` routes to 'wizard' (not 'analyze') when websiteUrl is truthy.
    - `handleBackToPrePhase` routes to 'sijainti' (not 'analyze') when websiteUrl is truthy.
    - `npx tsc --noEmit` passes with no new errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page.tsx client state | Routing decisions are purely client-side state transitions; no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-12 | Tampering | setPagePhase calls | low | accept | Phase changes are client-side UI state only; no server data is written by routing; existing JWT/RLS guards on all API calls are unchanged |

No package installs in this plan.
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Manual flow with a website URL: nimi-url → sijainti → wizard (analyze phase never shown).
- Manual flow without URL: nimi-url → sijainti → laji-skip → wizard (unchanged).
- Back button from wizard step 1 with URL returns to sijainti; without URL returns to laji-skip.
- Fast-forward resume (user already has saved location + URL): goes directly to wizard without stopping at sijainti or analyze.
</verification>

<success_criteria>
The analyze phase is never a user-visible navigation step in the onboarding flow. AI analysis fires silently in the background and wizard is entered directly from sijainti.
</success_criteria>

<output>
Create `.planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-05-SUMMARY.md` when done.
</output>
