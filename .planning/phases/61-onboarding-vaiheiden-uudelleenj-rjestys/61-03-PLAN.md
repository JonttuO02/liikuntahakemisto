---
phase: 61-onboarding-vaiheiden-uudelleenj-rjestys
plan: 03
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - app/business/onboarding/StepYhteystiedot.tsx
  - app/business/WizardInner.tsx
  - app/business/onboarding/ProgressBar.tsx
  - app/business/onboarding/StepEsikatselu.tsx
autonomous: true
requirements: [ONBOARD-22, ONBOARD-23, ONBOARD-24]
must_haves:
  truths:
    - "The onboarding wizard has 4 steps; there is no preview step and no step 5"
    - "StepYhteystiedot hides the website field in onboarding mode and submits inline as the final step"
    - "The progress bar's fifth milestone reads SUBMIT (Lähetys), not ESIKATSELU"
  artifacts:
    - "app/business/WizardInner.tsx (StepEsikatselu removed, clamped to 4, submit callback)"
    - "app/business/onboarding/StepYhteystiedot.tsx (website field gated by editMode, submit CTA)"
    - "app/business/onboarding/ProgressBar.tsx (stepSubmit label)"
    - "app/business/onboarding/StepEsikatselu.tsx DELETED"
  key_links:
    - "WizardInner handleYhteystiedotSubmit -> POST /api/business/onboarding/submit -> router.push('/business')"
    - "ProgressBar -> t('stepSubmit') key (Plan 01)"
---

<objective>
Finalize the wizard: delete the separate preview step (`StepEsikatselu`), collapse the wizard from 5 steps to 4, move submission inline into `StepYhteystiedot` (which also hides its website field during onboarding since the URL is now collected in step 1), and relabel the progress bar's final milestone from ESIKATSELU to SUBMIT.

Purpose: This is the self-contained "tail" of the onboarding flow. It touches WizardInner/StepYhteystiedot/ProgressBar/StepEsikatselu — a file set disjoint from Plan 02 — so it runs in parallel in wave 2.
Output: Three modified files and one deletion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-RESEARCH.md
@.planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-PATTERNS.md
@.planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Gate website field and add inline submit to StepYhteystiedot</name>
  <files>app/business/onboarding/StepYhteystiedot.tsx</files>
  <read_first>
    - app/business/onboarding/StepYhteystiedot.tsx (current website input, editMode branching, handleNext, debounced SET_YHTEYSTIEDOT dispatch, footer CTA)
    - .planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-RESEARCH.md (Pitfall 5, Pitfall 6, Open Questions 2 and 3)
    - .planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-PATTERNS.md (StepYhteystiedot section)
    - .planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-UI-SPEC.md (Component Contract 4)
  </read_first>
  <action>
    Wrap the website `<input type="url">` block in `{editMode && ( ... )}` so it renders only in edit mode. Remove the `initialBrandingWebsite` prop from the component's props (it is no longer supplied in onboarding). When `editMode === false`, initialize the `website` state to `''` and exclude it from the debounced `SET_YHTEYSTIEDOT` dispatch (or dispatch it as `''`) — `LivePreviewContext.website` is optional, so omitting it is safe. Change the footer CTA label so the loading state still shows the inline spinner, and the resting label is `editMode ? t('nextCta') : t('submitCta')`. Keep `handleNext()` calling the save-step write of `{ puhelin, email, kuvaus }` (no website) and then calling `onNext()` on success — do NOT add `useRouter` here; the actual submit + redirect lives in the `onNext` callback supplied by WizardInner (Task 2). Edit-mode behavior must remain unchanged (website field visible, CTA `t('nextCta')`, save+advance). Do not put booking/submit fetch logic inside this component.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The website `<input type="url">` is rendered only when `editMode` is truthy.
    - The `initialBrandingWebsite` prop is removed from the props interface and from all internal usage.
    - In onboarding mode the footer CTA resting label resolves to `t('submitCta')`; in edit mode it resolves to `t('nextCta')`.
    - `handleNext` does not import or use `useRouter`; it calls `onNext()` after a successful save-step.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>StepYhteystiedot hides the website field in onboarding, labels its CTA as submit, and delegates the actual submit to its onNext callback.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Collapse WizardInner to 4 steps and submit inline; delete StepEsikatselu</name>
  <files>app/business/WizardInner.tsx, app/business/onboarding/StepEsikatselu.tsx</files>
  <read_first>
    - app/business/WizardInner.tsx (StepEsikatselu import, OnboardingMode step clamps at the four numeric spots, the step-5 re-fetch effect, the StepYhteystiedot render call, the onBackToAnalyze prop, saveAndAdvance, router usage)
    - app/business/onboarding/StepEsikatselu.tsx (handleSubmit logic to transplant: POST onboarding/submit then router.push('/business'))
    - .planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-RESEARCH.md (Pitfall 4, Pitfall 6, Code Example Pattern 4)
    - .planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-PATTERNS.md (WizardInner section — all 10 numbered changes)
  </read_first>
  <action>
    In WizardInner OnboardingMode: (1) remove the `StepEsikatselu` import; (2) change the URL step-range guard from `rawStep > 5` to `rawStep > 4`; (3) change `Math.min(Math.max(n, 1), 5)` in goToStep to `... , 4)`; (4) change the completedSteps `Math.min(draft.current_step, 5)` to `... , 4)`; (5) change the resume `Math.min(savedStep, 5)` to `... , 4)`; (6) delete the entire `useEffect` guarded by the `step === 5` early-return (the preview re-fetch effect); (7) add an async `handleYhteystiedotSubmit` that retrieves the session, returns early if no session or `paikkaId === null`, POSTs to `/api/business/onboarding/submit` with body `{ paikka_id: paikkaId }` and `Authorization: Bearer <token>`, and on ok calls `router.push('/business')` (transplant the proven logic from StepEsikatselu's handleSubmit); (8) change the StepYhteystiedot render to pass `onNext={handleYhteystiedotSubmit}` instead of `() => saveAndAdvance(4)`, and remove the `initialBrandingWebsite` prop from that call; (9) remove the `step === 5` render branch that mounted StepEsikatselu. Keep the `onBackToAnalyze` prop name as-is (page.tsx re-points its implementation in Plan 04). After WizardInner no longer imports it, DELETE the file `app/business/onboarding/StepEsikatselu.tsx`. Edit mode (EditMode) is unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/business/onboarding/StepEsikatselu.tsx` no longer exists (`test ! -f app/business/onboarding/StepEsikatselu.tsx`).
    - No import of StepEsikatselu remains (tsc passes with the file deleted, proving no dangling import).
    - All four numeric clamps that previously used `5` for the onboarding step ceiling now use `4`; the `step === 5` re-fetch effect is gone.
    - `handleYhteystiedotSubmit` POSTs to `/api/business/onboarding/submit` and calls `router.push('/business')` on success; it is passed as StepYhteystiedot's `onNext`.
    - The StepYhteystiedot render no longer passes `initialBrandingWebsite`.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The onboarding wizard ends at step 4 (Yhteystiedot), submits inline, and the preview step and file are gone.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Relabel ProgressBar final milestone to SUBMIT</name>
  <files>app/business/onboarding/ProgressBar.tsx</files>
  <read_first>
    - app/business/onboarding/ProgressBar.tsx (the stepLabels array)
    - .planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-PATTERNS.md (ProgressBar section)
  </read_first>
  <action>
    In the `stepLabels` array, replace the fifth entry `t('stepPreview')` with `t('stepSubmit')`. The first four entries (`stepMedia`, `stepPricing`, `stepHours`, `stepContact`) are unchanged. Make no other changes to the component.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The `stepLabels` array's fifth entry is `t('stepSubmit')`.
    - No occurrence of `t('stepPreview')` remains in ProgressBar.tsx.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The progress bar's final milestone renders the SUBMIT label.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WizardInner (browser) → /api/business/onboarding/submit | Authenticated user finalizes their draft venue for review |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-07 | Elevation of Privilege | onboarding/submit called inline from StepYhteystiedot | low | mitigate | Reuses the unchanged existing submit route, which already verifies JWT and ownership server-side; no new authorization surface is introduced |
| T-61-08 | Tampering | step-range clamp regression (resume to invalid step) | low | mitigate | All four clamps changed consistently to 4 + step-5 effect removed (Task 2), preventing stale-state navigation |

Pure client/UI changes plus reuse of an existing route. No package installs; no new server boundary.
</threat_model>

<verification>
- `npx tsc --noEmit` passes after all tasks.
- `test ! -f app/business/onboarding/StepEsikatselu.tsx` succeeds.
- Manual UAT: onboarding shows 4 wizard steps; step 4 CTA is "Lähetä hyväksyttäväksi"; progress bar shows "Lähetys"; submit redirects to /business.
</verification>

<success_criteria>
The wizard has 4 steps, submits inline from Yhteystiedot, the website field is hidden in onboarding mode, the preview step is deleted, and the progress bar reads SUBMIT.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 03)

- Modified `app/business/WizardInner.tsx` — `handleYhteystiedotSubmit` callback added; OnboardingMode clamped to 4 steps; step-5 effect and StepEsikatselu import/render removed.
- Modified `app/business/onboarding/StepYhteystiedot.tsx` — website input gated behind `editMode`; `initialBrandingWebsite` prop removed; CTA label `editMode ? nextCta : submitCta`.
- Modified `app/business/onboarding/ProgressBar.tsx` — fifth `stepLabels` entry now `t('stepSubmit')`.
- DELETED `app/business/onboarding/StepEsikatselu.tsx`.
</artifacts>

<output>
Create `.planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-03-SUMMARY.md` when done.
</output>
