---
phase: 61-onboarding-vaiheiden-uudelleenj-rjestys
plan: 06
type: execute
wave: 4
depends_on: ["61-02"]
files_modified:
  - app/components/SijaintiPicker.tsx
autonomous: true
requirements: [ONBOARD-20]
gap_closure: true
must_haves:
  truths:
    - "SijaintiPicker map shows no default Google Maps UI controls (no satellite toggle, no fullscreen button)"
    - "The location pin in SijaintiPicker is the app's own custom black teardrop SVG, not the default Google red pin"
  artifacts:
    - "app/components/SijaintiPicker.tsx — Map has disableDefaultUI; AdvancedMarker wraps a 0x0 anchor div with a custom inline SVG pin"
  key_links:
    - "Map disableDefaultUI prop — same pattern as Etusivu.tsx, app/business/map/page.tsx, app/admin/[id]/page.tsx"
    - "AdvancedMarker 0x0 anchor div pattern — same as app/business/map/page.tsx line 142"
---

<objective>
Fix two visual inconsistencies in SijaintiPicker that were found during UAT: the Map component was missing disableDefaultUI (causing satellite/fullscreen controls to appear), and the AdvancedMarker was self-closing with no children (rendering the default Google red pin instead of the app's custom marker).

Purpose: Every other map in this codebase uses disableDefaultUI and a custom AdvancedMarker child in the 0×0 anchor div pattern. SijaintiPicker was created in Phase 61 Plan 02 but shipped without these two props. UAT test 3 flagged the visual mismatch as a minor defect.
Output: SijaintiPicker with visual parity to the rest of the app's map components.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-UAT.md
@app/components/SijaintiPicker.tsx
@app/business/map/page.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add disableDefaultUI and custom location pin to SijaintiPicker</name>
  <files>app/components/SijaintiPicker.tsx</files>
  <action>
    Read `app/components/SijaintiPicker.tsx` in full, then `app/business/map/page.tsx` lines 115–145 to confirm the exact disableDefaultUI and 0×0 anchor div pattern before editing.

    Change 1 — on the `<Map>` component (currently around line 116): add the `disableDefaultUI` boolean prop. Place it after `gestureHandling="greedy"` and before `style=...`. This hides the satellite/map-type toggle, fullscreen button, and street-view pegman — matching `Etusivu.tsx`, `app/business/map/page.tsx`, and `app/admin/[id]/page.tsx`.

    Change 2 — replace the self-closing `<AdvancedMarker position={pin} draggable onDragEnd={handleDragEnd} />` (around line 125) with a version that wraps the standard 0×0 anchor div and an inline SVG location pin:

    - The AdvancedMarker opening tag keeps `position={pin} draggable onDragEnd={handleDragEnd}` unchanged.
    - Inside, render `<div style={{ position: 'relative', width: 0, height: 0 }}>` as the anchor.
    - Inside that div, render an SVG: width 24, height 32, viewBox "0 0 24 32", positioned absolute with left -12px and bottom 0px so the teardrop tip aligns with the drag anchor, and pointerEvents none so drag events fall through to AdvancedMarker.
    - The SVG path is a teardrop shape that fills the viewBox with a pointed tip at the bottom, filled with the brand primary color #111111.
    - Add a white circle (cx 12, cy 12, r 4, fill white) inside the SVG to create a dot in the circle portion of the pin.

    Teardrop path to use (fits a 24×32 viewBox with tip at bottom-center): `M12 0C5.373 0 0 5.373 0 12c0 4.5 2.5 8.5 6.25 10.7L12 32l5.75-9.3C21.5 20.5 24 16.5 24 12 24 5.373 18.627 0 12 0Z`

    Do not change any other part of the component — the RecenterButton, AutocompleteZoomHandler, PlaceAutocompleteInput, address/city inputs, and the reverse-geocoding logic are all correct.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - `<Map>` has a `disableDefaultUI` prop alongside the existing `gestureHandling="greedy"`.
    - `<AdvancedMarker>` is no longer self-closing; it wraps a 0×0 anchor div containing the inline SVG pin.
    - `npx tsc --noEmit` passes with no new errors.
    - Visually: StepSijainti map shows no satellite toggle or fullscreen button, and the dropped pin is a black teardrop (not the default Google red pin).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SijaintiPicker (browser) | Pure visual change — no new data flows, no new API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-13 | Spoofing | custom pin SVG | low | accept | Inline SVG is static markup with no external src/href; no XSS vector introduced |

No package installs in this plan.
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- Manual check in StepSijainti: map renders without satellite toggle and fullscreen button.
- Clicking the map places a black teardrop pin (not the default Google red pin); pin is draggable.
- RecenterButton, autocomplete, and address/city inputs continue to function correctly.
</verification>

<success_criteria>
SijaintiPicker map has visual parity with the rest of the app's map components: disableDefaultUI hides default controls, and the location pin is a custom black teardrop matching the app's brand color.
</success_criteria>

<output>
Create `.planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-06-SUMMARY.md` when done.
</output>
