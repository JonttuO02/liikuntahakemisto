---
phase: 61-onboarding-vaiheiden-uudelleenj-rjestys
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/business/create-paikka/route.ts
  - app/api/business/update-paikka/route.ts
  - messages/fi.json
  - messages/en.json
autonomous: true
requirements: [ONBOARD-18, ONBOARD-19, ONBOARD-20, ONBOARD-24]
must_haves:
  truths:
    - "create-paikka accepts a name-only body and returns 200 with lat/lng stored as null"
    - "update-paikka accepts section 'sijainti' and persists osoite/kaupunki/latitude/longitude with coordinate-range validation"
    - "All 6 new Business i18n keys resolve in both fi.json and en.json"
  artifacts:
    - "app/api/business/create-paikka/route.ts (relaxed validation)"
    - "app/api/business/update-paikka/route.ts (new 'sijainti' section)"
    - "messages/fi.json and messages/en.json (6 new keys each)"
  key_links:
    - "StepSijainti (Plan 02) -> update-paikka 'sijainti' section"
    - "ProgressBar / StepNimiJaURL / StepSijainti -> new i18n keys"
    - "ClaimSearchForm (Plan 02) -> create-paikka name-only body"
---

<objective>
Establish the non-UI foundation for the onboarding reorder: relax `create-paikka` so a venue can be created from a name alone (location moves to a later step), add a `sijainti` write section to `update-paikka` so the new location step can persist coordinates, and add all six new i18n keys the reordered UI references.

Purpose: Every UI plan in this phase depends on these data-layer and i18n contracts existing first. Building the new step components against a `create-paikka` that still rejects null coordinates, or an `update-paikka` that returns "Invalid section" for `sijainti`, would fail at runtime.
Output: Two modified API route handlers and two modified message catalogs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-RESEARCH.md
@.planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relax create-paikka to require only yritysNimi</name>
  <files>app/api/business/create-paikka/route.ts</files>
  <read_first>
    - app/api/business/create-paikka/route.ts (current validation + insert; the coordinate parse already returns null for missing values)
    - .planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-RESEARCH.md (Pitfall 3, Code Example Pattern 5, Assumption A1)
    - .planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-PATTERNS.md (create-paikka section)
  </read_first>
  <action>
    In the validation guard, change the condition that currently requires `yritysNimi`, `osoite`, `kaupunki`, `latitude !== null`, and `longitude !== null` so it only requires `yritysNimi` (return the existing 400 `{ error: 'Missing fields' }` when `yritysNimi` is falsy). In the `.insert(...)` call, write `osoite: osoite || null` and `kaupunki: kaupunki || null`; leave `latitude` and `longitude` as their already-parsed values (these are null when absent). Do not change the auth check, the `business_paikka_links` insert, or the `laji: 'Muu'` / `published: false` defaults. Preserve the existing JWT verification via `supabaseAdmin.auth.getUser(token)`.
    GUARD (Assumption A1): Before finalizing, confirm `liikuntapaikat.latitude` and `liikuntapaikat.longitude` have no NOT NULL constraint — inspect supabase/migrations or the Supabase table editor. If a NOT NULL constraint exists, STOP and flag in the SUMMARY rather than shipping a route that will 500 on null coordinates.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The validation guard returns 400 only when `yritysNimi` is missing; a body with `yritysNimi` and no coordinates passes validation.
    - The insert payload writes `osoite: osoite || null` and `kaupunki: kaupunki || null`.
    - `npx tsc --noEmit` reports no errors.
    - SUMMARY records the result of the NOT NULL constraint check on latitude/longitude.
  </acceptance_criteria>
  <done>create-paikka accepts a name-only body; missing coordinates are stored as null instead of rejected.</done>
</task>

<task type="auto">
  <name>Task 2: Add 'sijainti' write section to update-paikka</name>
  <files>app/api/business/update-paikka/route.ts</files>
  <read_first>
    - app/api/business/update-paikka/route.ts (existing section branches, the ownership check, and the final `else` that returns 400 'Invalid section')
    - app/api/business/create-paikka/route.ts (the finite + ±90/±180 coordinate validation to mirror)
    - .planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-RESEARCH.md (Pitfall 9, Code Example Pattern 6, Security Domain V5)
    - .planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-PATTERNS.md (update-paikka section)
  </read_first>
  <action>
    Add a new `else if (section === 'sijainti')` branch immediately before the final `else` that returns `{ error: 'Invalid section' }`. In the branch, read `osoite`, `kaupunki`, `latitude`, `longitude` from `data`. Validate `latitude` as a finite number within -90..90 and `longitude` as a finite number within -180..180; if either is null/out-of-range, return 400 `{ error: 'Invalid coordinates' }`. Build the update payload with the validated `latitude`/`longitude`, `osoite` trimmed and sliced to 500 chars (null when not a string), and `kaupunki` trimmed and sliced to 500 chars (null when not a string). Do NOT add or weaken the existing ownership check via `business_paikka_links` — the new section must flow through the same ownership gate as the other sections (this covers the IDOR threat). Do not change any existing section.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - A `sijainti` branch exists and is reached before the `Invalid section` fallback.
    - Out-of-range or non-finite coordinates return 400 `{ error: 'Invalid coordinates' }`.
    - Valid coordinates produce an update payload containing `latitude`, `longitude`, `osoite`, `kaupunki`.
    - The `sijainti` branch is gated by the same `business_paikka_links` ownership check as existing sections (no new bypass).
    - `npx tsc --noEmit` reports no errors.
  </acceptance_criteria>
  <done>update-paikka persists location data for section 'sijainti' with range validation and existing ownership enforcement.</done>
</task>

<task type="auto">
  <name>Task 3: Add 6 onboarding i18n keys to fi.json and en.json</name>
  <files>messages/fi.json, messages/en.json</files>
  <read_first>
    - messages/fi.json (Business namespace — confirm none of the 6 keys already exist; insert alongside existing onboarding keys)
    - messages/en.json (Business namespace)
    - .planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-UI-SPEC.md (i18n Changes table + Copywriting Contract)
    - .planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-PATTERNS.md (messages section — exact key/value pairs)
  </read_first>
  <action>
    Add these six keys to the `Business` object in BOTH files (valid JSON, no trailing-comma errors). fi.json values: `stepSubmit` = "Lähetys", `stepNimiJaURLHeading` = "Paikkasi tiedot", `stepNimiJaURLWebsiteLabel` = "VERKKO-OSOITE (VALINNAINEN)", `stepNimiJaURLWebsiteHint` = "Verkko-osoitteen avulla täytämme tiedot automaattisesti", `stepNimiJaURLWebsitePlaceholder` = "https://...", `stepSijaintiHeading` = "Missä paikka sijaitsee?". en.json values: `stepSubmit` = "Submit", `stepNimiJaURLHeading` = "Your venue details", `stepNimiJaURLWebsiteLabel` = "WEBSITE (OPTIONAL)", `stepNimiJaURLWebsiteHint` = "We'll use your website to prefill venue details", `stepNimiJaURLWebsitePlaceholder` = "https://...", `stepSijaintiHeading` = "Where is your venue?". Leave the existing `stepPreview` key in place (cleanup deferred). Do not reorder or remove any existing keys.
  </action>
  <verify>
    <automated>node -e "['./messages/fi.json','./messages/en.json'].forEach(f=>{const b=require(f).Business;['stepSubmit','stepNimiJaURLHeading','stepNimiJaURLWebsiteLabel','stepNimiJaURLWebsiteHint','stepNimiJaURLWebsitePlaceholder','stepSijaintiHeading'].forEach(k=>{if(!b[k])throw new Error('missing '+k+' in '+f)})});console.log('i18n keys OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Both message files parse as valid JSON (the node check above does not throw).
    - All 6 keys are present in both `Business` objects with the specified values.
    - Existing keys, including `stepPreview`, are unchanged.
  </acceptance_criteria>
  <done>All six new keys resolve in both fi and en catalogs; the verify command prints "i18n keys OK".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → /api/business/create-paikka | Authenticated business user submits venue creation; previously trusted to send valid coordinates |
| browser client → /api/business/update-paikka | Authenticated business user writes a section of an owned venue |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-01 | Tampering | update-paikka 'sijainti' (IDOR on paikka_id) | medium | mitigate | Route the new section through the existing `business_paikka_links` ownership check — no new code path bypasses it (Task 2) |
| T-61-02 | Tampering | update-paikka 'sijainti' (out-of-range/non-finite coordinates) | low | mitigate | Explicit finite + ±90/±180 range validation, mirroring create-paikka (Task 2) |
| T-61-03 | Spoofing | create-paikka / update-paikka auth | low | mitigate | Existing `supabaseAdmin.auth.getUser(token)` JWT verification at the boundary is preserved unchanged |
| T-61-04 | Tampering | name-only create-paikka (junk venues) | low | accept | Auth required + `yritysNimi` non-empty; no real users yet, published:false default keeps unverified venues off the map |

No package-manager installs occur in this phase (RESEARCH: "No npm install needed" — VERIFIED), so no supply-chain (T-61-SC) threat applies.
</threat_model>

<verification>
- `npx tsc --noEmit` passes after all three tasks.
- A name-only POST to create-paikka returns 200 (manual UAT against running app).
- A POST to update-paikka with `section: 'sijainti'` and valid coordinates returns 200; out-of-range coordinates return 400 (manual UAT).
</verification>

<success_criteria>
create-paikka requires only `yritysNimi`; update-paikka supports a validated, ownership-gated `sijainti` section; all six new i18n keys resolve in fi and en.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 01)

- Modified `app/api/business/create-paikka/route.ts` — validation requires only `yritysNimi`; null coordinates accepted.
- Modified `app/api/business/update-paikka/route.ts` — new `section === 'sijainti'` branch writing `{ osoite, kaupunki, latitude, longitude }`.
- 6 new i18n keys in `messages/fi.json` and `messages/en.json` Business namespace: `stepSubmit`, `stepNimiJaURLHeading`, `stepNimiJaURLWebsiteLabel`, `stepNimiJaURLWebsiteHint`, `stepNimiJaURLWebsitePlaceholder`, `stepSijaintiHeading`.
</artifacts>

<output>
Create `.planning/phases/61-onboarding-vaiheiden-uudelleenj-rjestys/61-01-SUMMARY.md` when done.
</output>
