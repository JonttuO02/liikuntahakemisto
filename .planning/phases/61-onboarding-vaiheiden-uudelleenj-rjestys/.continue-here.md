---
context: phase
phase: 61-onboarding-vaiheiden-uudelleenj-rjestys
task: post-phase ad-hoc
status: paused
last_updated: 2026-06-26T15:00:01.298Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

<current_state>
Phase 61 oli jo merkitty valmiiksi (commit 13dd8de), mutta käyttäjä pyysi useita ad-hoc korjauksia
onboarding-flowiin suoraan keskustelussa. Kaikki muutokset on tehty mutta EI commitattu eikä
testattu selaimessa (vain `tsc --noEmit` läpäisty).

Muutetut tiedostot (uncommitted):
- app/components/ClaimSearchForm.tsx
- app/business/onboarding/page.tsx
- app/business/onboarding/ProgressBar.tsx
- app/business/onboarding/StepBrandingPick.tsx (UUSI)
- app/business/WizardInner.tsx
- lib/livePreview/LivePreviewContext.tsx
</current_state>

<completed_work>

### Korjaus 1: onboarding flow (page.tsx)
- `analyze`-phase poistettu kokonaan (AnalysoiSivusto ei enää onboarding-navigaatiossa)
- Lisätty `waiting`-phase: `WaitingForAI`-komponentti pollaa GET /api/business/analyze-website
  joka 2s, max 30 pollaus (~60s), sitten wizard tai skip
- sijainti `onNext`: URL → `waiting`, ei URL → `laji-skip`
- `handleBackToPrePhase`: `analyze` → `sijainti`

### Korjaus 2: ClaimSearchForm.tsx
- `type="url"` → `type="text"` (HTML5-validointi oli liian tiukka)
- `https://`-prepend ennen lähetystä (sama logiikka kuin AnalysoiSivusto)
- Fire-and-forget `POST /api/business/analyze-website` heti paikka luomisen jälkeen

### Korjaus 3: StepBrandingPick.tsx (uusi tiedosto)
- Wizard step 1 kun `brandingData.status === 'analyzed'`
- Logo-picker, galleria-picker, väri-picker, laji-picker
- Autosave PATCH `/api/business/branding` jokaisesta valinnasta
- `SET_MEDIA` dispatch live-previewiin logo/galleria
- `SET_BRAND_COLORS` dispatch väreille
- "Jatka →" → tallentaa media_urls + laji draftiin → re-fetch draft → wizard step 1 (StepMediat)

### Korjaus 4: WizardInner.tsx
- `inBrandingPick` state (true jos brandingData.status === 'analyzed')
- ProgressBar step-numerot +1 kun branding enabled (AI=1, Mediat=2, jne)
- `handleBrandingPickNext`: tallentaa valinnat + re-fetch draft
- `handleBackFromMediat`: palaa StepBrandingPickiin jos `brandingPickDone`
- Preview toggle FIX: `activeView === 'preview'` tarkistetaan ENNEN `inBrandingPick`

### Korjaus 5: ProgressBar.tsx
- `hasBrandingStep?: boolean` prop
- Labels kun true: `[AI-löydöt, Mediat, Hinnoittelu, Aukioloajat, Yhteystiedot]`

### Korjaus 6: LivePreviewContext.tsx
- `SET_BRAND_COLORS` action → `state.brandBgColor` / `state.brandAccentColor`
- `brandColor`/`accentColor` lukee ensin state:sta, sitten brandingData:sta
- `photo_urls` override useMemo:ssa: `state.media_urls?.photos` → `photo_urls`
  (buildBrandingPreview kovakoodasi tämän null:ksi)
</completed_work>

<remaining_work>

1. **git commit** — kaikki 6 muutettua tiedostoa uncommitted
2. **Browser-testaus** — koko flow:
   - /business → täytä nimi + URL → "Luo paikka"
   - → nimi-url (auto-skip) → sijainti → waiting (spinner + Ohita-nappi)
   - → StepBrandingPick (logo, värit, galleria, laji) → "Jatka →"
   - → wizard step 1 (StepMediat, pre-fillattuna)
3. **laji-skip testaus** — ilman URL:ia: sijainti → laji-skip → wizard
4. **Venue page preview** — puuttuu live-previewistä AI-löydöt stepissä
   (käyttäjä tunnisti, suunniteltu Phase 63:een LIVEPREV-05/PREV-05)
</remaining_work>

<decisions_made>

- **StepBrandingPick ei URL-routattu** — local `inBrandingPick` state. Ei step-numerointimuutoksia
  DB:ssä (current_step pysyy 1-4). Resume skiippaa branding-stepin (oikein: käyttäjä jo teki sen).
- **Värit: SET_BRAND_COLORS dispatch** — ei state-nostoa WizardInner/OnboardingModeen.
  Puhtaampi kuin prop-drillaus.
- **photo_urls fix kontekstitasolla** — buildBrandingPreview on jaettu (myös edit-mode käyttää),
  turvallisempi korjata LivePreviewContext.useMemo:ssa eikä builderissa.
- **ProgressBar labels swap** — 5 labelia pysyy vakiona; hasBrandingStep swappaa sisällön.
  Ei layout-muutoksia.
- **handleConfirm + handleSkip poistettu** — olivat dead code analyze-phasesin poiston jälkeen.
</decisions_made>

<blockers>
- Ei blokkereita. TypeScript puhdas (`tsc --noEmit` → 0 virheitä).
</blockers>

## Required Reading (in order)
1. Tämä tiedosto
2. `app/business/onboarding/page.tsx` — WaitingForAI + flow state machine
3. `app/business/WizardInner.tsx` — inBrandingPick + progressBar shift logic
4. `app/business/onboarding/StepBrandingPick.tsx` — uusi komponentti kokonaan

## Infrastructure State
- Next.js dev server: ei käynnissä (käynnistä `npm run dev`)
- TypeScript: puhdas
- Git: 6 uncommitted muutosta + 1 untracked (StepBrandingPick.tsx)

<context>
Tämä sessio oli kokonaan Phase 61:n jälkeisiä ad-hoc korjauksia käyttäjän pyynnöstä.
Ei virallista GSD-plania — kaikki muutokset tehty suoraan konversaatiossa.

Seuraava virallinen phase on Phase 62 (Venuepage-konsolidaatio), mutta sitä ei ole aloitettu.
Tee ensin git commit tämän session muutoksista, sitten testaa selaimessa.
</context>

<next_action>
1. `git add app/business/WizardInner.tsx app/business/onboarding/ProgressBar.tsx app/business/onboarding/page.tsx app/business/onboarding/StepBrandingPick.tsx app/components/ClaimSearchForm.tsx lib/livePreview/LivePreviewContext.tsx`
2. `git commit -m "feat(61): branding pick as wizard step 1 — waiting phase, StepBrandingPick, live preview fixes"`
3. Testaa selaimessa end-to-end
</next_action>
