---
phase: 57-dashboard-redirect-korjaus-kesken-tila
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [BIZPANEL-04, BIZPANEL-05]
files_modified:
  - lib/venueStatus.ts
  - lib/venueStatus.test.ts
  - app/business/page.tsx
  - messages/fi.json
  - messages/en.json
user_setup: []

must_haves:
  truths:
    - "Kirjautunut yritys jolla on kesken jäänyt onboarding-draft näkee /business-dashboardin (ei redirectiä /business/onboarding-sivulle)"
    - "Paikka jolla on onboarding_draft-rivi näyttää harmaan Kesken-badgen amber Pending-badgen sijaan"
    - "Kesken-rivin Jatka-nappi vie /business/onboarding?paikka_id=X-osoitteeseen"
    - "Tili jolla on 2+ samanaikaista draft-paikkaa näkee jokaisen erillisenä Kesken-rivinä"
    - "Kesken-rivin Esikatselu-nappi on disabloitu"
  artifacts:
    - path: "lib/venueStatus.ts"
      provides: "deriveVenueStatus pure helper — neljän tilan toisensa poissulkeva johtaminen (kesken/approved/rejected/pending)"
      exports: ["deriveVenueStatus"]
    - path: "lib/venueStatus.test.ts"
      provides: "Vitest-testit jotka todistavat Kesken-precedencen draft-rivin ollessa olemassa"
    - path: "app/business/page.tsx"
      provides: "Dashboard ilman automaattiredirectiä; per-rivi Kesken-badge + Jatka-CTA"
      contains: "keskenPaikkaIds"
    - path: "messages/fi.json"
      provides: "statusKesken + jatkaCta FI-käännökset"
      contains: "statusKesken"
    - path: "messages/en.json"
      provides: "statusKesken + jatkaCta EN-käännökset"
      contains: "statusKesken"
  key_links:
    - from: "app/business/page.tsx checkState()"
      to: "onboarding_draft-taulu"
      via: "supabase.from('onboarding_draft').select('paikka_id').eq('business_account_id', user.id) → keskenPaikkaIds Set"
      pattern: "from\\('onboarding_draft'\\)"
    - from: "app/business/page.tsx VenueRow"
      to: "/business/onboarding?paikka_id=X"
      via: "Jatka-CTA href kun isKesken on tosi"
      pattern: "/business/onboarding\\?paikka_id="
---

<objective>
Korjaa /business-dashboardin automaattiredirect ja lisää per-paikka Kesken-tila.

Tällä hetkellä `app/business/page.tsx`:n `checkState()` lähettää käyttäjän pakolla `/business/onboarding`-sivulle aina kun mikä tahansa `onboarding_draft`-rivi löytyy (rivit 189-200). Tämä estää dashboardin näkemisen ja sumentaa multi-venue-tilanteet yhdeksi booleaniksi.

Tämä plan poistaa redirectin (BIZPANEL-04 / D-01), hakee draft-paikkojen joukon tilaksi ja renderöi per-rivi neutraalin "Kesken"-badgen (BIZPANEL-05 / D-02..D-06) sekä "Jatka"-CTA:n joka vie `/business/onboarding?paikka_id=X` (D-08), Esikatselu disabloituna Kesken-riveillä (D-09). StatusCard pysyy ennallaan (D-07).

Purpose: viimeinen v3.0-phase, kytkeytyy Phase 56:n uudistettuun luo/claim-sisääntuloon muuttamatta sitä.
Output: `lib/venueStatus.ts` + testi, muokattu `app/business/page.tsx`, uudet i18n-avaimet molempiin kieliin.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-dashboard-redirect-korjaus-kesken-tila/57-CONTEXT.md
@.planning/phases/57-dashboard-redirect-korjaus-kesken-tila/57-PATTERNS.md
@.planning/phases/57-dashboard-redirect-korjaus-kesken-tila/57-UI-SPEC.md
</context>

## Artifacts this phase produces

This phase creates a small number of new symbols and edits existing ones:

**New exported symbols:**
- `deriveVenueStatus(claimStatus: string, hasDraft: boolean): 'kesken' | 'approved' | 'rejected' | 'pending'` — pure status-derivation helper in new file `lib/venueStatus.ts`. Encodes D-02/D-03/D-04 precedence (draft existence always wins → `'kesken'`).

**New files:**
- `lib/venueStatus.ts`
- `lib/venueStatus.test.ts`

**New i18n message keys** (added to both `messages/fi.json` and `messages/en.json`, `Business` namespace):
- `statusKesken` — FI "Kesken" / EN "In progress"
- `jatkaCta` — FI "Jatka" / EN "Continue"

**Edited existing symbols in `app/business/page.tsx`:**
- `checkState()` (inside `BusinessPage` `useEffect`) — redirect block removed; draft `paikka_id` fetch added.
- `BusinessPage` — new `useState` `keskenPaikkaIds: Set<number>`; `useRouter`/`router` removed if otherwise unused.
- `VenueRow` — new `isKesken: boolean` prop; badge ternary and action-button branch extended.

No brand-new exported component, decorator, class, CLI flag, or struct field beyond the above. No new file path other than the two `lib/venueStatus.*` files. No database schema changes (no new/modified `supabase/migrations/*.sql` — schema-history refs in CONTEXT.md are read-only context; no schema push task required).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract deriveVenueStatus pure helper with Vitest precedence test</name>
  <files>lib/venueStatus.ts, lib/venueStatus.test.ts</files>
  <read_first>
    - lib/normalizeNimi.test.ts (test convention: plain Vitest, Finnish `describe`/`it`, `import { describe, it, expect } from 'vitest'`, relative import of the helper)
    - vitest.config.ts (confirms `lib/**/*.test.ts` is included; node environment; `@` alias)
    - app/business/page.tsx lines 110-122 (existing claim_status ternary — the precedence this helper must reproduce: approved → rejected → pending fallback)
    - .planning/phases/57-dashboard-redirect-korjaus-kesken-tila/57-CONTEXT.md (D-02, D-03, D-04 — Kesken definition and mutual exclusion)
  </read_first>
  <behavior>
    - deriveVenueStatus('pending', true) → 'kesken' (draft exists → Kesken wins over pending, per D-04)
    - deriveVenueStatus('pending', false) → 'pending' (no draft → ordinary Pending, per D-03)
    - deriveVenueStatus('approved', false) → 'approved'
    - deriveVenueStatus('rejected', false) → 'rejected'
    - deriveVenueStatus('approved', true) → 'kesken' — draft existence takes precedence over every claim_status (D-02: draft existence = Kesken regardless of claim_status). This guards the mutual-exclusion invariant even for the theoretically-unreachable approved+draft combination.
    - deriveVenueStatus('rejected', true) → 'kesken' (same invariant)
    - Unknown/empty claim_status with hasDraft=false falls through to 'pending' (mirrors the existing ternary's else branch at line 115/121)
  </behavior>
  <action>
    RED first: write lib/venueStatus.test.ts covering all seven cases in the behavior block, importing `deriveVenueStatus` from './venueStatus'. Use the exact Vitest import and Finnish describe-block style from lib/normalizeNimi.test.ts. Run the test and confirm it fails (module not found / function undefined).

    GREEN: create lib/venueStatus.ts exporting a named function `deriveVenueStatus(claimStatus: string, hasDraft: boolean)` with return type union literal `'kesken' | 'approved' | 'rejected' | 'pending'`. Logic order is mandatory and reproduces D-04 precedence: check `hasDraft` FIRST and return `'kesken'` when true; only then branch on claimStatus — `'approved'` → `'approved'`, `'rejected'` → `'rejected'`, else → `'pending'`. The hasDraft check MUST be the first branch so draft existence always wins (D-02). Do not call toLowerCase or otherwise mutate claimStatus. No fenced code in this plan — implement per the behavior contract.

    This helper exists to make the four-state mutual-exclusion logic (D-04) unit-testable in node environment, since the dashboard itself is a client component with Supabase effects that cannot be unit-tested directly.
  </action>
  <verify>
    <automated>npx vitest run lib/venueStatus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/venueStatus.test.ts` exits 0 with all seven assertions passing
    - `lib/venueStatus.ts` exports a named `deriveVenueStatus` (grep: `export function deriveVenueStatus` or `export const deriveVenueStatus`)
    - In `deriveVenueStatus`, the `hasDraft` branch returning `'kesken'` appears before any `claimStatus` comparison (source assertion: first conditional references hasDraft)
    - Return type is the literal union `'kesken' | 'approved' | 'rejected' | 'pending'` (grep: `'kesken' | 'approved' | 'rejected' | 'pending'`)
  </acceptance_criteria>
  <done>deriveVenueStatus exists, is unit-tested, and encodes Kesken-first precedence; test suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove auto-redirect, fetch draft paikka_ids, render Kesken badge + Jatka CTA in app/business/page.tsx</name>
  <files>app/business/page.tsx</files>
  <read_first>
    - app/business/page.tsx (FULL FILE — current state: checkState() at 171-214 with redirect block 189-200; VenueRow at 93-159; badge ternary 110-122; action buttons 126-141; caller .map at 278-286; useState block 165-169; useRouter at 4 and 164)
    - lib/venueStatus.ts (the deriveVenueStatus helper from Task 1 — may be used to derive the badge/label, or VenueRow may take a precomputed isKesken boolean; either is acceptable as long as draft-existence precedence holds)
    - .planning/phases/57-dashboard-redirect-korjaus-kesken-tila/57-PATTERNS.md (exact replacement patterns for redirect removal, badge ternary, action buttons, and the new keskenPaikkaIds state)
    - .planning/phases/57-dashboard-redirect-korjaus-kesken-tila/57-UI-SPEC.md (Kesken badge color rgba(17,17,17,0.08)/rgba(17,17,17,0.55); badge structural classes; Jatka reuses muokkaaCta outlined-pill styling; layout must not change)
    - app/business/onboarding/page.tsx (confirms ?paikka_id=X resume param is read via useSearchParams — Jatka CTA target is valid, no new routing needed)
  </read_first>
  <action>
    Make these edits inside app/business/page.tsx. Re-verify exact line numbers against the live file before editing (PATTERNS.md notes Phase 56 may have drifted lines by ~2):

    1. REDIRECT REMOVAL (D-01, BIZPANEL-04): Delete the draft-existence early-return block currently at lines 189-200 (the `const { data: drafts } = await supabase.from('onboarding_draft').select('id').eq(...).limit(1)` query plus the `if (drafts && drafts.length > 0) { router.push('/business/onboarding'); return }` guard, including the two-line comment above it). Replace it with a draft fetch that selects `paikka_id` (no `.limit(1)`, no early return) and builds a `Set<number>` of draft paikka_ids, then stores it via `setKeskenPaikkaIds(...)`. Keep the existing `links` query (currently 202-209) and `setVenueLinks(...)` exactly as-is; add `setKeskenPaikkaIds(keskenSet)` before `setLoading(false)`.

    2. NEW STATE: Add `const [keskenPaikkaIds, setKeskenPaikkaIds] = useState<Set<number>>(new Set())` alongside the existing useState declarations near line 165, following the existing `useState<VenueLink[]>([])` convention.

    3. ROUTER CLEANUP: After removing the only `router.push` call, the `useRouter` import (line 4) and `const router = useRouter()` (line 164) become unused. Remove both. If TS/ESLint strict mode reports them still needed for another reason discovered at execution time, keep only what the compiler requires and note it in the SUMMARY.

    4. VENUEROW PROP: Add `isKesken: boolean` to the VenueRow props type and destructure (signature currently 93-103). Thread it from the caller `.map` (currently 278-286) as `isKesken={keskenPaikkaIds.has(link.paikka_id)}`.

    5. BADGE (D-04, D-05, D-06, BIZPANEL-05): Extend the badge className+label ternary (currently 110-122) so that when `isKesken` is true the badge uses background class `bg-[rgba(17,17,17,0.08)]` and text class `text-[rgba(17,17,17,0.55)]` and label `t('statusKesken')`, taking precedence BEFORE the existing approved/rejected/pending branches. Keep the structural classes `text-[10px] font-bold uppercase tracking-widest px-2 py-1 rounded-full shrink-0` unchanged. Never render both Kesken and a claim_status badge.

    6. ACTION BUTTONS (D-08, D-09): In the action-button block (currently 126-141): set the Esikatselu button `disabled` to `isKesken || !link.liikuntapaikat` (reuse the existing `disabled:opacity-40 disabled:pointer-events-none` classes already on the button — no new CSS). Make the second control's `href` conditional: `isKesken ? '/business/onboarding?paikka_id=' + link.paikka_id : '/business/' + link.paikka_id`, and its label conditional: `isKesken ? t('jatkaCta') : t('muokkaaCta')`. Keep the outlined-pill className unchanged.

    Do NOT touch StatusCard (D-07) or the empty-state branch (venueLinks.length === 0). Do NOT change row flex structure, padding, or height (UI-SPEC layout-impact note). No fenced code in this plan — apply per PATTERNS.md replacement patterns.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run lib/venueStatus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports no type errors
    - No `router.push('/business/onboarding')` remains in app/business/page.tsx (grep: `router.push` returns nothing in this file)
    - The `onboarding_draft` query selects `paikka_id` and has no `.limit(1)` (grep `from('onboarding_draft')` context shows `.select('paikka_id')` and no `.limit(`)
    - `keskenPaikkaIds` state exists and is passed as `isKesken={keskenPaikkaIds.has(link.paikka_id)}` to VenueRow (grep: `keskenPaikkaIds.has`)
    - Kesken badge uses `bg-[rgba(17,17,17,0.08)]` and `text-[rgba(17,17,17,0.55)]` (grep both literals present)
    - Jatka CTA href contains `/business/onboarding?paikka_id=` guarded by isKesken (grep: `/business/onboarding?paikka_id=`)
    - Esikatselu button disabled expression includes `isKesken` (grep: `disabled={isKesken`)
    - StatusCard function body is unchanged (no edits between its `function StatusCard` and the next `// --- VenueRow ---` marker)
  </acceptance_criteria>
  <done>Dashboard renders without redirect; Kesken rows show gray badge + Jatka CTA + disabled Esikatselu; tsc clean; helper test green.</done>
</task>

<task type="auto">
  <name>Task 3: Add statusKesken + jatkaCta i18n keys to fi.json and en.json</name>
  <files>messages/fi.json, messages/en.json</files>
  <read_first>
    - messages/fi.json lines 141-145 (statusPending/statusApproved/statusRejected cluster) and 206-209 (muokkaaCta/esikatseluCta cluster), inside the `Business` namespace
    - messages/en.json lines 141-145 and 206-209 (mirror structure)
    - .planning/phases/57-dashboard-redirect-korjaus-kesken-tila/57-UI-SPEC.md Copywriting Contract (FI "Kesken"/EN "In progress"; FI "Jatka"/EN "Continue")
  </read_first>
  <action>
    Add two new keys to the `Business` namespace in BOTH message files (CLEAN-06/07 full-coverage requirement — both files mirror key-for-key).

    fi.json: add `"statusKesken": "Kesken",` immediately after the `"statusRejected": "Hylätty",` line, and `"jatkaCta": "Jatka",` immediately after the `"muokkaaCta": "Muokkaa",` line.

    en.json: add `"statusKesken": "In progress",` immediately after `"statusRejected": "Rejected",`, and `"jatkaCta": "Continue",` immediately after `"muokkaaCta": "Edit",`.

    These are mid-object insertions, so each new line keeps a trailing comma matching surrounding keys. The file must remain valid JSON (no trailing comma on the object's actual last key). Key names must match exactly what page.tsx references: `statusKesken` and `jatkaCta`.
  </action>
  <verify>
    <automated>node -e "['fi','en'].forEach(l=>{const m=require('./messages/'+l+'.json').Business;if(!m.statusKesken||!m.jatkaCta)throw new Error(l+' missing keys');});console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Both messages/fi.json and messages/en.json parse as valid JSON (the node verify command exits 0)
    - fi.json Business.statusKesken === "Kesken" and Business.jatkaCta === "Jatka"
    - en.json Business.statusKesken === "In progress" and Business.jatkaCta === "Continue"
    - No other Business keys were removed or reordered (grep statusApproved/statusPending/muokkaaCta/esikatseluCta still present in both files)
  </acceptance_criteria>
  <done>statusKesken and jatkaCta present and correct in both locale files; both files valid JSON.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify Kesken dashboard behavior (4 ROADMAP success criteria)</name>
  <action>Human verification only — no code changes. The verifier exercises the running /business dashboard against the four Phase 57 success criteria using the steps in how-to-verify below.</action>
  <verify><human-check>See how-to-verify steps; verifier confirms each of BIZPANEL-04/05 and D-05/D-06/D-08/D-09 in the running app.</human-check></verify>
  <done>Verifier types "approved" after confirming no redirect, Kesken badge, Jatka resume, multi-draft rows, and EN copy.</done>
  <what-built>
    /business-dashboard no longer auto-redirects to onboarding; each venue with an in-progress onboarding_draft shows a gray "Kesken" / "In progress" badge (instead of the amber Pending badge) and a "Jatka" / "Continue" button linking to /business/onboarding?paikka_id=X, with Esikatselu disabled on those rows. Multiple simultaneous drafts each render as their own Kesken row. StatusCard top banner is unchanged.
  </what-built>
  <how-to-verify>
    Prereq: be logged in as a business account that has at least one venue with an in-progress onboarding draft (start onboarding for a created venue and stop before submit). For criterion 4, have two such venues.

    1. Visit /business while a draft exists. EXPECT: the dashboard renders (venue list visible) — you are NOT bounced to /business/onboarding. (BIZPANEL-04)
    2. Find the venue with the in-progress draft in the list. EXPECT: a gray badge reading "Kesken" (FI) / "In progress" (EN), NOT the amber "Odottaa hyväksyntää" / "Pending approval" badge. (BIZPANEL-05, D-05/D-06)
    3. On that Kesken row, EXPECT the action button reads "Jatka" / "Continue" (not "Muokkaa" / "Edit"), and the "Esikatselu" / "Preview" button is greyed out / unclickable. (D-08/D-09)
    4. Click "Jatka". EXPECT: navigation to /business/onboarding?paikka_id=<that venue's id> and the wizard resumes that venue. (BIZPANEL-05 criterion 3)
    5. With TWO in-progress drafts, EXPECT both appear as separate rows, each with its own Kesken badge and its own Jatka button. (criterion 4 — not collapsed to one boolean)
    6. Toggle locale to EN (profile language toggle) and re-check steps 2-3 show English copy. (CLEAN-06 coverage)
    7. Confirm an approved/pending/rejected venue WITHOUT a draft still shows its normal colored badge and "Muokkaa"/"Edit" button. (D-03/D-04 — no regression)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any mismatch (wrong badge color, redirect still firing, missing EN copy, drafts collapsed, etc.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase (browser client) | `checkState()` reads `onboarding_draft` and `business_paikka_links` via the business browser client, scoped by `business_account_id = user.id`; RLS is the enforcement layer |
| browser → /business/onboarding?paikka_id=X | user-controlled `paikka_id` query param consumed by the wizard resume flow |

## STRIDE Threat Register

ASVS level: 1. Block threshold: high. No high-severity threats identified in this phase (read-only dashboard query reuse + a navigation href; no new write path, no new endpoint, no new dependency, no schema change).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Information Disclosure | new `onboarding_draft` browser query selecting `paikka_id` | mitigate | Query is scoped `.eq('business_account_id', user.id)` exactly like the adjacent existing `links` query; RLS on `onboarding_draft` already restricts rows to the owning account. Selects only `paikka_id` (no draft contents). No widening of data exposure vs. the pre-existing `.select('id')` query it replaces. |
| T-57-02 | Tampering | user-supplied `paikka_id` in `/business/onboarding?paikka_id=X` Jatka link | accept | The href is built from `link.paikka_id` returned by the user's own RLS-scoped `business_paikka_links` query, not free user input. The wizard resume path (StepPaikkaPrePhase / WizardInner) already validates `paikka_id` ownership against `business_paikka_links` (pre-existing, Phase 56). This phase adds no new trust in the param beyond what already exists. |
| T-57-03 | Elevation of Privilege | redirect removal exposing dashboard to non-onboarded accounts | accept | Removing the redirect only changes navigation, not authorization. The existing `business_accounts` membership guard (`isNotBusinessAccount`) and per-route RSC auth guards remain. A user seeing their own dashboard instead of being bounced to onboarding is the intended behavior (BIZPANEL-04), not a privilege gain. |
| T-57-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase — no new dependencies added (all imports are existing: next-intl, framer-motion, lucide-react, supabase-business). No legitimacy gate required. |
</threat_model>

<verification>
- `npx vitest run lib/venueStatus.test.ts` — all assertions pass (deriveVenueStatus precedence)
- `npx tsc --noEmit` — no type errors after page.tsx edits
- `node -e "..."` JSON key check — statusKesken + jatkaCta present in both locale files
- `npx next lint` (if run) — no new lint errors; unused `useRouter` import removed
- Human-verify checkpoint — all 4 ROADMAP success criteria confirmed in the running app
- grep: no `router.push('/business/onboarding')` remains in app/business/page.tsx
</verification>

<success_criteria>
All four Phase 57 ROADMAP success criteria TRUE:
1. /business never auto-redirects to onboarding — dashboard always renders for a logged-in business account (redirect block removed; verified in checkpoint step 1).
2. In-progress onboarding shows a per-venue "Kesken" badge (gray, replaces amber Pending; verified step 2).
3. User can resume by selecting the venue → /business/onboarding?paikka_id=X (Jatka CTA; verified steps 3-4).
4. An account with 2+ simultaneous drafts sees each as a separate resumable row (Set-based per-row branching, not one boolean; verified step 5).

Plus: BIZPANEL-04 and BIZPANEL-05 satisfied; full FI/EN i18n coverage; no StatusCard regression (D-07); no schema change.
</success_criteria>

<output>
Create `.planning/phases/57-dashboard-redirect-korjaus-kesken-tila/57-01-SUMMARY.md` when done
</output>
