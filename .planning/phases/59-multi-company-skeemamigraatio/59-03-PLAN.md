---
phase: 59-multi-company-skeemamigraatio
plan: 03
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - app/api/admin/reject/route.ts
  - app/api/admin/approve/route.ts
  - app/api/admin/applications/route.ts
  - app/api/admin/applications/[id]/route.ts
  - app/admin/page.tsx
  - app/admin/AdminApplicationList.tsx
  - app/admin/[id]/page.tsx
autonomous: true
requirements: [ACCESS-01, ACCESS-02]

must_haves:
  truths:
    - "admin/approve and admin/reject read companies(name) via company_id for their notification emails"
    - "admin/applications and admin/applications/[id] embedded selects read business_accounts(role, role_in_company, user_id, companies(name)) — role_in_company preserved, NOT conflated with the new role enum"
    - "The duplicated Application TS type in all three admin UI files reads companies: { name } | null nested in business_accounts"
    - "The admin UI render sites read app.business_accounts?.companies?.name"
    - "npm run build (or tsc) succeeds — no type error from the changed Application shape"
  artifacts:
    - path: "app/api/admin/applications/route.ts"
      provides: "embedded select reading companies(name) through the company_id FK"
      contains: "companies(name)"
    - path: "app/admin/AdminApplicationList.tsx"
      provides: "Application type + render site using companies.name"
      contains: "companies"
  key_links:
    - from: "app/api/admin/applications/route.ts"
      to: "companies table"
      via: "business_accounts(role, role_in_company, user_id, companies(name)) embedded select"
      pattern: "companies\\(name\\)"
    - from: "app/admin/AdminApplicationList.tsx"
      to: "app/api/admin/applications/route.ts"
      via: "renders app.business_accounts?.companies?.name from the fetched embedded select"
      pattern: "companies\\?\\.name"
---

<objective>
Update the four admin Route Handlers and three admin UI files that read `business_accounts.company_name` so they join through the new `company_id` FK to `companies.name`, while strictly preserving the unrelated `role_in_company` free-text field (which must NOT be confused with the new `role` owner/member enum).

Purpose: After Plan 01 drops `business_accounts.company_name`, the admin application list/detail views and approval/rejection emails would break. These edits must ship in the same release as the migration (Pitfall 4).
Output: Four corrected admin routes and three corrected admin UI files with a green type check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/59-multi-company-skeemamigraatio/59-RESEARCH.md
@.planning/phases/59-multi-company-skeemamigraatio/59-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite approve + reject route email SELECTs to use companies(name)</name>
  <files>app/api/admin/approve/route.ts, app/api/admin/reject/route.ts</files>
  <read_first>
    - app/api/admin/approve/route.ts (current SELECT company_name by link.business_account_id ~lines 61-82)
    - app/api/admin/reject/route.ts (current SELECT company_name ~lines 70-92)
    - .planning/phases/59-multi-company-skeemamigraatio/59-PATTERNS.md (approve/reject shared Pattern Assignment)
  </read_first>
  <action>
    In BOTH files the email block today does `.from('business_accounts').select('company_name').eq('user_id', link.business_account_id).single()` then passes `companyName: biz.company_name` into `sendApprovalEmail` / `sendRejectionEmail`. Change the SELECT to `.select('companies(name)')` and the email argument to `companyName: biz.companies?.name`. The JWT-verify + `profiles.is_admin` authorization block (lines ~6-21 in both) is unchanged — do not touch it. The change is mechanically identical in both files.
  </action>
  <verify>
    <automated>grep -L "company_name" app/api/admin/approve/route.ts app/api/admin/reject/route.ts && grep -q "companies(name)" app/api/admin/approve/route.ts && grep -q "companies(name)" app/api/admin/reject/route.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Neither approve nor reject route references `company_name`
    - Both use `.select('companies(name)')` and pass `companyName: biz.companies?.name`
    - The is_admin authorization block is unchanged in both
  </acceptance_criteria>
  <done>Approval/rejection emails read the company name via the companies FK join.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite admin applications list + detail embedded selects (preserve role_in_company)</name>
  <files>app/api/admin/applications/route.ts, app/api/admin/applications/[id]/route.ts</files>
  <read_first>
    - app/api/admin/applications/route.ts (embedded select ~lines 16-22: business_accounts(company_name, role_in_company, user_id))
    - app/api/admin/applications/[id]/route.ts (same pattern ~lines 19-25)
    - .planning/phases/59-multi-company-skeemamigraatio/59-PATTERNS.md (applications Pattern Assignment + the role_in_company disambiguation)
    - .planning/phases/59-multi-company-skeemamigraatio/59-RESEARCH.md (App Code Call Sites — role_in_company vs role disambiguation, lines covering admin/applications)
  </read_first>
  <action>
    In BOTH files the embedded select today is `business_accounts(company_name, role_in_company, user_id)` nested inside the business_paikka_links select. Change the embedded `business_accounts(...)` field list to `business_accounts(role, role_in_company, user_id, companies(name))` — this nests the companies row through the second FK hop (Supabase JS embedded-resource syntax). CRITICAL: keep `role_in_company` (the existing free-text job-title field from 20260610000002) EXACTLY as-is; it is unrelated to and must not be conflated with the new `role` enum column. Include the new `role` column so the admin UI can surface owner/member if desired; do not remove role_in_company. Do not change the `.eq('claim_status','pending')` / `.order(...)` clauses.
  </action>
  <verify>
    <automated>grep -q "companies(name)" app/api/admin/applications/route.ts && grep -q "companies(name)" "app/api/admin/applications/[id]/route.ts" && grep -q "role_in_company" app/api/admin/applications/route.ts && grep -L "business_accounts(company_name" app/api/admin/applications/route.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both embedded selects read `business_accounts(role, role_in_company, user_id, companies(name))`
    - Neither file's embedded select still names `company_name`
    - `role_in_company` is preserved in both (not removed, not renamed to `role`)
  </acceptance_criteria>
  <done>Admin list and detail routes fetch the company name via the nested companies FK while preserving role_in_company.</done>
</task>

<task type="auto">
  <name>Task 3: Update the duplicated Application type + render sites in all three admin UI files</name>
  <files>app/admin/page.tsx, app/admin/AdminApplicationList.tsx, app/admin/[id]/page.tsx</files>
  <read_first>
    - app/admin/page.tsx (Application type ~line 14)
    - app/admin/AdminApplicationList.tsx (Application type ~line 13; render company_name ~lines 79-82)
    - app/admin/[id]/page.tsx (Application type ~line 30; render company_name ~lines 127-128)
    - .planning/phases/59-multi-company-skeemamigraatio/59-PATTERNS.md (admin UI Pattern Assignment — exact type + render change)
  </read_first>
  <action>
    The `Application` type is hand-duplicated across all three files. In each, the `business_accounts` member today is `{ company_name: string; role_in_company: string | null; user_id: string } | null`. Change it to `{ role_in_company: string | null; user_id: string; companies: { name: string } | null } | null` (drop `company_name`, add the nested `companies` object; keep `role_in_company`). Then change every render site that reads `app.business_accounts?.company_name` (AdminApplicationList.tsx ~line 79; app/admin/[id]/page.tsx ~line 127) to `app.business_accounts?.companies?.name ?? '—'`, preserving the existing `?? '—'` fallback. All three files must be updated together in one pass since the type is duplicated (no shared type import exists today; do not introduce one unless trivially safe). Keep all `role_in_company` rendering as-is.
  </action>
  <verify>
    <automated>grep -L "company_name" app/admin/page.tsx app/admin/AdminApplicationList.tsx "app/admin/[id]/page.tsx" && grep -q "companies?.name" app/admin/AdminApplicationList.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - None of the three admin UI files reference `company_name`
    - The `business_accounts` Application member in all three is `{ role_in_company: string | null; user_id: string; companies: { name: string } | null } | null`
    - Render sites read `app.business_accounts?.companies?.name ?? '—'`
    - `role_in_company` rendering preserved
    - `npm run build` (TypeScript type check) succeeds
  </acceptance_criteria>
  <done>All three admin UI files use the nested companies type and render company name through it; the build type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin client → admin Route Handlers | JWT-verified + profiles.is_admin gate at entry (unchanged this phase) |
| admin Route Handler → companies/business_accounts via supabaseAdmin | Service-role read of company name through FK join |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-09 | Elevation of Privilege | admin role / role_in_company conflation | mitigate | role_in_company (free-text) is explicitly preserved and kept distinct from the new role enum in every embedded select and type; no privilege field is reinterpreted as the other |
| T-59-10 | Information Disclosure | admin application list company name | accept | Admin routes already gate on profiles.is_admin (unchanged); the company name surfaced is the same datum as before, only the read path changes (FK join vs flat column) |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | No package installs — app-code edits only |
</threat_model>

<verification>
- No `company_name` references remain in the four admin routes or three admin UI files
- Embedded selects read companies(name); role_in_company preserved everywhere
- `npm run build` type-checks clean against the updated Application type
</verification>

<success_criteria>
- ACCESS-01/02: admin-side reads of the company name go through companies.name via company_id; the dropped column is fully removed from admin routes and UI
- role_in_company free-text field preserved, not conflated with the new role enum
</success_criteria>

<output>
Create `.planning/phases/59-multi-company-skeemamigraatio/59-03-SUMMARY.md` when done.
</output>
