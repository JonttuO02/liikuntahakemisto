---
phase: 59-multi-company-skeemamigraatio
plan: 04
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - app/api/business/register/route.ts
  - tests/api/register.test.ts
  - app/business/profiili/page.tsx
  - .planning/phases/59-multi-company-skeemamigraatio/59-DEPLOY-RUNBOOK.md
autonomous: false
requirements: [ACCESS-01, ACCESS-02]

must_haves:
  truths:
    - "A new business signup creates its own companies row first, then inserts business_accounts with company_id + role = 'owner' (the forward-going equivalent of the backfill)"
    - "If the business_accounts insert fails, the just-created companies row is rolled back (deleted); the auth user is never deleted on failure"
    - "tests/api/register.test.ts exists and asserts the company-then-account insert order, role='owner', and rollback-on-failure"
    - "app/business/profiili/page.tsx reads companies(name) via the anon-key client and resolves the account for a real business user under the new companies RLS SELECT policy"
    - "A deploy runbook documents the staging→merge→production-migration→Vercel-deploy ordering (Pitfall 4)"
    - "npx vitest run tests/api/register.test.ts passes"
  artifacts:
    - path: "app/api/business/register/route.ts"
      provides: "Signup creates a company then an owner business_accounts row with company_id, with rollback"
      contains: "role: 'owner'"
    - path: "tests/api/register.test.ts"
      provides: "Coverage for the new company-creation signup behavior (was zero coverage)"
      contains: "register"
    - path: "app/business/profiili/page.tsx"
      provides: "anon-key read of companies(name) via company_id under RLS"
      contains: "companies"
    - path: ".planning/phases/59-multi-company-skeemamigraatio/59-DEPLOY-RUNBOOK.md"
      provides: "Operator deploy-ordering runbook for the coupled SQL+app-code release"
      contains: "PITR"
  key_links:
    - from: "app/api/business/register/route.ts"
      to: "companies table"
      via: "INSERT companies(name) RETURNING id, then INSERT business_accounts({ user_id, company_id, role: 'owner', role_in_company }); rollback deletes companies on account-insert failure"
      pattern: "role: 'owner'"
    - from: "app/business/profiili/page.tsx"
      to: "companies RLS SELECT policy"
      via: "anon-key .select('companies(name), contact_phone'); requires companies SELECT policy USING (id = current_company_id())"
      pattern: "companies\\?\\.name|companies\\(name\\)"
---

<objective>
Add forward-going company-creation to the business signup route (every new account becomes the owner of its own new company, matching Plan 01's backfilled shape), create the missing test coverage for it, update the one anon-key RLS-subject read site (`profiili`) to join through `company_id`, and document the coupled SQL+app-code deploy runbook.

Purpose: Without register-route changes, every account created after the migration would have `company_id IS NULL`, immediately breaking `current_company_id()` for them. `profiili` is the highest-risk read site (Pitfall 3 — anon-key, RLS-subject): if the new companies SELECT policy is wrong, it silently redirects real users to `/business`. The runbook makes Pitfall 4's manual deploy ordering explicit.
Output: A corrected register route with new test coverage, a corrected profiili page, and a deploy runbook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/59-multi-company-skeemamigraatio/59-CONTEXT.md
@.planning/phases/59-multi-company-skeemamigraatio/59-RESEARCH.md
@.planning/phases/59-multi-company-skeemamigraatio/59-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add company-creation + owner role to the signup route (with rollback)</name>
  <files>app/api/business/register/route.ts</files>
  <read_first>
    - app/api/business/register/route.ts (current shape: JWT verify ~lines 4-13; INSERT business_accounts(user_id, company_name, role_in_company) ~lines 16-36; failure handling ~lines 39-43)
    - app/api/business/create-paikka/route.ts (the multi-step insert + rollback-on-failure pattern to mirror, ~lines 72-118)
    - .planning/phases/59-multi-company-skeemamigraatio/59-PATTERNS.md (register Pattern Assignment — exact new-behavior shape)
  </read_first>
  <action>
    Keep the JWT-verify-first block unchanged. Replace the single `INSERT business_accounts(user_id, company_name, role_in_company)` with a two-step insert that mirrors create-paikka's insert-then-rollback shape:
    (1) `INSERT INTO companies(name) VALUES (company_name) RETURNING id` via supabaseAdmin, capturing the new company id. The `company_name` request field is still parsed/validated exactly as today (its validation is unchanged) — it now feeds companies.name instead of business_accounts.company_name (D-05).
    (2) `INSERT business_accounts({ user_id: user.id, company_id: <new company id>, role: 'owner', role_in_company })` — note `role: 'owner'` (the new enum column, D-09 forward-going equivalent) AND the existing `role_in_company` free-text field both set; they are distinct, do not conflate. No longer write `company_name` to business_accounts (column dropped).
    (3) Rollback: if the business_accounts insert fails, delete the just-created companies row (`supabaseAdmin.from('companies').delete().eq('id', newCompanyId)`), log a CRITICAL message if the rollback delete itself fails (mirror create-paikka's orphaned-row log), and return the error response. Per the existing file's convention (lines ~39-43), do NOT delete the auth user on failure — only the orphaned companies row.
    These are supabaseAdmin service-role writes (CLAUDE.md) — no client-side companies write path is introduced, consistent with the companies table having no INSERT RLS policy (Plan 01 / Pitfall 3).
  </action>
  <verify>
    <automated>npx vitest run tests/api/register.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - register route inserts into `companies` first (RETURNING id), then business_accounts with `company_id` and `role: 'owner'`
    - `role_in_company` is still set and is distinct from `role`
    - business_accounts insert no longer writes `company_name`
    - On account-insert failure the new companies row is deleted (rollback); the auth user is not deleted
    - JWT-verify block unchanged
    - `npx vitest run tests/api/register.test.ts` passes
  </acceptance_criteria>
  <done>New signups create their own owner company atomically with rollback; covered by the new test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/api/register.test.ts (new coverage for the signup behavior)</name>
  <files>tests/api/register.test.ts</files>
  <read_first>
    - tests/api/create-paikka.test.ts (structural analog: how the supabaseAdmin client + auth.getUser are mocked, how insert/rollback paths are asserted)
    - tests/api/submit.test.ts (sibling mock style)
    - app/api/business/register/route.ts (the route under test, after Task 1)
    - .planning/phases/59-multi-company-skeemamigraatio/59-VALIDATION.md (Wave 0 gap definition for this file)
  </read_first>
  <behavior>
    - Happy path: valid auth + company_name → companies.insert called with { name: company_name }, then business_accounts.insert called with company_id (the returned id) + role: 'owner' + role_in_company; route returns success
    - Insert order: the companies insert resolves before the business_accounts insert is attempted
    - Rollback: when the business_accounts insert returns an error, companies.delete is called with the new company id, and the route returns an error status
    - Auth guard: missing/invalid Authorization header → 401, no inserts attempted
  </behavior>
  <action>
    Create `tests/api/register.test.ts` mirroring the mocking structure of create-paikka.test.ts (mock supabaseAdmin, auth.getUser, and the chained .from().insert()/.delete()/.select() calls). Cover the four behaviors in the behavior block: happy path (company-then-account insert with role='owner'), insert ordering, rollback-on-account-failure (companies.delete called), and the 401 auth guard. Assert role='owner' and that company_name is NOT written to business_accounts. Use Vitest (the project's framework). This file did not exist before — register/route.ts had zero coverage.
  </action>
  <verify>
    <automated>npx vitest run tests/api/register.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/api/register.test.ts exists and contains tests for happy path, insert ordering, rollback-on-failure, and 401 auth guard
    - A test asserts business_accounts insert receives role: 'owner' and a company_id
    - A test asserts companies.delete is called on account-insert failure
    - `npx vitest run tests/api/register.test.ts` passes
  </acceptance_criteria>
  <done>The previously-uncovered register route has a green Vitest suite covering its new company-creation behavior and rollback.</done>
</task>

<task type="auto">
  <name>Task 3: Update profiili anon-key read to join companies(name) under RLS</name>
  <files>app/business/profiili/page.tsx</files>
  <read_first>
    - app/business/profiili/page.tsx (full file — the only anon-key, RLS-subject business read site; SELECT company_name, contact_phone ~lines 11-19; redirect on null ~line)
    - .planning/phases/59-multi-company-skeemamigraatio/59-PATTERNS.md (profiili Pattern Assignment — highest-risk Pitfall 3 site)
    - .planning/phases/59-multi-company-skeemamigraatio/59-RESEARCH.md (Pitfall 3 — companies RLS SELECT policy must allow this anon-key read)
  </read_first>
  <action>
    Change the anon-key `createBusinessServerClient` read from `.select('company_name, contact_phone')` to `.select('companies(name), contact_phone')`, and change the prop passed to `BusinessProfiiliClient` from `companyName={account.company_name}` to `companyName={account.companies?.name ?? ''}`. Keep the `if (!account) redirect('/business')` guard and `.maybeSingle()` unchanged. This client uses the anon key (subject to RLS, unlike the supabaseAdmin sites in Plans 02/03), so this read depends on Plan 01's companies SELECT policy `USING (id = current_company_id())` being correct — this is the exact read the D-13 staging login check (Plan 01 Task 3 / production runbook) must confirm still resolves for a real business user.
  </action>
  <verify>
    <automated>grep -L "company_name" app/business/profiili/page.tsx && grep -q "companies" app/business/profiili/page.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - app/business/profiili/page.tsx no longer references `company_name`
    - Reads `.select('companies(name), contact_phone')` via the anon-key client
    - Passes `companyName={account.companies?.name ?? ''}`
    - The `if (!account) redirect('/business')` guard and `.maybeSingle()` are unchanged
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>The profiili page reads the company name through the companies FK under the new anon-key RLS policy.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 4 [BLOCKING]: Write the coupled SQL+app-code deploy runbook (Pitfall 4)</name>
  <files>.planning/phases/59-multi-company-skeemamigraatio/59-DEPLOY-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/59-multi-company-skeemamigraatio/59-CONTEXT.md (D-01 through D-04 — PITR + staging dry-run; no pg_dump/down-migration)
    - .planning/phases/59-multi-company-skeemamigraatio/59-RESEARCH.md (Pitfall 4 deploy-ordering; D-13 manual login regression)
    - .planning/phases/59-multi-company-skeemamigraatio/59-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <action>
    Create `.planning/phases/59-multi-company-skeemamigraatio/59-DEPLOY-RUNBOOK.md` documenting the production execution sequence as an operator checklist (NOT an automated step — there is no CI gate coupling Supabase migration apply to Vercel deploy). It must specify, in order: (1) confirm staging dry-run + backfill verification passed (Plan 01 Task 3); (2) confirm PITR is enabled at the platform level (D-01) — this is the "backup taken"; (3) merge the app-code PR (Plans 02/03/04 changes) but do NOT yet let it deploy ahead of the migration; (4) at the deploy window, run the production migration via Supabase SQL Editor / CLI; (5) immediately trigger/confirm the Vercel deploy completes — within a short window so company_name is never dropped while stale app code is live (Pitfall 4 — both failure directions: `column company_name does not exist` if migration leads, `column company_id does not exist` if app code leads); (6) post-deploy D-13 manual login regression: log in as 2-3 real business accounts in production, confirm same paikat visible, profiili resolves (Pitfall 3), no `permission denied for function current_company_id` in Supabase logs (Pitfall 2); (7) rollback path: if production breaks despite clean staging, restore via Supabase PITR to the pre-migration timestamp (D-03) — no custom down-migration exists by design (D-04). Explicitly state production migration execution is NOT part of normal phase execution / CI.
  </action>
  <verify>
    <automated>test -f .planning/phases/59-multi-company-skeemamigraatio/59-DEPLOY-RUNBOOK.md && grep -q "PITR" .planning/phases/59-multi-company-skeemamigraatio/59-DEPLOY-RUNBOOK.md && grep -q "current_company_id" .planning/phases/59-multi-company-skeemamigraatio/59-DEPLOY-RUNBOOK.md && echo OK</automated>
    <human-check>Operator has reviewed the runbook and confirms the production migration is executed manually (staging-verified → merge → migration → Vercel deploy), never auto-pushed by CI (D-02/Pitfall 4)</human-check>
  </verify>
  <acceptance_criteria>
    - 59-DEPLOY-RUNBOOK.md exists and documents the ordered steps: staging-verified → confirm PITR → merge app-code → run production migration → confirm Vercel deploy → D-13 login regression → PITR rollback fallback
    - States production migration execution is NOT part of normal phase execution / CI
    - References both Pitfall 4 failure directions (`company_name does not exist`, `company_id does not exist`)
    - References the D-13 manual login regression and the Pitfall 2/3 checks (permission denied for function, profiili resolution)
  </acceptance_criteria>
  <done>An operator runbook documents the coupled, manually-ordered production deploy with PITR rollback fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous signup client → register Route Handler | Untrusted request input; JWT-verified at entry, company_name validated as today |
| business client → profiili Server Component (anon-key) | RLS-subject read — the only anon-key business read site; depends on companies SELECT policy |
| register Route Handler → companies/business_accounts via supabaseAdmin | Service-role writes; companies has no client INSERT policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-11 | Elevation of Privilege | register sets role | mitigate | Server assigns role: 'owner' via supabaseAdmin only; clients cannot set role (companies/business_accounts role column is REVOKE-protected from authenticated UPDATE per Plan 01 T-59-04); no client-supplied role accepted |
| T-59-12 | Information Disclosure | profiili anon-key companies read | mitigate | Read is gated by the companies SELECT policy USING (id = current_company_id()) (Plan 01); a misconfigured policy would deny (silent redirect), not over-expose — verified by D-13 login check |
| T-59-13 | Data integrity | orphaned companies row on signup failure | mitigate | Rollback deletes the new companies row if the business_accounts insert fails; CRITICAL log on rollback-delete failure (mirrors create-paikka orphan handling) |
| T-59-14 | Tampering | production deploy ordering | mitigate | 59-DEPLOY-RUNBOOK.md enforces manual staging-verified → migration → deploy ordering with PITR fallback (Pitfall 4) |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | No package installs — app-code + test + docs only |
</threat_model>

<verification>
- No `company_name` references remain in register/route.ts or profiili/page.tsx
- register inserts companies-then-account with role='owner' and rolls back on failure
- tests/api/register.test.ts exists and passes (`npx vitest run tests/api/register.test.ts`)
- `npm run build` type-checks clean (profiili change)
- 59-DEPLOY-RUNBOOK.md documents the coupled manual deploy ordering + PITR fallback
</verification>

<success_criteria>
- ACCESS-01: forward-going signups create their own owner company (matches the backfilled shape); previously-uncovered route now tested
- ACCESS-02: the anon-key profiili read works under the new companies RLS SELECT policy
- Pitfall 4 deploy-ordering captured as an explicit operator runbook (production execution stays manual, not CI)
</success_criteria>

<output>
Create `.planning/phases/59-multi-company-skeemamigraatio/59-04-SUMMARY.md` when done.
</output>