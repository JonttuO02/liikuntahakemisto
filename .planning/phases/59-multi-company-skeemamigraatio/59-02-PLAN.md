---
phase: 59-multi-company-skeemamigraatio
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - app/api/business/create-paikka/route.ts
  - app/api/business/reapply/route.ts
  - app/api/business/onboarding/submit/route.ts
  - tests/api/create-paikka.test.ts
  - tests/api/submit.test.ts
autonomous: true
requirements: [ACCESS-01, ACCESS-02]

must_haves:
  truths:
    - "create-paikka writes the company name to companies.name (via company_id lookup), not to a dropped business_accounts.company_name column"
    - "create-paikka, reapply, and onboarding/submit each send the correct companyName in their admin notification email by reading companies(name) through the company_id FK"
    - "tests/api/create-paikka.test.ts and tests/api/submit.test.ts mock the companies(name) join shape, not the dropped flat company_name column"
    - "npx vitest run tests/api/create-paikka.test.ts tests/api/submit.test.ts passes"
  artifacts:
    - path: "app/api/business/create-paikka/route.ts"
      provides: "company name UPDATE targets companies.name via company_id; email SELECT reads companies(name)"
      contains: "companies"
    - path: "app/api/business/reapply/route.ts"
      provides: "admin-notification email reads companies(name) via company_id"
      contains: "companies(name)"
    - path: "app/api/business/onboarding/submit/route.ts"
      provides: "admin-notification email reads companies(name) via company_id"
      contains: "companies(name)"
  key_links:
    - from: "app/api/business/create-paikka/route.ts"
      to: "companies table"
      via: "SELECT company_id from business_accounts then UPDATE companies SET name WHERE id = company_id; email reads .select('companies(name)')"
      pattern: "companies"
    - from: "app/api/business/reapply/route.ts"
      to: "companies table"
      via: ".select('companies(name)') embedded read, companyName: biz.companies?.name"
      pattern: "companies\\?\\.name|companies\\(name\\)"
---

<objective>
Update the three business-side Route Handlers that read/write the now-dropped `business_accounts.company_name` so they join through the new `company_id` FK to `companies.name`, and update the two existing Vitest mocks that currently mock the flat `company_name` column so they reflect the post-migration `companies(name)` join shape.

Purpose: After Plan 01 drops `business_accounts.company_name`, these routes would 500 or send broken company names in admin emails. The SQL migration and these app-code edits MUST land in the same release (Pitfall 4 deploy-ordering).
Output: Three corrected Route Handlers and two corrected test files, with the touched-file test suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/59-multi-company-skeemamigraatio/59-RESEARCH.md
@.planning/phases/59-multi-company-skeemamigraatio/59-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite company_name UPDATE + email SELECT in create-paikka route to use companies(name)</name>
  <files>app/api/business/create-paikka/route.ts</files>
  <read_first>
    - app/api/business/create-paikka/route.ts (current state: company_name UPDATE ~lines 120-130, SELECT-for-email ~lines 144-150, email send ~lines 163-165)
    - .planning/phases/59-multi-company-skeemamigraatio/59-PATTERNS.md (create-paikka Pattern Assignment — exact two-step UPDATE shape and non-critical-log convention)
    - app/api/business/reapply/route.ts (sibling email-SELECT shape for consistency)
  </read_first>
  <action>
    Two changes, both preserving the existing "non-critical, log don't fail" convention (prefix `[create-paikka]`, suffix `(non-critical):`):
    (1) The UPDATE that today does `.from('business_accounts').update({ company_name: yritysNimi }).eq('user_id', user.id)` must become a two-step write: first SELECT `company_id` from `business_accounts` where `user_id = user.id`, then `.from('companies').update({ name: yritysNimi }).eq('id', company_id)`. The company_name column no longer exists on business_accounts (D-05), so writing to it would error. Keep the same non-critical error logging on failure of either step. Note these are supabaseAdmin (service-role) writes per CLAUDE.md — no RLS concern.
    (2) The SELECT-for-email that today does `.from('business_accounts').select('company_name').eq('user_id', user.id).single()` must become `.select('companies(name)')`, and the email call's `companyName:` argument must read `biz.companies?.name` instead of `biz.company_name`. (Supabase embedded-resource syntax reads the related companies row through the company_id FK.)
    Do not change the JWT-verify block, the liikuntapaikat/business_paikka_links insert+rollback logic, or any unrelated code.
  </action>
  <verify>
    <automated>npx vitest run tests/api/create-paikka.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - No reference to `company_name` remains in app/api/business/create-paikka/route.ts
    - The company-name write targets `companies` (`.from('companies').update({ name: ... })`) keyed by a company_id looked up from business_accounts
    - The email SELECT uses `.select('companies(name)')` and the email arg reads `biz.companies?.name`
    - Both new steps preserve the `[create-paikka] ... (non-critical):` log convention
    - `npx vitest run tests/api/create-paikka.test.ts` passes
  </acceptance_criteria>
  <done>create-paikka writes and reads company name through the companies table; its test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite admin-notification email SELECT in reapply and onboarding/submit routes</name>
  <files>app/api/business/reapply/route.ts, app/api/business/onboarding/submit/route.ts</files>
  <read_first>
    - app/api/business/reapply/route.ts (current SELECT company_name → sendAdminNotificationEmail ~lines 88-110)
    - app/api/business/onboarding/submit/route.ts (current SELECT company_name → sendAdminNotificationEmail ~lines 123-146)
    - .planning/phases/59-multi-company-skeemamigraatio/59-PATTERNS.md (reapply/submit shared Pattern Assignment — the change is identical in both)
  </read_first>
  <action>
    In BOTH files, the admin-notification block today does `.from('business_accounts').select('company_name').eq('user_id', user.id).single()` then passes `companyName: biz.company_name` into `sendAdminNotificationEmail({ ... })`. Change the SELECT to `.select('companies(name)')` and change the email argument to `companyName: biz.companies?.name`. The surrounding try/catch with the `(non-critical)` console.error wrapper is unchanged — copy it verbatim. The change is mechanically identical in both files; the only difference is the existing `[reapply]` vs `[submit/onboarding]` log prefix, which stays as-is.
    Do not change the JWT-verify block, the link/draft update logic, or the liikuntapaikat nimi SELECT.
  </action>
  <verify>
    <automated>npx vitest run tests/api/submit.test.ts && grep -L "company_name" app/api/business/reapply/route.ts app/api/business/onboarding/submit/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - Neither app/api/business/reapply/route.ts nor app/api/business/onboarding/submit/route.ts references `company_name`
    - Both use `.select('companies(name)')` and pass `companyName: biz.companies?.name`
    - The non-critical try/catch email wrapper is unchanged in both
    - `npx vitest run tests/api/submit.test.ts` passes
  </acceptance_criteria>
  <done>Both routes read the company name via the companies FK join for their admin emails; submit test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Update create-paikka.test.ts and submit.test.ts mocks to the companies(name) join shape</name>
  <files>tests/api/create-paikka.test.ts, tests/api/submit.test.ts</files>
  <read_first>
    - tests/api/create-paikka.test.ts (current flat company_name mock ~lines 38-41, 143)
    - tests/api/submit.test.ts (current flat company_name mock ~line 186)
    - app/api/business/create-paikka/route.ts (the route just changed in Task 1 — mock must match its new SELECT)
    - app/api/business/onboarding/submit/route.ts (route changed in Task 2)
  </read_first>
  <action>
    The existing mocks return a flat `{ company_name: 'Test Oy' }` / `{ company_name: 'Testi Oy' }` from the mocked business_accounts SELECT. After Tasks 1-2, the routes read `.select('companies(name)')`, so the mocked SELECT result must return the nested shape `{ companies: { name: 'Test Oy' } }` (preserving the existing string values so the email-content assertions still pass for the right reason, not by mocking a column that no longer exists). Update create-paikka.test.ts (~lines 41 and 143) and submit.test.ts (~line 186). If create-paikka.test.ts also mocks the company-name UPDATE path (Task 1's two-step write), update that mock to expect a `companies` update rather than a business_accounts company_name update. Run the touched suite to confirm assertions still pass against the new join shape.
  </action>
  <verify>
    <automated>npx vitest run tests/api/create-paikka.test.ts tests/api/submit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/api/create-paikka.test.ts and tests/api/submit.test.ts mock the nested `{ companies: { name: ... } }` shape, not a flat `company_name` field
    - The preserved company-name string values keep the email-content assertions meaningful
    - `npx vitest run tests/api/create-paikka.test.ts tests/api/submit.test.ts` passes
  </acceptance_criteria>
  <done>Both existing tests mock the post-migration join shape and pass for the right reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| business client → create-paikka/reapply/submit Route Handlers | Untrusted request input crosses here; JWT-verified at route entry (unchanged) |
| Route Handler → companies/business_accounts via supabaseAdmin | Service-role writes bypass RLS — must remain server-only (CLAUDE.md) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-07 | Tampering | company name write path (create-paikka) | accept | Write remains supabaseAdmin server-only keyed by the caller's own company_id (looked up from auth.uid()'s business_accounts row); no client-side write path introduced, consistent with CLAUDE.md anon-read-only |
| T-59-08 | Information Disclosure | admin-notification emails | accept | Email content (companyName, venueName) is the same data sent before this phase; the only change is the read path (FK join vs flat column), no new data exposed |
| T-59-SC | Tampering | npm/pip/cargo installs | accept | No package installs — app-code edits only, zero new dependencies |
</threat_model>

<verification>
- No `company_name` references remain in the three modified routes
- The two modified test files mock the nested companies(name) shape
- `npx vitest run tests/api/create-paikka.test.ts tests/api/submit.test.ts` passes
</verification>

<success_criteria>
- ACCESS-01/02: business-side reads/writes of the company name go through companies.name via company_id (the dropped column is fully removed from these routes)
- Existing regression tests updated to the new join shape and green
</success_criteria>

<output>
Create `.planning/phases/59-multi-company-skeemamigraatio/59-02-SUMMARY.md` when done.
</output>
