---
phase: 07-map-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - app/components/Etusivu.tsx
autonomous: true
requirements:
  - MAP-04

must_haves:
  truths:
    - "A re-center button is visible on the fullscreen map"
    - "Tapping the button moves the map view to the user's GPS position"
    - "The button is not present on the 3D preview map"
    - "The button is always rendered regardless of GPS availability"
    - "Tapping the button when GPS is unavailable does nothing (silent no-op)"
  artifacts:
    - path: "app/components/Etusivu.tsx"
      provides: "Re-center button rendered inside fullscreen map wrapper"
      contains: "RecenterButton"
  key_links:
    - from: "RecenterButton"
      to: "useMap() hook"
      via: "map.panTo(coords)"
      pattern: "map\\.panTo"
---

<objective>
Add a floating re-center button to the fullscreen map in Etusivu.tsx. The button uses `useMap()` to call `map.panTo(coords)` imperatively, giving users a manual way to return to their GPS position after panning away.

Purpose: Satisfies MAP-04 (user can return to their location via button). The button coexists with the existing `MapPanController` (auto-pan on GPS resolution) — they serve different user actions.
Output: `RecenterButton` inner component added to Etusivu.tsx; button rendered at bottom-right of fullscreen map.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-map-infrastructure/07-CONTEXT.md
@.planning/phases/07-map-infrastructure/07-UI-SPEC.md
@.planning/phases/07-map-infrastructure/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RecenterButton inner component and render it in the fullscreen map</name>
  <files>app/components/Etusivu.tsx</files>

  <read_first>
    - app/components/Etusivu.tsx — read the full file as it exists after Plan 01 changes; specifically note: the existing MapPanController function (~line 49-55) as the pattern for useMap-based inner components; the fullscreen map wrapper motion.div (~line 299-414) and the X close button (~line 354-359) for positioning sibling elements; the Lucide icon import on line 5 which needs `Locate` added
    - .planning/phases/07-map-infrastructure/07-CONTEXT.md — decisions D-08 through D-13 (scope, position, icon, visibility, behavior, MapPanController coexistence)
    - .planning/phases/07-map-infrastructure/07-UI-SPEC.md — Re-center Button component contract (glass-btn class, bottom-16 right-4, aria-label, whileTap)
  </read_first>

  <action>
    In app/components/Etusivu.tsx:

    1. Add `Locate` to the Lucide import on line 5. Current: `import { X, MapPin, Moon, Sun } from 'lucide-react'`. Change to: `import { X, MapPin, Moon, Sun, Locate } from 'lucide-react'`.

    2. After the `MapPanController` function definition (after the closing brace of that function, before `getTimeBasedFallback`), add a new inner component:
       ```
       function RecenterButton({ coords }: { coords: { lat: number; lng: number } | null }) {
         const map = useMap()
         return (
           <motion.button
             whileTap={{ scale: 0.95 }}
             onClick={() => { if (map && coords) map.panTo(coords) }}
             className="absolute bottom-16 right-4 z-10 w-10 h-10 glass-btn rounded-full flex items-center justify-center text-[rgba(17,17,17,0.6)] hover:text-[#111111] [transition:color_150ms_var(--ease-out)]"
             aria-label="Palaa omalle sijainnille"
           >
             <Locate className="w-4 h-4" />
           </motion.button>
         )
       }
       ```

    3. In the fullscreen map section, inside the inner motion.div (the one with the 3D transform, which contains the `<Map>`, the X close button, the day/night toggle, and the filter pills), add `<RecenterButton coords={coords} />` as a sibling to the X close button. Place it immediately after the closing `</Map>` tag and before the X close button `<button>`:
       ```
       <RecenterButton coords={coords} />

       {/* X close button */}
       <button ...>
       ```

    Important: RecenterButton must appear inside the inner motion.div that wraps the Map (the one with transformOrigin: 'bottom center'), NOT in the outer motion.div or outside the fullscreen AnimatePresence. It must NOT be placed inside the preview map section.
  </action>

  <verify>
    <automated>npm run build --prefix C:/ClaudeCodeTestit/liikuntahakemisto 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Etusivu.tsx imports `Locate` from lucide-react
    - Etusivu.tsx contains a `RecenterButton` function component definition
    - RecenterButton contains `map.panTo(coords)` inside an onClick handler
    - RecenterButton contains `aria-label="Palaa omalle sijainnille"`
    - RecenterButton contains `className` with `glass-btn`, `rounded-full`, `w-10`, `h-10`, `bottom-16`, `right-4`
    - RecenterButton contains `whileTap={{ scale: 0.95 }}`
    - `<RecenterButton coords={coords} />` appears only once in the JSX (inside the fullscreen map wrapper)
    - `<RecenterButton` does NOT appear inside the preview map AnimatePresence block
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>RecenterButton inner component added to Etusivu.tsx using useMap() + map.panTo(coords); rendered at bottom-16 right-4 inside the fullscreen map inner motion.div; Locate icon imported; aria-label set to "Palaa omalle sijainnille"; whileTap scale animation applied; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap → map.panTo | Button click calls map.panTo only if coords non-null; coords sourced from browser Geolocation API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Spoofing | RecenterButton / map.panTo | accept | Coords come from the browser's Geolocation API, not user-supplied input; panTo only moves the viewport, no data mutation |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages — motion and lucide-react already installed |
</threat_model>

<verification>
After task completes:
1. `npm run build` exits 0
2. `grep -n "RecenterButton" app/components/Etusivu.tsx` — shows the function definition and a single render site
3. `grep -n "Locate" app/components/Etusivu.tsx` — shows the import and the usage inside RecenterButton
4. `grep -n "bottom-16" app/components/Etusivu.tsx` — shows the positioning class on RecenterButton
5. Manual: open the app, expand the fullscreen map — a circular glass button with a Locate icon appears at bottom-right, above the filter pills. Tapping it with GPS active pans the map to the user's position.
</verification>

<success_criteria>
- RecenterButton component defined and renders inside the fullscreen map only
- Button positioned at `bottom-16 right-4` with `glass-btn rounded-full w-10 h-10`
- Tapping button calls `map.panTo(coords)` when coords is non-null
- Tapping button is a silent no-op when coords is null
- `aria-label="Palaa omalle sijainnille"` present
- `whileTap={{ scale: 0.95 }}` applied
- Button absent from the 3D preview map
- `npm run build` exits 0
- MAP-04 satisfied: user can return to their location using the re-center button
</success_criteria>

<output>
Create `.planning/phases/07-map-infrastructure/07-02-SUMMARY.md` when done
</output>
