---
phase: 06-ui-polish-and-data-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/paikat/[id]/page.tsx
autonomous: true
requirements:
  - UI-07
must_haves:
  truths:
    - "Profile page shows booking URL as a Row entry (icon + label + text link), not as a full-width button"
    - "Row appears between Hinta and Kuvaus rows"
  artifacts:
    - path: "app/paikat/[id]/page.tsx"
      provides: "Varaussivu Row replacing the full-width Varaa aika button"
      contains: "Varaussivu"
  key_links:
    - from: "app/paikat/[id]/page.tsx Varaussivu Row"
      to: "paikka.varauslinkki (external booking URL)"
      via: "anchor tag with target=_blank rel=noopener noreferrer"
      pattern: "varauslinkki"
---

<objective>
Replace the full-width "Varaa aika" button block at the bottom of the profile page content card with a `Row` entry (icon + label "Varaussivu" + plain styled anchor) inserted between the Hinta row and the Kuvaus row.

Purpose: Satisfies the profile-page half of UI-07 (D-16). The other half — removing the "Varaa aika" button from PaikkaKortti list cards — is in Plan 05. Phase 6 keeps the v1.0 "no booking system" stance and links out to the provider's own site as plain text.

Output: One file modified, one new icon import (`ExternalLink`), one block replaced, one block inserted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md
@.planning/phases/06-ui-polish-and-data-foundation/06-RESEARCH.md
@.planning/phases/06-ui-polish-and-data-foundation/06-PATTERNS.md
@.planning/phases/06-ui-polish-and-data-foundation/06-UI-SPEC.md
@CLAUDE.md
@app/paikat/[id]/page.tsx

<interfaces>
Within app/paikat/[id]/page.tsx (self-analog):
- `Row` function (defined at file bottom, lines ~135–147) signature:
    function Row({ icon, label, children }: { icon: React.ReactNode; label: string; children: React.ReactNode })
  Not exported — used internally; reuse as-is.
- Current row order inside the `flex flex-col gap-5` content card:
    1. Sijainti (MapPin)
    2. Aukioloajat (Clock)
    3. Puhelin (Phone)
    4. Hinta (CircleDollarSign)
    5. Kuvaus (Info)
- Existing full-width "Varaa aika →" anchor block: located in its own `<div className="px-6 sm:px-8 pb-8">` after the content card (per PATTERNS.md lines 470–489).
- `buttonVariants` is imported from `components/ui/button.tsx`. After this change, if `buttonVariants` has no remaining usages in the file, remove the import to keep the file lint-clean.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace full-width Varaa aika button with Varaussivu Row in profile page</name>
  <files>app/paikat/[id]/page.tsx</files>
  <read_first>
    - app/paikat/[id]/page.tsx (full file — current Varaa aika block at lines ~114–129, current Row component at lines ~135–147, current Hinta/Kuvaus Rows at lines ~97–111)
    - .planning/phases/06-ui-polish-and-data-foundation/06-PATTERNS.md (section "app/paikat/[id]/page.tsx — EDIT" with exact replacement snippets)
    - .planning/phases/06-ui-polish-and-data-foundation/06-UI-SPEC.md (UI-07 profile page block)
    - .planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md (D-16 — Row label "Varaussivu", ExternalLink icon)
    - CLAUDE.md (font weight 400/700 only — anchor uses `font-medium` (500) currently in PATTERNS; upgrade to `font-bold` per UI-SPEC if necessary)
  </read_first>
  <action>
    1) Add `ExternalLink` to the existing `lucide-react` named import in `app/paikat/[id]/page.tsx` (the line currently importing `Phone, MapPin, CircleDollarSign, Info, ChevronLeft, Clock`). Keep alphabetical or trailing-add order consistent with existing style.

    2) Delete the entire existing `{paikka.varauslinkki && ( ... )}` block that wraps the full-width `<a className={buttonVariants({...})}>Varaa aika →</a>` (the standalone block in its own `<div className="px-6 sm:px-8 pb-8">` after the content card per PATTERNS.md lines 470–489).

    3) Insert a NEW `{paikka.varauslinkki && ( ... )}` block INSIDE the `flex flex-col gap-5` content card, AFTER the Hinta Row (CircleDollarSign) and BEFORE the Kuvaus Row (Info). The block renders a `Row` with:
       - `icon`: `<ExternalLink className="w-5 h-5 text-[rgba(17,17,17,0.5)]" />`
       - `label`: the string literal "Varaussivu" (label caps will render via the Row component's existing styling)
       - `children`: an `<a>` with `href={paikka.varauslinkki}`, `target="_blank"`, `rel="noopener noreferrer"`, and the visible link text equal to `paikka.varauslinkki`. Anchor class: `text-sm text-[#111111] font-bold underline underline-offset-2 break-all hover:text-[rgba(17,17,17,0.6)] [transition:color_150ms_var(--ease-out)]` (font-bold per UI-SPEC UI-07 profile block; `font-medium` from PATTERNS is incorrect per the typography 400/700-only rule).

    4) If `buttonVariants` is no longer referenced anywhere in the file after the removal, remove the `import { buttonVariants } from '@/components/ui/button'` line. If it IS still referenced (e.g., other CTA), leave the import.

    5) Do NOT alter the Sijainti, Aukioloajat, Puhelin, Hinta, or Kuvaus Row blocks. Do NOT alter the page-level data fetching, params handling, hero section, or the Row helper at the file bottom.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/paikat/[id]/page.tsx','utf8'); if(s.includes('Varaa aika')) throw new Error('Varaa aika string must be removed'); if(!s.includes('ExternalLink')) throw new Error('ExternalLink icon import missing'); if(!s.includes('Varaussivu')) throw new Error('Varaussivu Row label missing'); if(!/target=\"_blank\"/.test(s)) throw new Error('target=_blank missing on booking anchor'); if(!/rel=\"noopener noreferrer\"/.test(s)) throw new Error('rel=noopener noreferrer missing'); if(s.includes('font-medium')) throw new Error('font-medium forbidden (use font-bold)'); console.log('OK')"</automated>
  </verify>
  <done>app/paikat/[id]/page.tsx no longer contains the string "Varaa aika"; contains "Varaussivu" inside a Row; the `<a>` has `target="_blank" rel="noopener noreferrer"`; `npx tsc --noEmit` exits 0.</done>
  <acceptance_criteria>
    - Source assertion: grep `Varaa aika` against app/paikat/[id]/page.tsx returns 0 matches
    - Source assertion: grep `Varaussivu` against app/paikat/[id]/page.tsx returns at least 1 match
    - Source assertion: grep `ExternalLink` against app/paikat/[id]/page.tsx returns at least 1 match (icon import + JSX use)
    - Source assertion: the booking anchor includes both `target="_blank"` and `rel="noopener noreferrer"` attributes
    - Source assertion: no `font-medium` class remains in app/paikat/[id]/page.tsx (typography rule)
    - Source order assertion: in the rendered file, the substring `label="Varaussivu"` appears AFTER the line containing `CircleDollarSign` (Hinta row icon) and BEFORE the line containing `<Info` (Kuvaus row icon)
    - Build assertion: `npx tsc --noEmit` exits 0
    - Behavior assertion (manual smoke): on the profile page for a venue with a `varauslinkki`, the URL renders inline as an underlined text link inside a Row (not as a full-width black button)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → External site | `varauslinkki` is an external URL stored in the DB and rendered as an outbound anchor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering (tabnabbing) | External booking link in profile page | mitigate | Anchor includes `rel="noopener noreferrer"` and `target="_blank"` — enforced by acceptance criterion. |
| T-06-06 | Tampering (XSS via varauslinkki) | URL string rendered as text and href | mitigate | React escapes the text node; href is opaque to the renderer. Anon DB role is read-only (RLS), so URL cannot be tampered post-write without service role. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- grep verifies "Varaa aika" is removed and "Varaussivu" is present
- Manual smoke: open a profile page with a `varauslinkki` and confirm the inline Row layout
</verification>

<success_criteria>
The profile page renders the booking URL as a styled inline anchor inside a `Row` with the label "Varaussivu" between Hinta and Kuvaus rows. The full-width "Varaa aika →" button is gone.
</success_criteria>

<output>
Create `.planning/phases/06-ui-polish-and-data-foundation/06-03-SUMMARY.md` when done.
</output>
