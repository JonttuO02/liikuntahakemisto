---
phase: 06-ui-polish-and-data-foundation
plan: 06
type: execute
wave: 2
depends_on:
  - 06-02
  - 06-04
files_modified:
  - app/components/LiikuntapaikatLista.tsx
autonomous: true
requirements:
  - UI-08
  - DATA-07
  - LEGAL-01
must_haves:
  truths:
    - "Sport filter is a native single-select <select> populated from LAJIT_FILTTERI (UI-08)"
    - "City filter is a native single-select <select> populated by deriveKaupungit(paikat) (DATA-07)"
    - "Filtering by a specific city excludes venues whose kaupunki is null/empty (D-21, Pitfall 6)"
    - "Hero subtitle updates to reflect aktiivKaupunki and suodatettu.length (D-22)"
    - "Tyhjennä haku resets ALL filters including aktiivKaupunki (D-23)"
    - "Footer renders a 'Tietosuoja' Link to /tietosuoja (LEGAL-01, D-05)"
    - "AnimatePresence grid key includes aktiivKaupunki so the grid re-renders when city changes"
  artifacts:
    - path: "app/components/LiikuntapaikatLista.tsx"
      provides: "City + sport dropdowns, city filter logic, privacy footer link"
      contains: "aktiivKaupunki"
  key_links:
    - from: "LiikuntapaikatLista.tsx"
      to: "lib/cityFilter.ts (deriveKaupungit)"
      via: "named import"
      pattern: "deriveKaupungit"
    - from: "LiikuntapaikatLista.tsx footer"
      to: "/tietosuoja route"
      via: "next/link Link href"
      pattern: "/tietosuoja"
---

<objective>
Apply three coordinated edits to `app/components/LiikuntapaikatLista.tsx`:
  1. Replace the horizontal pill scroll for sport filter with a native select populated from LAJIT_FILTTERI (UI-08, D-18).
  2. Add a city select to the same Row 1 (D-19 layout `[City] [Sport] [Etäisyydet]`), wire aktiivKaupunki state, derive options via deriveKaupungit(paikat), extend the suodatettu filter with matchesKaupunki, update the hero subtitle (D-22), update the AnimatePresence grid key, and extend Tyhjennä haku to reset the city filter (D-23) — closes DATA-07.
  3. Add a Tietosuoja footer Link to /tietosuoja below the venue grid (LEGAL-01 nav wiring, D-05).

Purpose: Closes UI-08 and DATA-07 entirely, and wires the GDPR page created in Plan 02 into the listing UI so users can discover it. Depends on Plan 02 (target route must exist) and Plan 04 (deriveKaupungit exists with tests).

Output: One file modified. Two new imports: deriveKaupungit from `@/lib/cityFilter`, and Link from `next/link` if not already imported.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md
@.planning/phases/06-ui-polish-and-data-foundation/06-RESEARCH.md
@.planning/phases/06-ui-polish-and-data-foundation/06-PATTERNS.md
@.planning/phases/06-ui-polish-and-data-foundation/06-UI-SPEC.md
@CLAUDE.md
@app/components/LiikuntapaikatLista.tsx
@lib/cityFilter.ts
@lib/lajit.ts

<interfaces>
From lib/cityFilter.ts (provided by Plan 04):
  - deriveKaupungit(paikat: Array<Pick<Liikuntapaikka,'kaupunki'>>): string[]
  - Returns: ['Kaikki', ...sortedUniqueNonEmptyCities]

From lib/lajit.ts (existing):
  - LAJIT_FILTTERI: string[]  — first element is 'Kaikki'

Existing filter state shape in LiikuntapaikatLista (per PATTERNS.md):
  - haku, aktiivinen, aktiivHinta, aukinyt
  - useMemo `suodatettu` already composes matchesLaji + matchesHaku + matchesHinta + matchesAuki
  - AnimatePresence grid key: `grid-${aktiivinen}-${aktiivHinta ?? 'all'}-${aukinyt}`
  - Tyhjennä haku handler in empty-state resets the four filters
  - Hero subtitle currently reads `Tampere · {paikat.length} paikkaa` (line ~77)
  - Sport pill scroll currently at lines ~103–135 (entire `<div className="flex gap-2 overflow-x-auto ...">`)
  - GPS Etäisyydet motion.button immediately follows the sport pill scroll inside Row 1

New state added by this plan:
  - aktiivKaupunki: string ("Kaikki" default)

Shared dropdown class (city + sport, exact match per UI-SPEC):
  `h-10 rounded-full border border-[rgba(0,0,0,0.12)] bg-white px-4 text-sm font-bold text-[#111111] focus:outline-none focus:ring-1 focus:ring-[#111111] [transition:border-color_150ms_var(--ease-out)] cursor-pointer`

Spacing/Typography tightening from UI-SPEC (apply globally to edits):
  - 400/700 weights only — replace any pre-existing `font-semibold` in lines touched
  - h-10 dropdowns are acceptable (40px; UI-SPEC Accessibility Contract permits)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace sport pill scroll with native select (UI-08)</name>
  <files>app/components/LiikuntapaikatLista.tsx</files>
  <read_first>
    - app/components/LiikuntapaikatLista.tsx (full file — Row 1 pill scroll at lines ~103–135; existing aktiivinen state at lines ~31–34; existing animation imports)
    - lib/lajit.ts (LAJIT_FILTTERI array shape and first element)
    - .planning/phases/06-ui-polish-and-data-foundation/06-PATTERNS.md (LiikuntapaikatLista section — exact JSX replacement for sport dropdown)
    - .planning/phases/06-ui-polish-and-data-foundation/06-UI-SPEC.md (UI-08 visual contract — exact className and aria-label)
    - .planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md (D-18, D-19)
    - CLAUDE.md (filter button animation rule: only whileTap on Etäisyydet — do NOT add motion to select elements)
  </read_first>
  <action>
    Make three coordinated edits inside Row 1 (the wrapper `<div>` that contains the sport pills and the Etäisyydet button):

    1) **Change the Row 1 wrapper className**: from `flex gap-2 overflow-x-auto pb-1 sm:pb-0 sm:flex-wrap ...` (or whatever variant is in the file) to `flex flex-wrap items-center gap-2`. This removes horizontal scroll (`overflow-x-auto`) so the city + sport + Etäisyydet elements wrap on narrow screens.

    2) **Replace the LAJIT_FILTTERI.map(...) motion-button loop** with a single native `<select>` element. The sport select must have:
      - `value={aktiivinen}`
      - `onChange={e => setAktiivinen(e.target.value)}`
      - `aria-label="Suodata lajin mukaan"`
      - className: the shared dropdown class enumerated in interfaces above
      - Body: `{LAJIT_FILTTERI.map(l => (<option key={l} value={l}>{l}</option>))}`
      Do NOT wrap the select in motion.button / motion.div / whileTap.

    3) **Fix the Etäisyydet motion.button typography**: locate the existing Etäisyydet motion.button (immediately after the sport pills in Row 1) and change any `font-semibold` in its className to `font-bold`. This is required by CLAUDE.md's 400/700-only weight rule. Do NOT change any other attribute of the Etäisyydet button.

    Do NOT touch Row 2 (price pills + count) or Row 3 (Auki nyt toggle). Do NOT touch the useGPS hook or requestLocation reference.

    Note: After Task 1, Row 1 reads: `[Sport dropdown] [Etäisyydet button]`. After Task 2 it reads: `[City dropdown] [Sport dropdown] [Etäisyydet button]`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/components/LiikuntapaikatLista.tsx','utf8'); if(!/aria-label=\"Suodata lajin mukaan\"/.test(s)) throw new Error('sport select aria-label missing'); if(!/value=\{aktiivinen\}/.test(s)) throw new Error('sport select value binding missing'); if(/LAJIT_FILTTERI\.map\([^)]*=>[^)]*motion\.button/s.test(s)) throw new Error('sport pill scroll still present'); if(!/flex flex-wrap items-center gap-2/.test(s)) throw new Error('Row 1 wrapper class not updated (overflow-x-auto not removed)'); const lines=s.split('\n'); const etIdx=lines.findIndex(l=>l.includes('requestLocation')); const nearby=lines.slice(Math.max(0,etIdx-5),etIdx+6).join('\n'); if(/font-semibold/.test(nearby)) throw new Error('font-semibold still present in Etäisyydet button area'); console.log('OK')"</automated>
  </verify>
  <done>The LAJIT_FILTTERI.map(...) motion.button pill scroll is replaced with a native select bound to aktiivinen/setAktiivinen. `npx tsc --noEmit` exits 0.</done>
  <acceptance_criteria>
    - Source assertion: grep `aria-label="Suodata lajin mukaan"` returns 1 match
    - Source assertion: file no longer contains a motion.button loop that maps over LAJIT_FILTTERI
    - Source assertion: file contains `LAJIT_FILTTERI.map(` followed (within ~5 lines) by `<option key=`
    - Source assertion: Row 1 wrapper no longer contains `overflow-x-auto` (grep for it in the touched div)
    - Source assertion: the Etäisyydet motion.button block no longer contains `font-semibold` (check: grep for `font-semibold` within the 10 lines surrounding the `requestLocation` call — must be 0 matches)
    - Build assertion: `npx tsc --noEmit` exits 0
    - Behavior assertion (manual smoke): Row 1 shows a single dropdown for sport plus the Etäisyydet button; selecting a sport filters the list as before
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add city filter, hero subtitle update, reset wiring (DATA-07)</name>
  <files>app/components/LiikuntapaikatLista.tsx</files>
  <read_first>
    - app/components/LiikuntapaikatLista.tsx (after Task 1 — Row 1 now has sport select + Etäisyydet button; suodatettu useMemo composition; hero subtitle line ~77; AnimatePresence grid key line ~191; Tyhjennä haku onClick handler lines ~214–215)
    - lib/cityFilter.ts (deriveKaupungit signature — Plan 04 output)
    - .planning/phases/06-ui-polish-and-data-foundation/06-PATTERNS.md (LiikuntapaikatLista section — exact city select snippet, useMemo for kaupungit, grid key update, reset handler update)
    - .planning/phases/06-ui-polish-and-data-foundation/06-UI-SPEC.md (DATA-07 visual contract — className shared with sport select; aria-label)
    - .planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md (D-19, D-20, D-21, D-22, D-23)
  </read_first>
  <action>
    1) Add the import `import { deriveKaupungit } from '@/lib/cityFilter'` next to the existing lib imports.

    2) Add state: `const [aktiivKaupunki, setAktiivKaupunki] = useState('Kaikki')` immediately after the existing useState declarations.

    3) Add memoized city list: `const kaupungit = useMemo(() => deriveKaupungit(paikat), [paikat])`. Place near the top of the component body alongside other useMemo derivations.

    4) Extend the suodatettu useMemo:
       - Inside the filter callback add `const matchesKaupunki = aktiivKaupunki === 'Kaikki' || p.kaupunki === aktiivKaupunki`.
       - Append `&& matchesKaupunki` to the return expression.
       - Add aktiivKaupunki to the useMemo dependency array.

    5) Update the AnimatePresence grid key to include city. Replace the existing key string with the literal template-string form `grid-${aktiivKaupunki}-${aktiivinen}-${aktiivHinta ?? 'all'}-${aukinyt}`. This forces a remount when city changes — keeping AnimatePresence keys stable per CLAUDE.md animation rules.

    6) Update the hero subtitle paragraph (currently `Tampere &nbsp;·&nbsp; {paikat.length} paikkaa`). Replace with conditional text:
        - When `aktiivKaupunki === 'Kaikki'`: render the string `Kaikki kaupungit` followed by `&nbsp;·&nbsp;` followed by `{suodatettu.length} paikkaa`.
        - When `aktiivKaupunki !== 'Kaikki'`: render `{aktiivKaupunki}` followed by `&nbsp;·&nbsp;` followed by `{suodatettu.length} paikkaa`.
       Implement using a ternary inside the existing `<p>` (do not duplicate the `<p>` element). Note the change: count is `suodatettu.length` (filtered count) not `paikat.length` (total).

    7) Insert the city select in Row 1 BEFORE the sport select (per D-19 ordering `[City] [Sport] [Etäisyydet]`). Wrap in `{kaupungit.length > 1 && (...)}` so the dropdown is hidden when only "Kaikki" is available (single-city data — current state). The city select uses:
       - `value={aktiivKaupunki}`
       - `onChange={e => setAktiivKaupunki(e.target.value)}`
       - `aria-label="Suodata kaupungin mukaan"`
       - the same className as the sport select (the shared dropdown class)
       - Body: `{kaupungit.map(k => (<option key={k} value={k}>{k}</option>))}`

    8) Extend the empty-state Tyhjennä haku onClick handler to also reset the city: append `setAktiivKaupunki('Kaikki')` to the chained reset call.

    9) Do NOT change the Etäisyydet button, Row 2 (price pills), Row 3 (Auki nyt toggle), or the venue grid / card rendering.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/components/LiikuntapaikatLista.tsx','utf8'); const checks={'deriveKaupungit import':/deriveKaupungit/.test(s),'aktiivKaupunki state':/aktiivKaupunki/.test(s),'city aria-label':/aria-label=\"Suodata kaupungin mukaan\"/.test(s),'matchesKaupunki filter':/matchesKaupunki/.test(s),'grid key has kaupunki':/grid-\$\{aktiivKaupunki\}/.test(s),'subtitle Kaikki kaupungit':s.includes('Kaikki kaupungit'),'subtitle uses suodatettu length':/\$\{suodatettu\.length\} paikkaa/.test(s),'reset includes setAktiivKaupunki':/setAktiivKaupunki\(['\"]Kaikki['\"]\)/.test(s)}; for(const k of Object.keys(checks)){ if(!checks[k]) throw new Error('FAIL: '+k) } console.log('OK')"</automated>
  </verify>
  <done>aktiivKaupunki state exists, kaupungit useMemo calls deriveKaupungit(paikat), the suodatettu filter has matchesKaupunki, the grid key includes aktiivKaupunki, the hero subtitle is dynamic per D-22, the Tyhjennä haku handler resets the city filter, and the city select renders in Row 1 only when kaupungit.length > 1. `npx tsc --noEmit` exits 0; `npx vitest run` exits 0.</done>
  <acceptance_criteria>
    - Source assertion: file contains `deriveKaupungit` (import + call)
    - Source assertion: file contains `aktiivKaupunki` (state declaration + filter usage)
    - Source assertion: grep `aria-label="Suodata kaupungin mukaan"` returns 1 match
    - Source assertion: file contains `matchesKaupunki`
    - Source assertion: file contains a template literal starting `grid-${aktiivKaupunki}-`
    - Source assertion: file contains the literal string `Kaikki kaupungit`
    - Source assertion: file contains `${suodatettu.length} paikkaa` (subtitle uses filtered count)
    - Source assertion: the Tyhjennä haku onClick handler contains `setAktiivKaupunki('Kaikki')`
    - Build assertion: `npx tsc --noEmit` exits 0
    - Test assertion: `npx vitest run` exits 0
    - Behavior assertion (manual smoke): with a multi-city seed dataset, the dropdown shows the cities, selecting one filters the grid, the subtitle updates, and Tyhjennä haku in the empty state resets the city back to Kaikki
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add Tietosuoja footer Link (LEGAL-01 wiring, D-05)</name>
  <files>app/components/LiikuntapaikatLista.tsx</files>
  <read_first>
    - app/components/LiikuntapaikatLista.tsx (full file — find the closing of the content section after the venue grid; existing Link imports from next/link if any)
    - .planning/phases/06-ui-polish-and-data-foundation/06-PATTERNS.md (LiikuntapaikatLista section — Footer link block snippet)
    - .planning/phases/06-ui-polish-and-data-foundation/06-UI-SPEC.md (LEGAL-01 footer placement — after the venue grid)
    - .planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md (D-05 — text link to /tietosuoja at the bottom of LiikuntapaikatLista; do NOT add a BottomNav tab)
  </read_first>
  <action>
    1) Ensure `import Link from 'next/link'` is present at the top of the file. If LiikuntapaikatLista already imports Link, do NOT add a duplicate.

    2) After the venue grid (after the AnimatePresence/grid block and any empty-state block, but BEFORE the component's outer closing tag), insert a footer block:
        - Wrapper `<div className="max-w-5xl mx-auto px-4 pb-6 pt-2 flex justify-center">`
        - Child Link to `/tietosuoja` with text `Tietosuoja` and className `text-xs text-[rgba(17,17,17,0.35)] hover:text-[rgba(17,17,17,0.6)] [transition:color_150ms_var(--ease-out)] underline underline-offset-2`

    3) Do NOT add the link to BottomNav (D-05 prohibits). Do NOT alter the venue grid or empty state.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/components/LiikuntapaikatLista.tsx','utf8'); if(!/href=\"\/tietosuoja\"/.test(s)) throw new Error('footer Link to /tietosuoja missing'); if(!/Tietosuoja/.test(s)) throw new Error('footer label Tietosuoja missing'); if(!/import Link from ['\"]next\/link['\"]/.test(s)) throw new Error('Link import missing'); console.log('OK')"</automated>
  </verify>
  <done>The component renders a centered footer Link with text `Tietosuoja` pointing to `/tietosuoja` after the venue grid. `npx tsc --noEmit` exits 0.</done>
  <acceptance_criteria>
    - Source assertion: grep `href="/tietosuoja"` in LiikuntapaikatLista.tsx returns 1 match
    - Source assertion: grep `Tietosuoja` returns at least 1 match (the link label)
    - Source assertion: file imports Link from `next/link`
    - Build assertion: `npx tsc --noEmit` exits 0
    - Behavior assertion (manual smoke): visiting `/` (list view) scrolls to the bottom and shows the underlined Tietosuoja link; clicking it navigates to /tietosuoja (Plan 02 page)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → Client component | paikat[].kaupunki rendered into option values |
| Client → Internal route | Footer Link navigates to /tietosuoja (same-origin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering (XSS) | kaupunki strings rendered into option text and used in dropdown value | mitigate | React escapes text nodes and attribute values for option/value bindings; deriveKaupungit returns plain strings; no dangerouslySetInnerHTML. |
| T-06-12 | Information Disclosure | Privacy footer link reveals /tietosuoja route to anonymous users | accept | Intentional — LEGAL-01 requires public access to the GDPR page. |
| T-06-13 | Spoofing | Filter state held only in client React (no URL persistence) | accept | Per CLAUDE.md, only `?nakyma=` is URL-encoded view state. City/sport filters are intentionally ephemeral; no auth boundary crossed. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` exits 0 (priceUtils + cityFilter tests still green)
- grep assertions per acceptance criteria
- Manual smoke: open `/` and verify Row 1 layout, city filter, subtitle, Tyhjennä haku reset, and footer Tietosuoja link
</verification>

<success_criteria>
List page Row 1 shows `[City dropdown] [Sport dropdown] [Etäisyydet]`, selecting a city filters the grid (excluding null-kaupunki venues), the hero subtitle reflects the active filter and filtered count, Tyhjennä haku in the empty state clears all four filters including city, and a centered Tietosuoja link below the grid routes to /tietosuoja.
</success_criteria>

<output>
Create `.planning/phases/06-ui-polish-and-data-foundation/06-06-SUMMARY.md` when done.
</output>
