---
phase: 06-ui-polish-and-data-foundation
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/priceUtils.ts
  - lib/priceUtils.test.ts
  - lib/cityFilter.ts
  - lib/cityFilter.test.ts
autonomous: true
requirements:
  - UI-05
  - DATA-07
must_haves:
  truths:
    - "isMembershipOnly(paikka) returns true only when hinta_kuvaus contains 'jäsenyys' (case-insensitive) AND both hinta_min and hinta_max are null"
    - "isMembershipOnly returns false when hinta_kuvaus is null even if both hinta fields are null (Pitfall 2)"
    - "deriveKaupungit(paikat) returns ['Kaikki', ...sorted unique non-null kaupunki strings]"
    - "deriveKaupungit excludes null, undefined, and empty-string kaupunki values (Pitfall 6)"
  artifacts:
    - path: "lib/priceUtils.ts"
      provides: "isMembershipOnly(paikka) heuristic"
      exports: ["isMembershipOnly"]
    - path: "lib/priceUtils.test.ts"
      provides: "Unit tests for isMembershipOnly covering all branches of D-11"
    - path: "lib/cityFilter.ts"
      provides: "deriveKaupungit(paikat) dedup helper"
      exports: ["deriveKaupungit"]
    - path: "lib/cityFilter.test.ts"
      provides: "Unit tests for deriveKaupungit covering Pitfall 6 edge cases"
  key_links:
    - from: "lib/priceUtils.ts"
      to: "PaikkaKortti price block (Plan 05 consumer)"
      via: "named import of isMembershipOnly"
      pattern: "isMembershipOnly"
    - from: "lib/cityFilter.ts"
      to: "LiikuntapaikatLista city dropdown (Plan 06 consumer)"
      via: "named import of deriveKaupungit"
      pattern: "deriveKaupungit"
---

<objective>
Create the two pure helper modules — `lib/priceUtils.ts` (isMembershipOnly heuristic per D-11, Pitfall 2) and `lib/cityFilter.ts` (deriveKaupungit dedup per D-21, Pitfall 6) — together with their vitest test files. Wave 2 plans 05 and 06 import these helpers, so this plan front-loads the contracts and unit tests so executors don't reinvent the heuristics inside components.

Purpose: Satisfies RESEARCH.md "Wave 0 Gaps" (`lib/priceUtils.test.ts`, `lib/cityFilter.test.ts`) and the validation architecture for UI-05 and DATA-07. Tests are written FIRST and MUST initially fail, then implementations pass them. This is a `type: tdd` plan.

Output: 2 new source files + 2 new test files in `lib/`. `npx vitest run` exits 0 with all new tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md
@.planning/phases/06-ui-polish-and-data-foundation/06-RESEARCH.md
@.planning/phases/06-ui-polish-and-data-foundation/06-PATTERNS.md
@vitest.config.ts
@lib/types.ts
@lib/utils.ts

<interfaces>
Existing contracts in lib/types.ts (consume, do not modify):
- Liikuntapaikka.hinta_kuvaus: string | null
- Liikuntapaikka.hinta_min: number | null
- Liikuntapaikka.hinta_max: number | null
- Liikuntapaikka.kaupunki: string | null

Existing helper to NOT replace (lib/utils.ts):
- hintateksti(min, max): kept as the numeric-range formatter; isMembershipOnly does NOT replace it

Vitest config scope: `include: ['lib/**/*.test.ts']`, `environment: 'node'`. Pure logic tests only — no DOM, no React.

New contracts being introduced (Wave 2 consumers will import):

  export function isMembershipOnly(p: Pick<Liikuntapaikka, 'hinta_kuvaus' | 'hinta_min' | 'hinta_max'>): boolean

  export function deriveKaupungit(paikat: Array<Pick<Liikuntapaikka, 'kaupunki'>>): string[]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD isMembershipOnly heuristic (UI-05, D-11, Pitfall 2)</name>
  <files>lib/priceUtils.ts, lib/priceUtils.test.ts</files>
  <read_first>
    - lib/types.ts (Liikuntapaikka type — confirms field nullability)
    - lib/utils.ts (existing hintateksti helper — DO NOT replace; coexists)
    - .planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md (D-11 — exact rule)
    - .planning/phases/06-ui-polish-and-data-foundation/06-RESEARCH.md (Pattern 2 + Pitfall 2 — heuristic boundary)
    - vitest.config.ts (test file glob and environment)
  </read_first>
  <behavior>
    isMembershipOnly returns true if and only if ALL three are true:
      1. paikka.hinta_kuvaus is a non-empty string
      2. paikka.hinta_kuvaus.toLowerCase() contains the substring "jäsenyys"
      3. paikka.hinta_min is null AND paikka.hinta_max is null

    Test cases (write these BEFORE the implementation):
      - "jäsenyys vaaditaan", min=null, max=null → true
      - "Jäsenyys 50€/v" (capital J), min=null, max=null → true (case-insensitive)
      - "Vain JÄSENYYS", min=null, max=null → true (case-insensitive)
      - "jäsenyys vaaditaan", min=20, max=null → false (numeric price present)
      - "jäsenyys vaaditaan", min=null, max=30 → false (numeric price present)
      - "kertakäynti 8€", min=null, max=null → false (no "jäsenyys" in text)
      - hinta_kuvaus=null, min=null, max=null → false (Pitfall 2: no affirmative evidence)
      - hinta_kuvaus="", min=null, max=null → false (empty string is no evidence)
      - hinta_kuvaus="kertakäynti 8€, jäsenyys 50€/v", min=null, max=null → true (substring match anywhere)
  </behavior>
  <action>
    RED phase:
      - Create `lib/priceUtils.test.ts` containing a vitest `describe('isMembershipOnly', ...)` with one `it()` per case enumerated in `<behavior>`. Each `it()` uses `expect(isMembershipOnly(input)).toBe(expected)`. Import from `./priceUtils` (file does not yet exist — test must FAIL with a module-not-found or undefined-function error).
      - Run `npx vitest run lib/priceUtils.test.ts` and confirm it fails. Commit: `test(06-04): add failing tests for isMembershipOnly`.

    GREEN phase:
      - Create `lib/priceUtils.ts` exporting `isMembershipOnly(p: Pick<Liikuntapaikka,'hinta_kuvaus'|'hinta_min'|'hinta_max'>): boolean` implementing the three-condition rule. Import `Liikuntapaikka` from `@/lib/types` (the project's tsconfig path alias) OR from `./types` — match whichever convention `lib/utils.ts` uses (read it).
      - Run `npx vitest run lib/priceUtils.test.ts` and confirm all cases pass. Commit: `feat(06-04): implement isMembershipOnly heuristic`.

    Do NOT delete or modify `hintateksti` in `lib/utils.ts`. Do NOT add any other exports to `lib/priceUtils.ts` (Plan 05 only needs `isMembershipOnly`; multi-line price splitting stays inline in PaikkaKortti per D-13).
  </action>
  <verify>
    <automated>npx vitest run lib/priceUtils.test.ts</automated>
  </verify>
  <done>Both `lib/priceUtils.ts` and `lib/priceUtils.test.ts` exist. All nine enumerated test cases pass under `npx vitest run lib/priceUtils.test.ts`. `npx tsc --noEmit` exits 0. Two commits exist (test-first then impl).</done>
  <acceptance_criteria>
    - Source assertion: file lib/priceUtils.ts exports `isMembershipOnly`
    - Source assertion: file lib/priceUtils.test.ts contains the literal `describe('isMembershipOnly'`
    - Test assertion: `npx vitest run lib/priceUtils.test.ts` exits 0 with at least 9 passing tests
    - Behavior assertion: `isMembershipOnly({hinta_kuvaus:null, hinta_min:null, hinta_max:null})` returns `false` (Pitfall 2)
    - Behavior assertion: `isMembershipOnly({hinta_kuvaus:'Jäsenyys 50€', hinta_min:null, hinta_max:null})` returns `true` (case-insensitive)
    - Build assertion: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD deriveKaupungit dedup helper (DATA-07, D-21, Pitfall 6)</name>
  <files>lib/cityFilter.ts, lib/cityFilter.test.ts</files>
  <read_first>
    - lib/types.ts (Liikuntapaikka.kaupunki field — string | null)
    - .planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md (D-21 — exact derivation rule and "Kaikki" first)
    - .planning/phases/06-ui-polish-and-data-foundation/06-RESEARCH.md (Pattern 4 + Pitfall 6 — null handling, no Tuntematon bucket)
    - .planning/phases/06-ui-polish-and-data-foundation/06-PATTERNS.md (LiikuntapaikatLista section — `useMemo` usage shape)
    - lib/priceUtils.ts (from Task 1 — to match import style/path conventions)
  </read_first>
  <behavior>
    deriveKaupungit(paikat) returns a string[] in this exact order:
      [ 'Kaikki', ...alphabetically-sorted unique non-empty kaupunki values ]

    Filtering rules (Pitfall 6):
      - null kaupunki: excluded from the list (not added as "Tuntematon")
      - undefined kaupunki: excluded
      - empty string "" or whitespace-only "   ": excluded
      - Duplicate strings: deduplicated (case-sensitive — "Tampere" and "tampere" count as distinct because DB normalization isn't guaranteed; do NOT lowercase-merge)

    Test cases (write BEFORE implementation):
      - [] → ["Kaikki"]
      - [{kaupunki:"Tampere"}] → ["Kaikki","Tampere"]
      - [{kaupunki:"Tampere"},{kaupunki:"Tampere"}] → ["Kaikki","Tampere"] (dedup)
      - [{kaupunki:"Turku"},{kaupunki:"Helsinki"},{kaupunki:"Tampere"}] → ["Kaikki","Helsinki","Tampere","Turku"] (sorted)
      - [{kaupunki:null},{kaupunki:"Tampere"}] → ["Kaikki","Tampere"] (null excluded)
      - [{kaupunki:""},{kaupunki:"Tampere"}] → ["Kaikki","Tampere"] (empty excluded)
      - [{kaupunki:"  "},{kaupunki:"Tampere"}] → ["Kaikki","Tampere"] (whitespace-only excluded)
      - [{kaupunki:null}] → ["Kaikki"] (no specific cities found)
      - "Kaikki" always appears at index 0 even if a DB row literally has "Kaikki" as a value — the literal "Kaikki" from DB is treated as a regular value and would appear after the sort would have placed it, but since "Kaikki" sorts before normal capital letters in Finnish locale only after "A"/"E"/"H" etc., test the specific case: [{kaupunki:"Kaikki"}] → ["Kaikki"] (the sentinel and the DB value collapse — dedup vs sentinel; choose: dedup so the result is ["Kaikki"]).
  </behavior>
  <action>
    RED phase:
      - Create `lib/cityFilter.test.ts` with `describe('deriveKaupungit', ...)` containing one `it()` per case in `<behavior>`. Use `expect(deriveKaupungit(input)).toEqual(expected)`. Import from `./cityFilter` (file does not exist — test must FAIL initially).
      - Run `npx vitest run lib/cityFilter.test.ts` — confirm failure. Commit: `test(06-04): add failing tests for deriveKaupungit`.

    GREEN phase:
      - Create `lib/cityFilter.ts` exporting `deriveKaupungit(paikat: Array<Pick<Liikuntapaikka,'kaupunki'>>): string[]`. Implementation:
          1. Map paikat to `kaupunki` strings.
          2. Filter to truthy strings whose `trim()` length > 0.
          3. Build a `Set` to dedup (case-sensitive).
          4. Sort using default lexicographic compare (do NOT call `.toLowerCase()` before compare — Finnish locale-aware sort is not required for v1.1; D-21 sample uses default sort).
          5. Dedup against the sentinel "Kaikki" so a DB-literal "Kaikki" does not appear twice.
          6. Return `['Kaikki', ...sortedUniqueSpecific]`.
      - Run `npx vitest run lib/cityFilter.test.ts` — confirm green. Commit: `feat(06-04): implement deriveKaupungit dedup helper`.

    Do NOT export any additional helpers. Do NOT add `useMemo` here — that wrapper stays in `LiikuntapaikatLista.tsx` (Plan 06).
  </action>
  <verify>
    <automated>npx vitest run lib/cityFilter.test.ts</automated>
  </verify>
  <done>Both `lib/cityFilter.ts` and `lib/cityFilter.test.ts` exist. All enumerated test cases pass. `npx tsc --noEmit` exits 0. Two commits exist (test-first then impl).</done>
  <acceptance_criteria>
    - Source assertion: file lib/cityFilter.ts exports `deriveKaupungit`
    - Source assertion: file lib/cityFilter.test.ts contains the literal `describe('deriveKaupungit'`
    - Test assertion: `npx vitest run lib/cityFilter.test.ts` exits 0 with at least 9 passing tests
    - Behavior assertion: `deriveKaupungit([])` deep-equals `["Kaikki"]`
    - Behavior assertion: `deriveKaupungit([{kaupunki:null},{kaupunki:'Tampere'}])` deep-equals `["Kaikki","Tampere"]`
    - Behavior assertion: `deriveKaupungit([{kaupunki:'Turku'},{kaupunki:'Helsinki'},{kaupunki:'Tampere'}])` deep-equals `["Kaikki","Helsinki","Tampere","Turku"]`
    - Build assertion: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helper module → consumer component | Pure functions invoked by client React components; no I/O, no DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | DB-supplied `kaupunki` strings | accept | RLS already restricts writes to service role; helper treats all input as opaque strings and renders inside React (auto-escaped). No interpolation into HTML or SQL here. |
| T-06-08 | Denial of Service | Large `paikat` arrays passed to `deriveKaupungit` | accept | Helper is O(n log n) due to sort; current data scale (~hundreds of rows) is trivial. No threshold mitigation needed. |
</threat_model>

<verification>
- `npx vitest run lib/priceUtils.test.ts lib/cityFilter.test.ts` exits 0
- `npx vitest run` (full suite) exits 0 — no regression of prior tests
- `npx tsc --noEmit` exits 0
- Git log shows four commits (two test-first RED, two impl GREEN) for traceability
</verification>

<success_criteria>
Two new pure helper modules and two new vitest test files exist in `lib/`. All tests are green. The TDD discipline produces a test-first commit and an implementation commit per helper. Wave 2 plans 05 and 06 can `import { isMembershipOnly } from '@/lib/priceUtils'` and `import { deriveKaupungit } from '@/lib/cityFilter'` without additional research.
</success_criteria>

<output>
Create `.planning/phases/06-ui-polish-and-data-foundation/06-04-SUMMARY.md` when done.
</output>
