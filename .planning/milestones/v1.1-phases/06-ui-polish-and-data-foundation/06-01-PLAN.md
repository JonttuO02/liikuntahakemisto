---
phase: 06-ui-polish-and-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
autonomous: true
requirements:
  - ADS-02
must_haves:
  truths:
    - "Featured column reaches the client so Sponsoroitu badge can render"
  artifacts:
    - path: "app/page.tsx"
      provides: "SELECT query including featured column"
      contains: "featured"
  key_links:
    - from: "app/page.tsx"
      to: "Supabase liikuntapaikat SELECT"
      via: "explicit column list"
      pattern: "featured"
---

<objective>
Add the `featured` column to the explicit SELECT query in `app/page.tsx` so the boolean reaches `Etusivu` and `LiikuntapaikatLista` and downstream components (PaikkaKortti, bottom-sheet) can render the "Sponsoroitu" badge.

Purpose: Unblocks ADS-02 in Wave 2 (PaikkaKortti badge and Etusivu bottom-sheet badge both read `paikka.featured` / `valittu.featured`; currently the field is `undefined` on the client because the column is missing from the SELECT — see RESEARCH.md Pitfall 4).

Output: One-line edit to the `.select(...)` argument string.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ui-polish-and-data-foundation/06-CONTEXT.md
@.planning/phases/06-ui-polish-and-data-foundation/06-RESEARCH.md
@.planning/phases/06-ui-polish-and-data-foundation/06-PATTERNS.md
@CLAUDE.md
@app/page.tsx
@lib/types.ts

<interfaces>
From lib/types.ts — the type already includes featured (no change required):
- Liikuntapaikka.featured?: boolean | null
- Liikuntapaikka.kaupunki: already part of type and SELECT
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add featured column to SELECT in app/page.tsx</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (current SELECT column list at the supabase.from('liikuntapaikat').select(...) call)
    - lib/types.ts (confirms Liikuntapaikka.featured exists — no type change needed)
    - .planning/phases/06-ui-polish-and-data-foundation/06-RESEARCH.md (Pitfall 4 — explains why this single edit is the unblocker)
  </read_first>
  <action>Append `, featured` to the end of the explicit column list passed to `.select(...)` in `app/page.tsx` (per D-09). Do not modify any other line in the file. Do not change ordering of existing columns. Do not switch to `select('*')` — keep the explicit column list. The final SELECT string must contain these exact 15 tokens (comma-separated): `id, nimi, laji, osoite, kaupunki, latitude, longitude, hinta_min, hinta_max, varauslinkki, kuvaus, puhelin, aukioloajat, hinta_kuvaus, featured`.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('app/page.tsx','utf8'); const m=s.match(/\.select\(['\"]([^'\"]+)['\"]\)/); if(!m) throw new Error('no .select() found'); const cols=m[1].split(',').map(c=>c.trim()); if(!cols.includes('featured')) throw new Error('featured missing'); console.log('OK featured present, total cols=' + cols.length)"</automated>
  </verify>
  <done>app/page.tsx `.select(...)` argument literally contains the token `featured` and the call still compiles (`npx tsc --noEmit` passes). No other line changed.</done>
  <acceptance_criteria>
    - Source assertion: grep `\.select\(.*featured.*\)` in app/page.tsx returns exactly one match
    - Source assertion: app/page.tsx contains no occurrence of `select('*')` (explicit list preserved)
    - Build assertion: `npx tsc --noEmit` exits 0
    - Behavior assertion: in the running app, `paikat[0].featured` is `boolean | null | undefined` (not always `undefined`) — verified by reading a row that has `featured=true` in DB
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Client | Supabase row data passed from `app/page.tsx` (server component) to `Etusivu` and `LiikuntapaikatLista` (client components) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `featured` column manipulation via anon key | accept | Already mitigated in Phase 1 — RLS `public_read` policy is SELECT-only; anon role cannot UPDATE the column. Verified in RESEARCH.md Security Domain. |
| T-06-02 | Information Disclosure | Boolean `featured` exposed to all clients | accept | `featured` is a marketing flag, not PII or sensitive data. Intentional public exposure (renders as Sponsoroitu badge to all users). |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- grep for `featured` inside the `.select(...)` argument returns one match
- `npx vitest run` (no test changes; sanity that suite still green)
</verification>

<success_criteria>
The string `featured` appears inside the `.select(...)` call in `app/page.tsx`, app compiles, and no other change was made.
</success_criteria>

<output>
Create `.planning/phases/06-ui-polish-and-data-foundation/06-01-SUMMARY.md` when done.
</output>
