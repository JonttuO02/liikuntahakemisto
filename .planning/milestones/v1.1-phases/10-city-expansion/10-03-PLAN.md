---
phase: 10-city-expansion
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - app/api/saasuositus/route.ts
autonomous: true
requirements:
  - DATA-06

must_haves:
  truths:
    - "GET /api/saasuositus?kaupunki=Helsinki returns weather data for Helsinki coordinates"
    - "POST /api/saasuositus with body { kaupunki: 'Turku' } returns Turku weather in the AI prompt"
    - "GET /api/saasuositus (no param) still works with Tampere fallback"
    - "Claude Haiku prompt uses the dynamic city name, not the hardcoded 'Tampere'"
    - "fetchWeather accepts lat/lng coordinates and uses them in the Open Meteo URL"
    - "getTimeBasedFallback no longer hardcodes 'Tampere' in the fallback text"
  artifacts:
    - path: "app/api/saasuositus/route.ts"
      provides: "City-aware weather and AI recommendation endpoint"
      contains: "kaupunki"
  key_links:
    - from: "app/api/saasuositus/route.ts"
      to: "lib/constants.ts"
      via: "import SUOMI_KAUPUNGIT"
      pattern: "SUOMI_KAUPUNGIT"
    - from: "fetchWeather"
      to: "Open Meteo API"
      via: "coordinates from SUOMI_KAUPUNGIT lookup"
      pattern: "latitude=.*longitude="
---

<objective>
Make the saasuositus weather + AI route city-aware by accepting a `kaupunki` parameter in both GET and POST, looking up coordinates from SUOMI_KAUPUNGIT, and passing the city name dynamically to the Claude Haiku prompt.

Purpose: Plan 04 (Etusivu map-center tracking) will send the detected nearest city to this endpoint. The route must already accept and use a kaupunki param before Etusivu can trigger city-specific recommendations.

Output: app/api/saasuositus/route.ts — fetchWeather parameterized with lat/lng, GET and POST both read kaupunki param, Haiku prompt uses dynamic city name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-city-expansion/10-CONTEXT.md
@.planning/phases/10-city-expansion/10-01-SUMMARY.md

<interfaces>
From lib/constants.ts (after Plan 01):
```typescript
export const SUOMI_KAUPUNGIT: { nimi: string; lat: number; lng: number }[] = [
  { nimi: 'Helsinki', lat: 60.1699, lng: 24.9384 },
  { nimi: 'Tampere',  lat: 61.4978, lng: 23.7610 },
  { nimi: 'Turku',    lat: 60.4518, lng: 22.2666 },
  // ... 22 more cities
]
```

Current saasuositus/route.ts structure:
- fetchWeather(): Promise<WeatherData> — hardcodes Tampere lat/lng in Open Meteo URL
- GET() — calls fetchWeather(), sends Tampere-hardcoded prompt to Claude Haiku
- POST(request) — reads suosikit[] from body, calls fetchWeather(), sends Tampere-hardcoded prompt
- getTimeBasedFallback() — hardcodes 'Tampere' in fallback strings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize fetchWeather and add kaupunki to GET/POST handlers</name>
  <files>app/api/saasuositus/route.ts</files>
  <read_first>
    app/api/saasuositus/route.ts — read full current implementation before any edits; note all Tampere hardcodings
    lib/constants.ts — verify SUOMI_KAUPUNGIT is exported (Plan 01 prerequisite)
  </read_first>
  <action>
    Make the following changes to app/api/saasuositus/route.ts:

    1. IMPORT: Add `import { SUOMI_KAUPUNGIT } from '@/lib/constants'` at the top of the file.

    2. fetchWeather SIGNATURE: Change to `async function fetchWeather(lat: number, lng: number): Promise<WeatherData>`. Replace the hardcoded Open Meteo URL `latitude=61.4978&longitude=23.7610` with `latitude=${lat}&longitude=${lng}` (per D-11). The return type and shape `{ temp, code, day, weatherDesc }` remain identical.

    3. getTimeBasedFallback SIGNATURE: Change to `function getTimeBasedFallback(kaupunki = 'Tampere'): string`. Replace the three hardcoded 'Tampereelta' strings with `${kaupunki}lta` or a simple dynamic form. For correctness with Finnish grammar: use `${kaupunki}lta` which handles most city names naturally (Tampereelta → `${kaupunki}lta` when kaupunki='Tampere'). Note: this is a fallback string, approximate grammar is acceptable.

    4. GET HANDLER: Change signature to `export async function GET(request: Request)`. Read kaupunki from URL: `const kaupunki = new URL(request.url).searchParams.get('kaupunki') ?? 'Tampere'`. Look up city coordinates: `const city = SUOMI_KAUPUNGIT.find(c => c.nimi === kaupunki) ?? { lat: 61.4978, lng: 23.7610 }`. Call `fetchWeather(city.lat, city.lng)`. In the Claude Haiku prompt, replace the hardcoded `'Tampereella'` / `'Tampereelta'` references with the dynamic kaupunki (per D-12). For the prompt, use the pattern: `Tänään on ${day} ${kaupunki}ssa. Lämpötila on ${temp}°C ja sää on ${weatherDesc}. Kirjoita YKSI lyhyt suomenkielinen lause joka suosittelee sopivaa liikuntapalvelua tai -lajia tähän säähän ${kaupunki}ssa. Mainitse "${kaupunki}" tai viittaa liikuntapaikan löytämiseen. Älä käytä emojeja.` — the Finnish locative suffix (ssa/lla/lla) is approximate but acceptable for a recommendation widget. Pass `kaupunki` to `getTimeBasedFallback(kaupunki)` in the catch branch.

    5. POST HANDLER: Read kaupunki from request body alongside suosikit: add `kaupunki = body.kaupunki` (string, validated: `typeof body.kaupunki === 'string' ? body.kaupunki : 'Tampere'`). Look up city coordinates from SUOMI_KAUPUNGIT using the same find pattern. Call `fetchWeather(city.lat, city.lng)`. Use the same dynamic prompt template as GET (per D-12), incorporating both kaupunki and suosikkiLista. Pass `kaupunki` to `getTimeBasedFallback(kaupunki)` in the catch branch.

    6. Do NOT change the response shape `{ text, temp, code, fallback }` — it must stay identical so Etusivu.tsx requires no changes in this plan.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v "^//" app/api/saasuositus/route.ts | grep -c "'Tampere'"` returns at most 2 (only the two fallback default values: one in GET, one in POST kaupunki extraction; not in prompt strings)
    - `grep "SUOMI_KAUPUNGIT" app/api/saasuositus/route.ts` matches (import and city lookup)
    - `grep "fetchWeather(city.lat" app/api/saasuositus/route.ts` matches in both GET and POST handlers
    - `grep "kaupunki" app/api/saasuositus/route.ts | wc -l` returns at least 8 lines (import, param read, lookup, prompt, fallback — in both handlers)
    - The function `fetchWeather` no longer contains the string `61.4978` or `23.7610` as a literal (coordinates come from parameters now)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>
    saasuositus route accepts kaupunki in GET (?kaupunki=Helsinki) and POST (body.kaupunki), fetches correct city weather, and uses city name in Haiku prompt. Tampere is only the fallback default, not a hardcoded value.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/saasuositus | kaupunki param from client (GET query string or POST body) |
| Route → Open Meteo API | Server-to-server weather fetch with lat/lng |
| Route → Anthropic API | Server-side Haiku call with dynamic prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering | kaupunki param injected into Haiku prompt | mitigate | Prompt injection risk mitigated by: (a) kaupunki is looked up from SUOMI_KAUPUNGIT whitelist for coordinates — unknown city names still work syntactically but coordinates fall back to Tampere; (b) kaupunki from POST body is validated as `typeof === 'string'`; (c) Haiku output is a short recommendation, not executed |
| T-10-03-02 | Denial of Service | Haiku calls per city map move | accept | Etusivu (Plan 04) debounces 3s and only triggers on city change — not per camera move; existing cache key pattern in Etusivu provides additional protection |
| T-10-03-SC | Tampering | No new npm installs | accept | No package manager invocations in this plan |
</threat_model>

<verification>
- TypeScript: `npx tsc --noEmit` — zero errors
- Source: `grep "fetchWeather(city.lat" app/api/saasuositus/route.ts` — present in GET and POST
- Source: `grep "61.4978" app/api/saasuositus/route.ts` — must return empty (coordinate no longer hardcoded in fetchWeather body)
- Behavior: `curl "http://localhost:3000/api/saasuositus?kaupunki=Helsinki"` returns JSON with text mentioning Helsinki (or at minimum no error)
</verification>

<success_criteria>
- GET /api/saasuositus?kaupunki=Helsinki returns weather for Helsinki coordinates and a Helsinki-aware AI text
- POST /api/saasuositus with { kaupunki: 'Turku', suosikit: [] } returns Turku weather
- GET /api/saasuositus (no param) still returns Tampere weather (fallback unchanged)
- Response shape { text, temp, code, fallback } is unchanged — Etusivu.tsx requires no modification in this plan
- No Tampere hardcoded coordinates or city name in fetchWeather function body
</success_criteria>

<output>
Create `.planning/phases/10-city-expansion/10-03-SUMMARY.md` when done
</output>
