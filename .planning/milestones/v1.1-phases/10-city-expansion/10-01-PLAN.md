---
phase: 10-city-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/constants.ts
  - lib/geo.ts
autonomous: true
requirements:
  - DATA-05
  - DATA-06

must_haves:
  truths:
    - "SUOMI_KAUPUNGIT list with 25 cities is exported from lib/constants.ts"
    - "nearestKaupunki(lat, lng) returns the name of the geographically closest city"
    - "nearestKaupunki correctly identifies Tampere for coordinates 61.4978, 23.761"
    - "nearestKaupunki correctly identifies Helsinki for coordinates 60.1699, 24.9384"
  artifacts:
    - path: "lib/constants.ts"
      provides: "SUOMI_KAUPUNGIT array and TAMPERE constant"
      exports: ["TAMPERE", "SUOMI_KAUPUNGIT"]
      contains: "SUOMI_KAUPUNGIT"
    - path: "lib/geo.ts"
      provides: "nearestKaupunki function"
      exports: ["haversineKm", "formatDistance", "nearestKaupunki"]
  key_links:
    - from: "lib/geo.ts"
      to: "lib/constants.ts"
      via: "import SUOMI_KAUPUNGIT"
      pattern: "SUOMI_KAUPUNGIT"
---

<objective>
Add SUOMI_KAUPUNGIT city list to lib/constants.ts and nearestKaupunki utility to lib/geo.ts.

Purpose: These are the shared building blocks that the sync route (Plan 02), the weather API (Plan 03), and Etusivu map tracking (Plan 04) all depend on. They must exist before any downstream plan can run.

Output: Two updated files — lib/constants.ts gains SUOMI_KAUPUNGIT, lib/geo.ts gains nearestKaupunki.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-city-expansion/10-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SUOMI_KAUPUNGIT to lib/constants.ts</name>
  <files>lib/constants.ts</files>
  <read_first>
    lib/constants.ts — read current content (currently only exports TAMPERE); the new export goes in the same file
  </read_first>
  <behavior>
    - SUOMI_KAUPUNGIT is exported as a typed const array: `{ nimi: string; lat: number; lng: number }[]`
    - The array contains exactly 25 cities ordered as specified in CONTEXT.md (Helsinki first, Kouvola last)
    - TAMPERE entry in SUOMI_KAUPUNGIT matches the existing TAMPERE constant: lat 61.4978, lng 23.7610
    - The existing `export const TAMPERE = { lat: 61.4978, lng: 23.761 }` is preserved unchanged
  </behavior>
  <action>
    Append to lib/constants.ts after the TAMPERE export. The new export is named SUOMI_KAUPUNGIT with explicit type annotation `{ nimi: string; lat: number; lng: number }[]`.

    Use the exact 25-city list from CONTEXT.md (per D-06), preserving the order and coordinate values verbatim. The cities are: Helsinki, Espoo, Tampere, Vantaa, Oulu, Turku, Jyväskylä, Lahti, Kuopio, Pori, Joensuu, Lappeenranta, Hämeenlinna, Vaasa, Seinäjoki, Rovaniemi, Mikkeli, Kotka, Salo, Porvoo, Kokkola, Hyvinkää, Lohja, Rauma, Kouvola.

    Do NOT use a TypeScript interface — use the inline type annotation directly on the const.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    lib/constants.ts exports both TAMPERE and SUOMI_KAUPUNGIT; `grep -c "SUOMI_KAUPUNGIT" lib/constants.ts` returns at least 1; TypeScript compiles without errors on this file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add nearestKaupunki to lib/geo.ts</name>
  <files>lib/geo.ts</files>
  <read_first>
    lib/geo.ts — read current exports (haversineKm, formatDistance); nearestKaupunki is added at the end
    lib/constants.ts — verify SUOMI_KAUPUNGIT is now exported (created in Task 1)
  </read_first>
  <behavior>
    - nearestKaupunki(lat: number, lng: number): string iterates SUOMI_KAUPUNGIT using haversineKm
    - Returns the `nimi` string of the city with the smallest haversine distance
    - For input (61.4978, 23.761) returns 'Tampere'
    - For input (60.1699, 24.9384) returns 'Helsinki'
    - For input (60.4518, 22.2666) returns 'Turku'
    - Import path: `import { SUOMI_KAUPUNGIT } from './constants'` (no @/ alias — geo.ts is in lib/)
  </behavior>
  <action>
    Add named import `{ SUOMI_KAUPUNGIT }` from `'./constants'` at the top of lib/geo.ts.

    Append nearestKaupunki function at the end of lib/geo.ts. The implementation follows the CONTEXT.md snippet verbatim (per D-07): initialize `best = SUOMI_KAUPUNGIT[0]`, `bestDist = Infinity`, loop over all cities with haversineKm, update best when distance is smaller, return `best.nimi`.

    The function signature is `export function nearestKaupunki(lat: number, lng: number): string`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    lib/geo.ts exports nearestKaupunki; `grep -c "nearestKaupunki" lib/geo.ts` returns at least 2 (export + function body reference); TypeScript compiles without errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lib modules | Pure in-process computation, no external I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-SC | Tampering | No new npm installs in this plan | accept | No package manager invocations; no slopcheck needed |
</threat_model>

<verification>
Run `npx tsc --noEmit` — zero errors expected.

Spot-check exports:
- `grep "export const SUOMI_KAUPUNGIT" lib/constants.ts` — must match
- `grep "export function nearestKaupunki" lib/geo.ts` — must match
- `grep "SUOMI_KAUPUNGIT" lib/geo.ts` — must appear (import line)
</verification>

<success_criteria>
- lib/constants.ts exports TAMPERE (unchanged) and SUOMI_KAUPUNGIT (25-city array)
- lib/geo.ts exports haversineKm, formatDistance, and nearestKaupunki
- nearestKaupunki correctly identifies the nearest Finnish city by haversine distance
- No TypeScript errors introduced
</success_criteria>

<output>
Create `.planning/phases/10-city-expansion/10-01-SUMMARY.md` when done
</output>
