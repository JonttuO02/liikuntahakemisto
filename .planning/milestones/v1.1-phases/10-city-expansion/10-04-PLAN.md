---
phase: 10-city-expansion
plan: 04
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-03
files_modified:
  - app/components/Etusivu.tsx
autonomous: true
requirements:
  - DATA-06

must_haves:
  truths:
    - "When the user pans the map to Helsinki, the AI widget updates after 3s to show Helsinki weather recommendation"
    - "The city name next to the temperature reading updates to reflect the nearest Finnish city"
    - "No new AI fetch fires when the map stays within the same nearest city"
    - "City detection uses nearestKaupunki from lib/geo.ts, not a hardcoded list"
    - "Debounce is ref-based (setTimeout/clearTimeout), not useCallback/useMemo"
    - "The list view city filter does NOT move the map (D-13 — map defaultCenter stays Tampere)"
  artifacts:
    - path: "app/components/Etusivu.tsx"
      provides: "Map-center-aware weather city tracking with debounced AI refetch"
      contains: "weatherKaupunki"
  key_links:
    - from: "app/components/Etusivu.tsx"
      to: "lib/geo.ts"
      via: "import nearestKaupunki"
      pattern: "nearestKaupunki"
    - from: "Etusivu onCameraChanged"
      to: "weatherKaupunki state"
      via: "3s debounce + nearestKaupunki"
      pattern: "debounceRef"
    - from: "AI fetch effect"
      to: "/api/saasuositus"
      via: "kaupunki param in GET querystring or POST body"
      pattern: "kaupunki.*weatherKaupunki"
---

<objective>
Add map-center awareness to Etusivu: track the map camera center position, debounce it 3 seconds, and trigger a new AI/weather fetch when the nearest Finnish city changes.

Purpose: DATA-06 requires the AI widget to be aware of where the user is looking on the map. When the user pans to Helsinki, the recommendation should say "Helsinki" not "Tampere". The saasuositus route (Plan 03) already accepts a kaupunki param; this plan wires Etusivu to use it.

Output: app/components/Etusivu.tsx — mapCenter state, onCameraChanged extended, debounce ref, weatherKaupunki state, AI fetch updated to pass kaupunki.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-city-expansion/10-CONTEXT.md
@.planning/phases/10-city-expansion/10-01-SUMMARY.md
@.planning/phases/10-city-expansion/10-03-SUMMARY.md

<interfaces>
From lib/geo.ts (after Plan 01):
```typescript
export function nearestKaupunki(lat: number, lng: number): string
// returns nimi of closest city from SUOMI_KAUPUNGIT by haversine distance
```

From /api/saasuositus (after Plan 03):
```
GET /api/saasuositus?kaupunki=Helsinki
POST /api/saasuositus  body: { suosikit: string[], kaupunki: string }
Response: { text: string, temp: number, code: number, fallback?: boolean }
```

Key Etusivu.tsx state (current, before this plan):
- const WEATHER_CITY = 'Tampere'  ← module-level const at line 29; used in UI at line ~602
- useState: saa, aiTeksti, zoomLevel, suosikitIds, suosikitSizeAndIds (memo)
- useRef: inFlight (Set<number>)
- onCameraChanged={ev => setZoomLevel(ev.detail.zoom)}  ← line ~324; only tracks zoom today
- AI fetch useEffect depends on [suosikitSizeAndIds]  ← must extend dependency to also re-run on city change
- Weather fetch useEffect: direct Open Meteo call at Tampere coords (line ~215-220) ← stays unchanged; saa state is for the temp display only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mapCenter state, debounce ref, and weatherKaupunki state</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    app/components/Etusivu.tsx — read the full import block (lines 1-30), all useState/useRef declarations (lines ~88-112), and the onCameraChanged callback (line ~324)
  </read_first>
  <action>
    Make the following targeted changes to app/components/Etusivu.tsx:

    1. IMPORT nearestKaupunki: Add `nearestKaupunki` to the import from `@/lib/geo.ts`. The file currently does not import from geo.ts — add: `import { nearestKaupunki } from '@/lib/geo'`.

    2. REMOVE module-level const: Delete `const WEATHER_CITY = 'Tampere'` (currently at line 29). It becomes state.

    3. ADD STATE: After the existing useState declarations (after `const [supabaseUser, ...]` etc.), add two new state values:
       - `const [mapCenter, setMapCenter] = useState<{ lat: number; lng: number }>(TAMPERE)` — tracks current map camera center; initialized to TAMPERE (already imported)
       - `const [weatherKaupunki, setWeatherKaupunki] = useState<string>('Tampere')` — the currently displayed city name for the AI widget; initialized to 'Tampere'

    4. ADD debounce ref: After the existing `const inFlight = useRef<Set<number>>(new Set())`, add:
       `const debounceRef = useRef<ReturnType<typeof setTimeout> | null>(null)`

    5. EXTEND onCameraChanged: Change the callback from `ev => setZoomLevel(ev.detail.zoom)` to a multi-statement arrow function (per D-08 and D-09):
       ```
       ev => {
         setZoomLevel(ev.detail.zoom)
         const center = ev.detail.center
         setMapCenter(center)
         if (debounceRef.current) clearTimeout(debounceRef.current)
         debounceRef.current = setTimeout(() => {
           const nearest = nearestKaupunki(center.lat, center.lng)
           setWeatherKaupunki(prev => nearest !== prev ? nearest : prev)
         }, 3000)
       }
       ```
       Use `setWeatherKaupunki(prev => nearest !== prev ? nearest : prev)` so React only triggers a re-render (and the AI fetch dependency) when the city actually changes.

    6. UI: Replace the JSX reference to `WEATHER_CITY` (around line ~602: `{WEATHER_CITY}`) with `{weatherKaupunki}`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "WEATHER_CITY" app/components/Etusivu.tsx` returns empty (const removed, all usages replaced)
    - `grep "weatherKaupunki" app/components/Etusivu.tsx | wc -l` returns at least 4 lines (useState, setter in callback, setter in effect, JSX usage)
    - `grep "nearestKaupunki" app/components/Etusivu.tsx` matches (import + usage in onCameraChanged callback)
    - `grep "debounceRef" app/components/Etusivu.tsx | wc -l` returns at least 3 lines (useRef declaration, clearTimeout, setTimeout)
    - `grep "ev.detail.center" app/components/Etusivu.tsx` matches in onCameraChanged
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>
    Etusivu tracks map center, debounces 3s, and updates weatherKaupunki state when the nearest city changes. The temperature display next to saa.temp shows the dynamic city name.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pass weatherKaupunki to AI fetch and update cache key</name>
  <files>app/components/Etusivu.tsx</files>
  <read_first>
    app/components/Etusivu.tsx — read the AI fetch useEffect (currently depends on [suosikitSizeAndIds], around lines ~227-255); read the saa weather fetch useEffect (lines ~215-220)
  </read_first>
  <action>
    Make the following targeted changes to the AI fetch useEffect in app/components/Etusivu.tsx:

    1. CACHE KEY: Change the sessionStorage key to include the city: `'saasuositus-' + new Date().toISOString().slice(0, 10) + '-' + weatherKaupunki + (suosikitIds.size > 0 ? '-' + suosikitIds.size : '')`. This ensures a new AI text is cached per city per day.

    2. FETCH URL — GET case: Change `fetch('/api/saasuositus', fetchOptions)` GET URL to include the city param: when using GET method, use `fetch('/api/saasuositus?kaupunki=' + encodeURIComponent(weatherKaupunki))` instead of `fetch('/api/saasuositus', { method: 'GET' })`. Restructure the conditional so that fetchOptions sets the method and the URL is always built with the kaupunki param for GET, or the body includes kaupunki for POST.

    Concretely: replace the current fetchOptions + fetch pattern with:
    - If suosikkiNimet.length > 0: `fetch('/api/saasuositus', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ suosikit: suosikkiNimet, kaupunki: weatherKaupunki }) })`
    - If suosikkiNimet.length === 0: `fetch('/api/saasuositus?kaupunki=' + encodeURIComponent(weatherKaupunki))`

    3. DEPENDENCY ARRAY: Add `weatherKaupunki` to the useEffect dependency array alongside `suosikitSizeAndIds`: `}, [suosikitSizeAndIds, weatherKaupunki])`. Update the eslint-disable comment if present to reflect the new deps.

    4. SAA WEATHER FETCH (Etusivu's own Open Meteo fetch): This useEffect at lines ~215-220 fetches weather data for the saa state (temp + code for the emoji display). This fetch is SEPARATE from the AI text fetch and currently hardcodes Tampere coordinates. Leave this fetch unchanged — it is for the temperature number display only, and changing it would require mapping weatherKaupunki to coordinates here too. The AI route (Plan 03) handles weather fetching by city on the server side. Accept the tradeoff: the temperature number shown in the widget will always be Tampere temperature regardless of map position (the AI TEXT will be city-aware). This matches the scope defined in CONTEXT.md.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "weatherKaupunki" app/components/Etusivu.tsx` appears in the AI fetch useEffect body (in cache key, fetch URL/body, and dependency array)
    - `grep "suosikitSizeAndIds, weatherKaupunki" app/components/Etusivu.tsx` matches (dependency array)
    - `grep "kaupunki.*weatherKaupunki" app/components/Etusivu.tsx | wc -l` returns at least 2 lines (GET url and POST body)
    - `grep "encodeURIComponent(weatherKaupunki)" app/components/Etusivu.tsx` matches (GET path)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>
    AI fetch effect passes weatherKaupunki to /api/saasuositus — via ?kaupunki= in GET URL and body.kaupunki in POST. Cache key includes city name so Helsinki and Tampere recommendations are cached separately. Effect re-runs when city changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Map camera event → weatherKaupunki state | Client-side only; no external data crosses boundary |
| Etusivu → /api/saasuositus | kaupunki string (nearestKaupunki output) sent as URL param or POST body field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Denial of Service | Rapid map panning triggering AI calls | mitigate | 3s debounce (ref-based clearTimeout); only fires when city changes (setWeatherKaupunki uses prev-comparison); daily sessionStorage cache further reduces calls |
| T-10-04-02 | Tampering | nearestKaupunki output injected into fetch URL | accept | nearestKaupunki returns values from a static hardcoded list (SUOMI_KAUPUNGIT) — output is always one of 25 known Finnish city names; encodeURIComponent applied to GET param |
| T-10-04-SC | Tampering | No new npm installs | accept | No package manager invocations in this plan |
</threat_model>

<verification>
- TypeScript: `npx tsc --noEmit` — zero errors
- Source: `grep "WEATHER_CITY" app/components/Etusivu.tsx` — empty (removed)
- Source: `grep "nearestKaupunki" app/components/Etusivu.tsx` — present
- Source: `grep "debounceRef" app/components/Etusivu.tsx` — present
- Source: `grep "weatherKaupunki.*suosikitSizeAndIds\|suosikitSizeAndIds.*weatherKaupunki" app/components/Etusivu.tsx` — present in dependency array

Manual verification in browser:
1. Open http://localhost:3000 — AI widget shows 'Tampere' city label
2. Pan the map to the Helsinki area (approximately 60.17°N, 24.94°E)
3. Wait 3-4 seconds without further panning
4. AI widget city label updates to 'Helsinki' and new recommendation text loads
5. Pan back to Tampere area — after 3s city label reverts to 'Tampere'
</verification>

<success_criteria>
- Map-center debounce fires after 3s of no panning and calls nearestKaupunki
- weatherKaupunki state updates only when the nearest city actually changes
- AI widget city label (next to temperature) updates to reflect the nearest city
- /api/saasuositus is called with the correct kaupunki param when city changes
- Existing Tampere behavior is unchanged at app startup (weatherKaupunki initializes to 'Tampere')
- No additional AI calls fire when the map stays within the same city region
</success_criteria>

<output>
Create `.planning/phases/10-city-expansion/10-04-SUMMARY.md` when done
</output>
