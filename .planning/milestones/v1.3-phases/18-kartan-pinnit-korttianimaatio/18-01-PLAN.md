---
phase: 18-kartan-pinnit-korttianimaatio
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sportPins.ts
  - app/components/Etusivu.tsx
autonomous: true
requirements:
  - MAP-08

must_haves:
  truths:
    - "All map pins use #ef4444 red regardless of sport type"
    - "Inside every pin: white circle background + sport SVG icon with #374151 dark stroke"
    - "Cluster pins (used in plan 18-02) can be generated via clusterPinUrl(count)"
    - "Mini-card laji badge still uses per-sport color (unchanged)"
  artifacts:
    - path: "lib/sportPins.ts"
      provides: "Fixed-red pinUrl + new clusterPinUrl export"
      exports: ["pinUrl", "clusterPinUrl"]
      min_lines: 30
    - path: "app/components/Etusivu.tsx"
      provides: "Pin render using fixed red, color variable removed"
      contains: "pinUrl(p.laji)"
  key_links:
    - from: "app/components/Etusivu.tsx"
      to: "lib/sportPins.ts"
      via: "pinUrl(p.laji) — no color argument"
      pattern: "pinUrl\\(p\\.laji\\)"
---

<objective>
Update all map pins to use a unified red color (#ef4444) with sport-type differentiation via a dark-stroked SVG icon on a white circle background. Also add clusterPinUrl() for use in plan 18-02.

Purpose: MAP-08 — pins must be visually uniform in color so users perceive a coherent map instead of a rainbow of per-sport colors. Sport type is still communicated by icon, not color.

Output: Updated lib/sportPins.ts + one-line Etusivu.tsx change removing the per-sport color lookup.
</objective>

<execution_context>
@C:/Users/joona/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joona/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/PROJECT.md
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/ROADMAP.md
@C:/ClaudeCodeTestit/liikuntahakemisto/.planning/STATE.md

<interfaces>
From lib/sportPins.ts (current):
```typescript
// Current signature — color param will be removed:
export function pinUrl(color: string, laji: string): string

// New signature after this plan:
export function pinUrl(laji: string): string
export function clusterPinUrl(count: number): string
```

From app/components/Etusivu.tsx (lines 393-403, current pin render):
```typescript
const color = (lajiKonfig as Record<string, { color: string }>)[p.laji]?.color ?? '#6b7280'
// ...
<img src={pinUrl(color, p.laji)} ... />
```
After this plan becomes:
```typescript
<img src={pinUrl(p.laji)} ... />
```

From lib/lajit.ts (laji badge colors — UNCHANGED, still used in mini-card):
```typescript
export const lajiKonfig: Record<string, { label: string; color: string }>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite lib/sportPins.ts — fixed red, white circle, dark icon stroke, clusterPinUrl</name>
  <files>lib/sportPins.ts</files>
  <action>
Rewrite lib/sportPins.ts with these exact changes (per D-01, D-02, D-03, D-05, D-07):

1. Remove the `color` parameter from `pinUrl`. The function signature becomes `pinUrl(laji: string): string`. The teardrop path fill is always `#ef4444` (hardcoded, no parameter).

2. Inside the pin, before the sport icon SVG, add a white circle background: `<circle cx="14" cy="14" r="8" fill="white"/>`. This goes between the teardrop path and the icon SVG element.

3. Change the `g()` helper's stroke from `"white"` to `"#374151"`. This makes all sport icon strokes dark on the white background. The stroke-width stays at 2.5 and other attributes are unchanged.

4. Update the icon position/size: the `<svg x="9" y="9" width="10" height="10">` can stay as-is since the white circle at r=8 provides a consistent 16px backdrop and the icon renders within it. If the icon viewBox at 10×10 renders too small visually, increase to `x="7" y="7" width="14" height="14"` — choose whichever gives better proportions at 28×38px overall pin size.

5. The fallback case (unknown laji) currently renders a plain circle. Replace with a generic fallback: `<svg x="7" y="7" width="14" height="14" viewBox="0 0 24 24"><g stroke="#374151" stroke-width="2.5" fill="none" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="4"/></g></svg>`.

6. Add a new export `clusterPinUrl(count: number): string` below `pinUrl`. It produces the same red teardrop SVG (same path, same `#ef4444` fill, same white circle `<circle cx="14" cy="14" r="8" fill="white"/>`), but instead of a sport icon it renders the count as `<text x="14" y="18" text-anchor="middle" font-size="8" font-weight="bold" fill="#374151" font-family="sans-serif">${count}</text>`. Clamp count display: if count > 9, render "9+" using `count > 9 ? '9+' : String(count)`.

Do NOT add any imports — the file is self-contained. Do NOT change the SVG viewBox "0 0 28 38" or the teardrop path data.
  </action>
  <verify>
    <automated>node -e "const {pinUrl,clusterPinUrl}=require('./lib/sportPins.ts'); const p=pinUrl('padel'); const c=clusterPinUrl(3); console.assert(p.includes('ef4444'),'pin must be red'); console.assert(p.includes('374151'),'icon must use dark stroke'); console.assert(p.includes('fill=\"white\"'),'white circle required'); console.assert(c.includes('ef4444'),'cluster must be red'); console.assert(c.includes('3'),'cluster must show count'); console.log('all assertions passed')"</automated>
  </verify>
  <done>pinUrl(laji) returns a data URI containing #ef4444 fill, a white circle, and #374151 icon stroke. clusterPinUrl(3) returns a data URI with count "3". Both functions exported.</done>
</task>

<task type="auto">
  <name>Task 2: Update Etusivu.tsx pin render — remove per-sport color lookup</name>
  <files>app/components/Etusivu.tsx</files>
  <action>
In app/components/Etusivu.tsx, inside the `paikatKartalla.map(p => { ... })` render loop (around lines 393–420):

1. Remove the line `const color = (lajiKonfig as Record<string, { color: string }>)[p.laji]?.color ?? '#6b7280'` — this variable is no longer needed for pins. (lajiKonfig is still imported and still used in mini-card laji badge; only this per-pin color lookup is removed.)

2. Update the pinUrl call: change `pinUrl(color, p.laji)` to `pinUrl(p.laji)`.

No other changes in this task. The laji badge in the mini-card (the `<span style={{ backgroundColor: lajiKonfig[p.laji]?.color }}` block) stays unchanged — per-sport colors remain in badges.

The `import { pinUrl } from '@/lib/sportPins'` line at the top of the file stays as-is; no new imports needed for this task (clusterPinUrl will be imported in plan 18-02).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | head -20</automated>
  </verify>
  <done>TypeScript compiles without errors. pinUrl is called with one argument (laji only). The `color` variable inside the map loop is gone. Mini-card laji badge still uses lajiKonfig color.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG data URI generation | Client-side SVG string concatenation — laji value comes from DB via server-fetched paikat prop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-18-01 | Tampering | pinUrl laji param | accept | laji comes from server-fetched Supabase data (read-only RLS), not user input; SVG is rendered as data URI in img src, not innerHTML |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. Run `npx tsc --noEmit` — zero errors.
2. Start dev server (`npm run dev`), open map view, zoom in to level 16+. Confirm all pins are red (#ef4444) not multi-colored.
3. Zoom in further — confirm mini-card laji badge still shows per-sport color (unchanged).
4. Visually confirm white circle + dark icon visible inside each pin at default zoom.
</verification>

<success_criteria>
- lib/sportPins.ts: pinUrl(laji) exported (no color param), clusterPinUrl(count) exported
- All pin teardrops render #ef4444 red
- Sport icon stroke is #374151 (dark) on white circle background
- TypeScript compiles clean
- Mini-card sport badges retain per-sport colors
</success_criteria>

<output>
Create `.planning/phases/18-kartan-pinnit-korttianimaatio/18-01-SUMMARY.md` when done.
</output>
