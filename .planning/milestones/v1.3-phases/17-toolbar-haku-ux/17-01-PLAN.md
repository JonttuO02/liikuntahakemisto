---
phase: 17-toolbar-haku-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Etusivu.tsx
autonomous: true
requirements:
  - UI-17
  - UI-18

must_haves:
  truths:
    - "There is exactly one button that opens the search field and filter panel together; no standalone SlidersHorizontal filter button remains in the toolbar"
    - "A separate LayoutList button in the same glass pill opens the search overlay without autofocusing the search input, giving a list-browse mode"
    - "The Search icon button opens the same overlay WITH autoFocus on the search input, giving a type-to-search mode"
    - "Tapping either button again when the overlay is open closes it (true toggle)"
    - "The map pin filter and the search overlay card list both react to the same sport filter state (searchLaji); changing sport in the overlay immediately changes which pins show on the map"
    - "The active-filter visual on the Search button appears when searchLaji is not 'Kaikki' OR any of searchHinta/searchAukinyt/searchKaupunki is active"
  artifacts:
    - path: "app/components/Etusivu.tsx"
      provides: "Refactored toolbar + unified filter state"
      contains: "searchFocused"
  key_links:
    - from: "paikatKartalla useMemo"
      to: "searchLaji state"
      via: "direct filter condition"
      pattern: "searchLaji === 'Kaikki'"
    - from: "Search toolbar button"
      to: "toggleSearch(true)"
      via: "onClick handler"
      pattern: "toggleSearch\\(true\\)"
    - from: "LayoutList toolbar button"
      to: "toggleSearch(false)"
      via: "onClick handler"
      pattern: "toggleSearch\\(false\\)"
    - from: "search input"
      to: "searchFocused state"
      via: "autoFocus prop"
      pattern: "autoFocus=\\{searchFocused\\}"
---

<objective>
Refactor the Etusivu toolbar from two separate buttons (SlidersHorizontal + Search) to a simplified two-icon glass pill (Search + LayoutList) backed by a unified sport filter state.

Purpose: Requirements UI-17 and UI-18 — users need one unified search+filter button and one independent list-toggle button; the parallel aktiivinen/searchLaji filter states cause map and list to be out of sync.

Output: app/components/Etusivu.tsx with removed states (aktiivinen, leftOpen, filterOpen, suodatettu), new searchFocused boolean state, paikatKartalla driven by searchLaji, simplified toolbar JSX with active-state indicators.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-toolbar-haku-ux/17-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from app/components/Etusivu.tsx. -->

States being REMOVED (lines 101, 104, 106, 301–303):
  const [aktiivinen, setAktiivinen] = useState('Kaikki')   // line 101
  const [leftOpen, setLeftOpen]     = useState(false)      // line 104
  const [filterOpen, setFilterOpen] = useState(false)      // line 106
  const suodatettu = useMemo(...)                           // lines 301-303, driven by aktiivinen

States being ADDED (new, after line 123):
  const [searchFocused, setSearchFocused] = useState(false)

States remaining (unchanged):
  const [searchOpen, setSearchOpen]   = useState(false)    // line 118
  const [searchLaji, setSearchLaji]   = useState('Kaikki') // line 120

Function being CHANGED:
  toggleSearch() at line 165 — currently no params, will accept focused: boolean

useMemo being CHANGED:
  paikatKartalla at line 311 — currently filters via suodatettu; must filter via searchLaji directly

JSX REMOVED:
  - anyOverlayOpen derived value (line 320) — remove leftOpen/filterOpen from it
  - Left toolbar contents lines 460–547: the entire AnimatePresence expansion + filter dropdown
  - Right toolbar: lines 575–581 (the "Haku" shortcut button in rightOpen expansion)

lucide-react icons needed:
  - LayoutList (new import — add to line 7 import list)
  - Search (already imported)
  - SlidersHorizontal (remove from import once no longer used)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove dead states and unify the sport filter</name>
  <files>app/components/Etusivu.tsx</files>

  <read_first>
    app/components/Etusivu.tsx — read the full file before any edits; all line references below are from this file at the time of reading
  </read_first>

  <action>
    Make the following surgical edits to app/components/Etusivu.tsx.

    1. IMPORTS (line 7): Remove SlidersHorizontal from the lucide-react import. Add LayoutList. Result should include: { X, MapPin, Moon, Sun, Locate, Search, Heart, MoreHorizontal, LogOut, User, LayoutList }

    2. REMOVE STATE — aktiivinen (line 101): Delete the line:
       const [aktiivinen, setAktiivinen] = useState('Kaikki')

    3. REMOVE STATE — leftOpen (line 104): Delete the line:
       const [leftOpen, setLeftOpen] = useState(false)

    4. REMOVE STATE — filterOpen (line 106): Delete the line:
       const [filterOpen, setFilterOpen] = useState(false)

    5. ADD STATE — searchFocused: After the searchKaupunki state line (line 123), add:
       const [searchFocused, setSearchFocused] = useState(false)

    6. REMOVE derived value — anyOverlayOpen (line 320): Delete the line:
       const anyOverlayOpen = leftOpen || rightOpen || filterOpen
       Replace with:
       const anyOverlayOpen = rightOpen

    7. REMOVE suodatettu useMemo (lines 301–303): Delete the entire useMemo block:
       const suodatettu = useMemo(
         () => paikat.filter(p => aktiivinen === 'Kaikki' || p.laji.toLowerCase() === aktiivinen.toLowerCase()),
         [paikat, aktiivinen]
       )

    8. UPDATE paikatKartalla useMemo (lines 311–318): Change its filter source from suodatettu to a direct filter on paikat. Replace the useMemo body so that it reads:
       const paikatKartalla = useMemo(
         () => paikat.filter(
           (p): p is Liikuntapaikka & { latitude: number; longitude: number } =>
             (searchLaji === 'Kaikki' || p.laji.toLowerCase() === searchLaji.toLowerCase()) &&
             p.latitude != null && p.longitude != null &&
             lajitKartalla.has(p.laji.toLowerCase())
         ),
         [paikat, searchLaji, lajitKartalla]
       )

    9. UPDATE closeOverlays (lines 159–163): Remove setLeftOpen(false) and setFilterOpen(false) from the function body. The function should only call setRightOpen(false).

    10. UPDATE toggleSearch (lines 165–172): Change the function signature to accept a focused parameter and set searchFocused accordingly. New implementation:
        function toggleSearch(focused: boolean) {
          if (searchOpen) { setSearchOpen(false); return }
          closeOverlays()
          setValittu(null)
          setSearchHaku('')
          setSearchFocused(focused)
          if (sheetPhase === 'open') setSheetPhase('sliding')
          setSearchOpen(true)
        }

    10a. UPDATE bare onClick references: After changing the signature, search for any remaining bare onClick={toggleSearch} references in the file (the old Search button at line ~475 inside the left toolbar pill is the known site). Change each bare reference from onClick={toggleSearch} to onClick={() => toggleSearch(true)}. There must be zero bare onClick={toggleSearch} references remaining after this step.

    After this task the file must compile without TypeScript errors (run: npx tsc --noEmit --project tsconfig.json).
  </action>

  <verify>
    <automated>cd /c/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - Etusivu.tsx does NOT contain the string `aktiivinen` anywhere
    - Etusivu.tsx does NOT contain the string `leftOpen` anywhere
    - Etusivu.tsx does NOT contain the string `filterOpen` anywhere
    - Etusivu.tsx does NOT contain the string `suodatettu` anywhere
    - Etusivu.tsx CONTAINS `searchFocused` as a useState declaration
    - Etusivu.tsx CONTAINS `LayoutList` in the lucide-react import line
    - Etusivu.tsx does NOT contain `SlidersHorizontal` anywhere
    - The paikatKartalla useMemo dependency array contains `searchLaji` (not `suodatettu`)
    - The toggleSearch function signature is `function toggleSearch(focused: boolean)`
    - Etusivu.tsx does NOT contain a bare `onClick={toggleSearch}` reference — all calls use `toggleSearch(true)` or `toggleSearch(false)` or `() => toggleSearch(true/false)` wrapper form
    - `npx tsc --noEmit` exits with code 0 (no type errors)
  </acceptance_criteria>

  <done>All dead states removed, suodatettu replaced, paikatKartalla uses searchLaji, toggleSearch accepts a focused boolean, no bare onClick={toggleSearch} references remain — TypeScript compiles clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Simplify the toolbar JSX and fix autoFocus</name>
  <files>app/components/Etusivu.tsx</files>

  <read_first>
    app/components/Etusivu.tsx — re-read after Task 1 changes are applied to see the updated line numbers; focus on: (a) the left toolbar div block (currently lines ~451–547), (b) the right toolbar rightOpen expansion block (lines ~565–631), (c) the backdrop div that references anyOverlayOpen (~449), (d) the search input autoFocus attribute (~765)
  </read_first>

  <action>
    Make the following surgical edits to app/components/Etusivu.tsx (read the file first to confirm current line numbers after Task 1 changes).

    1. LEFT TOOLBAR — replace the entire contents of the motion.div inside the left toolbar fixed div (the pill with layout animation). The new pill contains exactly two motion.button elements separated by a thin divider. The active-filter state (isFilterActive) is true when searchLaji is not 'Kaikki' OR searchHinta is not null OR searchAukinyt is true OR searchKaupunki is not 'Kaikki'.

       Replace everything from the opening `<motion.div layout` of the left toolbar pill down through the closing `</AnimatePresence>` of the filterOpen dropdown (inclusive) with this structure:

       Compute isFilterActive as a const above the JSX return (after the existing useMemos, before the return statement):
         const isFilterActive = searchLaji !== 'Kaikki' || searchHinta !== null || searchAukinyt || searchKaupunki !== 'Kaikki'

       The new toolbar pill JSX (replacing the old motion.div layout block and the filter dropdown AnimatePresence below it):
         <div className="glass rounded-full flex items-center" style={{ height: 40 }}>
           <motion.button
             whileTap={{ scale: 0.95 }}
             onClick={() => toggleSearch(true)}
             className="w-10 h-10 shrink-0 flex items-center justify-center [transition:color_150ms_ease] relative"
             style={{ color: isFilterActive || searchOpen ? '#111111' : 'rgba(17,17,17,0.7)' }}
             aria-label="Haku ja filtterit"
           >
             <Search className="w-4 h-4" />
             {isFilterActive && (
               <span
                 className="absolute bottom-1.5 left-1/2 -translate-x-1/2 w-1 h-1 rounded-full bg-[#111111]"
                 aria-hidden="true"
               />
             )}
           </motion.button>
           <div className="w-px h-4 bg-[rgba(0,0,0,0.1)]" />
           <motion.button
             whileTap={{ scale: 0.95 }}
             onClick={() => toggleSearch(false)}
             className="w-10 h-10 shrink-0 flex items-center justify-center [transition:color_150ms_ease]"
             style={{ color: searchOpen && !searchFocused ? '#111111' : 'rgba(17,17,17,0.7)' }}
             aria-label="Näytä lista"
           >
             <LayoutList className="w-4 h-4" />
           </motion.button>
         </div>

       Note: remove the `motion.div layout` wrapper around the pill — a static div is sufficient now that there is no expand/collapse animation. Also remove the filter dropdown AnimatePresence block that appeared below the left toolbar div; it is no longer needed.

    2. RIGHT TOOLBAR — remove the "Haku" shortcut button from the rightOpen expansion. The block to remove is:
         <button
           onClick={toggleSearch}
           className="flex items-center gap-1.5 px-3 h-8 rounded-full glass-btn text-sm font-bold text-[rgba(17,17,17,0.7)] hover:text-[#111111] [transition:color_150ms_ease]"
         >
           <Search className="w-3.5 h-3.5" />
           Haku
         </button>
       Delete only this button element (the one with the Search icon and "Haku" label); leave all other buttons (Profiili, Suosikit, Kirjaudu/Kirjaudu ulos) intact. Also update the remaining calls to toggleSearch in the right toolbar (there should be none after deleting this button).

    3. SEARCH INPUT autoFocus — find the search input element (it has `autoFocus` as a bare attribute on line ~765 in the original, may have shifted). Replace the bare `autoFocus` attribute with the conditional expression:
       autoFocus={searchFocused}

    After all edits, run the dev build to confirm no runtime errors.
  </action>

  <verify>
    <automated>cd /c/ClaudeCodeTestit/liikuntahakemisto && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - Etusivu.tsx CONTAINS the string `isFilterActive` as a const declaration
    - Etusivu.tsx CONTAINS `toggleSearch(true)` (Search button onClick)
    - Etusivu.tsx CONTAINS `toggleSearch(false)` (LayoutList button onClick)
    - Etusivu.tsx CONTAINS `autoFocus={searchFocused}` (conditional autoFocus)
    - Etusivu.tsx does NOT contain `autoFocus` as a bare (unconditional) attribute on the search input
    - Etusivu.tsx does NOT contain the string `SlidersHorizontal` (removed in Task 1 and confirmed absent here)
    - Etusivu.tsx does NOT contain a `<button` with the text content "Haku" inside the rightOpen block
    - Etusivu.tsx does NOT contain `motion.div layout` inside the left toolbar div (the pill is now a plain div)
    - Etusivu.tsx does NOT contain `filterOpen` anywhere
    - `npx tsc --noEmit` exits with code 0
    - The left toolbar glass pill renders exactly two icon buttons (Search + LayoutList) with a thin divider between them — confirmed by inspecting the JSX: the pill div contains exactly two `motion.button` elements and one divider div, with no AnimatePresence wrapper
  </acceptance_criteria>

  <done>Toolbar replaced with two-icon pill, autoFocus is conditional on searchFocused, "Haku" shortcut removed from right toolbar — TypeScript compiles clean. JSX pill confirmed to contain exactly 2 motion.button elements and 1 divider div with no AnimatePresence wrapper (grep 'motion.button' in the left toolbar block to verify).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI state → map filter | searchLaji is client-side state; it only affects which pins render client-side, no server trust boundary |
| User input → search field | searchHaku from uncontrolled input — already sanitized by being used only as a .toLowerCase().includes() comparator against local data, not sent to any API in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | toggleSearch function | accept | Function is client-only state mutation with no server call; no trust boundary crossed |
| T-17-02 | Information Disclosure | searchFocused state | accept | Boolean state, no PII, client-only; no disclosure risk |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — LayoutList is already in lucide-react (existing dependency) |
</threat_model>

<verification>
**Note on SC-3 vs. CONTEXT.md D-01:** Roadmap SC-3 says the list-toggle button "does not also trigger search or filters." CONTEXT.md D-01 (locked decision) defines the list-toggle as opening the same search overlay without autoFocus — i.e., it reuses the overlay as the list surface but does NOT independently manage filter state. The LayoutList button does NOT set any filter — it only opens the existing overlay in browse mode (searchFocused=false). SC-3 should be read as "does not manage its own search/filter state independently"; D-01 supersedes SC-3's literal wording and is the canonical implementation decision.

After both tasks complete, verify the full phase success criteria:

1. **Single unified button:** `grep -c "SlidersHorizontal" app/components/Etusivu.tsx` returns 0. The left toolbar contains exactly one Search icon button and one LayoutList icon button.

2. **Unified filter state:** `grep "paikatKartalla" app/components/Etusivu.tsx` — the useMemo shows `searchLaji` in its dependency array and filter condition, not `suodatettu` or `aktiivinen`.

3. **Separate list-toggle:** `grep "toggleSearch(false)" app/components/Etusivu.tsx` returns 1 match (the LayoutList button); `grep "toggleSearch(true)" app/components/Etusivu.tsx` returns 1 match (the Search button).

4. **autoFocus conditional:** `grep "autoFocus" app/components/Etusivu.tsx` returns exactly 1 match containing `autoFocus={searchFocused}`.

5. **TypeScript clean:** `npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
- app/components/Etusivu.tsx has no references to aktiivinen, leftOpen, filterOpen, or suodatettu
- The left toolbar pill has exactly two icon buttons: Search (calls toggleSearch(true)) and LayoutList (calls toggleSearch(false))
- paikatKartalla filters using searchLaji directly — changing the sport filter in the overlay immediately changes visible map pins
- The search input uses autoFocus={searchFocused}: true when opened via Search button, false when opened via LayoutList button
- The "Haku" link is removed from the right toolbar's MoreHorizontal expansion
- npx tsc --noEmit passes with exit code 0
</success_criteria>

<output>
Create `.planning/phases/17-toolbar-haku-ux/17-01-SUMMARY.md` when done
</output>
